---
phase: quick-260505-kh6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/plan-phase.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "plan-phase.md contains no bare gsd-tools executable invocations"
    - "All gsd-tools invocations use full node path"
  artifacts:
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Fixed workflow with full node path invocation"
  key_links:
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "node invocation"
      pattern: 'node.*gsd-tools\.cjs'
---

<objective>
Commit the already-completed fix for bare gsd-tools invocation in plan-phase.md.

Purpose: The bare `gsd-tools` call at line 1427 of plan-phase.md would fail on systems where gsd-tools is not on PATH. It has been replaced with `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs"`. This plan captures that fix in a commit.

Output: Git commit with the fixed plan-phase.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify fix and commit</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
Verify the fix is in place by confirming:
1. No bare `gsd-tools gap-analysis` invocation exists in plan-phase.md (grep to confirm)
2. The full node path invocation `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" gap-analysis` exists

Then stage and commit the file with message: "fix(plan-phase): replace bare gsd-tools invocation with full node path"
  </action>
  <verify>
    <automated>grep -n 'gsd-tools gap-analysis' get-shit-done/workflows/plan-phase.md | grep -v 'node ' | grep -v '^#' | wc -l | grep -q '^0$' && echo "PASS: no bare invocations" || echo "FAIL: bare invocations found"</automated>
  </verify>
  <done>plan-phase.md has no bare gsd-tools executable invocations; change is committed to git</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell PATH | gsd-tools.cjs must be invoked via explicit node path, not PATH-dependent bare name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kh6-01 | Tampering | plan-phase.md shell invocation | mitigate | Use full `node "$HOME/.claude/..."` path — already applied |
</threat_model>

<verification>
grep -c 'node.*gsd-tools\.cjs.*gap-analysis' get-shit-done/workflows/plan-phase.md
# Expected: 1 (the fixed invocation)

grep -n 'gsd-tools gap-analysis' get-shit-done/workflows/plan-phase.md | grep -v 'node '
# Expected: no output (no bare invocations remain outside prose/comments)
</verification>

<success_criteria>
- plan-phase.md contains `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" gap-analysis` at the formerly-bare invocation site
- No bare `gsd-tools gap-analysis` executable call remains in any .md file under commands/, agents/, or get-shit-done/workflows/
- Change is committed to git
</success_criteria>

<output>
After completion, create `.planning/quick/260505-kh6-fix-bare-gsd-tools-invocations-missing-f/260505-kh6-SUMMARY.md`
</output>
