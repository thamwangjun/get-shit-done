---
phase: 260531-mg7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/execute-phase.md]
autonomous: true
requirements: [QUICK-mg7]

must_haves:
  truths:
    - "The execute-phase.md execution_context block lazy-loads summary.md and tdd.md via @-ref instead of eager eta include"
    - "checkpoints.md, execute-plan.md, worktree-path-safety.md, executor-examples.md inclusion style is unchanged"
    - "reference_usage block documents when to consult summary.md and tdd.md, matching existing prose style"
    - "eta-template-syntax.test.cjs and agent-frontmatter tests still pass (no {%~ introduced, frontmatter untouched)"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Updated execution_context and reference_usage blocks"
      contains: "@~/.claude/get-shit-done/templates/summary.md"
  key_links:
    - from: "execution_context block"
      to: "reference_usage block"
      via: "@-ref'd files documented with consult-when guidance"
      pattern: "summary.md.*tdd.md"
---

<objective>
Convert two eager eta includes (`summary.md`, `tdd.md`) in the `<execution_context>` block of `execute-phase.md` to lazy-load `@~/.claude/...` @-ref notation, and extend the `<reference_usage>` block with "when to consult" guidance for both files.

Purpose: Continue the fork's established pattern (precedent at line 616: `@~/.claude/get-shit-done/references/checkpoints.md`) of switching reference/template includes to lazy @-refs, reducing eager context load while preserving discoverability via explicit consult-when guidance.

Output: Modified `get-shit-done/workflows/execute-phase.md`.
</objective>

<context>
@get-shit-done/workflows/execute-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert summary.md/tdd.md includes to @-refs and extend reference_usage</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>
  Read get-shit-done/workflows/execute-phase.md lines ~613-624 to confirm the current
  `<execution_context>` and `<reference_usage>` block contents before editing.

  Current execution_context block (around lines 613-620):
  ```
  <execution_context>
  <%~ include('get-shit-done/workflows/execute-plan.md') %>
  <%~ include('get-shit-done/templates/summary.md') %>
  @~/.claude/get-shit-done/references/checkpoints.md
  <%~ include('get-shit-done/references/tdd.md') %>
  <%~ include('get-shit-done/references/worktree-path-safety.md') %>
  ${CONTEXT_WINDOW < 200000 ? '' : '@~/.claude/get-shit-done/references/executor-examples.md'}
  </execution_context>
  ```
  </read_first>
  <action>
  Make two edits to get-shit-done/workflows/execute-phase.md:

  EDIT 1 — In the `<execution_context>` block, replace exactly two eta include lines with
  @-ref notation, matching the existing checkpoints.md @-ref style on the adjacent line:
  - Replace `<%~ include('get-shit-done/templates/summary.md') %>` with `@~/.claude/get-shit-done/templates/summary.md`
  - Replace `<%~ include('get-shit-done/references/tdd.md') %>` with `@~/.claude/get-shit-done/references/tdd.md`
  Leave the execute-plan.md, worktree-path-safety.md, and executor-examples.md lines UNCHANGED.

  EDIT 2 — Extend the `<reference_usage>` block (currently documents only checkpoints.md)
  with one consult-when sentence per newly @-ref'd file, in affirmative framing matching the
  existing checkpoints.md prose:
  - summary.md: consult it when writing the phase/plan SUMMARY.md to match the required
    structure and dependency-graph frontmatter (requires/provides, subsystem, tags, etc.).
  - tdd.md: consult it when a plan task is TDD-flagged / behavior-adding, to follow the
    red-green-refactor cycle and judge when TDD improves quality vs. when to skip it.

  Keep the file frontmatter untouched. Use affirmative/positive framing per fork standards.
  Introduce no `{%~` syntax (banned by eta-template-syntax.test.cjs).
  </action>
  <verify>
    <automated>command grep -q '@~/.claude/get-shit-done/templates/summary.md' get-shit-done/workflows/execute-phase.md && command grep -q '@~/.claude/get-shit-done/references/tdd.md' get-shit-done/workflows/execute-phase.md && ! command grep -qE "include\('get-shit-done/(templates/summary|references/tdd)\.md'\)" get-shit-done/workflows/execute-phase.md && node --test tests/eta-template-syntax.test.cjs tests/agent-frontmatter.test.cjs</automated>
  </verify>
  <done>
  The execution_context block lazy-loads summary.md and tdd.md via @~/.claude/... @-refs;
  no eta include remains for those two files; execute-plan.md/worktree-path-safety.md/
  executor-examples.md unchanged; reference_usage documents consult-when guidance for both
  new @-ref'd files; eta-template-syntax and agent-frontmatter tests pass; frontmatter untouched.
  </done>
</task>

</tasks>

<verification>
- `command grep -nE "include\('get-shit-done/(templates/summary|references/tdd)\.md'\)" get-shit-done/workflows/execute-phase.md` returns no matches.
- `command grep -n '@~/.claude/get-shit-done/templates/summary.md' get-shit-done/workflows/execute-phase.md` and the tdd.md equivalent each return one match inside the execution_context block.
- `node --test tests/eta-template-syntax.test.cjs tests/agent-frontmatter.test.cjs` passes.
</verification>

<success_criteria>
summary.md and tdd.md are lazy-loaded via @-ref in the execution_context block, the three other includes are untouched, reference_usage gives consult-when guidance for both new @-refs in affirmative framing, and fork tests pass.
</success_criteria>

<output>
Modified: get-shit-done/workflows/execute-phase.md
</output>
