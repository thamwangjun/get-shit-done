---
phase: quick-260609-43f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/no-issue-citations.test.cjs
  - commands/**/*.md
  - get-shit-done/workflows/*.md
  - agents/*.md
  - get-shit-done/references/*.md
  - get-shit-done/templates/**/*.md
autonomous: true
requirements: [QT-260609-43f]
must_haves:
  truths:
    - "A new permanent guard test runs under `npm test` and FAILS before cleanup, listing each offending file:line and matched citation text"
    - "After cleanup the guard test passes (green)"
    - "No issue/PR-number citations (#NNNN-shaped) remain in scoped .md files except allowlisted categories (hex colors, illustrative placeholders, heading markers)"
    - "Cleaned sentences read naturally — no double spaces, space-before-punctuation, empty (), or dangling connectors"
    - "agent-frontmatter and all other content tests still pass; frontmatter color: hex values are preserved exactly"
  artifacts:
    - path: "tests/no-issue-citations.test.cjs"
      provides: "Permanent guard test scanning five in-scope prompt dirs with explicit allowlist"
      contains: "test("
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "commands/, get-shit-done/workflows/, agents/, get-shit-done/references/, get-shit-done/templates/"
      via: "recursive .md scan with #NNN detection and allowlist exemption"
      pattern: "#\\[0-9\\]"
---

<objective>
Remove all issue/PR-number citations (e.g. `(#3668)`, `— #686`, `, #2282`, `by #2790`) from prompt
content `.md` files across five in-scope directories, and add a PERMANENT guard test that blocks
reintroduction. Test-first: write the guard, watch it fail with a real citation list, drive cleanup
from that list, then watch it pass.

Purpose: Citations to upstream issue/PR numbers leak repository-internal noise into user-facing
prompt content. A permanent guard prevents regression.

Output: New `tests/no-issue-citations.test.cjs` (permanent) + citation-cleaned `.md` files.
</objective>

<context>
!`cat .planning/quick/260609-43f-remove-issue-pr-number-citations-from-pr/260609-43f-CONTEXT.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the permanent guard test (RED)</name>
  <files>tests/no-issue-citations.test.cjs</files>
  <read_first>
    - tests/helpers.cjs (existing test conventions — createTempDir, assertion helpers)
    - agents/gsd-verifier.md (preserved placeholder examples: `issue #123`, `PR #123`, `#123`)
    - get-shit-done/templates/codebase/conventions.md (preserved placeholder `(issue #123)`)
    - commands/gsd/graphify.md (preserved hex example `#22c55e`)
  </read_first>
  <action>
    Create `tests/no-issue-citations.test.cjs` as a CommonJS file using Node's built-in
    `node:test` `test()` and `node:assert`. No external framework. Per CONTEXT.md decision
    (PERMANENT GUARD), the test must run under `npm test` and block citation reintroduction.

    Scan ONLY `.md` files, recursively, in exactly these five scope roots relative to repo root:
    `commands/`, `get-shit-done/workflows/`, `agents/`, `get-shit-done/references/`,
    `get-shit-done/templates/`. Resolve roots from the repo root (the test file lives in `tests/`,
    so go up one level). Skip any non-`.md` file.

    Detection: match `#` immediately followed by one or more digits (`/#\d+/`) on each line.

    Allowlist (per CONTEXT.md EXPLICIT ALLOWLIST decision — exempt, do NOT flag):
    1. Hex color codes — `#` followed by 3, 4, 6, or 8 hex digits where the token is a CSS/color
       value (e.g. `#22c55e`, `#8B5CF6`, `#f0fdf4`, `#fff`, `#e5e5e5`). Includes agent frontmatter
       `color:` fields. A pragmatic rule: if the matched token after `#` is composed solely of hex
       digits AND has length 3/4/6/8 AND contains at least one hex letter (a-f) OR appears in a
       `color:` frontmatter line OR a CSS-context line, treat as hex. (Pure-digit 6-length like
       `#123456` is rare here; bias the allowlist toward the confirmed examples and any token
       matching `/^#[0-9a-fA-F]{3,8}$/` that contains a hex letter.)
    2. Illustrative placeholders — the literal example tokens `#123` and `#45` used to show
       reference FORMAT (e.g. `issue #123`, `PR #123`, `(issue #123)`, inbox example `#45`).
       Exempt the bare placeholder numbers `123` and `45`.
    3. Markdown heading `#` markers and numbered list markers — these are `#` NOT followed by a
       digit (heading `## Foo`) or a leading list number, so the `/#\d+/` detector naturally
       ignores them. No special handling needed beyond the detector shape; only a TRAILING
       `(#NNNN)` on a heading line counts as a citation.

    Choose the allowlist data structure at your discretion (inline Set or small data block).

    On any non-allowlisted match, FAIL the test (single aggregated assertion at the end) with a
    clear message listing every offending `relativePath:lineNumber  ->  matchedText` and the full
    offending line. The message must enumerate each citation so Task 3 can be driven directly from
    it.

    Coverage note: coverage is measured only against `get-shit-done/bin/lib/*.cjs`, so this new
    `tests/*.test.cjs` file does not affect the 70% line-coverage gate.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | tee /tmp/43f-red.txt; command grep -q 'fail' /tmp/43f-red.txt</automated>
  </verify>
  <done>Test exists, runs under node --test, and FAILS initially with an enumerated list of real
  citations (file:line + matched text). Allowlisted hex/placeholder examples are NOT in the failure
  list.</done>
</task>

<task type="auto">
  <name>Task 2: Apply citation removals driven by the failure list, until green</name>
  <files>commands/**/*.md, get-shit-done/workflows/*.md, agents/*.md, get-shit-done/references/*.md, get-shit-done/templates/**/*.md</files>
  <read_first>
    - /tmp/43f-red.txt (the enumerated citation failure list from Task 1)
  </read_first>
  <action>
    Read the failure list from Task 1 and remove each enumerated citation from its `.md` file.
    Apply the removal rules and worked examples from CONTEXT.md `<specifics>`:
    - Trailing parenthetical: `fall back to global gsd-sdk (#3668)` -> `fall back to global gsd-sdk`
    - Em-dash / `see`-attached inside brackets: `deep agent nesting — see #686):` -> `deep agent nesting):`
    - Comma-attached inside brackets: `flat vs workstream, #2282):` -> `flat vs workstream):`
    - Inline mid-sentence `by/in/from #NNNN`: `folded into gsd-map-codebase flags by #2790.` -> `folded into gsd-map-codebase flags.`
    - Multi-number groups: `(#1916)(#2003)` and `(#2790, #3042)` -> remove the entire group.

    Cleanup after each removal (per CONTEXT.md): collapse double spaces to one, remove
    space-before-punctuation, remove empty `()`, remove dangling `—`/`,`/`see`/`by`/`in`/`from`
    connectors left orphaned by the deletion. LIGHT REPHRASE is allowed where a purely mechanical
    delete leaves an awkward or ungrammatical sentence — prioritize readability over zero-word-change
    (per CONTEXT.md "Sentence cleanup — LIGHT REPHRASE ALLOWED"). Per-line wording is Claude's
    discretion within that bound.

    Do NOT alter agent frontmatter `color:` hex values or any allowlisted hex/placeholder token —
    frontmatter must be preserved exactly (agent-frontmatter.test.cjs enforces this).

    Re-run the guard test after edits and iterate on remaining flagged lines until it passes green.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | tee /tmp/43f-green.txt; command grep -q 'pass' /tmp/43f-green.txt; command grep -qv 'fail' /tmp/43f-green.txt</automated>
  </verify>
  <done>Guard test passes (green). Remaining `command grep -rEn '#[0-9]+'` hits across scope are
  ONLY preserved/allowlisted categories. No double spaces, space-before-punctuation, empty (), or
  dangling connectors introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression check</name>
  <files>(no edits — verification only; fix-forward if a regression surfaces)</files>
  <action>
    Run the full test suite to confirm the new guard test passes alongside agent-frontmatter and
    all other content tests, and that no frontmatter or content assertion broke from the cleanup
    edits. If any test fails due to a citation removal touching an asserted string, fix-forward
    minimally (re-point the assertion or restore wording within the light-rephrase bound) — do not
    weaken the guard test.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt; command grep -Eq '(# fail 0|0 failing|tests .* 0 fail)' /tmp/gsd-test-output.txt || command grep -vq 'failing' /tmp/gsd-test-output.txt</automated>
  </verify>
  <done>`npm test` reports zero failures. Guard test, agent-frontmatter test, and all content tests
  pass. Frontmatter color hex values preserved exactly.</done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs` passes (was RED before Task 2).
- `command grep -rEn '#[0-9]+' commands/ get-shit-done/workflows/ agents/ get-shit-done/references/ get-shit-done/templates/ --include='*.md'` returns only allowlisted hex/placeholder hits.
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` reports zero failures.
</verification>

<success_criteria>
- New permanent guard test exists at `tests/no-issue-citations.test.cjs`, runs under `npm test`, failed RED before cleanup and passes green after.
- All real issue/PR-number citations removed from the five scoped directories' `.md` files.
- Preserved categories (hex colors incl. frontmatter `color:`, illustrative placeholders `#123`/`#45`, heading markers) untouched.
- Sentences read naturally; no orphaned punctuation/connectors.
- Full test suite green; agent frontmatter preserved exactly.
</success_criteria>

<output>
Cleanup applied in place across scoped `.md` files; new guard test committed. Create
`.planning/quick/260609-43f-remove-issue-pr-number-citations-from-pr/260609-43f-SUMMARY.md` when done.
</output>
