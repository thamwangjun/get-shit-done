---
phase: quick-260608-p8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
autonomous: true
requirements: []

must_haves:
  truths:
    - initialize step contains explicit error conditions for phase_found=false, plan_count=0, state_exists=false
    - initialize step states that plans within a wave execute sequentially when parallelization is false
    - auto-chain sync carries the ordering constraint "You MUST execute this bash block before any config reads"
    - schema_drift_gate timing instruction is present ("Run after execution completes but BEFORE verification marks success")
    - schema_drift_gate Parse JSON field list includes orms alongside unpushed_orms
    - codebase_drift_gate states "MUST fall through" and "phase is never failed by this gate"
    - HUMAN-UAT template includes a "## Gaps" section after Summary block
    - cross_ai_delegation includes a pre-execution dirty working tree check
    - discover_and_group_plans includes the full Execution Plan table template
    - stash step in post-wave hook includes the #3542 comment about refs/stash being shared across worktrees
    - failure_handling Quota entry includes the explicit sentinel string list
  artifacts:
    - path: get-shit-done/workflows/execute-phase.md
      provides: execute-phase workflow with restored fidelity
---

<objective>
Restore 10 confirmed fidelity losses in get-shit-done/workflows/execute-phase.md
identified by diff against commit 57a000b1.

Purpose: Compression in an earlier commit dropped operational instructions that change
model behavior. This plan restores the specific missing content while preserving
compression wins elsewhere.

Output: A patched execute-phase.md that has all essential and notable operational
instructions restored. No restoration of explanatory prose that was legitimately trimmed.
</objective>

<context>
Reference: git show 57a000b1:get-shit-done/workflows/execute-phase.md
Target: get-shit-done/workflows/execute-phase.md (current HEAD, 1003 lines)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore essential fidelity losses (items 1, 3, 4, 5)</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>get-shit-done/workflows/execute-phase.md</read_first>
  <action>
Read the file fully first. Apply four targeted edits — each one is a confirmed loss verified
against 57a000b1. Preserve surrounding compression-wins; restore only the missing instruction.

**Edit 1 — initialize error conditions + sequential parallelization note.**

In the initialize step (around line 113), after the two `- Planners consult...` lines and
BEFORE the `**Auto-chain sync (REQUIRED):**` line, insert:

```
**If `phase_found` is false:** Error — phase directory not found.
**If `plan_count` is 0:** Error — no plans found in phase.
**If `state_exists` is false but `.planning/` exists:** Offer reconstruct or continue.

When `parallelization` is false, plans within a wave execute sequentially.

```

**Edit 2 — schema_drift_gate: add timing instruction and restore `orms` field.**

Locate the schema_drift_gate step. Find this line:
```
Run post-execution schema drift detection:
```
Replace with:
```
**Run after execution completes but BEFORE verification marks success.**

Run post-execution schema drift detection:
```

Then find:
```
Parse for `drift_detected`, `blocking`, `schema_files`, `unpushed_orms`, `message`.
```
Replace with:
```
Parse JSON result for: `drift_detected`, `blocking`, `schema_files`, `orms`, `unpushed_orms`, `message`.
```

**Edit 3 — codebase_drift_gate: restore non-blocking contract language.**

Find the codebase_drift_gate step content:
```
Post-execution structural drift detection (non-blocking by contract). Load full step spec from `get-shit-done/workflows/execute-phase/steps/codebase-drift-gate.md`.
```
Replace with:
```
Post-execution structural drift detection (#2003). Non-blocking by contract:
any internal error here MUST fall through to `verify_phase_goal`. The phase
is never failed by this gate.

Load and follow the full step spec from
`get-shit-done/workflows/execute-phase/steps/codebase-drift-gate.md` —
covers the SDK call, JSON contract, `warn` vs `auto-remap` branches, mapper
spawn template, and the two `workflow.drift_*` config keys.
```

**Edit 4 — HUMAN-UAT template: restore `## Gaps` section.**

In the verify_phase_goal step, find the Step A description that ends with:
```
...and a `## Summary` block (`total`, `passed`, `issues`, `pending`, `skipped`, `blocked`). These fields are required by `/gsd:progress` and `/gsd:audit-uat`. Commit: `test({phase_num}): persist human verification items as UAT`.
```
Replace with:
```
...and a `## Summary` block (`total`, `passed`, `issues`, `pending`, `skipped`, `blocked`), and a `## Gaps` section (empty at creation, populated by gap-closure runs). These fields are required by `/gsd:progress` and `/gsd:audit-uat`. Commit: `test({phase_num}): persist human verification items as UAT`.
```
  </action>
  <verify>
    <automated>grep -n "phase_found.*false\|plan_count.*0\|state_exists.*false" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "parallelization.*false.*sequentially\|plans within a wave.*sequential" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "BEFORE verification marks success" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n '"orms"' get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "MUST fall through\|phase is never failed" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "## Gaps" get-shit-done/workflows/execute-phase.md | head -5</automated>
  </verify>
  <done>
All six grep checks return at least one match. File is valid and untruncated (wc -l shows close to original line count for these additions, ~1020+ lines).
  </done>
</task>

<task type="auto">
  <name>Task 2: Restore notable fidelity losses (items 6, 7, 8, 9, 10)</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>get-shit-done/workflows/execute-phase.md</read_first>
  <action>
Read the file fully. Apply five targeted edits for the notable losses.

**Edit 5 — auto-chain sync: restore ordering constraint.**

Find:
```
**Auto-chain sync (REQUIRED):** If user invoked manually (no `--auto`), clear ephemeral chain flag:
```
Replace with:
```
**REQUIRED — Sync chain flag with intent.** If user invoked manually (no `--auto`), clear the ephemeral chain flag from any previous interrupted `--auto` chain. This prevents stale `_auto_chain_active: true` from causing unwanted auto-advance. This does NOT touch `workflow.auto_advance` (the user's persistent settings preference). You MUST execute this bash block before any config reads:
```

**Edit 6 — cross_ai_delegation: restore pre-execution dirty working tree check.**

In the cross_ai_delegation step, find the block starting with:
```
**For each cross-ai plan:** Extract objective+tasks from PLAN.md. Pipe to external command via stdin (never shell-interpolate). Capture result:
```
Insert BEFORE that line:
```
**Check for dirty working tree before each cross-AI execution:**
```bash
if ! git diff --quiet HEAD 2>/dev/null; then
  echo "WARNING: dirty working tree detected — the external AI command may produce uncommitted changes that conflict with existing modifications"
fi
```

```

**Edit 7 — discover_and_group_plans: restore Execution Plan table template.**

Find:
```
Report wave structure with objectives.
```
Replace with:
```
Report:
```
## Execution Plan

**Phase {X}: {Name}** — {total_plans} matching plans across {wave_count} wave(s)

{If WAVE_FILTER is set: `Wave filter active: executing only Wave {WAVE_FILTER}`.}

| Wave | Plans | What it builds |
|------|-------|----------------|
| 1 | 01-01, 01-02 | {from plan objectives, 3-8 words} |
| 2 | 01-03 | ... |
```
```

**Edit 8 — stash step: restore #3542 shared-refs/stash comment.**

In the post-wave hook validation step (step 7), find:
```
  if (! git diff --quiet || ! git diff --cached --quiet) && git stash push -u -m "gsd-post-wave-hook-$$" >/dev/null 2>&1; then STASHED=true; fi
```
Replace with:
```
  # Stash uncommitted changes under a named ref so we always pop (bare `git stash` strands them on hook/script failure). #3542: `refs/stash` is shared across worktrees, so this helper runs ONLY in the orchestrator's main checkout after all wave worktrees have been merged + removed; executors are forbidden from running any `git stash` subcommand.
  if (! git diff --quiet || ! git diff --cached --quiet) && git stash push -u -m "gsd-post-wave-hook-$$" >/dev/null 2>&1; then STASHED=true; fi
```

**Edit 9 — failure_handling: restore quota sentinel string list.**

Find in the `<failure_handling>` block:
```
- **Quota/rate-limit (#3095):** `gsd-sdk query agent.classify-failure` → `class: quota-exceeded`. Do not retry-now; wait-for-reset and resume.
```
Replace with:
```
- **Quota / rate-limit (any runtime — #3095):** Agent return body contains a sentinel like `usage limit`, `rate limit`, `429`, `too many requests`, `RESOURCE_EXHAUSTED`, `usage_limit_reached`. Route via `gsd-sdk query agent.classify-failure` → `class: "quota-exceeded"`. Do not offer retry-now; the right action is wait-for-reset and resume.
```
  </action>
  <verify>
    <automated>grep -n "You MUST execute this bash block before any config reads" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "dirty working tree detected\|git diff.*quiet HEAD" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "What it builds\|Execution Plan" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "3542\|refs/stash is shared across worktrees" get-shit-done/workflows/execute-phase.md | head -5</automated>
    <automated>grep -n "usage limit.*rate limit\|RESOURCE_EXHAUSTED\|usage_limit_reached" get-shit-done/workflows/execute-phase.md | head -5</automated>
  </verify>
  <done>
All five grep checks return at least one match. Commit the file with message: `fix(quick-260608-p8h): restore 10 confirmed fidelity losses in execute-phase.md`
  </done>
</task>

</tasks>

<verification>
After both tasks:
- npm test passes (no test file asserts execute-phase.md content directly, but overall suite must pass)
- grep confirms all 10 restored items are present
- wc -l shows the file grew by ~30-40 lines vs HEAD (all additions, no unintended deletions)
- git diff HEAD shows only additions in the expected locations
</verification>

<success_criteria>
All 10 confirmed fidelity losses from the diff analysis are restored in execute-phase.md:
1. initialize: phase_found/plan_count/state_exists error conditions + sequential parallelization note
2. (offer_next hallucination guard was already adequate in HEAD — no action needed)
3. schema_drift_gate: timing instruction + orms field in parse list
4. codebase_drift_gate: MUST fall through / phase is never failed language
5. HUMAN-UAT template: ## Gaps section
6. auto-chain sync: "before any config reads" ordering constraint
7. cross_ai_delegation: pre-execution dirty working tree check
8. discover_and_group_plans: full Execution Plan table template
9. stash step: #3542 refs/stash shared-across-worktrees comment
10. failure_handling: explicit quota sentinel string list
</success_criteria>
