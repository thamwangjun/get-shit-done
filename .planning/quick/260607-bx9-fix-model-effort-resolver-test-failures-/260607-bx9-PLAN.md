---
phase: quick-260607-bx9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/golden-effort-snapshot.json
  - tests/model-profiles.test.cjs
  - tests/parse-model-effort.test.cjs
  - tests/issue-2517-runtime-aware-profiles.test.cjs
  - tests/feat-57-install-translation.test.cjs
autonomous: true
requirements: [BX9-01]
must_haves:
  truths:
    - "The golden snapshot fixture matches live resolver output (all ~90 golden tests + TEST-01 pass)"
    - "All hardcoded sonnet;medium slot expectations match the authoritative catalog (most flip to sonnet;high)"
    - "All Codex TOML effort expectations match real resolver output (gsd-roadmapper balanced codex -> high; gsd-executor balanced codex -> high)"
    - "D-01: The catalog (sdk/shared/model-catalog.json) is the source of truth — tests conform to it; the catalog, core.cjs, and resolvers are NOT modified"
    - "D-02: feat-58 TEST-04 antipattern guard stays green — feat-57 assertions keep the safe structured form includes('model_reasoning_effort = \"...\"'), never bare includes('high')"
    - "The targeted cluster is fully green: model-profiles, parse-model-effort, feat-58-regression, feat-57-install-translation, issue-2517-runtime-aware-profiles"
  artifacts:
    - path: "tests/fixtures/golden-effort-snapshot.json"
      provides: "Regenerated golden snapshot matching post-catalog-edit resolver output"
    - path: "tests/model-profiles.test.cjs"
      provides: "Profile slot expectations conforming to the catalog"
    - path: "tests/parse-model-effort.test.cjs"
      provides: "_resolveAgentSlot expectation for gsd-executor balanced (sonnet;high)"
    - path: "tests/issue-2517-runtime-aware-profiles.test.cjs"
      provides: "Codex sonnet-tier effort expectation (high)"
    - path: "tests/feat-57-install-translation.test.cjs"
      provides: "Codex TOML bare-slot effort expectation (high)"
  key_links:
    - from: "tests/fixtures/golden-effort-snapshot.json"
      to: "sdk/shared/model-catalog.json"
      via: "scripts/gen-golden-effort-snapshot.mjs driving the live resolver"
      pattern: "resolveReasoningEffortInternal"
---

<objective>
Fix the model-effort resolver test cluster (~105 failing tests) by making the
tests/fixtures conform to the authoritative, hand-edited
`sdk/shared/model-catalog.json`. The catalog was updated so many balanced/budget
slots are now `sonnet;high` (previously `sonnet;medium`); the committed golden
snapshot and several hardcoded test expectations are stale.

Purpose: Restore the targeted test cluster to green WITHOUT touching the catalog
or any resolver logic. THE CATALOG IS CORRECT (per D-01) — tests follow it.

Output: Regenerated golden snapshot fixture + corrected hardcoded expectations in
4 test files. No production/catalog/resolver edits.

Ground truth confirmed during planning (live resolver output):
- gsd-executor balanced (claude slot) = `sonnet;high`
- gsd-verifier balanced = `sonnet;high`; gsd-planner budget = `sonnet;high`
- gsd-roadmapper balanced codex => model gpt-5.3-codex, effort `high`
- gsd-executor balanced codex => model gpt-5.3-codex, effort `high`
- gsd-verifier budget codex => gpt-5.4-mini, effort `null` (haiku omits)

Files that DO NOT need editing (verified zero failures): tests/codex-config.test.cjs,
tests/feat-53-config-sites-and-golden.test.cjs, tests/feat-53-unified-effort-resolver.test.cjs.
</objective>

<context>
@sdk/shared/model-catalog.json
@scripts/gen-golden-effort-snapshot.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the golden snapshot fixture and verify the diff</name>
  <files>tests/fixtures/golden-effort-snapshot.json</files>
  <read_first>
    - scripts/gen-golden-effort-snapshot.mjs (the generator — drives the live resolver over the agent × profile × runtime matrix and atomically rewrites the fixture)
  </read_first>
  <action>
    Run `npm run gen:golden-snapshot` (which executes
    `node scripts/gen-golden-effort-snapshot.mjs`) to rewrite
    tests/fixtures/golden-effort-snapshot.json from live resolver output. Per D-01,
    the catalog is the source of truth — do NOT hand-edit the fixture; only the
    generator writes it.

    After regen, run `git diff tests/fixtures/golden-effort-snapshot.json` and
    eyeball that ONLY `expectedEffort` tokens changed (medium -> high, or otherwise
    expected) and that no `expectedModel` value unexpectedly swapped. If any model
    value changed, STOP and report — that would signal a resolver/catalog issue
    outside this task's scope (per D-01, do not "fix" it by editing the fixture by
    hand). This fixes the ~90 `golden: <agent>/<profile>/<runtime>` tests and
    `TEST-01: static golden snapshot` in tests/feat-58-regression.test.cjs.
  </action>
  <verify>
    <automated>node --test tests/feat-58-regression.test.cjs 2>&1 | command grep -c "✖" | command grep -qx 0 && echo PASS || (node --test tests/feat-58-regression.test.cjs 2>&1 | command grep "✖"; false)</automated>
  </verify>
  <done>feat-58-regression.test.cjs has zero ✖ failures; `git diff` of the fixture shows only effort-token changes (no unexpected model swaps).</done>
</task>

<task type="auto">
  <name>Task 2: Update hardcoded slot expectations to match the catalog</name>
  <files>tests/model-profiles.test.cjs, tests/parse-model-effort.test.cjs</files>
  <read_first>
    - tests/model-profiles.test.cjs (lines 46-56, 86-98 — validModels list and getAgentToModelMapForProfile assertions)
    - tests/parse-model-effort.test.cjs (lines 114-125 — _resolveAgentSlot gsd-executor balanced)
    - sdk/shared/model-catalog.json (agents block — look up each agent's actual slot before editing)
  </read_first>
  <action>
    Update literal stale expectations. Do NOT blanket find/replace medium->high;
    each value below was verified against the catalog during planning (per D-01,
    the catalog wins).

    In tests/model-profiles.test.cjs:
    - Line ~47 (`all profile values are valid model aliases`): add `'sonnet;high'`
      to the `validModels` array so it reads
      `['opus', 'sonnet', 'haiku', 'opus;low', 'sonnet;medium', 'sonnet;high']`.
      Keep `'sonnet;medium'` in the list (other agents may still use it).
    - Line ~91 (`returns correct models for balanced profile`):
      change `map['gsd-verifier']` expected from `'sonnet;medium'` to `'sonnet;high'`
      (catalog gsd-verifier.balanced = sonnet;high). Leave
      `map['gsd-planner']` = `'opus;low'` and `map['gsd-codebase-mapper']` = `'haiku'`
      unchanged (still correct).
    - Line ~96 (`returns correct models for budget profile`):
      change `map['gsd-planner']` expected from `'sonnet;medium'` to `'sonnet;high'`
      (catalog gsd-planner.budget = sonnet;high). Leave
      `map['gsd-phase-researcher']` = `'haiku'` unchanged.

    In tests/parse-model-effort.test.cjs:
    - Line ~124 (`_resolveAgentSlot returns a single slot string for gsd-executor
      on balanced`): change expected from `'sonnet;medium'` to `'sonnet;high'`
      (catalog gsd-executor.balanced = sonnet;high). Update the trailing comment if
      it references the old value.
  </action>
  <verify>
    <automated>node --test tests/model-profiles.test.cjs tests/parse-model-effort.test.cjs 2>&1 | command grep -c "✖" | command grep -qx 0 && echo PASS || (node --test tests/model-profiles.test.cjs tests/parse-model-effort.test.cjs 2>&1 | command grep "✖"; false)</automated>
  </verify>
  <done>model-profiles.test.cjs and parse-model-effort.test.cjs both report zero ✖ failures; every changed expectation matches its catalog slot.</done>
</task>

<task type="auto">
  <name>Task 3: Update Codex TOML effort expectations to real resolver output</name>
  <files>tests/issue-2517-runtime-aware-profiles.test.cjs, tests/feat-57-install-translation.test.cjs</files>
  <read_first>
    - tests/issue-2517-runtime-aware-profiles.test.cjs (lines 169-173 — sonnet tier codex effort)
    - tests/feat-57-install-translation.test.cjs (lines 195-208 — bare opus/sonnet slot TOML emit)
    - sdk/shared/model-catalog.json (runtimeTierDefaults.codex + agents block)
  </read_first>
  <action>
    Codex effort derives from BOTH the slot effort AND the codex base/floor, so
    verify each value against the live resolver rather than assuming. Planning
    already resolved these (per D-01):
    - gsd-roadmapper balanced codex => effort `high` (slot is sonnet;high).
    - gsd-executor balanced codex => effort `high` (slot is sonnet;high), so the
      emitted TOML line is `model_reasoning_effort = "high"`.

    In tests/issue-2517-runtime-aware-profiles.test.cjs:
    - Line ~172, test `sonnet tier -> gpt-5.3-codex with reasoning_effort medium`:
      change the expected effort for `gsd-roadmapper` from `'medium'` to `'high'`.
      Update the test title's trailing "medium" to "high" so the name matches the
      assertion. Leave the model assertion (`gpt-5.3-codex`) unchanged.

    In tests/feat-57-install-translation.test.cjs:
    - The test `Codex TOML emits model_reasoning_effort = "medium" for bare
      opus/sonnet slot (D-08 floor, translated)` (around line 195) uses
      `gsd-executor` on `balanced`, which is now `sonnet;high` -> effort `high`.
      Change the asserted substring from
      `'model_reasoning_effort = "medium"'` to `'model_reasoning_effort = "high"'`
      and update the test title/comment from "medium" to "high".
      CRITICAL (per D-02): keep the FULL structured-string form
      `toml.includes('model_reasoning_effort = "high"')` — do NOT reduce it to a
      bare `includes('high')`, which feat-58 TEST-04's `bareIncludesRe` guard would
      flag. The structured key=value string (with spaces, `=`, and `_`) is exempt.
    - Leave the `xhigh` test (opus;max override) and the haiku-omit test
      (gsd-verifier budget -> no line) unchanged; both already pass.
  </action>
  <verify>
    <automated>node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-57-install-translation.test.cjs 2>&1 | command grep -c "✖" | command grep -qx 0 && echo PASS || (node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-57-install-translation.test.cjs 2>&1 | command grep "✖"; false)</automated>
  </verify>
  <done>Both Codex test files report zero ✖ failures; feat-57 retains the structured-string assertion form (no bare includes token), so feat-58 TEST-04 stays green.</done>
</task>

</tasks>

<verification>
Run the full targeted cluster and confirm green:

```
node --test tests/model-profiles.test.cjs tests/parse-model-effort.test.cjs tests/feat-58-regression.test.cjs tests/feat-57-install-translation.test.cjs tests/issue-2517-runtime-aware-profiles.test.cjs 2>&1 | command grep "✖"
```

Expect: no `✖` lines emitted.

Then run the whole suite to confirm no collateral regressions (especially the
feat-58 TEST-04 antipattern guard which lints all test files):

```
npm test 2>&1 | tee /tmp/gsd-test-output.txt
```

Confirm the model-effort / golden / Codex cluster is green and TEST-04 did not
flag any edited file. The separate bug-2801 ingest-docs / gsd-sdk failures are
out of scope — ignore them.
</verification>

<success_criteria>
- tests/fixtures/golden-effort-snapshot.json regenerated via the npm script (not hand-edited); diff shows only effort-token changes.
- All hardcoded sonnet;medium expectations that are now stale updated to match catalog slots (sonnet;high where verified).
- Codex effort expectations match live resolver output (roadmapper/executor balanced codex -> high).
- feat-57 retains safe structured-string assertions; feat-58 TEST-04 guard stays green.
- Targeted cluster fully green; no edits to sdk/shared/model-catalog.json, core.cjs, or any resolver (D-01).
</success_criteria>

<output>
This is a quick task — no SUMMARY.md required by the quick workflow. On completion,
report which test files were changed and the final cluster status.
</output>
