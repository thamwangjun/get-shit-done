---
phase: quick
plan: 260530-e5j
type: execute
wave: 1
depends_on: []
files_modified: [.planning/ROADMAP.md]
autonomous: true
requirements: [ROADMAP-EDIT]

must_haves:
  truths:
    - "Phase 48 AC #2 no longer contains 'without false-positives on letter-suffix steps (e.g., Step 7a)'"
    - "Phase 48 AC #2 states scanner flags letter-suffix steps (e.g., Step 7a) as violations requiring renumbering to whole integers"
    - "Phase 48 Goal mentions letter-suffix steps alongside decimal step labels"
    - "Phase 49 AC #2 references letter-suffix steps being renumbered to sequential whole integers alongside decimal steps"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with corrected letter-suffix scope for Phases 48 and 49"
  key_links:
    - from: "Phase 48 Goal"
      to: "Phase 48 AC #2"
      via: "consistent framing of what the scanner must detect"
      pattern: "letter-suffix"
---

<objective>
Update .planning/ROADMAP.md to reflect the design decision that letter-suffix step labels (e.g., Step 7a, Step 7b) are violations that must be flagged and renumbered to whole integers — not false-positives to be excluded.

Three targeted edits:
1. Phase 48 AC #2: remove the "without false-positives on letter-suffix steps" clause and replace it with a clause stating the scanner flags them as violations requiring renumbering
2. Phase 48 Goal: add letter-suffix steps alongside decimal step labels
3. Phase 49 AC #2: add that letter-suffix steps in the corpus are renumbered to sequential whole integers alongside decimal steps

Purpose: The current ROADMAP treats letter-suffix steps as excluded false-positives. The design has changed — they are violations with the same treatment as decimal step labels.
Output: Updated .planning/ROADMAP.md
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Apply three targeted edits to ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read .planning/ROADMAP.md in full. Apply the following three edits, preserving all surrounding text exactly:

**Edit 1 — Phase 48, AC #2** (line ~232–233 area):

Current text:
```
  2. Scanner correctly detects Pattern A/B (`**Step N.M**` headings) and Pattern D (ordered-list decimal items like `2.5.`) without false-positives on letter-suffix steps (e.g., `Step 7a`) or code-fenced content
```

Replace with:
```
  2. Scanner correctly detects Pattern A/B (`**Step N.M**` headings), Pattern D (ordered-list decimal items like `2.5.`), and letter-suffix steps (e.g., `Step 7a`) as violations requiring renumbering to whole integers; code-fenced content is excluded
```

**Edit 2 — Phase 48 Goal** (line ~227 area):

Current text:
```
**Goal**: Scanner tests for decimal step labels and out-of-order step numbering exist and fail against the current unmodified corpus
```

Replace with:
```
**Goal**: Scanner tests for decimal step labels, letter-suffix step labels (e.g., Step 7a), and out-of-order step numbering exist and fail against the current unmodified corpus
```

**Edit 3 — Phase 49, AC #2** (line ~246 area):

Current text:
```
  2. `tests/step-numbering-scan.test.cjs` decimal-label corpus subtests pass GREEN after normalization (0 violations in agents/, commands/gsd/, get-shit-done/workflows/)
```

Replace with:
```
  2. `tests/step-numbering-scan.test.cjs` decimal-label and letter-suffix-label corpus subtests pass GREEN after normalization (0 violations in agents/, commands/gsd/, get-shit-done/workflows/)
```

After applying all three edits, write the file. No other lines should change.
  </action>
  <verify>
    <automated>
      command grep "letter-suffix steps (e.g., \`Step 7a\`) as violations requiring renumbering" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md | wc -l
      # Expect: 1

      command grep "letter-suffix step labels (e.g., Step 7a)" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md | wc -l
      # Expect: 1

      command grep "letter-suffix-label corpus subtests" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md | wc -l
      # Expect: 1

      command grep "without false-positives on letter-suffix" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md | wc -l
      # Expect: 0
    </automated>
  </verify>
  <done>
    - AC #2 in Phase 48 no longer contains "without false-positives on letter-suffix steps" — it now states the scanner flags them as violations requiring renumbering to whole integers
    - Phase 48 Goal now mentions letter-suffix step labels alongside decimal step labels
    - Phase 49 AC #2 now covers letter-suffix-label subtests going GREEN alongside decimal-label subtests
    - All other ROADMAP.md content is unchanged
  </done>
</task>

</tasks>

<verification>
grep "without false-positives on letter-suffix" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md
# Must return no output (removed)

grep "letter-suffix steps (e.g., \`Step 7a\`) as violations" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md
# Must match Phase 48 AC #2

grep "letter-suffix step labels (e.g., Step 7a)" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md
# Must match Phase 48 Goal

grep "letter-suffix-label corpus subtests" /home/thamw/development/remote-dev/get-shit-done/.planning/ROADMAP.md
# Must match Phase 49 AC #2
</verification>

<success_criteria>
- All three targeted edits applied; no other lines changed
- "without false-positives on letter-suffix steps" is absent from ROADMAP.md
- Phase 48 Goal, Phase 48 AC #2, and Phase 49 AC #2 all reference letter-suffix steps as violations (not exclusions)
</success_criteria>

<output>
No new files created. .planning/ROADMAP.md is updated in place.
</output>
