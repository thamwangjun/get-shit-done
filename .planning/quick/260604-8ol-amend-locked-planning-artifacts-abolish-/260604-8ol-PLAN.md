---
phase: quick-260604-8ol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SPAWN-02]
must_haves:
  truths:
    - "D-08: On {claude, codex}, a bare (un-assigned) slot floors to effort `medium` — not null/omit. Every category (c) guarantee text reflects this."
    - "D-04: Residual absent cases (inherit slots, the 8 non-effort runtimes) are handled by a pre-built carrier token substituted verbatim — still emit nothing."
    - "The {claude, codex} allowlist language is RETAINED everywhere it appears — it now scopes the medium floor (not deleted)."
    - "Category (a) parser `;`-delimiter text and category (b) non-effort-runtime omit text are UNCHANGED."
    - "Every requirement ID, table row, and checkbox state is preserved — only wording/semantics change."
    - "REQUIREMENTS.md SPAWN-02 no longer says 'omitting it entirely when absent'; it reflects the medium floor + inherit/non-effort omit."
    - "ROADMAP.md Phases 53, 54, 56, 57, 58 category (c) guarantee clauses reflect the medium floor; Phase 58 golden snapshot is reworded to prove INTENDED new resolution, not pre==post identity."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "SPAWN-02 amended to medium-floor semantics"
    - path: .planning/ROADMAP.md
      provides: "Milestone goal + Phases 53/54/56/57/58 amended to medium-floor semantics"
  key_links:
    - from: ".planning/phases/56-spawn-template-wiring/56-CONTEXT.md"
      to: ".planning/REQUIREMENTS.md + .planning/ROADMAP.md"
      via: "D-08 (medium floor) + D-04 (mechanistic carrier) drive every category (c) edit"
      pattern: "medium floor|D-08"
---

<objective>
Amend the two LIVE, locked planning artifacts (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`) so they reflect the milestone-level decision recorded in Phase 56 CONTEXT (D-08 + D-04): effort omission is ABOLISHED for effort-supporting runtimes `{claude, codex}`. A bare (un-assigned) slot now floors to `medium` instead of resolving to `null`/omit. The earlier "purely additive / bare configs resolve byte-identically / zero emission" invariant is intentionally dropped for `{claude, codex}`. Two residual omit paths remain unchanged: explicit `inherit` slots, and the 8 non-effort runtimes (RESOLVE-05 hard no-op).

Purpose: Remove the non-determinism of conditional effort omission. D-08 floors bare claude/codex slots to `medium` (almost no "absent" case left); D-04 handles residual absent cases via a pre-built carrier token the orchestrator substitutes verbatim — no conditional judgment.

Output: Amended REQUIREMENTS.md and ROADMAP.md. DOCS-ONLY — no code, no tests.

Commit rationale (record in the commit body): removing the non-determinism of effort omission by flooring un-assigned {claude,codex} slots to `medium` (D-08) plus a mechanistic pre-built carrier token (D-04).
</objective>

<context>
@.planning/phases/56-spawn-template-wiring/56-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

**Source of truth:** Phase 56 CONTEXT decisions D-08 (medium floor, allowlist-gated) and D-04 (mechanistic pre-built carrier token). The CONTEXT file's `<canonical_refs>` § "Locked artifacts this phase supersedes" lists exactly the text that must change.

**Classification rule (apply to EVERY hit before editing):**
- (a) parser `;`-delimiter behavior (e.g. PARSE-01/02, `lastIndexOf(';')`, colon-in-provider-ID) → KEEP unchanged.
- (b) non-effort-runtime omit / `inherit` omit (e.g. RESOLVE-05, RESOLVE-06, the 8 null-tier runtimes, "every runtime outside `{claude, codex}` omits") → KEEP — still true.
- (c) bare-config → null → zero-emission / byte-identical / "resolve identically" / "purely additive (behavioral)" guarantee for claude/codex → UPDATE to the medium floor.

Only category (c) changes. Preserve every requirement ID, table row, and checkbox state. Never delete the `{claude, codex}` allowlist language — it is retained and now scopes the floor.

**Scope of files:** Edit ONLY `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`. Prior-phase CONTEXT files (52–55) and `.planning/research/*.md` are historical/frozen records of what was locked at their time; the Phase 56 CONTEXT already states D-08 supersedes them. Do NOT edit them. (Task 3 documents the sweep evidence only.)

**Out of scope (do NOT touch):** `core.cjs`, the resolver, workflows, agents, any code, any test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep sweep + classify every hit (do this FIRST, before any edit)</name>
  <read_first>
    .planning/phases/56-spawn-template-wiring/56-CONTEXT.md (D-04, D-08, canonical_refs supersession list)
  </read_first>
  <files>(read-only this task — no edits)</files>
  <action>
Run the sweep and classify each hit by category before touching any file (line numbers in the known-edits list may have drifted — re-locate by text, not line number):

```
command grep -rniE "bare config|byte-identical|purely additive|additive-only|zero effort emission|omit(s|ting)? (it|effort)|effort: null|backward-compatible omit|inert until|resolve identically" .planning/REQUIREMENTS.md .planning/ROADMAP.md
```

For EACH hit, classify (a)/(b)/(c) per the context block. Build a short in-session table: file, anchoring text snippet, category, planned action. Only category (c) hits get edited in Tasks 2–3.

Expected category (c) targets (verify exact current wording before editing — these are the only LIVE edits):
- REQUIREMENTS.md line ~8 intro: "purely additive ... bare configs must resolve identically to today" → qualify per D-08.
- REQUIREMENTS.md SPAWN-02 (~line 50): "pass effort conditionally, omitting it entirely when absent" → medium-floor rewrite.
- REQUIREMENTS.md TEST-01 (~line 60): "proves the change is additive (existing bare configs resolve identically before and after)" → rebaseline wording.
- ROADMAP.md Milestone Goal (~line 308): "purely additive — bare configs must resolve identically to today" → qualify.
- ROADMAP.md Phase 53 Goal (~line 335) "while bare configs continue to omit effort everywhere"; criterion 2 (~342) "→ omit; the `inherit` profile and bare adaptive entries omit effort" → medium-floor for bare claude/codex; inherit + bare adaptive still omit.
- ROADMAP.md Phase 54 Goal (~359) "no-ops on a bare catalog"; criterion 4 (~367) "On a bare ... catalog, every exposed `*_effort` value is `null`/omitted ... inert until catalog values are assigned" → bare claude/codex now exposes `medium`.
- ROADMAP.md Phase 56 Goal (~460) + criterion 2 (~467) "omitting it entirely when ... absent" → medium-floor rewrite (Task covered in Task 3 with the rest of ROADMAP).
- ROADMAP.md Phase 57 criterion 3 (~481) "omit guard is preserved — installing a bare config produces zero effort emission in every runtime's output" → only non-effort runtimes emit zero; claude/codex emit `medium`.
- ROADMAP.md Phase 58 Goal (~487) "additive-only guarantee ... bare configs resolve identically" + criterion 1 (~492) "proves the change is additive — existing bare configs resolve identically before and after" → re-baseline: snapshot proves the INTENDED new resolution (bare claude/codex → `medium`; pre ≠ post is expected, not a regression). Keep parser fixtures (criterion 2) and the per-runtime omit/translate contract for the 8 non-effort runtimes (criterion 3) intact.

Expected KEEP hits (do NOT edit): PARSE-01/PARSE-02 (category a), RESOLVE-05 / RESOLVE-06 / criterion 4 "Every runtime outside {claude, codex} omits effort" (category b), CONFIG-01 "bare fully-qualified IDs still omit effort" (category a — parser/override delimiter behavior, unrelated to the floor).
  </action>
  <verify>
    <automated>MISSING — docs-only classification step; no automated test. Self-check: classification table lists every grep hit with an (a)/(b)/(c) label and a planned action; every category (c) row maps to an edit in Task 2 or Task 3.</automated>
  </verify>
  <done>A complete classification table exists in-session covering every grep hit; category (a)/(b) rows marked KEEP; category (c) rows mapped to specific edits. No file edited yet.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md category (c) hits</name>
  <read_first>
    .planning/REQUIREMENTS.md (lines 1–65)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit ONLY the category (c) lines identified in Task 1. Preserve the `- [ ] **SPAWN-02**:` ID and checkbox exactly; change only the descriptive clause.

1. Intro (~line 8): qualify "purely additive ... bare configs must resolve identically to today" — additive for parsing/resolver plumbing, BUT bare `{claude, codex}` slots now floor to `medium` (deliberate behavior change per Phase 56 D-08); `inherit` and non-effort runtimes still omit.

2. SPAWN-02 (~line 50): rewrite the clause "pass effort conditionally, omitting it entirely when absent" to: spawn templates pass resolved effort to spawned agents; an un-assigned slot on `{claude, codex}` defaults to `medium` (no omission, per D-08); explicit `inherit` slots and the 8 non-effort runtimes emit nothing (carried by the pre-built token, D-04). Keep SPAWN-01 and SPAWN-03 unchanged.

3. TEST-01 (~line 60): rebaseline the "proves the change is additive (existing bare configs resolve identically before and after)" wording — the golden snapshot now proves the INTENDED new resolution (bare claude/codex → `medium`); pre ≠ post for those slots is expected, not a regression. Existing-field MODEL values stay identical; effort siblings for bare claude/codex move null → `medium`.

Leave RESOLVE-05, RESOLVE-06, PARSE-01, PARSE-02, CONFIG-01 unchanged (categories a/b). Do not alter any other requirement.
  </action>
  <verify>
    <automated>command grep -nE "omitting it entirely when absent|resolve identically to today" .planning/REQUIREMENTS.md ; test $(command grep -c "SPAWN-02" .planning/REQUIREMENTS.md) -ge 1</automated>
  </verify>
  <done>SPAWN-02 no longer reads "omitting it entirely when absent"; intro and TEST-01 reflect the medium floor; the `{claude, codex}` allowlist language is intact; SPAWN-01/SPAWN-03/RESOLVE-05/RESOLVE-06 and all IDs/checkboxes unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md category (c) hits (Milestone goal + Phases 53/54/56/57/58)</name>
  <read_first>
    .planning/ROADMAP.md (lines 306–498)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit ONLY the category (c) clauses. Preserve every phase heading, requirement list, depends-on line, plan list, and checkbox state.

1. Milestone Goal (~line 308): qualify "The work is purely additive — bare configs must resolve identically to today." → additive for parsing/resolver plumbing, BUT bare `{claude, codex}` slots now floor to `medium` (deliberate behavior change per Phase 56 D-08).

2. Phase 53 Goal (~line 335): "while bare configs continue to omit effort everywhere" → bare slots within `{claude, codex}` now floor to `medium`; `inherit` and non-effort runtimes still omit. Criterion 2 (~342) "...→ omit; the `inherit` profile and bare adaptive entries omit effort": keep the precedence chain, but the bare-`{claude,codex}` fallthrough is now `medium`, not omit; `inherit` and bare adaptive entries still omit. KEEP criterion 4 (~344) "Every runtime outside `{claude, codex}` omits effort" verbatim (category b — still TRUE).

3. Phase 54 Goal (~line 359) "plumbing that no-ops on a bare catalog" and criterion 4 (~367) "On a bare ... catalog, every exposed `*_effort` value is `null`/omitted ... inert until catalog values are assigned" → bare `{claude, codex}` slots now expose `medium`; the exposure layer reflects the resolver floor. (Note: explicit-null sibling contract from Phase 54 D-01 still holds for inherit/non-effort runtimes.)

4. Phase 56 Goal (~line 460) and criterion 2 (~467): rewrite "conditionally pass resolved effort ... omitting it entirely when absent" / "pass effort conditionally, omitting it entirely when resolved effort is absent" → pass resolved effort via the pre-built carrier token; bare `{claude, codex}` slots floor to `medium` (D-08); the residual absent cases (explicit `inherit`, the 8 non-effort runtimes) emit nothing via the empty carrier token (D-04). KEEP criterion 3 (quality gates) and criterion 1 unchanged. Add a one-line note that D-08 reopens the Phase 53 resolver (`resolveReasoningEffortInternal` gains the allowlist-gated medium floor) — no longer frozen for Phase 56.

5. Phase 57 criterion 3 (~line 481): "The omit guard is preserved — installing a bare ... config produces zero effort emission in every runtime's output" → only the 8 non-effort runtimes emit zero; `{claude, codex}` emit `medium` for bare slots. Keep criteria 1–2 (Codex translation, `max`→`xhigh`, haiku tier) unchanged.

6. Phase 58 Goal (~line 487) "locks in the additive-only guarantee ... proves bare configs resolve identically" and criterion 1 (~492) "proves the change is additive — existing bare configs resolve identically before and after" → re-baseline: the golden snapshot proves the INTENDED new resolution (bare `{claude, codex}` → `medium`; pre ≠ post is expected, not a regression), with existing MODEL values unchanged. KEEP criterion 2 (parser fixtures), criterion 3 (per-runtime omit/translate contract for the 8 non-effort runtimes — claude emits, codex translates, others omit), criterion 4, criterion 5 intact.

Do not renumber criteria, do not alter checkbox states, do not touch Phases 52, 55, 55.1, 55.2, or any earlier milestone block.
  </action>
  <verify>
    <automated>command grep -niE "floor|medium" .planning/ROADMAP.md | command grep -i "claude" ; command grep -nE "Every runtime outside .{claude, codex}. omits effort" .planning/ROADMAP.md</automated>
  </verify>
  <done>Milestone goal + Phases 53/54/56/57/58 category (c) clauses reflect the medium floor; Phase 58 snapshot reworded to prove intended new resolution not identity; the `{claude, codex}` allowlist + "every runtime outside {claude,codex} omits" (category b) text retained; all IDs, depends-on lines, plan lists, and checkboxes unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Commit the amendment</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Stage only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` and commit. Use a `docs` commit with the rationale in the body:

```
docs(56): abolish effort omission for {claude,codex} — floor bare slots to medium

Amend locked planning artifacts (REQUIREMENTS.md, ROADMAP.md) to reflect
Phase 56 D-08 + D-04: bare {claude,codex} slots floor to `medium` instead of
null/omit, removing the non-determinism of conditional effort omission (D-08)
via a mechanistic pre-built carrier token (D-04). The "purely additive /
byte-identical / zero emission" invariant is intentionally dropped for
{claude,codex}; inherit slots and the 8 non-effort runtimes still omit.
The {claude,codex} allowlist is retained and now scopes the medium floor.
```

DOCS-ONLY — no code or test files in this commit.
  </action>
  <verify>
    <automated>git show --stat HEAD | command grep -E "REQUIREMENTS.md|ROADMAP.md" ; git show --stat HEAD | command grep -vqE "\.cjs|\.ts|\.test\." </automated>
  </verify>
  <done>A single docs commit contains only REQUIREMENTS.md and ROADMAP.md; commit body records the D-08 medium-floor + D-04 carrier-token rationale; no code/test files staged.</done>
</task>

</tasks>

<verification>
- `command grep -nE "omitting it entirely when absent" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns no live SPAWN-02/Phase-56 hit (only acceptable if rewritten).
- The `{claude, codex}` allowlist language still present in REQUIREMENTS.md and ROADMAP.md (RESOLVE-05 + Phase 53 criterion 4 + others).
- "Every runtime outside `{claude, codex}` omits effort" (category b) preserved verbatim.
- No code or test file modified (`git status` shows only the two `.planning/*.md` files).
- All requirement IDs and checkbox states unchanged (diff shows wording-only changes inside existing list items).
</verification>

<success_criteria>
- REQUIREMENTS.md SPAWN-02, intro, TEST-01 reflect the medium floor (D-08) + mechanistic carrier (D-04).
- ROADMAP.md milestone goal + Phases 53/54/56/57/58 category (c) clauses reflect the medium floor; Phase 58 snapshot reworded to prove intended new resolution, not pre==post identity.
- Category (a) parser text and category (b) non-effort-runtime/inherit omit text unchanged.
- `{claude, codex}` allowlist retained everywhere (now scopes the floor).
- Every requirement ID, table row, and checkbox state preserved.
- Single docs-only commit with the D-08 + D-04 rationale.
</success_criteria>

<output>
Amend `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` in place; commit as a single docs change. No SUMMARY scaffolding required beyond the quick-task close-out.
</output>
