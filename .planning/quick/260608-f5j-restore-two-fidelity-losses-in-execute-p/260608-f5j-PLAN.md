---
phase: quick-260608-f5j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
autonomous: true
requirements: [FID-01, FID-02]
must_haves:
  truths:
    - "The no-transition path emits a literal PHASE COMPLETE marker that plan-phase.md can grep for"
    - "CROSS_AI_CMD and CROSS_AI_TIMEOUT are defined before their use on line 286"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Restored no-transition completion block and cross-AI variable definitions"
  key_links:
    - from: "execute-phase.md no-transition path"
      to: "plan-phase.md chain-success detection"
      via: "literal PHASE COMPLETE marker"
      pattern: "PHASE COMPLETE"
    - from: "execute-phase.md cross_ai_delegation step"
      to: "timeout/external-command invocation on line 286"
      via: "CROSS_AI_CMD / CROSS_AI_TIMEOUT shell variables"
      pattern: "CROSS_AI_(CMD|TIMEOUT)="
---

<objective>
Restore two fidelity losses introduced by prompt-compression of `get-shit-done/workflows/execute-phase.md`. Make exactly two surgical edits to that one file — no other files, no frontmatter changes.

Purpose: The compression dropped (1) the literal `PHASE COMPLETE` completion contract that plan-phase.md and discuss-phase chain mode grep for, and (2) the bash definitions of `CROSS_AI_CMD`/`CROSS_AI_TIMEOUT` that line 286 consumes — leaving those variables undefined at use.
Output: Updated `get-shit-done/workflows/execute-phase.md` with both losses restored.
</objective>

<context>
@get-shit-done/workflows/execute-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore PHASE COMPLETE contract and cross-AI variable definitions</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>
  Read the two target regions before editing to anchor on exact current text:
  - Line 271 region (cross_ai_delegation step): the line reads
    `Otherwise: check frontmatter \`cross_ai: true\` AND config \`workflow.cross_ai_execution=true\`.`
  - Line 981 region (offer_next step): the line reads
    `**No-transition check:** If \`--no-transition\` flag present (execute-phase spawned by plan-phase's auto-advance), return completion status to parent and STOP. Do not run transition.`
  </read_first>
  <action>
  Make exactly two edits to `get-shit-done/workflows/execute-phase.md`. Edit ONLY this file; change no frontmatter, no other files.

  EDIT 1 — Replace the existing line-981 `**No-transition check:**` line in full with the expanded block (per FID-01). The replacement spells out that on the `--no-transition` path, after verification passes and roadmap is updated, the step returns a completion status to the parent in an exact format whose first marker is the literal `PHASE COMPLETE`, then STOPs without auto-advancing or running transition. Include the fields `Phase: ${PHASE_NUMBER} - ${PHASE_NAME}`, `Plans: ${completed_count}/${total_count}`, `Verification: {Passed | Gaps Found}`, an `[Include aggregate_results output]` placeholder, and the note explaining that plan-phase.md and discuss-phase chain mode grep for the literal `PHASE COMPLETE` marker to detect chain success. Use the precise wording supplied in the task spec.

  EDIT 2 — Immediately AFTER line 271 (`Otherwise: check frontmatter \`cross_ai: true\` AND config \`workflow.cross_ai_execution=true\`.`), insert a fenced ```bash block defining the three variables consumed later in the step (per FID-02): `CROSS_AI_ENABLED`, `CROSS_AI_CMD`, and `CROSS_AI_TIMEOUT`, each via `$GSD_SDK query config-get workflow.<key>` with a `2>/dev/null || echo "<default>"` fallback (`false`, empty string, and `300` respectively). This makes `CROSS_AI_CMD` and `CROSS_AI_TIMEOUT` defined before their use on line 286.

  Preserve surrounding text exactly. Keep the issue-number-free comment style and eta conventions (only `<%~` include syntax; never `{%~`). Do not touch any line outside these two edit sites.
  </action>
  <verify>
    <automated>command grep -c "PHASE COMPLETE" get-shit-done/workflows/execute-phase.md | command grep -qE '[1-9]' && command grep -q 'CROSS_AI_CMD=' get-shit-done/workflows/execute-phase.md && command grep -q 'CROSS_AI_TIMEOUT=' get-shit-done/workflows/execute-phase.md && echo PASS</automated>
  </verify>
  <done>
  `get-shit-done/workflows/execute-phase.md` contains the literal `PHASE COMPLETE` marker within the no-transition block, and contains bash definitions for `CROSS_AI_ENABLED`, `CROSS_AI_CMD`, and `CROSS_AI_TIMEOUT` inserted immediately after the former line 271, with `CROSS_AI_CMD`/`CROSS_AI_TIMEOUT` defined before their use on line 286. No other file is modified; no frontmatter changed.
  </done>
</task>

</tasks>

<verification>
- `command grep -n "PHASE COMPLETE" get-shit-done/workflows/execute-phase.md` returns at least one hit inside the offer_next no-transition block.
- `command grep -nE 'CROSS_AI_(ENABLED|CMD|TIMEOUT)=' get-shit-done/workflows/execute-phase.md` shows the three definitions appearing before line 286.
- `git diff --name-only` lists only `get-shit-done/workflows/execute-phase.md`.
</verification>

<success_criteria>
- Both edits applied to the single target file.
- Literal `PHASE COMPLETE` marker present in the no-transition path.
- `CROSS_AI_CMD` and `CROSS_AI_TIMEOUT` defined before their consumption on line 286.
- No other files touched; no frontmatter modified.
</success_criteria>

<output>
Modify `get-shit-done/workflows/execute-phase.md`. Create `.planning/quick/260608-f5j-restore-two-fidelity-losses-in-execute-p/260608-f5j-SUMMARY.md` when done.
</output>
