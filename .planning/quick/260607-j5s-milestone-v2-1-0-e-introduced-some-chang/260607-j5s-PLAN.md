---
phase: quick-260607-j5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "A structured gap report exists at the quick task directory path"
    - "Every changed prompt file in v2.1.0-e is accounted for (COVERED or GAP)"
    - "Each gap entry names the changed file, what changed, which test covers it (if any), gap type, and priority"
  artifacts:
    - path: ".planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-GAP-REPORT.md"
      provides: "Structured gap report for v2.1.0-e prompt file changes"
  key_links: []
---

<objective>
Investigate which v2.1.0-e prompt file changes lack regression test coverage and produce a structured gap report.

Purpose: Give the developer a prioritized list of testing gaps so that future quick tasks can write the missing guards.
Output: 260607-j5s-GAP-REPORT.md — one row per changed prompt file, marked COVERED or GAP with priority.
</objective>

<execution_context>
<purpose>
Execute the investigation and produce the gap report document.
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
</required_reading>

<atomic_close_out_invariant>
For each executed plan, the only complete close-out order is:
production-code commit(s) -> SUMMARY commit -> STATE/ROADMAP update.
</atomic_close_out_invariant>

<available_agent_types>
Valid GSD subagent types: gsd-executor
</available_agent_types>
</execution_context>

<context>
!`cat .planning/STATE.md`
!`cat .planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-CONTEXT.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the v2.1.0-e testing gap report</name>
  <files>.planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-GAP-REPORT.md</files>
  <action>
Produce the gap report by running the investigation steps below and writing the result to the file path above.
Do NOT write any production code or test files — this task is investigation only.

**Step 1: Bound the changeset**

```bash
git diff v2.1.0-a v2.1.0-e --name-only \
  | grep -E '\.(md)$' \
  | grep -E '^(agents|commands/gsd|get-shit-done/workflows)/' \
  | sort
```

These are ALL prompt files changed in the milestone. Every row in the report must come from this list.

**Step 2: Catalog the test files that exist**

```bash
ls tests/ | sort
```

For each prompt file in the changeset, determine which test file (if any) asserts the v2.1.0-e change. Use the following pre-researched findings to anchor the analysis (do not re-run all the greps — use the findings below and do targeted spot checks only):

**Pre-researched findings (use these directly):**

*Changes in v2.1.0-e by category:*

A. **Eta include conversions** (`@~/...` → `<%~ include(...) %>`):
   - Affected: 9 agents (gsd-debugger, gsd-executor, gsd-intel-updater, gsd-phase-researcher, gsd-plan-checker, gsd-planner, gsd-user-profiler, gsd-verifier, gsd-debug-session-manager implied via debugger) and most workflow files.
   - Test coverage: `tests/install-eta-regression.test.cjs` (TEST-01 full-install walk) + `tests/bug-phase45-eta-wiring.test.cjs` (INTG-02, INTG-03, INTG-06) + `tests/eta-template-syntax.test.cjs`.
   - Status: **COVERED** — these scans catch any surviving `@~/` in installed output.

B. **`@.planning/X` → `!cat .planning/X` conversion** in `agents/gsd-planner.md` context block:
   - Test coverage: `tests/bug-phase45-eta-wiring.test.cjs` INTG-03 scans `agents/` for bare-line `@.planning/` and fails on survivors.
   - Status: **COVERED**.

C. **Effort wiring — Group A init-fed workflows** (execute-phase, execute-plan, plan-phase, quick, new-project, new-milestone, verify-work, map-codebase):
   - Test coverage: `tests/phase-56-effort-wiring.test.cjs` GAP A section, one test per workflow.
   - Status: **COVERED**.

D. **Effort wiring — Group B standalone-resolve workflows** (audit-milestone, scan, secure-phase, ui-phase, ui-review, validate-phase, discuss-phase/modes/advisor, debug, gsd-debug-session-manager, docs-update):
   - Test coverage: `tests/phase-56-effort-wiring.test.cjs` GAP B section.
   - Status: **COVERED**.

E. **Effort wiring — 8 post-audit workflows added in quick task 260606-vf5** (audit-fix, diagnose-issues, code-review, code-review-fix, explore, import, ingest-docs, discuss-phase-assumptions):
   - Each of these workflows now carries `resolve-model-effort gsd-<agent>` and interpolates `effort={<agent>_model_effort_arg}` into Agent() spawns.
   - Test coverage: `tests/phase-56-effort-wiring.test.cjs` does NOT contain any test for these 8 workflows (confirmed: grep for "audit-fix", "diagnose-issues", "code-review", "code-review-fix", "explore", "import", "ingest-docs", "discuss-phase-assumptions" in the test file returns zero matches).
   - Status: **GAP** — High priority. Regression would be silent (effort silently drops to null).

F. **`effort=` keyword prefix fix** — bare `{*_effort_arg}` tokens in Agent() calls got `effort=` prefix (quick task 260606-x8b):
   - Test coverage: `tests/bare-effort-arg-scan.test.cjs` scans all three dirs and catches any bare token.
   - Status: **COVERED**.

G. **Null-omit comments** — standalone `effort={*_effort_arg}` lines inside Agent() calls got `# omit this line when` comment (quick task 260607-0kd):
   - Test coverage: `tests/null-omit-comment-scan.test.cjs` scans all three dirs.
   - Status: **COVERED**.

H. **`gsd-executor.md` worktree-vs-submodule guard** — the pre-commit HEAD safety block now distinguishes a real worktree (`.git` file containing `gitdir:.../.git/worktrees/...`) from a git submodule (`.git` file containing `gitdir:.../.git/modules/...`). The guard only fires for the worktree case.
   - Test coverage: `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` checks `worktrees/` path detection for cwd-drift but does NOT assert the new submodule exclusion branch. No other test checks for the `GIT_CONTENT` / `modules.*gitdir` guard pattern.
   - Status: **GAP** — Medium priority. A regression would re-apply the HEAD guard to submodule contexts, potentially halting commits unnecessarily.

I. **`gsd-executor.md` final_commit positive rephrasing** — "Do not fall back to raw `git add`..." replaced with "When the SDK returns `skipped: true`, accept the skip and move on." The functional constraint is preserved but the directive language is now affirmative.
   - Test coverage: `tests/bug-3678-executor-commit-docs-respect.test.cjs` A1 (still passes — `committed: false` and `skipped: true` strings remain present) and A2 (already skip-marked as "fork intentionally diverges from upstream contract").
   - Status: **COVERED** — A1 continues to pass with the new wording.

J. **Step renumbering in three agents** — decimal/letter-suffix step labels fixed to whole integers:
   - `agents/gsd-phase-researcher.md`: Step 1.3→Step 2, Step 1.5→Step 3, Step 2.5→Step 4, Step 2.6→not present (confirmed by grepping live file).
   - `agents/gsd-intel-updater.md`: Step 6.5→Step 7, Step 7→Step 8.
   - `agents/gsd-verifier.md`: Step 2a→Step 3 (roadmap loading step label change).
   - Test coverage: `tests/step-numbering-scan.test.cjs` is a SCANNER (not static assertions). It detects decimal/letter-suffix steps and would fail if they returned. No test positively asserts the new whole-integer step numbers exist — but that is by design (scanners catch regressions without hardcoding expected values).
   - Status: **COVERED** (by scanner). The comment inside the test still lists the pre-fix file names as "Phase 48 RED expectation" — that comment is stale/misleading but does not affect test correctness since the scanner is dynamic.
   - Note: The stale comment is a documentation gap (Low priority) — not a behavioral gap.

K. **`agents/gsd-debugger.md` security prompt rewrite** — the injection-guard paragraph was reworded from one sentence referencing `DATA_START`/`DATA_END` markers to a multi-line hardened paragraph using "untrusted user input" and "treat every byte... as evidence data only."
   - Test coverage: No test asserts the content of the debugger's security guard paragraph. `tests/prompt-injection-scan.test.cjs` scans for injection IN files (safety), not for whether a file CONTAINS a guard.
   - Status: **GAP** — Low priority. The change is a prompt-quality improvement; the lack of a test means a regression would only be caught by human review.

L. **`agents/gsd-user-profiler.md` load_rubric step rewrite** — the step that previously said "Read the user-profiling reference document at `~/.claude/...`" was reworded to say "The user-profiling rubric is included above in the `<reference>` block."
   - Test coverage: No test asserts the content of the gsd-user-profiler load_rubric step. This is a prose simplification (the Eta include change means the rubric is inlined; the step text reflects that).
   - Status: **GAP** — Low priority. The rubric content itself is tested by `tests/install-eta-regression.test.cjs` (inlining), but the step text accuracy is not.

M. **`tests/step-numbering-scan.test.cjs` stale comment** — the module-level comment lists `gsd-phase-researcher.md (Step 1.3, 1.5, 2.5, 2.6)` and `gsd-intel-updater.md (Step 6.5)` as "Phase 48 RED expectation" failures. These are now fixed; the comment is misleading.
   - Status: Documentation gap in a test file — **Low priority**, not a behavioral gap.

**Step 3: Write the gap report**

Write `260607-j5s-GAP-REPORT.md` at the path above. Use the table format below.

The report must contain:
1. A brief summary section (3-5 sentences) describing overall coverage quality.
2. A table of ALL gaps with columns: Gap ID, Changed File(s), What Changed, Covering Test (if any), Gap Type, Priority.
3. A "COVERED" section listing the coverage categories that are adequate.
4. A "Recommended next tasks" section listing one concrete quick task per gap.

Use this classification for Gap Type (from CONTEXT.md):
- Type 1: No regression test exists
- Type 2: Test exists but does not assert the new behavior
- Type 3: Structural/frontmatter gap
- Type 4: Content/behavior gap (prompt text changed but no behavioral test)

Priority criteria (from CONTEXT.md):
- High: silent behavioral regression (wrong effort value, wrong model, broken gate)
- Medium: functionality impacted but unlikely to regress silently
- Low: documentation/prose quality only; human review would catch regression
  </action>
  <verify>
    <automated>test -f ".planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-GAP-REPORT.md" &amp;&amp; grep -q "GAP-" ".planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-GAP-REPORT.md"</automated>
  </verify>
  <done>260607-j5s-GAP-REPORT.md exists and contains at least one GAP-ID entry with file, description, type, and priority columns populated.</done>
</task>

</tasks>

<verification>
The gap report file exists at `.planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-GAP-REPORT.md` and contains:
- A summary section
- Rows for the 3 identified behavioral gaps (E, H, K above) at minimum
- Coverage status for all major change categories
</verification>

<success_criteria>
- Gap report created with complete coverage of all v2.1.0-e prompt file changes
- Each gap has a priority and recommended next task
- No production code or test files written (investigation only)
- SUMMARY.md created and committed
</success_criteria>

<output>
Create `.planning/quick/260607-j5s-milestone-v2-1-0-e-introduced-some-chang/260607-j5s-SUMMARY.md` when done.
</output>
