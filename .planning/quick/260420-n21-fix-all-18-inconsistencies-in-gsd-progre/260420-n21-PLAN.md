---
phase: quick-260420-n21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [n21-fix-inconsistencies]
must_haves:
  truths:
    - "STATE.md frontmatter percent matches the body progress bar value"
    - "REQUIREMENTS.md all 38 v1 requirement checkboxes show [x]"
    - "REQUIREMENTS.md all 38 traceability rows show Satisfied"
    - "MILESTONES.md has a convention note and no missing v1.37.1 entry claim"
    - "ROADMAP.md duplicate phase-number rows have a disambiguating comment"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Corrected frontmatter and body progress metrics"
    - path: ".planning/ROADMAP.md"
      provides: "Phase numbering note and active-vs-shipped header convention note"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 38 checkboxes and traceability rows updated; scope note and v2 rows added; CAT-05 count corrected"
    - path: ".planning/MILESTONES.md"
      provides: "Convention note explaining shipped-only scope"
  key_links:
    - from: "STATE.md frontmatter percent"
      to: "STATE.md body progress bar"
      via: "must match numerically"
      pattern: "percent.*67.*67%"
---

<objective>
Apply all 18 fixes documented in FINDINGS.md (F-01 through F-18) across STATE.md, ROADMAP.md, REQUIREMENTS.md, and MILESTONES.md. All fixes are documentation-only — no code changes.

Purpose: Eliminate the three CRITICAL conflicts (F-04, F-12, F-13) and the 15 STALE/FORMATTING issues before Phase 11 begins executing so progress tracking is accurate and requirements traceability is truthful.

Output: Four updated planning files with every identified inconsistency resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/quick/260420-mjg-find-all-inconsistencies-in-gsd-progress/FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix STATE.md — frontmatter counts, percent, stopped_at, and Performance Metrics</name>
  <files>.planning/STATE.md</files>
  <action>
Read .planning/STATE.md in full, then apply every STATE.md fix in one write:

**F-01** — Change `total_phases: 4` to `total_phases: 6` (v1.37.1 has 6 phases: 7–12).

**F-02 + F-04** — The CRITICAL conflict: frontmatter says `percent: 100` but body says `50%`. The correct v1.37.1-scoped calculation is 4 completed of 6 phases = 67%. Apply:
- `completed_phases: 4` (already correct, leave as-is)
- `total_phases: 6` (from F-01 above)
- `percent: 67`
- Body progress bar: change `[██████████··········] 50%` to `[████████████········] 67%`

**F-03** — `total_plans: 6` and `completed_plans: 6` reflects only the 6 defined plans for Phases 7–10. Phases 11 and 12 will add plans. Set:
- `total_plans: 8` (6 complete + estimated 2 forthcoming; note this is a projection)
- `completed_plans: 6`
- Note: percent stays 67% (phase-scoped, not plan-scoped)

**F-05** — Change `stopped_at: Phase 10 complete` to `stopped_at: Entering Phase 11`.

**F-06** — Replace the placeholder performance metrics block with an explicit note. Change the `**Velocity:**` block from:
```
- Total plans completed: 6
- Average duration: —
- Total execution time: —

**By Phase:**

*Updated after each plan completion*
```
to:
```
- Total plans completed: 6 (v1.37.1 Phases 7–10)
- Average duration: Not tracked — metrics not retroactively enabled for completed phases
- Total execution time: Not tracked — metrics not retroactively enabled for completed phases

**By Phase:**

*Not tracked for completed phases. Will populate for Phase 11 onward.*
```

**F-07** — The directory path `260420-m8f-fix-documentation-inconsistencies-in-roa` is the actual on-disk directory name (intentionally truncated at filesystem limit). Add an inline comment to the Quick Tasks table. Change the link cell from:
`[260420-m8f-fix-documentation-inconsistencies-in-roa](./quick/260420-m8f-fix-documentation-inconsistencies-in-roa/)`
to:
`[260420-m8f-fix-documentation-inconsistencies-in-roa](./quick/260420-m8f-fix-documentation-inconsistencies-in-roa/) <!-- directory name truncated at filesystem limit -->`
  </action>
  <verify>
Read .planning/STATE.md after writing and confirm:
1. `percent: 67` in frontmatter
2. `total_phases: 6` in frontmatter
3. `completed_plans: 6` and `total_plans: 8` in frontmatter
4. `stopped_at: Entering Phase 11` in frontmatter
5. Body progress bar shows `67%`
6. Performance Metrics contains "Not tracked" text (not placeholder dashes)
7. Quick Tasks table directory link has the inline comment
  </verify>
  <done>STATE.md has no remaining numerical conflicts; percent matches body bar; placeholders replaced with explicit "Not tracked" notes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP.md — phase-numbering note, active-vs-shipped header convention note, and F-08/F-09 annotation</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read .planning/ROADMAP.md in full, then apply every ROADMAP.md fix in one write:

**F-08 and F-09** — Phase 11 and Phase 12 both show `**Plans**: 0 plans`. Per the planning constraints, do NOT fabricate plan counts. Add a note under each explaining the current state:

For Phase 11 section, change:
`**Plans**: 0 plans`
to:
`**Plans**: 0 plans — not yet planned (planning pending before Phase 11 execution)`

For Phase 12 section, change:
`**Plans**: 0 plans`
to:
`**Plans**: 0 plans — not yet planned (planning pending before Phase 12 execution)`

The Progress table rows for phases 11 and 12 already correctly show `0/0 | Not started | -` — leave those unchanged.

**F-10** — Add a comment immediately above the `## Progress` table header to document the per-milestone phase-numbering convention:
```
<!-- Phase numbers restart per milestone (v1.36.0.b has its own Phase 4–5; v1.36.0.a has its own Phase 4–6). The Milestone column provides disambiguation. -->
```

**F-11** — Document the active-vs-shipped `###`/`<details>` header convention. Add a comment immediately above the `### v1.37.1 Files (In Progress)` heading:
```
<!-- Convention: active milestones use plain ### headers to stay visible; shipped milestones are collapsed into <details> blocks. Wrap in <details> when v1.37.1 ships. -->
```
  </action>
  <verify>
Read .planning/ROADMAP.md after writing and confirm:
1. Phase 11 `**Plans**` line contains "not yet planned"
2. Phase 12 `**Plans**` line contains "not yet planned"
3. HTML comment above `## Progress` mentions "restart per milestone"
4. HTML comment above `### v1.37.1 Files (In Progress)` mentions the `###` vs `<details>` convention
  </verify>
  <done>ROADMAP.md has explanatory notes for all four ROADMAP-scoped findings (F-08, F-09, F-10, F-11). No plan counts fabricated.</done>
</task>

<task type="auto">
  <name>Task 3: Fix REQUIREMENTS.md — check all 38 boxes, update traceability, add scope note, add MAINT rows, fix CAT-05 count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read .planning/REQUIREMENTS.md in full, then apply every REQUIREMENTS.md fix in one write:

**F-12** — Update all 38 v1 requirement checkboxes from `[ ]` to `[x]`. Every line matching `- [ ] **MERGE-`, `- [ ] **CAT-`, `- [ ] **NEW-`, `- [ ] **MOD-`, `- [ ] **TEST-` becomes `- [x]`.

Full list of affected lines (all 38):
- MERGE-01, MERGE-02, MERGE-03, MERGE-04 (4 checkboxes)
- CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06 (6 checkboxes)
- NEW-01 through NEW-20 (20 checkboxes)
- MOD-01, MOD-02, MOD-03, MOD-04 (4 checkboxes)
- TEST-01, TEST-02, TEST-03, TEST-04 (4 checkboxes)

**F-13** — Update all 38 traceability table Status cells from `Pending` to `Satisfied`. Every row in the Traceability table under `## Traceability` that ends `| Pending |` becomes `| Satisfied |`.

**F-14** — Add MAINT-01 and MAINT-02 to the Traceability table. After the last `TEST-04` row, append:
```
| MAINT-01 | Future | Not started |
| MAINT-02 | Future | Not started |
```
Also update the Coverage section at the bottom from:
```
- v1 requirements: 38 total
- Mapped to phases: 38
- Unmapped: 0 ✓
```
to:
```
- v1 requirements: 38 total (all Satisfied)
- v2 requirements: 2 total (MAINT-01, MAINT-02 — not yet assigned to a phase)
- Mapped to phases: 38 v1 requirements
- Unmapped: 0 v1 ✓ (v2 requirements intentionally deferred)
```

**F-17** — In the CAT-05 requirement line, change `(~271)` to `(total=270, counts: commands=79, references=48, workflows=80, templates=32, agents=31)`. The full line becomes:
`- [x] **CAT-05**: CATALOGUE.json \`counts\` and \`total\` updated to reflect actual file count after merge (total=270, counts: commands=79, references=48, workflows=80, templates=32, agents=31)`

**F-18** — Add a scope clarification note immediately after the `**Core Value:**` line at the top of the file. Insert:
```
**Scope:** These requirements apply to milestone v1.37.1 only (Phases 7–12). Phases 1–6 are covered by their respective milestone roadmaps (v1.36.0-ROADMAP.md, v1.36.0.b-ROADMAP.md, v1.36.0.a-ROADMAP.md).
```

Also update the `*Last updated:` footer line to:
`*Last updated: 2026-04-20 — all 38 v1 requirements marked Satisfied; MAINT-01/MAINT-02 added to traceability; CAT-05 count corrected to 270; scope note added*`
  </action>
  <verify>
Read .planning/REQUIREMENTS.md after writing and confirm:
1. Zero `- [ ]` lines remain for any MERGE/CAT/NEW/MOD/TEST requirement
2. All 38 traceability rows show "Satisfied" (not "Pending")
3. MAINT-01 and MAINT-02 rows appear in the Traceability table with "Not started"
4. CAT-05 line contains "total=270" (not "~271")
5. Scope note appears near the file header mentioning "v1.37.1 only (Phases 7–12)"
  </verify>
  <done>All 38 REQUIREMENTS.md checkboxes are [x]; all 38 traceability rows show Satisfied; v2 requirements appear in traceability; CAT-05 count is accurate; scope confusion eliminated.</done>
</task>

<task type="auto">
  <name>Task 4: Fix MILESTONES.md — add shipped-only convention note and document ordering</name>
  <files>.planning/MILESTONES.md</files>
  <action>
Read .planning/MILESTONES.md in full, then apply every MILESTONES.md fix in one write:

**F-15** — Add a convention comment at the very top of the file (before the first `## ` heading) explaining that this file tracks shipped milestones only:
```
<!-- This file tracks shipped milestones only. Add a new entry here when a milestone ships. v1.37.1 entry will be added upon shipping. -->
```

**F-16** — The file currently uses newest-first ordering (v1.36.0.a, v1.36.0.b, v1.36.0). This differs from ROADMAP.md which uses oldest-first. Standardize by documenting the choice rather than reordering (reordering risks edit conflicts). Add a second comment on the line after the first:
```
<!-- Ordering: newest-first (reverse-chronological activity log). ROADMAP.md uses oldest-first for planning context — both conventions are intentional. -->
```

Do not reorder the milestone entries themselves — the content is correct, only the convention needed documenting.

No v1.37.1 entry to add yet (the milestone is in-progress, not shipped). The comment in F-15 explains this.
  </action>
  <verify>
Read .planning/MILESTONES.md after writing and confirm:
1. Two HTML comments appear at the top of the file before any `## ` heading
2. First comment mentions "shipped milestones only" and "v1.37.1 entry will be added upon shipping"
3. Second comment documents the newest-first ordering convention
4. Existing milestone content (v1.36.0.a, v1.36.0.b, v1.36.0) is unchanged
  </verify>
  <done>MILESTONES.md convention is documented; no fabricated v1.37.1 entry; ordering difference from ROADMAP.md is explained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → planning files | Documentation-only edits; no executable code or secrets touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n21-01 | Tampering | REQUIREMENTS.md checkbox update | accept | Low-risk documentation update; no production code or secrets; changes are visible in git diff |
</threat_model>

<verification>
After all four tasks complete:

1. `grep -c '\[ \]' .planning/REQUIREMENTS.md` returns 0 (no unchecked v1 requirement boxes remain)
2. `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 0 (no Pending traceability rows)
3. `grep 'percent:' .planning/STATE.md` returns `percent: 67`
4. `grep '67%' .planning/STATE.md` confirms the body progress bar also shows 67%
5. `grep 'total=270' .planning/REQUIREMENTS.md` confirms CAT-05 fix applied
6. `grep 'not yet planned' .planning/ROADMAP.md` returns 2 lines (Phase 11 and Phase 12)
7. `grep 'shipped milestones only' .planning/MILESTONES.md` returns 1 line
</verification>

<success_criteria>
- All 18 findings from FINDINGS.md are addressed (F-01 through F-18)
- No CRITICAL inconsistencies remain (F-04, F-12, F-13 fully resolved)
- F-08 and F-09 annotated as "not yet planned" without fabricating plan counts
- F-06 Performance Metrics replaced with explicit "Not tracked" note (not placeholder dashes)
- F-07 truncated directory path annotated with inline comment
- F-11 and F-16 formatting conventions documented in-place without rewriting content
- STATE.md frontmatter percent and body progress bar agree at 67%
- REQUIREMENTS.md checkboxes and traceability truthfully reflect that Phases 7–10 are complete
</success_criteria>

<output>
After completion, create `.planning/quick/260420-n21-fix-all-18-inconsistencies-in-gsd-progre/260420-n21-SUMMARY.md` following the standard summary template.
</output>
