---
phase: quick-260604-r78
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/skill-frontmatter-contract.test.cjs
autonomous: true
requirements: [QUICK-260604-r78]

must_haves:
  truths:
    - "node --test tests/skill-frontmatter-contract.test.cjs passes all tests"
    - "The --research-phase proximity check anchors on the '## 2. Parse and Normalize Arguments' section heading, not a loose argument/args/flags token search"
    - "The proximity window is tight (~300 chars), not 3500"
    - "The test still fails if --research-phase is moved out of the genuine argument-parsing section"
  artifacts:
    - path: "tests/skill-frontmatter-contract.test.cjs"
      provides: "Robustly-anchored --research-phase proximity test"
  key_links:
    - from: "tests/skill-frontmatter-contract.test.cjs"
      to: "get-shit-done/workflows/plan-phase.md"
      via: "indexOf('## 2. Parse and Normalize Arguments') anchor"
      pattern: "Parse and Normalize Arguments"
---

<objective>
Fix the brittle anchor in the `--research-phase` proximity test (test "workflows/plan-phase.md parses --research-phase and sets a research-only mode", ~line 75-91 of tests/skill-frontmatter-contract.test.cjs).

Purpose: The current `argsIdx = content.search(/(?:argument|args?|flags?)\b/i)` grabs the FIRST loose token match — prose "args" at offset 4010 — which sits ~3255 chars above the real flag section. A prior fix masked this by widening the window to 3500. Anchor instead on the real structural section heading so the test fails for the right reason.

Output: Updated test file with a stable structural anchor and a tight window.
</objective>

<context>
@tests/skill-frontmatter-contract.test.cjs
@get-shit-done/workflows/plan-phase.md
</context>

<verified_anchor_facts>
Measured against get-shit-done/workflows/plan-phase.md (do not re-derive — these are confirmed):
- Loose `/(?:argument|args?|flags?)\b/i` first match: offset 4010 = prose "args" (false positive).
- Real `--research-phase` first occurrence: offset 7265.
- Stable anchor `## 2. Parse and Normalize Arguments` (literal): offset 7116 → distance to `--research-phase` = 149 chars.
- A window of 300 covers 149 with margin while staying far below the 3539-char gap to any unrelated downstream match.
- SECOND check (~line 166): `sectionIdx = content.indexOf('**`--research-phase')` is already a literal exact-string structural anchor (offset 7549), NOT a loose regex. It is robust — LEAVE IT UNCHANGED.
</verified_anchor_facts>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor the --research-phase proximity check on the section heading with a tight window</name>
  <files>tests/skill-frontmatter-contract.test.cjs</files>
  <read_first>
    - tests/skill-frontmatter-contract.test.cjs (lines 75-91 — the target test)
  </read_first>
  <action>
In the test "workflows/plan-phase.md parses --research-phase and sets a research-only mode" (~line 75-91):

1. Replace the loose-token search:
   `const argsIdx = content.search(/(?:argument|args?|flags?)\b/i);`
   with a literal structural anchor on the argument-parsing section heading:
   `const argsIdx = content.indexOf('## 2. Parse and Normalize Arguments');`
   This heading is the stable seam that introduces the flag enumeration (the `flags (\`--research\`, ...)` line and the `--research-phase` bullet live directly under it). It is immune to the prose "args" / `*_model_effort_arg` false positives that sit ~3.2k chars upstream.

2. Update the `assert.ok(argsIdx >= 0, ...)` message to reference the section heading rather than a generic "argument/flags section" (e.g. "plan-phase workflow must contain a '## 2. Parse and Normalize Arguments' section").

3. Replace the misleading 3500-char window block (the comment that explained the effort-arg derivation distance no longer applies). Set a tight window of 300 chars: `const argsWindow = content.slice(argsIdx, argsIdx + 300);` The real distance heading→`--research-phase` is 149 chars; 300 gives margin without reaching unrelated content.

4. Replace the stale 3500-window comment (lines ~82-85) with a concise note documenting why this anchor is stable: it pins to the literal section heading that introduces the flag list, so the prose word "args" near the top of the workflow can no longer be the anchor; the flag must appear within ~300 chars of that heading.

5. Update the final assertion message to reference the new window value (300 instead of 3500).

Do NOT touch get-shit-done/workflows/plan-phase.md — the workflow content is correct.

Per the task review constraint: the SECOND window-based check (~line 166, `sectionIdx = content.indexOf('**\`--research-phase')` with a 1200-char window) already uses a literal exact-string structural anchor, not a loose regex — confirmed robust. Leave it unchanged.
  </action>
  <verify>
    <automated>node --test tests/skill-frontmatter-contract.test.cjs</automated>
  </verify>
  <done>
All tests in tests/skill-frontmatter-contract.test.cjs pass. The `--research-phase` proximity test anchors on `indexOf('## 2. Parse and Normalize Arguments')` with a 300-char window, carries an inline comment explaining anchor stability, and the assertion message references 300. The second check (sectionIdx) is unchanged.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/skill-frontmatter-contract.test.cjs` → all pass.
- Grep confirms `argsIdx = content.indexOf('## 2. Parse and Normalize Arguments')` and no remaining `3500` literal in that test.
- Sanity: the anchor resolves to offset 7116 (real flags section), not 4010 (prose "args").
</verification>

<success_criteria>
- Brittle loose-token anchor replaced with a stable literal section-heading anchor.
- Window tightened from 3500 to 300.
- Inline comment documents why the anchor is stable.
- Second window-based check reviewed and confirmed robust (unchanged).
- Test still fails if `--research-phase` leaves the genuine argument-parsing section.
- All tests pass.
</success_criteria>

<output>
Create `.planning/quick/260604-r78-fix-brittle-anchor-in-research-phase-pro/260604-r78-SUMMARY.md` when done.
</output>
