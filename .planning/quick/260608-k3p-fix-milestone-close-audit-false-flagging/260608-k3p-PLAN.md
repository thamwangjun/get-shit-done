---
phase: quick-260608-k3p
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/audit.cjs
  - tests/audit.test.cjs
  - .planning/quick/260603-execute-phase-context-analysis/260603-execute-phase-context-analysis-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "audit-open reports 0 open quick tasks from repo root"
    - "A quick SUMMARY with `completed:` but no `status:` is treated as complete and not reported"
    - "A SUMMARY with neither `status:` nor `completed:` is still reported"
    - "A quick task dir with no SUMMARY is still reported as missing"
    - "npm test passes with the new regression test green"
  artifacts:
    - path: "get-shit-done/bin/lib/audit.cjs"
      provides: "scanQuickTasks completion predicate accepting `completed:` frontmatter"
    - path: "tests/audit.test.cjs"
      provides: "regression coverage for the completed/status/missing cases"
    - path: ".planning/quick/260603-execute-phase-context-analysis/260603-execute-phase-context-analysis-SUMMARY.md"
      provides: "minimal SUMMARY pointing at ANALYSIS.md deliverable"
  key_links:
    - from: "scanQuickTasks"
      to: "extractFrontmatter result"
      via: "completion predicate on fm.status OR fm.completed"
      pattern: "fm\\.completed"
---

<objective>
Fix the milestone-close artifact audit so completed quick tasks (which carry a
`completed: <date>` frontmatter field, not `status: complete`) stop being
false-flagged as incomplete. Clean up the one genuinely undocumented quick task.

Purpose: `/gsd-complete-milestone` pre-close audit currently reports 35 false
"incomplete" quick tasks because `scanQuickTasks` only recognizes
`status: complete`. The quick SUMMARY template emits `completed:`, so this
recurs at every milestone close. The detector is the single source of truth fix.
Output: Patched `audit.cjs`, regression test, and a backfilled SUMMARY for the
analysis-only outlier task.
</objective>

<context>
@get-shit-done/bin/lib/audit.cjs
@tests/helpers.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treat `completed:` frontmatter as complete in scanQuickTasks</name>
  <files>get-shit-done/bin/lib/audit.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/audit.cjs (focus scanQuickTasks ~lines 123-159)
  </read_first>
  <action>
    In `scanQuickTasks`, update the completion predicate so a quick task is
    skipped (treated COMPLETE) when EITHER frontmatter `status` is `complete`
    (case-insensitive — existing behavior) OR frontmatter has a truthy,
    non-empty `completed` field (the date-stamp convention quick SUMMARYs use,
    confirmed against templates/summary*.md which emit `completed: YYYY-MM-DD`).

    Concretely: at the block around line 137-138 where `fm` is extracted, also
    read `fm.completed`. Replace the line-142 skip `if (status === 'complete') continue;`
    so it also continues when `fm.completed` is a non-empty string after trimming.
    Guard against non-string `completed` values (treat only truthy non-empty
    trimmed strings as present). Keep `status` reported as-is for the still-open
    `unknown`/`missing`/`unreadable` cases. Preserve the `missing` and
    `unreadable` paths and all `requireSafePath`/`sanitizeForDisplay` behavior
    exactly. Follow fork conventions: 2-space indent, single quotes, inline
    comment citing the rationale.
  </action>
  <verify>
    <automated>cd /home/thamw/development/remote-dev/get-shit-done &amp;&amp; node get-shit-done/bin/gsd-tools.cjs query audit-open 2>&amp;1 | head -40</automated>
  </verify>
  <done>audit-open reports 0 open quick tasks once the outlier SUMMARY exists (Task 2); the only remaining flagged item before Task 2 is the analysis dir.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill SUMMARY for the analysis-only outlier task</name>
  <files>.planning/quick/260603-execute-phase-context-analysis/260603-execute-phase-context-analysis-SUMMARY.md</files>
  <action>
    The dir `.planning/quick/260603-execute-phase-context-analysis/` holds
    `ANALYSIS.md` (dated 2026-06-03) but no SUMMARY, so it is reported as
    `missing`. Create a minimal SUMMARY named
    `260603-execute-phase-context-analysis-SUMMARY.md` with YAML frontmatter
    that includes `completed: 2026-06-03` (the field the patched detector
    recognizes) plus a `phase: quick-260603-execute-phase-context-analysis`
    line, and a one-line body noting the deliverable is ANALYSIS.md (an
    analysis-only task that produced ANALYSIS.md rather than implementation).
    Do NOT add `status:`. Do NOT backfill any of the 34 historical SUMMARY files.
  </action>
  <verify>
    <automated>cd /home/thamw/development/remote-dev/get-shit-done &amp;&amp; node get-shit-done/bin/gsd-tools.cjs query audit-open 2>&amp;1 | head -20</automated>
  </verify>
  <done>audit-open from repo root reports 0 open quick tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test for completed-field detection</name>
  <files>tests/audit.test.cjs</files>
  <read_first>
    - tests/helpers.cjs (createTempDir, cleanup)
    - get-shit-done/bin/lib/audit.cjs (export names: scanQuickTasks / auditOpenArtifacts)
  </read_first>
  <behavior>
    - A quick-task SUMMARY with `completed: 2026-06-03` and NO `status:` is treated as complete and is NOT returned by scanQuickTasks/auditOpenArtifacts.
    - A quick-task SUMMARY with neither `status:` nor `completed:` IS returned (open).
    - A quick-task dir with no SUMMARY IS returned (status missing).
    - Existing `status: complete` behavior still skips the task.
  </behavior>
  <action>
    Create `tests/audit.test.cjs` (file is absent) using the Node built-in
    `--test` runner and `tests/helpers.cjs` (`createTempDir`, `cleanup`).
    Require the audit module from `get-shit-done/bin/lib/audit.cjs`; confirm the
    exact exported function name (`scanQuickTasks` and/or `auditOpenArtifacts`)
    and target whichever is exported. Build a temp planDir with a `quick/`
    subtree containing fixture task dirs for each behavior case above, write the
    SUMMARY fixtures, invoke the scanner, and assert membership/non-membership by
    slug. Follow fork conventions: CommonJS, kebab-case, 2-space indent, single
    quotes; clean up the temp dir in test teardown.
  </action>
  <verify>
    <automated>cd /home/thamw/development/remote-dev/get-shit-done &amp;&amp; node --test tests/audit.test.cjs 2>&amp;1 | tail -20</automated>
  </verify>
  <done>New test file passes; the completed/status/missing cases assert as specified.</done>
</task>

</tasks>

<verification>
- `cd /home/thamw/development/remote-dev/get-shit-done && node get-shit-done/bin/gsd-tools.cjs query audit-open` reports 0 open quick tasks.
- `cd /home/thamw/development/remote-dev/get-shit-done && npm test 2>&1 | tee /tmp/gsd-test-output.txt` passes with 0 new failures; `tests/audit.test.cjs` green.
</verification>

<success_criteria>
- scanQuickTasks treats `completed:` frontmatter as complete (in addition to `status: complete`).
- The analysis-only outlier has a SUMMARY with `completed: 2026-06-03`.
- Regression test covers completed / neither-field / no-SUMMARY cases.
- No `status:` backfilled into the 34 historical SUMMARY files.
</success_criteria>

<output>
Create `.planning/quick/260608-k3p-fix-milestone-close-audit-false-flagging/260608-k3p-SUMMARY.md` when done.
</output>
