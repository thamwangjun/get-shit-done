---
phase: quick
plan: 260530-6xt
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "Root cause of the failing test is identified and documented"
    - "Resolution options are presented to the user for a decision"
  artifacts: []
  key_links: []
---

<objective>
Investigate the failing test "import command loads doc-conflict-engine reference" in
tests/ingest-docs.test.cjs:297 and report findings with resolution options.

Purpose: The test is failing because of a bug in the test regex, not in the command file
being tested. The user needs a clear explanation and options before any fix is applied.

Output: A written findings report returned as the final agent message — no files created,
no source files modified.
</objective>

<context>
Read STATE.md for project context (already loaded by planner).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm findings and report root cause with resolution options</name>
  <files>tests/ingest-docs.test.cjs, commands/gsd/import.md</files>
  <action>
Read tests/ingest-docs.test.cjs lines 290–314 in full. Read commands/gsd/import.md in full.

Confirm the following:

1. The file under test — commands/gsd/import.md — contains the line:
   `<%~ include('get-shit-done/references/doc-conflict-engine.md') %>`
   which is syntactically correct Eta include syntax (closing `%>`).

2. The test regex on line 300 reads:
   `/<%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%\}/`
   The closing delimiter in the regex is `%\}` (escaped curly brace `}`), but the Eta
   closing tag is `%>` (angle bracket). These do not match. The test is the bug.

3. The legacy fallback check (line 299) uses string `.includes()` looking for
   `@~/.claude/get-shit-done/references/doc-conflict-engine.md`. This string is not
   present in the current command file (Phase 260525-o1n converted all @-notation to Eta
   includes). So both branches of the OR condition fail, causing the assertion to throw.

Then write a findings report directly as your output (no file creation). Structure it as:

## Findings: Failing Test — import command loads doc-conflict-engine reference

### Root Cause

The bug is in the test, not in the command file.

State the exact mismatch: regex uses `%\}` (closing curly brace) but Eta syntax closes
with `%>` (closing angle bracket). The command file line 27 is correct. The test regex
has a typo introduced when the test was written or modified.

### Evidence

Show the relevant lines from both files side-by-side to make the mismatch unambiguous.

### Resolution Options

Present three options:

**Option A — Fix the test regex (recommended)**
Change `%\}` to `%>` in the regex on tests/ingest-docs.test.cjs:300.
The corrected regex: `/<%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%>/.test(cmdContent)`
Impact: 1-line change to the test file only. The command file is already correct.

**Option B — Add the legacy @-notation back to the command file**
Add `@~/.claude/get-shit-done/references/doc-conflict-engine.md` back to import.md
so the legacy branch of the OR passes.
Impact: Reverts part of the Phase 260525-o1n Eta conversion for this one file.
Not recommended — the conversion was intentional and the Eta form is the correct form.

**Option C — Remove the test**
Delete or skip the "import command loads doc-conflict-engine reference" test case.
Impact: Loses the regression guard that was added in the #2387 refactor.
Not recommended unless the test is considered obsolete.

### Recommendation

Option A. The command file is correct. The test has a one-character typo (`}` vs `>`).
  </action>
  <verify>
    <automated>command grep -n 'include.*doc-conflict-engine' /home/thamw/development/remote-dev/get-shit-done/commands/gsd/import.md && command grep -n 'doc-conflict-engine' /home/thamw/development/remote-dev/get-shit-done/tests/ingest-docs.test.cjs</automated>
  </verify>
  <done>Findings report is returned as agent output text. Root cause identified, three options presented, recommendation stated. No files modified.</done>
</task>

</tasks>

<verification>
Run the failing test to confirm current state:
  node --test --test-name-pattern "import command loads doc-conflict-engine" tests/ingest-docs.test.cjs

Expected: test fails (confirms the bug exists). This is verification of the diagnosis, not of a fix.
</verification>

<success_criteria>
Root cause clearly stated: the test regex closing delimiter is `%\}` but should be `%>`.
Evidence shown: exact file lines from both test and command file.
Three resolution options presented with tradeoffs.
Recommendation given.
No source files modified.
</success_criteria>

<output>
Report returned as final agent message text — no SUMMARY.md, no file writes.
</output>
