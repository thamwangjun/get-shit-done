---
phase: quick-260610-d6m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/resolved/install-scanner-missing.md
  - .planning/debug/resolved/install-version-no-network.md
autonomous: true
requirements: []

must_haves:
  truths:
    - ".planning/debug/ contains no resolved session files (only resolved/ subdirectory remains)"
    - ".planning/debug/resolved/ contains both archived session files"
  artifacts:
    - path: ".planning/debug/resolved/install-scanner-missing.md"
      provides: "Archived resolved debug session"
    - path: ".planning/debug/resolved/install-version-no-network.md"
      provides: "Archived resolved debug session"
  key_links: []
---

<objective>
Archive two resolved debug sessions from .planning/debug/ into .planning/debug/resolved/ so that the debug directory only surfaces active/unresolved sessions.

Purpose: Keep .planning/debug/ clean — resolved sessions add noise when scanning for active investigations.
Output: Both session files moved to .planning/debug/resolved/; the source files no longer exist at their original paths.
</objective>

<execution_context>
<purpose>
Execute a quick-task plan and record the outcome in SUMMARY.md.
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
</required_reading>
</execution_context>

<context>
!`cat .planning/STATE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resolved directory and move both session files</name>
  <files>
    .planning/debug/resolved/install-scanner-missing.md
    .planning/debug/resolved/install-version-no-network.md
  </files>
  <action>
    Create the directory .planning/debug/resolved/ if it does not already exist.

    Move both files:
      mv .planning/debug/install-scanner-missing.md .planning/debug/resolved/install-scanner-missing.md
      mv .planning/debug/install-version-no-network.md .planning/debug/resolved/install-version-no-network.md

    No edits to file contents — this is a pure filesystem move operation.
  </action>
  <verify>
    Run:
      ls .planning/debug/
      ls .planning/debug/resolved/

    Expected:
    - .planning/debug/ lists only the resolved/ subdirectory (no .md files at the top level)
    - .planning/debug/resolved/ lists both install-scanner-missing.md and install-version-no-network.md
    - Neither original path exists: [ ! -f .planning/debug/install-scanner-missing.md ] and [ ! -f .planning/debug/install-version-no-network.md ]
  </verify>
  <done>
    .planning/debug/resolved/install-scanner-missing.md exists.
    .planning/debug/resolved/install-version-no-network.md exists.
    .planning/debug/install-scanner-missing.md does NOT exist.
    .planning/debug/install-version-no-network.md does NOT exist.
  </done>
</task>

</tasks>

<verification>
ls .planning/debug/ && ls .planning/debug/resolved/
[ ! -f .planning/debug/install-scanner-missing.md ] && echo "source 1 gone" || echo "FAIL: source 1 still exists"
[ ! -f .planning/debug/install-version-no-network.md ] && echo "source 2 gone" || echo "FAIL: source 2 still exists"
[ -f .planning/debug/resolved/install-scanner-missing.md ] && echo "dest 1 present" || echo "FAIL: dest 1 missing"
[ -f .planning/debug/resolved/install-version-no-network.md ] && echo "dest 2 present" || echo "FAIL: dest 2 missing"
</verification>

<success_criteria>
- .planning/debug/ contains no loose .md files — only the resolved/ subdirectory
- .planning/debug/resolved/ contains both install-scanner-missing.md and install-version-no-network.md
</success_criteria>

<output>
Create .planning/quick/260610-d6m-archive-resolved-debug-sessions/260610-d6m-SUMMARY.md when done.
</output>
