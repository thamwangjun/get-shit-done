---
phase: quick-260607-b0u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/docs-update.md
autonomous: true
requirements: [REG-1, REG-2, REG-3]
must_haves:
  truths:
    - "D-01: When --verify-only is present, preservation_check routes directly to verify_only_report and no generation step runs"
    - "D-02: TEXT_MODE is defined early enough that all subsequent AskUserQuestion calls in the workflow are covered"
    - "D-03: When --force is present, preservation_check treats all docs as mode: regenerate, not merely skipping the preservation prompt"
    - "No restored prose references the nonexistent detect_runtime_capabilities step; routing targets only real steps"
    - "npm test passes (agent-frontmatter + eta-template tests stay green)"
  artifacts:
    - path: "get-shit-done/workflows/docs-update.md"
      provides: "docs-update workflow with restored --verify-only routing, TEXT_MODE definition, and --force=regenerate semantics"
  key_links:
    - from: "preservation_check"
      to: "verify_only_report"
      via: "explicit --verify-only routing"
      pattern: "verify-only.*verify_only_report"
---

<objective>
Restore three functional regressions introduced by the prompt-compression pass on `get-shit-done/workflows/docs-update.md`, without undoing the rest of the compression and without touching any other file.

Purpose: The compressed workflow silently generates docs under `--verify-only`, lost the TEXT_MODE definition needed for non-Claude runtimes, and weakened `--force` to a mere skip instead of regenerate-all.
Output: Updated `get-shit-done/workflows/docs-update.md` with three restored behaviors using concise, positive/affirmative framing.
</objective>

<context>
@get-shit-done/workflows/docs-update.md
@.planning/quick/260607-b0u-restore-lost-functionality-in-compressed/260607-b0u-CONTEXT.md

Original pre-compression reference (read-only):
!`git show HEAD:get-shit-done/workflows/docs-update.md | sed -n '95,105p;344,392p'`

Real step list (route only to these): init_context, classify_project, build_doc_queue, resolve_modes, preservation_check, dispatch_wave_1, collect_wave_1, dispatch_wave_2, collect_wave_2, dispatch_monorepo_packages, sequential_generation, verify_docs, fix_loop, verify_only_report, scan_for_secrets, commit_docs, report.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore TEXT_MODE definition in build_doc_queue</name>
  <files>get-shit-done/workflows/docs-update.md</files>
  <read_first>
    The current `build_doc_queue` step (lines ~54-100) and the original TEXT_MODE prose:
    ```
    **Text mode (`workflow.text_mode: true` in config or `--text` flag):** Set `TEXT_MODE=true` if `--text` is present in `$ARGUMENTS` OR `text_mode` from init JSON is `true`. When TEXT_MODE is active, replace every `AskUserQuestion` call with a plain-text numbered list and ask the user to type their choice number. This is required for non-Claude runtimes (OpenAI Codex, Gemini CLI, etc.) where `AskUserQuestion` is not available.
    ```
  </read_first>
  <action>
    Restore the TEXT_MODE definition (per D-02) near the top of the `build_doc_queue` step, before the first `AskUserQuestion` call in that step, so every subsequent `AskUserQuestion` across the workflow is covered. Insert this concise affirmative paragraph:

    "**Text mode (`workflow.text_mode: true` in config or `--text` flag):** Set `TEXT_MODE=true` when `--text` is present in `$ARGUMENTS` OR `text_mode` from init JSON is `true`. When TEXT_MODE is active, replace every `AskUserQuestion` call with a plain-text numbered list and ask the user to type their choice number. Non-Claude runtimes (OpenAI Codex, Gemini CLI, etc.) require this because `AskUserQuestion` is unavailable there."

    Keep the existing "or text mode if unavailable" parenthetical as-is or tidy it to reference TEXT_MODE; the load-bearing change is adding the definition.
  </action>
  <verify>
    <automated>command grep -q 'Set `TEXT_MODE=true`' get-shit-done/workflows/docs-update.md</automated>
  </verify>
  <done>TEXT_MODE detection (--text or text_mode init flag) is defined in build_doc_queue with the plain-text-list substitution behavior for non-Claude runtimes.</done>
</task>

<task type="auto">
  <name>Task 2: Restore --force=regenerate and --verify-only routing in preservation_check</name>
  <files>get-shit-done/workflows/docs-update.md</files>
  <read_first>
    The current `preservation_check` step (lines ~163-184), specifically the first line "Skip if: `--force` present, `--verify-only` present, or no hand-written docs exist."
  </read_first>
  <action>
    Rewrite the skip/routing logic at the top of `preservation_check` as an ordered list of conditions (per D-03 and D-01), replacing the single weak "Skip if: ..." line:

    1. "When `--force` is present in `$ARGUMENTS`: treat all queued docs as mode: regenerate, then continue to dispatch_wave_1." (per D-03 — `--force` forces regenerate-all, not a mere skip)
    2. "When `--verify-only` is present in `$ARGUMENTS`: jump to verify_only_report and run no generation step (dispatch_wave_1/2, sequential_generation)." (per D-01 — verify-only must bypass all generation)
    3. "When no queued doc has `has_gsd_marker: false`: skip the preservation prompt and continue to dispatch_wave_1."

    Route only to real steps — use `dispatch_wave_1` as the post-check destination and `verify_only_report` for the verify-only jump. Do NOT reference `detect_runtime_capabilities` (no such step exists). Keep the existing per-doc AskUserQuestion block, the preserve/supplement/regenerate queue updates, and the fallback default-to-preserve behavior intact below the routing list.
  </action>
  <verify>
    <automated>command grep -qE 'verify-only.*verify_only_report' get-shit-done/workflows/docs-update.md && command grep -qi 'force.*regenerate' get-shit-done/workflows/docs-update.md && ! command grep -q 'detect_runtime_capabilities' get-shit-done/workflows/docs-update.md</automated>
  </verify>
  <done>preservation_check routes --force to regenerate-all then dispatch_wave_1, jumps --verify-only to verify_only_report bypassing generation, skips the prompt when no hand-written docs exist, and contains no detect_runtime_capabilities reference.</done>
</task>

<task type="auto">
  <name>Task 3: Verify test suite stays green</name>
  <files>get-shit-done/workflows/docs-update.md</files>
  <action>
    Confirm the edits introduce no regressions in the test suite (per the fork's frontmatter and eta-template constraints). Run the full suite once, pipe to a temp file, and read the result.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-b0u-test.txt | command grep -E '# (pass|fail)'</automated>
  </verify>
  <done>npm test reports zero failures; agent-frontmatter.test.cjs and eta-template tests pass.</done>
</task>

</tasks>

<verification>
- `command grep 'Set `TEXT_MODE=true`'` matches in docs-update.md (Task 1)
- `--verify-only` routes to verify_only_report; `--force` maps to regenerate; no `detect_runtime_capabilities` reference (Task 2)
- `npm test` passes with zero failures (Task 3)
</verification>

<success_criteria>
- TEXT_MODE defined in build_doc_queue covering all later AskUserQuestion calls
- preservation_check: --force = regenerate-all → dispatch_wave_1; --verify-only → verify_only_report (no generation); no-hand-written-docs → skip prompt
- Routing targets only real steps; detect_runtime_capabilities not reintroduced
- Restored prose is concise and uses positive/affirmative framing
- No other file modified; npm test green
</success_criteria>

<output>
This is a quick task — no SUMMARY.md required unless the quick workflow requests one.
</output>
