---
phase: quick-260531-m4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md]
autonomous: true
requirements: [QUICK-260531-m4s]
must_haves:
  truths:
    - "CLAUDE.md is ~40-50% shorter than the original 339 lines (target ~170-200 lines)"
    - "Every GSD marker pair from the original survives in the new file with identical names and source: attributes (7 pairs, 14 markers)"
    - "All load-bearing facts remain present exactly once, in their most appropriate location"
    - "The file is valid, readable, grammatical Markdown — not telegraphic like CLAUDE.compressed.md"
  artifacts:
    - path: CLAUDE.md
      provides: "Compressed, readable project guidance file with intact GSD markers"
  key_links:
    - from: CLAUDE.md
      to: "GSD content-injection tooling"
      via: "<!-- GSD:*-start ... --> / <!-- GSD:*-end --> HTML comment markers"
      pattern: "<!-- GSD:.*-(start|end)"
---

<objective>
Compress CLAUDE.md to a moderate, readable density — roughly 40-50% smaller than the current 339 lines — by collapsing the heavy duplication between the hand-written top half and the GSD-injected sections, while preserving every GSD marker and every operationally-important fact.

Purpose: The current CLAUDE.md repeats the same facts (four-layer architecture, tech stack, testing/frontmatter rules) in both the hand-written top and the auto-injected GSD blocks. Collapsing this duplication makes the file faster to read and cheaper to load without losing meaning.
Output: Overwritten CLAUDE.md (~170-200 lines). CLAUDE.original.md untouched as backup.
</objective>

<context>
@CLAUDE.md
@CLAUDE.compressed.md

- `CLAUDE.original.md` — preserved backup. DO NOT modify.
- `CLAUDE.compressed.md` — an OVER-aggressive experiment that strips articles/verbs into near-unreadable telegraphic text. Reference for what NOT to produce. DO NOT emulate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md at moderate, readable density</name>
  <files>CLAUDE.md</files>
  <read_first>
  - CLAUDE.md (read fully — this is the source to compress)
  - CLAUDE.compressed.md (reference for what NOT to do — too lossy/telegraphic)
  </read_first>
  <action>
Rewrite CLAUDE.md in place using the Edit/Write tool. Apply MODERATE compression: terse but fully grammatical English, ~40-50% fewer lines (target ~170-200 lines). Do NOT emulate CLAUDE.compressed.md's telegraphic article/verb-stripped style.

PRESERVE EXACTLY — all 7 GSD marker pairs (14 markers total), byte-for-byte including their `source:` attributes:
  `<!-- GSD:project-start source:PROJECT.md -->` / `<!-- GSD:project-end -->`
  `<!-- GSD:stack-start source:codebase/STACK.md -->` / `<!-- GSD:stack-end -->`
  `<!-- GSD:conventions-start source:CONVENTIONS.md -->` / `<!-- GSD:conventions-end -->`
  `<!-- GSD:architecture-start source:ARCHITECTURE.md -->` / `<!-- GSD:architecture-end -->`
  `<!-- GSD:skills-start source:skills/ -->` / `<!-- GSD:skills-end -->`
  `<!-- GSD:workflow-start source:GSD defaults -->` / `<!-- GSD:workflow-end -->`
  `<!-- GSD:profile-start -->` / `<!-- GSD:profile-end -->`
GSD tooling regenerates content between these markers — removing or altering a marker breaks the tooling.

COLLAPSE DUPLICATION (the main win): the hand-written top half and the GSD-injected sections repeat the same facts. Keep each load-bearing fact ONCE, in its most appropriate home. Concretely:
  - The four-layer architecture appears in the top "## Architecture" AND in the GSD:architecture block — keep the concise version in the top, and trim the GSD:architecture block to non-duplicative detail (or its terse essentials), but keep the markers.
  - The tech stack appears implicitly in the top and verbosely in GSD:stack — trim GSD:stack to the distinct, load-bearing items (languages, Node version, the single runtime dep, key env vars/config fields), dropping repetition.
  - Testing/frontmatter rules appear in the top "## Testing Conventions" AND in GSD:conventions — keep the rules once in the top, trim GSD:conventions to genuinely distinct conventions.
Trim the GSD blocks' internal content aggressively where it duplicates; preserve the markers regardless.

MUST PRESERVE these facts (trim wording, do not drop the fact):
  - `npm test`, `npm run test:coverage`, `npm run build:hooks` commands (and that build:hooks is required before npm publish)
  - The single-test-file command and Node >=20/Node >=22 runtime requirement
  - The four-layer architecture summary (Commands → Workflows → Agents → CLI tools) and the data-flow arrow
  - "context rot" / fresh 200K context window per agent rationale
  - The agent-frontmatter.test.cjs rules: no `skills:` in frontmatter, file-writing agents must include "Only use the Write tool" + commented `# hooks:`, workflows/commands spawn via `subagent_type:`, add new agent to the valid list
  - The fork's positive-framing replacement rule and the fork's core value (every file meets the quality bar before shipping; upstream content modified not accepted verbatim)
  - The Key Source Files table — may be trimmed (drop the most peripheral rows, keep core: install.js, state.cjs, phase.cjs, verify.cjs, init.cjs, core.cjs, config.cjs, frontmatter.cjs, gsd-tools.cjs entry, hooks/, references/, templates/)
  - config.json key settings: `mode` (yolo/interactive), `parallelization.enabled`, absent=enabled pattern, model profiles (Opus planning / Sonnet execution)
  - The "Reading Files" truncation guidance and the "Running tests efficiently" tip (may be condensed to one line each)
  - The GSD Workflow Enforcement block (inside its markers) — keep its entry points (/gsd-quick, /gsd-debug, /gsd-execute-phase) and the bypass note
  - Deep-Dive References list (may be condensed)

Keep valid Markdown with coherent heading structure. The GSD:skills and GSD:profile blocks are short placeholders — keep markers, keep one terse line of content each.
  </action>
  <verify>
  <automated>command grep -c '<!-- GSD:.*-start' CLAUDE.md | command grep -qx 7 && command grep -c '<!-- GSD:.*-end' CLAUDE.md | command grep -qx 7 && test "$(wc -l < CLAUDE.md)" -le 205 && test "$(wc -l < CLAUDE.md)" -ge 150 && echo PASS</automated>
  </verify>
  <done>
  - CLAUDE.md exists and is valid Markdown
  - All 7 start markers and 7 end markers present with identical names + source: attributes (diff the marker lines against CLAUDE.original.md to confirm)
  - Line count between ~150 and ~205 (40-50% reduction from 339)
  - All MUST-PRESERVE facts present; prose is grammatical and readable (not telegraphic)
  - CLAUDE.original.md unchanged
  </done>
</task>

</tasks>

<verification>
Confirm marker parity against the backup and the line-count reduction:

```bash
# Markers must match the original exactly (names + source: attributes)
diff <(command grep '<!-- GSD:' CLAUDE.original.md) <(command grep '<!-- GSD:' CLAUDE.md) && echo "MARKERS MATCH"
# Line count dropped ~40-50%
echo "original: $(wc -l < CLAUDE.original.md)  new: $(wc -l < CLAUDE.md)"
```
</verification>

<success_criteria>
- CLAUDE.md is ~40-50% shorter (target ~170-200 lines, hard bound 150-205)
- `diff` of GSD marker lines vs CLAUDE.original.md is empty (perfect marker parity)
- Every load-bearing fact listed in the action is present exactly once
- Prose is terse but grammatical and readable — distinctly unlike CLAUDE.compressed.md
- CLAUDE.original.md untouched
</success_criteria>

<output>
Overwrite `CLAUDE.md` in place. No SUMMARY required beyond the quick-workflow default.
</output>
