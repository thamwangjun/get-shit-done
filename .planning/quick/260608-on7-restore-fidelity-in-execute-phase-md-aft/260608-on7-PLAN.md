---
phase: quick-260608-on7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
autonomous: true
requirements:
  - QUICK-260608-on7

must_haves:
  truths:
    - CODEX RUNTIME stop-working rule appears OUTSIDE and AFTER the Agent() closing paren in execute_waves (not inside the prompt= string)
    - CODEX RUNTIME stop-working rule appears OUTSIDE and AFTER the Agent() closing paren in verify_phase_goal
    - check_blocking_antipatterns three questions each carry their sub-clauses (own-words, failure explanation, concrete-step)
    - available_agent_types list carries enforcement preamble directing use of exact names
    - Each item A-I is either confirmed-fixed or confirmed-absent (verified via diff, not assumed)
  artifacts:
    - path: get-shit-done/workflows/execute-phase.md
      provides: Restored execute-phase workflow
  key_links:
    - from: task diff analysis
      to: exact reference content
      via: git show 57a000b1
      pattern: "CODEX RUNTIME"
---

<objective>
Restore fidelity in get-shit-done/workflows/execute-phase.md after prompt compression, by diffing HEAD against reference commit 57a000b1, independently verifying each alleged regression, and making minimal targeted fixes.

Purpose: HEAD is a ~911-line compressed version of a 1722-line reference. Nine specific regressions were alleged. Each must be independently confirmed from the diff before any edit is made — alleged regressions that do not appear in the diff are skipped.

Output: get-shit-done/workflows/execute-phase.md with all confirmed regressions restored, committed atomically.
</objective>

<execution_context>
<purpose>
Execute this quick task plan and produce SUMMARY.md.
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
</required_reading>
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Diff, verify, and restore confirmed regressions</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <action>
    Step 1 — Produce the reference file and diff:

    ```bash
    git show 57a000b1ad818c1837b2b128011c94149221fd1e:get-shit-done/workflows/execute-phase.md > /tmp/ref.md
    diff /tmp/ref.md get-shit-done/workflows/execute-phase.md > /tmp/ep-diff.txt
    wc -l /tmp/ref.md get-shit-done/workflows/execute-phase.md
    ```

    Read the full diff output. This is your ground truth — only fix content that the diff confirms is missing or changed.

    Step 2 — Verify each alleged regression independently against the diff. For each item, search both HEAD and reference for the key text to confirm presence/absence:

    A. CODEX RUNTIME rule in execute_waves:
       Search reference for "ORCHESTRATOR RULE" and "CODEX RUNTIME" near the execute_waves Agent() dispatch. Confirm where it appears relative to the Agent() closing paren. Check HEAD for the same text and its location.

    B. CODEX RUNTIME rule in verify_phase_goal:
       Same check for the gsd-verifier Agent() call section.

    C. check_blocking_antipatterns three sub-clauses:
       Search reference for "Describe it in your own words", "Explain the specific failure", "concrete step". Check HEAD for same.

    D. available_agent_types enforcement preamble:
       Search reference for "Always use the exact name from this list". Check HEAD for same.

    E. Cross-phase context files paragraph in initialize:
       Search reference for the paragraph describing SUMMARY.md/CONTEXT.md/RESEARCH.md/REQUIREMENTS.md as always-provided vs conditional files.

    F. Sequential dispatch CORRECT/WRONG contrast:
       Search reference for "# CORRECT: one Agent() per message" and "# WRONG: multiple Agent() calls". Check HEAD.

    G. Wave-close heartbeat ordering:
       Search reference for heartbeat firing order relative to spot-checks and "## Wave N Complete". Check HEAD.

    H. close_parent_artifacts detail:
       Search reference for numbered steps (1-6) with find-phase SDK call and specific commit --files paths. Compare HEAD's version.

    I. AskUserQuestion directive in regression_gate:
       Search reference for "Use AskUserQuestion to present the options." at end of the failure branch.

    Step 3 — For each CONFIRMED regression (text present in reference, absent or wrong in HEAD):
    Use the Edit tool to restore the exact content from the reference. Do not rewrite, paraphrase, or expand beyond what the reference contains. Each edit inserts or replaces precisely the missing content.

    For items NOT confirmed by the diff (reference and HEAD match, or HEAD has equivalent content):
    Skip — make no edit. Note the item as "not reproduced" in the commit message body.

    Step 4 — After all edits, verify the key fixes are present:

    ```bash
    grep -n "CODEX RUNTIME" get-shit-done/workflows/execute-phase.md
    grep -n "Describe it in your own words" get-shit-done/workflows/execute-phase.md
    grep -n "Always use the exact name from this list" get-shit-done/workflows/execute-phase.md
    grep -n "CORRECT: one Agent" get-shit-done/workflows/execute-phase.md
    grep -n "AskUserQuestion" get-shit-done/workflows/execute-phase.md
    wc -l get-shit-done/workflows/execute-phase.md
    ```

    Expected: line count increases from ~911 toward ~1000-1100 (exact count depends on which regressions are confirmed).

    Step 5 — Commit all fixes atomically:

    ```bash
    git add get-shit-done/workflows/execute-phase.md
    git commit -m "fix(quick-260608-on7): restore confirmed fidelity losses in execute-phase.md

    Verified each alleged regression against diff of HEAD vs 57a000b1.
    Restored exact content from reference for confirmed items.
    Skipped items not reproduced by the diff.

    Authored by: Tham Wang Jun <6513021+thamwangjun@users.noreply.github.com>

    Co-Authored-By: Claude <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>grep -c "CODEX RUNTIME" get-shit-done/workflows/execute-phase.md && echo "CODEX RUNTIME present"</automated>
  </verify>
  <done>
    All confirmed regressions (items A-I that were found missing by diff) are restored with exact reference content. Items not reproduced by the diff are left untouched. Single atomic commit made. Line count is higher than 911.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SUMMARY.md and update state</name>
  <files>.planning/quick/260608-on7-restore-fidelity-in-execute-phase-md-aft/260608-on7-SUMMARY.md</files>
  <action>
    Create SUMMARY.md at .planning/quick/260608-on7-restore-fidelity-in-execute-phase-md-aft/260608-on7-SUMMARY.md documenting:
    - Which items (A-I) were confirmed and fixed
    - Which items were not reproduced (not in diff)
    - Final line count of execute-phase.md
    - Commit hash

    Then update STATE.md Quick Tasks Completed table with a new row for this task (260608-on7).

    Commit metadata:
    ```bash
    git add .planning/quick/260608-on7-restore-fidelity-in-execute-phase-md-aft/260608-on7-SUMMARY.md .planning/STATE.md
    git commit -m "docs(quick-260608-on7): complete 260608-on7 state record

    Authored by: Tham Wang Jun <6513021+thamwangjun@users.noreply.github.com>

    Co-Authored-By: Claude <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>test -f .planning/quick/260608-on7-restore-fidelity-in-execute-phase-md-aft/260608-on7-SUMMARY.md && echo "SUMMARY exists"</automated>
  </verify>
  <done>SUMMARY.md committed and STATE.md updated with quick task entry for 260608-on7.</done>
</task>

</tasks>

<verification>
grep -c "CODEX RUNTIME" get-shit-done/workflows/execute-phase.md
grep -c "Describe it in your own words" get-shit-done/workflows/execute-phase.md
wc -l get-shit-done/workflows/execute-phase.md
</verification>

<success_criteria>
- All confirmed regressions (per diff vs 57a000b1) are restored with exact reference content
- CODEX RUNTIME stop-working rule present in execute_waves (outside Agent() paren)
- check_blocking_antipatterns sub-clauses present if confirmed missing
- available_agent_types enforcement preamble present if confirmed missing
- Single atomic fix commit + single docs commit
- SUMMARY.md created, STATE.md updated
</success_criteria>

<output>
Create .planning/quick/260608-on7-restore-fidelity-in-execute-phase-md-aft/260608-on7-SUMMARY.md when done.
</output>
