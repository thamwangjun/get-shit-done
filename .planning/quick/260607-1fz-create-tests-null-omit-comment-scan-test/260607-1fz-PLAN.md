---
phase: quick-260607-1fz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/null-omit-comment-scan.test.cjs
autonomous: true
requirements:
  - QUICK-260607-1fz
must_haves:
  truths:
    - "tests/null-omit-comment-scan.test.cjs exists and passes on current codebase"
    - "Every standalone effort={*_effort_arg} line inside an Agent() invocation that lacks '# omit this line when' is reported as a violation"
    - "Inline (non-standalone) effort= occurrences in known workflow files are NOT flagged"
  artifacts:
    - path: "tests/null-omit-comment-scan.test.cjs"
      provides: "Regression guard for null-omit comment convention"
      min_lines: 80
  key_links:
    - from: "tests/null-omit-comment-scan.test.cjs"
      to: "agents/, get-shit-done/workflows/, commands/"
      via: "collectMarkdownFiles + paren-depth state machine"
      pattern: "effort=\\{[A-Za-z_][A-Za-z_0-9]*_effort_arg\\}"
---

<objective>
Create tests/null-omit-comment-scan.test.cjs — a regression guard that fails if any
standalone effort={*_effort_arg} line inside an Agent() invocation is missing its
'# omit this line when' null-omit comment.

Purpose: Guards against regressions to the convention established in quick task 260607-0kd.
Output: tests/null-omit-comment-scan.test.cjs, passing on current codebase, committed.
</objective>

<execution_context>
<purpose>
Execute a quick task plan and commit the result.
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
</required_reading>
</execution_context>

<context>
!`cat .planning/STATE.md`
@tests/bare-effort-arg-scan.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/null-omit-comment-scan.test.cjs</name>
  <files>tests/null-omit-comment-scan.test.cjs</files>
  <action>
Create `/Users/thamw/development/local/get-shit-done/tests/null-omit-comment-scan.test.cjs` with the following exact structure:

1. `'use strict';` at top
2. Header line: `// allow-test-rule: source-text-is-the-product`
3. Module-level JSDoc block: guards the null-omit comment convention added in quick task 260607-0kd; explains that a standalone effort= line without the comment means the AI won't know to skip the parameter when the variable is null.
4. Require exactly: `require('node:test')` (destructure `{ describe, test }`), `require('node:assert/strict')`, `require('node:fs')`, `require('node:path')`.
5. `const ROOT = path.join(__dirname, '..')`.
6. `SCAN_DIRS` array: `['agents', 'get-shit-done/workflows', 'commands/gsd']` (same dirs as bare-effort-arg-scan).
7. Copy the `collectMarkdownFiles` function VERBATIM from `tests/bare-effort-arg-scan.test.cjs` (lines 36–52 of that file). Do not rename it or alter the implementation.
8. Collect `ALL_FILES` using the same pattern as bare-effort-arg-scan (lines 54–57).
9. Write a `scanForMissingNullOmitComment(filePath, content)` function that:
   a. Splits content into lines and runs the paren-depth state machine VERBATIM from `scanForBareEffortArg` in bare-effort-arg-scan.test.cjs — the char-by-char `Agent(` detection, depth increment/decrement, and `inAgentRanges` building logic must be identical.
   b. For each line where depth > 0 (i.e., inside Agent()), check if the full line matches `/^\s*effort=\{([A-Za-z_][A-Za-z_0-9]*_effort_arg)\}/` (standalone effort= line anchor).
   c. If it matches AND the line does NOT contain the substring `# omit this line when`: record a violation `{ file: filePath, line: lineIdx + 1, col: line.indexOf('effort=') + 1, token: matched capture group (the varname) }`.
   d. Return the violations array.
10. Single `describe` block named exactly: `'null-omit-comment-scan: effort= lines must carry null-omit comment'`
11. Single `test` inside it named exactly: `'every standalone effort={*_effort_arg} in Agent invocations has # omit this line when'`
12. Test body: iterate ALL_FILES, call `scanForMissingNullOmitComment(relPath, content)` for each, aggregate violations, then `assert.equal(violations.length, 0, ...)` with failure message formatted as:
    `Standalone effort= lines found inside Agent() invocations without null-omit comment.\nFix: add  # omit this line when <condition>\n` followed by each violation as `  file:line:col — effort={token} missing null-omit comment`.

The standalone regex `/^\s*effort=\{([A-Za-z_][A-Za-z_0-9]*_effort_arg)\}/` uses a `^` anchor at the start, so lines like `Agent(model="x", effort={foo_effort_arg}, ...)` on a single line will NOT match (the line doesn't start with optional whitespace then `effort=`). This correctly excludes the known inline cases in code-review-fix.md, new-milestone.md, and discuss-phase-assumptions.md without any special-case exclusion list.
  </action>
  <verify>
    <automated>node --test --test-name-pattern='null-omit-comment-scan' tests/null-omit-comment-scan.test.cjs</automated>
  </verify>
  <done>Test file exists, runs without error, and the single test passes (zero violations on current codebase).</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and commit</name>
  <files>tests/null-omit-comment-scan.test.cjs</files>
  <action>
Run the full test suite to confirm the new file introduces no regressions:

  npm test

If all tests pass, commit with message:
  `test(quick-260607-1fz): add null-omit-comment-scan regression guard`

Stage only `tests/null-omit-comment-scan.test.cjs`.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>npm test passes. New file committed with the specified message.</done>
</task>

</tasks>

<verification>
node --test --test-name-pattern='null-omit-comment-scan' tests/null-omit-comment-scan.test.cjs
npm test
</verification>

<success_criteria>
- tests/null-omit-comment-scan.test.cjs exists
- The single test inside passes with zero violations on the current codebase
- npm test continues to pass (no regressions)
- File committed with message: test(quick-260607-1fz): add null-omit-comment-scan regression guard
</success_criteria>

<output>
Create .planning/quick/260607-1fz-create-tests-null-omit-comment-scan-test/260607-1fz-SUMMARY.md when done.
</output>
