---
phase: quick
plan: 260530-710
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ingest-docs.test.cjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "The regex on line 300 of tests/ingest-docs.test.cjs uses %> as the closing delimiter"
    - "npm test passes with no failures in ingest-docs.test.cjs"
  artifacts:
    - path: "tests/ingest-docs.test.cjs"
      provides: "Corrected Eta tag closer regex"
      contains: "%>"
  key_links:
    - from: "tests/ingest-docs.test.cjs"
      to: "get-shit-done/references/doc-conflict-engine.md"
      via: "Eta include tag regex match"
      pattern: "<%~.*doc-conflict-engine.*%>"
---

<objective>
Fix one-character typo in the closing Eta tag delimiter on line 300 of the test file.

Purpose: The regex currently uses `%\}` (curly brace) instead of `%>` (angle bracket), causing the test to never match a valid Eta include tag and always fail.
Output: tests/ingest-docs.test.cjs with corrected regex.
</objective>

<context>
!`cat .planning/STATE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix closing Eta tag delimiter in ingest-docs.test.cjs line 300</name>
  <files>tests/ingest-docs.test.cjs</files>
  <action>
    Read tests/ingest-docs.test.cjs to confirm the current content of line 300, then apply a surgical one-character edit:

    Change `%\}` to `%>` in the hasEtaRef regex on line 300.

    Before:
      const hasEtaRef = /<%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%\}/.test(cmdContent);

    After:
      const hasEtaRef = /<%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%>/.test(cmdContent);

    No other lines or files are touched.
  </action>
  <verify>
    <automated>node --test tests/ingest-docs.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>tests/ingest-docs.test.cjs line 300 contains `%>` (not `%\}`), and running `node --test tests/ingest-docs.test.cjs` exits with code 0.</done>
</task>

</tasks>

<verification>
node --test tests/ingest-docs.test.cjs
</verification>

<success_criteria>
All tests in tests/ingest-docs.test.cjs pass. The regex on line 300 ends with `%>`.
</success_criteria>

<output>
No new files created. tests/ingest-docs.test.cjs modified in place.
</output>
