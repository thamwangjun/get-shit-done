---
phase: quick
plan: 260530-710
subsystem: tests
tags: [bugfix, regex, eta]
key-files:
  modified:
    - tests/ingest-docs.test.cjs
decisions:
  - "One-character fix: replaced %\\} with %> in the Eta closing delimiter regex on line 300"
metrics:
  duration: "< 1 minute"
  completed: "2026-05-30"
---

# Quick 260530-710: Fix Eta closing delimiter regex in ingest-docs.test.cjs

One-character typo corrected: `%\}` → `%>` in the `hasEtaRef` regex on line 300.

## What Was Done

Fixed the closing Eta tag delimiter in `tests/ingest-docs.test.cjs` line 300. The regex used `%\}` (curly brace) instead of `%>` (angle bracket), causing the `import command loads doc-conflict-engine reference` test to never match a valid Eta include tag.

## Commit

- `5e0fd08b`: fix(260530-710): correct Eta closing delimiter in ingest-docs regex

## Verification

`node --test tests/ingest-docs.test.cjs` exits with code 0: 37 pass, 0 fail, 3 skipped.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- File modified: tests/ingest-docs.test.cjs (confirmed)
- Commit exists: 5e0fd08b (confirmed)
- Tests pass: 37/37, 0 failures
