---
phase: quick-260608-m6w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent-frontmatter.test.cjs
  - tests/debug-session-management.test.cjs
  - .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - HDOC suite in agent-frontmatter.test.cjs is active (not skipped) and asserts /always use the write tool/i
    - Duplicate per-agent assertion removed from debug-session-management.test.cjs
    - Fork decision recorded in UPSTREAM_TO_FORK_CHANGES_GUIDE.md under Scanner precedence rule section
    - npm test passes with no regressions
  artifacts:
    - path: tests/agent-frontmatter.test.cjs
      provides: Active HDOC suite with correct phrasing
    - path: tests/debug-session-management.test.cjs
      provides: Duplicate test removed
    - path: .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
      provides: Fork decision documented
  key_links:
    - from: tests/agent-frontmatter.test.cjs
      to: agents/gsd-*.md
      via: FILE_WRITING_AGENTS filter + /always use the write tool/i regex
      pattern: "always use the write tool"
---

<objective>
Fix the anti-heredoc test suite to reflect the fork's positive-framing rule ("Always use the Write tool" vs upstream's "Only use the Write tool"), remove the now-redundant per-agent duplicate in debug-session-management.test.cjs, and record the fork decision in the upstream changes guide.

Purpose: The HDOC suite is currently skipped (`describe.skip`) and asserts the wrong phrasing. The fork's positive-framing replacement rule requires "Always use the Write tool"; the test must validate fork behavior, not upstream behavior. The duplicate assertion in debug-session-management.test.cjs will drift again on the next upstream merge — agent-frontmatter.test.cjs is the single owner.

Output: Active HDOC test suite, de-duplicated debug test, documented fork decision, clean test run.
</objective>

<execution_context>
<purpose>
Execute a quick task plan and commit the result.
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
</required_reading>
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Fix agent-frontmatter.test.cjs HDOC suite and remove debug-session-management duplicate</name>
  <files>tests/agent-frontmatter.test.cjs, tests/debug-session-management.test.cjs</files>
  <action>
    In tests/agent-frontmatter.test.cjs:
    - Line 38: change `describe.skip(` to `describe(` to activate the HDOC suite.
    - Line 43: change the regex from `/only use the write tool/i` to `/always use the write tool/i`.
    - Line 44: change the assertion message from `${agent} missing 'Only use the Write tool' instruction` to `${agent} missing 'Always use the Write tool' instruction`.

    In tests/debug-session-management.test.cjs:
    - Delete the entire 3-line test block at lines 141–143 (inclusive):
        test('gsd-debug-session-manager includes anti-heredoc rule', () => {
          assert.ok(/only use the write tool/i.test(sessionManager), 'session manager missing anti-heredoc rule');
        });
    Reason: agent-frontmatter.test.cjs is the single owner of this invariant for all file-writing agents. A per-agent duplicate in debug-session-management.test.cjs will drift on the next upstream merge.
  </action>
  <verify>
    <automated>node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "HDOC|pass|fail|skip"</automated>
  </verify>
  <done>HDOC suite runs (not skipped), asserts /always use the write tool/i, and passes for all file-writing agents. The duplicate test is absent from debug-session-management.test.cjs.</done>
</task>

<task type="auto">
  <name>Task 2: Record fork decision in UPSTREAM_TO_FORK_CHANGES_GUIDE.md</name>
  <files>.planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md</files>
  <action>
    Append the following subsection into .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md after line 232 (the "Scanner precedence rule" paragraph body, ending "Tests should validate fork behavior, not upstream behavior.") and before the `---` separator that precedes "Category 10: Planning Artifacts".

    Insert this block between the Scanner precedence rule paragraph and the `---` separator:

    ### Anti-heredoc phrasing: "Always use the Write tool"

    Upstream uses the phrasing "Only use the Write tool" in file-writing agent prompts and the matching test assertion `/only use the write tool/i` in `agent-frontmatter.test.cjs`. The fork's positive-framing replacement rule rewrites this to "Always use the Write tool".

    **Test consequence:** The `HDOC: anti-heredoc instruction` suite in `agent-frontmatter.test.cjs` asserts `/always use the write tool/i` (not the upstream form). Per-agent tests in other test files (e.g. `debug-session-management.test.cjs`) must not duplicate this assertion — `agent-frontmatter.test.cjs` is the single owner. If a per-agent duplicate appears after an upstream merge, remove it rather than update it.
  </action>
  <verify>
    <automated>grep -n "Always use the Write tool" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md</automated>
  </verify>
  <done>The Anti-heredoc phrasing subsection appears in UPSTREAM_TO_FORK_CHANGES_GUIDE.md between the Scanner precedence rule paragraph and the Category 10 separator.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and commit</name>
  <files></files>
  <action>
    Run the full test suite: `npm test 2>&1 | tee /tmp/gsd-test-output.txt`

    Confirm:
    - HDOC suite runs (not skipped) and passes.
    - No regressions in any other test file.
    - Exit code is 0.

    If any test fails, diagnose and fix before committing.

    Commit all three changed files in a single atomic commit:
      git add tests/agent-frontmatter.test.cjs tests/debug-session-management.test.cjs .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
      git commit -m "test: fix anti-heredoc assertion phrasing (always → only) and record fork decision"
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt; echo "Exit: $?"</automated>
  </verify>
  <done>All tests pass (exit code 0), HDOC suite is active and green, commit exists with all three files.</done>
</task>

</tasks>

<verification>
npm test 2>&1 | tee /tmp/gsd-test-output.txt
grep -E "HDOC|skip" /tmp/gsd-test-output.txt
</verification>

<success_criteria>
- HDOC suite in agent-frontmatter.test.cjs is active (not skipped) and uses /always use the write tool/i
- Duplicate test removed from debug-session-management.test.cjs
- Fork decision subsection present in UPSTREAM_TO_FORK_CHANGES_GUIDE.md
- npm test exits 0 with no regressions
- Single commit covering all three files
</success_criteria>

<output>
No separate summary file required for quick tasks. Confirm completion inline.
</output>
