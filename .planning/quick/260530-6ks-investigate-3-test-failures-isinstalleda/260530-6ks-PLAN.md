---
phase: quick
plan: 260530-6ks
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - Root cause of isInstalledAheadOfLatest failure is documented with file and line evidence
    - Root cause of import command doc-conflict-engine test failure is documented with file and line evidence
  artifacts:
    - path: ".planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md"
      provides: "Root cause analysis for both test failures"
  key_links: []
---

<objective>
Investigate two test failures and document their root causes. No code changes.

Purpose: Identify exactly why each test fails so a fix plan can be written with full context.
Output: FINDINGS.md with root cause analysis for each failure.
</objective>

<context>
!`cat .planning/STATE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose isInstalledAheadOfLatest missing export</name>
  <files>.planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md</files>
  <action>
Read hooks/gsd-statusline.js in full. Confirm that `isInstalledAheadOfLatest` is absent from the `module.exports` block at line 488-492, which exports only: `readGsdState`, `parseStateMd`, `formatGsdState`, `readGsdConfig`, `getConfigValue`, `readLastSlashCommand`, `composeStatusline`. The function does not exist anywhere in the file.

Read tests/gsd-statusline.test.cjs lines 18-23 and 225-233 to confirm the test imports `isInstalledAheadOfLatest` from `../hooks/gsd-statusline.js` and calls it with semver-style strings like `('1.2.1-beta.1', '1.2.0')`.

Determine: the function is missing from both the source file (not defined anywhere) and the exports. This is a gap between what the test expects and what the hook provides. The test was likely written ahead of an implementation that was never added, or the implementation was removed.

Search for any prior version of `isInstalledAheadOfLatest` in the file with: grep -n "isInstalledAheadOfLatest" hooks/gsd-statusline.js
  </action>
  <verify>grep -n "isInstalledAheadOfLatest" /home/thamw/development/remote-dev/get-shit-done/hooks/gsd-statusline.js || echo "NOT FOUND IN SOURCE"</verify>
  <done>Confirmed whether isInstalledAheadOfLatest exists anywhere in the source file, and what the exports block contains.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnose import command doc-conflict-engine test regex mismatch</name>
  <files>.planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md</files>
  <action>
Read tests/ingest-docs.test.cjs lines 291-308 to confirm the test regex. The test at line 300 uses: `/\{%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%\}/`

Read commands/gsd/import.md to see the actual include syntax used. Per the investigation context, the file uses `<%~ include(...)` (angle bracket form), not `{%~ include(...)` (curly brace form).

CLAUDE.md states: "Only `<%~` include syntax allowed; `{%~` is banned; enforced by eta-template-syntax.test.cjs"

The test regex checks for `{%~` but the file (correctly, per CLAUDE.md) uses `<%~`. The test regex was written with the wrong Eta delimiter. The fix is to update the test regex from `\{%~` to `<%~` — the source file `commands/gsd/import.md` is correct.

Confirm the exact line in import.md that includes doc-conflict-engine.md and what delimiter it uses.
  </action>
  <verify>command grep -n "doc-conflict-engine\|include" /home/thamw/development/remote-dev/get-shit-done/commands/gsd/import.md</verify>
  <done>Confirmed the exact include syntax in import.md and identified the mismatch with the test regex.</done>
</task>

<task type="auto">
  <name>Task 3: Write FINDINGS.md with root causes and fix recommendations</name>
  <files>.planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md</files>
  <action>
Create FINDINGS.md documenting both root causes based on what was found in Tasks 1 and 2.

Structure:

## Failure 1: isInstalledAheadOfLatest is not a function
- Test: tests/gsd-statusline.test.cjs lines 225-233
- Import: line 22 imports `isInstalledAheadOfLatest` from `../hooks/gsd-statusline.js`
- Root cause: The function `isInstalledAheadOfLatest` is not defined anywhere in hooks/gsd-statusline.js and is not in the module.exports block (lines 488-492). The test calls it with semver-style strings `('1.2.1-beta.1', '1.2.0')` but the hook only exports 7 functions: readGsdState, parseStateMd, formatGsdState, readGsdConfig, getConfigValue, readLastSlashCommand, composeStatusline.
- Fix options:
  A) Implement `isInstalledAheadOfLatest(installed, latest)` in gsd-statusline.js and add to exports — compares semver prerelease to stable base (installed prerelease patch > latest = true, same base version = false)
  B) Remove or skip the two tests if this function was intentionally removed

## Failure 2: /gsd-import must load the shared conflict-engine contract
- Test: tests/ingest-docs.test.cjs lines 297-301
- Root cause: The test regex at line 300 checks for `{%~` Eta delimiter, but CLAUDE.md mandates `<%~` as the only valid syntax. commands/gsd/import.md correctly uses `<%~ include('get-shit-done/references/doc-conflict-engine.md') %>` (angle bracket form). The test was written or left with the banned `{%~` delimiter in the regex.
- Fix: Update the test regex at line 300 from `\{%~` to `<%~` (change opening brace to angle bracket). The source file import.md is correct and should not be changed.
  </action>
  <verify>test -f /home/thamw/development/remote-dev/get-shit-done/.planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md && echo "FINDINGS.md created"</verify>
  <done>FINDINGS.md exists with documented root causes and fix recommendations for both failures.</done>
</task>

</tasks>

<verification>
Both root causes documented in FINDINGS.md with file paths, line numbers, and fix recommendations.
</verification>

<success_criteria>
- isInstalledAheadOfLatest: confirmed absent from hooks/gsd-statusline.js source and exports block
- doc-conflict-engine regex: confirmed test uses {%~ but source file uses <%~ (correct per CLAUDE.md)
- FINDINGS.md written with actionable fix options for each failure
</success_criteria>

<output>
Create: .planning/quick/260530-6ks-investigate-3-test-failures-isinstalleda/260530-6ks-FINDINGS.md
</output>
