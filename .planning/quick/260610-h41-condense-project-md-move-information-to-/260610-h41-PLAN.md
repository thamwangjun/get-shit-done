---
phase: quick-260610-h41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/PROJECT_HISTORY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PROJECT.md no longer contains the 6 full inline 'Shipped: vX' milestone summary blocks; in their place is one concise shipped-versions summary with a pointer to PROJECT_HISTORY.md"
    - "PROJECT.md no longer contains the full '### Validated' requirements list; in its place is a concise count + milestone-span summary with a pointer to PROJECT_HISTORY.md, while '### Active' and '### Out of Scope' remain inline"
    - "PROJECT.md Key Decisions table retains only decisions that still guide future upstream-merge work; archived rows are summarized inline with a pointer to PROJECT_HISTORY.md"
    - "PROJECT_HISTORY.md gains the 6 shipped milestone blocks (Shipped Milestone section), the full Validated requirements list (Validated Requirements section), and the archived decision rows (Historical Key Decisions table) — appended into existing sections, not new top-level structure"
    - "D-01: Nothing is deleted outright — every block moved from PROJECT.md is replaced by a concise summary plus an explicit 'consult PROJECT_HISTORY.md for details' reference"
    - "PROJECT_HISTORY.md leading note block (lines 1-6) is preserved unchanged"
    - "Both files' 'Last updated' / 'Archived from' footers are updated"
  artifacts:
    - path: .planning/PROJECT.md
      provides: "Condensed active project reference with summaries + pointers"
    - path: .planning/PROJECT_HISTORY.md
      provides: "Expanded historical archive with moved content"
  key_links:
    - from: .planning/PROJECT.md
      to: .planning/PROJECT_HISTORY.md
      via: "explicit 'consult PROJECT_HISTORY.md for details' pointers in each condensed section"
---

<objective>
Condense `.planning/PROJECT.md` by relocating three categories of settled/historical content to `.planning/PROJECT_HISTORY.md`, replacing each moved block in PROJECT.md with a concise summary plus an explicit pointer to the archive.

Purpose: PROJECT.md (currently 251 lines) carries heavy historical detail that no longer guides active work. Moving it to the archive keeps the active reference scannable while preserving every detail.
Output: A shorter PROJECT.md (summaries + pointers) and an expanded PROJECT_HISTORY.md (full moved content).
</objective>

<context>
!`cat .planning/PROJECT.md`
!`cat .planning/PROJECT_HISTORY.md`
!`cat .planning/quick/260610-h41-condense-project-md-move-information-to-/260610-h41-CONTEXT.md`

Documentation reorganization only — no source code, tests, or runtime behavior. The only files touched are the two above.

OVERARCHING RULE (per D-01): nothing is deleted outright. Every block moved out of PROJECT.md must be replaced by a concise summary of what moved plus an explicit "consult PROJECT_HISTORY.md for details" reference. Use the existing pointer at PROJECT.md line 229 ("Historical Key Decisions ... archived in .planning/PROJECT_HISTORY.md") as the reference-style model.

Preserve PROJECT_HISTORY.md's leading note block (lines 1-6) and its existing section structure. Append/merge into the existing "Shipped Milestone", "Validated Requirements", and "Historical Key Decisions" sections — do not invent new top-level structure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move shipped milestones and validated requirements into PROJECT_HISTORY.md, condense in PROJECT.md</name>
  <files>.planning/PROJECT.md, .planning/PROJECT_HISTORY.md</files>
  <read_first>.planning/PROJECT.md, .planning/PROJECT_HISTORY.md</read_first>
  <action>
Handle the two largest moves in one pass since both span PROJECT.md and PROJECT_HISTORY.md.

Shipped milestones (PROJECT.md ~lines 11-29, the six `## Shipped: vX ...` blocks: v2.1.0-e, v2.1.0-d, v2.1.0-c, v2.1.0-g, v2.1.0-f):
- Move each full block into PROJECT_HISTORY.md as new entries under the existing "Shipped Milestone" section group (the `## Shipped Milestone: vX ...` entries that begin at line 18). Preserve the existing PROJECT_HISTORY.md milestone entries; place the newer fork milestones (v2.1.0-* series) above the older v1.37.x entries so the archive reads newest-first, matching PROJECT.md's existing order. Keep each block's full text (headline + paragraph + ROADMAP pointer) verbatim.
- In PROJECT.md, replace the six blocks with ONE concise section titled `## Shipped Versions` containing a compact list — one line per shipped version: `vX (date) — <one-line headline>` — followed by an explicit pointer line: "Full milestone summaries are archived in .planning/PROJECT_HISTORY.md (Shipped Milestone section)." (per D-01)
- Preserve the `## Current Milestone: Planning Next` block in PROJECT.md unchanged.

Validated requirements (PROJECT.md ~lines 37-146, the entire `### Validated` block under `## Requirements`):
- Move the full `### Validated` list (all checked requirement bullets, v1.37.2 through v2.1.0-g) into PROJECT_HISTORY.md's existing "Validated Requirements" section (begins at line 52). Append below the existing `### Validated` content already there; keep the existing PROJECT_HISTORY.md validated entries intact. Preserve all bullets verbatim.
- In PROJECT.md, replace the `### Validated` block with a concise summary: a one-to-two line statement giving the count of validated requirements and the milestone span (e.g. "N validated requirements spanning v1.37.2 → v2.1.0-g"), then the explicit pointer: "Full per-requirement validation records are archived in .planning/PROJECT_HISTORY.md (Validated Requirements section)." (per D-01) Count the bullets accurately from the current file.
- Keep the `### Active` and `### Out of Scope` subsections in PROJECT.md inline and unchanged.
  </action>
  <verify>
    <automated>command grep -c '^## Shipped: v' .planning/PROJECT.md; command grep -c '^### Validated' .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md has 0 `## Shipped: v` blocks (replaced by one `## Shipped Versions` summary with pointer) and 0 inline `### Validated` requirement lists (replaced by a count+span summary with pointer). PROJECT_HISTORY.md contains all 6 shipped milestone blocks and the full validated requirements list, appended into existing sections, with the leading note block intact. `### Active` and `### Out of Scope` remain inline in PROJECT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Archive settled Key Decisions rows, condense PROJECT.md Key Decisions, update footers</name>
  <files>.planning/PROJECT.md, .planning/PROJECT_HISTORY.md</files>
  <read_first>.planning/PROJECT.md, .planning/PROJECT_HISTORY.md</read_first>
  <action>
Key Decisions (PROJECT.md ~lines 187-229):
- Classify each Key Decisions row. KEEP inline (still guides future upstream-merge work): the broad fork-policy decisions — "Fork reference files override upstream content decisions", "Tests may be modified when they conflict with fork standards", "`plans/` is the canonical playbook for all recurring work types", "`<intent>` tag in command layer (not `<task>`)", "Scanner-first approach: run negative-framing scanner before any edit", "Preserve D-07 SECURITY `Never X — always Y` patterns verbatim", "XML tag hierarchy conversion removed from fork scope". These define ongoing fork conventions. (Claude's discretion governs the exact keep/archive split — favor keeping rows that state a still-active policy, archive rows that record a one-time implementation choice tied to a specific shipped milestone.)
- ARCHIVE (implementation-specific / fully settled, tied to a completed milestone): the per-milestone implementation rows — the AUDIT-03 examined-and-found-none row, the Manual-read-audit row, the per-bug rows (Bug #3242 cmdStateJson heuristic, etc.), the SHA-equality / GitHub-Commits-API / changelog-extraction / injectable-request-seam / placeholder-asymmetry rows (v2.1.0-a), the Eta pivot/delimiter/Phase-47.1/TEST-06-drop rows (v2.1.0-c), the step-numbering scanner/letter-suffix/Pattern-C/normalize-script/scanForOutOfOrder/cross-file-ref rows (v2.1.0-d), and the semicolon-delimiter/static-allowlist/D-08-floor/effort-argument/CATALOG-02/triage-phases/rawSlotForRuntime rows (v2.1.0-e), plus the v1.41.x batch-staging and allowlist rows.
- Move the archived rows into PROJECT_HISTORY.md's existing "Historical Key Decisions" table (begins at line 103, header at lines 107-108). Append the rows below the existing archived rows, preserving the same three-column format (Decision | Rationale | Outcome) verbatim.
- In PROJECT.md, the existing pointer line at line 229 ("> Historical Key Decisions (implementation-specific, settled) are archived in .planning/PROJECT_HISTORY.md.") already serves as the D-01 reference — retain/expand it so it explicitly notes that N implementation-specific decision rows were archived there. Add a one-line summary of what was archived above or beside the pointer. (per D-01)

Footers:
- Update PROJECT.md's `*Last updated:*` footer (line 251) to note this condensation (date 2026-06-10, "PROJECT.md condensed; historical detail moved to PROJECT_HISTORY.md").
- Update PROJECT_HISTORY.md's `*Archived from PROJECT.md — ...*` footer (line 132) to reflect the new archive date (2026-06-10) while preserving the original archive note context.
  </action>
  <verify>
    <automated>command grep -c '^| ' .planning/PROJECT.md; command grep -n 'Last updated' .planning/PROJECT.md; command grep -n 'Archived from PROJECT.md' .planning/PROJECT_HISTORY.md</automated>
  </verify>
  <done>PROJECT.md Key Decisions table retains only the still-guiding fork-policy rows; archived implementation-specific rows are removed from PROJECT.md and present in PROJECT_HISTORY.md's Historical Key Decisions table (verbatim, same column format). PROJECT.md retains an explicit pointer noting the archived rows (per D-01). Both files' footers are updated to 2026-06-10. PROJECT_HISTORY.md leading note block unchanged.</done>
</task>

</tasks>

<verification>
- `command grep -c '^## Shipped: v' .planning/PROJECT.md` returns 0; a `## Shipped Versions` summary section with a pointer exists instead.
- `command grep -c '^### Validated' .planning/PROJECT.md` returns 0; a concise validated-requirements summary with count + span + pointer exists instead.
- `### Active` and `### Out of Scope` still present inline in PROJECT.md.
- PROJECT_HISTORY.md contains the 6 moved milestone blocks, the full validated requirements list, and the archived decision rows — all merged into existing sections, with the leading note block (lines 1-6) intact.
- Every condensed PROJECT.md section carries an explicit "consult PROJECT_HISTORY.md for details" pointer (D-01 satisfied — nothing deleted outright).
- Both file footers updated to 2026-06-10.
</verification>

<success_criteria>
- PROJECT.md is materially shorter, retaining only active/still-guiding content plus concise summaries and pointers.
- All moved content is preserved verbatim in PROJECT_HISTORY.md under its existing section structure.
- No source code, tests, or runtime behavior changed — only the two `.planning/` markdown files.
</success_criteria>

<output>
Both `.planning/PROJECT.md` and `.planning/PROJECT_HISTORY.md` are updated in place. Create the quick-task SUMMARY at `.planning/quick/260610-h41-condense-project-md-move-information-to-/260610-h41-SUMMARY.md` when done.
</output>
