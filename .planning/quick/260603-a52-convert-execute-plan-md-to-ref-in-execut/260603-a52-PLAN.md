---
status: complete
---

# Quick Task 260603-a52: Convert execute-plan.md to @-ref in execute-phase.md

## Goal

Convert the `execute-plan.md` eta include inside the `gsd-executor` spawn block to a lazy
`@`-reference (matching the existing lazy-ref pattern in the same `<execution_context>`), and
add an explicit `<reference_usage>` entry stating when/why to consult it.

## Rationale

`execute-plan.md` (26 KB) currently inlines via `<%~ include %>` into the executor spawn prompt,
so the orchestrator carries all 26 KB on every run merely to forward it. As an `@`-ref the
orchestrator emits a one-line pointer; the executor Reads it in its fresh window (which it always
does anyway). The file already sits alongside `@`-refs (`summary.md`, `checkpoints.md`, `tdd.md`,
`executor-examples.md`) that follow this exact pattern, so the conversion makes the block consistent.

## Tasks

### Task 1 — Convert include to @-ref + document usage
- **files:** `get-shit-done/workflows/execute-phase.md`
- **action:**
  1. Line 602: replace `<%~ include('get-shit-done/workflows/execute-plan.md') %>` with
     `@~/.claude/get-shit-done/workflows/execute-plan.md` (consistent with the sibling @-refs).
  2. Add a `<reference_usage>` entry (placed first, as the primary contract) instructing the
     executor to read execute-plan.md FIRST as its primary execution contract — covering the
     task loop, atomic commit / git_commit_metadata step, deviation rules, and worktree-mode
     auto-detection — and noting both consumers (spawned worktree executor + sequential inline
     fallback) depend on it.
- **verify:** `grep -n "@~/.claude/get-shit-done/workflows/execute-plan.md" execute-phase.md` shows
  the @-ref; no `<%~ include('get-shit-done/workflows/execute-plan.md')` remains; eta-template-syntax
  test passes.
- **done:** include is gone, @-ref present, reference_usage documents when to consult it.

## must_haves

- truths: execute-plan.md is forwarded payload (spawn path) and the inline-fallback contract; always needed by whichever path executes.
- artifacts: `get-shit-done/workflows/execute-phase.md` (modified).
- key_links: `execute-phase.md:602` (include→@-ref), `execute-phase.md` `<reference_usage>` block.
