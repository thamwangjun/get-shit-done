---
status: complete
---

# Quick Task 260603-a52 — Summary

**Goal:** Convert the `execute-plan.md` eta include in `execute-phase.md` to a lazy `@`-ref and document when to consult it.

## What changed

`get-shit-done/workflows/execute-phase.md` (commit `d9c627c1`):

1. **Line 602** — `<%~ include('get-shit-done/workflows/execute-plan.md') %>` → `@~/.claude/get-shit-done/workflows/execute-plan.md`, making it consistent with the sibling `@`-refs (`summary.md`, `checkpoints.md`, `tdd.md`, `executor-examples.md`) already in the `<execution_context>` block.

2. **`<reference_usage>` block** — added a new entry, placed first, framing execute-plan.md as the *primary execution contract* (read FIRST, not conditional). It documents the "when": always, before executing any task; explains what it defines (task loop, atomic-commit/`git_commit_metadata`, deviation rules, worktree-mode auto-detection); and cross-links both consumers — the spawned worktree executor (loads it here) and the orchestrator's sequential inline-execution fallback (reads the same file directly, per `<runtime_compatibility>` and the step-3 "If Execute" inline path).

## Effect

- Orchestrator stops carrying ~26 KB (~6,600 tokens) of forwarded executor payload on every run; it now emits a one-line pointer. The executor Reads the file in its fresh 200 KB window as it already did.
- Inline-fallback path improves too: the orchestrator now loads execute-plan.md only when it actually enters fallback, instead of always holding the inlined copy.

## Verification

- `grep` confirms the `@`-ref is present and no `execute-plan.md` `<%~ include %>` remains (the two remaining includes — `worktree-path-safety.md` and the three top-level `required_reading` refs — are intentionally untouched).
- `tests/eta-template-syntax.test.cjs` passes (0 fail).
- No test couples to the removed include directive (checked `tests/`).

## Notes / follow-ups

- `worktree-path-safety.md` (line 606) remains an eta include and is the next candidate if further orchestrator-budget trimming is wanted (conditional: worktree mode only).
- Prose mentions of execute-plan.md (lines ~6, 13, 245, 588, 594) were left as-is — they already describe reading the file directly and are consistent with the `@`-ref model.
