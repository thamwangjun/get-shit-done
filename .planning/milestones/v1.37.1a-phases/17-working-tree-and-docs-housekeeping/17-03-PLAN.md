---
phase: "17"
plan: "03"
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - .planning/config.json
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/STACK.md
  - package-lock.json
  - sdk/package-lock.json
  - .planning/phases/15-test-suite-gate/15-01-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "git status shows a clean working tree — no uncommitted modifications to tracked files"
    - "Phase 15 SUMMARY records 4168 (the historically correct count) in all test-count references"
  artifacts:
    - path: ".planning/phases/15-test-suite-gate/15-01-SUMMARY.md"
      provides: "Corrected historical test count (4168) matching the true Phase 15 measurement"
    - path: ".planning/config.json"
      provides: "Config changes committed"
    - path: "package-lock.json"
      provides: "Lock file committed"
  key_links:
    - from: "CONTEXT.md D-05"
      to: ".planning/phases/15-test-suite-gate/15-01-SUMMARY.md"
      via: "unconditional 4164 → 4168 replacement"
      pattern: "4168"
---

<objective>
Commit all remaining modified tracked files as a housekeeping bundle, and correct the Phase 15 SUMMARY test count from 4164 to 4168.

Purpose: Close tech debt items D-02 and D-05 from the v1.37.1a audit — bring the working tree to a fully clean state, and apply the historical correction to the Phase 15 SUMMARY so it records the true count that Phase 15 measured.
Output: Clean git working tree; Phase 15 SUMMARY with the historically accurate test count of 4168.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/PROJECT.md
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Phase 15 SUMMARY historical test count 4164 → 4168 (per D-05)</name>
  <files>.planning/phases/15-test-suite-gate/15-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-test-suite-gate/15-01-SUMMARY.md — read in full to locate all occurrences of `4164` that need replacing
  </read_first>
  <action>
This is a historical correction. Phase 15 measured 4168 tests; its SUMMARY was written while test file modifications were uncommitted (worktree mode), so it incorrectly recorded 4164. D-05 unconditionally directs changing 4164 → 4168. The current live npm test count after Plan 01's duplicate removal is irrelevant to this correction.

Read `.planning/phases/15-test-suite-gate/15-01-SUMMARY.md` in full. Using the Edit tool with exact OLD TEXT from the file, replace every occurrence of `4164` with `4168` where it refers to the test count. Expected locations:

- Heading/title line: `4164/4164 npm tests green` → `4168/4168 npm tests green`
- Accomplishments bullet: `passes with 4164 tests` → `passes with 4168 tests`
- Deviation section: `ℹ pass 4164` → `ℹ pass 4168`
- Issues section: `npm test reported 4164 tests (not 4168 as anticipated)` — this entire sentence is now stale; replace it with `npm test confirmed 4168 tests (matching the anticipated count)` to close the issue accurately

Do NOT add or remove any other content. Use the Edit tool for each replacement (exact old text → new text).
  </action>
  <verify>
    <automated>grep "4168" /home/thamw/development/happier/get-shit-done/.planning/phases/15-test-suite-gate/15-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "4168" .planning/phases/15-test-suite-gate/15-01-SUMMARY.md` returns at least 3 matches (heading, accomplishments, deviation section)
    - `grep "4164" .planning/phases/15-test-suite-gate/15-01-SUMMARY.md` returns 0 matches (no stale count remains)
    - File is otherwise unchanged — no structural edits, no content beyond the count corrections
  </acceptance_criteria>
  <done>Phase 15 SUMMARY records 4168 in all test-count references. No occurrence of 4164 remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Commit Phase 15 SUMMARY update and all modified tracked files (per D-02)</name>
  <files>
    .planning/phases/15-test-suite-gate/15-01-SUMMARY.md
    .planning/config.json
    .planning/research/ARCHITECTURE.md
    .planning/research/FEATURES.md
    .planning/research/PITFALLS.md
    .planning/research/STACK.md
    package-lock.json
    sdk/package-lock.json
  </files>
  <read_first>
    - No file read needed — run git status first to confirm which files remain modified, then stage and commit
  </read_first>
  <action>
First run `git status --short` to confirm which modified tracked files are still uncommitted (Plan 01 already committed `tests/negative-framing-scan.test.cjs`).

Stage all remaining modified tracked files and the Phase 15 SUMMARY:

```bash
git add .planning/phases/15-test-suite-gate/15-01-SUMMARY.md
git add .planning/config.json
git add .planning/research/ARCHITECTURE.md
git add .planning/research/FEATURES.md
git add .planning/research/PITFALLS.md
git add .planning/research/STACK.md
git add package-lock.json
git add sdk/package-lock.json
```

Commit with:
```bash
git commit -m "chore(17): commit modified tracked files housekeeping bundle

- .planning/config.json: config updates from v1.37.1a milestone work
- .planning/research/*.md: research file updates (ARCHITECTURE, FEATURES, PITFALLS, STACK)
- package-lock.json, sdk/package-lock.json: lock file updates
- .planning/phases/15-test-suite-gate/15-01-SUMMARY.md: correct historical test count
  4164 → 4168 (Phase 15 measured 4168; SUMMARY was stale due to uncommitted worktree, D-05)"
```

If `git status --short` shows any of the listed files are NOT modified (already clean), skip adding those files — only stage files that are actually modified. Do not stage files that are already committed or unchanged.
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && git status --short</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` outputs nothing (completely clean working tree), OR outputs only files from the active plan set (17-01-SUMMARY.md, 17-02-SUMMARY.md, 17-03-SUMMARY.md) that will be created as part of plan output
    - `git status --short` shows NO modified (`M`) entries for: `.planning/config.json`, `.planning/research/ARCHITECTURE.md`, `.planning/research/FEATURES.md`, `.planning/research/PITFALLS.md`, `.planning/research/STACK.md`, `package-lock.json`, `sdk/package-lock.json`
    - `git log --oneline -1` shows the "chore(17)" housekeeping commit
  </acceptance_criteria>
  <done>Working tree is clean. All modified tracked files committed. git status shows no uncommitted modifications to non-SUMMARY tracked files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git staging area → repo | Housekeeping commit; all files are documentation/config — no executable code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | package-lock.json, sdk/package-lock.json | accept | Lock files are committing pre-existing working-tree state; no new dependencies introduced in this phase |
</threat_model>

<verification>
1. `grep "4168" .planning/phases/15-test-suite-gate/15-01-SUMMARY.md` → at least 3 matches
2. `grep "4164" .planning/phases/15-test-suite-gate/15-01-SUMMARY.md` → 0 matches
3. `git status --short` → empty output (clean working tree)
4. `git log --oneline -3` → shows the three Phase 17 commits
</verification>

<success_criteria>
- `git status` shows a clean working tree with no uncommitted modifications
- Phase 15 SUMMARY records 4168 (not 4164) in all test-count references — the historical correction is applied unconditionally per D-05
- All modified tracked files from D-02 list are committed
- `npm test` continues to pass with 0 failures (no regressions introduced by commit operations)
</success_criteria>

<output>
After completion, create `.planning/phases/17-working-tree-and-docs-housekeeping/17-03-SUMMARY.md`

Include in the SUMMARY:
- Final git status confirmation (clean tree)
- Confirmation that Phase 15 SUMMARY was updated: 4164 → 4168 in all test-count references
- Confirmation that all 7 tech debt items from v1.37.1a-MILESTONE-AUDIT.md are closed
</output>
