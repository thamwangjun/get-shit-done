---
phase: "17"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/negative-framing-scan.test.cjs
  - mise.toml
  - .planning/v1.37.1a-MILESTONE-AUDIT.md
  - .planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md
  - .planning/notes/2026-04-22-scanner-bug-isconditionalorfactual.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "tests/negative-framing-scan.test.cjs has exactly one describe block named 'corpus scan — DO NOT primary directives (case-insensitive)'"
    - "npm test passes with 0 failures after the duplicate block is removed"
    - "All four untracked items are committed to git"
  artifacts:
    - path: "tests/negative-framing-scan.test.cjs"
      provides: "Deduped test file — single canonical 4-subtest describe block"
    - path: "mise.toml"
      provides: "Dev tool version pin committed to repo"
    - path: ".planning/v1.37.1a-MILESTONE-AUDIT.md"
      provides: "Milestone audit artifact committed under .planning/"
    - path: ".planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md"
      provides: "Phase 14 planning artifact committed in place"
    - path: ".planning/notes/2026-04-22-scanner-bug-isconditionalorfactual.md"
      provides: "Dev notes committed under .planning/notes/"
  key_links:
    - from: "tests/negative-framing-scan.test.cjs"
      to: "npm test"
      via: "node --test runner"
      pattern: "describe\\('corpus scan — DO NOT primary directives"
---

<objective>
Remove the duplicate `describe('corpus scan — DO NOT primary directives (case-insensitive)', ...)` block from the test file, confirm the test suite passes, then commit all four untracked planning artifacts.

Purpose: Close tech debt items D-01 and D-03 from the v1.37.1a audit — deduplicate the test output so corpus scan results are unambiguous, and bring the four untracked files under version control.
Output: Clean test file with one canonical describe block; four git commits (or one bundled commit) covering all untracked items.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/PROJECT.md
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate describe block from tests/negative-framing-scan.test.cjs (per D-03)</name>
  <files>tests/negative-framing-scan.test.cjs</files>
  <read_first>
    - tests/negative-framing-scan.test.cjs — read in full to confirm current line count and verify canonical block at line 400 vs duplicate starting at line 564
  </read_first>
  <action>
Delete the duplicate describe block and its preceding comment block from tests/negative-framing-scan.test.cjs.

The block to DELETE is lines 557–657 (end of file), which consists of:

1. The comment block (lines 557–563):
```
// ─── Corpus scan: DO NOT primary directive (case-insensitive) ────────────────
//
// Guards against bare "do not" directives in any capitalisation that lack a
// positive complement — e.g. "do not commit", "Do Not skip", "DO NOT add".
// Constraint pairs ("do not X — use Y instead") and conditional branches
// ("If X does not match...") are exempt per the scanner logic above.
```

2. The entire second `describe('corpus scan — DO NOT primary directives (case-insensitive)', () => { ... });` block (lines 564–657).

The CANONICAL block to KEEP is at lines 400–449:
```javascript
describe('corpus scan — DO NOT primary directives (case-insensitive)', () => {
```
This block has 2 subtests: 'no bare DO NOT directives in agent files' and 'no bare DO NOT directives in command files'. Keep it exactly as-is.

After the deletion, the file should end with the closing `});` of the NEVER corpus scan block (currently at line 555). Line 556 is blank.

Use the Edit tool to remove the range. The OLD TEXT to match is the full content from line 557 to end of file (line 657). Replace it with nothing (empty string), or with a single trailing newline if the file should end cleanly.

Exact OLD TEXT start (line 557):
`// ─── Corpus scan: DO NOT primary directive (case-insensitive) ────────────────`

Exact OLD TEXT end (line 657, last line of file):
`});`

After editing, verify line count dropped from 657 to ~556.
  </action>
  <verify>
    <automated>grep -c "describe('corpus scan — DO NOT primary directives" /home/thamw/development/happier/get-shit-done/tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('corpus scan — DO NOT primary directives" tests/negative-framing-scan.test.cjs` outputs `1` (exactly one such describe block)
    - `wc -l tests/negative-framing-scan.test.cjs` shows approximately 556 lines (original 657 minus ~101 deleted lines)
    - The canonical block starting at line 400 is intact: `grep -n "describe('corpus scan — DO NOT" tests/negative-framing-scan.test.cjs` shows one match near line 400
  </acceptance_criteria>
  <done>Exactly one 'corpus scan — DO NOT primary directives (case-insensitive)' describe block exists in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Run npm test to confirm 0 failures after dedup</name>
  <files></files>
  <read_first>
    - No file read needed — run the test command directly
  </read_first>
  <action>
Run `npm test` from the repo root and verify the output shows 0 failures.

The expected output should include:
- `ℹ fail 0`
- The `corpus scan — DO NOT primary directives (case-insensitive)` suite appearing ONCE with its subtests

Note the actual test count reported (the `ℹ pass N` line). Record this number — it will be needed for Plan 03 Task 1 (updating Phase 15 SUMMARY).

Expected count after removing the duplicate 4-subtest block: approximately 4164 (the duplicate block added 4 tests: agent, workflow, reference, command — removing it brings the count from 4168 back to 4164). If the actual count differs, record the actual number.
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && npm test 2>&1 | grep -E "^ℹ (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - Output contains `ℹ fail 0`
    - Output contains `ℹ pass` followed by a number (record this number for Plan 03)
    - The corpus scan `DO NOT` suite does NOT appear twice in the test output
  </acceptance_criteria>
  <done>npm test passes with 0 failures. Actual test count recorded for use in Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Commit test file dedup and all four untracked planning files (per D-01, D-03)</name>
  <files>
    tests/negative-framing-scan.test.cjs
    mise.toml
    .planning/v1.37.1a-MILESTONE-AUDIT.md
    .planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md
    .planning/notes/2026-04-22-scanner-bug-isconditionalorfactual.md
  </files>
  <read_first>
    - No file read needed — run git commands directly
  </read_first>
  <action>
Stage and commit the test file change and the four untracked files in a single housekeeping commit.

Commands:
```bash
git add tests/negative-framing-scan.test.cjs
git add mise.toml
git add .planning/v1.37.1a-MILESTONE-AUDIT.md
git add .planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md
git add .planning/notes/
git commit -m "chore(17): commit untracked artifacts and remove duplicate describe block

- tests/negative-framing-scan.test.cjs: delete duplicate 'corpus scan — DO NOT
  primary directives (case-insensitive)' describe block (lines 557-657); canonical
  block at line 400 retained (2 subtests)
- mise.toml: dev tool version pin committed to repo root
- .planning/v1.37.1a-MILESTONE-AUDIT.md: milestone audit artifact for v1.37.1a
- .planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md: planning
  artifact committed in place
- .planning/notes/: scanner bug dev note committed"
```
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && git status --short | grep -E "^\?\? (mise\.toml|\.planning/v1\.37\.1a|\.planning/phases/14|\.planning/notes)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` shows no `??` entries for: `mise.toml`, `.planning/v1.37.1a-MILESTONE-AUDIT.md`, `.planning/phases/14-workflow-reference-and-command-fixes/14-PATTERNS.md`, `.planning/notes/`
    - `git status --short` shows no `M` or `??` entry for `tests/negative-framing-scan.test.cjs`
    - `git log --oneline -1` shows a commit with "chore(17)" in the subject
    - The above verify command outputs `0` (no untracked items remain for those paths)
  </acceptance_criteria>
  <done>All four untracked items and the deduped test file are committed. git status shows them as clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → file system | Test runner reads markdown files from disk; no external network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | tests/negative-framing-scan.test.cjs | accept | Edit is a deletion only — no new logic introduced; npm test gate confirms no behavioral regression |
</threat_model>

<verification>
1. `grep -c "describe('corpus scan — DO NOT primary directives" tests/negative-framing-scan.test.cjs` → `1`
2. `npm test` → `ℹ fail 0`, exits 0
3. `git status --short` → no untracked files for the four target paths
</verification>

<success_criteria>
- Exactly one 'corpus scan — DO NOT primary directives (case-insensitive)' describe block in test file
- npm test: 0 failures
- git: mise.toml, v1.37.1a-MILESTONE-AUDIT.md, 14-PATTERNS.md, .planning/notes/ all committed
- Actual post-removal test count recorded (expected ~4164)
</success_criteria>

<output>
After completion, create `.planning/phases/17-working-tree-and-docs-housekeeping/17-01-SUMMARY.md`

Include in the SUMMARY:
- The actual test count from npm test (the `ℹ pass N` value) — this is needed by Plan 03
- Confirmation that exactly 1 describe block remains
- The commit hash
</output>
