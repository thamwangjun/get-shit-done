---
phase: "17"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md FRAMING-01 through FRAMING-06 each have a Fixed: annotation on the line immediately below the requirement bullet"
    - "The Fixed: annotations use the same indented format as FRAMING-07–17"
    - "The annotation text matches the actual before/after text from Phase 13 git commits"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete FRAMING annotation coverage (FRAMING-01–17 all annotated)"
      contains: "Fixed: `Do NOT generate more areas"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/13-agent-fixes/13-01-SUMMARY.md"
      via: "before/after text sourced from SUMMARY"
      pattern: "Fixed:"
---

<objective>
Add `Fixed:` inline annotations to FRAMING-01 through FRAMING-06 in REQUIREMENTS.md, matching the format already established for FRAMING-07–17.

Purpose: Close tech debt item D-04 from the v1.37.1a audit — FRAMING-01–06 have [x] checkboxes but lack the before/after annotation text that FRAMING-07–17 all have. This makes the requirements file a complete record of all framing fixes.
Output: REQUIREMENTS.md with 6 new Fixed: annotation lines (one per FRAMING-01–06 entry), committed.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/PROJECT.md
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fixed: annotations to FRAMING-01–06 in REQUIREMENTS.md (per D-04)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read lines 1–60 to confirm the current state of FRAMING-01–06 (lines 12–17) and verify the Fixed: format used by FRAMING-07–17 (lines 22–48)
    - .planning/phases/13-agent-fixes/13-01-SUMMARY.md — authoritative source for FRAMING-01, FRAMING-05, FRAMING-06 before/after text
    - .planning/phases/13-agent-fixes/13-02-SUMMARY.md — authoritative source for FRAMING-02, FRAMING-03, FRAMING-04 before/after text
  </read_first>
  <action>
Add a `  - Fixed: <before> → <after>` line immediately below each of the six FRAMING-01–06 bullets in REQUIREMENTS.md. Use the Edit tool — one edit per requirement to minimize error surface.

The format to use (matching FRAMING-07–17 established pattern):
```
  - Fixed: `<original text>` → `<replacement text>`
```

The exact annotations to add, sourced from Phase 13 SUMMARY files:

**FRAMING-01** (line 12 currently reads):
`- [x] **FRAMING-01**: \`agents/gsd-assumptions-analyzer.md:111\` — bare \`Do NOT generate more areas\` replaced with positive instruction specifying the correct area count limit`

Insert immediately after line 12:
`  - Fixed: \`Do NOT generate more areas than the calibration tier specifies\` → \`Keep area count within the tier limit defined in \`<calibration_tiers>\` above\``

**FRAMING-02** (line 13 currently reads):
`- [x] **FRAMING-02**: \`agents/gsd-code-fixer.md:138\` — bare \`Do NOT skip the fix\` replaced with positive instruction to always apply the fix regardless of syntax checker availability`

Insert immediately after line 13:
`  - Fixed: \`Do NOT skip the fix just because syntax checking is unavailable\` → \`Apply the fix even when syntax checking is unavailable\``

**FRAMING-03** (line 14 currently reads):
`- [x] **FRAMING-03**: \`agents/gsd-code-fixer.md:240\` — bare \`Do NOT create REVIEW-FIX.md\` replaced with positive exit instruction`

Insert immediately after line 14:
`  - Fixed: \`Do NOT create REVIEW-FIX.md\` → bullet deleted`

**FRAMING-04** (line 15 currently reads):
`- [x] **FRAMING-04**: \`agents/gsd-code-fixer.md:344\` — bare \`Do NOT leave uncommitted changes\` replaced with positive instruction to commit all changes before continuing`

Insert immediately after line 15:
`  - Fixed: \`Do NOT leave uncommitted changes\` → \`Restore all files to pre-fix state before continuing\``

**FRAMING-05** (line 16 currently reads):
`- [x] **FRAMING-05**: \`agents/gsd-doc-verifier.md:92\` — list header \`Do NOT verify the following:\` replaced with affirmative equivalent (e.g. "Skip verification for the following:")`

Insert immediately after line 16:
`  - Fixed: \`Do NOT verify the following:\` → \`Skip verification for the following:\``

**FRAMING-06** (line 17 currently reads):
`- [x] **FRAMING-06**: \`agents/gsd-user-profiler.md:88\` — bare \`Do not proceed to message analysis\` replaced with positive sequencing gate (load rubric first, then proceed)`

Insert immediately after line 17:
`  - Fixed: \`Do not proceed to message analysis until the rubric is loaded.\` → \`Load the rubric fully before proceeding to message analysis.\``

After all six inserts, the FRAMING-01–06 section should look structurally identical to the FRAMING-07–17 section — each requirement bullet followed by its indented Fixed: sub-line.

Use the Edit tool with OLD TEXT = the existing bullet line and NEW TEXT = the bullet line plus the Fixed: line appended. Apply changes sequentially (FRAMING-01 first, then FRAMING-02, etc.) — each edit changes a different line so there is no conflict.
  </action>
  <verify>
    <automated>grep -A1 "FRAMING-0[1-6]" /home/thamw/development/happier/get-shit-done/.planning/REQUIREMENTS.md | grep -c "Fixed:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "FRAMING-01" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do NOT generate more areas than the calibration tier specifies\``
    - `grep -A1 "FRAMING-02" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do NOT skip the fix just because syntax checking is unavailable\``
    - `grep -A1 "FRAMING-03" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do NOT create REVIEW-FIX.md\` → bullet deleted`
    - `grep -A1 "FRAMING-04" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do NOT leave uncommitted changes\``
    - `grep -A1 "FRAMING-05" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do NOT verify the following:\``
    - `grep -A1 "FRAMING-06" .planning/REQUIREMENTS.md` shows a line matching `  - Fixed: \`Do not proceed to message analysis`
    - The verify command outputs `6` (all six FRAMING-01–06 entries have a Fixed: line)
  </acceptance_criteria>
  <done>All six FRAMING-01–06 entries in REQUIREMENTS.md have Fixed: annotations in the established format.</done>
</task>

<task type="auto">
  <name>Task 2: Commit REQUIREMENTS.md Fixed: annotation additions</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - No file read needed — run git commands directly
  </read_first>
  <action>
Stage and commit the REQUIREMENTS.md change.

```bash
git add .planning/REQUIREMENTS.md
git commit -m "docs(17): add Fixed: annotations to FRAMING-01–06 in REQUIREMENTS.md

Add the same Fixed: before/after inline annotation format established
by FRAMING-07–17 (Phase 15) to the six Phase 13 requirements that
were previously missing annotations.

Sources: 13-01-SUMMARY.md (FRAMING-01, FRAMING-05, FRAMING-06),
         13-02-SUMMARY.md (FRAMING-02, FRAMING-03, FRAMING-04)"
```
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && git status --short .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short .planning/REQUIREMENTS.md` outputs nothing (file is clean, not staged/modified)
    - `git log --oneline -1` shows the commit with "docs(17)" subject
    - `git show --stat HEAD` lists `.planning/REQUIREMENTS.md` in the changed files
  </acceptance_criteria>
  <done>REQUIREMENTS.md committed. Fixed: annotations for FRAMING-01–06 are in git history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → REQUIREMENTS.md | Text-only edit to a planning document; no code execution involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Tampering | .planning/REQUIREMENTS.md | accept | Changes are additive annotation lines only — existing requirement bullets not modified; grep acceptance criteria verify exact strings |
</threat_model>

<verification>
1. `grep -c "Fixed:" .planning/REQUIREMENTS.md` → at least 17 (11 from FRAMING-07–17 + 6 new for FRAMING-01–06)
2. `grep -A1 "FRAMING-0[1-6]" .planning/REQUIREMENTS.md | grep "Fixed:" | wc -l` → `6`
3. `git status --short .planning/REQUIREMENTS.md` → empty (committed)
</verification>

<success_criteria>
- REQUIREMENTS.md has Fixed: annotation lines for all of FRAMING-01–06
- Annotation format matches FRAMING-07–17 (indented two spaces, backtick-delimited before/after text)
- Before/after text matches Phase 13 SUMMARY files exactly
- File committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/17-working-tree-and-docs-housekeeping/17-02-SUMMARY.md`
</output>
