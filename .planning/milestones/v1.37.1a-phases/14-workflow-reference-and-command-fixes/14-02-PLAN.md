---
phase: 14-workflow-reference-and-command-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/docs-update.md
  - commands/gsd/execute-phase.md
  - commands/gsd/reapply-patches.md
  - tests/execute-phase-active-flags.test.cjs
autonomous: true
requirements:
  - FRAMING-15
  - FRAMING-16
  - FRAMING-17

must_haves:
  truths:
    - "docs-update.md line ~42 contains 'Treat a flag as active only if its literal token is present in `$ARGUMENTS`' with no 'do not' phrasing"
    - "execute-phase.md line ~54 contains the identical positive inference rule with no 'do not' phrasing"
    - "tests/execute-phase-active-flags.test.cjs line 50 asserts the new positive text; old 'Do not infer...' assertion string is gone"
    - "reapply-patches.md line ~271 contains 'Proceed to Step 6 only after the user confirms all unverified hunks are resolved'"
    - "npm test execute-phase-active-flags test suite passes (0 failures)"
    - "npm test command DO NOT corpus scan subtest shows 0 violations"
  artifacts:
    - path: "commands/gsd/docs-update.md"
      provides: "FRAMING-15 fix"
      contains: "Treat a flag as active only if its literal token is present in `$ARGUMENTS`"
    - path: "commands/gsd/execute-phase.md"
      provides: "FRAMING-16 fix"
      contains: "Treat a flag as active only if its literal token is present in `$ARGUMENTS`"
    - path: "tests/execute-phase-active-flags.test.cjs"
      provides: "D-11 test sync"
      contains: "Treat a flag as active only if its literal token is present in `$ARGUMENTS`"
    - path: "commands/gsd/reapply-patches.md"
      provides: "FRAMING-17 fix"
      contains: "Proceed to Step 6 only after the user confirms all unverified hunks are resolved"
  key_links:
    - from: "commands/gsd/execute-phase.md"
      to: "tests/execute-phase-active-flags.test.cjs"
      via: "test asserts string content from source file — must be updated atomically in same task"
      pattern: "Treat a flag as active only if its literal token is present in `$ARGUMENTS`"
---

<objective>
Fix FRAMING-15, FRAMING-16, and FRAMING-17: replace 3 bare "do not" directive violations in command files with affirmative positive instructions, and update the co-dependent test assertion in execute-phase-active-flags.test.cjs to assert the new text.

FRAMING-16 source edit and test update are in the same task (atomicity requirement per D-11 and RESEARCH Pitfall 2): if execute-phase.md is edited without updating the test in the same task, the test suite breaks mid-execution.

Purpose: Clear all bare "do not" violations in the commands directory and keep the test suite green throughout.
Output: 3 edited command markdown files and 1 updated test file assertion.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/14-workflow-reference-and-command-fixes/14-CONTEXT.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/REQUIREMENTS.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/14-workflow-reference-and-command-fixes/14-CONTEXT.md

<interfaces>
<!-- Exact current text at each violation site — verified by direct file read in 14-RESEARCH.md -->

FRAMING-15 (docs-update.md:42):
  current:  `- Do not infer that a flag is active just because it is documented in this prompt`
  replace:  `- Treat a flag as active only if its literal token is present in `$ARGUMENTS``
  read_ctx: lines 39–46

FRAMING-16 (execute-phase.md:54):
  current:  `- Do not infer that a flag is active just because it is documented in this prompt`
  replace:  `- Treat a flag as active only if its literal token is present in `$ARGUMENTS``
  read_ctx: lines 50–58

D-11 (tests/execute-phase-active-flags.test.cjs:50–51):
  current line 50:
    `      content.includes('Do not infer that a flag is active just because it is documented in this prompt'),`
  current line 51:
    `      'context should forbid inferring flags from documentation alone'`
  replace lines 50–51:
    `      content.includes('Treat a flag as active only if its literal token is present in `$ARGUMENTS`'),`
    `      'context should require deriving flag state from $ARGUMENTS literal token'`
  read_ctx: lines 44–65

FRAMING-17 (reapply-patches.md:271):
  current:  `Do not proceed to cleanup until the user confirms they have resolved all unverified hunks.`
  replace:  `Proceed to Step 6 only after the user confirms all unverified hunks are resolved.`
  read_ctx: lines 268–275
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FRAMING-15 (docs-update.md) and FRAMING-16 + test sync (execute-phase.md + execute-phase-active-flags.test.cjs)</name>
  <files>
    commands/gsd/docs-update.md
    commands/gsd/execute-phase.md
    tests/execute-phase-active-flags.test.cjs
  </files>
  <read_first>
    - commands/gsd/docs-update.md (lines 39–46 — read before edit to confirm exact surrounding context)
    - commands/gsd/execute-phase.md (lines 50–58 — read before edit to confirm exact surrounding context)
    - tests/execute-phase-active-flags.test.cjs (lines 44–65 — read before edit to confirm assertion at line 50 and failure message at line 51)
  </read_first>
  <action>
Apply three edits in sequence using the Edit tool (not Write). The execute-phase.md and test file edits must be applied in the same task run without stopping between them.

**FRAMING-15 — docs-update.md (line ~42)**
Find and replace (exact string match):
  old: `- Do not infer that a flag is active just because it is documented in this prompt`
  new: `- Treat a flag as active only if its literal token is present in `$ARGUMENTS``

**FRAMING-16 — execute-phase.md (line ~54)**
Find and replace (exact string match — identical text to FRAMING-15 for consistency per D-10):
  old: `- Do not infer that a flag is active just because it is documented in this prompt`
  new: `- Treat a flag as active only if its literal token is present in `$ARGUMENTS``

**D-11 — tests/execute-phase-active-flags.test.cjs (lines 50–51, MUST follow immediately)**
Use Edit to replace both lines 50 and 51 together (use the two-line block as old_string).

old_string (exact two-line block):
```
      content.includes('Do not infer that a flag is active just because it is documented in this prompt'),
      'context should forbid inferring flags from documentation alone'
```

new_string (exact two-line block):
```
      content.includes('Treat a flag as active only if its literal token is present in `$ARGUMENTS`'),
      'context should require deriving flag state from $ARGUMENTS literal token'
```

CRITICAL: Do not leave the test file with the old assertion string after execute-phase.md has been edited. Apply both the source edit and the test update before moving on.
  </action>
  <verify>
    <automated>grep -n "Treat a flag as active only if" /home/thamw/development/happier/get-shit-done/commands/gsd/docs-update.md && grep -n "Treat a flag as active only if" /home/thamw/development/happier/get-shit-done/commands/gsd/execute-phase.md && grep -n "Treat a flag as active only if" /home/thamw/development/happier/get-shit-done/tests/execute-phase-active-flags.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "do not infer" commands/gsd/docs-update.md` returns no output (0 matches)
    - `grep "Treat a flag as active only if its literal token is present" commands/gsd/docs-update.md` returns exactly 1 match
    - `grep -i "do not infer" commands/gsd/execute-phase.md` returns no output (0 matches)
    - `grep "Treat a flag as active only if its literal token is present" commands/gsd/execute-phase.md` returns exactly 1 match
    - `grep "Do not infer that a flag is active" tests/execute-phase-active-flags.test.cjs` returns no output (old assertion string removed)
    - `grep "Treat a flag as active only if its literal token is present" tests/execute-phase-active-flags.test.cjs` returns exactly 1 match
    - `grep "context should require deriving flag state from" tests/execute-phase-active-flags.test.cjs` returns exactly 1 match (failure message updated)
  </acceptance_criteria>
  <done>docs-update.md, execute-phase.md, and the test assertion are all updated atomically — no broken intermediate state where source changed but test still asserts the old string.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FRAMING-17 (reapply-patches.md)</name>
  <files>
    commands/gsd/reapply-patches.md
  </files>
  <read_first>
    - commands/gsd/reapply-patches.md (lines 268–275 — read before edit to confirm exact surrounding context and current violation text)
  </read_first>
  <action>
Apply one single-line replacement using the Edit tool (not Write).

**FRAMING-17 — reapply-patches.md (line ~271)**
Find and replace (exact string match):
  old: `Do not proceed to cleanup until the user confirms they have resolved all unverified hunks.`
  new: `Proceed to Step 6 only after the user confirms all unverified hunks are resolved.`

Read lines 268–275 first to confirm the current text. The surrounding context from 14-RESEARCH.md shows:
  line ~269: `  (b) Restore from backup: cp {patches_dir}/{file} {installed_path}`
  line ~270: (blank line)
  line ~271: `Do not proceed to cleanup until the user confirms they have resolved all unverified hunks.`
  line ~272: (blank line)
  line ~273: `**Only when all rows show `verified: yes`**...`
  </action>
  <verify>
    <automated>grep -n "Proceed to Step 6 only after" /home/thamw/development/happier/get-shit-done/commands/gsd/reapply-patches.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "do not proceed to cleanup" commands/gsd/reapply-patches.md` returns no output (0 matches)
    - `grep "Proceed to Step 6 only after the user confirms all unverified hunks are resolved" commands/gsd/reapply-patches.md` returns exactly 1 match
  </acceptance_criteria>
  <done>reapply-patches.md line ~271 contains the positive sequencing gate with no "do not" phrasing.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite gate — verify all command fixes and execute-phase-active-flags pass</name>
  <files></files>
  <read_first>
    - tests/negative-framing-scan.test.cjs (to understand scanner scope — confirm commands/ directory is scanned)
    - tests/execute-phase-active-flags.test.cjs (skim to confirm all assertions after line 50 still make sense)
  </read_first>
  <action>
Run the full test suite and verify:
1. The command DO NOT corpus scan subtest shows 0 violations
2. The execute-phase-active-flags test suite passes with 0 failures
3. No regressions introduced in previously-passing tests

Command:
```bash
npm test 2>&1 | grep -E "(no bare DO NOT directives in command|execute-phase-active-flags|context should|Tests:|pass|fail)" | head -30
```

If any test fails:
- `execute-phase-active-flags` failure → re-read tests/execute-phase-active-flags.test.cjs lines 44–65 and commands/gsd/execute-phase.md lines 50–58; ensure assertion string matches source text exactly (including backtick style)
- Command corpus scan failure → read the failing line from the scanner output; confirm the Edit applied to the correct file

Run `npm test` for the complete pass/fail count.
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && npm test 2>&1 | grep -E "(no bare DO NOT directives in command|context explicitly warns|Tests:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test 2>&1 | grep "no bare DO NOT directives in command files"` shows a passing line (no assertion error)
    - `npm test 2>&1 | grep "execute-phase-active-flags"` shows all subtests passing (no "fail" lines)
    - `npm test 2>&1 | grep "context should require deriving flag state"` shows the updated assertion passing
    - `npm test` full output shows 0 test failures (same pass count as before this phase began or higher)
  </acceptance_criteria>
  <done>Full test suite green: command DO NOT corpus scan passes, execute-phase-active-flags passes with the updated assertion, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→markdown file | Text edits applied to command prompt files — no secrets, no auth |
| editor→test file | Test assertion string updated to match new source text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | execute-phase.md + test atomicity | mitigate | Both files edited in same task; test suite run immediately after to confirm no broken intermediate state |
| T-14-04 | Tampering | Assertion string mismatch (backtick style) | mitigate | Read test file lines 44–65 before editing; copy exact replacement string from D-11 verbatim including backtick characters |
</threat_model>

<verification>
After all tasks complete:
- `grep -i "do not infer" commands/gsd/docs-update.md` → 0 results
- `grep -i "do not infer" commands/gsd/execute-phase.md` → 0 results
- `grep "Do not infer" tests/execute-phase-active-flags.test.cjs` → 0 results
- `grep -i "do not proceed to cleanup" commands/gsd/reapply-patches.md` → 0 results
- `npm test 2>&1 | grep "no bare DO NOT directives in command files"` → passes
- `npm test 2>&1 | grep "execute-phase-active-flags"` → all subtests passing
</verification>

<success_criteria>
1. docs-update.md, execute-phase.md, and reapply-patches.md each contain only the affirmative inference rule / sequencing gate — zero "do not" phrasing at those lines
2. tests/execute-phase-active-flags.test.cjs line 50 asserts the new positive text; the old "Do not infer..." string does not appear anywhere in the file
3. npm test command DO NOT corpus scan subtest shows 0 violations
4. npm test execute-phase-active-flags suite passes with 0 failures
5. Full npm test shows 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/14-workflow-reference-and-command-fixes/14-02-SUMMARY.md`
</output>
