---
phase: 14-workflow-reference-and-command-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/analyze-dependencies.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/import.md
  - get-shit-done/workflows/transition.md
  - get-shit-done/workflows/verify-phase.md
  - get-shit-done/references/planner-source-audit.md
autonomous: true
requirements:
  - FRAMING-07
  - FRAMING-08
  - FRAMING-09
  - FRAMING-10
  - FRAMING-11
  - FRAMING-12
  - FRAMING-13
  - FRAMING-14

must_haves:
  truths:
    - "analyze-dependencies.md line ~100 contains 'Preserve the existing phase order — relocate only the dependency field' with no 'do not' phrasing"
    - "discuss-phase.md line ~172 contains 'Stop here — power mode handles all remaining steps' with no 'do not' phrasing"
    - "execute-plan.md line ~203 contains 'Scope auto-fixes to issues introduced by the current task only' with no 'do not' phrasing"
    - "import.md Anti-Patterns block is replaced with Required Patterns — no 'Do NOT:' header remains, all 7 list items are positive imperatives"
    - "transition.md lines containing 'Do NOT suggest' and 'Do NOT auto-invoke' are deleted; the '**Stop here.**' line remains intact"
    - "verify-phase.md line ~241 contains 'Source inputs exclusively from actual test fixtures and codebase examples' with no 'do not' phrasing"
    - "planner-source-audit.md line ~30 contains 'Treat these as expected and exclude them from MISSING flags:' with no 'do not' phrasing"
    - "npm test shows the workflow and reference DO NOT corpus scan suites passing"
  artifacts:
    - path: "get-shit-done/workflows/analyze-dependencies.md"
      provides: "FRAMING-07 fix"
      contains: "Preserve the existing phase order — relocate only the dependency field"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "FRAMING-08 fix"
      contains: "Stop here — power mode handles all remaining steps"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "FRAMING-09 fix"
      contains: "Scope auto-fixes to issues introduced by the current task only"
    - path: "get-shit-done/workflows/import.md"
      provides: "FRAMING-10 fix"
      contains: "Required Patterns"
    - path: "get-shit-done/workflows/transition.md"
      provides: "FRAMING-11 + FRAMING-12 fix"
    - path: "get-shit-done/workflows/verify-phase.md"
      provides: "FRAMING-13 fix"
      contains: "Source inputs exclusively from actual test fixtures and codebase examples"
    - path: "get-shit-done/references/planner-source-audit.md"
      provides: "FRAMING-14 fix"
      contains: "Treat these as expected and exclude them from MISSING flags:"
  key_links:
    - from: "get-shit-done/workflows/import.md"
      to: "Required Patterns block"
      via: "block rewrite of Anti-Patterns section (lines 274–283)"
      pattern: "Required Patterns"
    - from: "get-shit-done/workflows/transition.md"
      to: "**Stop here.** line"
      via: "deletion of lines 567–568, preserving the Stop here gate"
      pattern: "Stop here"
---

<objective>
Fix FRAMING-07 through FRAMING-14: replace 8 bare "do not" directive violations across 6 workflow files and 1 reference file with affirmative positive instructions. These are pure text edits — no new functionality, no structural changes beyond the targeted lines.

Purpose: Clear all bare "do not" violations in the workflow and reference directories so the corpus scan tests pass for those directories.
Output: 7 edited markdown files with all negative directives replaced by affirmative equivalents.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/14-workflow-reference-and-command-fixes/14-CONTEXT.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/REQUIREMENTS.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/14-workflow-reference-and-command-fixes/14-CONTEXT.md

<interfaces>
<!-- Exact current text at each violation site — verified by direct file read in 14-RESEARCH.md -->

FRAMING-07 (analyze-dependencies.md:100):
  current:  `- Do not reorder phases`
  replace:  `- Preserve the existing phase order — relocate only the dependency field`
  read_ctx: lines 97–103

FRAMING-08 (discuss-phase.md:172):
  current:  `- Do not continue with the steps below`
  replace:  `- Stop here — power mode handles all remaining steps`
  read_ctx: lines 169–175

FRAMING-09 (execute-plan.md:203):
  current:  `- **Scope boundary**: do not auto-fix pre-existing issues unrelated to current task`
  replace:  `- **Scope boundary**: Scope auto-fixes to issues introduced by the current task only — leave pre-existing issues untouched`
  read_ctx: lines 198–207

FRAMING-10 (import.md:276 — block rewrite, lines 274–283):
  current block:
    ## Anti-Patterns

    Do NOT:
    - Use markdown tables (`|---|`) in the conflict detection report — use plain-text [BLOCKER]/[WARNING]/[INFO] labels
    - Write PLAN.md files as `PLAN-01.md` or `plan-01.md` — always use `{NN}-{MM}-PLAN.md`
    - Use `pbr:plan-checker` or `pbr:planner` — use `gsd-plan-checker` and `gsd-planner`
    - Write `.planning/.active-skill` — this is a PBR pattern with no GSD equivalent
    - Reference `pbr-tools`, `pbr:`, or `PLAN-BUILD-RUN` anywhere
    - Write any PLAN.md file when blockers exist — the safety gate must hold
    - Skip path validation on the --from file argument
  replace block:
    ## Required Patterns

    Use these conventions in all import operations:
    - Use plain-text [BLOCKER]/[WARNING]/[INFO] labels in the conflict detection report
    - Name PLAN.md files using the `{NN}-{MM}-PLAN.md` format
    - Use `gsd-plan-checker` and `gsd-planner` for plan checking and planning
    - Omit `.planning/.active-skill` — this PBR pattern has no GSD equivalent
    - Use only GSD-native tool names and prefixes
    - Hold the safety gate: skip PLAN.md creation when blockers exist
    - Validate the `--from` file argument path before proceeding

FRAMING-11 + FRAMING-12 (transition.md:567–568 — deletion):
  current lines 567–568:
    Do NOT suggest `/gsd-complete-milestone` or `/gsd-new-milestone`.
    Do NOT auto-invoke any further slash commands.
  replace: delete both lines entirely (including blank line between --- and **Stop here.**)
  read_ctx: lines 562–575

FRAMING-13 (verify-phase.md:241):
  current:  `   Do NOT invent example inputs.`   (3-space indent preserved)
  replace:  `   Source inputs exclusively from actual test fixtures and codebase examples.`
  read_ctx: lines 235–246

FRAMING-14 (planner-source-audit.md:30):
  current:  `Do not flag these as MISSING:`
  replace:  `Treat these as expected and exclude them from MISSING flags:`
  read_ctx: lines 27–35
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FRAMING-07 through FRAMING-09 (analyze-dependencies, discuss-phase, execute-plan)</name>
  <files>
    get-shit-done/workflows/analyze-dependencies.md
    get-shit-done/workflows/discuss-phase.md
    get-shit-done/workflows/execute-plan.md
  </files>
  <read_first>
    - get-shit-done/workflows/analyze-dependencies.md (lines 97–103 — read before edit to confirm exact surrounding context)
    - get-shit-done/workflows/discuss-phase.md (lines 169–175 — read before edit to confirm exact surrounding context)
    - get-shit-done/workflows/execute-plan.md (lines 198–207 — read before edit to confirm exact surrounding context)
  </read_first>
  <action>
Apply three single-line replacements using the Edit tool (not Write). Read each file at the specified offset before editing to confirm the exact current text.

**FRAMING-07 — analyze-dependencies.md**
Find and replace (exact string match):
  old: `- Do not reorder phases`
  new: `- Preserve the existing phase order — relocate only the dependency field`

**FRAMING-08 — discuss-phase.md**
Find and replace (exact string match):
  old: `- Do not continue with the steps below`
  new: `- Stop here — power mode handles all remaining steps`

**FRAMING-09 — execute-plan.md**
Find and replace (exact string match):
  old: `- **Scope boundary**: do not auto-fix pre-existing issues unrelated to current task`
  new: `- **Scope boundary**: Scope auto-fixes to issues introduced by the current task only — leave pre-existing issues untouched`

Use the Edit tool for each replacement individually. Do not use Write on any of these files — Write risks clobbering unrelated content or mutating line endings.
  </action>
  <verify>
    <automated>grep -n "Preserve the existing phase order" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/analyze-dependencies.md && grep -n "Stop here — power mode" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/discuss-phase.md && grep -n "Scope auto-fixes to issues introduced" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Do not reorder phases" get-shit-done/workflows/analyze-dependencies.md` returns no output (0 matches)
    - `grep "Preserve the existing phase order" get-shit-done/workflows/analyze-dependencies.md` returns exactly 1 match
    - `grep "Do not continue with the steps below" get-shit-done/workflows/discuss-phase.md` returns no output (0 matches)
    - `grep "Stop here — power mode handles all remaining steps" get-shit-done/workflows/discuss-phase.md` returns exactly 1 match
    - `grep -i "do not auto-fix" get-shit-done/workflows/execute-plan.md` returns no output (0 matches)
    - `grep "Scope auto-fixes to issues introduced by the current task only" get-shit-done/workflows/execute-plan.md` returns exactly 1 match
  </acceptance_criteria>
  <done>All three workflow files contain the affirmative replacement text; no "do not" phrasing remains at those lines.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FRAMING-10 through FRAMING-13 (import.md, transition.md, verify-phase.md) and FRAMING-14 (planner-source-audit.md)</name>
  <files>
    get-shit-done/workflows/import.md
    get-shit-done/workflows/transition.md
    get-shit-done/workflows/verify-phase.md
    get-shit-done/references/planner-source-audit.md
  </files>
  <read_first>
    - get-shit-done/workflows/import.md (lines 273–285 — read the full Anti-Patterns block before the block rewrite)
    - get-shit-done/workflows/transition.md (lines 562–575 — read to confirm exact text of lines to delete and that Stop here. line remains)
    - get-shit-done/workflows/verify-phase.md (lines 235–246 — read before edit, note 3-space indentation on line 241)
    - get-shit-done/references/planner-source-audit.md (lines 27–35 — read before edit to confirm surrounding list-header structure)
  </read_first>
  <action>
Apply four changes using the Edit tool (not Write). Read each file at the specified offset before editing.

**FRAMING-10 — import.md (block rewrite, lines ~274–283)**
Use Edit with the FULL current block as old_string and the replacement block as new_string. This is an atomic block rewrite — do not edit only the header line.

old_string (exact):
```
## Anti-Patterns

Do NOT:
- Use markdown tables (`|---|`) in the conflict detection report — use plain-text [BLOCKER]/[WARNING]/[INFO] labels
- Write PLAN.md files as `PLAN-01.md` or `plan-01.md` — always use `{NN}-{MM}-PLAN.md`
- Use `pbr:plan-checker` or `pbr:planner` — use `gsd-plan-checker` and `gsd-planner`
- Write `.planning/.active-skill` — this is a PBR pattern with no GSD equivalent
- Reference `pbr-tools`, `pbr:`, or `PLAN-BUILD-RUN` anywhere
- Write any PLAN.md file when blockers exist — the safety gate must hold
- Skip path validation on the --from file argument
```

new_string (exact):
```
## Required Patterns

Use these conventions in all import operations:
- Use plain-text [BLOCKER]/[WARNING]/[INFO] labels in the conflict detection report
- Name PLAN.md files using the `{NN}-{MM}-PLAN.md` format
- Use `gsd-plan-checker` and `gsd-planner` for plan checking and planning
- Omit `.planning/.active-skill` — this PBR pattern has no GSD equivalent
- Use only GSD-native tool names and prefixes
- Hold the safety gate: skip PLAN.md creation when blockers exist
- Validate the `--from` file argument path before proceeding
```

**FRAMING-11 + FRAMING-12 — transition.md (delete lines 567–568)**
Read lines 562–575 first to confirm exact current text. Use Edit to delete the two violation lines.

old_string (exact — find this block in the file):
```
Do NOT suggest `/gsd-complete-milestone` or `/gsd-new-milestone`.
Do NOT auto-invoke any further slash commands.
```
new_string: empty string (blank — delete those lines entirely)

After deletion, verify the surrounding structure is intact: the `---` separator before and the `**Stop here.**` line after must both remain.

**FRAMING-13 — verify-phase.md (line ~241)**
Find and replace (exact string match, preserving 3-space indent):
  old: `   Do NOT invent example inputs.`
  new: `   Source inputs exclusively from actual test fixtures and codebase examples.`

**FRAMING-14 — planner-source-audit.md (line ~30)**
Find and replace (exact string match):
  old: `Do not flag these as MISSING:`
  new: `Treat these as expected and exclude them from MISSING flags:`

Note: preserve the trailing colon — the bullet items below attach to it grammatically.
  </action>
  <verify>
    <automated>grep -n "Required Patterns" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/import.md && grep -c "Do NOT" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/import.md && grep -n "Stop here" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/transition.md && grep -n "Source inputs exclusively" /home/thamw/development/happier/get-shit-done/get-shit-done/workflows/verify-phase.md && grep -n "Treat these as expected and exclude them from MISSING flags" /home/thamw/development/happier/get-shit-done/get-shit-done/references/planner-source-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Anti-Patterns" get-shit-done/workflows/import.md` returns 0 (section header renamed)
    - `grep "Required Patterns" get-shit-done/workflows/import.md` returns exactly 1 match
    - `grep "Do NOT:" get-shit-done/workflows/import.md` returns no output (header deleted)
    - `grep -i "do not suggest" get-shit-done/workflows/transition.md` returns no output (line deleted)
    - `grep -i "do not auto-invoke" get-shit-done/workflows/transition.md` returns no output (line deleted)
    - `grep "Stop here" get-shit-done/workflows/transition.md` returns at least 1 match (adjacent line preserved)
    - `grep "Do NOT invent" get-shit-done/workflows/verify-phase.md` returns no output
    - `grep "Source inputs exclusively from actual test fixtures and codebase examples" get-shit-done/workflows/verify-phase.md` returns exactly 1 match
    - `grep "Do not flag these as MISSING" get-shit-done/references/planner-source-audit.md` returns no output
    - `grep "Treat these as expected and exclude them from MISSING flags:" get-shit-done/references/planner-source-audit.md` returns exactly 1 match (colon present)
  </acceptance_criteria>
  <done>import.md Anti-Patterns block fully converted to Required Patterns; transition.md violation lines deleted; verify-phase.md and planner-source-audit.md lines replaced with affirmative text.</done>
</task>

<task type="auto">
  <name>Task 3: Run corpus scan gate for workflow and reference directories</name>
  <files></files>
  <read_first>
    - tests/negative-framing-scan.test.cjs (to understand what the scanner checks — confirm the workflow and reference subtests)
  </read_first>
  <action>
Run the full test suite and verify the workflow and reference corpus scan suites show 0 failures.

Command:
```bash
npm test 2>&1 | grep -E "(no bare DO NOT directives|DO NOT primary directives|workflow|reference|pass|fail)" | head -40
```

If ANY violation remains in the workflow or reference directories, the grep output will show which file and line. Read that file at the specified line, identify why the Edit did not apply, and re-apply the fix.

The scanner targets:
- `get-shit-done/workflows/` — must have 0 bare DO NOT violations
- `get-shit-done/references/` — must have 0 bare DO NOT violations

A "bare" DO NOT directive is one with no positive complement on the same line (no em-dash complement, no parenthetical).

Do NOT run the full `npm test` yet — that is reserved for Plan 02 after all command fixes are applied. Run only the targeted grep on test output to check workflow and reference subdirectories.

Actually: run `npm test` — the full suite takes ~5 seconds and gives the complete picture. Check that the workflow and reference DO NOT subtests show green.
  </action>
  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && npm test 2>&1 | grep -E "no bare DO NOT directives in (workflow|reference)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test 2>&1 | grep "no bare DO NOT directives in workflow files"` shows a passing line (no failure/assertion error)
    - `npm test 2>&1 | grep "no bare DO NOT directives in reference files"` shows a passing line (no failure/assertion error)
    - `grep -ri "do not" get-shit-done/workflows/analyze-dependencies.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/import.md get-shit-done/workflows/transition.md get-shit-done/workflows/verify-phase.md get-shit-done/references/planner-source-audit.md` returns 0 bare violations (em-dash complements are acceptable per scanner logic, but none should remain from our target lines)
  </acceptance_criteria>
  <done>Corpus scan confirms 0 bare DO NOT violations remain in the workflow and reference directories addressed by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→markdown file | Text edits applied to prompt files — no secrets, no auth, no runtime execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Edit tool on markdown files | accept | Pure text replacement; Edit tool sends diffs not rewrites; low-value target |
| T-14-02 | Tampering | FRAMING-10 block rewrite | mitigate | Read full block before edit to confirm old_string matches exactly; avoid accidental content drop |
</threat_model>

<verification>
After all tasks complete:
- `grep -rn "Do not reorder" get-shit-done/workflows/analyze-dependencies.md` → 0 results
- `grep -rn "Do not continue with the steps below" get-shit-done/workflows/discuss-phase.md` → 0 results
- `grep -in "do not auto-fix" get-shit-done/workflows/execute-plan.md` → 0 results
- `grep "Anti-Patterns" get-shit-done/workflows/import.md` → 0 results
- `grep -i "Do NOT suggest" get-shit-done/workflows/transition.md` → 0 results
- `grep -i "Do NOT auto-invoke" get-shit-done/workflows/transition.md` → 0 results
- `grep "Do NOT invent" get-shit-done/workflows/verify-phase.md` → 0 results
- `grep "Do not flag these as MISSING" get-shit-done/references/planner-source-audit.md` → 0 results
- `npm test 2>&1 | grep "no bare DO NOT directives in workflow files"` → passes
- `npm test 2>&1 | grep "no bare DO NOT directives in reference files"` → passes
</verification>

<success_criteria>
1. All 7 workflow and reference files contain only affirmative instructions at the previously-violating lines — zero occurrences of bare "do not" (any capitalisation) at those lines
2. import.md section heading "Anti-Patterns" no longer exists; "Required Patterns" heading is present; all 7 list items are positive imperatives
3. transition.md lines containing "Do NOT suggest" and "Do NOT auto-invoke" are deleted; the "**Stop here.**" line remains
4. npm test corpus scan subtests for workflow and reference directories show 0 violations
</success_criteria>

<output>
After completion, create `.planning/phases/14-workflow-reference-and-command-fixes/14-01-SUMMARY.md`
</output>
