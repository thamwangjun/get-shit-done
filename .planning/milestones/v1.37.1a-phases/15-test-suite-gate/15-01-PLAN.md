---
phase: 15-test-suite-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TEST-05

must_haves:
  truths:
    - "REQUIREMENTS.md contains a Fixed: annotation under each of FRAMING-07 through FRAMING-17 with before/after text"
    - "ROADMAP.md Phase 15 SC-2 reads 'at least 4168 tests passing' (not 4142)"
    - "REQUIREMENTS.md TEST-05 description references 4168 (not 4142) if it contains a count"
    - "Full npm test run exits with 0 failures and 4168 tests passing"
    - "TEST-05 is marked ✓ in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Annotated violation requirements and marked-complete TEST-05"
      contains: "Fixed:"
    - path: ".planning/ROADMAP.md"
      provides: "Updated test count floor in Phase 15 success criteria"
      contains: "4168"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "TEST-05"
      via: "✓ checkbox marking"
      pattern: "✓.*TEST-05"
    - from: ".planning/ROADMAP.md"
      to: "Phase 15 SC-2"
      via: "test count floor"
      pattern: "4168"
---

<objective>
Document the Phase 14 fixes inline in REQUIREMENTS.md (FRAMING-07 through FRAMING-17 Fixed: annotations), update the test count floor from 4142 to 4168 in ROADMAP.md and REQUIREMENTS.md, run the full test suite to confirm 4/4 corpus scan subtests pass and 0 failures, then mark TEST-05 complete.

Purpose: Formally close the v1.37.1a Do-Not Framing Pass milestone with verified evidence that all violations were fixed and no regressions were introduced.
Output: Annotated REQUIREMENTS.md (with Fixed: entries and ✓ TEST-05), updated ROADMAP.md SC-2 test count, passing test run.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/PROJECT.md
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/STATE.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/15-test-suite-gate/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate FRAMING-07 through FRAMING-17 in REQUIREMENTS.md with Fixed: before/after text</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read the full file to see the current FRAMING-07 through FRAMING-17 entries and TEST-05 entry before making any edits
  </read_first>
  <action>
Edit .planning/REQUIREMENTS.md to add a `Fixed:` annotation line directly below each FRAMING-07 through FRAMING-17 entry. The annotation must go on a new indented line immediately after the existing requirement description. Use this exact before/after text extracted from the Phase 14 git diff:

FRAMING-07 (analyze-dependencies.md):
  Fixed: `Do not reorder phases` → `Preserve the existing phase order — relocate only the dependency field`

FRAMING-08 (discuss-phase.md):
  Fixed: `Do not continue with the steps below` → `Stop here — power mode handles all remaining steps`

FRAMING-09 (execute-plan.md):
  Fixed: `do not auto-fix pre-existing issues unrelated to current task` → `Scope auto-fixes to issues introduced by the current task only — leave pre-existing issues untouched`

FRAMING-10 (import.md):
  Fixed: `Do NOT:` block header + 7 negative list items → `Required Patterns` section header + 7 affirmative imperative conventions (full block rewritten)

FRAMING-11 (transition.md):
  Fixed: `Do NOT suggest /gsd-complete-milestone or /gsd-new-milestone.` → line deleted; `**Stop here.**` gate already covers the intent

FRAMING-12 (transition.md):
  Fixed: `Do NOT auto-invoke any further slash commands.` → line deleted; `**Stop here.**` gate already covers the intent

FRAMING-13 (verify-phase.md):
  Fixed: `Do NOT invent example inputs.` → `Source inputs exclusively from actual test fixtures and codebase examples.`

FRAMING-14 (planner-source-audit.md):
  Fixed: `Do not flag these as MISSING:` → `Treat these as expected and exclude them from MISSING flags:`

FRAMING-15 (docs-update.md):
  Fixed: `Do not infer that a flag is active just because it is documented in this prompt` → `Treat a flag as active only if its literal token is present in \`$ARGUMENTS\``

FRAMING-16 (execute-phase.md + test):
  Fixed: `Do not infer that a flag is active just because it is documented in this prompt` → `Treat a flag as active only if its literal token is present in \`$ARGUMENTS\``; `tests/execute-phase-active-flags.test.cjs` line 50 assertion updated to assert new positive text

FRAMING-17 (reapply-patches.md):
  Fixed: `Do not proceed to cleanup until the user confirms they have resolved all unverified hunks.` → `Proceed to Step 6 only after the user confirms all unverified hunks are resolved.`

Format pattern: the Fixed: line is an indented continuation of the requirement bullet, for example:
```
- [x] **FRAMING-07**: `get-shit-done/workflows/analyze-dependencies.md:100` — bare `Do not reorder phases` replaced with positive instruction to preserve phase order
  - Fixed: `Do not reorder phases` → `Preserve the existing phase order — relocate only the dependency field`
```

Apply this pattern to all 11 entries (FRAMING-07 through FRAMING-17).
  </action>
  <verify>
    <automated>grep -c "Fixed:" /home/thamw/development/happier/get-shit-done/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Fixed:" .planning/REQUIREMENTS.md` returns exactly 11
    - `grep "Fixed:" .planning/REQUIREMENTS.md` shows one annotation for each of FRAMING-07 through FRAMING-17
    - `grep "Fixed:.*Do not reorder" .planning/REQUIREMENTS.md` returns the FRAMING-07 annotation line
    - `grep "Fixed:.*Treat a flag as active" .planning/REQUIREMENTS.md` returns exactly 2 lines (FRAMING-15 and FRAMING-16)
    - `grep "Fixed:.*Proceed to Step 6" .planning/REQUIREMENTS.md` returns the FRAMING-17 annotation line
  </acceptance_criteria>
  <done>All 11 FRAMING-XX entries (FRAMING-07 through FRAMING-17) in REQUIREMENTS.md have a Fixed: annotation line with the exact before/after text from the Phase 14 git commits.</done>
</task>

<task type="auto">
  <name>Task 2: Update test count floor to 4168 in ROADMAP.md and REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md — read the Phase 15 section to locate SC-2 ("4142" occurrence) before editing
    - .planning/REQUIREMENTS.md — read the TEST-05 entry to check if it contains a count reference before editing
  </read_first>
  <action>
Two edits required (per D-03):

1. In .planning/ROADMAP.md: Find the Phase 15 success criterion SC-2 which currently reads "at least 4142 tests passing". Change `4142` to `4168` in that line only.

2. In .planning/REQUIREMENTS.md: Locate the TEST-05 entry under "### Test Gate". The current text is:
   `- [ ] **TEST-05**: \`corpus scan — DO NOT primary directives (case-insensitive)\` suite passes — all 4 tests green; no \`do not\` (any capitalisation) present in any fixed line`

   This entry does not contain "4142" — no count change needed in REQUIREMENTS.md. Leave the TEST-05 text as-is; the count update only applies to ROADMAP.md SC-2.

Only change `4142` → `4168` in ROADMAP.md. Do not change any other numbers.
  </action>
  <verify>
    <automated>grep "4168" /home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "4168" .planning/ROADMAP.md` returns at least one line containing "4168" in the Phase 15 success criteria block
    - `grep "4142" .planning/ROADMAP.md` returns zero lines (the old count is fully replaced)
    - `grep "4142" .planning/REQUIREMENTS.md` returns zero lines (confirm no stale count in requirements)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 15 SC-2 reads "at least 4168 tests passing" with no remaining "4142" occurrences in either ROADMAP.md or REQUIREMENTS.md.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite gate and mark TEST-05 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read the TEST-05 entry (under "### Test Gate") to confirm its current checkbox state before marking complete
    - tests/negative-framing-scan.test.cjs — read lines 1-30 to understand which subtests the corpus scanner runs
  </read_first>
  <action>
Two steps in sequence:

Step 1 — Run the tests. Execute both commands and capture output:
```
node --test tests/negative-framing-scan.test.cjs
npm test
```

Confirm the corpus scanner shows 4/4 subtests passing for "DO NOT primary directives (case-insensitive)" and "NEVER primary directives". Confirm `npm test` exits with 0 failures and reports 4168 total tests passing.

Step 2 — Mark TEST-05 complete. In .planning/REQUIREMENTS.md, change the TEST-05 checkbox from:
  `- [ ] **TEST-05**:`
to:
  `- [x] **TEST-05**:`

Also update the Traceability table row for TEST-05 from `Pending` to `Complete`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - `npm test 2>&1 | grep "corpus scan — DO NOT primary directives"` output shows 4 passing subtests with no failures
    - `npm test 2>&1 | grep "corpus scan — NEVER primary directives"` output shows 4 passing subtests with no failures
    - `npm test 2>&1 | grep -E "^# tests"` shows 4168 (not less)
    - `grep "\[x\].*TEST-05" .planning/REQUIREMENTS.md` returns one line (checkbox is checked)
    - `grep "TEST-05.*Complete" .planning/REQUIREMENTS.md` returns one line in the Traceability table
  </acceptance_criteria>
  <done>npm test passes with 0 failures (4168 tests), corpus scan shows 4/4 DO NOT and 4/4 NEVER subtests green, and TEST-05 is marked ✓ in REQUIREMENTS.md with the Traceability table updated to "Complete".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → file system | Test reads prompt files — no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | REQUIREMENTS.md manual edits | accept | Documentation-only phase; no production code changed; git history provides audit trail |
| T-15-02 | Repudiation | Test result not captured | accept | npm test output is transient; SUMMARY.md captures pass/fail evidence post-execution |
</threat_model>

<verification>
After all three tasks complete:
- `grep -c "Fixed:" .planning/REQUIREMENTS.md` returns 11
- `grep "4168" .planning/ROADMAP.md` shows SC-2 line
- `grep "4142" .planning/ROADMAP.md` returns nothing
- `grep "\[x\].*TEST-05" .planning/REQUIREMENTS.md` returns one match
- `grep "TEST-05.*Complete" .planning/REQUIREMENTS.md` returns one match in the Traceability table
- `npm test` exits 0 with 4168 passing tests
</verification>

<success_criteria>
1. All 11 FRAMING-07 through FRAMING-17 entries in REQUIREMENTS.md carry a `Fixed:` annotation with exact before/after text from the Phase 14 git commits (11 annotations total, one per entry).
2. ROADMAP.md Phase 15 SC-2 reads "at least 4168 tests passing" — the old "4142" does not appear anywhere in ROADMAP.md.
3. `npm test` exits with 0 failures; corpus scan shows 4/4 DO NOT subtests and 4/4 NEVER subtests passing; total test count is 4168.
4. TEST-05 is marked `[x]` in REQUIREMENTS.md and shows "Complete" in the Traceability table.
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-suite-gate/15-01-SUMMARY.md` following the summary template.
</output>
