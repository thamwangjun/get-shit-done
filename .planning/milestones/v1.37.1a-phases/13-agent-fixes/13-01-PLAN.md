---
phase: 13-agent-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-assumptions-analyzer.md
  - agents/gsd-doc-verifier.md
  - agents/gsd-user-profiler.md
autonomous: true
requirements:
  - FRAMING-01
  - FRAMING-05
  - FRAMING-06
must_haves:
  truths:
    - "agents/gsd-assumptions-analyzer.md line 111 contains no 'do not' phrase and references <calibration_tiers>"
    - "agents/gsd-doc-verifier.md line 92 reads 'Skip verification for the following:' with no 'do not' phrase"
    - "agents/gsd-user-profiler.md line 88 is a positive sequencing gate with no 'do not' phrase"
  artifacts:
    - path: "agents/gsd-assumptions-analyzer.md"
      provides: "FRAMING-01 fix: positive tier-reference bullet replacing bare Do NOT directive"
      contains: "Keep area count within the tier limit defined in"
    - path: "agents/gsd-doc-verifier.md"
      provides: "FRAMING-05 fix: affirmative skip_rules header"
      contains: "Skip verification for the following:"
    - path: "agents/gsd-user-profiler.md"
      provides: "FRAMING-06 fix: positive sequencing gate"
      contains: "Load the rubric fully before proceeding to message analysis."
  key_links:
    - from: "agents/gsd-assumptions-analyzer.md:111"
      to: "<calibration_tiers> block (lines 39-56)"
      via: "backreference in replacement text"
      pattern: "calibration_tiers"
---

<objective>
Replace three bare "do not" directive violations in three single-line targeted edits across gsd-assumptions-analyzer.md, gsd-doc-verifier.md, and gsd-user-profiler.md.

Purpose: Bring FRAMING-01, FRAMING-05, and FRAMING-06 into compliance with the fork's positive framing standard. Each replacement specifies the correct behavior using affirmative imperative language — no negative phrase remains.

Output: Three modified agent files. Each previously-violating line now uses a positive instruction. The negative-framing corpus scanner will no longer flag these lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-agent-fixes/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FRAMING-01 — gsd-assumptions-analyzer.md line 111</name>
  <files>agents/gsd-assumptions-analyzer.md</files>
  <read_first>
    - agents/gsd-assumptions-analyzer.md lines 105-114 (confirm exact current text of the violation and that no frontmatter contains "do not")
    - agents/gsd-assumptions-analyzer.md lines 39-56 (confirm <calibration_tiers> block is present — the replacement references it)
  </read_first>
  <action>
Use the Edit tool to replace the single violating bullet at line 111.

OLD TEXT (exact):
```
- Do NOT generate more areas than the calibration tier specifies
```

NEW TEXT (exact, per D-01):
```
- Keep area count within the tier limit defined in `<calibration_tiers>` above
```

Constraints:
- Use Edit tool only (not Write). The file is large; Write risks corrupting unrelated content.
- Do not modify any surrounding lines.
- Do not inline tier counts — the replacement must point to the block, not restate numbers.
- Confirm the <calibration_tiers> block at lines 39-56 uses the tag name `<calibration_tiers>` so the backreference in the replacement text is accurate.
  </action>
  <verify>
    <automated>grep -n "calibration_tiers" agents/gsd-assumptions-analyzer.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do NOT generate more areas" agents/gsd-assumptions-analyzer.md` returns no output (line deleted/replaced)
    - `grep -n "Keep area count within the tier limit" agents/gsd-assumptions-analyzer.md` returns exactly one match at approximately line 111
    - `grep -n "calibration_tiers" agents/gsd-assumptions-analyzer.md` shows the block tag AND the replacement reference line both present
    - `grep -in "do not generate" agents/gsd-assumptions-analyzer.md` returns no output (case-insensitive check)
  </acceptance_criteria>
  <done>Line 111 of gsd-assumptions-analyzer.md reads: `- Keep area count within the tier limit defined in \`<calibration_tiers>\` above` — the original "Do NOT generate" phrase is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FRAMING-05 — gsd-doc-verifier.md line 92</name>
  <files>agents/gsd-doc-verifier.md</files>
  <read_first>
    - agents/gsd-doc-verifier.md lines 88-100 (confirm exact current text of the violation, surrounding list context, and that no YAML frontmatter is near line 92)
  </read_first>
  <action>
Use the Edit tool to replace the list header at line 92.

OLD TEXT (exact):
```
Do NOT verify the following:
```

NEW TEXT (exact, per D-03 and REQUIREMENTS.md sample wording):
```
Skip verification for the following:
```

Constraints:
- Replace the header line only. The blank line below it and all bullet items beneath it stay untouched.
- Use Edit tool only (not Write).
- The replacement must contain zero instances of "do not", "DO NOT", or "NEVER".
  </action>
  <verify>
    <automated>grep -n "verify the following" agents/gsd-doc-verifier.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do NOT verify the following" agents/gsd-doc-verifier.md` returns no output
    - `grep -n "Skip verification for the following:" agents/gsd-doc-verifier.md` returns exactly one match at approximately line 92
    - `grep -in "do not verify" agents/gsd-doc-verifier.md` returns no output (case-insensitive check)
  </acceptance_criteria>
  <done>Line 92 of gsd-doc-verifier.md reads: `Skip verification for the following:` — the "Do NOT verify" phrase is gone. The bullet list beneath the header is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Fix FRAMING-06 — gsd-user-profiler.md line 88</name>
  <files>agents/gsd-user-profiler.md</files>
  <read_first>
    - agents/gsd-user-profiler.md lines 83-92 (confirm exact current text of the violation, that it is the last line before the closing </step> tag, and no YAML frontmatter is near)
  </read_first>
  <action>
Use the Edit tool to replace the sequencing gate sentence at line 88.

OLD TEXT (exact):
```
Do not proceed to message analysis until the rubric is loaded.
```

NEW TEXT (exact, per D-03 and REQUIREMENTS.md sample wording):
```
Load the rubric fully before proceeding to message analysis.
```

Constraints:
- Replace this one sentence only. The closing `</step>` tag on the next line and all surrounding content stay untouched.
- Use Edit tool only (not Write).
- The replacement must contain zero instances of "do not", "DO NOT", or "NEVER".
  </action>
  <verify>
    <automated>grep -n "rubric" agents/gsd-user-profiler.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do not proceed to message analysis" agents/gsd-user-profiler.md` returns no output
    - `grep -n "Load the rubric fully before proceeding to message analysis." agents/gsd-user-profiler.md` returns exactly one match at approximately line 88
    - `grep -in "do not proceed" agents/gsd-user-profiler.md` returns no output (case-insensitive check)
  </acceptance_criteria>
  <done>Line 88 of gsd-user-profiler.md reads: `Load the rubric fully before proceeding to message analysis.` — the "Do not proceed" phrase is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | The executor receives exact old/new text in this plan. If that text drifts from file reality (file was edited after research), the Edit tool will fail with no match — not silently corrupt. |
| Edit tool → file system | Edit is a targeted diff; it only modifies the matched region. File corruption is bounded to the matched block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | agents/*.md — unintended line edits | mitigate | Use Edit tool (not Write); read surrounding lines before each edit to confirm exact match; read_first block requires executor to verify current text |
| T-13-02 | Tampering | YAML frontmatter corruption | mitigate | read_first includes verifying frontmatter region is not near target lines; YAML integrity confirmed by agent-frontmatter test gate in Plan 03 |
| T-13-03 | Information Disclosure | "do not" phrase variant surviving in replacement | mitigate | acceptance_criteria includes case-insensitive grep for any "do not" variant after each edit; replacement text was pre-checked against scanner logic in RESEARCH.md |
| T-13-04 | Denial of Service | Test suite broken by accidental surrounding-line change | mitigate | Edit tool only touches the matched region; acceptance_criteria verifies adjacent structure (tags, bullets) are preserved |
</threat_model>

<verification>
After all three tasks complete:

```bash
grep -in "do not generate" agents/gsd-assumptions-analyzer.md
grep -in "do not verify" agents/gsd-doc-verifier.md
grep -in "do not proceed" agents/gsd-user-profiler.md
```

All three must return no output.

```bash
grep -n "Keep area count within the tier limit" agents/gsd-assumptions-analyzer.md
grep -n "Skip verification for the following:" agents/gsd-doc-verifier.md
grep -n "Load the rubric fully before proceeding" agents/gsd-user-profiler.md
```

All three must return exactly one match each.
</verification>

<success_criteria>
- agents/gsd-assumptions-analyzer.md line 111: `- Keep area count within the tier limit defined in \`<calibration_tiers>\` above`
- agents/gsd-doc-verifier.md line 92: `Skip verification for the following:`
- agents/gsd-user-profiler.md line 88: `Load the rubric fully before proceeding to message analysis.`
- No "do not" phrase of any capitalisation remains in any edited line
- No "NEVER" directive introduced in any replacement
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-fixes/13-01-SUMMARY.md`
</output>
