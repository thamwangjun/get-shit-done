---
phase: 13-agent-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified: []
autonomous: true
requirements:
  - FRAMING-01
  - FRAMING-02
  - FRAMING-03
  - FRAMING-04
  - FRAMING-05
  - FRAMING-06
must_haves:
  truths:
    - "corpus scan — DO NOT primary directives (case-insensitive) subtest for agent files passes with 0 violations"
    - "agent-frontmatter test passes — all 4 agent YAML blocks intact"
    - "All 6 previously-violating lines no longer match the /\\bdo not\\b/i pattern"
  artifacts:
    - path: "tests/negative-framing-scan.test.cjs"
      provides: "authoritative corpus scanner — must pass for agent files subtest"
      contains: "corpus scan"
    - path: "tests/agent-frontmatter.test.cjs"
      provides: "YAML integrity gate — must pass after all edits"
      contains: "agent-frontmatter"
  key_links:
    - from: "agents/*.md (all 4 edited files)"
      to: "tests/negative-framing-scan.test.cjs"
      via: "corpus scan reads SCAN_DIRS agent directory"
      pattern: "SCAN_DIRS"
    - from: "agents/*.md (all 4 edited files)"
      to: "tests/agent-frontmatter.test.cjs"
      via: "test parses YAML frontmatter of all agent files"
      pattern: "agent-frontmatter"
---

<objective>
Run both test gates to confirm all 6 "do not" violations from Plans 01 and 02 are resolved and no YAML frontmatter was accidentally corrupted.

Purpose: The test suite is the authoritative correctness gate for this phase. This plan makes the pass/fail verdict explicit and verifiable before the phase closes.

Output: Both test commands exit 0. The agent-files subtest in the corpus scan and the agent-frontmatter test both pass. Phase 13 is ready for handoff to Phase 14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-fixes/13-01-SUMMARY.md
@.planning/phases/13-agent-fixes/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run corpus scan — confirm agent DO NOT violations are cleared</name>
  <files></files>
  <read_first>
    - tests/negative-framing-scan.test.cjs lines 1-30 (confirm SCAN_DIRS includes agents/ and understand the subtest structure so the output can be interpreted correctly)
  </read_first>
  <action>
Run the negative framing corpus scan and confirm the agent files subtest passes.

Command:
```bash
node --test tests/negative-framing-scan.test.cjs 2>&1
```

Interpret the output:
- Look for the subtest named `no bare DO NOT directives in agent files` (or equivalent agent-directory subtest).
- It must show `pass` status.
- If any violation is reported in an agent file, identify which file and line, then determine whether it is a pre-existing violation in a non-phase-13 line or a regression from Plan 01 or 02.

If the subtest FAILS:
- Read the failing file at the reported line using the Read tool with offset/limit.
- Determine if the failure is in one of the 6 edited lines or in a different line not touched by this phase.
- If in an edited line: the Edit tool replacement introduced a residual "do not" phrase — re-edit that line using the exact replacement text specified in Plan 01 or Plan 02.
- If in a different line: this is a pre-existing violation outside Phase 13 scope — document in the SUMMARY and flag for Phase 14 triage.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs 2>&1 | grep -E "(pass|fail|DO NOT)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs 2>&1` exits 0 OR the agent-files subtest shows `pass` and any remaining failures are in non-agent SCAN_DIRS (workflow/reference/command files — those are Phase 14 scope)
    - The output contains no lines reporting violations in `agents/gsd-assumptions-analyzer.md`, `agents/gsd-code-fixer.md`, `agents/gsd-doc-verifier.md`, or `agents/gsd-user-profiler.md`
    - `grep -in "do not" agents/gsd-assumptions-analyzer.md agents/gsd-code-fixer.md agents/gsd-doc-verifier.md agents/gsd-user-profiler.md` returns only lines that are NOT the 6 previously-violating lines (any remaining matches must be in anti_patterns blocks referencing other agents' behaviors, or similar non-directive context exempt by the scanner)
  </acceptance_criteria>
  <done>The corpus scan agent-files subtest passes. None of the 6 edited lines appear in the violation report.</done>
</task>

<task type="auto">
  <name>Task 2: Run agent-frontmatter test — confirm YAML integrity</name>
  <files></files>
  <read_first>
    - tests/agent-frontmatter.test.cjs lines 1-40 (confirm which files are tested and which YAML fields are validated, so output can be interpreted)
  </read_first>
  <action>
Run the agent frontmatter integrity test and confirm it passes.

Command:
```bash
node --test tests/agent-frontmatter.test.cjs 2>&1
```

Interpret the output:
- All subtests must show `pass`.
- If any subtest FAILS, it indicates YAML frontmatter corruption in the named agent file.

If a test FAILS:
- Read the frontmatter of the failing agent file (lines 1-20 of that file) using the Read tool.
- Identify which YAML field is malformed or missing.
- Determine if the corruption was caused by an Edit from Plan 01 or Plan 02 (compare the failing field to the edit that was made).
- Restore the correct frontmatter field using the Edit tool with the exact correct YAML value (do not use Write on the full file).
- Re-run `node --test tests/agent-frontmatter.test.cjs` to confirm the fix.
  </action>
  <verify>
    <automated>node --test tests/agent-frontmatter.test.cjs 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/agent-frontmatter.test.cjs 2>&1` exits 0
    - Output shows all subtests as `pass` with 0 failures
    - No `fail` substring appears in the output
  </acceptance_criteria>
  <done>The agent-frontmatter test exits 0 with all subtests passing. YAML integrity confirmed for all 4 edited agent files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited agent files → test runner | The test runner reads files from disk. If Plan 01 or 02 left a residual "do not" phrase, Task 1 catches it. If Plan 01 or 02 corrupted YAML, Task 2 catches it. |
| test output → executor interpretation | Test output must be read carefully: the corpus scan tests multiple directories; only the agent subtest must pass at this phase (other directories are Phase 14 scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Tampering | Corpus scan reporting Phase 14 violations as Phase 13 failures | accept | Acceptance_criteria explicitly scopes pass condition to agent files only; executor instructed to distinguish agent vs. non-agent failures in output |
| T-13-11 | Tampering | YAML corruption undetected before this plan runs | mitigate | agent-frontmatter test is the authoritative YAML gate; Task 2 runs it and provides remediation path if it fails |
| T-13-12 | Denial of Service | Test runner not available (node not found) | accept | node:test is Node.js built-in; project already uses it in CI; fallback `npm test -- --test-name-pattern="agent-frontmatter"` documented in RESEARCH.md |
</threat_model>

<verification>
Both test commands must pass before this plan is complete:

```bash
node --test tests/agent-frontmatter.test.cjs 2>&1
```
Expected: exits 0, all subtests pass.

```bash
node --test tests/negative-framing-scan.test.cjs 2>&1 | grep -E "agent"
```
Expected: agent-files subtest shows pass.

Quick pre-run sanity check on all 4 edited files:
```bash
grep -in "do not generate" agents/gsd-assumptions-analyzer.md
grep -in "do not verify" agents/gsd-doc-verifier.md
grep -in "do not proceed" agents/gsd-user-profiler.md
grep -in "do not skip\|do not create review\|do not leave" agents/gsd-code-fixer.md
```
All must return no output before the test commands are run.
</verification>

<success_criteria>
- `node --test tests/agent-frontmatter.test.cjs` exits 0
- The agent-files subtest in `node --test tests/negative-framing-scan.test.cjs` passes
- No "do not" violation in any of the 4 phase-13 agent files appears in the scanner report
- Phase 13 is ready to hand off to Phase 14 (Workflow, Reference, and Command Fixes)
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-fixes/13-03-SUMMARY.md`
</output>
