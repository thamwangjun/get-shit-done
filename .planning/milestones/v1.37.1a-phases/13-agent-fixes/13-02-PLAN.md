---
phase: 13-agent-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-code-fixer.md
autonomous: true
requirements:
  - FRAMING-02
  - FRAMING-03
  - FRAMING-04
must_haves:
  truths:
    - "agents/gsd-code-fixer.md line 138 contains a positive instruction with no 'do not' phrase"
    - "agents/gsd-code-fixer.md line 240 ('Do NOT create REVIEW-FIX.md') no longer exists — bullet is deleted"
    - "agents/gsd-code-fixer.md rollback block contains a positive commit instruction with no 'do not' phrase"
  artifacts:
    - path: "agents/gsd-code-fixer.md"
      provides: "FRAMING-02/03/04 fixes: three targeted edits to remove all bare Do NOT directives"
      contains: "Apply the fix even when syntax checking is unavailable"
  key_links:
    - from: "agents/gsd-code-fixer.md:138"
      to: "Tier 3 Fallback block (lines 135-139)"
      via: "in-place bullet replacement maintaining block structure"
      pattern: "Apply the fix even when"
    - from: "agents/gsd-code-fixer.md:240"
      to: "exit block (lines 238-241)"
      via: "bullet deletion leaving two-bullet list"
      pattern: "Exit code 0"
    - from: "agents/gsd-code-fixer.md:344"
      to: "commit-failure rollback block (lines 341-346)"
      via: "in-place bullet replacement"
      pattern: "Restore all files to pre-fix state"
---

<objective>
Remove all three bare "do not" directive violations in gsd-code-fixer.md using three targeted edits: replace line 138, delete line 240 (no replacement needed), and replace line 344.

Purpose: Bring FRAMING-02, FRAMING-03, and FRAMING-04 into compliance with the fork's positive framing standard. One edit is a deletion (per D-02 locked decision — the surrounding exit block already implies nothing is created).

Output: gsd-code-fixer.md with three fewer "do not" violations. The two replacement lines use affirmative imperative language. The deleted line leaves a clean two-bullet exit block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-agent-fixes/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FRAMING-02 — gsd-code-fixer.md line 138</name>
  <files>agents/gsd-code-fixer.md</files>
  <read_first>
    - agents/gsd-code-fixer.md lines 133-142 (confirm exact current text of the Tier 3 Fallback block and the violation at line 138)
  </read_first>
  <action>
Use the Edit tool to replace the violating bullet in the Tier 3 Fallback block.

OLD TEXT (exact):
```
- Do NOT skip the fix just because syntax checking is unavailable
```

NEW TEXT (exact, per D-03 — affirmative rewrite matching adjacent bullet style):
```
- Apply the fix even when syntax checking is unavailable
```

Constraints:
- Use Edit tool only (not Write).
- Replace this one bullet. The surrounding bullets ("- Accept Tier 1 result" and "- Proceed to commit if Tier 1 passed") remain unchanged.
- The replacement must contain zero instances of "do not", "DO NOT", or "NEVER".
  </action>
  <verify>
    <automated>grep -n "syntax checking is unavailable" agents/gsd-code-fixer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do NOT skip the fix" agents/gsd-code-fixer.md` returns no output
    - `grep -n "Apply the fix even when syntax checking is unavailable" agents/gsd-code-fixer.md` returns exactly one match at approximately line 138
    - `grep -in "do not skip" agents/gsd-code-fixer.md` returns no output (case-insensitive check)
    - `grep -n "Accept Tier 1 result" agents/gsd-code-fixer.md` still returns a match (adjacent bullet intact)
  </acceptance_criteria>
  <done>The Tier 3 Fallback block at approximately line 135-139 has its middle bullet reading `- Apply the fix even when syntax checking is unavailable`. The "Do NOT skip the fix" phrase is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FRAMING-03 — delete gsd-code-fixer.md line 240</name>
  <files>agents/gsd-code-fixer.md</files>
  <read_first>
    - agents/gsd-code-fixer.md lines 236-244 (confirm exact current three-bullet exit block including the line to delete, and verify the surrounding lines that must remain)
  </read_first>
  <action>
Use the Edit tool to delete the redundant bullet at line 240 (no replacement — per D-02 locked decision).

The exit block currently reads (lines 238-241):
```
If status is `"clean"` or `"skipped"`:
- Exit with message: "No issues to fix -- REVIEW.md status is {status}."
- Do NOT create REVIEW-FIX.md
- Exit code 0 (not an error, just nothing to do)
```

Delete the `- Do NOT create REVIEW-FIX.md` bullet so the block becomes:
```
If status is `"clean"` or `"skipped"`:
- Exit with message: "No issues to fix -- REVIEW.md status is {status}."
- Exit code 0 (not an error, just nothing to do)
```

To delete the line using the Edit tool, use an old/new replacement where the old text includes the full line plus its trailing newline, and the new text omits it entirely. Specifically:

OLD TEXT (exact — include the trailing newline so no orphan blank line remains):
```
- Exit with message: "No issues to fix -- REVIEW.md status is {status}."
- Do NOT create REVIEW-FIX.md
- Exit code 0 (not an error, just nothing to do)
```

NEW TEXT (exact):
```
- Exit with message: "No issues to fix -- REVIEW.md status is {status}."
- Exit code 0 (not an error, just nothing to do)
```

Constraints:
- Use Edit tool only (not Write).
- After the edit, verify the exit block has exactly two bullets with no orphan blank line between them (per Pitfall 5 in RESEARCH.md).
  </action>
  <verify>
    <automated>grep -n "REVIEW-FIX.md" agents/gsd-code-fixer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do NOT create REVIEW-FIX.md" agents/gsd-code-fixer.md` returns no output
    - `grep -in "do not create review" agents/gsd-code-fixer.md` returns no output (case-insensitive check)
    - `grep -n "Exit code 0 (not an error, just nothing to do)" agents/gsd-code-fixer.md` returns exactly one match (bullet still present)
    - `grep -n 'Exit with message: "No issues to fix' agents/gsd-code-fixer.md` returns exactly one match (first bullet still present)
    - Reading lines 236-243 after the edit shows no orphan blank line between the two remaining bullets
  </acceptance_criteria>
  <done>The exit block for "clean" or "skipped" status contains exactly two bullets: the exit message bullet and the exit-code bullet. The "Do NOT create REVIEW-FIX.md" line is gone with no orphan blank lines.</done>
</task>

<task type="auto">
  <name>Task 3: Fix FRAMING-04 — gsd-code-fixer.md line 344 (adjusted for deletion shift)</name>
  <files>agents/gsd-code-fixer.md</files>
  <read_first>
    - agents/gsd-code-fixer.md lines 338-350 (confirm exact current text of the commit-failure rollback block; note: line number may have shifted by -1 after the FRAMING-03 deletion in Task 2 — read to find the exact current line before editing)
  </read_first>
  <action>
Use the Edit tool to replace the violating bullet in the commit-failure rollback block.

The rollback block looks like (line numbers approximate after FRAMING-03 deletion):
```
**If commit FAILS after successful edit:**
- Mark as "skipped: commit failed"
- Execute rollback_strategy to restore files to pre-fix state
- Do NOT leave uncommitted changes
- Document commit error in skip reason
- Continue to next finding
```

OLD TEXT (exact):
```
- Do NOT leave uncommitted changes
```

NEW TEXT (exact, per D-03 — avoids redundancy with the preceding `Execute rollback_strategy` bullet):
```
- Restore all files to pre-fix state before continuing
```

Constraints:
- Use Edit tool only (not Write).
- The replacement must contain zero instances of "do not", "DO NOT", or "NEVER".
- Do not modify the surrounding bullets ("- Mark as...", "- Execute rollback_strategy...", "- Document commit error...", "- Continue to next finding").
  </action>
  <verify>
    <automated>grep -n "uncommitted changes" agents/gsd-code-fixer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Do NOT leave uncommitted changes" agents/gsd-code-fixer.md` returns no output
    - `grep -n "Restore all files to pre-fix state before continuing" agents/gsd-code-fixer.md` returns exactly one match
    - `grep -in "do not leave" agents/gsd-code-fixer.md` returns no output (case-insensitive check)
    - `grep -n "Execute rollback_strategy to restore files to pre-fix state" agents/gsd-code-fixer.md` still returns a match (adjacent bullet intact)
    - `grep -n "Document commit error in skip reason" agents/gsd-code-fixer.md` still returns a match (adjacent bullet intact)
  </acceptance_criteria>
  <done>The commit-failure rollback block has its "Do NOT leave uncommitted changes" bullet replaced with `- Restore all files to pre-fix state before continuing`. All other bullets in the block are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | Exact old/new text supplied for all three edits. Line numbers may shift after Task 2 deletion — Task 3 read_first instructs executor to verify current line before editing. |
| Edit tool → file system | Edit is a targeted diff; corruption bounded to the matched region. The file is ~400 lines; Write is prohibited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | gsd-code-fixer.md — line shift after deletion | mitigate | Task 3 read_first explicitly notes line numbers may shift by -1 after Task 2 deletion; executor must re-read lines 338-350 before editing |
| T-13-06 | Tampering | Orphan blank line after bullet deletion (FRAMING-03) | mitigate | Task 2 action uses multi-line old text spanning the deleted bullet to prevent orphan newline; acceptance_criteria requires reading lines 236-243 post-edit |
| T-13-07 | Tampering | YAML frontmatter corruption | mitigate | Violation lines 138, 240, 344 are deep in the file body, far from the frontmatter YAML block; read_first confirms surrounding context; agent-frontmatter test gate in Plan 03 catches any corruption |
| T-13-08 | Information Disclosure | "do not" variant surviving in replacement | mitigate | All three replacements pre-checked against /\bdo not\b/i scanner pattern; acceptance_criteria includes case-insensitive grep after each edit |
| T-13-09 | Tampering | FRAMING-03 deletion removing wrong bullet | mitigate | Old text in Task 2 spans three lines to uniquely anchor the deletion within the exit block; no other three-line sequence in the file matches |
</threat_model>

<verification>
After all three tasks complete:

```bash
grep -in "do not skip" agents/gsd-code-fixer.md
grep -in "do not create review" agents/gsd-code-fixer.md
grep -in "do not leave" agents/gsd-code-fixer.md
```

All three must return no output.

```bash
grep -n "Apply the fix even when syntax checking is unavailable" agents/gsd-code-fixer.md
grep -n "Exit code 0 (not an error, just nothing to do)" agents/gsd-code-fixer.md
grep -n "Restore all files to pre-fix state before continuing" agents/gsd-code-fixer.md
```

All three must return exactly one match each.
</verification>

<success_criteria>
- gsd-code-fixer.md line ~138: `- Apply the fix even when syntax checking is unavailable`
- gsd-code-fixer.md line ~240: bullet `- Do NOT create REVIEW-FIX.md` deleted; exit block has two bullets, no orphan blank line
- gsd-code-fixer.md line ~344 (adjusted): `- Restore all files to pre-fix state before continuing`
- No "do not" phrase of any capitalisation remains in any edited or surrounding line
- No "NEVER" directive introduced
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-fixes/13-02-SUMMARY.md`
</output>
