---
phase: 33-positive-framing-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-planner.md
  - commands/gsd/discuss-phase.md
  - get-shit-done/workflows/edit-phase.md
  - get-shit-done/workflows/secure-phase.md
  - get-shit-done/workflows/reapply-patches.md
autonomous: true
requirements:
  - FRAME-01
  - FRAME-02
  - SCAN-12

must_haves:
  truths:
    - "node --test tests/negative-framing-scan.test.cjs reports # fail 0 with all 6 formerly-failing subtests green"
    - "get-shit-done/workflows/debug.md passes the scanner at 0 violations and 0 warnings (already clean — FRAME-01 satisfied by existing file state)"
    - "get-shit-done/workflows/reapply-patches.md passes the scanner at 0 violations after line 323 is fixed (FRAME-02)"
    - "All 12 lines across 5 files no longer contain flagged tokens in any active scanner bucket"
    - "D-01: All 5 currently-failing scanner files fixed (edit-phase.md, secure-phase.md, gsd-executor.md, gsd-planner.md, discuss-phase.md) — Phase 34 starts clean"
    - "D-02: Framing violations only — no structural critique changes applied; persona rewrites and tag conversions from .planning/critique/ are deferred"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "gsd-executor — DO NOT at line 517 replaced with affirmative form"
      contains: "Always HALT"
    - path: "agents/gsd-planner.md"
      provides: "gsd-planner — NEVER at line 203 replaced with affirmative form"
      contains: "Keep `<action>`"
    - path: "commands/gsd/discuss-phase.md"
      provides: "discuss-phase — Do not at line 33 replaced with affirmative instruction"
      contains: "Read the mode routing instructions first"
    - path: "get-shit-done/workflows/edit-phase.md"
      provides: "edit-phase — mustNot at line 191, antiPatterns at line 271, dont at lines 273–277 all replaced"
      contains: "<expected_patterns>"
    - path: "get-shit-done/workflows/secure-phase.md"
      provides: "secure-phase — mustNot + doNot at line 76 replaced with affirmative rewrite"
      contains: "always proceed to Step 5"
    - path: "get-shit-done/workflows/reapply-patches.md"
      provides: "reapply-patches — doNot at line 323 replaced with affirmative form"
  key_links:
    - from: "tests/negative-framing-scan.test.cjs"
      to: "all 5 modified prompt files"
      via: "corpus subtests in describe blocks: doNot/never/dont/mustNot/antiPatterns"
      pattern: "scanForNegativeFraming"
---

<objective>
Fix all 12 negative-framing scanner violations across 5 prompt files, clearing the 6 currently-failing subtests in `tests/negative-framing-scan.test.cjs`.

Purpose: Phase 34 (Gate and Merge) requires 0 scanner failures. These 6 subtests are the only failing tests in the full suite. FRAME-01 (debug.md) is already satisfied — no edits needed. FRAME-02 (reapply-patches.md) and SCAN-12 (all other upstream files) require prose rewrites.
Output: Five modified prompt files; `node --test tests/negative-framing-scan.test.cjs` reports `# fail 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Scanner violation bucket rules extracted from tests/negative-framing-scan.test.cjs -->
<!-- These rules determine whether a rewrite passes — the scanner output is the gate. -->

Bucket → required prose action:
- doNot   (/\bdo not\b/i) — flagged UNLESS isConditionalOrFactual OR hasPositiveComplement
- never   (/\bNEVER\b/)   — flagged UNLESS isFactualNever OR isReframePattern OR isConditionalOrFactual
- dont    (/\bdon't\b/i)  — flagged UNLESS isFactualDont OR isConditionalOrFactual OR hasPositiveComplement
- mustNot (/\bmust\s+not\b/i) — NO complement filter; every match is a hard fail; remove token entirely
- antiPatterns (/<anti_patterns>/i) — opening tag only; rename tag to <expected_patterns>

hasPositiveComplement matches:
  - " — "  (em-dash WITH spaces both sides)
  - " -- "  (double-dash WITH spaces both sides)
  - /[.!]\*{0,2}\s+[A-Z]/ (period/bang then uppercase, optionally through **)
  - /\([^)]{4,}\)/ (parenthetical ≥ 4 chars)

Verification gate: node --test tests/negative-framing-scan.test.cjs
Expected result: # fail 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix doNot and never violations in agent and command files</name>
  <files>agents/gsd-executor.md, agents/gsd-planner.md, commands/gsd/discuss-phase.md</files>
  <read_first>
    - agents/gsd-executor.md line 517 — contains the DO NOT violation (bucket: doNot agent)
    - agents/gsd-planner.md line 203 — contains the NEVER violation (bucket: never agent)
    - commands/gsd/discuss-phase.md line 33 — contains the Do not violation (bucket: doNot command)
    - tests/negative-framing-scan.test.cjs lines 63–76 — hasPositiveComplement logic (confirm exact match rules before editing)
  </read_first>
  <action>
Make exactly these three line-level replacements. The scanner output is the gate — re-run after each file.

**agents/gsd-executor.md line 517 (bucket: doNot)**

Find the line containing `DO NOT` (the line ends with "by force-rewinding the protected ref —" or similar). Replace with:
```
commits landed there — **Always HALT and surface a blocker** instead of "recovering" by force-rewinding the protected ref, which silently destroys concurrent commits.
```
Note: if the original line already has ` — ` (spaces both sides) after `DO NOT`, the `hasPositiveComplement` test may already exempt it — but the `DO NOT` token itself is still flagged by the doNot bucket's literal check. The safe fix is to remove `DO NOT` and lead with the affirmative action. Preserve surrounding prose unchanged.

**agents/gsd-planner.md line 203 (bucket: never)**

Find the bullet containing `NEVER place fenced code blocks`. Replace with:
```
- Keep `<action>` as directive prose only — fenced code blocks belong in `<read_first>` source files. Action is directive prose, not implementation code.
```
The trailing sentence `Action is directive prose, not implementation code.` already satisfies `hasPositiveComplement` via period+capital (`. Action`), but the `NEVER` token itself still trips the scanner. Remove `NEVER` and lead with the affirmative form above. Preserve surrounding list context.

**commands/gsd/discuss-phase.md line 33 (bucket: doNot)**

Find the line: `Do not pre-load any workflow files before reading the mode routing instructions.`
Replace with:
```
Read the mode routing instructions first; load workflow files only on-demand in the <process> section.
```
Preserve surrounding context (this is typically inside a set of initial instructions near the top of the file).

After all three edits, run: `node --test tests/negative-framing-scan.test.cjs`
Confirm these subtests go green:
- `no bare DO NOT directives in agent files`
- `no bare DO NOT directives in command files`
- `no NEVER primary directives in agent files`
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs 2>&1 | grep -E "fail|pass|# " | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs` shows `# fail 0` for the three subtests: "no bare DO NOT directives in agent files", "no bare DO NOT directives in command files", "no NEVER primary directives in agent files"
    - `grep -n "DO NOT" agents/gsd-executor.md` returns 0 lines with unexempted DO NOT tokens near line 517
    - `grep -n "NEVER place" agents/gsd-planner.md` returns 0 matches
    - `grep -n "Do not pre-load" commands/gsd/discuss-phase.md` returns 0 matches
    - `grep -c "Always HALT" agents/gsd-executor.md` returns 1
    - `grep -c "Read the mode routing instructions first" commands/gsd/discuss-phase.md` returns 1
  </acceptance_criteria>
  <done>Three agent/command file violations cleared; scanner subtests for doNot (agents + commands) and never (agents) show green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mustNot, antiPatterns, and dont violations in workflow files</name>
  <files>get-shit-done/workflows/edit-phase.md, get-shit-done/workflows/secure-phase.md, get-shit-done/workflows/reapply-patches.md</files>
  <read_first>
    - get-shit-done/workflows/edit-phase.md lines 185–285 — contains mustNot at 191, antiPatterns at 271, and dont at 273–279
    - get-shit-done/workflows/secure-phase.md lines 70–85 — contains mustNot + doNot at line 76
    - get-shit-done/workflows/reapply-patches.md — scan the file for `do not` (case-insensitive) to locate the doNot violation at approximately line 323; the CONTEXT.md line numbers are stale — use the scanner output to find the exact line
    - tests/negative-framing-scan.test.cjs lines 63–76 — mustNot bucket has NO complement filter; every must not token is a hard fail regardless of surrounding text
  </read_first>
  <action>
Apply all edits below. The scanner output is the gate — re-run after editing each file.

**get-shit-done/workflows/edit-phase.md**

Line ~191 (bucket: mustNot) — find: `It must not reference itself (phase {target})`
Replace with:
```
- It must reference a different phase — self-reference (phase {target}) is invalid.
```

Lines ~271 and closing tag (bucket: antiPatterns) — find the `<anti_patterns>` opening tag and its corresponding `</anti_patterns>` closing tag. Rename both:
```
<expected_patterns>
```
and
```
</expected_patterns>
```

Line ~273 (bucket: dont, inside the renamed block) — find: `- Don't modify other phases when editing one`
Replace with:
```
- Edit only the target phase; leave all other phases untouched.
```

Line ~275 (bucket: dont) — find: `- Don't write without showing a diff and getting confirmation`
Replace with:
```
- Always show a diff and get confirmation before writing.
```

Line ~276 (bucket: dont) — find: `- Don't edit in_progress/completed phases without --force`
Replace with:
```
- Edit in_progress/completed phases only when --force is passed.
```

Line ~277 (bucket: dont) — find: `- Don't use raw Write on ROADMAP.md without reading it first; always replace section in place`
Replace with:
```
- Read ROADMAP.md first, then replace the section in place using a targeted edit.
```

Lines ~278–279 (currently pass scanner but are inside the renamed block; reframe for consistency):
- Find: `- Don't modify the phase directory structure — only ROADMAP.md changes`
  Replace: `- Change only ROADMAP.md; leave the phase directory structure intact.`
- Find: `- Don't commit the change — that's the user's decision`
  Replace: `- Leave committing to the user — that's their decision.`

**get-shit-done/workflows/secure-phase.md**

Line ~76 (buckets: mustNot + doNot) — find the line containing both `do NOT skip` and `must not rubber-stamp`. It reads approximately:
```
- If `threats_open: 0 AND register_authored_at_plan_time: false` → **do NOT skip**. Empty-by-no-planning must not rubber-stamp a clean SECURITY.md. Proceed to Step 5 in retroactive-STRIDE mode...
```
Replace the entire line with (preserving any list marker and surrounding context):
```
- If `threats_open: 0 AND register_authored_at_plan_time: false` → **always proceed to Step 5 in retroactive-STRIDE mode**. An empty-by-no-planning register requires a real audit — a clean SECURITY.md is not sufficient without one.
```
IMPORTANT: Semantic preservation check — the replacement MUST still communicate that an empty-by-no-planning register requires a retroactive STRIDE audit. Verify the rewritten sentence conveys this before committing.

**get-shit-done/workflows/reapply-patches.md**

Run: `grep -n "do not\|Do not\|DO NOT" get-shit-done/workflows/reapply-patches.md` to locate the exact line (research says approximately line 323; CONTEXT.md refs are stale). Convert the `do not` token to affirmative form following the vocabulary:
- `do not X` → `X is required` / `Always X` / imperative affirmative

The line is within a patch-application instruction context. Preserve surrounding meaning; remove the `do not` token entirely and lead with the positive action.

After all edits, run: `node --test tests/negative-framing-scan.test.cjs`
Confirm: `# fail 0` for all 6 subtests.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs` reports `# fail 0` (all 6 formerly-failing subtests green)
    - `grep -c "<anti_patterns>" get-shit-done/workflows/edit-phase.md` returns 0
    - `grep -c "<expected_patterns>" get-shit-done/workflows/edit-phase.md` returns at least 1
    - `grep -in "must not" get-shit-done/workflows/edit-phase.md` returns 0 matches at line 191 (the mustNot violation line; other occurrences in non-scanned sections are acceptable)
    - `grep -in "must not" get-shit-done/workflows/secure-phase.md` returns 0 matches in the former line 76 area
    - `grep -c "always proceed to Step 5" get-shit-done/workflows/secure-phase.md` returns 1
    - `grep -in "don't" get-shit-done/workflows/edit-phase.md` returns 0 matches in lines 273–277 area
    - `grep -c "Edit only the target phase" get-shit-done/workflows/edit-phase.md` returns 1
    - `node --test tests/negative-framing-scan.test.cjs 2>&1 | grep "# fail"` outputs `# fail 0`
  </acceptance_criteria>
  <done>All 6 failing scanner subtests green; workflows/edit-phase.md, secure-phase.md, and reapply-patches.md contain no flagged negative-framing tokens. FRAME-01, FRAME-02, SCAN-12 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose content → scanner | Scanner reads markdown files from disk; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | secure-phase.md line 76 rewrite | mitigate | Rewrite must preserve semantic meaning: "empty-by-no-planning requires a real audit." Acceptance criteria explicitly verifies `always proceed to Step 5` is present. Executor must re-read the rewritten line for semantic equivalence before committing. |
| T-33-02 | Tampering | Affirmative rewrite weakens a security gate directive | accept | All 12 rewrite targets are structural framing tokens (DO NOT, NEVER, don't), not security-gate logic. The only security-adjacent rewrite is T-33-01 above, which is specifically mitigated. |
</threat_model>

<verification>
**Full scanner pass:** `node --test tests/negative-framing-scan.test.cjs` reports `# fail 0`.

**Targeted subtest verification:**
```bash
node --test tests/negative-framing-scan.test.cjs 2>&1 | grep -E "✓|✗|fail|pass"
```

**No regressions in full suite:**
```bash
node scripts/run-tests.cjs 2>&1 | tail -10
# Expect: fail 0, skipped 1 (HDOC), todo 3 (Bug 3242 still todo-marked until Plan 02)
```
</verification>

<success_criteria>
- `node --test tests/negative-framing-scan.test.cjs` exits 0 with `# fail 0`
- All 6 formerly-failing subtests pass: doNot (agents), doNot (commands), never (agents), dont (workflows), antiPatterns (workflows), mustNot (workflows)
- `node scripts/run-tests.cjs` shows 0 failures (scanner workstream complete; todo 3 is normal until Plan 02 runs)
- FRAME-01: `debug.md` already passes (no edits needed; confirmed by scanner)
- FRAME-02: `reapply-patches.md` line 323 doNot fixed; scanner reports 0 violations
- SCAN-12: all 12 violation lines across 5 files rewritten to affirmative form
</success_criteria>

<output>
After completion, create `.planning/phases/33-positive-framing-pass/33-01-SUMMARY.md`
</output>
