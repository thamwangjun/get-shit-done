---
phase: 34-gate-and-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GATE-03, MERGE-01]

must_haves:
  truths:
    - "D-01: Phase 34 is zero-fix — if npm test shows unexpected failures, plan aborts and escalates; no inline fixes permitted"
    - "D-02: Local fast-forward only — git checkout thamw-main && git merge --ff-only thamw-v1.41.3; no remote push"
    - "D-03: Branch identity verified with git log thamw-main..thamw-v1.41.3 (empty = identical); spot-check uses grep -i isNewer hooks/gsd-check-update-worker.js"
    - "npm test on thamw-v1.41.3 reports 0 failures (1 intentional HDOC skip permitted)"
    - "thamw-main and thamw-v1.41.3 are identical after fast-forward — git log thamw-main..thamw-v1.41.3 shows no commits"
  artifacts:
    - path: "none — this phase produces no new files"
      provides: "Gate confirmation and local branch advance"
  key_links:
    - from: "thamw-v1.41.3 branch"
      to: "thamw-main branch"
      via: "git merge --ff-only"
      pattern: "git log thamw-main..thamw-v1.41.3 produces empty output"
---

<objective>
Run the full npm test suite on the current branch (thamw-v1.41.3); if it passes at 0 failures with at most 1 intentional HDOC skip, fast-forward thamw-main to thamw-v1.41.3 locally.

Purpose: Validate that Phases 32 and 33 produced a clean working tree before advancing the fork's main branch to the new milestone.
Output: Confirmed test pass + local thamw-main advanced to thamw-v1.41.3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-gate-and-merge/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite gate (GATE-03)</name>
  <files>none — shell operation only</files>
  <read_first>
    none — shell operation only; no files to read before running the test command
  </read_first>
  <action>
    Verify you are on branch thamw-v1.41.3:

      git branch --show-current

    If not on thamw-v1.41.3, abort and report — do NOT switch branches.

    Run the full test suite:

      npm test

    Capture the summary lines from the output. The acceptable passing state is:
    - 0 failures
    - Exactly 1 skip permitted — the intentional HDOC describe block skip in
      tests/agent-frontmatter.test.cjs (fork decision overrides upstream assertion)

    If the output shows ANY unexpected failures (beyond the 1 permitted HDOC skip),
    STOP immediately. Per D-01 (zero-fix rule): do NOT attempt to fix anything.
    Report the exact failing test names and subtest output to the user, then halt.

    If the suite passes at 0 failures, record the exact summary line from npm test
    output (e.g., "pass 99 | fail 0 | skip 1") in the SUMMARY.md you write at the end.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - Output contains "fail 0" (zero failures)
    - Output contains at most "skip 1" (the HDOC block — no unexpected skips)
    - If any of the above conditions are NOT met, the plan halts and escalates; the merge task below MUST NOT run
  </acceptance_criteria>
  <done>Test suite reports 0 failures with at most 1 skip; executor has captured the summary line for the SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 2: Fast-forward thamw-main to thamw-v1.41.3 and verify identity (MERGE-01, D-02, D-03)</name>
  <files>none — git branch operation only</files>
  <read_first>
    none — git branch operation only; no files to read
  </read_first>
  <action>
    This task runs only if Task 1 passed at 0 failures.

    Step 1 — Confirm current branch is thamw-v1.41.3 before any branch switch:

      git branch --show-current

    Must output: thamw-v1.41.3

    Step 2 — Fast-forward thamw-main (per D-02 — local only, no push):

      git checkout thamw-main
      git merge --ff-only thamw-v1.41.3

    If git reports "fatal: Not possible to fast-forward, aborting." stop immediately
    and report the divergence to the user — do NOT force merge or rebase.

    Step 3 — Verify branch identity (per D-03):

      git log thamw-main..thamw-v1.41.3

    Expected output: empty (no lines). Empty output means the branches are identical.

    Step 4 — MERGE-01 spot-check (per D-03 — use isNewer grep, NOT the stale thamwangjun grep):

      grep -i isNewer hooks/gsd-check-update-worker.js

    This should return at least one match, confirming the fork's update detection logic
    is present on thamw-main after the fast-forward.

    Step 5 — Return to thamw-v1.41.3 branch (keep working branch consistent):

      git checkout thamw-v1.41.3

    Do NOT push to remote — this phase ends after local confirmation (D-02).
  </action>
  <verify>
    <automated>git log thamw-main..thamw-v1.41.3</automated>
  </verify>
  <acceptance_criteria>
    - `git checkout thamw-main && git merge --ff-only thamw-v1.41.3` exits 0
    - `git log thamw-main..thamw-v1.41.3` produces empty output (branches identical)
    - `grep -i isNewer hooks/gsd-check-update-worker.js` returns at least one match
    - No `git push` command is run (D-02 local-only constraint)
  </acceptance_criteria>
  <done>thamw-main is at the same commit as thamw-v1.41.3; branch identity confirmed; spot-check passes; no remote push performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → file system | npm test reads all test files and source files from the working tree |
| git CLI → local repository | git checkout and merge --ff-only operate on the local repo only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | Working tree before npm test | accept | Working tree is on thamw-v1.41.3 (committed); git status verified before running tests; no uncommitted edits expected |
| T-34-02 | Spoofing | Branch identity check | mitigate | Use `git log thamw-main..thamw-v1.41.3` (empty = identical) rather than trusting branch name alone; git log output is authoritative |
| T-34-03 | Elevation of Privilege | Fast-forward overwrites thamw-main | mitigate | `--ff-only` flag prevents any non-fast-forward merge; if branches have diverged git aborts with a fatal error — no force is applied |
| T-34-04 | Denial of Service | Unexpected test failures block merge | accept | D-01 zero-fix rule is the mitigation — abort and escalate rather than masking failures; low risk in gate-and-merge phase |
| T-34-05 | Information Disclosure | Test output logged to terminal | accept | No secrets in test output; tests operate on local prompt content files only |
</threat_model>

<verification>
Full phase verification:

1. `npm test` exits 0 with 0 failures (1 HDOC skip permitted)
2. `git log thamw-main..thamw-v1.41.3` returns empty output
3. `grep -i isNewer hooks/gsd-check-update-worker.js` returns at least 1 match
</verification>

<success_criteria>
- GATE-03: `npm test` on thamw-v1.41.3 reports 0 failures (1 intentional HDOC skip permitted)
- MERGE-01: thamw-main fast-forwarded to thamw-v1.41.3 locally; branches are identical; no remote push performed
</success_criteria>

<output>
After completion, create `.planning/phases/34-gate-and-merge/34-01-SUMMARY.md` using the summary template.

Include in the summary:
- Exact npm test summary line (pass/fail/skip counts)
- Output of `git log thamw-main..thamw-v1.41.3` (confirm empty)
- Output of `grep -i isNewer hooks/gsd-check-update-worker.js` (confirm at least 1 match)
- Note that remote push is deferred to manual user action per D-02
</output>
