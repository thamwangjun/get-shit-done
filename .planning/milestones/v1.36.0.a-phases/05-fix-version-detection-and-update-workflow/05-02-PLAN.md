---
phase: 05-fix-version-detection-and-update-workflow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/update.md
autonomous: true
requirements: [UPD-01, UPD-02]
must_haves:
  truths:
    - "update.md run_update step clears ~/.cache/gsd/gsd-update-check.json after a successful update"
    - "The rm -f line is outside (after) the per-runtime-dir for-loops, not inside them"
    - "The grep -Eq '^[0-9a-f]{7}' gate and INSTALLED_VERSION logic in update.md are unchanged"
    - "The /gsd-update 'already on latest' path is reachable once VERSION contains a valid SHA (unblocked by plan 01)"
  artifacts:
    - path: "get-shit-done/workflows/update.md"
      provides: "Cache-clear for shared worker cache path"
      contains: ".cache/gsd/gsd-update-check.json"
  key_links:
    - from: "get-shit-done/workflows/update.md run_update step"
      to: "~/.cache/gsd/gsd-update-check.json"
      via: "rm -f \"$HOME/.cache/gsd/gsd-update-check.json\""
      pattern: "\\.cache/gsd/gsd-update-check\\.json"
---

<objective>
Add a single `rm -f "$HOME/.cache/gsd/gsd-update-check.json"` line to the run_update step in get-shit-done/workflows/update.md, and verify that the version comparison logic (UPD-02) is already correctly implemented in a single bash context.

Purpose: The background worker (gsd-check-update-worker.js) writes its update cache to `~/.cache/gsd/gsd-update-check.json` — a shared, runtime-agnostic path. The current run_update step only clears runtime-dir subpaths (`$dir/cache/gsd-update-check.json`). This mismatch causes the statusline ⬆ indicator to persist after a successful /gsd-update. The fix is a one-line addition outside the existing for-loops.

UPD-02 (version comparison in single bash context) is already correctly implemented per D-06. This plan verifies it by reading the source — no code changes are needed for UPD-02.

Output: Modified get-shit-done/workflows/update.md with the shared cache path cleared. Verified (not changed) UPD-02 comparison logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-version-detection-and-update-workflow/05-CONTEXT.md
@.planning/phases/05-fix-version-detection-and-update-workflow/05-RESEARCH.md
@.planning/phases/05-fix-version-detection-and-update-workflow/05-PATTERNS.md

<interfaces>
<!-- Key code the executor must read before touching anything. -->

From get-shit-done/workflows/update.md run_update step (lines 510–523, the insertion point):
```bash
for dir in "${CACHE_DIRS[@]}"; do
  if [ -n "$dir" ]; then
    rm -f "$dir/cache/gsd-update-check.json"
  fi
done

for dir in .claude .config/opencode .opencode .gemini .config/kilo .kilo .codex; do
  rm -f "./$dir/cache/gsd-update-check.json"
  rm -f "$HOME/$dir/cache/gsd-update-check.json"
done
```
# ← insert new line HERE, after the closing `done` of the second for-loop (line 519), before the closing ``` of the bash block

Line to add (D-05):
```bash
# The worker (gsd-check-update.js) writes to a shared OS cache path — not a
# runtime-dir subpath. Clear it unconditionally once, outside the per-runtime loop.
rm -f "$HOME/.cache/gsd/gsd-update-check.json"
```

From hooks/gsd-check-update.js lines 35–36 (verified source of truth for the cache path):
```javascript
const cacheFile = path.join(os.homedir(), '.cache', 'gsd', 'gsd-update-check.json');
```

From get-shit-done/workflows/update.md (UPD-02 verify — these lines must NOT be changed):
- Line 108: `grep -Eq '^[0-9a-f]{7}'` gate for INSTALLED_VERSION
- Line 216: INSTALLED_VERSION assignment
- Line 226: LATEST_VERSION / SHA comparison logic
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared cache-clear line to update.md run_update step (D-05)</name>
  <files>get-shit-done/workflows/update.md</files>
  <read_first>
    - get-shit-done/workflows/update.md — read lines 490–535 to locate the run_update step's cache-clear for-loops and find the exact insertion point (the line immediately after the closing `done` of the second for-loop).
    - .planning/phases/05-fix-version-detection-and-update-workflow/05-PATTERNS.md — read the "get-shit-done/workflows/update.md" section for the structural constraint and exact line to add.
    - hooks/gsd-check-update.js — read lines 30–40 to confirm the worker's cache write path is `path.join(os.homedir(), '.cache', 'gsd', 'gsd-update-check.json')`.
  </read_first>
  <action>
    In get-shit-done/workflows/update.md, find the run_update step's bash block. Locate the second for-loop (the one iterating `.claude .config/opencode .opencode .gemini .config/kilo .kilo .codex`). After the closing `done` of that loop, and before the closing triple-backtick of the bash block, add these three lines:

    ```
    # The worker (gsd-check-update.js) writes to a shared OS cache path — not a
    # runtime-dir subpath. Clear it unconditionally once, outside the per-runtime loop.
    rm -f "$HOME/.cache/gsd/gsd-update-check.json"
    ```

    CRITICAL structural constraint (from 05-RESEARCH.md Pitfall 3): Do NOT add this line inside any loop body. The `~/.cache/gsd/` path is a single OS-level user cache path shared across all runtimes. Adding it inside a loop would expand it with a loop variable (e.g., `~/.claude/.cache/gsd/...`) which is wrong. Place it after both loops as a standalone unconditional rm command.

    Do NOT modify any other part of update.md. Specifically:
    - Lines 108, 216, 226 (grep gate and version comparison logic) — leave unchanged (D-06)
    - The `check_latest_version` step — leave unchanged
    - The `compare_versions` step — leave unchanged
    - The `get_installed_version` step — leave unchanged
  </action>
  <verify>
    <automated>grep -n 'cache/gsd/gsd-update-check.json' get-shit-done/workflows/update.md</automated>
  </verify>
  <done>
    `grep -n '.cache/gsd/gsd-update-check.json' get-shit-done/workflows/update.md` returns at least one match. The match is NOT inside a `for dir in` loop body. The surrounding context shows it appears after the second for-loop's closing `done`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify UPD-02 — single-bash-context comparison logic is intact (D-06)</name>
  <files>get-shit-done/workflows/update.md</files>
  <read_first>
    - get-shit-done/workflows/update.md — read lines 100–130 (INSTALLED_VERSION grep gate), lines 200–240 (get_installed_version and compare_versions steps). Confirm these are self-contained bash blocks.
    - .planning/phases/05-fix-version-detection-and-update-workflow/05-CONTEXT.md — read D-06 and D-07 to understand what "already implemented correctly" means for UPD-02.
    - .planning/phases/05-fix-version-detection-and-update-workflow/05-RESEARCH.md — read "Pitfall 4: UPD-02 Is Verify-Only" to understand why no code changes are made.
  </read_first>
  <action>
    This task is verify-only — no code changes.

    Read get-shit-done/workflows/update.md and confirm:

    1. The `get_installed_version` step contains `grep -Eq '^[0-9a-f]{7}'` to gate INSTALLED_VERSION. Find the exact line number.

    2. The `compare_versions` step compares INSTALLED_VERSION to LATEST_VERSION (or REMOTE_SHA) within what appears to be a bash block. Confirm the comparison is in a single bash context (the variables set in prior steps are available — either via a single heredoc, or via variable exports, or the workflow design executes the full comparison in one bash invocation).

    3. Confirm that no variable-loss-between-steps issue exists as described in UPD-02. The mechanism is: if each step is a separate `<step>` XML element but the bash content is a single pipeline or is accumulated into a single shell script block, state persists correctly.

    After confirming, document findings in the task SUMMARY:
    - Exact line number of the `grep -Eq '^[0-9a-f]{7}'` gate
    - How INSTALLED_VERSION persists into the comparison step (single script, exports, or other mechanism)
    - Confirmation: "UPD-02: comparison logic is single-bash-context — no changes needed"

    If you find that the comparison mechanism is actually broken (variables ARE lost between steps), immediately flag this as a gap: return the finding to the user with the specific lines showing the breakage before proceeding. Do NOT silently fix it — this would be out of scope for D-06.
  </action>
  <verify>
    <automated>grep -n "INSTALLED_VERSION\|grep -Eq\|REMOTE_SHA\|LATEST_VERSION" get-shit-done/workflows/update.md | head -20</automated>
  </verify>
  <done>
    The verify command output shows `grep -Eq '^[0-9a-f]{7}'` gate present. INSTALLED_VERSION is set and consumed within a coherent bash context. The SUMMARY documents the exact mechanism. No code changes were made to update.md comparison logic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.cache/gsd/gsd-update-check.json → update.md | Cache file written by background worker is read/cleared by the update workflow |
| GitHub API response → update.md LATEST_VERSION | Remote SHA from api.github.com enters version comparison (existing, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | ~/.cache/gsd/gsd-update-check.json | accept | Cache file is read for update status only; no authentication or secrets stored; worst case is stale ⬆ indicator — D-05 fix clears it after each update |
| T-05-06 | Tampering | update.md LATEST_VERSION from GitHub API | accept | Existing SHA validation `/^[0-9a-f]{40}$/` in the check_latest_version step guards against malformed API responses (Phase 4 established this pattern; unchanged here) |
| T-05-07 | Information Disclosure | update.md displays installed SHA in terminal | accept | SHA is a non-sensitive identifier; displayed intentionally in update output for user confirmation |
| T-05-08 | Denial of Service | rm -f on non-existent path | accept | `rm -f` is silent on missing files; no DoS risk |
</threat_model>

<verification>
Run after both tasks are complete:

```bash
# Verify cache-clear line present
grep -n '.cache/gsd/gsd-update-check.json' get-shit-done/workflows/update.md

# Verify it is NOT inside a for-loop (should appear after line 519 based on current structure)
grep -n -A2 -B2 '.cache/gsd/gsd-update-check.json' get-shit-done/workflows/update.md

# Verify UPD-02 grep gate unchanged
grep -n "grep -Eq" get-shit-done/workflows/update.md

# Full test suite (update.md is a workflow file, not tested by unit tests — ensure no regressions)
npm test
```
</verification>

<success_criteria>
1. `grep -c '.cache/gsd/gsd-update-check.json' get-shit-done/workflows/update.md` returns at least 1
2. The matched line is outside any `for dir in` loop body (confirmed by context lines)
3. `grep -n "grep -Eq" get-shit-done/workflows/update.md` still returns the version gate line (unchanged)
4. `grep -n "INSTALLED_VERSION" get-shit-done/workflows/update.md` still shows the assignment logic (unchanged)
5. `npm test` exits 0 (no regressions)
6. SUMMARY documents the UPD-02 mechanism finding (single-bash-context confirmed or gap flagged)
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-version-detection-and-update-workflow/05-02-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
