---
phase: 09-fork-standards-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/inbox.md
  - commands/gsd/sketch.md
  - commands/gsd/sketch-wrap-up.md
  - commands/gsd/spec-phase.md
  - commands/gsd/spike.md
  - commands/gsd/spike-wrap-up.md
  - get-shit-done/references/autonomous-smart-discuss.md
  - get-shit-done/references/debugger-philosophy.md
  - get-shit-done/references/mandatory-initial-read.md
  - get-shit-done/references/project-skills-discovery.md
  - get-shit-done/references/sketch-interactivity.md
  - get-shit-done/references/sketch-theme-system.md
  - get-shit-done/references/sketch-tooling.md
  - get-shit-done/references/sketch-variant-patterns.md
  - get-shit-done/workflows/sketch.md
  - get-shit-done/workflows/sketch-wrap-up.md
  - get-shit-done/workflows/spec-phase.md
  - get-shit-done/workflows/spike.md
  - get-shit-done/workflows/spike-wrap-up.md
autonomous: true
requirements:
  - NEW-01
  - NEW-02
  - NEW-03
  - NEW-04
  - NEW-05
  - NEW-06
  - NEW-07
  - NEW-08
  - NEW-09
  - NEW-10
  - NEW-11
  - NEW-12
  - NEW-13
  - NEW-14
  - NEW-15
  - NEW-16
  - NEW-17
  - NEW-18
  - NEW-19
  - NEW-20

must_haves:
  truths:
    - "Negative-framing scanner reports 34/34 pass (zero violations) across all 19 new prompt files — scanner confirms baseline is unchanged from pre-plan state"
    - "All 6 new command files (inbox, sketch, sketch-wrap-up, spec-phase, spike, spike-wrap-up) conform to the V09 command structure: <objective> first, <execution_context> second, <context> middle, <process> last"
    - "All 5 new workflow files (sketch, sketch-wrap-up, spec-phase, spike, spike-wrap-up) conform to the V09 workflow structure: <purpose> + <required_reading> + <process> with named <step> elements"
    - "All 8 new reference files pass V09 review: behavioral-instruction references have a purpose statement; data/taxonomy references use plain Markdown without forced XML wrapping"
    - "npm test returns at least 4110/4112 — no regression introduced by V09 audit edits"

  artifacts:
    - path: "commands/gsd/inbox.md"
      provides: "V09-conformant command: <objective> + <execution_context> + <context> + <process>"
    - path: "commands/gsd/sketch.md"
      provides: "V09-conformant command"
    - path: "commands/gsd/sketch-wrap-up.md"
      provides: "V09-conformant command"
    - path: "commands/gsd/spec-phase.md"
      provides: "V09-conformant command with <success_criteria>"
    - path: "commands/gsd/spike.md"
      provides: "V09-conformant command"
    - path: "commands/gsd/spike-wrap-up.md"
      provides: "V09-conformant command"
    - path: "get-shit-done/workflows/sketch.md"
      provides: "V09-conformant workflow: <purpose> + <required_reading> + <process>/<step name=...>"
    - path: "get-shit-done/workflows/spec-phase.md"
      provides: "V09-conformant workflow"
    - path: "get-shit-done/workflows/spike.md"
      provides: "V09-conformant workflow"

  key_links:
    - from: "commands/gsd/sketch.md"
      to: "get-shit-done/workflows/sketch.md"
      via: "<execution_context> @file include"
      pattern: "execution_context"
    - from: "commands/gsd/spike.md"
      to: "get-shit-done/workflows/spike.md"
      via: "<execution_context> @file include"
      pattern: "execution_context"
    - from: "tests/negative-framing-scan.test.cjs"
      to: "all 19 new files"
      via: "node --test"
      pattern: "pass 34"
---

<objective>
Confirm the negative-framing scanner baseline is green on all 19 new prompt files added by v1.37.1, then apply a full PROMPT_ENGINEERING_GUIDE V09 structural audit to every in-scope file and fix all quality gaps found.

Purpose: The 19 new files were added by upstream v1.37.1 and already pass the scanner (Phase 7 FORK-CORRUPTION triage resolved framing violations). The remaining work is a structural quality pass — ensuring each file uses the correct V09 XML block vocabulary for its layer (command, workflow, reference), places blocks in the correct attention-order, and contains no padding or missing required sections.

Output: 19 new prompt files that conform to both the positive framing standard (scanner 34/34) and the V09 structural quality bar (PROMPT_IMPROVEMENT_GUIDE_V01.md Final Checklist). NEW-15 (templates/spec.md) is exempt and excluded from this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-fork-standards-pass/09-CONTEXT.md
@.planning/phases/09-fork-standards-pass/09-RESEARCH.md
@.planning/phases/09-fork-standards-pass/09-PATTERNS.md

<interfaces>
<!-- Key quality references for V09 audit. Executor MUST read these before auditing any file. -->

PROMPT_IMPROVEMENT_GUIDE_V01.md — 8-step process, Final Checklist (30 items).
Read: .planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md

PROMPT_ENGINEERING_GUIDE_V09.md — Full V09 quality bar, XML tag vocabulary.
Read: .planning/references/PROMPT_ENGINEERING_GUIDE_V09.md

V09 command structure (analog: commands/gsd/spike.md):
  <objective>    — What the command achieves. HIGH attention (top).
  <execution_context>  — @file includes pointing to the workflow.
  <runtime_note>      — Optional Copilot compatibility note.
  <context>      — Variable substitution, flag descriptions, $ARGUMENTS. MIDDLE.
  <process>      — One delegation sentence: "Execute the X workflow end-to-end."
  <success_criteria>  — Optional; add when command has measurable acceptance criteria.

V09 workflow structure (analog: get-shit-done/workflows/spike.md):
  <purpose>        — One paragraph: what the workflow produces and where artifacts land.
  <required_reading>  — "Read all files referenced by the invoking prompt's execution_context before starting."
                       Plus explicit @file includes when workflow references additional resources.
  <process>
    <step name="step_name"> — Each discrete action in its own named step.
    </step>
  </process>

V09 reference structure (analogs: get-shit-done/references/sketch-interactivity.md, project-skills-discovery.md):
  - Data/taxonomy references: plain Markdown with # Title + one-line purpose. NO forced XML wrapping.
  - Behavioral-instruction references: may use <task> or <purpose> block. Check content type before auditing.

8 V09 dimensions to check per file:
  1. Task specification: <objective>/<purpose> present, one clear sentence/paragraph
  2. Positive framing: zero bare negative directives (scanner gate)
  3. XML structure: correct vocabulary for layer; named <step> elements in workflows
  4. Context placement: high-attention blocks first, context middle, input last
  5. Priority ordering: explicit <priority_order> when multiple criteria conflict
  6. Persona: present only for open-ended creative tasks; gender-neutral
  7. CoT gating: chain-of-thought trigger only for math/symbolic/multi-step logic
  8. Constraint enforcement: restrictions paired with <permitted> alternatives

Scanner invocation:
  node --test tests/negative-framing-scan.test.cjs
  Expected: ℹ tests 34 / ℹ pass 34 / ℹ fail 0

Full suite gate (run after all edits complete):
  npm test
  Gate: ≥ 4110/4112 pass
</interfaces>
</context>

<!-- Split decision (WARNING 3): Plan 01 covers 19 files_modified, above the 15-file threshold.
     A 3-plan split (commands / workflows / references) was evaluated. Not applied because:
     (a) Task 0 adds explicit audit of the 4 analog files — no files_modified, so effective
         modification scope is 15 files across Tasks 1-2;
     (b) Tasks 1 and 2 are already grouped by semantic boundary (commands vs. workflows+references)
         and fit within the 50% context budget;
     (c) A 3rd plan would be Wave 1 with identical depends_on — no parallelism gain.
     The 4 analog files in Task 0 are read-only verification, not modification targets. -->

<tasks>

<task type="auto">
  <name>Task 0: Confirm V09 conformance for 4 analog files (NEW-05, NEW-11, NEW-16, NEW-19)</name>

  <read_first>
    - commands/gsd/spike.md — file being confirmed (NEW-05)
    - get-shit-done/references/sketch-interactivity.md — file being confirmed (NEW-11)
    - get-shit-done/workflows/sketch.md — file being confirmed (NEW-16)
    - get-shit-done/workflows/spike.md — file being confirmed (NEW-19)
  </read_first>

  <action>
Read each of the 4 files against the 8 V09 dimensions. These files are used as structural analogs throughout the phase (spike.md command is the primary command analog; sketch-interactivity.md is the primary data-reference analog; sketch.md and spike.md workflows are the primary workflow analogs). Expected outcome: all 4 are already conformant — no edits needed.

For each file, check all 8 dimensions and record the result (PASS / GAP FOUND):
1. Task specification: objective or purpose present and clear
2. Positive framing: zero bare negative directives (scanner confirmed CLEAN)
3. XML structure: correct vocabulary for layer (command/workflow/reference)
4. Context placement: high-attention blocks first
5. Priority ordering: priority_order present if multiple criteria conflict
6. Persona: absent (none needed for these file types)
7. CoT gating: absent (these files do not have math/symbolic reasoning)
8. Constraint enforcement: any restrictions paired with permitted alternatives

If all 4 files are conformant: record "already conformant — no edits made" for each and proceed to Task 1.
If any file has a genuine gap: fix it using targeted Edit tool replacement, then re-run the scanner before proceeding.

This task documents requirement coverage. NEW-05, NEW-11, NEW-16, and NEW-19 require V09 audit per D-02. These files serve as analogs in later tasks precisely because they already meet V09 standard. This task formalizes that finding.
  </action>

  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>

  <done>All 4 analog files (spike.md command, sketch-interactivity.md, sketch.md workflow, spike.md workflow) confirmed V09-conformant. Scanner remains 34/34. NEW-05, NEW-11, NEW-16, NEW-19 requirement coverage documented.</done>
</task>

<task type="auto">
  <name>Task 1: Confirm scanner baseline and perform V09 audit on 6 new command files</name>

  <read_first>
    - tests/negative-framing-scan.test.cjs — scanner logic and exemption rules; run BEFORE any edit
    - .planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md — 8-step process and Final Checklist; read fully before auditing
    - .planning/references/PROMPT_ENGINEERING_GUIDE_V09.md — V09 tag vocabulary
    - commands/gsd/spike.md — primary V09-conformant command analog; use as structural reference
    - commands/gsd/spec-phase.md — extended variant with <success_criteria>; use for commands that have measurable acceptance criteria
    - commands/gsd/inbox.md — file being audited
    - commands/gsd/sketch.md — file being audited
    - commands/gsd/sketch-wrap-up.md — file being audited
    - commands/gsd/spike-wrap-up.md — file being audited
  </read_first>

  <action>
**Step A — Confirm scanner baseline (do this first, before any edits):**

Run the negative-framing scanner:
```
node --test tests/negative-framing-scan.test.cjs
```
Expected result: 34 tests, 34 pass, 0 fail. If any test fails, STOP — do not proceed with V09 edits until the regression is diagnosed. A scanner failure here means something regressed between research and now.

**Step B — V09 audit: 4 new command files (inbox, sketch, sketch-wrap-up, spike-wrap-up)**

For each of the 4 command files, apply this audit protocol:
1. Read the file fully
2. Check all 8 V09 dimensions against the PROMPT_IMPROVEMENT_GUIDE_V01.md Final Checklist
3. Compare structure against the `commands/gsd/spike.md` analog (the gold-standard conformant command)
4. Fix any gaps using targeted Edit tool replacements — never full rewrites

**Conformant command structure to audit against** (from spike.md pattern):
```
---
[frontmatter — DO NOT TOUCH]
---
<objective>
{What the command achieves — one clear sentence. HIGH attention (top position).}
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/<workflow>.md
[additional @file refs if needed]
</execution_context>

<runtime_note>
**Copilot (VS Code):** Use `vscode_askquestions` wherever this workflow calls `AskUserQuestion`.
</runtime_note>

<context>
{Variable substitution and flag descriptions. MIDDLE position.}
$ARGUMENTS
</context>

<process>
Execute the <name> workflow from @~/.claude/get-shit-done/workflows/<name>.md end-to-end.
</process>
```

**Per-file audit focus (common gaps for thin command wrappers):**
- Missing `<objective>` or objective buried inside `<context>` → move to top as `<objective>`
- `<context>` placed before `<objective>` → reorder to: objective, execution_context, runtime_note, context, process
- Multi-paragraph `<process>` block → compress to one delegation sentence
- Unnecessary `<persona>` block (commands typically do not need persona — only add for open-ended creative tasks)
- Unnecessary CoT trigger (commands delegate to workflows; CoT belongs in the workflow, not the command)
- `<constraints>` block without `<permitted>` pairing → add `<permitted>` or remove if redundant with the workflow's constraints

**Dimension 6 (Persona):** For command files, persona is almost never appropriate — commands are thin wrappers that delegate to workflows. If a file has a persona block that is not needed for an open-ended creative task, remove it.

**Dimension 7 (CoT):** Commands that simply invoke a workflow do not need chain-of-thought triggers. If present and not applicable, remove.

After all edits to these 4 files, run the scanner again to confirm still 34/34:
```
node --test tests/negative-framing-scan.test.cjs
```
  </action>

  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs` reports `pass 34` and `fail 0` both before and after edits in this task
    - `commands/gsd/inbox.md` has `<objective>` as the first content block after frontmatter (grep -n "objective" commands/gsd/inbox.md shows it before any other XML tag)
    - `commands/gsd/sketch.md` has `<objective>` as first content block
    - `commands/gsd/sketch-wrap-up.md` has `<objective>` as first content block
    - `commands/gsd/spike-wrap-up.md` has `<objective>` as first content block
    - Each file's `<process>` block is a single delegation sentence pointing to the corresponding workflow (grep -c "<step" commands/gsd/inbox.md returns 0 — process steps belong in the workflow, not the command)
    - No file has a `<persona>` block unless the file's purpose is an open-ended creative task (grep -l "persona" commands/gsd/*.md — any match must be justified)
    - All 4 audited files contain `<execution_context>` block with at least one @file include
  </acceptance_criteria>

  <done>Scanner remains 34/34 after this task. All 4 command files (inbox, sketch, sketch-wrap-up, spike-wrap-up) conform to V09 structure: <objective> at top, <execution_context> pointing to workflow, <context> in middle, <process> as single delegation sentence.</done>
</task>

<task type="auto">
  <name>Task 2: V09 audit on 5 new workflow files and 8 new reference files</name>

  <read_first>
    - .planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md — Final Checklist; read if not already in context
    - get-shit-done/workflows/spike.md — primary V09-conformant workflow analog
    - get-shit-done/workflows/sketch.md — sketch variant (adds @file includes inside <required_reading>)
    - get-shit-done/references/sketch-interactivity.md — reference analog (data/table format)
    - get-shit-done/references/project-skills-discovery.md — reference analog (procedural-steps format)
    - get-shit-done/workflows/sketch-wrap-up.md — file being audited
    - get-shit-done/workflows/spec-phase.md — file being audited
    - get-shit-done/workflows/spike-wrap-up.md — file being audited
    - get-shit-done/references/autonomous-smart-discuss.md — file being audited
    - get-shit-done/references/debugger-philosophy.md — file being audited
    - get-shit-done/references/mandatory-initial-read.md — file being audited
    - get-shit-done/references/project-skills-discovery.md — file being audited
    - get-shit-done/references/sketch-theme-system.md — file being audited
    - get-shit-done/references/sketch-tooling.md — file being audited
    - get-shit-done/references/sketch-variant-patterns.md — file being audited
  </read_first>

  <action>
**Step A — V09 audit: 5 new workflow files**

Workflows to audit: sketch-wrap-up.md, spec-phase.md, spike-wrap-up.md (sketch.md and spike.md serve as the reference analogs — they are already conformant, do not re-audit them unless a specific gap is found during reference reading).

**Conformant workflow structure** (from spike.md and sketch.md analogs):
```
<purpose>
{One paragraph: what the workflow produces and where artifacts land.
Companion workflow reference if applicable.}
</purpose>

<required_reading>
Read all files referenced by the invoking prompt's execution_context before starting.

[Optional: explicit @file includes when the workflow itself references resources
 not loaded by the invoking command — see sketch.md for example]
</required_reading>

<process>

<step name="step_name">
{Step content}
</step>

...

</process>
```

**Per-file audit focus for workflow files (common gaps):**
- Missing `<purpose>` block, or purpose statement merged into the first step → extract as standalone `<purpose>` at top
- `<process>` placed before `<purpose>` → reorder
- Steps using `##` Markdown headers instead of `<step name="...">` XML tags → convert to named step tags
- Missing `<required_reading>` block → add with the standard boilerplate sentence
- Bare negative directives inside steps (scanner will catch, but also check manually for context-lost directives that scanner might exempt via em-dash rule)
- Priority ordering: if a step has multiple criteria that could conflict, add explicit ordering guidance inside the step

**For spec-phase.md specifically:** This is a complex multi-step orchestration workflow. Read it fully end-to-end. The key structural check is whether every discrete action is in a named `<step>` element (not Markdown `##` headers) and whether the `<purpose>` accurately captures what the workflow produces.

**Step B — V09 audit: 8 new reference files**

References to audit: autonomous-smart-discuss.md, debugger-philosophy.md, mandatory-initial-read.md, project-skills-discovery.md, sketch-theme-system.md, sketch-tooling.md, sketch-variant-patterns.md (sketch-interactivity.md serves as an analog — do not re-audit it).

**Critical judgment before auditing each reference file:**
First classify the file: is it (a) a data/taxonomy reference (tables, code examples, rules lists) or (b) a behavioral-instruction reference (tells the agent what to do)?

- **Data/taxonomy references:** Use plain Markdown with `# Title` + one-line purpose statement. Do NOT add XML wrapping. Adding `<objective>` or `<task>` to a data file makes it structurally incorrect.
- **Behavioral-instruction references:** May benefit from a `<purpose>` or `<task>` block when the instruction set is complex enough to warrant it.

**Per-file audit focus for reference files:**
- Missing one-line purpose statement after the `# Title` → add one sentence explaining what this reference is for and how agents use it
- Redundant sections or padding content that does not serve the injection purpose → compress
- For behavioral references: check dimension 1 (task spec) and dimension 8 (constraint enforcement)
- For all references: dimension 2 (positive framing) — scanner covers this, but also look for em-dash patterns that the scanner exempts but that read as negative in context
- Do NOT add XML tags to data documents — `sketch-interactivity.md` and `sketch-theme-system.md` are data documents

**After all edits, run the scanner and then the full suite:**
```
node --test tests/negative-framing-scan.test.cjs
npm test
```
The full suite gate: ≥ 4110/4112 pass (the 2 pre-existing failures in managed-hooks.test.cjs and verification-overrides.test.cjs are Phase 10 scope — do not fix them here, just confirm they are still the only failures).
  </action>

  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs` reports `pass 34` and `fail 0`
    - `npm test` reports at least 4110 passing out of 4112 total
    - `npm test` failure count is exactly 2 (the pre-existing managed-hooks.test.cjs and verification-overrides.test.cjs failures — no new failures introduced)
    - Each workflow file (sketch-wrap-up.md, spec-phase.md, spike-wrap-up.md) contains `<purpose>` as the first block (grep -n "<purpose>" get-shit-done/workflows/sketch-wrap-up.md shows it before any <step> or <process> tag)
    - Each workflow file contains `<required_reading>` (grep -l "required_reading" get-shit-done/workflows/ — all 5 workflow files must match)
    - Each workflow file uses `<step name=` tags for discrete actions — no `##` Markdown headers inside `<process>` (grep -c "^## " get-shit-done/workflows/sketch-wrap-up.md returns 0 for headers inside process; headers outside process are acceptable)
    - Data reference files (sketch-theme-system.md, sketch-tooling.md, sketch-variant-patterns.md, sketch-interactivity.md) do NOT contain `<objective>` or `<task>` XML tags (grep -l "<objective>\|<task>" get-shit-done/references/sketch-theme-system.md get-shit-done/references/sketch-tooling.md returns empty)
    - Each reference file has a one-line purpose statement immediately after the `# Title` heading (read first 5 lines of each file to verify)
  </acceptance_criteria>

  <done>Scanner remains 34/34. Full suite remains at 4110/4112 (no regressions). All 5 new workflow files have <purpose> + <required_reading> + <process> with named <step> elements. All 8 new reference files have a clear purpose statement and correct structural classification (data vs behavioral). All 19 in-scope new files pass the V09 quality bar (per D-01, D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File content → audit judgment | The executor reads upstream-authored text and decides what to edit — risk of misclassifying valid constraint pairs as violations |
| Scanner exemption logic → edit decisions | The scanner exempts em-dash and parenthetical DO NOT patterns — executor must not double-fix exempt lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | YAML frontmatter in reference/workflow files | mitigate | Read file first; identify `---` boundaries with `grep -n "^---" <file>`; make zero changes inside frontmatter; verify with `npm test` which runs agent-frontmatter validation |
| T-09-01-02 | Tampering | Scanner-exempt constraint pairs (em-dash, parenthetical DO NOT) | mitigate | Before editing any DO NOT line, check for em-dash ` — `, period+sentence, or parenthetical pattern; if present, skip — it is a valid constraint pair per scanner exemption rules |
| T-09-01-03 | Denial of service | V09 audit scope inflation — rewriting conformant files | mitigate | For each V09 dimension, first assess whether the file already satisfies it; fix only genuine gaps; do not restructure files that already pass all 8 dimensions |
| T-09-01-04 | Information disclosure | Executor silently skips scanner baseline run | mitigate | Task 1 action block requires scanner run as Step A with an explicit STOP instruction on failure — baseline must be confirmed before any edit |
| T-09-01-05 | Repudiation | Edits to reference data files that break downstream @file include behavior | accept | Reference files are text fragments; structural edits that remove a section an agent depends on could degrade behavior, but no automated test catches this — executor must read surrounding context before compressing any section |
</threat_model>

<verification>
After both tasks complete:

1. Scanner gate: `node --test tests/negative-framing-scan.test.cjs` — must report `pass 34`, `fail 0`
2. Full suite gate: `npm test` — must report at least 4110/4112 passing; failure count must remain exactly 2
3. Structural spot-check (commands): `grep -n "<objective>" commands/gsd/inbox.md commands/gsd/sketch.md commands/gsd/sketch-wrap-up.md commands/gsd/spike.md commands/gsd/spike-wrap-up.md commands/gsd/spec-phase.md` — every file must have at least one match
4. Structural spot-check (workflows): `grep -rn "<purpose>" get-shit-done/workflows/sketch.md get-shit-done/workflows/sketch-wrap-up.md get-shit-done/workflows/spec-phase.md get-shit-done/workflows/spike.md get-shit-done/workflows/spike-wrap-up.md` — every file must have at least one match
5. Structural spot-check (workflows): `grep -rn "<step name=" get-shit-done/workflows/sketch.md get-shit-done/workflows/sketch-wrap-up.md get-shit-done/workflows/spec-phase.md get-shit-done/workflows/spike.md get-shit-done/workflows/spike-wrap-up.md` — every file must have at least one match
6. Data reference integrity: `grep -l "<objective>\|<task>" get-shit-done/references/sketch-theme-system.md get-shit-done/references/sketch-tooling.md get-shit-done/references/sketch-variant-patterns.md` — must return empty (no XML tags in data documents)
</verification>

<success_criteria>
- Negative-framing scanner: 34/34 pass (unchanged from pre-plan baseline)
- Full test suite: ≥ 4110/4112 pass (no regressions from V09 audit edits)
- All 19 in-scope new files pass both the scanner gate and the V09 structural quality bar (per D-01, D-02 in CONTEXT.md)
- NEW-15 (templates/spec.md) confirmed exempt — no edits made to it
- All command files have <objective> as first block, <process> as single delegation sentence
- All workflow files have <purpose> + <required_reading> + <process>/<step name="..."> structure
- All reference files correctly classified (data vs behavioral) — no XML wrapping forced onto data documents
</success_criteria>

<output>
After completion, create `.planning/phases/09-fork-standards-pass/09-01-SUMMARY.md` covering:
- Scanner baseline confirmed (34/34) — document the exact output
- Per-file audit results: for each of the 19 files, list which V09 dimensions had gaps and what was fixed (or "no gaps found — file already conformant")
- Any files that required zero edits (already fully V09-conformant)
- Final scanner result and npm test result after all edits
</output>
