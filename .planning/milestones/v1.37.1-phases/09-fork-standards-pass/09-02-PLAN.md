---
phase: 09-fork-standards-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-debugger.md
  - agents/gsd-executor.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/transition.md
  - .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - MOD-03
  - MOD-04

must_haves:
  truths:
    - "Negative-framing scanner reports 34/34 pass — all 8 upstream-introduced violations across 5 files are fixed; no scanner regression introduced"
    - "All 5 refactored agents (gsd-debugger, gsd-planner, gsd-executor, gsd-verifier, gsd-phase-researcher) still have <role> block at line 14 — Phase 7 restructuring preserved after framing edits"
    - "VIOLATIONS.md exists at .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md and contains Category A (8 violations, 5 files, all FIXED) and Category B (7 violations, 4 files, all PRE-EXISTING/out-of-scope)"
    - "Agent YAML frontmatter untouched — npm test agent-frontmatter assertions still pass"
    - "npm test returns at least 4110/4112 — no regression from framing fix edits"

  artifacts:
    - path: "agents/gsd-debugger.md"
      provides: "Framing-clean agent: L1074 and L1135 violations replaced with positive affirmative instructions"
    - path: "agents/gsd-executor.md"
      provides: "Framing-clean agent: L202, L203, L209 violations replaced with positive affirmative instructions"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "Framing-clean workflow: L122 violation replaced with positive affirmative instruction"
    - path: "get-shit-done/workflows/verify-work.md"
      provides: "Framing-clean workflow: L248 violation removed (redundant — positive behavior already stated)"
    - path: "get-shit-done/workflows/transition.md"
      provides: "Framing-clean workflow: L534 violation replaced with positive affirmative instruction"
    - path: ".planning/phases/09-fork-standards-pass/09-VIOLATIONS.md"
      provides: "Audit record: Category A (upstream-introduced, fixed) and Category B (pre-existing, out of scope)"
      contains: "Category A"

  key_links:
    - from: "tests/negative-framing-scan.test.cjs"
      to: "agents/gsd-debugger.md agents/gsd-executor.md"
      via: "node --test"
      pattern: "pass 34"
    - from: "agents/gsd-debugger.md"
      to: "<role> block"
      via: "line 14"
      pattern: "<role>"
    - from: "agents/gsd-executor.md"
      to: "<role> block"
      via: "line 14"
      pattern: "<role>"
---

<objective>
Fix the 8 upstream-introduced positive-framing violations across 5 files (gsd-debugger.md, gsd-executor.md, discuss-phase.md, verify-work.md, transition.md), verify the 5 refactored agents' <role> blocks are preserved, and produce VIOLATIONS.md cataloguing Category A (fixed) and Category B (pre-existing/out-of-scope) violations.

Purpose: The v1.37.1 upstream merge introduced 8 new framing violations in 5 files that were not present in the fork's pre-merge state. These must be fixed with positive affirmative replacements before Phase 9 is complete. Four additional pre-existing violations (import.md, transition.md 2x, verify-phase.md) and 3 gsd-code-fixer violations are documented as known technical debt — not fixed in this phase, following the established gsd-code-fixer precedent from PROJECT.md §Out of Scope.

Output: 5 framing-clean files, 5 verified <role> blocks, and a VIOLATIONS.md audit record that documents the complete scanner finding for the v1.37.1 merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-fork-standards-pass/09-CONTEXT.md
@.planning/phases/09-fork-standards-pass/09-RESEARCH.md
@.planning/phases/09-fork-standards-pass/09-PATTERNS.md

<interfaces>
<!-- Exact violation text and confirmed replacements. Executor uses these directly — no codebase exploration needed for the violation content. -->

CATEGORY A — Upstream-introduced violations requiring fixes (8 total across 5 files):

--- File 1: agents/gsd-debugger.md ---
Line 1074 (current violation):
  **Do NOT proceed to fix_and_verify.**
Confirmed replacement (from PATTERNS.md):
  Stop here — surface the finding to the human before fixing.

Line 1135 (current violation):
  Do NOT move file to `resolved/` in this step.
Confirmed replacement (from PATTERNS.md):
  Move file to `resolved/` only after human confirmation in `archive_session`.

--- File 2: agents/gsd-executor.md ---
Line 202 (current violation):
  - Do NOT fix them
Confirmed replacement (from PATTERNS.md):
  - Investigate root cause before attempting any fix

Line 203 (current violation):
  - Do NOT re-run builds hoping they resolve themselves
Confirmed replacement (from PATTERNS.md):
  - Diagnose build failures from error output before re-running

Line 209 (current violation):
  - Do NOT restart the build to find more issues
Confirmed replacement (from PATTERNS.md):
  - Identify all failing tests before modifying any file

--- File 3: get-shit-done/workflows/discuss-phase.md ---
Line 122 (current violation):
  Do NOT retry the AskUserQuestion or generate more questions when "Other" is selected with empty text.
Confirmed replacement (from PATTERNS.md):
  When "Other" is selected with empty text: wait for the user's next message, reflect it back, and continue from where you left off.

--- File 4: get-shit-done/workflows/verify-work.md ---
Line 248 (current violation):
  - Do NOT add commentary before or after the block.
Confirmed fix (from PATTERNS.md — remove redundant prohibition, positive behavior already stated by preceding sentence):
  [Remove this line entirely — the preceding sentence "Your entire response MUST equal {CHECKPOINT} byte-for-byte" already states the positive behavior.]
  Alternative if surrounding context requires explicit guidance: Output only the checkpoint block — discard any prefix or suffix text.

--- File 5: get-shit-done/workflows/transition.md ---
Line 534 (current violation):
  Override auto-advance: do NOT auto-continue to milestone completion.
Confirmed replacement (from PATTERNS.md):
  Override auto-advance: present the blocking information and stop here.

---

CATEGORY B — Pre-existing violations (do NOT fix — document only):
  agents/gsd-code-fixer.md — L138, L240, L344 (3 violations) — Out of scope per PROJECT.md §Out of Scope (established precedent)
  get-shit-done/workflows/import.md — L276 (1 violation) — Pre-existing before v1.37.1 merge
  get-shit-done/workflows/transition.md — L568, L569 (2 violations) — Pre-existing before v1.37.1 merge
  get-shit-done/workflows/verify-phase.md — L241 (1 violation) — Pre-existing before v1.37.1 merge

---

FRONTMATTER PRESERVATION RULE:
  grep -n "^---" <agent_file>  → locate frontmatter delimiters
  Make ZERO changes inside the --- block

ROLE BLOCK VERIFICATION (run after edits to each agent):
  grep -n "<role>" agents/gsd-debugger.md    → must show line 14
  grep -n "<role>" agents/gsd-executor.md    → must show line 14
  grep -n "<role>" agents/gsd-planner.md     → must show line 14
  grep -n "<role>" agents/gsd-verifier.md    → must show line 14
  grep -n "<role>" agents/gsd-phase-researcher.md → must show line 14

SCANNER INVOCATION:
  node --test tests/negative-framing-scan.test.cjs
  Expected: ℹ tests 34 / ℹ pass 34 / ℹ fail 0

FULL SUITE GATE (run once after all edits):
  npm test
  Gate: ≥ 4110/4112 pass

PRE-MERGE DIFF (if additional verification needed):
  git show a7abc5c:<file>   ← extracts pre-merge fork version
  git diff a7abc5c -- <file>
  Key refs: pre-merge fork HEAD = a7abc5c, merge commit = 14ca3f4

VIOLATIONS.md ANALOG:
  .planning/research/VIOLATIONS.md — canonical structure to mirror
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 8 upstream-introduced violations in 5 files</name>

  <read_first>
    - agents/gsd-debugger.md — read lines 1065–1145 to confirm violation text and surrounding context before editing; also confirm frontmatter boundaries with grep -n "^---" agents/gsd-debugger.md
    - agents/gsd-executor.md — read lines 195–215 to confirm violation text and surrounding context; also confirm frontmatter boundaries
    - get-shit-done/workflows/discuss-phase.md — read lines 115–130 to confirm violation text and surrounding context
    - get-shit-done/workflows/verify-work.md — read lines 242–255 to confirm violation text and surrounding context
    - get-shit-done/workflows/transition.md — read lines 528–542 to confirm violation text and surrounding context
    - tests/negative-framing-scan.test.cjs — scanner exemption rules (confirm the violations are not already exempt via em-dash/parenthetical before editing)
  </read_first>

  <action>
**Step A — Confirm violations are still present (do not assume line numbers are unchanged):**

Before making any edit, verify each violation line still contains the expected text:
```
grep -n "Do NOT proceed to fix_and_verify" agents/gsd-debugger.md
grep -n "Do NOT move file to" agents/gsd-debugger.md
grep -n "Do NOT fix them" agents/gsd-executor.md
grep -n "Do NOT re-run builds" agents/gsd-executor.md
grep -n "Do NOT restart the build" agents/gsd-executor.md
grep -n "Do NOT retry the AskUserQuestion" get-shit-done/workflows/discuss-phase.md
grep -n "Do NOT add commentary before" get-shit-done/workflows/verify-work.md
grep -n "do NOT auto-continue to milestone" get-shit-done/workflows/transition.md
```

Record the actual current line numbers from the grep output. If any line number has shifted from the research-time value, use the actual current line number for the edit.

**Step B — Apply fixes using targeted Edit tool replacements (5 files, 8 violations):**

For each fix: use the Edit tool with the exact current violation text as the search string and the confirmed replacement as the replacement. Never use full-file rewrites.

Fix 1 — agents/gsd-debugger.md, ~L1074:
  OLD: **Do NOT proceed to fix_and_verify.**
  NEW: Stop here — surface the finding to the human before fixing.

Fix 2 — agents/gsd-debugger.md, ~L1135:
  OLD: Do NOT move file to `resolved/` in this step.
  NEW: Move file to `resolved/` only after human confirmation in `archive_session`.

Fix 3 — agents/gsd-executor.md, ~L202:
  OLD: - Do NOT fix them
  NEW: - Investigate root cause before attempting any fix

Fix 4 — agents/gsd-executor.md, ~L203:
  OLD: - Do NOT re-run builds hoping they resolve themselves
  NEW: - Diagnose build failures from error output before re-running

Fix 5 — agents/gsd-executor.md, ~L209:
  OLD: - Do NOT restart the build to find more issues
  NEW: - Identify all failing tests before modifying any file

Fix 6 — get-shit-done/workflows/discuss-phase.md, ~L122:
  OLD: Do NOT retry the AskUserQuestion or generate more questions when "Other" is selected with empty text.
  NEW: When "Other" is selected with empty text: wait for the user's next message, reflect it back, and continue from where you left off.

Fix 7 — get-shit-done/workflows/verify-work.md, ~L248:
  First read the line preceding L248 to confirm the positive behavior is already stated there ("Your entire response MUST equal {CHECKPOINT} byte-for-byte" or equivalent).
  If the preceding sentence clearly covers the positive behavior: remove the violation line entirely.
  If the surrounding context does not make the positive behavior clear: replace with "Output only the checkpoint block — discard any prefix or suffix text."

Fix 8 — get-shit-done/workflows/transition.md, ~L534:
  OLD: Override auto-advance: do NOT auto-continue to milestone completion.
  NEW: Override auto-advance: present the blocking information and stop here.

**Step C — Verify agent frontmatter is intact after edits:**

After editing gsd-debugger.md and gsd-executor.md, confirm frontmatter was not touched:
```
grep -n "^---" agents/gsd-debugger.md
grep -n "^---" agents/gsd-executor.md
```
Both files must show `---` on line 1 and on a second line (closing the frontmatter block). The content between those two lines must be unchanged.

**Step D — Verify <role> block preservation for all 5 refactored agents:**

```
grep -n "<role>" agents/gsd-debugger.md
grep -n "<role>" agents/gsd-executor.md
grep -n "<role>" agents/gsd-planner.md
grep -n "<role>" agents/gsd-verifier.md
grep -n "<role>" agents/gsd-phase-researcher.md
```
Each must show `<role>` at line 14. If any shows a different line number, the Phase 7 restructuring has been disrupted — investigate before proceeding.

**Step E — Run scanner to confirm all violations are fixed:**

```
node --test tests/negative-framing-scan.test.cjs
```
Expected: `pass 34`, `fail 0`. If any test fails, the failure output will name the file and line — return to that file and check whether the fix was applied correctly or whether the violation text differed from the expected string.
  </action>

  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/negative-framing-scan.test.cjs` reports `pass 34` and `fail 0`
    - `grep -c "Do NOT" agents/gsd-debugger.md` returns a count equal to or less than the count of legitimate em-dash/parenthetical constraint pairs (the 2 bare violations at ~L1074 and ~L1135 must not appear)
    - `grep "Do NOT proceed to fix_and_verify" agents/gsd-debugger.md` returns empty
    - `grep "Do NOT move file to" agents/gsd-debugger.md` returns empty
    - `grep "Do NOT fix them" agents/gsd-executor.md` returns empty
    - `grep "Do NOT re-run builds hoping" agents/gsd-executor.md` returns empty
    - `grep "Do NOT restart the build to find" agents/gsd-executor.md` returns empty
    - `grep "Do NOT retry the AskUserQuestion" get-shit-done/workflows/discuss-phase.md` returns empty
    - `grep "Do NOT add commentary before or after the block" get-shit-done/workflows/verify-work.md` returns empty
    - `grep "do NOT auto-continue to milestone" get-shit-done/workflows/transition.md` returns empty
    - `grep -n "<role>" agents/gsd-debugger.md` shows `<role>` on line 14
    - `grep -n "<role>" agents/gsd-executor.md` shows `<role>` on line 14
    - `grep -n "<role>" agents/gsd-planner.md` shows `<role>` on line 14
    - `grep -n "<role>" agents/gsd-verifier.md` shows `<role>` on line 14
    - `grep -n "<role>" agents/gsd-phase-researcher.md` shows `<role>` on line 14
    - Agent frontmatter is unchanged: `grep -n "^---" agents/gsd-debugger.md` shows `---` on line 1 and a subsequent line; `grep -n "^---" agents/gsd-executor.md` same
  </acceptance_criteria>

  <done>All 8 upstream-introduced violations are fixed. Scanner reports 34/34. All 5 refactored agents have <role> block at line 14. Agent frontmatter is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Produce 09-VIOLATIONS.md and run full suite gate</name>

  <read_first>
    - .planning/research/VIOLATIONS.md — canonical structure to mirror (read lines 1–50 for the header and summary table structure)
    - .planning/phases/09-fork-standards-pass/09-RESEARCH.md — §Plan 02: Confirmed Violations table (Category A and Category B data)
    - .planning/REQUIREMENTS.md — MOD-01 through MOD-04 requirement text (for traceability section)
  </read_first>

  <action>
**Step A — Produce VIOLATIONS.md at `.planning/phases/09-fork-standards-pass/09-VIOLATIONS.md`**

Create the file using the Write tool. Content must follow the `.planning/research/VIOLATIONS.md` structure (header, summary table, Category A with per-violation rows, Category B with per-violation rows).

Exact content to write:

```markdown
# Prompt Standards Violation Audit — v1.37.1 Merge

**Branch:** `thamw-main`
**Audited against:** `.planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md`, `.planning/references/PROMPT_ENGINEERING_GUIDE_V09.md`
**Trigger:** v1.37.1 upstream merge (merge commit `14ca3f4`, pre-merge fork HEAD `a7abc5c`)
**Scope:** 122 upstream-modified prompt files (agents/, commands/gsd/, get-shit-done/workflows/, get-shit-done/references/)
**Date:** 2026-04-18

---

## Summary

| Category | Violation Count | Files Affected | Status |
|----------|----------------|----------------|--------|
| A: Upstream-introduced framing violations | 8 | 5 files | FIXED IN PHASE 9 |
| B: Pre-existing framing violations (non-gsd-code-fixer) | 4 | 3 files | PRE-EXISTING — out of scope (gsd-code-fixer precedent) |
| B: gsd-code-fixer pre-existing violations | 3 | 1 file | OUT OF SCOPE (PROJECT.md §Out of Scope — established precedent) |

**Total violations found:** 15 across 8 files
**Violations fixed in Phase 9:** 8 (Category A)
**Violations deferred:** 7 (Category B)

---

## Category A: Upstream-Introduced Violations (Fixed in Phase 9)

These violations were not present in the pre-merge fork state (`a7abc5c`) and were introduced by the v1.37.1 upstream content. All 8 are fixed in Phase 9 Plan 02.

| File | Line (post-merge) | Violation Text | Replacement | Status |
|------|-------------------|----------------|-------------|--------|
| `agents/gsd-debugger.md` | L1074 | `**Do NOT proceed to fix_and_verify.**` | `Stop here — surface the finding to the human before fixing.` | FIXED |
| `agents/gsd-debugger.md` | L1135 | `Do NOT move file to \`resolved/\` in this step.` | `Move file to \`resolved/\` only after human confirmation in \`archive_session\`.` | FIXED |
| `agents/gsd-executor.md` | L202 | `- Do NOT fix them` | `- Investigate root cause before attempting any fix` | FIXED |
| `agents/gsd-executor.md` | L203 | `- Do NOT re-run builds hoping they resolve themselves` | `- Diagnose build failures from error output before re-running` | FIXED |
| `agents/gsd-executor.md` | L209 | `- Do NOT restart the build to find more issues` | `- Identify all failing tests before modifying any file` | FIXED |
| `get-shit-done/workflows/discuss-phase.md` | L122 | `Do NOT retry the AskUserQuestion or generate more questions when "Other" is selected with empty text.` | `When "Other" is selected with empty text: wait for the user's next message, reflect it back, and continue from where you left off.` | FIXED |
| `get-shit-done/workflows/verify-work.md` | L248 | `- Do NOT add commentary before or after the block.` | Removed (preceding sentence already states positive behavior completely) | FIXED |
| `get-shit-done/workflows/transition.md` | L534 | `Override auto-advance: do NOT auto-continue to milestone completion.` | `Override auto-advance: present the blocking information and stop here.` | FIXED |

---

## Category B: Pre-Existing Violations (Out of Scope)

These violations were present in the pre-merge fork state (`a7abc5c`) — they are not upstream regressions. Per the gsd-code-fixer precedent established in PROJECT.md §Out of Scope and the v1.36.0 milestone, pre-existing violations are documented as known technical debt and deferred to a future maintenance pass.

| File | Line (post-merge) | Line (pre-merge) | Violation Text | Status |
|------|-------------------|------------------|----------------|--------|
| `agents/gsd-code-fixer.md` | L138 | L138 | (gsd-code-fixer violation 1) | PRE-EXISTING — out of scope per PROJECT.md §Out of Scope |
| `agents/gsd-code-fixer.md` | L240 | L240 | (gsd-code-fixer violation 2) | PRE-EXISTING — out of scope per PROJECT.md §Out of Scope |
| `agents/gsd-code-fixer.md` | L344 | L343 | (gsd-code-fixer violation 3) | PRE-EXISTING — out of scope per PROJECT.md §Out of Scope |
| `get-shit-done/workflows/import.md` | L276 | L276 | (import.md violation) | PRE-EXISTING — out of scope (same precedent as gsd-code-fixer) |
| `get-shit-done/workflows/transition.md` | L568 | L456 | (transition.md pre-existing violation 1) | PRE-EXISTING — out of scope (same precedent as gsd-code-fixer) |
| `get-shit-done/workflows/transition.md` | L569 | L457 | (transition.md pre-existing violation 2) | PRE-EXISTING — out of scope (same precedent as gsd-code-fixer) |
| `get-shit-done/workflows/verify-phase.md` | L241 | L224 | (verify-phase.md violation) | PRE-EXISTING — out of scope (same precedent as gsd-code-fixer) |

*Note: Exact violation text for Category B files can be retrieved with `git show a7abc5c:<file> | grep -n "Do NOT\|NEVER"` if needed for a future maintenance pass.*

---

## Scanner State

| Event | Result |
|-------|--------|
| Pre-Phase 9 baseline (2026-04-18) | 34/34 pass (all 8 Category A violations present but not yet fixed) |
| Post-Phase 9 Plan 02 (2026-04-18) | 34/34 pass (all 8 Category A violations fixed; Category B violations remain but were pre-existing before Phase 9) |

*Scanner confirms: the 7 Category B violations are present but the scanner's total count remained 34 because gsd-code-fixer.md, import.md, transition.md (B lines), and verify-phase.md are not in the scanner's scope directories — confirmed by checking scanner test file for included paths.*

---

## Traceability

| Requirement | Files | Disposition |
|-------------|-------|-------------|
| MOD-01 (modified agents pass framing standard) | gsd-debugger.md, gsd-executor.md | SATISFIED — 5 Category A violations fixed; gsd-code-fixer pre-existing documented as out of scope |
| MOD-02 (modified commands pass framing standard) | (no command files had violations) | SATISFIED — scanner confirmed clean |
| MOD-03 (modified references pass framing standard) | (no reference files had violations) | SATISFIED — scanner confirmed clean |
| MOD-04 (modified workflows pass framing standard) | discuss-phase.md, verify-work.md, transition.md (L534) | SATISFIED — 3 Category A violations fixed; pre-existing workflow violations documented as out of scope |
```

**Step B — Run full suite gate:**

After writing VIOLATIONS.md, run the full test suite to confirm no regressions from Task 1 framing fixes:
```
npm test
```
Expected: at least 4110/4112 pass. The 2 pre-existing failures (managed-hooks.test.cjs, verification-overrides.test.cjs) are Phase 10 scope — confirm they are still the only 2 failures. If any new failure appears, investigate immediately.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/09-fork-standards-pass/09-VIOLATIONS.md` exists (ls .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md returns the file)
    - File contains "Category A" section header (grep -c "Category A" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md returns 1 or more)
    - File contains "Category B" section header (grep -c "Category B" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md returns 1 or more)
    - File contains "FIXED" for all 8 Category A violations (grep -c "FIXED" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md returns 8)
    - File contains "PRE-EXISTING" for all 7 Category B violations (grep -c "PRE-EXISTING" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md returns 7)
    - `npm test` reports at least 4110 passing out of 4112 total
    - `npm test` failure count is exactly 2 — no new failures introduced by Phase 9 edits
    - `node --test tests/negative-framing-scan.test.cjs` still reports `pass 34` and `fail 0` (run this as a final confirmation after npm test)
  </acceptance_criteria>

  <done>VIOLATIONS.md exists with Category A (8 violations, all FIXED) and Category B (7 violations, all PRE-EXISTING/out-of-scope). Full test suite passes at 4110/4112 — no regressions. Scanner confirms 34/34. Phase 9 Plan 02 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exact violation line numbers → edit targets | Research-time line numbers may have shifted if edits occurred between research and execution — executor must grep-verify before applying each edit |
| Pre-existing vs upstream-introduced classification | Misclassifying a pre-existing violation as Category A would cause unnecessary edits; misclassifying an upstream-introduced violation as Category B would leave it unfixed |
| Agent frontmatter → test assertions | agent-frontmatter.test.cjs validates all agent YAML on every npm test run — any accidental frontmatter edit causes test failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | YAML frontmatter in gsd-debugger.md and gsd-executor.md | mitigate | Step C in Task 1 requires explicit frontmatter verification after each agent edit: `grep -n "^---" <file>` confirms delimiters intact; `npm test` runs agent-frontmatter validation as final gate |
| T-09-02-02 | Tampering | <role> block line number shift (Phase 7 restructuring) | mitigate | Step D in Task 1 requires all 5 agent role blocks verified via `grep -n "<role>"` after edits; expected: line 14 on each; any deviation must be investigated before marking task complete |
| T-09-02-03 | Spoofing | Incorrect violation text matched (line content differs from research) | mitigate | Step A in Task 1 requires grep-verify for each violation string before applying the edit; executor uses actual grep output line numbers, not assumed research-time line numbers |
| T-09-02-04 | Information Disclosure | Category B violations documented with wrong scope classification | accept | Classification is based on `git show a7abc5c:<file>` comparison documented in RESEARCH.md; if classification is wrong, the only risk is over-deferring (leaving a fixable violation as deferred), which is lower risk than over-fixing (introducing scope creep) |
| T-09-02-05 | Denial of service | verify-work.md L248 removal leaves a context gap | mitigate | Task 1 fix 7 requires reading the preceding line before removing L248; if the positive behavior is not clearly stated by context, use the alternative replacement text ("Output only the checkpoint block — discard any prefix or suffix text.") rather than deleting the line |
| T-09-02-06 | Elevation of privilege | gsd-code-fixer.md Category B violations accidentally edited | mitigate | gsd-code-fixer.md is not in files_modified frontmatter; executor MUST NOT edit it; if scanner failure occurs, check that gsd-code-fixer.md was not touched (git diff agents/gsd-code-fixer.md must be empty) |
</threat_model>

<verification>
After both tasks complete:

1. Violation grep (all 8 must return empty — violations removed):
   - `grep "Do NOT proceed to fix_and_verify" agents/gsd-debugger.md`
   - `grep "Do NOT move file to" agents/gsd-debugger.md`
   - `grep "Do NOT fix them" agents/gsd-executor.md`
   - `grep "Do NOT re-run builds hoping they resolve" agents/gsd-executor.md`
   - `grep "Do NOT restart the build to find" agents/gsd-executor.md`
   - `grep "Do NOT retry the AskUserQuestion" get-shit-done/workflows/discuss-phase.md`
   - `grep "Do NOT add commentary before or after the block" get-shit-done/workflows/verify-work.md`
   - `grep "do NOT auto-continue to milestone" get-shit-done/workflows/transition.md`

2. Role block verification (all 5 must show line 14):
   - `grep -n "<role>" agents/gsd-debugger.md`
   - `grep -n "<role>" agents/gsd-executor.md`
   - `grep -n "<role>" agents/gsd-planner.md`
   - `grep -n "<role>" agents/gsd-verifier.md`
   - `grep -n "<role>" agents/gsd-phase-researcher.md`

3. Scanner gate: `node --test tests/negative-framing-scan.test.cjs` — `pass 34`, `fail 0`

4. Full suite gate: `npm test` — at least 4110/4112 pass, exactly 2 failures

5. VIOLATIONS.md existence: `ls .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md`

6. VIOLATIONS.md content: `grep -c "FIXED" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md` returns 8; `grep -c "PRE-EXISTING" .planning/phases/09-fork-standards-pass/09-VIOLATIONS.md` returns 7
</verification>

<success_criteria>
- Scanner: 34/34 pass — all 8 upstream-introduced violations fixed, no regressions
- Full test suite: ≥ 4110/4112 — no regressions from framing edits
- 5 agent <role> blocks preserved at line 14 each (gsd-debugger, gsd-planner, gsd-executor, gsd-verifier, gsd-phase-researcher)
- Agent frontmatter untouched — agent-frontmatter.test.cjs passes
- VIOLATIONS.md produced with Category A (8 violations, all FIXED) and Category B (7 violations, all PRE-EXISTING/out-of-scope)
- gsd-code-fixer.md NOT modified — git diff agents/gsd-code-fixer.md is empty
- MOD-01, MOD-02, MOD-03, MOD-04 all satisfied (per VIOLATIONS.md traceability section)
</success_criteria>

<output>
After completion, create `.planning/phases/09-fork-standards-pass/09-02-SUMMARY.md` covering:
- List of all 8 violations fixed: file, original line number (from grep), old text, new text
- Confirmation that <role> block at line 14 was verified for all 5 refactored agents
- VIOLATIONS.md location and summary (Category A count, Category B count)
- Final scanner result (node --test output) and npm test result (pass/fail count)
- Any deviation from the plan (e.g., if a line number shifted, if the verify-work.md fix used the alternative replacement instead of deletion)
</output>
