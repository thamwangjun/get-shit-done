---
phase: 07-merge-and-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - tests/agent-frontmatter.test.cjs
  - tests/semver-compare.test.cjs
  - tests/negative-framing-scan.test.cjs
  - tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs
  - tests/version-detection.test.cjs
  - tests/execute-phase-wave.test.cjs
  - tests/ios-scaffold-safety.test.cjs
autonomous: false
requirements:
  - MERGE-01
  - MERGE-02
  - MERGE-03
  - MERGE-04

must_haves:
  truths:
    - "All fork-specific tests pass: agent-frontmatter, negative-framing-scan, bug-1924-ensure-hooks-dist-on-demand, execute-phase-wave, ios-scaffold-safety, version-detection, semver-compare"
    - "A documented baseline failure list exists for all upstream-introduced failures (Phase 10 scope)"
    - "The distinction between fork-corruption failures (fix now) and upstream-introduced failures (defer) is applied per D-07"
  artifacts:
    - path: ".planning/phases/07-merge-and-conflict-resolution/07-02-SUMMARY.md"
      provides: "Baseline failure list for Phase 10"
      contains: "BASELINE FAILURES"
  key_links:
    - from: "tests/agent-frontmatter.test.cjs"
      to: "agents/ fork prompt files"
      via: "assert.ok with /only use the write tool/i"
      pattern: "only use the write tool"
    - from: "tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs"
      to: "bin/install.js ensureHooksDist"
      via: "static source analysis (readFileSync)"
      pattern: "ensureHooksDist"
    - from: "tests/semver-compare.test.cjs"
      to: "hooks/gsd-check-update-worker.js isNewer"
      via: "mirrored function definition"
      pattern: "SHA equality"
---

<objective>
Run the full test suite after the merge commit, triage all failures by the D-07 rule (fork-corruption failure vs upstream-introduced failure), fix every fork-specific test failure in Phase 7, and document all upstream-introduced failures as the baseline input for Phase 10.

Purpose: Establishes the post-merge test state so downstream phases (8, 9, 10) know exactly what is already broken vs what they need to fix. Prevents Phase 10 from reopening conflicts that were actually caused by merge corruption rather than upstream content gaps.

Output: All fork-specific tests pass. A BASELINE FAILURES section in the plan summary documents every upstream-introduced test failure with its root cause classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-merge-and-conflict-resolution/07-CONTEXT.md
@.planning/phases/07-merge-and-conflict-resolution/07-RESEARCH.md
@.planning/phases/07-merge-and-conflict-resolution/07-01-SUMMARY.md

<interfaces>
<!-- Fork-specific tests that MUST pass after Phase 7 — do not defer these. -->
<!-- Source: CONTEXT.md D-06, RESEARCH.md §Post-Merge Test Run -->

Fork-specific test files (must pass in Phase 7):
- tests/agent-frontmatter.test.cjs — 135 tests; validates MERGE-04 (positive-framing assertion in agents)
- tests/negative-framing-scan.test.cjs — fork-added scanner; validates fork's positive framing standard across all prompt files
- tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs — 8 tests; validates MERGE-03 (ensureHooksDist in bin/install.js)
- tests/execute-phase-wave.test.cjs — validates fork workflow strings in execute-phase.md
- tests/ios-scaffold-safety.test.cjs — validates fork reference file
- tests/version-detection.test.cjs — 64 tests; validates fork's SHA-based version detection
- tests/semver-compare.test.cjs — validates fork's SHA isNewer semantics

D-07 triage rule:
- FORK-CORRUPTION: "Test was passing before the merge AND now fails because a fork-specific file was corrupted during conflict resolution" → FIX IN PHASE 7
- UPSTREAM-INTRODUCED: "Test fails because upstream added new content that the fork's standards scanner would catch, OR because a new upstream test validates upstream-only content" → DOCUMENT AS BASELINE, defer to Phase 10

Known upstream-introduced failures (DO NOT fix in Phase 7):
- tests/command-count-sync.test.cjs — will fail because ARCHITECTURE.md command count not yet updated (Phase 8 scope — CAT-06)
- Any test that fails because new upstream prompt files do not yet meet fork standards (Phase 9 scope)
- Any test that fails because docs/ counts are wrong by exactly the count of new files added in v1.37.1

Static analysis test pattern (from tests/version-detection.test.cjs and tests/semver-compare.test.cjs):
```javascript
// Read source as string — avoids require() side effects
const installSrc = fs.readFileSync(path.join(__dirname, '..', 'bin', 'install.js'), 'utf8');
// Mirror function for local testing (worker has no module.exports)
function isNewer(latest, installed) {
  return !!latest && latest.slice(0, 7) !== installed;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture baseline failure list</name>
  <files>n/a — read-only diagnostic run; no files modified</files>
  <read_first>
    - .planning/phases/07-merge-and-conflict-resolution/07-01-SUMMARY.md (confirms merge is complete, notes any unexpected resolutions that may affect test expectations)
    - .planning/phases/07-merge-and-conflict-resolution/07-CONTEXT.md (D-06 and D-07 triage rules)
    - .planning/phases/07-merge-and-conflict-resolution/07-RESEARCH.md §Post-Merge Test Run and §New Test Files Added by Upstream
  </read_first>
  <action>
**Step 1 — Run the full test suite:**
```bash
npm test 2>&1 | tee /tmp/phase7-test-output.txt
```
Capture complete output including pass/fail counts and all failure messages.

**Step 2 — Extract the failure list:**
```bash
grep -E "^not ok|FAIL|Error:|AssertionError" /tmp/phase7-test-output.txt | head -100
```

**Step 3 — Run fork-specific tests individually to get clean output:**
```bash
node --test tests/agent-frontmatter.test.cjs 2>&1 | tail -20
node --test tests/negative-framing-scan.test.cjs 2>&1 | tail -20
node --test tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs 2>&1 | tail -20
node --test tests/version-detection.test.cjs 2>&1 | tail -20
node --test tests/semver-compare.test.cjs 2>&1 | tail -20
```

**Step 4 — Apply the D-07 triage rule to every failing test:**

For each failure, ask: "Was this test passing before the merge?"

If YES and it now fails — is it because:
  a) A fork-patched file was corrupted (e.g., the test asserts for `thamwangjun` but the file no longer has it) → FORK-CORRUPTION — fix in Task 2
  b) A new upstream file added content that violates fork standards → UPSTREAM-INTRODUCED — add to baseline list, defer to Phase 10

If the test is NEW (added by upstream in v1.37.1) and fails → UPSTREAM-INTRODUCED — add to baseline list, defer to Phase 10.

**Step 5 — Record the triage results in a working list:**
For each failing test, record:
- Test file name
- Failure message (exact string)
- Classification: FORK-CORRUPTION or UPSTREAM-INTRODUCED
- For FORK-CORRUPTION: which fork patch was corrupted and which file
- For UPSTREAM-INTRODUCED: which Phase scope (8, 9, or 10) will fix it

**Known UPSTREAM-INTRODUCED failures (do not investigate or fix):**
- `tests/command-count-sync.test.cjs` — Phase 8 scope (CAT-06)
- Any test asserting for exact file counts in docs/ARCHITECTURE.md
  </action>
  <verify>
    <automated>
node --test tests/agent-frontmatter.test.cjs 2>&1 | tail -5
node --test tests/semver-compare.test.cjs 2>&1 | tail -5
node --test tests/version-detection.test.cjs 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/phase7-test-output.txt` exists and contains the full `npm test` output
    - Every failing test has been classified as FORK-CORRUPTION or UPSTREAM-INTRODUCED in the working list
    - No failing test is left unclassified
  </acceptance_criteria>
  <done>Complete triage list produced. Every test failure has a classification and an owner phase.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all FORK-CORRUPTION test failures</name>
  <files>tests/agent-frontmatter.test.cjs, tests/semver-compare.test.cjs, tests/negative-framing-scan.test.cjs, tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs, tests/version-detection.test.cjs, tests/execute-phase-wave.test.cjs, tests/ios-scaffold-safety.test.cjs</files>
  <read_first>
    - Each failing fork-specific test file before touching it (read current state)
    - hooks/gsd-check-update-worker.js (if semver-compare or version-detection fails — verify the mirrored isNewer semantics match the worker's actual function)
    - bin/install.js around lines 58-73 and 207-237 (if bug-1924 or version-detection fails — confirm the fork patches survived)
    - tests/agent-frontmatter.test.cjs (if it fails — re-read the assertion at lines 38-41 and the skip condition at line 53)
    - .planning/phases/07-merge-and-conflict-resolution/07-PATTERNS.md §Shared Patterns (Node.js built-in test runner pattern — all test files use `require('node:test')` and `require('node:assert/strict')`)
  </read_first>
  <action>
**Only act on tests classified as FORK-CORRUPTION in Task 1.**

For each FORK-CORRUPTION failure, the root cause is that a fork-patched file was corrupted during merge conflict resolution. The fix follows one of two paths:

**Path A — The fork-patched source file was corrupted (not the test file):**
Example: `tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs` fails because `ensureHooksDist` is missing from `bin/install.js`.
Fix: Re-apply the fork patch to the source file (`bin/install.js` or `hooks/gsd-check-update-worker.js`), not the test. The test is correct; the source file regressed.
After fix: run `grep ensureHooksDist bin/install.js` or `grep thamwangjun hooks/gsd-check-update-worker.js` to confirm the patch is back, then re-run the individual test.

**Path B — The test file itself was corrupted during conflict resolution:**
Example: `tests/agent-frontmatter.test.cjs` now contains the upstream prohibition-form assertion instead of the fork's positive-framing assertion.
Fix: Re-apply the fork's assertion to the test file. Exact strings are in the `<interfaces>` block above.
- `assert.ok(/only use the write tool/i.test(content), ...)` at lines 38-41
- `if (line.includes('NEVER') || line.trim().startsWith('```')) continue;` at line 53
After fix: run `node --test tests/agent-frontmatter.test.cjs`.

**Specific known FORK-CORRUPTION patterns (from RESEARCH.md §Pitfall 4):**
If `tests/execute-phase-wave.test.cjs` fails: Check whether `get-shit-done/workflows/execute-phase.md` was overwritten by upstream during merge. If the workflow file now uses upstream strings that differ from fork strings, this is a FORK-CORRUPTION (the workflow file should have been preserved from fork). Re-apply fork's content. But if the upstream workflow simply added new content the test does not account for, that is UPSTREAM-INTRODUCED — classify it and move to the baseline list.

**After fixing each FORK-CORRUPTION:**
```bash
node --test tests/<fixed-file>.test.cjs
```
Must pass before moving to the next file.

**After all FORK-CORRUPTION fixes, run fork-specific tests as a set:**
```bash
node --test \
  tests/agent-frontmatter.test.cjs \
  tests/negative-framing-scan.test.cjs \
  tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs \
  tests/version-detection.test.cjs \
  tests/semver-compare.test.cjs \
  tests/execute-phase-wave.test.cjs \
  tests/ios-scaffold-safety.test.cjs
```
All must pass.

**If Task 1 produced zero FORK-CORRUPTION failures:**
All fork-specific tests already pass. Skip to verify step — run the command above to confirm and record the result.

**Stage and commit any fixes:**
```bash
git add <fixed-files>
git commit -m "fix(07): restore fork-specific test assertions after merge"
```
Use this format only if files were actually changed. If no files needed fixing, no commit is needed.
  </action>
  <verify>
    <automated>
node --test tests/agent-frontmatter.test.cjs tests/negative-framing-scan.test.cjs tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs tests/version-detection.test.cjs tests/semver-compare.test.cjs 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/agent-frontmatter.test.cjs` exits 0
    - `node --test tests/negative-framing-scan.test.cjs` exits 0
    - `node --test tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs` exits 0
    - `node --test tests/version-detection.test.cjs` exits 0
    - `node --test tests/semver-compare.test.cjs` exits 0
    - If tests/execute-phase-wave.test.cjs was classified FORK-CORRUPTION: it exits 0
    - If tests/ios-scaffold-safety.test.cjs was classified FORK-CORRUPTION: it exits 0
    - No additional commits introduce new failures in any previously-passing test
  </acceptance_criteria>
  <done>All FORK-CORRUPTION test failures are fixed. All fork-specific tests listed above pass individually and as a set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of baseline failure list</name>
  <files>n/a — review only; no files modified</files>
  <read_first>
    - /tmp/phase7-test-output.txt (the full npm test output from Task 1)
    - Task 1 triage working list (in Task 1's execution notes or the summary)
  </read_first>
  <action>
Human reviews the triage list from Task 1, confirms all FORK-CORRUPTION items are fixed, and confirms the UPSTREAM-INTRODUCED baseline list is correctly classified before approving Phase 7 as complete.
  </action>
  <verify>
    <automated>
node --test tests/agent-frontmatter.test.cjs tests/negative-framing-scan.test.cjs tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs tests/version-detection.test.cjs tests/semver-compare.test.cjs 2>&1 | tail -5
    </automated>
  </verify>
  <what-built>
    Task 1 ran `npm test` and classified every failing test as FORK-CORRUPTION or UPSTREAM-INTRODUCED. Task 2 fixed all FORK-CORRUPTION failures. The baseline failure list now contains only UPSTREAM-INTRODUCED failures that are deferred to Phases 8, 9, and 10.
  </what-built>
  <how-to-verify>
    1. Review the triage list from Task 1. Confirm all FORK-CORRUPTION items have been fixed (Task 2 passed).
    2. Review the UPSTREAM-INTRODUCED list. Confirm these are correctly deferred:
       - `tests/command-count-sync.test.cjs` failure is expected (Phase 8 will fix via CAT-06)
       - Any framing-scanner failures on new upstream files are expected (Phase 9 will fix)
       - No fork-specific test (`agent-frontmatter`, `negative-framing-scan`, `bug-1924`, `version-detection`, `semver-compare`) should appear in the UPSTREAM-INTRODUCED list
    3. Run: `node --test tests/agent-frontmatter.test.cjs tests/negative-framing-scan.test.cjs tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs tests/version-detection.test.cjs tests/semver-compare.test.cjs`
       Confirm all pass.
    4. Confirm the failure count is reasonable: pre-merge baseline was 3945/3945. Post-merge upstream-introduced failures are expected primarily from `command-count-sync.test.cjs` and new upstream framing-scanner tests, not from fork-specific tests.
  </how-to-verify>
  <resume-signal>Type "approved" with a brief note on the upstream failure count (e.g., "approved — 12 upstream-introduced failures documented"), or describe any misclassified failures for Task 2 to fix.</resume-signal>
  <acceptance_criteria>
    - All 5 core fork-specific tests pass (verified by automated command above)
    - No fork-specific test appears in the UPSTREAM-INTRODUCED baseline list
    - Human has confirmed the baseline failure count and classification are correct
  </acceptance_criteria>
  <done>Human approves the baseline failure list. Phase 7 is complete and the baseline is recorded in the plan summary for Phase 10 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm test output → triage classification | Test failure messages must be read carefully; misclassifying FORK-CORRUPTION as UPSTREAM-INTRODUCED would silently ship broken fork behavior |
| fork test files → conflict resolution outcome | Test files themselves may have been corrupted during merge (Path B in Task 2) — reading them before acting is mandatory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Repudiation | D-07 triage classification | mitigate | Human-verify checkpoint (Task 3) requires developer to confirm baseline list before phase closes |
| T-07-09 | Tampering | tests/negative-framing-scan.test.cjs | mitigate | Read before touching; verify exit 0 after any fix; this test guards the positive-framing standard for all 9 phases |
| T-07-10 | Denial of Service | scripts/run-tests.cjs SERIAL_FILES isolation | accept | scripts/run-tests.cjs SERIAL_FILES block preserved during merge (verified in Plan 07-01); hooks/dist/ race condition cannot occur |
| T-07-11 | Information Disclosure | /tmp/phase7-test-output.txt | accept | Local temp file; contains only test pass/fail output; no credentials or secrets |
| T-07-12 | Elevation of Privilege | git commit after test fixes | accept | Normal commit with no force-push; standard branch protections apply |
| T-07-13 | Spoofing | Upstream-introduced test misclassification | mitigate | Task 3 human checkpoint explicitly verifies no fork-specific test appears in upstream-introduced list |
</threat_model>

<verification>
Phase 7 closes only when all of the following are true:

```bash
# Fork-specific tests all pass
node --test \
  tests/agent-frontmatter.test.cjs \
  tests/negative-framing-scan.test.cjs \
  tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs \
  tests/version-detection.test.cjs \
  tests/semver-compare.test.cjs
# All must exit 0

# MERGE-01 through MERGE-04 still hold after any Task 2 fixes
git log --oneline upstream/main ^thamw-main | wc -l  # 0
grep thamwangjun hooks/gsd-check-update-worker.js    # match
grep ensureHooksDist bin/install.js                   # match (fn def + call site)
grep -i "only use" tests/agent-frontmatter.test.cjs  # match
```

The UPSTREAM-INTRODUCED baseline failure list is documented in the plan summary and consumed by Phase 10 task planning.
</verification>

<success_criteria>
- All fork-specific tests pass: agent-frontmatter, negative-framing-scan, bug-1924-ensure-hooks-dist-on-demand, version-detection, semver-compare, execute-phase-wave (if applicable), ios-scaffold-safety (if applicable)
- MERGE-01 through MERGE-04 grep checks still pass after any Task 2 fixes
- Baseline failure list recorded in plan summary: each entry has test name, failure message, classification (UPSTREAM-INTRODUCED), and owner phase (8, 9, or 10)
- Human has reviewed and approved the baseline failure list
- Phase 10 has sufficient input to plan its test remediation work
</success_criteria>

<output>
After completion, create `.planning/phases/07-merge-and-conflict-resolution/07-02-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.

The BASELINE FAILURES section is mandatory and must include for each upstream-introduced failure:
- Test file name
- Failure message (quoted exactly)
- Owner phase (8, 9, or 10) and requirement ID that will fix it
- Brief root cause (e.g., "ARCHITECTURE.md command count not updated — fixed by CAT-06 in Phase 8")

Example format:
```
## BASELINE FAILURES (upstream-introduced — Phase 10 input)

| Test | Failure Message | Owner Phase | Req ID | Root Cause |
|------|----------------|-------------|--------|------------|
| tests/command-count-sync.test.cjs | "command count mismatch: expected 50, got 44" | Phase 8 | CAT-06 | ARCHITECTURE.md not updated for +6 new commands |
```
</output>
