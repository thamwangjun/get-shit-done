---
phase: 08-catalogue-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CATALOGUE.json
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - CAT-06

must_haves:
  truths:
    - "All 6 new commands appear in CATALOGUE.json commands array"
    - "All 8 new references appear in CATALOGUE.json references array"
    - "All 5 new workflows appear in CATALOGUE.json workflows array"
    - "spec.md appears in CATALOGUE.json templates array"
    - "CATALOGUE.json counts and total reflect the 20-entry addition (total=270)"
    - "tests/command-count-sync.test.cjs passes"
  artifacts:
    - path: "CATALOGUE.json"
      provides: "Updated registry with all 20 new v1.37.1 entries"
      contains: "inbox.md, sketch.md, sketch-wrap-up.md, spec-phase.md, spike.md, spike-wrap-up.md"
    - path: "docs/ARCHITECTURE.md"
      provides: "Accurate command count (79) that passes the sync test"
      contains: "Total commands: 79"
  key_links:
    - from: "CATALOGUE.json counts block"
      to: "actual array lengths"
      via: "manual count verification"
      pattern: '"total": 270'
    - from: "docs/ARCHITECTURE.md"
      to: "commands/gsd/*.md count"
      via: "tests/command-count-sync.test.cjs"
      pattern: "Total commands.*79"
---

<objective>
Add all 20 new v1.37.1 prompt files to CATALOGUE.json (6 commands, 8 references, 5 workflows, 1 template), update the counts block atomically, and verify docs/ARCHITECTURE.md command count passes the sync test.

Purpose: CATALOGUE.json is the machine-readable registry that downstream tooling and documentation depend on. Phase 9 and Phase 10 both assume CATALOGUE.json is accurate. Closing this phase unblocks the fork standards pass.
Output: Updated CATALOGUE.json with 270 entries and a passing command-count-sync test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-catalogue-sync/08-CONTEXT.md
@.planning/phases/08-catalogue-sync/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run disk diff, insert 20 CATALOGUE.json entries, and update counts block</name>
  <files>CATALOGUE.json</files>

  <read_first>
    - CATALOGUE.json — read the full file to understand current structure and exact line positions before any edit
    - .planning/phases/08-catalogue-sync/08-PATTERNS.md — contains the pre-computed insertion positions, exact entry JSON, and counts block target values
    - .planning/phases/08-catalogue-sync/08-CONTEXT.md — contains D-01 through D-05 decisions governing entry descriptions and count targets
  </read_first>

  <action>
**Step 1 — Pre-flight disk diff (per D-05)**

Run the following and confirm exactly 20 files are on disk but absent from CATALOGUE.json before writing anything:

```bash
# Commands
ls commands/gsd/*.md | xargs -n1 basename | sort > /tmp/disk-commands.txt
node -e "const c=require('./CATALOGUE.json'); c.commands.forEach(e=>console.log(e.file.split('/').pop()))" | sort > /tmp/cat-commands.txt
diff /tmp/disk-commands.txt /tmp/cat-commands.txt

# References
ls get-shit-done/references/*.md | xargs -n1 basename | sort > /tmp/disk-refs.txt
node -e "const c=require('./CATALOGUE.json'); c.references.forEach(e=>console.log(e.file.split('/').pop()))" | sort > /tmp/cat-refs.txt
diff /tmp/disk-refs.txt /tmp/cat-refs.txt

# Workflows
ls get-shit-done/workflows/*.md | xargs -n1 basename | sort > /tmp/disk-wf.txt
node -e "const c=require('./CATALOGUE.json'); c.workflows.forEach(e=>console.log(e.file.split('/').pop()))" | sort > /tmp/cat-wf.txt
diff /tmp/disk-wf.txt /tmp/cat-wf.txt

# Templates
ls get-shit-done/templates/*.md | xargs -n1 basename | sort > /tmp/disk-tmpl.txt
node -e "const c=require('./CATALOGUE.json'); c.templates.forEach(e=>console.log(e.file.split('/').pop()))" | sort > /tmp/cat-tmpl.txt
diff /tmp/disk-tmpl.txt /tmp/cat-tmpl.txt
```

If the diff shows anything other than the 20 expected files, stop and reconcile before editing. The 20 expected new entries are:
- Commands (6): inbox.md, sketch.md, sketch-wrap-up.md, spec-phase.md, spike.md, spike-wrap-up.md
- References (8): autonomous-smart-discuss.md, debugger-philosophy.md, mandatory-initial-read.md, project-skills-discovery.md, sketch-interactivity.md, sketch-theme-system.md, sketch-tooling.md, sketch-variant-patterns.md
- Workflows (5): sketch.md, sketch-wrap-up.md, spec-phase.md, spike.md, spike-wrap-up.md
- Templates (1): spec.md

**Step 2 — Edit CATALOGUE.json**

Read the current CATALOGUE.json in full, then write the updated file with:

**A. Counts block (lines 2-9) — update to:**
```json
{
  "total": 270,
  "counts": {
    "commands": 79,
    "workflows": 80,
    "agents": 31,
    "references": 48,
    "templates": 32
  }
}
```

**B. Commands array — insert 6 entries at correct alphabetical positions:**

Insertion 1 — after `commands/gsd/help.md`, before `commands/gsd/import.md`:
```json
{ "file": "commands/gsd/inbox.md", "description": "Triage and review all open GitHub issues and PRs against project templates and contribution guidelines" },
```

Insertion 2 — after `commands/gsd/settings.md`, before `commands/gsd/ship.md`:
```json
{ "file": "commands/gsd/sketch.md", "description": "Rapidly sketch UI/design ideas using throwaway HTML mockups with multi-variant exploration" },
{ "file": "commands/gsd/sketch-wrap-up.md", "description": "Package sketch design findings into a persistent project skill for future build conversations" },
```

Insertion 3 — after `commands/gsd/stats.md`, before `commands/gsd/thread.md`:
```json
{ "file": "commands/gsd/spec-phase.md", "description": "Socratic spec refinement — clarify WHAT a phase delivers with ambiguity scoring before discuss-phase" },
{ "file": "commands/gsd/spike.md", "description": "Rapidly spike an idea with throwaway experiments to validate feasibility before planning" },
{ "file": "commands/gsd/spike-wrap-up.md", "description": "Package spike findings into a persistent project skill for future build conversations" },
```

**C. References array — insert 8 entries at correct alphabetical positions:**

Insertion 1 — after `references/artifact-types.md`, before `references/checkpoints.md`:
```json
{ "file": "get-shit-done/references/autonomous-smart-discuss.md", "description": "Autonomous-mode variant of discuss-phase — proposes grey-area answers in batch tables for user acceptance or override" },
```

Insertion 2 — after `references/decimal-phase-calculation.md`, before `references/domain-probes.md`:
```json
{ "file": "get-shit-done/references/debugger-philosophy.md", "description": "Evergreen debugging disciplines applicable across every bug, language, and system — loaded by gsd-debugger" },
```

Insertion 3 — after `references/model-profiles.md`, before `references/phase-argument-parsing.md`:
```json
{ "file": "get-shit-done/references/mandatory-initial-read.md", "description": "Protocol requiring agents to load all required_reading files before performing any other actions" },
```

Insertion 4 — after `references/planning-config.md`, before `references/questioning.md`:
```json
{ "file": "get-shit-done/references/project-skills-discovery.md", "description": "Steps for discovering and applying project-defined skills before execution — shared across all GSD agents" },
```

Insertion 5 — after `references/revision-loop.md`, before `references/tdd.md`:
```json
{ "file": "get-shit-done/references/sketch-interactivity.md", "description": "Required interactivity patterns for sketch mockups — every interactive element must respond to user actions" },
{ "file": "get-shit-done/references/sketch-theme-system.md", "description": "Shared CSS variable theme system for sketch artifacts stored in .planning/sketches/themes/" },
{ "file": "get-shit-done/references/sketch-tooling.md", "description": "Floating toolbar specification included in every sketch for reset, variant switching, and note capture" },
{ "file": "get-shit-done/references/sketch-variant-patterns.md", "description": "Multi-variant HTML patterns for sketches — tab-based switching between 2-3 design variants" },
```

**D. Workflows array — insert 5 entries at correct alphabetical positions:**

Insertion 1 — after `workflows/settings.md`, before `workflows/ship.md`:
```json
{ "file": "get-shit-done/workflows/sketch.md", "description": "Explores design directions through throwaway HTML mockups with multi-variant comparison and artifact storage" },
{ "file": "get-shit-done/workflows/sketch-wrap-up.md", "description": "Curates sketch findings and packages them into a persistent project skill with a wrap-up summary" },
```

Insertion 2 — after `workflows/stats.md`, before `workflows/transition.md`:
```json
{ "file": "get-shit-done/workflows/spec-phase.md", "description": "Clarifies phase deliverables through Socratic ambiguity scoring and produces a SPEC.md with falsifiable requirements" },
{ "file": "get-shit-done/workflows/spike.md", "description": "Rapid feasibility validation through focused throwaway experiments with observable evidence" },
{ "file": "get-shit-done/workflows/spike-wrap-up.md", "description": "Curates spike findings and packages them into a persistent project skill with a wrap-up summary" },
```

**E. Templates array — insert 1 entry at correct alphabetical position:**

Insertion — after `templates/SECURITY.md`, before `templates/state.md`:
```json
{ "file": "get-shit-done/templates/spec.md", "description": "Phase spec template locking falsifiable requirements before discuss-phase begins" },
```

**Minimal diff principle (from Phase 7):** Change only the counts block and the 20 new entry insertions. Do not reformat any existing entries, reorder existing lines, or alter whitespace outside the insertion points.

**Atomic write:** Write all insertions and the counts block in a single file write so CATALOGUE.json is never in a partially-updated state.
  </action>

  <verify>
    <automated>
# 1. Verify counts block values
node -e "
const c = require('./CATALOGUE.json');
const errors = [];
if (c.total !== 270) errors.push('total: expected 270, got ' + c.total);
if (c.counts.commands !== 79) errors.push('commands: expected 79, got ' + c.counts.commands);
if (c.counts.workflows !== 80) errors.push('workflows: expected 80, got ' + c.counts.workflows);
if (c.counts.references !== 48) errors.push('references: expected 48, got ' + c.counts.references);
if (c.counts.templates !== 32) errors.push('templates: expected 32, got ' + c.counts.templates);
if (c.counts.agents !== 31) errors.push('agents: expected 31, got ' + c.counts.agents);
if (errors.length) { console.error('FAIL:', errors.join(', ')); process.exit(1); }
console.log('counts block: OK');
"

# 2. Verify actual array lengths match counts
node -e "
const c = require('./CATALOGUE.json');
const errors = [];
if (c.commands.length !== c.counts.commands) errors.push('commands array length ' + c.commands.length + ' != counts ' + c.counts.commands);
if (c.workflows.length !== c.counts.workflows) errors.push('workflows array length ' + c.workflows.length + ' != counts ' + c.counts.workflows);
if (c.references.length !== c.counts.references) errors.push('references array length ' + c.references.length + ' != counts ' + c.counts.references);
if (c.templates.length !== c.counts.templates) errors.push('templates array length ' + c.templates.length + ' != counts ' + c.counts.templates);
if (errors.length) { console.error('FAIL:', errors.join(', ')); process.exit(1); }
console.log('array lengths match counts: OK');
"

# 3. Verify all 20 specific entries are present
node -e "
const c = require('./CATALOGUE.json');
const missing = [];
const cmdFiles = c.commands.map(e => e.file);
const refFiles = c.references.map(e => e.file);
const wfFiles = c.workflows.map(e => e.file);
const tmplFiles = c.templates.map(e => e.file);
['commands/gsd/inbox.md','commands/gsd/sketch.md','commands/gsd/sketch-wrap-up.md','commands/gsd/spec-phase.md','commands/gsd/spike.md','commands/gsd/spike-wrap-up.md'].forEach(f => { if (!cmdFiles.includes(f)) missing.push(f); });
['get-shit-done/references/autonomous-smart-discuss.md','get-shit-done/references/debugger-philosophy.md','get-shit-done/references/mandatory-initial-read.md','get-shit-done/references/project-skills-discovery.md','get-shit-done/references/sketch-interactivity.md','get-shit-done/references/sketch-theme-system.md','get-shit-done/references/sketch-tooling.md','get-shit-done/references/sketch-variant-patterns.md'].forEach(f => { if (!refFiles.includes(f)) missing.push(f); });
['get-shit-done/workflows/sketch.md','get-shit-done/workflows/sketch-wrap-up.md','get-shit-done/workflows/spec-phase.md','get-shit-done/workflows/spike.md','get-shit-done/workflows/spike-wrap-up.md'].forEach(f => { if (!wfFiles.includes(f)) missing.push(f); });
['get-shit-done/templates/spec.md'].forEach(f => { if (!tmplFiles.includes(f)) missing.push(f); });
if (missing.length) { console.error('FAIL — missing entries:', missing.join(', ')); process.exit(1); }
console.log('all 20 entries present: OK');
"

# 4. Verify JSON is valid
node -e "JSON.parse(require('fs').readFileSync('./CATALOGUE.json','utf8')); console.log('JSON valid: OK')"
    </automated>
  </verify>

  <done>
- CATALOGUE.json is valid JSON with no parse errors
- `total` is 270, `counts.commands` is 79, `counts.references` is 48, `counts.workflows` is 80, `counts.templates` is 32, `counts.agents` is 31
- All 6 command entries, 8 reference entries, 5 workflow entries, and 1 template entry are present with correct `file` paths
- No `docs/gsd-sdk-query-migration-blurb.md` or `docs/skills/discovery-contract.md` entry exists (per D-04)
- Existing entries are unchanged (minimal diff principle: no reformatting, no reordering)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ARCHITECTURE.md command count and run gate test</name>
  <files>docs/ARCHITECTURE.md</files>

  <read_first>
    - tests/command-count-sync.test.cjs — understand exactly what the test checks (prose count pattern and tree count pattern) before running it
    - docs/ARCHITECTURE.md — read lines 110–120 and 405–420 to see current values of the two count references
    - .planning/phases/08-catalogue-sync/08-CONTEXT.md — D-06 decision: run the test first, only edit ARCHITECTURE.md if it fails
  </read_first>

  <action>
**Step 1 — Run the gate test first (per D-06)**

```bash
node --test tests/command-count-sync.test.cjs
```

**If the test passes:** ARCHITECTURE.md requires no changes. Task is complete — skip Step 2.

**If the test fails:** Proceed to Step 2.

**Step 2 — Update ARCHITECTURE.md (only if test failed)**

The test checks two patterns. Both must show the actual `commands/gsd/*.md` count (79 after the Phase 8 command additions):

First, confirm the actual count:
```bash
ls commands/gsd/*.md | wc -l
```

Then update both occurrences in `docs/ARCHITECTURE.md`:

1. The prose line — find the pattern `**Total commands:** N` and update N to match the actual count (expected 79).
2. The tree comment line — find the pattern `commands/gsd/*.md  # N slash commands` and update N to match the actual count (expected 79).

Use minimal-diff principle: change only these two numbers. Do not reformat surrounding content.

After editing, re-run the test to confirm it passes:
```bash
node --test tests/command-count-sync.test.cjs
```
  </action>

  <verify>
    <automated>node --test tests/command-count-sync.test.cjs</automated>
  </verify>

  <done>
- `node --test tests/command-count-sync.test.cjs` exits 0 with all 5 subtests passing
- Both count values in docs/ARCHITECTURE.md match the actual `commands/gsd/*.md` file count (79)
- If docs/ARCHITECTURE.md was already correct (D-06 expected state), the file is unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file system → CATALOGUE.json write | Executor writes JSON directly; malformed write would corrupt the registry |
| CATALOGUE.json → downstream consumers | Phase 9 and Phase 10 rely on CATALOGUE.json accuracy; incorrect entries would silently propagate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | CATALOGUE.json write | mitigate | Validate JSON parses successfully immediately after write (`JSON.parse` in verify step); run array-length-vs-counts check to catch any partial write |
| T-08-02 | Tampering | counts block | mitigate | Verify all 6 counts fields atomically in Task 1 verify step; counts and array lengths cross-validated against each other |
| T-08-03 | Information Disclosure | docs/ entries in CATALOGUE | mitigate | D-04 explicitly excludes docs/gsd-sdk-query-migration-blurb.md and docs/skills/discovery-contract.md; acceptance criteria checks for their absence |
| T-08-04 | Denial of Service | ARCHITECTURE.md silent mismatch | accept | D-06 gate test runs before any write; test failure surfaces immediately rather than silently; low risk (no security data involved) |
| T-08-05 | Repudiation | CATALOGUE.json ordering | accept | Arrays are alphabetically ordered; executor validates insertions using disk diff pre-flight (D-05); any ordering error caught by diff |
</threat_model>

<verification>
After both tasks complete, verify the full phase goal:

```bash
# 1. All 6 commands present
node -e "const c=require('./CATALOGUE.json'); const files=c.commands.map(e=>e.file); ['commands/gsd/inbox.md','commands/gsd/sketch.md','commands/gsd/sketch-wrap-up.md','commands/gsd/spec-phase.md','commands/gsd/spike.md','commands/gsd/spike-wrap-up.md'].forEach(f=>{if(!files.includes(f))throw new Error('MISSING: '+f)}); console.log('CAT-01: OK')"

# 2. All 8 references present
node -e "const c=require('./CATALOGUE.json'); const files=c.references.map(e=>e.file); ['get-shit-done/references/autonomous-smart-discuss.md','get-shit-done/references/debugger-philosophy.md','get-shit-done/references/mandatory-initial-read.md','get-shit-done/references/project-skills-discovery.md','get-shit-done/references/sketch-interactivity.md','get-shit-done/references/sketch-theme-system.md','get-shit-done/references/sketch-tooling.md','get-shit-done/references/sketch-variant-patterns.md'].forEach(f=>{if(!files.includes(f))throw new Error('MISSING: '+f)}); console.log('CAT-02: OK')"

# 3. All 5 workflows present
node -e "const c=require('./CATALOGUE.json'); const files=c.workflows.map(e=>e.file); ['get-shit-done/workflows/sketch.md','get-shit-done/workflows/sketch-wrap-up.md','get-shit-done/workflows/spec-phase.md','get-shit-done/workflows/spike.md','get-shit-done/workflows/spike-wrap-up.md'].forEach(f=>{if(!files.includes(f))throw new Error('MISSING: '+f)}); console.log('CAT-03: OK')"

# 4. spec.md template present
node -e "const c=require('./CATALOGUE.json'); if(!c.templates.find(e=>e.file==='get-shit-done/templates/spec.md'))throw new Error('MISSING: spec.md'); console.log('CAT-04: OK')"

# 5. Counts correct (total=270)
node -e "const c=require('./CATALOGUE.json'); if(c.total!==270)throw new Error('total wrong: '+c.total); console.log('CAT-05: OK (total='+c.total+')')"

# 6. Command count sync test passes
node --test tests/command-count-sync.test.cjs
```

All 6 commands should output OK or test pass. Any failure is a blocking issue to fix before marking the phase complete.
</verification>

<success_criteria>
Phase 8 is complete when all five ROADMAP success criteria are satisfied:

1. All 6 new commands (inbox.md, sketch.md, sketch-wrap-up.md, spec-phase.md, spike.md, spike-wrap-up.md) have entries in CATALOGUE.json commands array
2. All 8 new references have entries in CATALOGUE.json references array
3. All 5 new workflows have entries in CATALOGUE.json workflows array and spec.md is in templates array
4. CATALOGUE.json `total` is 270 and counts are: commands=79, references=48, workflows=80, templates=32, agents=31
5. `node --test tests/command-count-sync.test.cjs` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-catalogue-sync/08-01-SUMMARY.md` following the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.

The summary must record:
- Which 20 entries were added (file paths only, grouped by category)
- Final counts block values written to CATALOGUE.json
- Whether docs/ARCHITECTURE.md was modified (and what was changed) or left unchanged (D-06 gate passed)
- The output of `node --test tests/command-count-sync.test.cjs` confirming pass
</output>
