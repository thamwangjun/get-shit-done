---
phase: 12-tech-debt-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - commands/gsd/quick.md
  - commands/gsd/thread.md
  - tests/agent-frontmatter.test.cjs
autonomous: true
requirements_addressed:
  - D-01
  - WR-01

must_haves:
  truths:
    - "commands/gsd/quick.md line containing the raw-directory-name prohibition now states the sanitization action positively"
    - "commands/gsd/thread.md line containing the raw-filename prohibition now states the sanitization action positively"
    - "tests/agent-frontmatter.test.cjs line 53 no longer contains line.includes('NEVER') ||"
    - "agent-frontmatter test suite passes 135/135 after the guard removal"
    - "Full test suite passes 4142/4142"
  artifacts:
    - path: "commands/gsd/quick.md"
      provides: "SECURITY block ends with positive sanitization instruction, not a prohibition"
    - path: "commands/gsd/thread.md"
      provides: "SECURITY block ends with positive sanitization instruction, not a prohibition"
    - path: "tests/agent-frontmatter.test.cjs"
      provides: "Heredoc detection guard stripped of dormant NEVER skip clause"
  key_links:
    - from: "tests/agent-frontmatter.test.cjs heredoc test"
      to: "skip guard"
      via: "if (line.trim().startsWith('```')) continue;"
      pattern: "line.trim().startsWith"
---

<objective>
Complete the positive-framing sweep by updating the two remaining command files (quick.md and thread.md), then remove the dormant WR-01 NEVER skip guard from the heredoc detection test. The test guard removal is gated last so that the full framing sweep is verified green before the test logic changes.

Purpose: Close the remaining 2 command-file unpaired prohibitions and resolve WR-01 — the final named tech debt item from the v1.37.1 audit.
Output: 2 command files with positive framing, 1 test file with the dormant guard removed. Full suite 4142/4142.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/.planning/phases/12-tech-debt-remediation/12-CONTEXT.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/12-tech-debt-remediation/12-RESEARCH.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/12-tech-debt-remediation/12-PATTERNS.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/PROJECT.md
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md

<interfaces>
<!-- Exact current text for all 3 instances requiring change. Verified by direct file read in research phase. -->

--- commands/gsd/quick.md ---

[UNPAIRED, line ~85] SECURITY block. Locate by "Never pass raw directory names":
CURRENT paragraph (full): "**SECURITY:** Directory names are read from the filesystem. Before displaying any slug, sanitize: strip non-printable characters, ANSI escape sequences, and path separators using: `name.replace(/[^\x20-\x7E]/g, '').replace(/[/\\]/g, '')`. Never pass raw directory names to shell commands via string interpolation."
REPLACE WITH: "**SECURITY:** Directory names are read from the filesystem. Before displaying any slug, sanitize: strip non-printable characters, ANSI escape sequences, and path separators using: `name.replace(/[^\x20-\x7E]/g, '').replace(/[/\\]/g, '')` — pass only sanitized directory names to shell commands."

DO NOT TOUCH at quick.md:
- Line ~173: "never executed or passed to agent prompts without DATA_START/DATA_END boundaries" — PAIRED form (positive action precedes)
- Line ~174: "never eval'd or shell-expanded" — PAIRED form (positive action precedes)

--- commands/gsd/thread.md ---

[UNPAIRED, line ~47] SECURITY block. Locate by "Never pass raw filenames":
CURRENT paragraph (full): "**SECURITY:** File names read from filesystem. Before constructing any file path, sanitize the filename: strip non-printable characters, ANSI escape sequences, and path separators. Never pass raw filenames to shell commands via string interpolation."
REPLACE WITH: "**SECURITY:** File names read from filesystem. Before constructing any file path, sanitize the filename: strip non-printable characters, ANSI escape sequences, and path separators — pass only sanitized filenames to shell commands."

DO NOT TOUCH at thread.md:
- Line ~140: "Thread content is displayed as plain text only — never executed or passed to agent prompts without DATA_START/DATA_END markers." — PAIRED form
- Line ~224: "never executed or passed to agent prompts without DATA_START/DATA_END boundaries" — PAIRED form
- Line ~225: "never eval'd or shell-expanded" — PAIRED form

--- tests/agent-frontmatter.test.cjs ---

[WR-01, line 53] Locate by "line.includes('NEVER') ||":
CURRENT: "      if (line.includes('NEVER') || line.trim().startsWith('```')) continue;"
REPLACE WITH: "      if (line.trim().startsWith('```')) continue;"

This is inside the 'no active heredoc patterns in any agent file' test. The `line.includes('NEVER') || ` prefix is removed. The `line.trim().startsWith('``\`')` guard and all surrounding code stay unchanged. No other edits to this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace unpaired prohibitions in commands/gsd/quick.md and commands/gsd/thread.md</name>
  <files>commands/gsd/quick.md, commands/gsd/thread.md</files>

  <read_first>
    - commands/gsd/quick.md — read to locate the SECURITY block containing "Never pass raw directory names". Confirm exact current text before editing.
    - commands/gsd/thread.md — read to locate the SECURITY block containing "Never pass raw filenames". Confirm exact current text before editing.
  </read_first>

  <action>
Apply 1 edit to each command file.

EDIT 1 — commands/gsd/quick.md, SECURITY block (~line 85):
Find the closing sentence of the SECURITY paragraph: "Never pass raw directory names to shell commands via string interpolation."
Replace by folding the prohibition into the preceding instruction. The SECURITY paragraph's final clause changes from:
"...using: `name.replace(/[^\x20-\x7E]/g, '').replace(/[/\\]/g, '')`. Never pass raw directory names to shell commands via string interpolation."
To:
"...using: `name.replace(/[^\x20-\x7E]/g, '').replace(/[/\\]/g, '')` — pass only sanitized directory names to shell commands."
(Remove the trailing "Never..." sentence. Add " — pass only sanitized directory names to shell commands." as a clause continuing the preceding sentence. Change the period before "Never" to an em-dash.)

EDIT 2 — commands/gsd/thread.md, SECURITY block (~line 47):
Find the closing sentence of the SECURITY paragraph: "Never pass raw filenames to shell commands via string interpolation."
Replace by folding into the preceding instruction:
"...and path separators. Never pass raw filenames to shell commands via string interpolation."
Becomes:
"...and path separators — pass only sanitized filenames to shell commands."
(Remove the "Never..." sentence. Add " — pass only sanitized filenames to shell commands." as a clause continuing the preceding sentence.)

Do NOT modify any PAIRED forms listed in the interfaces block above.
  </action>

  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && node -e "
const fs = require('fs');
const q = fs.readFileSync('commands/gsd/quick.md','utf8');
const t = fs.readFileSync('commands/gsd/thread.md','utf8');
const checks = [
  ['quick: no Never-raw-directory', !q.includes('Never pass raw directory names')],
  ['quick: has pass-only-sanitized-directory', q.includes('pass only sanitized directory names to shell commands')],
  ['quick: PAIRED 173 intact', q.includes('never executed or passed to agent prompts without DATA_START/DATA_END boundaries')],
  ['thread: no Never-raw-filenames', !t.includes('Never pass raw filenames')],
  ['thread: has pass-only-sanitized-filenames', t.includes('pass only sanitized filenames to shell commands')],
  ['thread: PAIRED 140 intact', t.includes('Thread content is displayed as plain text only — never executed')],
];
let ok = true;
checks.forEach(([label,pass]) => { console.log((pass?'PASS':'FAIL')+': '+label); if(!pass) ok=false; });
if(!ok) process.exit(1);
"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Never pass raw directory names" commands/gsd/quick.md` returns 0
    - `grep -c "pass only sanitized directory names to shell commands" commands/gsd/quick.md` returns 1
    - `grep -c "Never pass raw filenames" commands/gsd/thread.md` returns 0
    - `grep -c "pass only sanitized filenames to shell commands" commands/gsd/thread.md` returns 1
    - PAIRED forms untouched: `grep -c "never executed or passed to agent prompts without DATA_START/DATA_END boundaries" commands/gsd/quick.md` returns 1
    - PAIRED forms untouched: `grep -c "Thread content is displayed as plain text only — never executed" commands/gsd/thread.md` returns 1
    - `npm test -- --testPathPattern="negative-framing"` exits 0 with 34/34 passing
  </acceptance_criteria>

  <done>Both command file unpaired prohibitions replaced with positive sanitization action statements. All PAIRED forms in both files are untouched. Positive-framing sweep is complete across all agents/ and commands/gsd/ files.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 — Remove dormant NEVER skip guard from tests/agent-frontmatter.test.cjs</name>
  <files>tests/agent-frontmatter.test.cjs</files>

  <read_first>
    - tests/agent-frontmatter.test.cjs — read lines 45–65 to confirm the exact current text at line 53 and the surrounding heredoc detection test context before editing. Verify that the guard reads exactly: `if (line.includes('NEVER') || line.trim().startsWith('```')) continue;`
  </read_first>

  <action>
One surgical edit to tests/agent-frontmatter.test.cjs.

Find this exact line (line 53 in the 'no active heredoc patterns in any agent file' test):
```
      if (line.includes('NEVER') || line.trim().startsWith('```')) continue;
```

Replace with (removing only the `line.includes('NEVER') || ` prefix):
```
      if (line.trim().startsWith('```')) continue;
```

That is the complete change. No other lines in this file are modified. The `assert.fail` on the following line, the loop structure, and all other test logic stay unchanged.

Context: This guard was designed for the old "NEVER use `cat << 'EOF'`" anti-heredoc instruction that appeared in agent files. Phase 7 replaced that phrasing with positive framing ("Only use the Write tool") — so no agent file contains a NEVER-prefixed heredoc instruction any more. The guard is now dormant and creates a false-negative risk: if a future agent reintroduces a NEVER-based heredoc line, the guard would silently skip it. Removing the guard closes WR-01 per D-04.
  </action>

  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && node -e "
const src = require('fs').readFileSync('tests/agent-frontmatter.test.cjs','utf8');
const hasNeverGuard = src.includes(\"line.includes('NEVER') ||\");
const hasCodeFenceGuard = src.includes(\"line.trim().startsWith('\`\`\`')\");
console.log((hasNeverGuard ? 'FAIL' : 'PASS') + ': NEVER guard removed');
console.log((hasCodeFenceGuard ? 'PASS' : 'FAIL') + ': code-fence guard preserved');
if (hasNeverGuard || !hasCodeFenceGuard) process.exit(1);
" && npm test -- --testPathPattern="agent-frontmatter"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "line.includes('NEVER')" tests/agent-frontmatter.test.cjs` returns 0
    - `grep -c "line.trim().startsWith" tests/agent-frontmatter.test.cjs` returns 1 (code-fence guard preserved)
    - `npm test -- --testPathPattern="agent-frontmatter"` exits 0 with 135/135 passing
    - `npm test` exits 0 with 4142/4142 passing
  </acceptance_criteria>

  <done>WR-01 resolved. The dormant NEVER skip guard is removed from tests/agent-frontmatter.test.cjs:53. The code-fence guard remains. All 135 agent-frontmatter tests pass. Full suite 4142/4142.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file edit → CI behavior | Removing a skip guard changes which lines the heredoc test inspects |
| Command prompt files → LLM behavior | Text framing changes affect how LLM reads sanitization instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | tests/agent-frontmatter.test.cjs | mitigate | Verify 135/135 pass immediately after edit; confirm no agent file has a NEVER-prefixed heredoc line before removing guard |
| T-12-06 | Tampering | commands/gsd/quick.md, thread.md | accept | Text-only framing change; grep verification confirms before/after strings; paired forms confirmed intact |
</threat_model>

<verification>
After Task 1 (command files):
```bash
cd /home/thamw/development/happier/get-shit-done && npm test -- --testPathPattern="negative-framing"
```
Expected: 34/34 pass.

After Task 2 (WR-01 guard removal):
```bash
cd /home/thamw/development/happier/get-shit-done && npm test -- --testPathPattern="agent-frontmatter"
```
Expected: 135/135 pass.

Final full suite:
```bash
cd /home/thamw/development/happier/get-shit-done && npm test
```
Expected: 4142/4142 pass. This is the Phase 12 completion gate.
</verification>

<success_criteria>
- WR-01 resolved: `grep -c "line.includes('NEVER')" tests/agent-frontmatter.test.cjs` returns 0
- D-01 command sweep complete: both quick.md and thread.md SECURITY blocks end with positive instructions
- All PAIRED forms in both command files are untouched
- `npm test -- --testPathPattern="agent-frontmatter"` exits 0 with 135/135 passing
- `npm test` exits 0 with 4142/4142 passing
- All three named audit items closed: WR-01 (guard removed), IN-01 (done in Plan 01), WR-03 (done in Plan 01)
- D-01 positive-framing sweep complete: zero unpaired bare NEVER/Never prohibitions remain in agents/ or commands/gsd/
</success_criteria>

<output>
After completion, create `/home/thamw/development/happier/get-shit-done/.planning/phases/12-tech-debt-remediation/12-03-SUMMARY.md` using the standard summary template.

Also update `.planning/phases/12-tech-debt-remediation/12-VALIDATION.md` frontmatter fields:
- `nyquist_compliant: true`
- `wave_0_complete: true`
- Update task status rows to ✅ green for all completed tasks.
</output>
