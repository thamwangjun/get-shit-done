---
phase: 66-citation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/add-backlog.md
  - get-shit-done/workflows/ai-integration-phase.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/discuss-phase/modes/advisor.md
  - get-shit-done/workflows/discuss-phase/modes/chain.md
  - get-shit-done/workflows/discuss-phase/templates/context.md
  - get-shit-done/workflows/execute-phase/steps/codebase-drift-gate.md
  - get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/help/modes/full.md
  - get-shit-done/workflows/ingest-docs.md
  - get-shit-done/workflows/new-milestone.md
  - get-shit-done/workflows/new-project.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/quick.md
  - get-shit-done/workflows/reapply-patches.md
  - get-shit-done/workflows/settings-integrations.md
  - get-shit-done/workflows/settings.md
  - get-shit-done/workflows/update.md
  - get-shit-done/workflows/verify-phase.md
autonomous: true
requirements: [CITE-06, CITE-07, CITE-08, CITE-09]

must_haves:
  truths:
    - "D-01: Per-file executor edits — no regex script is created"
    - "D-08: YAML frontmatter blocks (between leading --- delimiters) are untouched"
    - "D-09: Fenced code blocks (triple-backtick) are untouched"
    - "D-10: Allowlisted tokens (#1, #2, #45, #123, hex colors, heading markers) are preserved"
    - "All 20 get-shit-done/workflows/ files (including subdir files) are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences"
    - "execute-plan.md line 111 dense multi-citation paragraph is cleaned while preserving every technical instruction it carries"
    - "Cleaned prose contains no double spaces, empty parentheses, or dangling connectors at sentence boundaries"
    - "Guard test reports zero ✖ lines for get-shit-done/workflows/ files after cleanup"
  artifacts:
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Cleaned workflow file — dense citation paragraph at line 111 rewritten naturally"
    - path: "get-shit-done/workflows/ingest-docs.md"
      provides: "Cleaned workflow file"
    - path: "get-shit-done/workflows/quick.md"
      provides: "Cleaned workflow file"
    - path: "get-shit-done/workflows/discuss-phase/modes/advisor.md"
      provides: "Cleaned workflow sub-file"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "get-shit-done/workflows/**/*.md"
      via: "directory scan + per-file ✖ enumeration"
      pattern: "get-shit-done/workflows/.*\\.md"
---

<objective>
Remove every issue/PR citation from the 20 failing files in `get-shit-done/workflows/` (including subdirectory files under `discuss-phase/`, `execute-phase/steps/`, and `help/modes/`) and repair the surrounding prose. The dense multi-citation paragraph at `execute-plan.md` line 111 is the highest-risk single edit in this plan and is called out explicitly.

Purpose: Close CITE-06 / CITE-07 / CITE-08 / CITE-09 for the `get-shit-done/workflows/` directory so the Phase 65 guard test goes GREEN for this slice.

Output: 20 modified `.md` files committed atomically. Guard test reports zero ✖ lines for `get-shit-done/workflows/` paths.
</objective>

<context>
!`cat .planning/phases/66-citation-cleanup/66-CONTEXT.md`
!`cat .planning/REQUIREMENTS.md`

# Phase 65 guard test (success gate — reference for detection rules and allowlist)
@tests/no-issue-citations.test.cjs

# Phase 64 findings (citation inventory)
@.planning/phases/64-citation-pattern-exploration/64-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean citations from 13 workflows/ top-level files</name>
  <files>get-shit-done/workflows/add-backlog.md, get-shit-done/workflows/ai-integration-phase.md, get-shit-done/workflows/discuss-phase.md, get-shit-done/workflows/ingest-docs.md, get-shit-done/workflows/new-milestone.md, get-shit-done/workflows/new-project.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/quick.md, get-shit-done/workflows/reapply-patches.md, get-shit-done/workflows/settings-integrations.md, get-shit-done/workflows/settings.md, get-shit-done/workflows/update.md, get-shit-done/workflows/verify-phase.md</files>
  <read_first>
    - get-shit-done/workflows/add-backlog.md
    - get-shit-done/workflows/ai-integration-phase.md
    - get-shit-done/workflows/discuss-phase.md
    - get-shit-done/workflows/ingest-docs.md
    - get-shit-done/workflows/new-milestone.md
    - get-shit-done/workflows/new-project.md
    - get-shit-done/workflows/plan-phase.md
    - get-shit-done/workflows/quick.md
    - get-shit-done/workflows/reapply-patches.md
    - get-shit-done/workflows/settings-integrations.md
    - get-shit-done/workflows/settings.md
    - get-shit-done/workflows/update.md
    - get-shit-done/workflows/verify-phase.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    For each of the 13 top-level files in `<files>`, work file-by-file using Read and Edit (per D-01: per-file agent edits, no regex script per D-02):

    1. Read the file once. Identify the YAML frontmatter range (first `---` line through the matching closing `---`) and every fenced code block (lines from an opening triple-backtick fence through its closing fence). Skip citation tokens inside those zones per D-08 and D-09.

    2. Outside those zones, remove every citation token of these forms: bare `#NNN` (3+ digits), parenthetical `(#NNN)`, and inline `feat-NNNN`. Preserve allowlisted tokens `#1`, `#2`, `#45`, `#123`, hex color literals (e.g. `#e8c170`), and markdown heading markers (`##`, `###`, etc.) per D-10.

    3. Apply contextual prose repair at the exact point of each removed citation:
       - If the citation is the only content of its clause (e.g. `(see #1486)`, `— #2015`, parenthetical-only `(#2924)` after a complete sentence), drop the entire clause including its leading connector (`—`, `,`, `see`, `by`, `in`, `from`, `per`) and any wrapping parentheses per D-06.
       - If the clause carries independent meaning (e.g. "prevents destructive HEAD-on-master recovery (#2924)"), keep the prose and strip only the citation token plus immediately surrounding parentheses/whitespace per D-07.
       - After every removal, collapse any resulting double space to a single space, remove any space directly before a comma/period/semicolon/colon, and delete any empty parentheses `()` left behind.

    4. Do NOT touch any line inside the YAML frontmatter block or inside any fenced code block. Modifying frontmatter risks breaking `agent-frontmatter.test.cjs`; modifying code-fence content changes example snippets the guard test deliberately exempts.

    5. After all 13 files in this task are edited, run the guard test once to confirm zero `✖` lines reference any of these 13 paths. Use this exact command:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep -E 'get-shit-done/workflows/(add-backlog|ai-integration-phase|discuss-phase|ingest-docs|new-milestone|new-project|plan-phase|quick|reapply-patches|settings-integrations|settings|update|verify-phase)\.md' || echo "CLEAN: workflows/ top-level"`
       Expected stdout: `CLEAN: workflows/ top-level`. If any `✖` line appears, re-read the offending file and apply the same removal + repair procedure.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep -E 'get-shit-done/workflows/(add-backlog|ai-integration-phase|discuss-phase|ingest-docs|new-milestone|new-project|plan-phase|quick|reapply-patches|settings-integrations|settings|update|verify-phase)\.md' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0`.
    - `git diff --stat` shows changes to all 13 files listed in `<files>`.
    - For each modified file, the YAML frontmatter block (line 1 through second `---`) is byte-identical to its pre-edit version.
  </acceptance_criteria>
  <done>
    All 13 top-level workflows files are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences. Allowlisted tokens preserved. Surrounding prose flows naturally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean citations from 6 workflows/ subdirectory files</name>
  <files>get-shit-done/workflows/discuss-phase/modes/advisor.md, get-shit-done/workflows/discuss-phase/modes/chain.md, get-shit-done/workflows/discuss-phase/templates/context.md, get-shit-done/workflows/execute-phase/steps/codebase-drift-gate.md, get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md, get-shit-done/workflows/help/modes/full.md</files>
  <read_first>
    - get-shit-done/workflows/discuss-phase/modes/advisor.md
    - get-shit-done/workflows/discuss-phase/modes/chain.md
    - get-shit-done/workflows/discuss-phase/templates/context.md
    - get-shit-done/workflows/execute-phase/steps/codebase-drift-gate.md
    - get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md
    - get-shit-done/workflows/help/modes/full.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    Apply the same per-file procedure as Task 1 to each of the 6 subdirectory files in `<files>` (per D-01 / D-08 / D-09 / D-10):

    1. Read each file in full once. Identify frontmatter and code-fence zones; skip citations inside them.
    2. Outside those zones, remove bare `#NNN`, parenthetical `(#NNN)`, and inline `feat-NNNN` citations. Preserve `#1`, `#2`, `#45`, `#123`, hex colors, and heading markers.
    3. Repair prose per D-05/D-06/D-07: drop citation-only clauses with their leading connector and any wrapping parentheses; keep clauses with independent meaning but strip the citation token plus surrounding whitespace/parentheses. Collapse double spaces, remove space-before-punctuation, delete empty `()`.
    4. After all 6 files in this task are edited, run:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep -E 'get-shit-done/workflows/(discuss-phase|execute-phase|help)/' || echo "CLEAN: workflows/ subdirs"`
       Expected stdout: `CLEAN: workflows/ subdirs`. Re-edit until clean.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep -E 'get-shit-done/workflows/(discuss-phase|execute-phase|help)/' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0`.
    - `git diff --stat` shows changes to all 6 files listed in `<files>`.
    - YAML frontmatter blocks in each modified file are byte-identical to their pre-edit versions.
  </acceptance_criteria>
  <done>
    All 6 workflows subdirectory files are clean of citations outside frontmatter and code fences; prose reads naturally; frontmatter untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean execute-plan.md including the dense multi-citation paragraph near line 111, then verify whole workflows/ directory</name>
  <files>get-shit-done/workflows/execute-plan.md</files>
  <read_first>
    - get-shit-done/workflows/execute-plan.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    `execute-plan.md` is the most citation-dense file in the workflows/ slice and is called out in 66-CONTEXT.md `<specifics>` (line 111 is a dense multi-citation paragraph). Treat this task as a single file edit with extra care:

    1. Read the entire file once to see all citations in context — there are multiple `(#NNNN)` references threaded through prose that describes runtime/worktree behavior, atomic-close-out invariants, and resume logic. Identify the YAML frontmatter range and every fenced code block; skip citations inside those zones per D-08 / D-09.

    2. For the dense paragraph at and near line 111: read at least 20 lines of surrounding context (line 95 through line 140 or beyond) before editing so the rewrite preserves every technical instruction (atomic close-out ordering, pre-commit hook handling, worktree-branch-check rules, EXPECTED_BASE checks, etc.). Per D-07, the rationale text describing what each rule does has independent meaning and must be kept; only the citation tokens themselves are removed.

    3. Outside frontmatter and code fences, remove every `#NNN` (3+ digits), parenthetical `(#NNN)`, and inline `feat-NNNN` citation. Preserve allowlisted tokens `#1`, `#2`, `#45`, `#123`, hex colors, and heading markers per D-10.

    4. Apply contextual prose repair per D-05/D-06/D-07 at each removal:
       - Drop entire clauses that exist only to cite (e.g. `(see #1486)`, `— #2015`, `(per #2924)` standing alone) including their leading connector and any wrapping parentheses per D-06.
       - For citations attached to substantive prose (e.g. "prevents destructive HEAD-on-master self-recovery path (#2924)"), strip only the parenthetical citation plus immediately surrounding whitespace per D-07 — the explanatory clause stays.
       - Collapse any resulting double space to a single space, remove any space directly before a comma/period/semicolon/colon, delete any empty parentheses `()`.

    5. Do NOT touch lines inside the YAML frontmatter or inside any fenced code block. Code-fence content includes shell snippets and example XML — leave those exactly as-is.

    6. After editing `execute-plan.md`, run the directory-wide guard test to confirm the entire `get-shit-done/workflows/` slice (this file plus the files cleaned in Tasks 1 and 2) is clean:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/workflows/" || echo "CLEAN: workflows/ (all)"`
       Expected stdout: `CLEAN: workflows/ (all)`. If any `✖` line referencing `get-shit-done/workflows/` appears, re-read that file and re-edit until the slice is clean.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/workflows/" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0` (whole workflows/ slice clean, not just execute-plan.md).
    - `git diff --stat get-shit-done/workflows/execute-plan.md` shows that file was modified.
    - `grep -nE '(^|[^&#])#[0-9]{3,}' get-shit-done/workflows/execute-plan.md | grep -vE '#(1|2|45|123)([^0-9]|$)' | wc -l | tr -d ' '` outputs exactly `0` (no unallowlisted bare `#NNN` citations of 3+ digits remain).
    - `grep -nE 'feat-[0-9]{3,}' get-shit-done/workflows/execute-plan.md | wc -l | tr -d ' '` outputs exactly `0`.
    - The YAML frontmatter block of `execute-plan.md` is byte-identical to its pre-edit version.
  </acceptance_criteria>
  <done>
    execute-plan.md is clean of all citations outside frontmatter and code fences. The dense paragraph near line 111 preserves every technical instruction with only citation tokens removed. The whole get-shit-done/workflows/ slice (20 files) is clean per the guard test.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/workflows/"` returns no matches.
- `node --test tests/agent-frontmatter.test.cjs` exits 0.
</verification>

<success_criteria>
- 20 workflows files modified across three tasks; all citations outside frontmatter/code-fences removed (CITE-06, CITE-07).
- Sentences read naturally (CITE-08); execute-plan.md dense paragraph preserves technical content.
- YAML frontmatter blocks are byte-identical to pre-edit state (CITE-09).
- Guard test produces zero ✖ lines for get-shit-done/workflows/ paths.
</success_criteria>

<output>
Create `.planning/phases/66-citation-cleanup/66-02-SUMMARY.md` when done, citing the post-edit guard test output for the workflows/ slice and noting the execute-plan.md line-111 handling specifically.
</output>
