---
phase: 67-full-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CITE-10, CITE-11, CITE-12]
must_haves:
  truths:
    - "tests/no-issue-citations.test.cjs passes GREEN — zero citations in scoped dirs (CITE-10)"
    - "Every non-allowlisted #NNN hit from raw grep is reconciled against the guard test allowlist; no unexplained citation survives (CITE-11)"
    - "npm test reports 0 failures across the whole suite (CITE-12)"
    - "negative-framing-scan stays at the v2.1.0-f baseline of 99/99 subtests"
    - "agent-frontmatter, step-numbering-scan, cross-file-step-refs remain GREEN"
    - "D-04: Any failure surfaced by npm test triggers the documented remediation decision point (fix-in-place vs route to debug/replan) before the phase is declared complete"
  artifacts:
    - path: "tests/no-issue-citations.test.cjs"
      provides: "Issue-citation guard test (must pass GREEN)"
    - path: "/tmp/gsd-test-output.txt"
      provides: "Captured full-suite output for inspection without re-running"
  key_links:
    - from: "npm test"
      to: "tests/no-issue-citations.test.cjs"
      via: "node --test runner over tests/*.test.cjs"
      pattern: "no-issue-citations"
    - from: "tests/no-issue-citations.test.cjs"
      to: "scoped prompt-content dirs"
      via: "scanContent allowlist (hex/placeholder/heading/frontmatter/code-fence)"
      pattern: "PLACEHOLDER_DIGITS|code fence|frontmatter"
---

<objective>
Final verification gate for the v2.1.0-g Citation Cleanup milestone. Confirm the
Phase 66 cleanup landed cleanly: the issue-citation guard test is GREEN, the
full `npm test` suite passes with zero failures, and the pre-existing content
scanners (negative-framing, step-numbering, cross-file-step-refs,
agent-frontmatter) are unaffected.

Purpose: Close out CITE-10, CITE-11, CITE-12 — the three remaining verification
requirements of the milestone. This phase VERIFIES; it does not add features. The
only code changes permitted are remediation of failures the suite surfaces.

Output: A confirmed-GREEN test suite and a recorded verification result. No new
source files expected beyond any remediation forced by a real failure.
</objective>

<context>
!`cat .planning/phases/67-full-verification/67-CONTEXT.md`
!`cat .planning/REQUIREMENTS.md`

Phase 66 cleanup being verified (read only if a failure points at one of these slices):
- .planning/phases/66-citation-cleanup/66-01-SUMMARY.md
- .planning/phases/66-citation-cleanup/66-02-SUMMARY.md
- .planning/phases/66-citation-cleanup/66-03-SUMMARY.md
- .planning/phases/66-citation-cleanup/66-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm guard test GREEN and reconcile the CITE-11 raw grep (CITE-10, CITE-11)</name>
  <read_first>
    - .planning/phases/67-full-verification/67-CONTEXT.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-04-SUMMARY.md (documents the code-fence #NNN hits that survive raw grep but are guard-exempt)
  </read_first>
  <action>
    Run the guard test in isolation: `node --test tests/no-issue-citations.test.cjs 2>&1 | tee /tmp/gsd-guard-output.txt`.
    Confirm it passes GREEN with the established baseline of 326/326 subtests (per D-01) and 0 failures.

    For CITE-11, encode the allowlist rule via the guard test rather than a bare grep
    pass/fail (per Claude's Discretion in CONTEXT.md). Justification: the guard test's
    scanContent applies the full allowlist (hex lookbehind, PLACEHOLDER_DIGITS, heading
    markers, frontmatter exclusion, code-fence exclusion); a naive `grep -rEn '#[0-9]+'`
    over the scoped dirs DELIBERATELY over-matches — 66-04-SUMMARY confirms 4 surviving
    hits inside bash code fences in get-shit-done/references/worktree-path-safety.md that
    are legitimately exempt. A raw-grep == 0 gate would therefore fail spuriously.

    Run the raw grep as an INFORMATIONAL reconciliation step, not a hard gate:
    `command grep -rEn '#[0-9]+' commands/ get-shit-done/workflows/ agents/ get-shit-done/references/ get-shit-done/templates/ | tee /tmp/gsd-cite11-grep.txt`.
    For every line it returns, classify it as one of: hex color, illustrative placeholder
    (#1/#2/#45/#123), markdown heading marker, frontmatter color field, or code-fence
    content. If any hit is a real issue/PR citation that is NOT one of those allowlisted
    categories, that is a genuine CITE-11 failure → escalate to Task 2's remediation
    decision point. If every hit maps to an allowlisted category, CITE-11 is satisfied.
  </action>
  <acceptance_criteria>
    - `node --test tests/no-issue-citations.test.cjs` exits 0 with `pass 326` and `fail 0` in /tmp/gsd-guard-output.txt.
    - `command grep -rEn '#[0-9]+'` over the 5 scoped dirs produces only hits classifiable as hex color, placeholder (#1/#2/#45/#123), heading marker, frontmatter color, or code-fence content — recorded with classification in the SUMMARY.
    - Zero unexplained (non-allowlisted) #NNN citation hits remain.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs</automated>
  </verify>
  <done>Guard test GREEN at 326/326; every raw-grep hit reconciled to an allowlist category; no real citation survives.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite, assert baselines, and apply the remediation decision point (CITE-12)</name>
  <read_first>
    - ./CLAUDE.md (canonical `npm test 2>&1 | tee /tmp/gsd-test-output.txt` invocation; Node >=22; run once then read the file)
    - .planning/phases/67-full-verification/67-CONTEXT.md (D-02 baseline; failure-remediation discretion)
    - tests/negative-framing-scan.test.cjs (only if its count drifts off baseline)
  </read_first>
  <action>
    Run the full suite ONCE and capture output: `npm test 2>&1 | tee /tmp/gsd-test-output.txt`.
    Inspect the captured file rather than re-running.

    Assert the suite-level result and the named scanner baselines:
    - Overall: `fail 0` (CITE-12 — zero failures across the whole suite).
    - negative-framing-scan: unchanged at the v2.1.0-f baseline of 99/99 subtests (per D-02
      and success criterion #3) — hard-assert this exact count, since any drift here is a
      real regression signal, not noise.
    - agent-frontmatter, step-numbering-scan, cross-file-step-refs: present in the run and
      reporting 0 failures (success criterion #2).

    DOCUMENTED REMEDIATION DECISION POINT (per CONTEXT.md Claude's Discretion / D-04):
    If `npm test` reports any failure, do NOT silently fix-forward at scale. Triage:
      (a) The failure is a trivial, localized assertion drift directly caused by a Phase 66
          citation removal (e.g. a content test still asserting an old cited string) →
          fix-in-place with a minimal, fix-forward assertion re-point (per REQUIREMENTS.md
          "Out of Scope": weakening/removing tests is prohibited; re-point minimally), then
          re-run the affected test file to confirm GREEN and re-run the full suite once more.
      (b) The failure is non-trivial, spans multiple files, or its root cause is unclear →
          STOP. Record the failure (file, test name, assertion, captured output) as a
          blocker in the SUMMARY and recommend routing to /gsd-debug or a follow-up replan
          rather than improvising a fix inside this verification phase.
    Apply at most a small number of minimal (a)-class fixes; anything beyond that is a
    (b)-class escalation.
  </action>
  <acceptance_criteria>
    - /tmp/gsd-test-output.txt shows `fail 0` for the overall run (CITE-12).
    - negative-framing-scan reports exactly 99/99 subtests passing (unchanged from v2.1.0-f baseline).
    - agent-frontmatter, step-numbering-scan, and cross-file-step-refs each report 0 failures.
    - Any failure encountered is either minimally fix-forwarded (class a, with the affected file re-run GREEN and the full suite re-run to `fail 0`) or escalated as a documented blocker (class b) — never silently skipped.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt; command grep -E "tests [0-9]+|fail [0-9]+" /tmp/gsd-test-output.txt | tail -5</automated>
  </verify>
  <done>Full suite at `fail 0`; negative-framing at 99/99; named scanners GREEN; any failure remediated per the documented decision point.</done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs` → 326/326, fail 0 (CITE-10).
- Raw `grep -rEn '#[0-9]+'` over the 5 scoped dirs → only allowlisted/code-fence hits, each classified (CITE-11).
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` → `fail 0` overall; negative-framing 99/99; agent-frontmatter, step-numbering-scan, cross-file-step-refs all GREEN (CITE-12).
</verification>

<success_criteria>
1. Guard test GREEN (zero citations detected in scoped dirs).
2. Full `npm test` reports 0 failures.
3. negative-framing scanner unchanged at the v2.1.0-f baseline (99/99).
</success_criteria>

<output>
Create `.planning/phases/67-full-verification/67-01-SUMMARY.md` when done, recording:
guard test result, the CITE-11 grep reconciliation table (hit → allowlist category),
the full-suite `fail` count, the negative-framing count, and any remediation
decision taken (class a fix or class b escalation).
</output>
