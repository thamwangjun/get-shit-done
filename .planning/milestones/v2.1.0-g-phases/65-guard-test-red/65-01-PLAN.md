---
phase: 65-guard-test-red
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/no-issue-citations.test.cjs
autonomous: true
requirements:
  - CITE-03
  - CITE-04
  - CITE-05
must_haves:
  truths:
    - "tests/no-issue-citations.test.cjs exists at the project root tests/ directory and is picked up by `npm test` without manual registration"
    - "Running `node --test tests/no-issue-citations.test.cjs` fails RED — the failure output enumerates every offending file:line and matched text (not just a count)"
    - "The corpus subtest failure message for each offending file lists `file:line #NNN (category)` lines plus an indented context line per D-06"
    - "The final failure message line reads exactly: To add an allowlist exemption: add the digit to PLACEHOLDER_DIGITS (per D-06)"
    - "Hex color codes (e.g., `#e8c170`, `#22c55e`, `#A78BFA` including frontmatter `color: '#A78BFA'`) do not produce test failures (per D-11 lookbehind)"
    - "Illustrative placeholders #1, #2, #45, #123 do not produce test failures (per D-04 PLACEHOLDER_DIGITS set)"
    - "Markdown heading markers (`## Heading`, `### Heading`) do not produce test failures"
    - "Agent YAML frontmatter blocks (lines between leading `---` ... `---` when frontmatter starts on line 1) are skipped (per D-09)"
    - "Fenced code blocks (triple-backtick fences) are skipped (per D-10)"
    - "The test detects the canonical `feat-form` hit `feat-3347` at get-shit-done/references/planner-graphify-auto-update.md:62 (per D-07 + Phase 64 FINDINGS)"
    - "The test detects the canonical 3-digit real citation `#686` at get-shit-done/workflows/discuss-phase/modes/chain.md:57 (per Phase 64 FINDINGS specifics)"
    - "Effective RED violation count is approximately 97 (103 raw hits from 64-FINDINGS minus ~6 placeholder hits in PLACEHOLDER_DIGITS)"
    - "D-01: detection logic is inline — the test file does NOT `require()` scripts/scan-citations.cjs"
  artifacts:
    - path: "tests/no-issue-citations.test.cjs"
      provides: "Permanent citation regression guard with inline detection"
      contains: "PLACEHOLDER_DIGITS"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "5 SCAN_DIRS (commands/, get-shit-done/workflows/, agents/, get-shit-done/references/, get-shit-done/templates/)"
      via: "collectMarkdownFiles recursive .md traversal"
      pattern: "SCAN_DIRS"
    - from: "tests/no-issue-citations.test.cjs"
      to: "PLACEHOLDER_DIGITS allowlist"
      via: "parseInt match → Set.has() guard before pushing hit"
      pattern: "PLACEHOLDER_DIGITS\\.has"
---

<objective>
Create `tests/no-issue-citations.test.cjs` — a permanent, self-contained corpus
guard that detects every citation pattern from Phase 64 FINDINGS (`#NNN` inline,
`(#NNN)` parenthetical, `feat-NNNN`) across the 5 scoped prompt-content dirs and
fails RED, enumerating each offending file:line and matched text.

Purpose: Establish a permanent regression guard for the v2.1.0-g milestone — the
test goes GREEN only after Phase 66 cleans up citations, and any upstream merge
that reintroduces citations breaks the suite. This implements CITE-03 (test
exists, runs under `npm test`), CITE-04 (fails RED enumerating violations), and
CITE-05 (allowlist exempts hex colors, illustrative placeholders, heading
markers, and frontmatter blocks).

Output: A new test file at `tests/no-issue-citations.test.cjs` (created on disk,
not edited from a template). No deletions in this plan — Plan 02 handles the
discovery artifact cleanup once the guard is proven independent.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus → test file I/O | Markdown files in the 5 SCAN_DIRS are read by Node.js `fs.readFileSync` in a test process; no untrusted input crosses a network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | tests/no-issue-citations.test.cjs detection regex | mitigate | Apply hex color lookbehind `(?<![0-9a-fA-F#])` per D-11 so hex tails (`#e8c170`) cannot mask real citations or be flagged as false positives |
| T-65-02 | Information Disclosure | failure message content | accept | Failure output enumerates file:line + matched text from the local corpus only — no secrets in scope |
| T-65-03 | Denial of Service | recursive directory traversal | accept | Bounded to 5 known dirs (`commands/`, `get-shit-done/workflows/`, `agents/`, `get-shit-done/references/`, `get-shit-done/templates/`); ENOENT tolerated silently as in `step-numbering-scan.test.cjs` |
| T-65-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — pure test file authoring |
</threat_model>

<context>
!`cat .planning/STATE.md`
!`cat .planning/phases/65-guard-test-red/65-CONTEXT.md`
!`cat .planning/phases/64-citation-pattern-exploration/64-FINDINGS.md`

# Structural analog — copy module-level layout, describe-per-pattern, corpus + unit subtests
@tests/step-numbering-scan.test.cjs

# Source for inlined detection regexes (INLINE_RE, FEAT_FORM_RE) and exclusion state machines
@scripts/scan-citations.cjs

# Reference for exact-value Set allowlist pattern (ALLOWED_INLINE_REFS — model for PLACEHOLDER_DIGITS)
@tests/install-eta-regression.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/no-issue-citations.test.cjs with inline detection, allowlist, exclusion state machines, and corpus subtests</name>
  <files>tests/no-issue-citations.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (primary structural analog — copy module layout, ALL_FILES initializer, describe-per-pattern, corpus subtest format using `assert.deepStrictEqual([...], [])` with enumerated failure lines)
    - scripts/scan-citations.cjs (inline these regexes verbatim: INLINE_RE = `/(?<![0-9a-fA-F#])#(\d+)\b/g`, FEAT_FORM_RE = `/\bfeat-(\d{3,})\b/g`; also inline the frontmatter state-machine logic with the line==1 guard and the `frontmatterDone` latch, and the code-fence toggle on `/^```/`)
    - tests/install-eta-regression.test.cjs (lines 40-160 — reference for exact-value Set allowlist pattern, the failure message that hints at how to extend the allowlist, and the per-file iteration that pushes only non-allowlisted hits)
    - .planning/phases/64-citation-pattern-exploration/64-FINDINGS.md (canonical detector contract — the 103-hit findings table is the expected RED truth)
    - .planning/phases/65-guard-test-red/65-CONTEXT.md (decisions D-01..D-11 govern every implementation choice)
  </read_first>
  <behavior>
    - Unit test 1 (INLINE_RE positive): scanning a synthetic line containing `#2924` produces exactly one hit with category `inline` and text `#2924`.
    - Unit test 2 (parenthetical category): scanning a synthetic line containing `(#3097)` produces exactly one hit with category `parenthetical` and text `#3097`.
    - Unit test 3 (FEAT_FORM_RE positive): scanning a synthetic line containing `feat-3347` produces exactly one hit with category `feat-form` and text `feat-3347`.
    - Unit test 4 (hex color exemption — D-11): scanning a synthetic line containing `#e8c170` (6-char hex) produces zero hits — the lookbehind `(?<![0-9a-fA-F#])` blocks the trailing `#70` / `#170` substring.
    - Unit test 5 (PLACEHOLDER_DIGITS exemption — D-04): scanning `#1`, `#2`, `#45`, `#123` produces zero hits — parsed integer is in `PLACEHOLDER_DIGITS = new Set([1, 2, 45, 123])`.
    - Unit test 6 (heading marker exemption): scanning `## Section` and `### Subsection` produces zero hits — the lookbehind plus the leading `#` count blocks heading markers.
    - Unit test 7 (frontmatter exclusion — D-09): scanning content `---\ncolor: "#A78BFA"\n---\nbody` with frontmatter on line 1 produces zero hits — the `color: "#A78BFA"` line is inside the frontmatter block.
    - Unit test 8 (code fence exclusion — D-10): scanning content `` ```\n#2924 inside fence\n``` `` produces zero hits.
    - Unit test 9 (non-line-1 `---` is not frontmatter): scanning content `body\n---\n#2924 after thematic break` produces one hit — the `---` on line 2 is a thematic break, not frontmatter open.
    - Corpus subtest (RED expectation): the per-file `assert.deepStrictEqual(hits, [])` call FAILS for each file with citations listed in 64-FINDINGS.md. The aggregate failed-subtest count is approximately 49 (the file count in 64-FINDINGS); the aggregate hit count across all files is approximately 97 (103 from 64-FINDINGS minus ~6 placeholder hits exempted by PLACEHOLDER_DIGITS — concretely: `agents/gsd-verifier.md:442` contains three `#123` hits, `get-shit-done/templates/codebase/conventions.md:232` one `#123`, `get-shit-done/workflows/verify-phase.md:343` three `#123` hits — these 7 hits are all exempted by `123` being in PLACEHOLDER_DIGITS).
  </behavior>
  <action>
    Create `tests/no-issue-citations.test.cjs` as a CommonJS Node.js test file using `node:test` and `node:assert/strict`, structured as a self-contained mirror of `tests/step-numbering-scan.test.cjs` (D-01 — no `require('scripts/scan-citations.cjs')`).

    Required module-level constants (concrete values):
    - `PROJECT_ROOT = path.join(__dirname, '..')`
    - `SCAN_DIRS = ['commands', 'get-shit-done/workflows', 'agents', 'get-shit-done/references', 'get-shit-done/templates']` (per D-08)
    - `PLACEHOLDER_DIGITS = new Set([1, 2, 45, 123])` (per D-04). Inline comment above this declaration MUST cite the Phase 64 FINDINGS provenance (per D-05) using prose like: `// Allowlist of illustrative placeholder digits per Phase 64 FINDINGS Allowlist Candidates table. #45 is included defensively (no real corpus hit) per D-05.` — do NOT cite the digit literal `#45` as an issue number.
    - `INLINE_RE = /(?<![0-9a-fA-F#])#(\d+)\b/g` (per D-11 hex lookbehind; verbatim from `scripts/scan-citations.cjs:90`)
    - `FEAT_FORM_RE = /\bfeat-(\d{3,})\b/g` (verbatim from `scripts/scan-citations.cjs:96`)

    Required helpers:
    - `collectMarkdownFiles(dir)` — copy verbatim from `scripts/scan-citations.cjs:59-77` (recursive `.md` collector, ENOENT-tolerant).
    - `scanContent(content)` — single function that walks `content.split('\n')`, maintains `inCodeBlock`, `inFrontmatter`, `frontmatterDone` state per `scripts/scan-citations.cjs:130-160` (frontmatter active only when opening `---` is on line 1; both `---` lines are skipped; thematic break on later lines is content). Returns array of `{ lineNumber, text, category }` for every match where (a) the line is not inside frontmatter, (b) not inside a code fence, (c) for inline/parenthetical hits the parsed integer is NOT in `PLACEHOLDER_DIGITS`. Category resolution: inspect `line[matchStart-1]` and `line[matchEnd]` — `(` and `)` → `parenthetical`, else → `inline`. Feat-form always emits `feat-form` category.

    File collection at module scope:
    - `const ALL_FILES = []; for (const dir of SCAN_DIRS) ALL_FILES.push(...collectMarkdownFiles(path.join(PROJECT_ROOT, dir)));`

    Test structure:
    - `describe('scanContent() — inline citation detection', ...)` with the 5 inline/parenthetical/feat-form/hex-exemption/placeholder-exemption unit tests above.
    - `describe('scanContent() — exclusion state machines', ...)` with the 4 exclusion unit tests (heading, frontmatter, code fence, late `---` thematic break).
    - `describe('corpus scan — no issue citations', ...)` iterating `ALL_FILES`. Each subtest: read file, call `scanContent`, then call `assert.deepStrictEqual(hits, [], `Citations in ${relPath}. Remove issue/PR number citations from prompt content.\n${enumerated}\nTo add an allowlist exemption: add the digit to PLACEHOLDER_DIGITS`)` where `enumerated` is the per-D-06 formatted block: each violation as `  ${file}:${lineNumber} ${text} (${category})\n      ${trimmedContextLine}`. The literal trailing line `To add an allowlist exemption: add the digit to PLACEHOLDER_DIGITS` MUST appear in the failure message (per D-06).

    File header JSDoc must state: purpose, the 5 SCAN_DIRS scope, the three citation categories detected (`inline`, `parenthetical`, `feat-form` per D-07), the allowlist policy (PLACEHOLDER_DIGITS + frontmatter + code fence + hex lookbehind), and that this test is RED until Phase 66 cleanup.

    Do NOT add the file to any test runner config — `npm test` picks up `tests/*.test.cjs` automatically (per CONTEXT.md Integration Points).
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&amp;1 | tee /tmp/gsd-65-01-output.txt; grep -E "tests [0-9]+|pass [0-9]+|fail [0-9]+" /tmp/gsd-65-01-output.txt | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `[ -f tests/no-issue-citations.test.cjs ]` exits 0.
    - File is CommonJS: `head -5 tests/no-issue-citations.test.cjs | grep -E "require\\(['\"]node:test['\"]\\)"` returns a match.
    - PLACEHOLDER_DIGITS literal present: `grep -E "PLACEHOLDER_DIGITS\s*=\s*new Set\(\[1, 2, 45, 123\]\)" tests/no-issue-citations.test.cjs` returns a match.
    - Phase 64 provenance comment present: `grep -E "Phase 64|FINDINGS" tests/no-issue-citations.test.cjs` returns at least one match (per D-05).
    - INLINE_RE lookbehind present: `grep -F "(?<![0-9a-fA-F#])#" tests/no-issue-citations.test.cjs` returns a match (per D-11).
    - FEAT_FORM_RE present: `grep -F "feat-(\\d{3,})" tests/no-issue-citations.test.cjs` returns a match.
    - SCAN_DIRS contains all 5 directories: `grep -E "'commands'" tests/no-issue-citations.test.cjs && grep -E "'get-shit-done/workflows'" tests/no-issue-citations.test.cjs && grep -E "'agents'" tests/no-issue-citations.test.cjs && grep -E "'get-shit-done/references'" tests/no-issue-citations.test.cjs && grep -E "'get-shit-done/templates'" tests/no-issue-citations.test.cjs` — all 5 greps return matches (per D-08).
    - No import of the soon-to-be-deleted scanner (D-01): `! grep -F "require('../scripts/scan-citations')" tests/no-issue-citations.test.cjs && ! grep -F "require('./../scripts/scan-citations" tests/no-issue-citations.test.cjs && ! grep -F "scripts/scan-citations" tests/no-issue-citations.test.cjs` — exits 0 (no matches).
    - D-06 failure-hint sentence present in source: `grep -F "To add an allowlist exemption: add the digit to PLACEHOLDER_DIGITS" tests/no-issue-citations.test.cjs` returns a match.
    - Unit tests all pass: running `node --test tests/no-issue-citations.test.cjs` reports the unit-test `describe` blocks as passing (search /tmp/gsd-65-01-output.txt for the 9 unit test names and verify none appear as `not ok`).
    - Corpus subtests FAIL RED with enumeration: the failure block for `agents/gsd-executor.md` in /tmp/gsd-65-01-output.txt contains the literal string `agents/gsd-executor.md:410 #3097 (inline)` (or equivalent format with file:line, hash-digit, parenthesized category) — proves D-06 enumeration format works on a real corpus hit.
    - Corpus subtests FAIL RED on `feat-form` hit: failure output contains `get-shit-done/references/planner-graphify-auto-update.md:62 feat-3347 (feat-form)` — proves feat-form detection works.
    - Corpus subtests FAIL RED on canonical 3-digit hit: failure output contains `get-shit-done/workflows/discuss-phase/modes/chain.md:57 #686` — proves placeholder allowlist does NOT mask real 3-digit citations.
    - Hex color exemption proven on real corpus: failure output does NOT contain any reference to a 6-character hex color tail (no `#e8c170`, `#22c55e`, `#A78BFA`, `#c170`, `#22c5`, `#170`, `#5e`) — `! grep -E "#e8c170|#22c55e|#A78BFA|#A78B|#c170|#22c5" /tmp/gsd-65-01-output.txt`.
    - Placeholder exemption proven on real corpus: failure output does NOT enumerate any `#123`, `#1`, `#2`, or `#45` violations — `! grep -E " #1 \\(inline\\)| #2 \\(inline\\)| #45 \\(inline\\)| #123 \\(inline\\)" /tmp/gsd-65-01-output.txt`.
    - Aggregate failure count consistent with 64-FINDINGS: count of `(inline)` + `(parenthetical)` + `(feat-form)` enumeration lines in /tmp/gsd-65-01-output.txt is between 90 and 100 inclusive (target ~97; explicit slack for whitespace-only line stripping). Command: `grep -cE " \\((inline|parenthetical|feat-form)\\)$" /tmp/gsd-65-01-output.txt` returns a number in [90, 100].
    - npm test picks up the file automatically (CITE-03 verification): `npm test 2>&amp;1 | tee /tmp/gsd-65-01-npm.txt | grep -F "no-issue-citations.test.cjs"` returns a match. (Suite-level failure is expected — this plan establishes RED state.)
  </acceptance_criteria>
  <done>
    A new file `tests/no-issue-citations.test.cjs` exists. All unit subtests for inline detection, parenthetical category, feat-form detection, hex lookbehind, PLACEHOLDER_DIGITS exemption, heading exemption, frontmatter exclusion, code-fence exclusion, and late-`---` thematic-break handling pass. The corpus describe block fails RED on approximately 49 files with approximately 97 enumerated violations (per D-06 format: `file:line #NNN (category)` + indented context line). The final failure-message line for each failing subtest reads `To add an allowlist exemption: add the digit to PLACEHOLDER_DIGITS`. `npm test` picks up the file without manual registration (CITE-03). No reference to `scripts/scan-citations.cjs` exists in the new file (D-01).
  </done>
</task>

</tasks>

<verification>
- File exists at `tests/no-issue-citations.test.cjs`.
- `node --test tests/no-issue-citations.test.cjs` runs all subtests and reports the unit `describe` blocks as passing.
- Corpus subtests fail RED with the per-D-06 enumeration format on ~49 files / ~97 violations.
- `feat-3347` at planner-graphify-auto-update.md:62 appears in the failure enumeration.
- `#686` at chain.md:57 appears in the failure enumeration.
- No hex color tails (`#e8c170`, `#22c55e`, `#A78BFA`) appear as flagged violations.
- No PLACEHOLDER_DIGITS members (1, 2, 45, 123) appear as flagged violations on the live corpus.
- `npm test` automatically picks up the file (CITE-03 satisfied).
</verification>

<success_criteria>
- CITE-03: New permanent test exists at `tests/no-issue-citations.test.cjs` and runs under `npm test` without manual registration.
- CITE-04: The test fails RED before any cleanup, enumerating each offending file:line and matched text (~97 violations across ~49 files).
- CITE-05: Allowlist correctly exempts hex color codes (lookbehind), illustrative placeholders (PLACEHOLDER_DIGITS), markdown heading markers, and YAML frontmatter blocks — none produce test failures on the corpus.
</success_criteria>

<output>
A single new file: `tests/no-issue-citations.test.cjs`. No edits to existing files.
After Plan 02 completes (deletions of `scripts/scan-citations.cjs` and `tests/citation-scan.test.cjs`), the test remains independent (D-01) and still fails RED with the same violations.
</output>
