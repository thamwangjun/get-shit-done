---
phase: 65-guard-test-red
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - scripts/scan-citations.cjs
  - tests/citation-scan.test.cjs
autonomous: true
requirements:
  - CITE-03
must_haves:
  truths:
    - "scripts/scan-citations.cjs is deleted from disk (per D-02)"
    - "tests/citation-scan.test.cjs is deleted from disk (per D-03)"
    - "After both deletions, `node --test tests/no-issue-citations.test.cjs` still runs successfully (the guard test does not import the deleted scanner — D-01) and still fails RED with the same enumerated violations as in Plan 01"
    - "After both deletions, `npm test` does NOT produce any module-resolution error (`Cannot find module 'scripts/scan-citations'` or `MODULE_NOT_FOUND`) referencing the deleted files"
    - "No test file in tests/ still requires scripts/scan-citations.cjs after deletion"
    - "No file in tests/ or scripts/ still references the deleted citation-scan test path"
  artifacts:
    - path: "scripts/scan-citations.cjs"
      provides: "DELETED — Phase 64 discovery artifact removed (D-02)"
    - path: "tests/citation-scan.test.cjs"
      provides: "DELETED — Phase 64 Nyquist test for the deleted scanner removed (D-03)"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "(no longer references scripts/scan-citations.cjs)"
      via: "D-01 inline detection — broken link is the intended end state"
      pattern: "scripts/scan-citations"
---

<objective>
Delete the Phase 64 discovery artifacts now that the Phase 65 permanent guard
test in Plan 01 has been verified to detect citations independently. This
removes `scripts/scan-citations.cjs` (D-02) and `tests/citation-scan.test.cjs`
(D-03), then re-verifies that `tests/no-issue-citations.test.cjs` still operates
correctly with no orphaned imports across the wider test suite.

Purpose: Close out the Phase 64 → Phase 65 handoff cleanly. The scanner served
its discovery purpose (101 hits documented in 64-FINDINGS.md); the guard test
now replaces its detection function permanently. Keeping the scanner around
creates two sources of truth for citation detection, which is exactly the
divergence class CONTEXT.md D-01 was written to avoid.

Output: Two deletions. No edits to other files. After this plan, the only
citation-detection logic in the repo lives in `tests/no-issue-citations.test.cjs`.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted files → remaining test suite | Removing files may surface unknown imports elsewhere; the verify step runs the full `npm test` to catch any module-resolution fallout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Tampering | git index | mitigate | Use `git rm` (not raw `rm`) so the deletion is staged as one atomic git operation, matching the project's commit conventions |
| T-65-05 | Information Disclosure | git history | accept | The deleted files remain in git history (43cca234 et al.) — this is intended for provenance per D-05 |
| T-65-06 | Denial of Service | npm test pipeline | mitigate | Run `npm test` post-deletion to confirm no other file imports the removed scanner; if a hit surfaces, halt and surface via deviation handling |
| T-65-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — pure file deletion |
</threat_model>

<context>
!`cat .planning/STATE.md`
!`cat .planning/phases/65-guard-test-red/65-CONTEXT.md`

# Plan 01's PLAN.md establishes the guard test that replaces these artifacts
@.planning/phases/65-guard-test-red/65-01-PLAN.md

# The scanner being deleted (read so executor confirms the file targeted matches D-02)
@scripts/scan-citations.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete scripts/scan-citations.cjs and tests/citation-scan.test.cjs, then re-verify no-issue-citations.test.cjs is independent</name>
  <files>scripts/scan-citations.cjs, tests/citation-scan.test.cjs</files>
  <read_first>
    - tests/no-issue-citations.test.cjs (the Plan 01 output — confirm it does NOT contain `require('../scripts/scan-citations')` or any path referencing the soon-to-be-deleted scanner; if it does, HALT and escalate as a Plan 01 regression)
    - scripts/scan-citations.cjs (target of D-02 deletion — read header to confirm this is the Phase 64 discovery scanner before deleting)
    - tests/citation-scan.test.cjs (target of D-03 deletion — read header to confirm this is the Phase 64 Nyquist test for the scanner before deleting)
    - .planning/phases/65-guard-test-red/65-CONTEXT.md (D-02 and D-03 govern these deletions)
  </read_first>
  <action>
    Per D-02 and D-03, delete the two Phase 64 discovery artifacts so that
    `tests/no-issue-citations.test.cjs` is the only citation-detection source
    of truth in the repo.

    Pre-deletion gate (run first; HALT if any check fails):
    1. Confirm `tests/no-issue-citations.test.cjs` exists (Plan 01 output present).
    2. Confirm `tests/no-issue-citations.test.cjs` contains NO `require(...)` path matching `scripts/scan-citations` (D-01 was enforced in Plan 01). If a match exists, this is a Plan 01 violation — halt and surface via deviation handling rather than masking the bug by deleting the dependency.
    3. Search the entire `tests/` and `scripts/` trees for any OTHER file that imports the scanner via `require('.../scan-citations')` or references `tests/citation-scan` by path. If any other file imports them, halt and escalate — those dependents must be addressed first.

    Deletion (atomic, staged via git):
    - `git rm scripts/scan-citations.cjs`
    - `git rm tests/citation-scan.test.cjs`

    Post-deletion verification:
    - Re-run `node --test tests/no-issue-citations.test.cjs` — the guard test MUST still execute (proving D-01 inline detection holds with no scanner present) and MUST still fail RED with the same approximate violation count from Plan 01 (~97 enumerated hits across ~49 files). The unit-test `describe` blocks for INLINE_RE, FEAT_FORM_RE, hex exemption, PLACEHOLDER_DIGITS exemption, frontmatter exclusion, and code-fence exclusion MUST all still pass.
    - Run the full `npm test 2>&amp;1 | tee /tmp/gsd-65-02-npm.txt` and confirm no `Cannot find module` or `MODULE_NOT_FOUND` error references either deleted path. The pre-Plan-01 baseline already had `npm test` failing on the guard test (RED is intended through Phase 66); the post-deletion run must have the SAME failure pattern as the post-Plan-01 run — no NEW failures attributable to the deletion. Concretely: the number of failing test files in /tmp/gsd-65-02-npm.txt MUST be no greater than the count from /tmp/gsd-65-01-npm.txt minus 1 (Plan 01 already counted `tests/citation-scan.test.cjs` as a passing or failing file; after deletion that file is gone). If the failure count increases beyond that arithmetic, halt and surface the new failure.

    Commit message: `chore(65-02): delete Phase 64 scanner and Nyquist test (per D-02, D-03)` — single atomic commit covering both deletions.
  </action>
  <verify>
    <automated>[ ! -e scripts/scan-citations.cjs ] && [ ! -e tests/citation-scan.test.cjs ] && node --test tests/no-issue-citations.test.cjs 2>&amp;1 | tee /tmp/gsd-65-02-guard.txt | grep -E "tests [0-9]+|fail [0-9]+" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Scanner deleted from disk: `[ ! -e scripts/scan-citations.cjs ]` exits 0.
    - Nyquist test deleted from disk: `[ ! -e tests/citation-scan.test.cjs ]` exits 0.
    - Both deletions staged in git: `git status --porcelain scripts/scan-citations.cjs tests/citation-scan.test.cjs | grep -E "^D " | wc -l` returns 2 (one deleted line per file). Equivalent post-commit check after the chore commit: `git show --name-status HEAD | grep -E "^D\s+(scripts/scan-citations\.cjs|tests/citation-scan\.test\.cjs)" | wc -l` returns 2.
    - Plan 01 guard test file is untouched: `git diff HEAD~1 HEAD -- tests/no-issue-citations.test.cjs` returns empty (no diff between this commit and the prior commit on that file).
    - Guard test still independent (D-01): `! grep -F "scripts/scan-citations" tests/no-issue-citations.test.cjs` exits 0 (no match — never had the import, still does not).
    - Guard test still runs: `node --test tests/no-issue-citations.test.cjs` produces test runner output (no `Cannot find module` thrown before tests begin).
    - Guard test still fails RED with ~97 enumerated violations: `grep -cE " \\((inline|parenthetical|feat-form)\\)$" /tmp/gsd-65-02-guard.txt` returns a number in [90, 100].
    - feat-form canonical hit still detected: `grep -F "planner-graphify-auto-update.md:62 feat-3347" /tmp/gsd-65-02-guard.txt` returns a match.
    - 3-digit canonical hit still detected: `grep -F "chain.md:57 #686" /tmp/gsd-65-02-guard.txt` returns a match.
    - No orphan imports of the deleted scanner anywhere in tests/ or scripts/: `! grep -rE "require\\(['\"][^'\"]*scan-citations['\"]\\)" tests/ scripts/ 2>/dev/null` exits 0 (no matches).
    - No orphan path references to the deleted Nyquist test: `! grep -rF "tests/citation-scan.test.cjs" tests/ scripts/ get-shit-done/ commands/ agents/ 2>/dev/null` exits 0 (no matches outside `.planning/` history).
    - Full suite has no NEW module-resolution errors: `! grep -E "Cannot find module.*(scan-citations|citation-scan)" /tmp/gsd-65-02-npm.txt` exits 0 (no match).
    - npm test failure count consistent with deletion accounting: failing-file count in /tmp/gsd-65-02-npm.txt is less than or equal to the count from /tmp/gsd-65-01-npm.txt (since the previously-existing `tests/citation-scan.test.cjs` is now gone). Concrete check: `grep -cE "✖|not ok" /tmp/gsd-65-02-npm.txt` returns a value less than or equal to `grep -cE "✖|not ok" /tmp/gsd-65-01-npm.txt`.
  </acceptance_criteria>
  <done>
    `scripts/scan-citations.cjs` and `tests/citation-scan.test.cjs` are deleted from disk and staged in git as deletions. `tests/no-issue-citations.test.cjs` still runs and still fails RED with the same ~97 enumerated violations from Plan 01, proving the guard is fully independent per D-01. Full-suite `npm test` produces no NEW module-resolution errors attributable to either deleted file. A single atomic commit `chore(65-02): delete Phase 64 scanner and Nyquist test (per D-02, D-03)` covers both deletions.
  </done>
</task>

</tasks>

<verification>
- Both target files are absent from disk and staged as deleted in git.
- `tests/no-issue-citations.test.cjs` is unchanged from Plan 01 output.
- Running the guard test in isolation still fails RED with the canonical Phase 64 violations (`feat-3347`, `#686`) enumerated.
- `npm test` produces no `Cannot find module` errors referencing the deleted paths.
- No other file in `tests/`, `scripts/`, `get-shit-done/`, `commands/`, or `agents/` imports or references the deleted paths.
</verification>

<success_criteria>
- D-02 satisfied: `scripts/scan-citations.cjs` removed.
- D-03 satisfied: `tests/citation-scan.test.cjs` removed.
- CITE-03 closure: the guard test remains the sole, self-contained citation detector and continues to run under `npm test` after the deletions, confirming D-01 independence on the live test suite (not just unit-tested in Plan 01).
</success_criteria>

<output>
Two file deletions, one atomic git commit. No other files modified.
After this plan, Phase 65 is complete: the permanent guard exists, the discovery
artifacts are gone, and Phase 66 can proceed to cleanup.
</output>
