---
phase: "018"
plan: "02"
type: execute
wave: 2
depends_on:
  - "018-01"
files_modified: []
autonomous: false
requirements:
  - TEST-GATE-01

must_haves:
  truths:
    - "npm test total passing count exceeds 4163 (pre-phase baseline) — the two new test files add subtests that all pre-existing passing tests remain passing"
    - "The 2 pre-existing qwen-install.test.cjs failures are the only failures in pre-existing test files"
    - "No test file that was green before Phase 18 is now red"
    - "User has confirmed the deliberate <role> guard is active (manual checkpoint)"
  artifacts:
    - path: "tests/fork-persona-tag.test.cjs"
      provides: "Confirmed auto-discovered by npm test"
      exports: []
    - path: "tests/fork-intent-tag.test.cjs"
      provides: "Confirmed auto-discovered by npm test"
      exports: []
  key_links:
    - from: "scripts/run-tests.cjs"
      to: "tests/fork-persona-tag.test.cjs"
      via: "readdirSync glob of tests/*.test.cjs"
      pattern: "fork-persona-tag"
    - from: "scripts/run-tests.cjs"
      to: "tests/fork-intent-tag.test.cjs"
      via: "readdirSync glob of tests/*.test.cjs"
      pattern: "fork-intent-tag"
---

<objective>
Verify the full test suite remains healthy after integrating the two new test files, and confirm the persona guard is a live (non-vacuous) check.

Purpose: TEST-GATE-01 requires that no NEW failures are introduced in pre-existing tests. This plan runs the full suite, audits the failure delta, and captures a manual guard-liveness check before phase sign-off.

Output: Confirmed test suite state, documented failure delta, phase complete.
</objective>

<execution_context>
@/home/thamw/development/happier/get-shit-done/get-shit-done/workflows/execute-plan.md
@/home/thamw/development/happier/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thamw/development/happier/get-shit-done/.planning/ROADMAP.md
@/home/thamw/development/happier/get-shit-done/.planning/phases/018-fork-tag-corpus-tests/018-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and audit failure delta (TEST-GATE-01)</name>
  <files></files>

  <read_first>
    - .planning/phases/018-fork-tag-corpus-tests/018-01-SUMMARY.md — confirm both test files were created and their isolated run counts
    - tests/fork-persona-tag.test.cjs — verify it exists before running full suite
    - tests/fork-intent-tag.test.cjs — verify it exists before running full suite
  </read_first>

  <action>
Run the full test suite and verify the failure delta matches expectations.

Step 1 — Confirm new files exist:
```
ls tests/fork-persona-tag.test.cjs tests/fork-intent-tag.test.cjs
```
Both must be present. If either is missing, do NOT proceed — surface the gap.

Step 2 — Run the full suite:
```
cd /home/thamw/development/happier/get-shit-done && npm test 2>&1 | tail -20
```

Step 3 — Parse the output for:
- Total passing count (must be > 4163)
- Total failing count
- Which test files contribute failures

Step 4 — Audit failures. Expected failure profile:
- qwen-install.test.cjs: 2 failures (pre-existing, unrelated to Phase 18)
- fork-intent-tag.test.cjs: 32 failures (expected by design — <objective> files)
- ALL other test files: 0 failures

Step 5 — Compute new-failure count:
  new_failures = total_failing - 2 (qwen) - 32 (intent by design)

If new_failures > 0: identify which test file(s) regressed, do NOT proceed to the checkpoint — fix the regression first.

If new_failures === 0: proceed to the checkpoint task.

Document the exact npm test output tail in the SUMMARY.
  </action>

  <verify>
    <automated>cd /home/thamw/development/happier/get-shit-done && npm test 2>&1 | grep -E "^(pass|fail|▶)" | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` total passing count is greater than 4163 (the pre-phase baseline)
    - `npm test` output includes both `fork-persona-tag` and `fork-intent-tag` in its test file list
    - Failures in `qwen-install.test.cjs` are present (2 failures — pre-existing, acceptable)
    - Failures in `fork-intent-tag.test.cjs` are present (32 failures — expected, by design)
    - No test file OTHER than `qwen-install.test.cjs` and `fork-intent-tag.test.cjs` shows any failures
    - `npm test` output grep for `fork-persona-tag` shows `62 pass` and `0 fail`
  </acceptance_criteria>

  <done>Full suite runs with total passing > 4163, no new failures in pre-existing tests, failure delta matches the expected profile (2 qwen + 32 intent-by-design).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm persona guard liveness (manual injection test)</name>

  <what-built>
    Task 1 (auto) confirmed the test suite failure delta. This checkpoint verifies that
    fork-persona-tag.test.cjs is a live guard — not a vacuous pass — by injecting a
    deliberate <role> tag into one agent file, confirming the test fails, then reverting.
  </what-built>

  <how-to-verify>
    Run these steps in order:

    1. Pick any agent file — e.g. agents/gsd-planner.md

    2. Open the file and add ONE line anywhere in the file body (not inside a code fence):
       ```
       <role>temporary injection for guard test — DELETE ME</role>
       ```

    3. Run the persona test:
       ```
       node --test tests/fork-persona-tag.test.cjs 2>&1 | grep -E "(fail|FAIL|gsd-planner)"
       ```

    4. Confirm the output shows a FAILURE specifically for gsd-planner.md (or whichever
       agent you chose). The test should show at least 1 fail. If it still shows 0 fail,
       the guard is not working — do NOT approve.

    5. Revert the change:
       ```
       git checkout agents/gsd-planner.md
       ```

    6. Run the persona test again to confirm it returns to 62 pass / 0 fail:
       ```
       node --test tests/fork-persona-tag.test.cjs 2>&1 | tail -3
       ```

    Expected after revert: `62 pass` `0 fail`
  </how-to-verify>

  <resume-signal>
    Type "approved" if steps 1-6 completed successfully (guard detected the injection AND reverted cleanly).
    Type "failed: [description]" if the guard did not detect the <role> injection.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → CI | npm test output determines phase sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-018-03 | Spoofing | TEST-GATE-01 pass declaration | mitigate | Require explicit failure delta audit (not just "tests passed") before checkpoint |
| T-018-04 | Repudiation | Guard liveness (vacuous pass risk) | mitigate | Manual injection checkpoint: inject <role>, confirm failure, revert, confirm recovery |
</threat_model>

<verification>
TEST-GATE-01 is satisfied when ALL of the following are true:

1. `npm test` total passing count > 4163 (pre-phase baseline)
2. `npm test` shows fork-persona-tag.test.cjs: 62 pass, 0 fail
3. `npm test` shows fork-intent-tag.test.cjs: 47 pass, 32 fail (by design)
4. No test file other than qwen-install.test.cjs and fork-intent-tag.test.cjs has any failures
5. Manual guard-liveness checkpoint approved by user
</verification>

<success_criteria>
Phase 18 is complete when:
1. Both new test files are in tests/ and auto-discovered by npm test
2. fork-persona-tag.test.cjs: 62 pass / 0 fail
3. fork-intent-tag.test.cjs: 47 pass / 32 fail (documented as expected)
4. Total npm test passing count exceeds pre-phase baseline of 4163
5. No regressions introduced in any test file that was green before Phase 18
6. User confirmed the persona guard is live (manual injection test passed)
</success_criteria>

<output>
After completion, create `.planning/phases/018-fork-tag-corpus-tests/018-02-SUMMARY.md`
</output>
