---
status: complete
phase: 018-fork-tag-corpus-tests
source: [018-01-SUMMARY.md, 018-02-SUMMARY.md]
started: 2026-04-28T00:00:00Z
updated: 2026-04-28T07:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Persona Tag Corpus Guard
expected: Run `node --test tests/fork-persona-tag.test.cjs` — should report 62 pass, 0 fail. Each of 31 agent files passes both subtests: <persona> present and <role> absent outside code fences.
result: pass

### 2. Intent Tag Corpus Guard
expected: Run `node --test tests/fork-intent-tag.test.cjs` — should report 46 pass, 33 fail. The 33 failures are BY DESIGN (command files still using <objective> tags, documented in the test header).
result: pass
note: actual 47 pass / 32 fail — corpus improved by 1 since phase execution (one <objective> file converted); guard functioning correctly

### 3. Full Suite Gate (no new regressions)
expected: Run `npm test` — total passing should be > 4163 (actual expected: 4271). Failures should be exactly: 33 from fork-intent-tag (by design) + 2 from qwen-install (pre-existing) = 35 total. Zero failures in any previously-passing test.
result: pass
note: actual 4272 pass / 34 fail (32 intent by-design + 2 qwen = 34); consistent with corpus improvement; 0 new regressions confirmed

### 4. Guard Liveness (injection test)
expected: Inject `<role>test</role>` on a new line at the end of any agent file (e.g., agents/gsd-planner.md). Run `node --test tests/fork-persona-tag.test.cjs`. That one agent's "does not use <role>" subtest should fail (61 pass, 1 fail). Revert the injection (`git checkout agents/gsd-planner.md`), re-run — back to 62 pass, 0 fail.
result: pass
note: injection → 61 pass, 1 fail; revert → 62 pass, 0 fail; guard confirmed live and non-vacuous

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
