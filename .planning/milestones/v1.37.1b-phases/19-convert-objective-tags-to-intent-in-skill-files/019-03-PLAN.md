---
phase: 019
plan: 03
type: execute
wave: 2
depends_on:
  - "019-01"
  - "019-02"
files_modified: []
autonomous: false
requirements:
  - CONVERT-01

must_haves:
  truths:
    - "npm test shows fork-intent-tag.test.cjs at 79/79 pass, 0 fail"
    - "No new failures introduced in any pre-existing test"
    - "Total passing subtests has increased by at least 33 vs pre-phase baseline"
  artifacts:
    - path: "tests/fork-intent-tag.test.cjs"
      provides: "Test file that now passes 79/79 (verified by running npm test)"
      contains: "all 79 commands/gsd"
  key_links:
    - from: "npm test output"
      to: "fork-intent-tag.test.cjs"
      via: "Node.js built-in test runner"
      pattern: "79 pass"
---

<objective>
Run the full test suite to confirm `fork-intent-tag.test.cjs` passes 79/79 subtests with 0 failures, and that no pre-existing tests have regressed.

Purpose: This is the final acceptance gate for CONVERT-01. It proves the conversion is complete and correct, not just that the files were edited.

Output: Verified test run output. No files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/19-CONTEXT.md
@.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-01-SUMMARY.md
@.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm 79/79 for fork-intent-tag.test.cjs</name>
  <files></files>

  <read_first>
    - .planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-01-SUMMARY.md — confirm Plan 01 completed (33 files converted, DESIGN NOTE updated)
    - .planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-02-SUMMARY.md — confirm Plan 02 completed (REQUIREMENTS.md, PROJECT.md, guide updated)
  </read_first>

  <action>
Run the full test suite:

```bash
npm test 2>&1
```

From the output, verify:
1. `fork-intent-tag.test.cjs` shows 79 passing subtests and 0 failing subtests
2. `fork-persona-tag.test.cjs` still passes 62/62 (no regression)
3. All pre-existing tests that were passing before Phase 19 are still passing
4. The total passing count is at or above the pre-phase baseline of 4306 plus 33 (the 33 newly-passing intent subtests) = approximately 4339 pass total

If any test that was passing before Phase 19 is now failing, investigate immediately — the `<objective>` → `<intent>` rename should only affect `fork-intent-tag.test.cjs`, not any other test file.

If `fork-intent-tag.test.cjs` does NOT show 79/79: run `grep -l '<objective>' commands/gsd/*.md` to identify any files that were missed by Plan 01's sed command, fix them individually using the Edit tool (replace `<objective>` with `<intent>` and `</objective>` with `</intent>`), then re-run `npm test`.
  </action>

  <verify>
    <automated>npm test 2>&1 | grep -E "fork-intent-tag|pass|fail" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` output contains a line matching `fork-intent-tag.test.cjs` with `79` passing and `0` failing
    - `npm test` output does NOT show any new failures compared to the pre-phase baseline (pre-existing failures in `qwen-install.test.cjs` are acceptable and expected)
    - `npm test` output contains `fork-persona-tag.test.cjs` with `62` passing and `0` failing (no regression)
    - Total passing subtests in `npm test` output is at least 4306 + 33 = 4339 (the 33 newly-passing intent subtests added to the pre-phase baseline)
    - Running `grep -l '<objective>' commands/gsd/*.md` after a passing test run returns no output
  </acceptance_criteria>

  <done>npm test shows fork-intent-tag.test.cjs at 79/79 pass, 0 fail. No pre-existing passing tests have regressed. CONVERT-01 is satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 converted 33 command files from `<objective>` to `<intent>` and updated the DESIGN NOTE in `tests/fork-intent-tag.test.cjs`. Plan 02 added CONVERT-01 to REQUIREMENTS.md, updated PROJECT.md Key Decisions, and corrected UPSTREAM_TO_FORK_CHANGES_GUIDE.md. Plan 03 Task 1 ran the full test suite and confirmed 79/79 for fork-intent-tag.test.cjs.
  </what-built>
  <how-to-verify>
    Run these checks in order:

    1. Confirm zero objective tags remain:
       ```bash
       grep -r '<objective>' commands/gsd/*.md
       ```
       Expected: no output.

    2. Confirm test gate passes:
       ```bash
       npm test 2>&1 | grep "fork-intent-tag"
       ```
       Expected: line showing `fork-intent-tag.test.cjs` with 79 passing, 0 failing.

    3. Confirm REQUIREMENTS.md has CONVERT-01:
       ```bash
       grep "CONVERT-01" .planning/REQUIREMENTS.md
       ```
       Expected: 2+ lines.

    4. Confirm PROJECT.md Key Decisions updated:
       ```bash
       grep "Phase 19" .planning/PROJECT.md
       ```
       Expected: at least 1 line containing the conversion decision.

    5. Optionally open one of the converted files (e.g., `commands/gsd/add-backlog.md`) and confirm the content inside the `<intent>` block reads correctly (content unchanged from what was in `<objective>`).
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm test runner → test files | Test runner reads command files and verifies tags; risk is a missed file causing a subtest failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-019-05 | Repudiation | npm test failure after conversion | mitigate | If fork-intent-tag.test.cjs fails, run `grep -l '<objective>' commands/gsd/*.md` to identify missed files and fix before re-running |
| T-019-06 | Denial | Pre-existing test regression caused by edit side effect | mitigate | Total passing count check catches any regression; pre-existing qwen-install.test.cjs failures are the only accepted pre-existing failures |
</threat_model>

<verification>
Final state:

```bash
# No objective tags remain
grep -r '<objective>' commands/gsd/*.md
# expect: no output, exit code 1

# Test gate
npm test 2>&1 | grep "fork-intent-tag"
# expect: 79 pass, 0 fail

# Requirements documented
grep "CONVERT-01" .planning/REQUIREMENTS.md
# expect: 2+ matches

# Decision recorded
grep "Phase 19" .planning/PROJECT.md
# expect: at least 1 match
```
</verification>

<success_criteria>
- `fork-intent-tag.test.cjs` reports 79/79 subtests passing in `npm test` output
- Zero pre-existing passing tests have become failures
- `grep -r '<objective>' commands/gsd/*.md` returns no output
- User has approved the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-03-SUMMARY.md`
</output>
