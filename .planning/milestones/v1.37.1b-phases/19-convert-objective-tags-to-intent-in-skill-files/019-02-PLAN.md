---
phase: 019
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
autonomous: true
requirements:
  - CONVERT-01

must_haves:
  truths:
    - "REQUIREMENTS.md contains a CONVERT-01 requirement entry with 79/79 pass language"
    - "REQUIREMENTS.md Traceability table lists CONVERT-01 mapped to Phase 19"
    - "PROJECT.md Key Decisions table has a row for the <objective> to <intent> conversion"
    - "UPSTREAM_TO_FORK_CHANGES_GUIDE.md scope is corrected to reflect 100% conversion"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONVERT-01 requirement entry"
      contains: "CONVERT-01"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for objective-to-intent conversion"
      contains: "objective"
    - path: ".planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md"
      provides: "Updated scope and example for command layer section"
      contains: "79 command files"
  key_links:
    - from: ".planning/REQUIREMENTS.md CONVERT-01"
      to: "tests/fork-intent-tag.test.cjs"
      via: "requirement describes 79/79 pass gate"
      pattern: "CONVERT-01"
---

<objective>
Add CONVERT-01 to REQUIREMENTS.md, record the `<objective>` → `<intent>` conversion in PROJECT.md Key Decisions, and update UPSTREAM_TO_FORK_CHANGES_GUIDE.md to reflect 100% command file conversion (per D-02, D-04, D-06, and Claude's discretion on the guide).

Purpose: Closes the documentation loop on Phase 18's INTENT-01 and provides a full record of the completed conversion.

Output: Three updated documentation files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONVERT-01 to REQUIREMENTS.md and update Traceability table (per D-02, D-04)</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md — read the full file to find the exact insertion point for CONVERT-01 and the Traceability table structure before editing
  </read_first>

  <action>
Read `.planning/REQUIREMENTS.md` in full. Then make two edits:

**Edit 1 — Add new section after `## v1.37.1b Requirements` block.** After the `### Test Suite Gate` section (after the `TEST-GATE-01` line), add a new section:

```markdown
## Intent Tag Conversion

### v1.37.1b Completion — Objective Tag Removal

- [ ] **CONVERT-01**: All 79 `commands/gsd/*.md` files pass `fork-intent-tag.test.cjs` (79/79 subtests pass, 0 failures) — closes the loop on INTENT-01 from Phase 18 by converting the 33 remaining `<objective>` files to `<intent>`
```

**Edit 2 — Update Traceability table.** In the `## Traceability` section, add a row for CONVERT-01:

```markdown
| CONVERT-01 | Phase 19 | Pending |
```

Also update the Coverage count at the bottom of the Traceability section from "v1.37.1b requirements: 3 total" to "v1.37.1b requirements: 4 total" and "Mapped to phases: 3" to "Mapped to phases: 4".

Do not change any other content in the file — existing PERSONA-01, INTENT-01, TEST-GATE-01 entries, Future Requirements, Out of Scope, and the date lines remain intact.
  </action>

  <verify>
    <automated>grep -n "CONVERT-01" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CONVERT-01" .planning/REQUIREMENTS.md` returns at least 2 matches (the requirement entry and the Traceability row)
    - `grep "79/79 subtests pass, 0 failures" .planning/REQUIREMENTS.md` returns 1 match
    - `grep "Phase 19" .planning/REQUIREMENTS.md` returns 1 match (in Traceability table)
    - `grep "v1.37.1b requirements: 4 total" .planning/REQUIREMENTS.md` returns 1 match
    - `grep "closes the loop on INTENT-01" .planning/REQUIREMENTS.md` returns 1 match
    - Existing entries untouched: `grep "PERSONA-01" .planning/REQUIREMENTS.md` returns at least 1 match, `grep "INTENT-01" .planning/REQUIREMENTS.md` returns at least 1 match
  </acceptance_criteria>

  <done>CONVERT-01 requirement added. Traceability table updated with Phase 19 row. Coverage count corrected to 4 total.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md Key Decisions and UPSTREAM_TO_FORK_CHANGES_GUIDE.md (per D-06 and Claude's discretion)</name>
  <files>
    .planning/PROJECT.md
    .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
  </files>

  <read_first>
    - .planning/PROJECT.md — read the Key Decisions table to find the exact insertion point (add new row after the last existing row)
    - .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md — read lines 68–82 (the `<task> → <intent>` section) to see the exact text to update
  </read_first>

  <action>
**PROJECT.md — Key Decisions table update (per D-06):**

Read `.planning/PROJECT.md`. Find the Key Decisions table (starts with `| Decision | Rationale | Outcome |`). Append a new row at the end of the table, before the `## Evolution` section:

```markdown
| `<objective>` → `<intent>` conversion for 33 command files | All command files now use the fork's `<intent>` tag standard; closes loop on INTENT-01 from Phase 18; mechanical find-and-replace only (no content changes) | ✓ Good — Phase 19 |
```

Also update the Active section: the INTENT-01 entry currently reads:
```
- [x] **INTENT-01**: `tests/fork-intent-tag.test.cjs` — 79 subtests, 46 pass / 33 fail by design (<objective> files). Validated in Phase 18
```
Update it to:
```
- [x] **INTENT-01**: `tests/fork-intent-tag.test.cjs` — 79 subtests, 79 pass / 0 fail. Validated in Phase 18; <objective> files converted in Phase 19 (CONVERT-01)
```

**UPSTREAM_TO_FORK_CHANGES_GUIDE.md — command layer section update (Claude's discretion):**

Read `.planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md`. Find the `### \`<task>\` → \`<intent>\` in command layer` section (around lines 68–82). Update two items:

1. Line `**Scope:** All 51 command files in \`commands/gsd/\`.` — update to:
   ```
   **Scope:** All 79 command files in `commands/gsd/`.
   ```

2. The example block currently shows:
   ```
   Upstream:   <objective>Extract implementation decisions...</objective>
   Fork:       <intent>Gather implementation decisions...</intent>
   ```
   Update to:
   ```
   Upstream:   <task>Extract implementation decisions...</task>
   Fork:       <intent>Gather implementation decisions...</intent>
   ```
   (The "Upstream:" line should show `<task>` not `<objective>` — `<objective>` was the intermediate state during phased conversion, not the canonical upstream tag. The section title already says `<task>` → `<intent>`.)

3. After `**Verified by:** \`tests/agent-frontmatter.test.cjs\` — 155 subtests.` (this line is in the persona section above), find the equivalent for intent: there is no "Verified by" line in the intent section yet. Add after the scope line:
   ```
   **Verified by:** `tests/fork-intent-tag.test.cjs` — 79 subtests (79/79 pass after Phase 19 conversion).
   ```
  </action>

  <verify>
    <automated>grep -n "CONVERT-01\|Phase 19\|79 command files\|fork-intent-tag" .planning/PROJECT.md .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md</automated>
  </verify>

  <acceptance_criteria>
    PROJECT.md:
    - `grep "Phase 19" .planning/PROJECT.md` returns at least 1 match (Key Decisions row)
    - `grep "closes loop on INTENT-01" .planning/PROJECT.md` returns 1 match
    - `grep "79 pass / 0 fail" .planning/PROJECT.md` returns 1 match (updated INTENT-01 Active entry)
    - `grep "46 pass / 33 fail by design" .planning/PROJECT.md` returns no output (old text removed)
    - The Key Decisions table structure is intact: `grep "| Decision | Rationale | Outcome |" .planning/PROJECT.md` returns 1 match

    UPSTREAM_TO_FORK_CHANGES_GUIDE.md:
    - `grep "All 79 command files" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns 1 match
    - `grep "fork-intent-tag.test.cjs" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns 1 match
    - `grep "79/79 pass" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns 1 match
    - `grep "All 51 command files" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns no output (old count removed)
  </acceptance_criteria>

  <done>PROJECT.md Key Decisions has Phase 19 row and INTENT-01 updated to 79/79. UPSTREAM_TO_FORK_CHANGES_GUIDE.md scope updated to 79 files with verified-by line added and example corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit tool → documentation files | In-place edits to markdown tables; risk is breaking table formatting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-019-03 | Tampering | Markdown table row insertion breaking pipe alignment | accept | Markdown table rendering is whitespace-tolerant; grep-based acceptance criteria verify content presence regardless of alignment |
| T-019-04 | Tampering | Overwriting existing INTENT-01 Active entry with wrong text | mitigate | Read the file first; acceptance criteria verify both old text absent and new text present |
</threat_model>

<verification>
After both tasks:

```bash
# REQUIREMENTS.md has CONVERT-01
grep -c "CONVERT-01" .planning/REQUIREMENTS.md
# expect: 2 or more

# PROJECT.md has Phase 19 decision row
grep "Phase 19" .planning/PROJECT.md
# expect: at least 1 match

# Guide updated with correct scope
grep "All 79 command files" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
# expect: 1 match

# Old INTENT-01 failure language gone from PROJECT.md
grep "33 fail by design" .planning/PROJECT.md
# expect: no output
```
</verification>

<success_criteria>
- `grep "CONVERT-01" .planning/REQUIREMENTS.md` returns 2+ matches
- `grep "Phase 19" .planning/PROJECT.md` returns at least 1 match
- `grep "All 79 command files" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns 1 match
- `grep "fork-intent-tag.test.cjs" .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md` returns 1 match
</success_criteria>

<output>
After completion, create `.planning/phases/19-convert-objective-tags-to-intent-in-skill-files/019-02-SUMMARY.md`
</output>
