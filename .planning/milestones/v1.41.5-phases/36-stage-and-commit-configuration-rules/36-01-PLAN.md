---
phase: 36-stage-and-commit-configuration-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stage-batch-1.cjs
autonomous: true
requirements:
  - STAGE-01
must_haves:
  truths:
    - "D-01: Only stage CATALOGUE.json, mise.toml, .planning/config.json, and files under .planning/references/*"
    - "D-02: Skip staging silently if any expected file has no changes since v1.41.2"
    - "D-03: Automatically unstage pre-existing staged changes before staging Batch 1"
    - "D-04: The verification step must match the expected Batch 1 files exactly; extra files cause abort"
    - "D-05: Verify staged files are a subset of the expected list"
    - "Commit message matches chore(config): refactor rules and configuration files (Batch 1) exactly"
  artifacts:
    - path: "scripts/stage-batch-1.cjs"
      provides: "Autostaging, verification, and committing for Batch 1 configuration & rules"
  key_links: []
---

<objective>
Stage and commit Batch 1 configuration and rule files (CATALOGUE.json, mise.toml, .planning/config.json, and .planning/references/*.md) using a self-verifying staging script to ensure history refactoring integrity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-stage-and-commit-configuration-rules/36-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 36-01-01: Write and run Batch 1 staging and validation script (STAGE-01, D-01, D-02, D-03, D-04, D-05)</name>
  <files>scripts/stage-batch-1.cjs</files>
  <read_first>
    - scripts/stage-batch-1.cjs
    - .planning/phases/36-stage-and-commit-configuration-rules/36-CONTEXT.md
    - scripts/run-tests.cjs
    - CATALOGUE.json
    - mise.toml
    - .planning/config.json
  </read_first>
  <action>
    Create the staging script `scripts/stage-batch-1.cjs` to automate staging and validation. The script must:
    1. Import `fs`, `path`, and `execFileSync` from CommonJS modules.
    2. Check if the latest git commit message is already "chore(config): refactor rules and configuration files (Batch 1)". If so, print "Batch 1 already committed" and exit 0.
    3. Initialize a Set of expected files containing 'CATALOGUE.json', 'mise.toml', and '.planning/config.json'.
    4. Dynamically scan `.planning/references/` for markdown files (`.md`) and add their relative paths to the expected files Set.
    5. Run `git reset` using `execFileSync` to unstage any pre-existing staged files.
    6. Iterate through the expected files list. For each file, check if it exists on disk and has changes since `v1.41.2` (e.g. by checking if `git diff --quiet v1.41.2 -- <file>` exits with a non-zero status). If the file has no changes or does not exist, skip staging and log output for it entirely silently. Otherwise, execute `git add` using `execFileSync`.
    7. Execute `git diff --cached --name-only` using `execFileSync` to list all currently staged files.
    8. Verify that all staged files are a subset of the expected files Set. If any file is staged that is not in the expected files Set, immediately abort by executing `git reset`, log a detailed error listing the unauthorized files, and exit the process with status 1.
    9. If there are no staged files at all, log "No modifications to stage for Batch 1" and exit 0.
    10. If the validation passes and files are staged, execute `git commit -m "chore(config): refactor rules and configuration files (Batch 1)"` using `execFileSync`.
    
    After writing the script, execute it by running:
    
    `node scripts/stage-batch-1.cjs`
  </action>
  <verify>
    <automated>node scripts/stage-batch-1.cjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/stage-batch-1.cjs uses CommonJS require format and execFileSync for command execution
    - The script successfully runs and exits with status 0
    - If run when changes exist, it unstages all other files and only commits Batch 1
    - If run when Batch 1 is already committed, it exits 0 without errors
  </acceptance_criteria>
  <done>Staging script is written and executed successfully, creating the Batch 1 commit.</done>
</task>

<task type="auto">
  <name>Task 36-01-02: Verify git status and commit history (STAGE-01, D-04, D-05)</name>
  <files>none — git repository checks</files>
  <read_first>
    - .planning/phases/36-stage-and-commit-configuration-rules/36-VALIDATION.md
  </read_first>
  <action>
    Verify that the staging script performed the staging and commit operations correctly:
    1. Run `git status` to verify that there are no remaining staged files.
    2. Run `git log -n 1 --pretty=format:%s` to check the latest commit message. It must match: `chore(config): refactor rules and configuration files (Batch 1)`.
    3. Run `git show --name-only --pretty=format:` on HEAD to list the files changed in the latest commit. Verify that all listed files are a subset of the expected Batch 1 files:
       - CATALOGUE.json
       - mise.toml
       - .planning/config.json
       - .planning/references/PROMPT_ENGINEERING_GUIDE_V10.md
       - .planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md
       - .planning/references/UPSTREAM_TO_FORK_CHANGES_GUIDE.md
  </action>
  <verify>
    <automated>[[ "$(git log -n 1 --pretty=format:%s)" == "chore(config): refactor rules and configuration files (Batch 1)" ]]</automated>
  </verify>
  <acceptance_criteria>
    - git status output shows no staged changes
    - git log -n 1 --pretty=format:%s output is exactly "chore(config): refactor rules and configuration files (Batch 1)"
    - git show --name-only --pretty=format: output contains only files from the expected Batch 1 list
  </acceptance_criteria>
  <done>Git repository state and commit history are verified to match Phase 36 requirements exactly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git CLI → local repository | git commands operate on the local git repository index and history |
| Node.js child_process → shell | execFileSync executes git CLI commands as subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | Staging Index | mitigate | Script runs `git reset` to clean the index, then explicitly adds specific paths; custom verification rejects commits if unauthorized files are staged. |
| T-36-02 | Spoofing | Commit metadata | mitigate | Script uses hardcoded message `chore(config): refactor rules and configuration files (Batch 1)` to prevent formatting errors. |
| T-36-03 | Information Disclosure | Configuration file staging | mitigate | .planning/config.json is verified to have brave_search, firecrawl, exa_search set to false, and contains no credentials. |
</threat_model>

<verification>
Full phase verification checks:
1. Running `git status` shows no staged changes.
2. `git log -n 1 --pretty=format:%s` returns exactly `chore(config): refactor rules and configuration files (Batch 1)`.
3. `git show --name-only --pretty=format: HEAD` returns a list of files that is a subset of the allowed Batch 1 files.
</verification>

<success_criteria>
- STAGE-01: Batch 1 files (CATALOGUE.json, mise.toml, .planning/config.json, and .planning/references/*.md) are staged and committed using the conventional commit message: `chore(config): refactor rules and configuration files (Batch 1)`.
- No extra files from subsequent batches are staged or committed.
</success_criteria>

<output>
After completion, create `.planning/phases/36-stage-and-commit-configuration-rules/36-01-SUMMARY.md` using the summary template.
</output>
