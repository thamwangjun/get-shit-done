---
phase: 38-stage-and-commit-workflows-agents-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stage-batch-3.cjs
autonomous: true
requirements:
  - STAGE-03
must_haves:
  truths:
    - "D-01: Create a dedicated node script scripts/stage-batch-3.cjs to automate staging, validation, and committing of Batch 3 files"
    - "D-02: Keep the script untracked in the workspace as a development artifact"
    - "D-03: Include an active branch guard in scripts/stage-batch-3.cjs that aborts the script if the current branch is not thamw-main, unless ALLOW_ANY_BRANCH=1 is set"
    - "D-04: Verify staging targets strictly align with the exact subset of Batch 3 files"
    - "D-05: Automatically run git reset to unstage pre-existing staged changes before staging Batch 3 files"
    - "D-06: Check the latest commit message and if it matches refactor(prompts): refactor workflows, agents, and templates (Batch 3), exit 0 early"
  artifacts:
    - path: "scripts/stage-batch-3.cjs"
      provides: "Autostaging, verification, and committing for Batch 3 workflows, agents, commands, non-test documentation, and templates"
  key_links: []
---

<objective>
Stage and commit Batch 3 workflows, agents, commands, non-test documentation, and templates using a self-verifying staging script to ensure history refactoring integrity.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-stage-and-commit-workflows-agents-templates/38-CONTEXT.md
@.planning/phases/38-stage-and-commit-workflows-agents-templates/38-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 38-01-01: Write and run Batch 3 staging and validation script (STAGE-03, D-01, D-02, D-03, D-04, D-05, D-06)</name>
  <files>scripts/stage-batch-3.cjs</files>
  <read_first>
    - scripts/stage-batch-2.cjs
    - .planning/phases/38-stage-and-commit-workflows-agents-templates/38-CONTEXT.md
  </read_first>
  <action>
    Create the staging script `scripts/stage-batch-3.cjs` to automate staging and validation. The script must:
    1. Import `fs`, `path`, and `execFileSync` from CommonJS modules.
    2. Check the active branch guard: if `process.env.ALLOW_ANY_BRANCH !== '1'`, retrieve current branch using git, and if it is not "thamw-main", log error and abort with exit code 1.
    3. Check if the latest git commit message is already "refactor(prompts): refactor workflows, agents, and templates (Batch 3)". If so, print "Batch 3 already committed" and exit 0.
    4. Initialize a Set of expected files containing the 61 Batch 3 files:
       - 'README.md'
       - 'README.ja-JP.md'
       - 'README.ko-KR.md'
       - 'README.pt-BR.md'
       - 'README.zh-CN.md'
       - 'agents/gsd-advisor-researcher.md'
       - 'agents/gsd-assumptions-analyzer.md'
       - 'agents/gsd-code-fixer.md'
       - 'agents/gsd-code-reviewer.md'
       - 'agents/gsd-codebase-mapper.md'
       - 'agents/gsd-debugger.md'
       - 'agents/gsd-doc-classifier.md'
       - 'agents/gsd-doc-synthesizer.md'
       - 'agents/gsd-doc-verifier.md'
       - 'agents/gsd-doc-writer.md'
       - 'agents/gsd-eval-auditor.md'
       - 'agents/gsd-executor.md'
       - 'agents/gsd-intel-updater.md'
       - 'agents/gsd-pattern-mapper.md'
       - 'agents/gsd-phase-researcher.md'
       - 'agents/gsd-plan-checker.md'
       - 'agents/gsd-planner.md'
       - 'agents/gsd-roadmapper.md'
       - 'agents/gsd-security-auditor.md'
       - 'agents/gsd-ui-checker.md'
       - 'agents/gsd-ui-researcher.md'
       - 'agents/gsd-verifier.md'
       - 'commands/gsd/discuss-phase.md'
       - 'commands/gsd/docs-update.md'
       - 'commands/gsd/execute-phase.md'
       - 'docs/FEATURES.md'
       - 'docs/INVENTORY-MANIFEST.json'
       - 'docs/INVENTORY.md'
       - 'docs/USER-GUIDE.md'
       - 'docs/zh-CN/README.md'
       - 'docs/DEVELOPMENT.md'
       - 'docs/GETTING-STARTED.md'
       - 'docs/SDK-LOCAL-DEV.md'
       - 'docs/TESTING.md'
       - 'get-shit-done/workflows/autonomous.md'
       - 'get-shit-done/workflows/discuss-phase-assumptions.md'
       - 'get-shit-done/workflows/docs-update.md'
       - 'get-shit-done/workflows/edit-phase.md'
       - 'get-shit-done/workflows/execute-phase.md'
       - 'get-shit-done/workflows/extract-learnings.md'
       - 'get-shit-done/workflows/fast.md'
       - 'get-shit-done/workflows/graduation.md'
       - 'get-shit-done/workflows/import.md'
       - 'get-shit-done/workflows/insert-phase.md'
       - 'get-shit-done/workflows/map-codebase.md'
       - 'get-shit-done/workflows/new-workspace.md'
       - 'get-shit-done/workflows/plan-phase.md'
       - 'get-shit-done/workflows/reapply-patches.md'
       - 'get-shit-done/workflows/remove-phase.md'
       - 'get-shit-done/workflows/secure-phase.md'
       - 'get-shit-done/workflows/settings-integrations.md'
       - 'get-shit-done/workflows/spike.md'
       - 'get-shit-done/workflows/undo.md'
       - 'get-shit-done/workflows/verify-phase.md'
       - 'get-shit-done/workflows/join-discord.md'
       - 'get-shit-done/workflows/set-profile.md'
    5. Perform file existence checks on disk: for each expected file, if it is missing on disk, log validation failed and exit 1.
    6. Run `git reset` using `execFileSync` to unstage any pre-existing staged files.
    7. Iterate through the expected files list. For each file, check if it has changes since `v1.41.2` (e.g. check if `git diff --quiet v1.41.2 -- <file>` exits with a non-zero status or is a new file). If the file has no changes, skip staging and log output for it silently. Otherwise, execute `git add -f <file>` using `execFileSync`.
    8. Execute `git diff --cached --name-only` to list all currently staged files.
    9. Verify that all staged files are a subset of the expected files Set. If any file is staged that is not in the expected files Set, abort by executing `git reset`, log unauthorized files, and exit 1.
    10. If there are no staged files at all, log "No modifications to stage for Batch 3" and exit 0.
    11. If validation passes and files are staged, execute `git commit -m "refactor(prompts): refactor workflows, agents, and templates (Batch 3)"`.

    After writing the script, execute it by running:

    `node scripts/stage-batch-3.cjs`
  </action>
  <verify>
    <automated>node scripts/stage-batch-3.cjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/stage-batch-3.cjs uses CommonJS format and execFileSync for command execution
    - The script successfully runs and exits with status 0
    - If run when changes exist, it unstages all other files and only commits Batch 3
    - If run when Batch 3 is already committed, it exits 0 without duplicate commit
    - Script remains untracked on disk
  </acceptance_criteria>
  <done>Staging script is written and executed successfully, creating the Batch 3 commit.</done>
</task>

<task type="auto">
  <name>Task 38-01-02: Verify git status and commit history (STAGE-03, D-04, D-05)</name>
  <files>none — git repository checks</files>
  <read_first>
    - .planning/phases/38-stage-and-commit-workflows-agents-templates/38-VALIDATION.md
  </read_first>
  <action>
    Verify that the staging script performed the staging and commit operations correctly:
    1. Run `git status` to verify that there are no remaining staged files.
    2. Run `git log -n 1 --pretty=format:%s` to check the latest commit message. It must match: `refactor(prompts): refactor workflows, agents, and templates (Batch 3)`.
    3. Run `git show --name-only --pretty=format:` on HEAD to list the files changed in the latest commit. Verify that all listed files are a subset of the expected Batch 3 files.
  </action>
  <verify>
    <automated>[[ "$(git log -n 1 --pretty=format:%s)" == "refactor(prompts): refactor workflows, agents, and templates (Batch 3)" ]]</automated>
  </verify>
  <acceptance_criteria>
    - git status output shows no staged changes
    - git log -n 1 --pretty=format:%s output is exactly "refactor(prompts): refactor workflows, agents, and templates (Batch 3)"
    - git show --name-only --pretty=format: output contains only files from the expected Batch 3 list
  </acceptance_criteria>
  <done>Git repository state and commit history are verified to match Phase 38 requirements exactly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git CLI → local repository | git commands operate on the local git repository index and history |
| Node.js child_process → shell | execFileSync executes git CLI commands as subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | Staging Index | mitigate | Script runs `git reset` to clean the index, then explicitly adds specific paths; custom verification rejects commits if unauthorized files are staged. |
| T-38-02 | Spoofing | Commit metadata | mitigate | Script uses hardcoded message `refactor(prompts): refactor workflows, agents, and templates (Batch 3)` to prevent formatting errors. |
| T-38-03 | Elevation of Privilege | Active branch pollution | mitigate | Script has a branch guard requiring thamw-main unless ALLOW_ANY_BRANCH=1 is explicitly passed. |
</threat_model>

<verification>
Full phase verification checks:
1. Running `git status` shows no staged changes.
2. `git log -n 1 --pretty=format:%s` returns exactly `refactor(prompts): refactor workflows, agents, and templates (Batch 3)`.
3. `git show --name-only --pretty=format: HEAD` returns a list of files that is a subset of the allowed Batch 3 files.
</verification>

<success_criteria>
- STAGE-03: Batch 3 files are staged and committed using the conventional commit message: `refactor(prompts): refactor workflows, agents, and templates (Batch 3)`.
- No extra files from subsequent batches are staged or committed.
</success_criteria>

<output>
After completion, create `.planning/phases/38-stage-and-commit-workflows-agents-templates/38-01-SUMMARY.md` using the summary template.
</output>
