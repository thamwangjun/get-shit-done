---
phase: 37-stage-and-commit-scanner-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stage-batch-2.cjs
autonomous: true
requirements:
  - STAGE-02
must_haves:
  truths:
    - "D-01: Create a dedicated node script scripts/stage-batch-2.cjs to automate staging, validation, and committing of Batch 2 files"
    - "D-02: Keep the script untracked in the workspace as a development artifact"
    - "D-03: Include an active branch guard in scripts/stage-batch-2.cjs that aborts the script if the current branch is not thamw-main, unless ALLOW_ANY_BRANCH=1 is set"
    - "D-04: Fail the staging process if hooks/gsd-read-injection-scanner.js, scripts/audit-tags.js, or hooks/gsd-check-update.js are missing on disk"
    - "D-05: Skip staging silently for any target file that exists but has no changes since v1.41.2"
    - "D-06: Defer full validation checks of the scanner logic until the final validation phase (Phase 41) to prevent breaking on intermediate commits"
    - "D-07: Automatically run git reset to unstage pre-existing staged changes before staging Batch 2 files"
    - "D-08: Check the latest commit message and if it matches feat(scanner): refactor scanner logic and audit scripts (Batch 2), exit 0 early"
  artifacts:
    - path: "scripts/stage-batch-2.cjs"
      provides: "Autostaging, verification, and committing for Batch 2 scanner logic and audit scripts"
  key_links: []
---

<objective>
Stage and commit Batch 2 scanner logic and audit files (hooks/gsd-read-injection-scanner.js, scripts/audit-tags.js, and hooks/gsd-check-update.js) using a self-verifying staging script to ensure history refactoring integrity.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-stage-and-commit-scanner-logic/37-CONTEXT.md
@.planning/phases/37-stage-and-commit-scanner-logic/37-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 37-01-01: Write and run Batch 2 staging and validation script (STAGE-02, D-01, D-02, D-03, D-04, D-05, D-07, D-08)</name>
  <files>scripts/stage-batch-2.cjs</files>
  <read_first>
    - scripts/stage-batch-1.cjs
    - .planning/phases/37-stage-and-commit-scanner-logic/37-CONTEXT.md
    - hooks/gsd-read-injection-scanner.js
    - scripts/audit-tags.js
    - hooks/gsd-check-update.js
  </read_first>
  <action>
    Create the staging script `scripts/stage-batch-2.cjs` to automate staging and validation. The script must:
    1. Import `fs`, `path`, and `execFileSync` from CommonJS modules.
    2. Check the active branch guard: if `process.env.ALLOW_ANY_BRANCH !== '1'`, retrieve current branch using git, and if it is not "thamw-main", log error and abort with exit code 1.
    3. Check if the latest git commit message is already "feat(scanner): refactor scanner logic and audit scripts (Batch 2)". If so, print "Batch 2 already committed" and exit 0.
    4. Initialize a Set of expected files containing:
       - 'hooks/gsd-read-injection-scanner.js'
       - 'scripts/audit-tags.js'
       - 'hooks/gsd-check-update.js'
    5. Perform file existence checks on disk: for each expected file, if it is missing on disk, log validation failed and exit 1.
    6. Run `git reset` using `execFileSync` to unstage any pre-existing staged files.
    7. Iterate through the expected files list. For each file, check if it has changes since `v1.41.2` (e.g. check if `git diff --quiet v1.41.2 -- <file>` exits with a non-zero status or is a new file). If the file has no changes, skip staging and log output for it silently. Otherwise, execute `git add -f <file>` using `execFileSync`.
    8. Execute `git diff --cached --name-only` to list all currently staged files.
    9. Verify that all staged files are a subset of the expected files Set. If any file is staged that is not in the expected files Set, abort by executing `git reset`, log unauthorized files, and exit 1.
    10. If there are no staged files at all, log "No modifications to stage for Batch 2" and exit 0.
    11. If validation passes and files are staged, execute `git commit -m "feat(scanner): refactor scanner logic and audit scripts (Batch 2)"`.

    After writing the script, execute it by running:

    `node scripts/stage-batch-2.cjs`
  </action>
  <verify>
    <automated>node scripts/stage-batch-2.cjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/stage-batch-2.cjs uses CommonJS format and execFileSync for command execution
    - The script successfully runs and exits with status 0
    - If run when changes exist, it unstages all other files and only commits Batch 2
    - If run when Batch 2 is already committed, it exits 0 without duplicate commit
    - Script remains untracked on disk
  </acceptance_criteria>
  <done>Staging script is written and executed successfully, creating the Batch 2 commit.</done>
</task>

<task type="auto">
  <name>Task 37-01-02: Verify git status and commit history (STAGE-02, D-04, D-05)</name>
  <files>none — git repository checks</files>
  <read_first>
    - .planning/phases/37-stage-and-commit-scanner-logic/37-VALIDATION.md
  </read_first>
  <action>
    Verify that the staging script performed the staging and commit operations correctly:
    1. Run `git status` to verify that there are no remaining staged files.
    2. Run `git log -n 1 --pretty=format:%s` to check the latest commit message. It must match: `feat(scanner): refactor scanner logic and audit scripts (Batch 2)`.
    3. Run `git show --name-only --pretty=format:` on HEAD to list the files changed in the latest commit. Verify that all listed files are a subset of the expected Batch 2 files:
       - hooks/gsd-read-injection-scanner.js
       - scripts/audit-tags.js
       - hooks/gsd-check-update.js
  </action>
  <verify>
    <automated>[[ "$(git log -n 1 --pretty=format:%s)" == "feat(scanner): refactor scanner logic and audit scripts (Batch 2)" ]]</automated>
  </verify>
  <acceptance_criteria>
    - git status output shows no staged changes
    - git log -n 1 --pretty=format:%s output is exactly "feat(scanner): refactor scanner logic and audit scripts (Batch 2)"
    - git show --name-only --pretty=format: output contains only files from the expected Batch 2 list
  </acceptance_criteria>
  <done>Git repository state and commit history are verified to match Phase 37 requirements exactly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git CLI → local repository | git commands operate on the local git repository index and history |
| Node.js child_process → shell | execFileSync executes git CLI commands as subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | Staging Index | mitigate | Script runs `git reset` to clean the index, then explicitly adds specific paths; custom verification rejects commits if unauthorized files are staged. |
| T-37-02 | Spoofing | Commit metadata | mitigate | Script uses hardcoded message `feat(scanner): refactor scanner logic and audit scripts (Batch 2)` to prevent formatting errors. |
| T-37-03 | Elevation of Privilege | Active branch pollution | mitigate | Script has a branch guard requiring thamw-main unless ALLOW_ANY_BRANCH=1 is explicitly passed. |
</threat_model>

<verification>
Full phase verification checks:
1. Running `git status` shows no staged changes.
2. `git log -n 1 --pretty=format:%s` returns exactly `feat(scanner): refactor scanner logic and audit scripts (Batch 2)`.
3. `git show --name-only --pretty=format: HEAD` returns a list of files that is a subset of the allowed Batch 2 files.
</verification>

<success_criteria>
- STAGE-02: Batch 2 files (hooks/gsd-read-injection-scanner.js, scripts/audit-tags.js, and hooks/gsd-check-update.js) are staged and committed using the conventional commit message: `feat(scanner): refactor scanner logic and audit scripts (Batch 2)`.
- No extra files from subsequent batches are staged or committed.
</success_criteria>

<output>
After completion, create `.planning/phases/37-stage-and-commit-scanner-logic/37-01-SUMMARY.md` using the summary template.
</output>
