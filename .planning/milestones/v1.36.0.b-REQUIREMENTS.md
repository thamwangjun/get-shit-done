# Requirements Archive: v1.36.0.b Fix Hooks Installation

**Archived:** 2026-04-17
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements — v1.36.0.b Fix Hooks Installation

## Milestone v1.36.0.b Requirements

### Hook Installation Fix

- [x] **FIX-01**: User running `node bin/install.js --claude --global` from a cloned repo gets all GSD hooks installed to `$HOME/.claude/hooks/` even when `hooks/dist/` has not been built
- [x] **FIX-02**: User sees a console notice when install.js triggers an on-demand build of `hooks/dist/` so the fallback is not silent
- [x] **FIX-03**: Regression test verifies hooks are installed correctly when `hooks/dist/` is absent at install time

## Future Requirements

(None identified — this is a targeted bug fix.)

## Out of Scope

- Changing the build step itself (`scripts/build-hooks.js`) — it works correctly; the issue is install.js not triggering it
- Committing `hooks/dist/` to the repo — it is intentionally gitignored as a build artifact
- Fixing any other install paths (npm publish path already works correctly via `prepublishOnly`)

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| FIX-01 | Phase 4 | Complete |
| FIX-02 | Phase 4 | Complete |
| FIX-03 | Phase 5 | Complete |
