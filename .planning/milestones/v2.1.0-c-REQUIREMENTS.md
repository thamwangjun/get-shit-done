# Requirements Archive: v2.1.0-c — Install-Time Content Materialization

**Archived:** 2026-05-29
**Milestone shipped:** 2026-05-29
**Status:** All active requirements satisfied

---

# Requirements: GSD — Prompt-Engineered Fork

**Defined:** 2026-05-28
**Core Value:** Every agent, command, and workflow file on `main` meets the fork's prompt engineering quality bar before it ships — upstream content additions are modified, not accepted verbatim.

## v2.1.0-c Requirements

Requirements for the Install-Time Content Materialization milestone. Each maps to roadmap phases.

### Resolver

> **Superseded by Eta pivot (Phase 45).** Phase 44 implemented a custom `resolveIncludes()` resolver; Phase 45 replaces it with Eta v4. These requirements are satisfied and then superseded — the implementation they describe no longer exists in the codebase.

- [x] **RESV-01**: `resolveIncludes(content, sourceRoot, seen)` function exists in `bin/install.js` and inlines `@~/.claude/` bare-line references by reading and substituting the referenced file content at source repo paths — **satisfied/superseded** (implemented Phase 44, then superseded by Eta pivot in Phase 45)
- [x] **RESV-02**: `resolveIncludes` also inlines `` !`cat $HOME/.claude/...` `` and `` !`cat ~/.claude/...` `` bare-line references — **satisfied/superseded**
- [x] **RESV-03**: Resolver detects and preserves the conditional `@~` expression in `execute-phase.md:619` — **satisfied/superseded**
- [x] **RESV-04**: Resolver skips `@~` and `` !`...` `` patterns inside fenced code blocks and `${...}` template expressions — **satisfied/superseded**
- [x] **RESV-05**: Resolver detects circular include chains via a `seen` Set and aborts with a clear error — **satisfied/superseded**
- [x] **RESV-06**: Resolver aborts with a clear error naming the source file and unresolvable path when a referenced file does not exist — **satisfied/superseded**
- [x] **RESV-07**: Resolver supports nested includes up to depth 3 — **satisfied/superseded** (depth guard implemented; function removed in Eta pivot)

### Integration

- [x] **INTG-01**: `eta` v4 added to `dependencies` in `package.json`; module-level Eta instance configured with default `<%`/`%>` delimiters, `autoEscape: false`, `useWith: true`, and `views` = repo root exists in `bin/install.js` — **satisfied** (Phase 45)
- [x] **INTG-02**: All install-time GSD static refs across `commands/gsd/` (55 files), `agents/` (7 files), `get-shit-done/workflows/` (19 files), and `get-shit-done/references/` (1 file) converted to `<%~ include('get-shit-done/X') %>` Eta tags — **satisfied** (Phase 45)
- [x] **INTG-03**: Runtime `.planning/` bare-line refs in the agents layer converted to `` !`cat .planning/X` `` form — **satisfied** (Phase 45)
- [x] **INTG-04**: Eta renderer wired into `copyWithPathReplacement()` as the first transform step; skills path: `renderEtaContent` wired into `wrappedConverter` in `runtime-artifact-layout.cjs` — **satisfied** (Phase 45 + Phase 47.1)
- [x] **INTG-05**: Eta renderer wired into the agent install loop as the first transform step — **satisfied** (Phase 45)
- [x] **INTG-06**: Skills path (`applyRuntimeContentRewritesInPlace`) confirmed as not requiring a renderer call — `SKILL.md` files contain 0 install-time include refs — **satisfied** (Phase 45)

### Tests

- [x] **TEST-01**: Test verifies that installed output files contain no unresolved `@~/.claude/` references; also verifies no unrendered `<%~` Eta directives survive in installed output — **satisfied** (Phase 46 + Phase 47 + Phase 47.1)
- [x] **TEST-02**: Test verifies the conditional `@~` expression in `execute-phase.md` is preserved verbatim in installed output — **satisfied** (Phase 46)
- [x] ~~**TEST-03**: Test verifies inlined reference content is present in installed agent files~~ — **descoped** (tool-name transformation orthogonal to Eta include resolution; deferred to future milestone)
- [x] **TEST-04**: Test verifies circular include detection — a file that includes itself causes a thrown error, not infinite recursion — **satisfied** (Phase 46)
- [x] **TEST-05**: Test verifies missing-file handling — an unresolvable reference causes a thrown error with a message naming the source file and missing path — **satisfied** (Phase 46)
- ~~**TEST-06**~~: **dropped** — installed agent size budgets provide no testing value; size varies by platform and profile selection (per CONTEXT.md D-11)

### Quality Gate

- [x] **GATE-01**: Full `npm test` passes after all changes with 0 regressions beyond pre-existing failures (7459 tests / 49 failures — better than baseline 7458/50) — **satisfied** (Phase 47, closed 2026-05-29)
- [x] **GATE-02**: Negative-framing scanner passes at 99/99 after all file edits — **satisfied** (Phase 47, closed 2026-05-29 via git commit 6c349066)
- [x] **GATE-03**: `grep -r '@~/.claude/' <install-dir>` on a fresh install across all supported runtimes returns 0 results — **satisfied** (Phase 47 + Phase 47.1, skills-path gap closed)

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| RESV-01 | Phase 44 | Satisfied/Superseded |
| RESV-02 | Phase 44 | Satisfied/Superseded |
| RESV-03 | Phase 44 | Satisfied/Superseded |
| RESV-04 | Phase 44 | Satisfied/Superseded |
| RESV-05 | Phase 44 | Satisfied/Superseded |
| RESV-06 | Phase 44 | Satisfied/Superseded |
| RESV-07 | Phase 44 | Satisfied/Superseded |
| INTG-01 | Phase 45 | Complete |
| INTG-02 | Phase 45 | Complete |
| INTG-03 | Phase 45 | Complete |
| INTG-04 | Phase 45 + 47.1 | Complete |
| INTG-05 | Phase 45 | Complete |
| INTG-06 | Phase 45 | Complete |
| TEST-01 | Phase 46 + 47 + 47.1 | Complete |
| TEST-02 | Phase 46 | Complete |
| TEST-03 | Phase 46 | Descoped |
| TEST-04 | Phase 46 | Complete |
| TEST-05 | Phase 46 | Complete |
| TEST-06 | Phase 46 | Dropped (CONTEXT.md D-11) |
| GATE-01 | Phase 47 | Complete |
| GATE-02 | Phase 47 | Complete |
| GATE-03 | Phase 47 + 47.1 | Complete |

**Coverage:**
- v2.1.0-c requirements: 22 total
- Active (satisfied): 19/19
- Superseded by Eta pivot: RESV-01..07 (7)
- Descoped: TEST-03 (1)
- Dropped: TEST-06 (1)
- Unmapped: 0 ✓

---
*Requirements defined: 2026-05-28*
*Archived: 2026-05-29 at milestone close*
