# Requirements Archive: v1.37.1a Do-Not Framing Pass

**Archived:** 2026-04-23
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: GSD Prompt-Engineered Fork

**Defined:** 2026-04-22
**Core Value:** Every prompt file on `thamw-main` meets the fork's prompt engineering quality bar before it ships

## v1.37.1a Requirements

Maintenance pass: convert all bare "do not" directives to positive instructions. The negative framing phrase must not exist in the file after the fix — rewrites must specify what to do, not what to avoid.

### Agent Fixes

- [x] **FRAMING-01**: `agents/gsd-assumptions-analyzer.md:111` — bare `Do NOT generate more areas` replaced with positive instruction specifying the correct area count limit
  - Fixed: `Do NOT generate more areas than the calibration tier specifies` → `Keep area count within the tier limit defined in `<calibration_tiers>` above`
- [x] **FRAMING-02**: `agents/gsd-code-fixer.md:138` — bare `Do NOT skip the fix` replaced with positive instruction to always apply the fix regardless of syntax checker availability
  - Fixed: `Do NOT skip the fix just because syntax checking is unavailable` → `Apply the fix even when syntax checking is unavailable`
- [x] **FRAMING-03**: `agents/gsd-code-fixer.md:240` — bare `Do NOT create REVIEW-FIX.md` replaced with positive exit instruction
  - Fixed: `Do NOT create REVIEW-FIX.md` → bullet deleted
- [x] **FRAMING-04**: `agents/gsd-code-fixer.md:344` — bare `Do NOT leave uncommitted changes` replaced with positive instruction to commit all changes before continuing
  - Fixed: `Do NOT leave uncommitted changes` → `Restore all files to pre-fix state before continuing`
- [x] **FRAMING-05**: `agents/gsd-doc-verifier.md:92` — list header `Do NOT verify the following:` replaced with affirmative equivalent (e.g. "Skip verification for the following:")
  - Fixed: `Do NOT verify the following:` → `Skip verification for the following:`
- [x] **FRAMING-06**: `agents/gsd-user-profiler.md:88` — bare `Do not proceed to message analysis` replaced with positive sequencing gate (load rubric first, then proceed)
  - Fixed: `Do not proceed to message analysis until the rubric is loaded.` → `Load the rubric fully before proceeding to message analysis.`

### Workflow Fixes

- [x] **FRAMING-07**: `get-shit-done/workflows/analyze-dependencies.md:100` — bare `Do not reorder phases` replaced with positive instruction to preserve phase order
  - Fixed: `Do not reorder phases` → `Preserve the existing phase order — relocate only the dependency field`
- [x] **FRAMING-08**: `get-shit-done/workflows/discuss-phase.md:172` — bare `Do not continue with the steps below` replaced with positive stop instruction
  - Fixed: `Do not continue with the steps below` → `Stop here — power mode handles all remaining steps`
- [x] **FRAMING-09**: `get-shit-done/workflows/execute-plan.md:203` — bare `do not auto-fix pre-existing issues` replaced with positive scope boundary
  - Fixed: `do not auto-fix pre-existing issues unrelated to current task` → `Scope auto-fixes to issues introduced by the current task only — leave pre-existing issues untouched`
- [x] **FRAMING-10**: `get-shit-done/workflows/import.md:276` — list header `Do NOT:` replaced with affirmative equivalent
  - Fixed: `Do NOT:` block header + 7 negative list items → `Required Patterns` section header + 7 affirmative imperative conventions (full block rewritten)
- [x] **FRAMING-11**: `get-shit-done/workflows/transition.md:567` — bare `Do NOT suggest /gsd-complete-milestone` replaced with positive instruction stating which commands to offer
  - Fixed: `Do NOT suggest /gsd-complete-milestone or /gsd-new-milestone.` → line deleted; `**Stop here.**` gate already covers the intent
- [x] **FRAMING-12**: `get-shit-done/workflows/transition.md:568` — bare `Do NOT auto-invoke any further slash commands` replaced with positive instruction to wait for explicit user request
  - Fixed: `Do NOT auto-invoke any further slash commands.` → line deleted; `**Stop here.**` gate already covers the intent
- [x] **FRAMING-13**: `get-shit-done/workflows/verify-phase.md:241` — bare `Do NOT invent example inputs` replaced with positive instruction to use actual codebase examples
  - Fixed: `Do NOT invent example inputs.` → `Source inputs exclusively from actual test fixtures and codebase examples.`

### Reference Fixes

- [x] **FRAMING-14**: `get-shit-done/references/planner-source-audit.md:30` — list header `Do not flag these as MISSING:` replaced with affirmative equivalent
  - Fixed: `Do not flag these as MISSING:` → `Treat these as expected and exclude them from MISSING flags:`

### Command Fixes

- [x] **FRAMING-15**: `commands/gsd/docs-update.md:42` — bare `Do not infer that a flag is active` replaced with positive inference rule
  - Fixed: `Do not infer that a flag is active just because it is documented in this prompt` → `Treat a flag as active only if its literal token is present in \`$ARGUMENTS\``
- [x] **FRAMING-16**: `commands/gsd/execute-phase.md:54` — same fix as FRAMING-15; `tests/execute-phase-active-flags.test.cjs:50` assertion updated to match new text
  - Fixed: `Do not infer that a flag is active just because it is documented in this prompt` → `Treat a flag as active only if its literal token is present in \`$ARGUMENTS\``; `tests/execute-phase-active-flags.test.cjs` line 50 assertion updated to assert new positive text
- [x] **FRAMING-17**: `commands/gsd/reapply-patches.md:271` — bare `Do not proceed to cleanup` replaced with positive sequencing gate
  - Fixed: `Do not proceed to cleanup until the user confirms they have resolved all unverified hunks.` → `Proceed to Step 6 only after the user confirms all unverified hunks are resolved.`

### Test Gate

- [x] **TEST-05**: `corpus scan — DO NOT primary directives (case-insensitive)` suite passes — all 4 tests green; no `do not` (any capitalisation) present in any fixed line

## Out of Scope

| Feature | Reason |
|---------|--------|
| Em-dash complement pattern ("do not X — use Y") | Replacement rule requires full positive rewrite; negative phrase must not remain |
| Fixing violations in `get-shit-done/templates/` | Templates are user-facing boilerplate, not AI prompts — established fork precedent |
| Fixing violations in `sdk/` or `tests/` | Out of scan scope per `SCAN_DIRS` in the test file |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| FRAMING-01 | Phase 13 | Complete |
| FRAMING-02 | Phase 13 | Complete |
| FRAMING-03 | Phase 13 | Complete |
| FRAMING-04 | Phase 13 | Complete |
| FRAMING-05 | Phase 13 | Complete |
| FRAMING-06 | Phase 13 | Complete |
| FRAMING-07 | Phase 14 | Complete |
| FRAMING-08 | Phase 14 | Complete |
| FRAMING-09 | Phase 14 | Complete |
| FRAMING-10 | Phase 14 | Complete |
| FRAMING-11 | Phase 14 | Complete |
| FRAMING-12 | Phase 14 | Complete |
| FRAMING-13 | Phase 14 | Complete |
| FRAMING-14 | Phase 14 | Complete |
| FRAMING-15 | Phase 14 | Complete |
| FRAMING-16 | Phase 14 | Complete |
| FRAMING-17 | Phase 14 | Complete |
| TEST-05 | Phase 15 | Complete |

**Coverage:**
- v1.37.1a requirements: 18 total
- Mapped to phases: 18
- Unmapped: 0 ✓

---
*Requirements defined: 2026-04-22*
*Last updated: 2026-04-22 — traceability updated after roadmap creation (Phases 13–15)*
