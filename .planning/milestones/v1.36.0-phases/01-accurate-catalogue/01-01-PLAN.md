---
phase: 01-accurate-catalogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CATALOGUE.json
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - CAT-06

must_haves:
  truths:
    - "CATALOGUE.json total is 250"
    - "CATALOGUE.json counts match: commands=73, workflows=75, agents=31, references=40, templates=31"
    - "All 5 new command entries are present with correct paths and descriptions"
    - "All 3 new workflow entries are present with correct paths and descriptions"
    - "All 7 new agent entries are present with correct paths and descriptions"
    - "All 7 new reference entries are present with correct paths and descriptions"
    - "The new template entry AI-SPEC.md is present with correct path and description"
    - "JSON is syntactically valid — node can require() the file without error"
  artifacts:
    - path: "CATALOGUE.json"
      provides: "Complete index of all 250 GSD prompt files"
      contains: "\"total\": 250"
  key_links:
    - from: "CATALOGUE.json counts.commands"
      to: "CATALOGUE.json commands array length"
      via: "declared value must equal actual array length"
      pattern: "\"commands\": 73"
    - from: "CATALOGUE.json counts.agents"
      to: "CATALOGUE.json agents array length"
      via: "declared value must equal actual array length"
      pattern: "\"agents\": 31"
---

<objective>
Add 23 missing v1.36.0 file entries to CATALOGUE.json and update the counts and total to 250.

Purpose: Phase 2 (applying fork standards) operates on the list of files CATALOGUE.json declares. A catalogue missing 23 files means Phase 2 would skip those files. This plan makes the catalogue accurate before Phase 2 begins.
Output: CATALOGUE.json with 250 entries, correct per-category counts, and valid JSON.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-accurate-catalogue/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 23 missing entries to CATALOGUE.json and update counts</name>
  <files>CATALOGUE.json</files>

  <read_first>
    - CATALOGUE.json — read the full current file before making any edits; know the exact current state before writing
    - .planning/phases/01-accurate-catalogue/01-RESEARCH.md — the research file contains all 23 verified descriptions and the exact alphabetical insertion positions; use these directly
  </read_first>

  <action>
Read CATALOGUE.json in full. Then write the updated CATALOGUE.json with the following 23 entries inserted at the specified positions. Use the exact `file` and `description` values below — do not paraphrase or shorten descriptions.

**COMMANDS array — insert 5 entries (CAT-01):**

After `{ "file": "commands/gsd/add-todo.md", ... }`, before `{ "file": "commands/gsd/analyze-dependencies.md", ... }`, insert:
```json
{ "file": "commands/gsd/ai-integration-phase.md", "description": "Generate AI design contract (AI-SPEC.md) for phases that involve building AI systems — framework selection, implementation guidance from official docs, and evaluation strategy" }
```

After `{ "file": "commands/gsd/docs-update.md", ... }`, before `{ "file": "commands/gsd/execute-phase.md", ... }`, insert:
```json
{ "file": "commands/gsd/eval-review.md", "description": "Retroactively audit an executed AI phase's evaluation coverage — scores each eval dimension as COVERED/PARTIAL/MISSING and produces an actionable EVAL-REVIEW.md with remediation plan" }
```

After `{ "file": "commands/gsd/explore.md", ... }`, before `{ "file": "commands/gsd/fast.md", ... }`, insert:
```json
{ "file": "commands/gsd/extract_learnings.md", "description": "Extract decisions, lessons, patterns, and surprises from completed phase artifacts" }
```

After `{ "file": "commands/gsd/forensics.md", ... }`, before `{ "file": "commands/gsd/health.md", ... }`, insert (two entries — from-gsd2 then graphify):
```json
{ "file": "commands/gsd/from-gsd2.md", "description": "Import a GSD-2 (.gsd/) project back to GSD v1 (.planning/) format" },
{ "file": "commands/gsd/graphify.md", "description": "Build, query, and inspect the project knowledge graph in .planning/graphs/" }
```

**WORKFLOWS array — insert 3 entries (CAT-02):**

After `{ "file": "get-shit-done/workflows/add-todo.md", ... }`, before `{ "file": "get-shit-done/workflows/analyze-dependencies.md", ... }`, insert:
```json
{ "file": "get-shit-done/workflows/ai-integration-phase.md", "description": "Generates AI design contract (AI-SPEC.md) for phases involving AI systems — orchestrates framework-selector, ai-researcher, domain-researcher, and eval-planner agents" }
```

After `{ "file": "get-shit-done/workflows/docs-update.md", ... }`, before `{ "file": "get-shit-done/workflows/execute-phase.md", ... }`, insert:
```json
{ "file": "get-shit-done/workflows/eval-review.md", "description": "Retroactive audit of an implemented AI phase's evaluation coverage — produces scored EVAL-REVIEW.md with gap analysis and remediation plan" }
```

After `{ "file": "get-shit-done/workflows/explore.md", ... }`, before `{ "file": "get-shit-done/workflows/fast.md", ... }`, insert:
```json
{ "file": "get-shit-done/workflows/extract_learnings.md", "description": "Extracts decisions, lessons learned, patterns, and surprises from completed phase artifacts into a structured LEARNINGS.md file" }
```

**AGENTS array — insert 7 entries (CAT-03), strict alphabetical order:**

After `{ "file": "agents/gsd-advisor-researcher.md", ... }`, before `{ "file": "agents/gsd-assumptions-analyzer.md", ... }`, insert:
```json
{ "file": "agents/gsd-ai-researcher.md", "description": "Researches a chosen AI framework's official docs to produce implementation-ready guidance — best practices, syntax, core patterns, and pitfalls distilled for the specific use case. Writes the Framework Quick Reference and Implementation Guidance sections of AI-SPEC.md. Spawned by /gsd-ai-integration-phase orchestrator." }
```

After `{ "file": "agents/gsd-debugger.md", ... }`, before `{ "file": "agents/gsd-doc-verifier.md", ... }`, insert:
```json
{ "file": "agents/gsd-debug-session-manager.md", "description": "Manages multi-cycle /gsd-debug checkpoint and continuation loop in isolated context. Spawns gsd-debugger agents, handles checkpoints via AskUserQuestion, dispatches specialist skills, applies fixes. Returns compact summary to main context. Spawned by /gsd-debug command." }
```

After `{ "file": "agents/gsd-doc-writer.md", ... }`, before `{ "file": "agents/gsd-executor.md", ... }`, insert:
```json
{ "file": "agents/gsd-domain-researcher.md", "description": "Researches the business domain and real-world application context of the AI system being built. Surfaces domain expert evaluation criteria, industry-specific failure modes, regulatory context, and what \"good\" looks like for practitioners in this field — before the eval-planner turns it into measurable rubrics. Spawned by /gsd-ai-integration-phase orchestrator." }
```

After `{ "file": "agents/gsd-executor.md", ... }`, before `{ "file": "agents/gsd-integration-checker.md", ... }`, insert (three entries in order — eval-auditor, eval-planner, framework-selector):
```json
{ "file": "agents/gsd-eval-auditor.md", "description": "Retroactive audit of an implemented AI phase's evaluation coverage. Checks implementation against the AI-SPEC.md evaluation plan. Scores each eval dimension as COVERED/PARTIAL/MISSING. Produces a scored EVAL-REVIEW.md with findings, gaps, and remediation guidance. Spawned by /gsd-eval-review orchestrator." },
{ "file": "agents/gsd-eval-planner.md", "description": "Designs a structured evaluation strategy for an AI phase. Identifies critical failure modes, selects eval dimensions with rubrics, recommends tooling, and specifies the reference dataset. Writes the Evaluation Strategy, Guardrails, and Production Monitoring sections of AI-SPEC.md. Spawned by /gsd-ai-integration-phase orchestrator." },
{ "file": "agents/gsd-framework-selector.md", "description": "Presents an interactive decision matrix to surface the right AI/LLM framework for the user's specific use case. Produces a scored recommendation with rationale. Spawned by /gsd-ai-integration-phase and /gsd-select-framework orchestrators." }
```

After `{ "file": "agents/gsd-nyquist-auditor.md", ... }`, before `{ "file": "agents/gsd-phase-researcher.md", ... }`, insert:
```json
{ "file": "agents/gsd-pattern-mapper.md", "description": "Analyzes codebase for existing patterns and produces PATTERNS.md mapping new files to closest analogs. Read-only codebase analysis spawned by /gsd-plan-phase orchestrator before planning." }
```

**REFERENCES array — insert 7 entries (CAT-04), strict alphabetical order:**

After `{ "file": "get-shit-done/references/artifact-types.md", ... }`, before `{ "file": "get-shit-done/references/checkpoints.md", ... }`, insert (two entries — ai-evals then ai-frameworks):
```json
{ "file": "get-shit-done/references/ai-evals.md", "description": "AI evaluation reference: frameworks, metrics, and rubric patterns for gsd-eval-planner and gsd-eval-auditor" },
{ "file": "get-shit-done/references/ai-frameworks.md", "description": "AI framework decision matrix for framework selection and comparison" }
```

After `{ "file": "get-shit-done/references/domain-probes.md", ... }`, before `{ "file": "get-shit-done/references/gate-prompts.md", ... }`, insert:
```json
{ "file": "get-shit-done/references/executor-examples.md", "description": "Executor extended examples — on-demand reference for gsd-executor agent" }
```

After `{ "file": "get-shit-done/references/gate-prompts.md", ... }`, before `{ "file": "get-shit-done/references/git-integration.md", ... }`, insert:
```json
{ "file": "get-shit-done/references/gates.md", "description": "Canonical gate types taxonomy used across GSD workflows" }
```

After `{ "file": "get-shit-done/references/git-planning-commit.md", ... }`, before `{ "file": "get-shit-done/references/model-profile-resolution.md", ... }`, insert:
```json
{ "file": "get-shit-done/references/ios-scaffold.md", "description": "iOS app scaffold rules and patterns for creating new iOS app targets" }
```

After `{ "file": "get-shit-done/references/planning-config.md", ... }`, before `{ "file": "get-shit-done/references/planner-gap-closure.md", ... }`, insert:
```json
{ "file": "get-shit-done/references/planner-antipatterns.md", "description": "Planner anti-patterns and specificity examples — on-demand reference for gsd-planner" }
```

After `{ "file": "get-shit-done/references/planner-reviews.md", ... }`, before `{ "file": "get-shit-done/references/planner-revision.md", ... }`, insert:
```json
{ "file": "get-shit-done/references/planner-source-audit.md", "description": "Planner source audit and authority limits — multi-source coverage audit format and constraints" }
```

**TEMPLATES array — append 1 entry at the end (CAT-05):**

After `{ "file": "get-shit-done/templates/verification-report.md", ... }` (currently the last entry), append:
```json
{ "file": "get-shit-done/templates/AI-SPEC.md", "description": "AI integration specification template for phases that involve building AI systems" }
```

**UPDATE counts and total (CAT-06):**

Change the top-level fields from:
```json
"total": 227,
"counts": {
  "commands": 68,
  "workflows": 72,
  "agents": 24,
  "references": 33,
  "templates": 30
}
```

To:
```json
"total": 250,
"counts": {
  "commands": 73,
  "workflows": 75,
  "agents": 31,
  "references": 40,
  "templates": 31
}
```

**JSON safety rules:**
- All paths are repo-relative without leading `./` — e.g., `commands/gsd/ai-integration-phase.md` not `./commands/gsd/...`
- The `gsd-domain-researcher.md` description contains a double-quote character inside the string (`"good"`). Escape it as `\"good\"` in the JSON string value.
- Do NOT re-sort any array — only insert at the positions specified above.
- After writing, run `node -e "require('./CATALOGUE.json'); console.log('JSON valid')"` at the repo root to confirm no syntax errors.
  </action>

  <acceptance_criteria>
    - `node -e "require('./CATALOGUE.json'); console.log('valid')"` exits 0 and prints `valid`
    - `node -e "const c=require('./CATALOGUE.json'); console.log(c.total)"` prints `250`
    - `node -e "const c=require('./CATALOGUE.json'); console.log(JSON.stringify(c.counts))"` prints `{"commands":73,"workflows":75,"agents":31,"references":40,"templates":31}`
    - `grep -c '"file"' CATALOGUE.json` prints `250` (one `"file"` key per entry)
    - `grep '"commands/gsd/ai-integration-phase.md"' CATALOGUE.json` returns a match
    - `grep '"commands/gsd/eval-review.md"' CATALOGUE.json` returns a match
    - `grep '"commands/gsd/extract_learnings.md"' CATALOGUE.json` returns a match
    - `grep '"commands/gsd/from-gsd2.md"' CATALOGUE.json` returns a match
    - `grep '"commands/gsd/graphify.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/workflows/ai-integration-phase.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/workflows/eval-review.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/workflows/extract_learnings.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-ai-researcher.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-debug-session-manager.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-domain-researcher.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-eval-auditor.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-eval-planner.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-framework-selector.md"' CATALOGUE.json` returns a match
    - `grep '"agents/gsd-pattern-mapper.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/ai-evals.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/ai-frameworks.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/executor-examples.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/gates.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/ios-scaffold.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/planner-antipatterns.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/references/planner-source-audit.md"' CATALOGUE.json` returns a match
    - `grep '"get-shit-done/templates/AI-SPEC.md"' CATALOGUE.json` returns a match
  </acceptance_criteria>

  <verify>
    <automated>node -e "require('./CATALOGUE.json'); console.log('JSON valid')" && node -e "const c=require('./CATALOGUE.json'); console.log(c.total)" && node -e "const c=require('./CATALOGUE.json'); console.log(JSON.stringify(c.counts))"</automated>
  </verify>

  <done>CATALOGUE.json has 250 entries. counts fields (commands=73, workflows=75, agents=31, references=40, templates=31) each equal the actual array lengths. total=250. JSON is syntactically valid. All 23 v1.36.0 files have entries.</done>
</task>

<task type="auto">
  <name>Task 2: Verify counts, array lengths, and no stale entries</name>
  <files>CATALOGUE.json</files>

  <read_first>
    - CATALOGUE.json — read after Task 1 completes to confirm the written state
  </read_first>

  <action>
Run these verification commands from the repo root:

1. JSON syntax check:
```bash
node -e "require('./CATALOGUE.json'); console.log('JSON valid')"
```
Must print `JSON valid` and exit 0. If it errors, the JSON has a syntax problem — go back and fix.

2. Count verification:
```bash
node -e "
const c = require('./CATALOGUE.json');
const cats = ['commands','workflows','agents','references','templates'];
let ok = true;
cats.forEach(k => {
  const match = c.counts[k] === c[k].length;
  console.log(k + ': declared=' + c.counts[k] + ' actual=' + c[k].length + (match ? ' OK' : ' MISMATCH'));
  if (!match) ok = false;
});
const sum = cats.reduce((s,k) => s + c[k].length, 0);
const totalMatch = c.total === sum;
console.log('total: declared=' + c.total + ' sum=' + sum + (totalMatch ? ' OK' : ' MISMATCH'));
if (!ok || !totalMatch) process.exit(1);
"
```
Must exit 0. Every line must end in `OK`.

3. No stale entries check — confirm every path in CATALOGUE.json exists on disk:
```bash
node -e "
const c = require('./CATALOGUE.json');
const fs = require('fs');
const cats = ['commands','workflows','agents','references','templates'];
let stale = [];
cats.forEach(k => c[k].forEach(e => { if (!fs.existsSync(e.file)) stale.push(e.file); }));
if (stale.length) { console.error('STALE:', stale); process.exit(1); }
console.log('No stale entries');
"
```
Must print `No stale entries` and exit 0.
  </action>

  <acceptance_criteria>
    - Command 1 exits 0 and prints `JSON valid`
    - Command 2 exits 0 and every output line ends in `OK`
    - Command 3 exits 0 and prints `No stale entries`
  </acceptance_criteria>

  <verify>
    <automated>node -e "require('./CATALOGUE.json'); console.log('JSON valid')" && node -e "const c=require('./CATALOGUE.json'); const cats=['commands','workflows','agents','references','templates']; cats.forEach(k=>{const ok=c.counts[k]===c[k].length; if(!ok)process.exit(1)}); console.log('counts match')" && node -e "const c=require('./CATALOGUE.json'),fs=require('fs'); const cats=['commands','workflows','agents','references','templates']; let stale=[]; cats.forEach(k=>c[k].forEach(e=>{if(!fs.existsSync(e.file))stale.push(e.file)})); if(stale.length){process.exit(1)} console.log('no stale entries')"</automated>
  </verify>

  <done>All three verification commands pass. CATALOGUE.json is internally consistent, counts match array lengths, and no entry points to a missing file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor | This plan provides exact JSON values; executor writes them verbatim |
| CATALOGUE.json→consumers | Phase 2 reads CATALOGUE.json to determine which files to process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | CATALOGUE.json write | accept | File is a local repo file with no authentication boundary. Git history provides audit trail. Low-value target with no secrets or user input. |
| T-01-02 | Denial | JSON syntax error breaks downstream phases | mitigate | Task 2 runs `node -e "require('./CATALOGUE.json')"` as automated gate — syntactically invalid JSON causes process.exit(1) and blocks plan completion |
| T-01-03 | Information Disclosure | n/a — no PII or secrets in file listing | accept | CATALOGUE.json contains only public file paths and descriptions. No sensitive data. |

Note: ASVS categories V2–V6 (auth, session, access control, crypto, input validation) do not apply to this phase. The phase edits a static data file with no user input, network access, or authentication.
</threat_model>

<verification>
Phase 1 complete when:

1. `node -e "require('./CATALOGUE.json'); console.log('valid')"` exits 0
2. `node -e "const c=require('./CATALOGUE.json'); console.log(c.total)"` prints `250`
3. `node -e "const c=require('./CATALOGUE.json'); console.log(JSON.stringify(c.counts))"` prints `{"commands":73,"workflows":75,"agents":31,"references":40,"templates":31}`
4. All 23 new entries present (grep each path — see Task 1 acceptance_criteria for full list)
5. No stale entries — every path in CATALOGUE.json exists on disk
</verification>

<success_criteria>
1. CATALOGUE.json total=250 with counts commands=73, workflows=75, agents=31, references=40, templates=31
2. All 23 v1.36.0 files have entries in the correct category array
3. JSON is syntactically valid
4. No existing entries were removed or modified
5. Phase 2 has a complete, accurate file list to operate against
</success_criteria>

<output>
After completion, create `.planning/phases/01-accurate-catalogue/01-01-SUMMARY.md`
</output>
