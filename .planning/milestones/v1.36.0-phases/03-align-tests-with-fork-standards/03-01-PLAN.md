---
phase: 03-align-tests-with-fork-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ios-scaffold-safety.test.cjs
  - get-shit-done/references/ios-scaffold.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "ios-scaffold-safety.test.cjs no longer contains the prohibitsPackageSwift or prohibitsExecutableTarget test blocks"
    - "ios-scaffold.md line 13 code comment uses a positive label, not a DO NOT directive"
    - "The 5 remaining assertions in ios-scaffold-safety.test.cjs continue to pass unchanged"
  artifacts:
    - path: "tests/ios-scaffold-safety.test.cjs"
      provides: "Pruned test file — 2 failing prohibition-check blocks removed, 5 positive assertions intact"
    - path: "get-shit-done/references/ios-scaffold.md"
      provides: "Reference file with positive code comment on line 13"
  key_links:
    - from: "tests/ios-scaffold-safety.test.cjs"
      to: "get-shit-done/references/ios-scaffold.md"
      via: "fs.readFileSync(IOS_SCAFFOLD_REF)"
      pattern: "IOS_SCAFFOLD_REF"
---

<objective>
Remove 2 prohibition-style test assertions from `ios-scaffold-safety.test.cjs` (per D-01/D-02) and replace the `DO NOT USE` code comment in `ios-scaffold.md` with a positive label (per D-04).

Purpose: The 2 removed tests checked for keyword sets (`NEVER`, `never`, `prohibited`, `do not`, `Do not`, `must not`) that contradict the fork's positive framing standard. Phase 2 replaced the upstream NEVER body paragraph with positive framing; the tests were written against upstream text and must be updated, not reverted. The code comment fix completes the positive-framing pass on ios-scaffold.md.

Output: Two files modified with surgical deletions/replacements. No new logic. The 5 remaining ios-scaffold assertions still pass.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-align-tests-with-fork-standards/03-CONTEXT.md
@.planning/phases/03-align-tests-with-fork-standards/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 2 prohibition-check test blocks from ios-scaffold-safety.test.cjs</name>
  <files>tests/ios-scaffold-safety.test.cjs</files>

  <read_first>
    - tests/ios-scaffold-safety.test.cjs — read the complete file to confirm current line numbers before any edit; locate the exact blocks at lines 37–53 and 55–71
  </read_first>

  <action>
Remove the entire test block at lines 37–53 (the `reference prohibits Package.swift as primary build system for iOS apps` test). The block to remove starts with `test('reference prohibits Package.swift` and ends with the closing `});` at line 53.

Remove the entire test block at lines 55–71 (the `reference prohibits .executableTarget for iOS apps` test). The block to remove starts with `test('reference prohibits .executableTarget` and ends with the closing `});` at line 71.

After removal, the first `describe` block should contain exactly 4 `test(...)` calls:
1. `reference file exists at get-shit-done/references/ios-scaffold.md` (currently line 30)
2. `reference requires project.yml (XcodeGen spec) for iOS app scaffolding` (currently line 73)
3. `reference requires xcodegen generate command` (currently line 81)
4. `reference documents iOS deployment target compatibility` (currently line 89)

The two outer `describe` blocks (`gsd-executor.md references ios-scaffold guidance` and `universal-anti-patterns.md documents iOS SPM anti-pattern`) are unchanged.

No blank lines or comments need to be inserted where the blocks were removed — close the gap naturally.

Rationale (per D-01, D-02): These tests checked for prohibition keywords (`NEVER`, `never`, `prohibited`, `do not`, `Do not`, `must not`) that are no longer present after Phase 2's positive-framing conversion. The tests should validate what the file instructs the AI to do, not assert the presence of negative directives.
  </action>

  <verify>
    <automated>node --test tests/ios-scaffold-safety.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "prohibitsPackageSwift\|prohibitsExecutableTarget\|NEVER\|do not\|Do not\|must not" tests/ios-scaffold-safety.test.cjs` returns no matches (the removed assertions are gone)
    - `grep -c "test(" tests/ios-scaffold-safety.test.cjs` returns 6 (4 in first describe + 1 in second + 1 in third; was 8)
    - `node --test tests/ios-scaffold-safety.test.cjs` exits 0 with `pass 6, fail 0` (or `pass 4` counting only the first describe's tests that reference ios-scaffold.md directly — accept any count so long as fail=0)
    - The strings `reference requires project.yml`, `reference requires xcodegen`, `reference documents iOS deployment target` remain in the file (retained tests not disturbed)
  </acceptance_criteria>

  <done>Both prohibition-check test blocks are removed. `node --test tests/ios-scaffold-safety.test.cjs` exits with 0 failures. The 5 remaining ios-scaffold assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DO NOT USE code comment in ios-scaffold.md</name>
  <files>get-shit-done/references/ios-scaffold.md</files>

  <read_first>
    - get-shit-done/references/ios-scaffold.md — read to confirm line 13 contains `// Package.swift — DO NOT USE for iOS apps` and verify lines 7 and 11 (which must not change)
  </read_first>

  <action>
On line 13, replace:
```
// Package.swift — DO NOT USE for iOS apps
```
with:
```
// Incorrect — produces macOS CLI, not an iOS app
```

This is inside the Swift code block that starts at line 12 (` ```swift `) and ends after line 15 (` ``` `). Change only line 13.

Leave unchanged (per D-05, D-06):
- Line 7: `## Critical Rule: Never Use Package.swift as the Primary Build System for iOS Apps`
- Line 11: `**Prohibited pattern:**`

All other content in the file is unchanged.

Rationale (per D-04): `DO NOT USE` is a bare negative imperative. The replacement `// Incorrect — produces macOS CLI, not an iOS app` labels the wrong pattern positively by describing the actual outcome, consistent with the fork's positive framing standard.
  </action>

  <verify>
    <automated>node -e "const c = require('fs').readFileSync('get-shit-done/references/ios-scaffold.md','utf-8'); process.exit(c.includes('Incorrect — produces macOS CLI') && !c.includes('DO NOT USE') ? 0 : 1)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "DO NOT USE" get-shit-done/references/ios-scaffold.md` returns no output (the directive is gone)
    - `grep "Incorrect — produces macOS CLI" get-shit-done/references/ios-scaffold.md` returns exactly 1 match (the replacement is present)
    - `grep "Never Use Package.swift" get-shit-done/references/ios-scaffold.md` returns 1 match (section heading untouched per D-05)
    - `grep "Prohibited pattern" get-shit-done/references/ios-scaffold.md` returns 1 match (subheading untouched per D-06)
  </acceptance_criteria>

  <done>Line 13 of ios-scaffold.md reads `// Incorrect — produces macOS CLI, not an iOS app`. The `DO NOT USE` directive is absent. Section heading and `**Prohibited pattern:**` subheading are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → production file content | Tests assert strings present in ios-scaffold.md; if test is too permissive, actual regressions in the reference file go undetected |
| fork standards → test expectations | Tests must reflect fork standards, not upstream conventions; wrong direction of change (reverting fork edits) would pass tests while violating the fork standard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | ios-scaffold-safety.test.cjs | mitigate | Remove only the 2 prohibition-check blocks; grep after edit confirms 6 remaining test() calls and no prohibition keyword assertions remain |
| T-03-01-02 | Repudiation | get-shit-done/references/ios-scaffold.md | mitigate | D-05 requires the section heading `Never Use Package.swift` to remain — its presence is verified by grep in acceptance_criteria, proving the safety intent is preserved even though the code-comment keyword is changed |
| T-03-01-03 | Information Disclosure | test quality degradation | mitigate | The 5 remaining assertions (`project.yml`, `xcodegen`, deployment target, executor reference, anti-patterns reference) still verify that ios-scaffold.md provides correct, actionable guidance — the structural safety net is intact |
| T-03-01-04 | Elevation of Privilege | scope creep into D-06 | accept | `**Prohibited pattern:**` subheading is explicitly out of scope per D-06; no REQUIREMENTS.md item targets it; leaving it unchanged is the correct call |
</threat_model>

<verification>
Run after both tasks complete:

```bash
node --test tests/ios-scaffold-safety.test.cjs
```

Expected: `fail 0`. All remaining assertions pass. The 2 removed test blocks no longer appear in output.
</verification>

<success_criteria>
- `node --test tests/ios-scaffold-safety.test.cjs` exits 0 with 0 failures
- `grep "DO NOT USE" get-shit-done/references/ios-scaffold.md` returns no output
- `grep "Incorrect — produces macOS CLI" get-shit-done/references/ios-scaffold.md` returns 1 match
- `grep -c "prohibitsPackageSwift\|prohibitsExecutableTarget" tests/ios-scaffold-safety.test.cjs` returns 0
- TEST-01 and TEST-02 satisfied: the test no longer asserts prohibition keywords; the file no longer uses prohibition directives in code comments
</success_criteria>

<output>
After completion, create `.planning/phases/03-align-tests-with-fork-standards/03-01-SUMMARY.md` using the summary template. Record: files modified, exact lines changed, test pass count before/after, and confirmation that D-01, D-02, D-04, D-05, D-06 were honored.
</output>
