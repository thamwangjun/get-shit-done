---
phase: 03-align-tests-with-fork-standards
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/bug-patterns-reference.test.cjs
  - tests/execute-phase-wave.test.cjs
autonomous: true
requirements:
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "bug-patterns-reference.test.cjs no longer asserts that common-bug-patterns.md starts with a markdown heading"
    - "execute-phase-wave.test.cjs partial-wave guardrail test checks positive-framing strings, not the removed Do NOT directives"
    - "npm test exits with 0 failures across all 3932+ tests"
    - "No production file (execute-phase.md, common-bug-patterns.md) was changed to make tests pass"
  artifacts:
    - path: "tests/bug-patterns-reference.test.cjs"
      provides: "Updated test — startsWith heading assertion removed, includes('---') retained"
    - path: "tests/execute-phase-wave.test.cjs"
      provides: "Updated test — 2 string literals match current positive-framing text in execute-phase.md"
  key_links:
    - from: "tests/execute-phase-wave.test.cjs"
      to: "get-shit-done/workflows/execute-phase.md"
      via: "content.includes() string match"
      pattern: "phase verification is handled separately|ROADMAP.md and STATE.md unchanged"
    - from: "tests/bug-patterns-reference.test.cjs"
      to: "get-shit-done/references/common-bug-patterns.md"
      via: "content.includes('---')"
      pattern: "includes.*---"
---

<objective>
Remove the `startsWith('# Common Bug Patterns')` assertion from `bug-patterns-reference.test.cjs` (per D-07/D-08), and update 2 string literals in `execute-phase-wave.test.cjs` to match the positive-framing replacement text now in `execute-phase.md`. Then run the full test suite to confirm 0 failures.

Purpose: Both test failures are caused by Phase 2 converting upstream negative directives to positive framing. The correct fix is to update the tests to match the positive text — not to revert the Phase 2 changes. This completes TEST-03 and closes the final failing test (TEST-04).

Output: Two test files with surgical edits. Full suite green.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-align-tests-with-fork-standards/03-CONTEXT.md
@.planning/phases/03-align-tests-with-fork-standards/03-RESEARCH.md
@.planning/phases/03-align-tests-with-fork-standards/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove startsWith assertion from bug-patterns-reference.test.cjs</name>
  <files>tests/bug-patterns-reference.test.cjs</files>

  <read_first>
    - tests/bug-patterns-reference.test.cjs — read the complete file to confirm lines 43–52 contain the `has title and intro` test with both the startsWith assertion and the includes('---') assertion before editing
  </read_first>

  <action>
In the `has title and intro` test block (lines 43–52), remove lines 45–48 only:
```javascript
    assert.ok(
      content.startsWith('# Common Bug Patterns'),
      'File should start with "# Common Bug Patterns" title'
    );
```

After removal, the `has title and intro` test must read exactly:
```javascript
  test('has title and intro', () => {
    const content = fs.readFileSync(REFERENCE_PATH, 'utf-8');
    assert.ok(
      content.includes('---'),
      'File should contain --- separator after intro'
    );
  });
```

The test name `has title and intro` may remain as-is or be updated to `has separator` — use judgment (Claude's discretion per CONTEXT.md).

The `content.includes('---')` assertion on what was line 49 must be retained (per D-08).

All other tests in the file are unchanged: `reference file exists`, `contains at least 5 of 10 expected categories`, `each pattern category has at least one bold bullet item`, `gsd-debugger.md exists`, `gsd-debugger.md references common-bug-patterns.md`, `reference is inside <required_reading> block`.

Rationale (per D-07): `common-bug-patterns.md` now opens with `<task>` (fork-standard XML opener) not `# Common Bug Patterns`. The startsWith check is overly specific to the upstream heading convention and fails against the fork-standard file.
  </action>

  <verify>
    <automated>node --test tests/bug-patterns-reference.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `grep "startsWith" tests/bug-patterns-reference.test.cjs` returns no output (assertion removed)
    - `grep "Common Bug Patterns" tests/bug-patterns-reference.test.cjs` appears only in the top comment block and the EXPECTED_CATEGORIES array — not in any `assert.ok` call
    - `grep "includes('---')" tests/bug-patterns-reference.test.cjs` returns 1 match (D-08 assertion retained)
    - `node --test tests/bug-patterns-reference.test.cjs` exits 0 with 0 failures
  </acceptance_criteria>

  <done>The `startsWith('# Common Bug Patterns')` assertion is gone. The `includes('---')` assertion is present. `node --test tests/bug-patterns-reference.test.cjs` passes with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Update 2 string literals in execute-phase-wave.test.cjs and verify full suite</name>
  <files>tests/execute-phase-wave.test.cjs</files>

  <read_first>
    - tests/execute-phase-wave.test.cjs — read lines 70–84 to confirm the current `workflow has partial-wave completion guardrail` test block before editing
    - get-shit-done/workflows/execute-phase.md — read lines 1018–1025 to confirm the exact positive-framing replacement text that the updated assertions will check for
  </read_first>

  <action>
In the `workflow has partial-wave completion guardrail` test block (lines 70–84), update 2 string literals:

Change line 77:
```javascript
      content.includes('Do NOT run phase verification'),
```
to:
```javascript
      content.includes('phase verification is handled separately'),
```

Change line 81:
```javascript
      content.includes('Do NOT mark the phase complete'),
```
to:
```javascript
      content.includes('ROADMAP.md and STATE.md unchanged'),
```

The failure messages on lines 78 and 82 (`'partial wave step should skip phase verification'` and `'partial wave step should skip phase completion'`) may remain unchanged — they still accurately describe the behavior.

The first assertion in the block (line 73–75, `content.includes('<step name="handle_partial_wave_execution">')`) is unchanged.

After the edit, the full test block reads:
```javascript
  test('workflow has partial-wave completion guardrail', () => {
    const content = fs.readFileSync(WORKFLOW_PATH, 'utf-8');
    assert.ok(
      content.includes('<step name="handle_partial_wave_execution">'),
      'workflow should have a partial wave handling step'
    );
    assert.ok(
      content.includes('phase verification is handled separately'),
      'partial wave step should skip phase verification'
    );
    assert.ok(
      content.includes('ROADMAP.md and STATE.md unchanged'),
      'partial wave step should skip phase completion'
    );
  });
```

Rationale (TEST-04 / fork standard): Phase 2 commit `83f1d01` converted `Do NOT run phase verification` and `Do NOT mark the phase complete` to positive framing in `execute-phase.md`. The test hardcoded the old text. Reverting `execute-phase.md` would violate the fork standard — the test must be updated instead.

After editing, run the full test suite:
```bash
npm test
```

Do NOT modify `get-shit-done/workflows/execute-phase.md` or any production file to make the tests pass. Only `tests/execute-phase-wave.test.cjs` is modified in this task.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Do NOT run phase verification\|Do NOT mark the phase complete" tests/execute-phase-wave.test.cjs` returns no output (old strings replaced)
    - `grep "phase verification is handled separately" tests/execute-phase-wave.test.cjs` returns 1 match (new string present)
    - `grep "ROADMAP.md and STATE.md unchanged" tests/execute-phase-wave.test.cjs` returns 1 match (new string present)
    - `node --test tests/execute-phase-wave.test.cjs` exits 0 with 0 failures
    - `npm test` exits 0 — `ℹ fail 0` in output (full suite green; previously 4 failing, now 0)
    - `git diff get-shit-done/workflows/execute-phase.md` is empty (production workflow not touched)
  </acceptance_criteria>

  <done>`execute-phase-wave.test.cjs` checks for positive-framing strings. `npm test` exits with 0 failures. No production file was modified. TEST-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → workflow content | execute-phase-wave.test.cjs asserts strings present in execute-phase.md; overly permissive strings could mask future regressions |
| test quality → fork standard fidelity | Removing assertions must not create blind spots where fork-standard violations go undetected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | execute-phase-wave.test.cjs | mitigate | Replacement strings (`phase verification is handled separately`, `ROADMAP.md and STATE.md unchanged`) are verified unique in execute-phase.md per RESEARCH.md — the test still confirms the specific behavior, not a generic substring |
| T-03-02-02 | Tampering | bug-patterns-reference.test.cjs | mitigate | `includes('---')` assertion is retained per D-08; `each pattern category has at least one bold bullet item` structural test remains — the quality bar is not lowered by removing the startsWith check |
| T-03-02-03 | Repudiation | production files not modified | mitigate | acceptance_criteria requires `git diff get-shit-done/workflows/execute-phase.md` is empty — explicitly prevents the prohibited pattern of fixing tests by reverting fork changes |
| T-03-02-04 | Denial of Service | false green (test passes but behavior broken) | accept | The 3 strings checked in the updated guardrail test are semantically accurate descriptions of partial-wave behavior: the step exists, phase verification is deferred, state files are not updated. Risk of false green is low; behavior is unchanged in execute-phase.md |
</threat_model>

<verification>
Run after both tasks complete:

```bash
npm test
```

Expected: `ℹ fail 0`. All tests in `ios-scaffold-safety.test.cjs`, `bug-patterns-reference.test.cjs`, and `execute-phase-wave.test.cjs` pass. No production file diff (only the 4 files listed in `files_modified` across plans 03-01 and 03-02 were changed).
</verification>

<success_criteria>
- `npm test` exits 0 — `ℹ fail 0` (all 3930+ tests green after 2 test blocks removed and 3 assertions updated)
- `grep "startsWith" tests/bug-patterns-reference.test.cjs` returns no output
- `grep "Do NOT run phase verification\|Do NOT mark the phase complete" tests/execute-phase-wave.test.cjs` returns no output
- `grep "phase verification is handled separately" tests/execute-phase-wave.test.cjs` returns 1 match
- `grep "ROADMAP.md and STATE.md unchanged" tests/execute-phase-wave.test.cjs` returns 1 match
- TEST-03 satisfied: bug-patterns-reference.test.cjs no longer fails on XML-opener files
- TEST-04 satisfied: full suite is green
- No fork-standard edit was reverted in any production file
</success_criteria>

<output>
After completion, create `.planning/phases/03-align-tests-with-fork-standards/03-02-SUMMARY.md` using the summary template. Record: exact lines changed in each test file, full suite pass count, and confirmation that no production files were modified. Note that TEST-03 and TEST-04 are satisfied.
</output>
