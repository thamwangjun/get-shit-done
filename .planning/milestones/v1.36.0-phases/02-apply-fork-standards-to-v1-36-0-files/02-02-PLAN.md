---
phase: 02-apply-fork-standards-to-v1-36-0-files
plan: 02
type: execute
wave: 2
depends_on:
  - 02-03
files_modified:
  - agents/gsd-advisor-researcher.md
  - agents/gsd-executor.md
  - agents/gsd-phase-researcher.md
  - commands/gsd/quick.md
  - commands/gsd/reapply-patches.md
  - commands/gsd/thread.md
  - get-shit-done/workflows/complete-milestone.md
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/pr-branch.md
  - get-shit-done/workflows/update.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/new-milestone.md
  - get-shit-done/workflows/next.md
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - MOD-03
  - MOD-04
  - MOD-05
  - MOD-06
  - MOD-07
  - MOD-08
  - MOD-09
  - MOD-10
  - MOD-11
  - MOD-12
  - MOD-13
  - MOD-14
  - MOD-15

must_haves:
  truths:
    - "All 15 modified files pass the negative-framing scanner with 0 violations"
    - "Files with actual scanner violations receive affirmative replacements specifying correct behavior"
    - "Files where VIOLATIONS.md listed violations that actually pass the scanner are confirmed without unnecessary edits"
    - "SECURITY-style Never X — always Y patterns (D-07) are confirmed unchanged in complete-milestone.md, execute-phase.md, verify-work.md, thread.md"
    - "YAML frontmatter in all agent files is unchanged"
  artifacts:
    - path: "agents/gsd-advisor-researcher.md"
      provides: "Failing Do NOT lines converted; passing lines confirmed"
    - path: "agents/gsd-executor.md"
      provides: "Do not skip documentation → affirmative; boilerplate already done by 02-03"
    - path: "agents/gsd-phase-researcher.md"
      provides: "Do not skip → affirmative; Don't Hand-Roll section heading renamed"
    - path: "commands/gsd/quick.md"
      provides: "Do NOT proceed → flow-control positive form; Never pattern confirmed"
    - path: "commands/gsd/thread.md"
      provides: "2 Do NOT proceed → affirmative; Never pattern confirmed"
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Do NOT modify STATE.md line converted; Do NOT run/mark lines converted; Never pattern confirmed D-07"
    - path: "get-shit-done/workflows/verify-work.md"
      provides: "Do NOT add commentary → affirmative; Never pattern confirmed D-07"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "Do NOT retry line evaluated and handled"
  key_links:
    - from: "all 15 files"
      to: "tests/negative-framing-scan.test.cjs"
      via: "node --test"
      pattern: "Do NOT|do NOT|DO NOT|NEVER"
---

<objective>
Apply the fork's positive framing standard to the 15 files modified by v1.36.0 (MOD-01 through MOD-15). Each file is processed independently: read the file, run the scanner to establish its current state, apply targeted affirmative replacements only to lines that actually fail the scanner, and confirm with a post-edit test run.

Critical insight from RESEARCH.md: many violations listed in VIOLATIONS.md use `Do not` (lowercase n) which does NOT trigger the scanner. The scanner only flags `DO NOT`, `Do NOT`, `do NOT` (capital O) and uppercase `NEVER`. The executor MUST run the scanner before editing each file to avoid unnecessary changes.

Note: The `Do NOT load full AGENTS.md files` boilerplate in `agents/gsd-executor.md` was already removed by Plan 02-03 (wave 1). This plan handles all remaining violations in the 15 modified files.

Purpose: Completing the positive framing pass on all modified v1.36.0 files closes Phase 2.

Output: 15 confirmed-passing files; some with targeted edits, some confirmed-passing without edits.
</objective>

<execution_context>
@/Users/thamw/.claire/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/VIOLATIONS.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-CONTEXT.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-RESEARCH.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-03-SUMMARY.md

<interfaces>
<!-- SCANNER BEHAVIOR (critical — read before editing any file): -->
<!-- TRIGGERS: DO NOT | Do NOT | do NOT (capital O in NOT) | NEVER (uppercase only) -->
<!-- DOES NOT TRIGGER: Do not (lowercase n) | do not | don't | never (lowercase) -->
<!-- PASSES despite triggering if: em-dash present | double-dash | period+uppercase | parenthetical 13+ chars -->

<!-- LOCKED DECISIONS: -->
<!-- D-07: Never X — always Y paired patterns → confirm as valid reframe, leave unchanged -->
<!-- D-08: Every replacement must specify correct behavior, not just delete prohibition -->

<!-- CANONICAL CONVERSIONS (from VIOLATIONS.md + RESEARCH.md): -->
<!-- "Do not skip documentation lookups because MCP tools are unavailable" -->
<!--   → "Use the CLI fallback when MCP tools are unavailable — skip nothing" -->
<!-- "Do NOT run phase verification" → "Proceed to the next step — phase verification is handled separately" -->
<!-- "Do NOT mark the phase complete in ROADMAP/STATE" → "Leave ROADMAP.md and STATE.md unchanged — the orchestrator handles that update" -->
<!-- "Do NOT modify STATE.md or ROADMAP.md" → "Leave STATE.md and ROADMAP.md unmodified — execute-plan.md handles those updates." -->
<!-- "Do NOT add commentary before or after the block." → "Output the block only — omit all commentary before and after." -->
<!-- "Do NOT proceed to further steps." (after STOP) → "Display and stop." / "Commit and stop." (contextual) -->

<!-- AGENT SAFETY RULES: -->
<!-- After editing any agent file: verify YAML frontmatter unchanged -->
<!-- After editing any file-writing agent: verify "Only use the Write tool" string still present -->
<!-- Use Edit tool (not Write) for all modifications -->

<!-- VIOLATION MAP PER FILE (pre-computed from RESEARCH.md): -->
<!--
  gsd-advisor-researcher.md: Lines that FAIL scanner (no complement):
    - ~128: "Do NOT research beyond the single assigned gray area" → "Scope research to the single assigned gray area only"
    - ~131: "Do NOT use time estimates in the Complexity column" → "Use qualitative labels (Low / Medium / High) in the Complexity column — omit time estimates"
    - ~134: "Do NOT produce extended analysis paragraphs beyond the single rationale paragraph" → "Limit analysis output to the single rationale paragraph — write the table and stop"
    Also fix line ~47: "Do not skip documentation lookups because MCP tools are unavailable" → canonical replacement
    Lines that PASS (have complements, confirm only): ~107, ~129, ~130, ~132, ~133

  gsd-executor.md (boilerplate already done by 02-03):
    - Lines ~56-57: "Do not skip documentation lookups...Do not rely on training knowledge alone"
      → "Use the CLI fallback when MCP tools are unavailable — skip nothing; rely on documentation over training knowledge"
    - Line ~234: "Avoid restarting the build to find more issues — continue forward" → em-dash, passes scanner; confirm only

  gsd-phase-researcher.md:
    - Line ~96: "Do not skip documentation lookups because MCP tools are unavailable" → canonical replacement
    - Line ~344: "- **Don't Hand-Roll:**..." → lowercase don't → passes scanner; confirm only
    - Line ~610: "## Don't Hand-Roll" section heading → lowercase, passes scanner BUT per MOD-03 requirement: rename to "## Solved Problems"

  commands/gsd/quick.md:
    - Line ~85: "Never pass raw directory names..." → lowercase never → passes scanner; D-07 confirm
    - Line ~101: "STOP after displaying the list. Do NOT proceed to further steps." → convert: "Display the list, then stop."

  commands/gsd/reapply-patches.md:
    - Line ~202: "Do NOT silently skip." → has Ask the user sentence before → check complement detection → likely passes; confirm
    - Line ~221: "(do not block):" → lowercase do not → passes scanner
    - Line ~271: "Do not proceed to cleanup until..." → lowercase Do not → passes scanner
    Expected: 0 edits needed; confirm only

  commands/gsd/thread.md:
    - Line ~47: "Never pass raw filenames..." → lowercase never → passes scanner; D-07 confirm
    - Line ~68: "STOP after displaying. Do NOT proceed to further steps." → convert: "Display and stop."
    - Line ~95: "STOP after committing. Do NOT proceed to further steps." → convert: "Commit and stop."

  get-shit-done/workflows/complete-milestone.md:
    - Line ~110: "Never inject raw file content into STATE.md." → lowercase never → D-07 confirm; passes scanner
    - Line ~116: "Never inject raw user-supplied content into STATE.md without sanitization." → same
    Expected: 0 edits; confirm only

  get-shit-done/workflows/execute-phase.md:
    - Line ~333: "Never shell-interpolate the prompt — always pipe via stdin..." → lowercase never + em-dash → D-07 confirm; passes scanner
    - Line ~499: "IMPORTANT: Do NOT modify STATE.md or ROADMAP.md." → convert: "Leave STATE.md and ROADMAP.md unmodified — execute-plan.md handles those updates."
    - Line ~506: "...Do NOT skip or defer" → has parenthetical complement → passes scanner; confirm
    - Line ~560: "Use normal git commits (with hooks). Do NOT use --no-verify." → preceding period+uppercase → passes scanner; confirm
    - Line ~1020: "Do NOT run phase verification" → convert: "Proceed to the next step — phase verification is handled separately"
    - Line ~1021: "Do NOT mark the phase complete in ROADMAP/STATE" → convert: "Leave ROADMAP.md and STATE.md unchanged — the orchestrator handles that update"

  get-shit-done/workflows/plan-phase.md:
    - Lines ~43, ~63, ~533: "Do not fall back..." / "do not fall back..." / "Do not continue." → all lowercase n → pass scanner
    Expected: 0 edits; confirm with test

  get-shit-done/workflows/pr-branch.md:
    - Read the file and grep for Do NOT / do NOT / NEVER patterns; VIOLATIONS.md lists 2 but exact lines were not pre-computed
    - Run scanner first; edit only lines that actually fail

  get-shit-done/workflows/update.md:
    - Line ~371: "Do not use bash path-stripping..." → lowercase Do not → passes scanner
    Expected: 0 edits; confirm with test

  get-shit-done/workflows/verify-work.md:
    - Line ~238: "Do NOT add commentary before or after the block." → convert: "Output the block only — omit all commentary before and after."
    - Line ~452: "Never pass raw file content to subagents without DATA_START/DATA_END wrapping." → lowercase never → D-07 confirm; passes scanner

  get-shit-done/workflows/discuss-phase.md:
    - Lines ~107, ~167, ~614, ~1040: "Do not..." → all lowercase n → pass scanner
    - Line ~110: "Do NOT retry the AskUserQuestion..." → check complement: "when 'Other' is selected with empty text." — conditional clause; verify scanner behavior by running test
    - Line ~658: "Do NOT ask the standard 4 questions — the table already provided the context" → em-dash complement → passes scanner; confirm
    - Line ~739: "Do NOT re-read your own CONTEXT.md..." → em-dash present later in text → check scanner; likely passes

  get-shit-done/workflows/new-milestone.md:
    - Lines ~73, ~82, ~313, ~608: "do not..." → lowercase → pass scanner
    - Line ~285: "Do NOT persist this choice..." → period + next sentence starts with "The" → passes scanner
    Expected: 0 edits; confirm with test

  get-shit-done/workflows/next.md:
    - Line ~116: "don't have overrides" → possessive contraction → passes scanner
    - Line ~133: "planning never ran" → factual description → passes scanner
    Expected: 0 edits; confirm with test
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scanner violations in agent and command files (MOD-01 to MOD-06)</name>
  <files>
    agents/gsd-advisor-researcher.md,
    agents/gsd-executor.md,
    agents/gsd-phase-researcher.md,
    commands/gsd/quick.md,
    commands/gsd/reapply-patches.md,
    commands/gsd/thread.md
  </files>
  <action>
**Establish baseline before any edits:**
`node --test tests/negative-framing-scan.test.cjs` — note the pass count.

**Process each file. For each: Read → Grep for scanner-triggering patterns → Run scanner → Edit only lines that fail → Confirm.**

---

**FILE 1: `agents/gsd-advisor-researcher.md` (MOD-01)**

Read the file. Grep for `Do NOT` / `do NOT` / `NEVER`. Run `node --test tests/negative-framing-scan.test.cjs` to see which lines actually fail.

Lines expected to FAIL the scanner (apply edits):
- ~47: `Do not skip documentation lookups because MCP tools are unavailable`
  → `Use the CLI fallback when MCP tools are unavailable — skip nothing`
- ~128: `Do NOT research beyond the single assigned gray area`
  → `Scope research to the single assigned gray area only`
- ~131: `Do NOT use time estimates in the Complexity column`
  → `Use qualitative labels (Low / Medium / High) in the Complexity column — omit time estimates`
- ~134: `Do NOT produce extended analysis paragraphs beyond the single rationale paragraph`
  → `Limit analysis output to the single rationale paragraph — write the table and stop`

Lines expected to PASS (confirm only, no edit):
- ~107: `Do NOT include extended analysis -- table + rationale only.` — double-dash complement
- ~129: `Do NOT present output directly to user (main agent synthesizes)` — parenthetical
- ~130: `Do NOT add columns beyond the 5-column format (...)` — parenthetical
- ~132: `Do NOT rank options or declare a single winner (use conditional recommendations)` — parenthetical
- ~133: `Do NOT invent filler options to pad the table -- only genuinely viable approaches` — double-dash

After all edits: verify YAML frontmatter unchanged. Run `node --test tests/negative-framing-scan.test.cjs`.

---

**FILE 2: `agents/gsd-executor.md` (MOD-02)**

Note: The `Do NOT load full AGENTS.md files` boilerplate (formerly line 70) was already replaced by Plan 02-03.

Read the file. Locate around lines 56-57: `Do not skip documentation lookups` and/or `Do not rely on training knowledge alone`
- `Do not` (lowercase n) → does NOT trigger scanner. Verify with test first.
- If scanner passes for this file already, mark MOD-02 done — no edit needed.
- If scanner fails: apply canonical replacement for the `Do not skip` line only.

Line ~234: `Avoid restarting the build to find more issues — continue forward` — em-dash, passes scanner; confirm only.

After confirming: verify YAML frontmatter unchanged. Verify `Only use the Write tool` string still present.
Run `node --test tests/negative-framing-scan.test.cjs`.

---

**FILE 3: `agents/gsd-phase-researcher.md` (MOD-03)**

Read the file. Locate:
- ~96: `Do not skip documentation lookups because MCP tools are unavailable` → lowercase n → likely passes scanner; confirm
- ~344: `- **Don't Hand-Roll:**...` → lowercase don't → passes scanner; confirm
- ~610: `## Don't Hand-Roll` section heading → lowercase, passes scanner, BUT per MOD-03 the requirement is to evaluate and rename this heading. Per RESEARCH.md analysis: rename to `## Solved Problems` (this is a semantic improvement even though scanner does not flag it — it was explicitly called out in the requirement)

Apply Edit to rename the section heading at line ~610: `## Don't Hand-Roll` → `## Solved Problems`

Run `node --test tests/negative-framing-scan.test.cjs`.
Verify YAML frontmatter unchanged.

---

**FILE 4: `commands/gsd/quick.md` (MOD-04)**

Read the file. Locate:
- ~85: `Never pass raw directory names to shell commands via string interpolation.` → lowercase `Never` → `isFactualNever()` returns true → passes scanner; D-07 confirm, no change
- ~101: `STOP after displaying the list. Do NOT proceed to further steps.`
  → `Do NOT proceed` with no em-dash complement → FAILS scanner
  → Replace with: `Display the list, then stop.`

After edit: run `node --test tests/negative-framing-scan.test.cjs`.

---

**FILE 5: `commands/gsd/reapply-patches.md` (MOD-05)**

Read the file. Locate the three VIOLATIONS.md-listed patterns:
- ~202: `Do NOT silently skip.` — check if `Ask the user:` precedes on the same line or if a period+uppercase preceding sentence creates a complement → run scanner first
- ~221: `(do not block):` — lowercase → passes scanner
- ~271: `Do not proceed to cleanup until...` — lowercase `Do not` (capital D, lowercase n) → passes scanner

Run `node --test tests/negative-framing-scan.test.cjs` BEFORE any edits. If scanner passes for this file, mark MOD-05 done — no edits.
If scanner fails: apply targeted edit only to the actual failing line.

---

**FILE 6: `commands/gsd/thread.md` (MOD-06)**

Read the file. Locate:
- ~47: `Never pass raw filenames to shell commands via string interpolation.` → lowercase never → passes scanner; D-07 confirm, no change
- ~68: `STOP after displaying. Do NOT proceed to further steps.`
  → FAILS scanner → Replace with: `Display and stop.`
- ~95: `STOP after committing. Do NOT proceed to further steps.`
  → FAILS scanner → Replace with: `Commit and stop.`

After edits: run `node --test tests/negative-framing-scan.test.cjs`.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <done>
    - `agents/gsd-advisor-researcher.md`: 4 affirmative replacements applied; scanner passes
    - `agents/gsd-executor.md`: confirmed pass or targeted edit applied; boilerplate already done by 02-03
    - `agents/gsd-phase-researcher.md`: "## Solved Problems" heading applied; scanner passes
    - `commands/gsd/quick.md`: "Display the list, then stop." applied; scanner passes
    - `commands/gsd/reapply-patches.md`: confirmed pass (lowercase patterns do not trigger scanner)
    - `commands/gsd/thread.md`: 2 affirmative replacements applied; scanner passes
    - All YAML frontmatter in agent files unchanged
    - No test regressions
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix and confirm workflow files (MOD-07 to MOD-15)</name>
  <files>
    get-shit-done/workflows/complete-milestone.md,
    get-shit-done/workflows/execute-phase.md,
    get-shit-done/workflows/plan-phase.md,
    get-shit-done/workflows/pr-branch.md,
    get-shit-done/workflows/update.md,
    get-shit-done/workflows/verify-work.md,
    get-shit-done/workflows/discuss-phase.md,
    get-shit-done/workflows/new-milestone.md,
    get-shit-done/workflows/next.md
  </files>
  <action>
**For each workflow file: Read → Run scanner → Edit only failing lines → Confirm.**

---

**FILE 1: `get-shit-done/workflows/complete-milestone.md` (MOD-07)**

Read the file. Locate:
- ~110: `Never inject raw file content into STATE.md.` → lowercase never → D-07 valid reframe; passes scanner
- ~116: `Never inject raw user-supplied content into STATE.md without sanitization.` → same

Run `node --test tests/negative-framing-scan.test.cjs`. Expected: passes. Mark MOD-07 done — no edits (D-07 exceptions confirmed).

---

**FILE 2: `get-shit-done/workflows/execute-phase.md` (MOD-08)**

Read the file. This file has multiple patterns — run the scanner FIRST to see which lines actually fail.

Lines expected to FAIL (apply edits):
- ~499: `IMPORTANT: Do NOT modify STATE.md or ROADMAP.md.`
  → `Leave STATE.md and ROADMAP.md unmodified — execute-plan.md handles those updates.`
- ~1020: `Do NOT run phase verification`
  → `Proceed to the next step — phase verification is handled separately`
- ~1021: `Do NOT mark the phase complete in ROADMAP/STATE`
  → `Leave ROADMAP.md and STATE.md unchanged — the orchestrator handles that update`

Lines expected to PASS (confirm only, D-07 exceptions):
- ~333: `Never shell-interpolate the prompt — always pipe via stdin` → lowercase never + em-dash → D-07; passes scanner
- ~506: `...Do NOT skip or defer` → has parenthetical complement → passes
- ~560: `Use normal git commits (with hooks). Do NOT use --no-verify.` → period+uppercase preceding → passes

After scanner run and edits: run `node --test tests/negative-framing-scan.test.cjs`. Confirm D-07 patterns unchanged.

---

**FILE 3: `get-shit-done/workflows/plan-phase.md` (MOD-09)**

Read the file. Locate:
- ~43: `Do not fall back to general-purpose agents:` → lowercase `Do not` → passes scanner
- ~63: `do not fall back to 'general-purpose'` → lowercase → passes scanner
- ~533: `Do not continue.` → lowercase → passes scanner

Run `node --test tests/negative-framing-scan.test.cjs`. Expected: passes with no failures for this file. Mark MOD-09 done — no edits.

If scanner fails unexpectedly: read the exact failing line from test output, apply D-08 to that specific line only.

---

**FILE 4: `get-shit-done/workflows/pr-branch.md` (MOD-10)**

Read the file. Grep for `Do NOT` / `do NOT` / `NEVER` (the pre-computed violation map did not have exact lines for this file). VIOLATIONS.md lists 2 violations.

Run `node --test tests/negative-framing-scan.test.cjs` with this file in scope.

For each line that actually fails the scanner:
- If it is a bare `Do NOT X` with no complement: apply D-08 — write an affirmative instruction specifying what to do instead
- If it has an em-dash, double-dash, or parenthetical: it passes, no edit needed
- `Never X — always Y` patterns: D-07 valid reframe, leave unchanged

After edits (if any): run scanner again to confirm.

---

**FILE 5: `get-shit-done/workflows/update.md` (MOD-11)**

Read the file. Locate:
- ~371: `**Do not use bash path-stripping (\`${filepath#$RUNTIME_DIR/}\`) or \`node -e require()\``
  → `Do not` (lowercase n) → does NOT trigger scanner

Run `node --test tests/negative-framing-scan.test.cjs`. Expected: passes. Mark MOD-11 done — no edits.

---

**FILE 6: `get-shit-done/workflows/verify-work.md` (MOD-12)**

Read the file. Run scanner first.

Lines expected to FAIL (apply edits):
- ~238: `Do NOT add commentary before or after the block.`
  → `Output the block only — omit all commentary before and after.`

Lines expected to PASS (D-07, confirm only):
- ~452: `Never pass raw file content to subagents without DATA_START/DATA_END wrapping.` → lowercase never → D-07

After edit: run `node --test tests/negative-framing-scan.test.cjs`. Confirm D-07 pattern unchanged.

---

**FILE 7: `get-shit-done/workflows/discuss-phase.md` (MOD-13)**

Read the file. This file has many patterns — run scanner FIRST.

Lines expected to PASS (lowercase Do not):
- ~107: `Do not call any tools. Do not output any further text.`
- ~167: `Do not continue with the steps below`
- ~614: `Do not remove detail; translate it.`
- ~1040: `Do not use as input to planning...`

Lines expected to PASS (em-dash complements):
- ~658: `Do NOT ask the standard 4 questions — the table already provided the context`
- ~739: `Do NOT re-read your own CONTEXT.md...` — check: has em-dash later → likely passes

Line to VERIFY (may or may not fail):
- ~110: `Do NOT retry the AskUserQuestion or generate more questions when "Other" is selected...`
  → The trailing clause "when 'Other' is selected with empty text." is a conditional, not an em-dash complement. Run scanner to determine: if it FAILS → `When "Other" is selected with empty text, accept the input and proceed`; if it passes, leave unchanged.

After all checks and any edits: run `node --test tests/negative-framing-scan.test.cjs`.

---

**FILE 8: `get-shit-done/workflows/new-milestone.md` (MOD-14)**

Read the file. Locate:
- ~73: `do not print any message or prompt.` → lowercase → passes
- ~82: `do not prompt the user.` → lowercase → passes
- ~285: `Do NOT persist this choice to config.json.` → the sentence after it starts with `The` → period+uppercase → `hasPositiveComplement()` true → passes
- ~313: `Do not re-research capabilities already validated.` → lowercase → passes
- ~608: `Do not batch commits.` → lowercase → passes

Run `node --test tests/negative-framing-scan.test.cjs`. Expected: passes. Mark MOD-14 done — no edits.

---

**FILE 9: `get-shit-done/workflows/next.md` (MOD-15)**

Read the file. Locate:
- ~116: `don't have overrides` → possessive contraction, not a directive
- ~133: `planning never ran` → factual description, not a directive

Run `node --test tests/negative-framing-scan.test.cjs`. Expected: passes. Mark MOD-15 done — no edits.

---

**Final verification for all of Plan 02-02:**

After all 9 workflow files are confirmed:
`node --test tests/negative-framing-scan.test.cjs`

This is the definitive pass/fail for Task 2 and the completion signal for Plan 02-02 and Phase 2 as a whole.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <done>
    - `complete-milestone.md`: Never patterns confirmed as D-07 valid reframes; no edits
    - `execute-phase.md`: 3 affirmative replacements applied; Never pattern confirmed D-07; scanner passes
    - `plan-phase.md`: all lowercase Do not — confirmed pass; no edits
    - `pr-branch.md`: 2 violations located by reading file; fixed if scanner-failing or confirmed if passing
    - `update.md`: lowercase Do not — confirmed pass; no edits
    - `verify-work.md`: 1 affirmative replacement applied; Never D-07 confirmed; scanner passes
    - `discuss-phase.md`: Do NOT retry line evaluated; other lowercase patterns confirmed; scanner passes
    - `new-milestone.md`: all patterns are lowercase or have complements; confirmed pass; no edits
    - `next.md`: possessive/factual text confirmed; no edits
    - Final `node --test tests/negative-framing-scan.test.cjs` passes with no regressions
    - Phase 2 success criterion 3 met: all 26 affected files pass the scanner
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → workflow prompt files | Edit tool modifies workflow files that orchestrate multi-agent execution; incorrect edits change agent behavior at runtime |
| editor → agent prompt files | Edit tool modifies agent constraint lists; removing a safety constraint (e.g., Never inject) would expose downstream processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | SECURITY Never X — always Y patterns | mitigate | D-07 is absolute: identify all such patterns by reading each file before editing; confirm they are unchanged after edit by re-reading; do not convert lowercase `never` patterns |
| T-02-02-02 | Tampering | agent YAML frontmatter | mitigate | Read each agent file before editing; verify `---` frontmatter block unchanged after edit; run `npm test --test-name-pattern agent-frontmatter` if uncertain |
| T-02-02-03 | Tampering | execute-phase.md orchestration logic | mitigate | Use Edit tool (not Write) for targeted line replacements; verify adjacent lines unchanged after each edit; run scanner to confirm no content was accidentally removed |
| T-02-02-04 | Denial of Service | none — no user data, secrets, or external services | accept | This is a prompt-file-only editing task; lowest possible threat surface |

Note: The `Never inject raw ... into STATE.md` patterns in complete-milestone.md are genuine security controls (D-07 valid reframe exceptions). These must be preserved verbatim — converting or removing them would weaken injection safety guarantees.
</threat_model>

<verification>
1. `node --test tests/negative-framing-scan.test.cjs` — must pass (no regressions from pre-plan baseline; all 15 files clean)
2. `grep -n "Do NOT\|do NOT\|DO NOT\|NEVER" get-shit-done/workflows/execute-phase.md` — lines 499, 1020, 1021 should NOT appear in results; line 333 (`Never shell-interpolate`) should still be present
3. `grep -n "Never inject" get-shit-done/workflows/complete-milestone.md` — must return 2 lines (D-07 patterns preserved)
4. `grep "## Solved Problems" agents/gsd-phase-researcher.md` — must return 1 match (heading renamed per MOD-03)
5. `npm test -- --test-name-pattern agent-frontmatter` — must pass (YAML frontmatter intact in all agent files)
6. Phase 2 global: `grep -r "Do NOT load full" agents/` → 0 matches (from 02-03); `node --test tests/negative-framing-scan.test.cjs` → all pass
</verification>

<success_criteria>
- All 15 modified files (MOD-01 through MOD-15) pass the negative-framing scanner
- Genuine failing violations receive affirmative replacements per D-08
- SECURITY-style Never X — always Y patterns (complete-milestone.md, execute-phase.md, verify-work.md, thread.md) are confirmed unchanged (D-07)
- Files where VIOLATIONS.md listed lowercase `Do not` patterns are confirmed as already-passing without unnecessary edits
- No test regressions introduced
- YAML frontmatter in all agent files unchanged
- Phase 2 complete: all 26 affected files (11 new + 15 modified) pass the scanner
</success_criteria>

<output>
After completion, create `.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-02-SUMMARY.md` using the summary template. Record:
- For each file: what was changed (or confirmed-as-passing with no change needed)
- Final scanner test result
- Any scanner vs. VIOLATIONS.md mismatches encountered (files listed as violations that already passed)
- Confirmation that Phase 2 success criteria 1-4 are all met
</output>
