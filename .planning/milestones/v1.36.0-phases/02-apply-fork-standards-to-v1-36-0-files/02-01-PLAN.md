---
phase: 02-apply-fork-standards-to-v1-36-0-files
plan: 01
type: execute
wave: 2
depends_on:
  - 02-03
files_modified:
  - commands/gsd/graphify.md
  - agents/gsd-debug-session-manager.md
  - agents/gsd-domain-researcher.md
  - agents/gsd-ai-researcher.md
  - get-shit-done/workflows/eval-review.md
  - get-shit-done/workflows/extract_learnings.md
  - get-shit-done/references/planner-antipatterns.md
  - get-shit-done/references/planner-source-audit.md
  - get-shit-done/references/ai-evals.md
  - get-shit-done/references/ai-frameworks.md
autonomous: true
requirements:
  - NEW-01
  - NEW-02
  - NEW-03
  - NEW-04
  - NEW-05
  - NEW-06
  - NEW-07
  - NEW-08
  - NEW-09
  - NEW-10

must_haves:
  truths:
    - "All 10 new files (11 new - 1 already boilerplate-free) pass the negative-framing scanner with 0 violations"
    - "graphify.md's STOP section is deleted and remaining Do NOT directives are replaced with affirmative instructions"
    - "ai-evals.md and ai-frameworks.md are confirmed as containing editorial/quoted text only — no scanner-flaggable directives"
    - "YAML frontmatter in all agent files is unchanged"
    - "Files with no scanner violations are confirmed without unnecessary edits"
  artifacts:
    - path: "commands/gsd/graphify.md"
      provides: "STOP section deleted; Do NOT directives converted; confirmed pass"
    - path: "agents/gsd-debug-session-manager.md"
      provides: "2 violations converted to affirmative"
    - path: "agents/gsd-domain-researcher.md"
      provides: "2 violations converted to affirmative"
    - path: "agents/gsd-ai-researcher.md"
      provides: "1 violation converted to affirmative"
    - path: "get-shit-done/workflows/eval-review.md"
      provides: "1 violation converted to affirmative"
    - path: "get-shit-done/workflows/extract_learnings.md"
      provides: "Confirmed pass (em-dash complement present)"
    - path: "get-shit-done/references/planner-antipatterns.md"
      provides: "1 violation converted to affirmative"
    - path: "get-shit-done/references/planner-source-audit.md"
      provides: "1 violation converted to affirmative"
    - path: "get-shit-done/references/ai-evals.md"
      provides: "Confirmed pass — editorial voice, no scanner violations"
    - path: "get-shit-done/references/ai-frameworks.md"
      provides: "Confirmed pass — quoted speech preserved, no scanner violations"
  key_links:
    - from: "all 10 files"
      to: "tests/negative-framing-scan.test.cjs"
      via: "node --test"
      pattern: "Do NOT|do NOT|DO NOT|NEVER"
---

<objective>
Apply the fork's positive framing standard to the 10 new files added in v1.36.0 (NEW-01 through NEW-10). Each file is handled independently. Files requiring edits receive targeted affirmative replacements. Files that already pass the scanner are confirmed with a test run and marked done without unnecessary edits.

Note: NEW-11 (the `Do NOT load full AGENTS.md files` boilerplate) was handled in Plan 02-03 (wave 1). Any of these 10 files that also contained the boilerplate have already had that line fixed. This plan handles all remaining violations in these files.

Purpose: Completing the positive framing pass on new files ensures all 11 new v1.36.0 files meet the fork standard before Phase 3 (test alignment).

Output: 10 confirmed-passing files; 4–6 files with targeted edits; 4–6 files confirmed-passing without edits.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/VIOLATIONS.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-CONTEXT.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-RESEARCH.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-03-SUMMARY.md

<interfaces>
<!-- SCANNER BEHAVIOR (from RESEARCH.md — Key Insight section): -->
<!-- The scanner flags ONLY: DO NOT, Do NOT, do NOT (capital O in NOT), and NEVER (uppercase) -->
<!-- The scanner does NOT flag: Do not (lowercase n), do not, don't, never (lowercase) -->
<!-- The scanner PASSES any flagged line that has: em-dash, double-dash, period+uppercase, or parenthetical complement -->

<!-- LOCKED DECISIONS from CONTEXT.md: -->
<!-- D-03: graphify.md STOP section — delete entirely -->
<!-- D-04: Quoted user speech in ai-frameworks.md — preserve as-is -->
<!-- D-05: Editorial don't in ai-evals.md — preserve as-is -->
<!-- D-07: Never X — always Y paired patterns — confirm and leave unchanged -->
<!-- D-08: Every replacement must specify correct behavior, not just delete the prohibition -->

<!-- CANONICAL REPLACEMENTS (from VIOLATIONS.md conversion table): -->
<!-- "Do not skip documentation lookups because MCP tools are unavailable" -->
<!--   → "Use the CLI fallback when MCP tools are unavailable — skip nothing" -->
<!-- "Do not fabricate X — only Y" -->
<!--   → "Source X exclusively from Y" -->
<!-- "Do NOT load full AGENTS.md files (100KB+ context cost)" [already done by 02-03] -->
<!--   → "Load specific agent files only" -->

<!-- VIOLATION MAP FOR TASK 1 (files requiring edits): -->
<!--
  commands/gsd/graphify.md:
    - Lines ~11+: "STOP -- DO NOT READ THIS FILE..." section → DELETE entire section (D-03)
    - Line ~89: "**STOP** after displaying results. Do not spawn an agent." → "Display results and stop."
    - Line ~103: "**STOP** after displaying status. Do not spawn an agent." → "Display status and stop."
    - Line ~119: "**STOP** after displaying diff. Do not spawn an agent." → "Display diff and stop."
    - Line ~163: "Do NOT delete .planning/graphs/" → has em-dash positive complement → PASSES SCANNER, no change

  agents/gsd-debug-session-manager.md (boilerplate already fixed by 02-03):
    - Line ~22: "Do not load the full codebase into your context." → "Load only the debug file and project metadata into context."
    - Line ~22 (same block): "Pass file paths to spawned agents — never inline file contents." → em-dash complement, passes scanner

  agents/gsd-domain-researcher.md (boilerplate NOT in this file per VIOLATIONS.md):
    - Line ~38: "Do not skip documentation lookups because MCP tools are unavailable" → "Use the CLI fallback when MCP tools are unavailable — skip nothing"
    - Line ~139: "do not list every possible regulation" → em-dash complement preceding, passes scanner
    - Line ~141: "Do not fabricate criteria" → "Source criteria exclusively from research or well-established practitioner knowledge"

  agents/gsd-ai-researcher.md (boilerplate NOT in this file per VIOLATIONS.md):
    - Line ~38: "Do not skip documentation lookups because MCP tools are unavailable" → "Use the CLI fallback when MCP tools are unavailable — skip nothing"

  get-shit-done/workflows/eval-review.md:
    - Line ~134: "Do not deploy until gaps are addressed." → "Address all gaps before deployment."
-->

<!-- VIOLATION MAP FOR TASK 2 (files to confirm — likely no edits needed): -->
<!--
  get-shit-done/workflows/extract_learnings.md:
    - Line ~229: "Do not fabricate learnings — only extract what is explicitly documented in artifacts"
    - Has em-dash complement → passes scanner → CONFIRM, no edit

  get-shit-done/references/planner-antipatterns.md:
    - Audit listed 1 violation — read the file to locate exact text
    - If "Do not flag these as MISSING:" → "Treat these as present — flag only items absent from both sources"
    - Run scanner to confirm whether this actually fails

  get-shit-done/references/planner-source-audit.md:
    - Line ~30: "Do not flag these as MISSING:" → "Treat these as present — flag only items absent from both sources"
    - Run scanner to confirm whether this actually fails (Do not = lowercase n = passes scanner)

  get-shit-done/references/ai-evals.md:
    - Line ~87: "don't build for hypothetical coverage" → D-05: editorial voice → preserve
    - Line ~151: "they don't; model evals are a filter" → D-05: editorial voice → preserve
    - Confirm 0 scanner violations

  get-shit-done/references/ai-frameworks.md:
    - Line ~18: quoted user speech "I don't know my requirements yet" → D-04: preserve
    - "Avoid if:" table patterns → not prompt directives → preserve
    - Confirm 0 scanner violations
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scanner violations in 5 new files requiring edits (NEW-01 to NEW-05)</name>
  <files>
    commands/gsd/graphify.md,
    agents/gsd-debug-session-manager.md,
    agents/gsd-domain-researcher.md,
    agents/gsd-ai-researcher.md,
    get-shit-done/workflows/eval-review.md
  </files>
  <action>
**Before any edits, run the baseline test:**
`node --test tests/negative-framing-scan.test.cjs` — note the pass count.

**Process each file in order. For each file: Read → Locate violations → Edit → Confirm.**

---

**FILE 1: `commands/gsd/graphify.md` (NEW-01)**

Read the file. Locate the `STOP -- DO NOT READ THIS FILE` section. Per D-03, delete the entire section — find where it starts (the heading or opening line) and where it ends (just before the next section begins). Use Edit to remove these lines. Do not rewrite them; delete them.

Then locate remaining `Do NOT` patterns:
- Find any line matching: `**STOP** after displaying [results|status|diff]. Do not spawn an agent.`
  - Replace each with: `Display [results|status|diff] and stop.` (adjust the noun for each instance)
- Find: `Do NOT delete .planning/graphs/` — this line has an em-dash and a positive complement. Run the scanner to verify it passes. If it passes, leave it unchanged.

After all edits to graphify.md, run: `node --test tests/negative-framing-scan.test.cjs`
The test must not regress (same or better pass count than baseline).

**SAFETY: graphify.md is a command file (no YAML frontmatter to protect). But verify the file structure is intact after deletion.**

---

**FILE 2: `agents/gsd-debug-session-manager.md` (NEW-02)**

Note: The `Do NOT load full AGENTS.md files` boilerplate was already removed by Plan 02-03. This agent file may have been updated.

Read the file. Locate around line 22 (or current location) the text: `Do not load the full codebase into your context.`
- Replace with: `Load only the debug file and project metadata into context.`

Also on the same line or nearby: `Pass file paths to spawned agents — never inline file contents.`
- This uses lowercase `never` with an em-dash complement. Run the scanner to confirm it passes. Leave unchanged.

After edits: `node --test tests/negative-framing-scan.test.cjs` — must not regress.

**SAFETY: After editing, verify the YAML frontmatter block (`---` at top) is unchanged. Verify `Only use the Write tool` is present if this agent had Write in its tools.**

---

**FILE 3: `agents/gsd-domain-researcher.md` (NEW-03)**

Read the file. Locate:
1. Around line 38: `Do not skip documentation lookups because MCP tools are unavailable`
   - Replace with: `Use the CLI fallback when MCP tools are unavailable — skip nothing`
2. Around line 139: `do not list every possible regulation` — check if this line has a preceding em-dash or is part of a "only ... — do not" structure. Run scanner; if it passes, leave unchanged.
3. Around line 141: `Do not fabricate criteria`
   - Replace with: `Source criteria exclusively from research or well-established practitioner knowledge`

After edits: `node --test tests/negative-framing-scan.test.cjs` — must not regress.

**SAFETY: Verify YAML frontmatter unchanged after edits.**

---

**FILE 4: `agents/gsd-ai-researcher.md` (NEW-04)**

Read the file. Locate around line 38: `Do not skip documentation lookups because MCP tools are unavailable`
- Replace with: `Use the CLI fallback when MCP tools are unavailable — skip nothing`

After edit: `node --test tests/negative-framing-scan.test.cjs` — must not regress.

**SAFETY: Verify YAML frontmatter unchanged after edit.**

---

**FILE 5: `get-shit-done/workflows/eval-review.md` (NEW-05)**

Read the file. Locate around line 134: `Do not deploy until gaps are addressed.`
- Replace with: `Address all gaps before deployment.`

After edit: `node --test tests/negative-framing-scan.test.cjs` — must not regress.

---

**Decision rule for any unexpected violations found while reading:**
- If you encounter a `Do NOT` or `NEVER` (uppercase) pattern not listed above: run the scanner first. If it already passes (complement present), leave it. If it fails the scanner, apply D-08: write an affirmative instruction specifying what to do instead.
- If you encounter `Do not` (lowercase n) or `never` (lowercase): these do not trigger the scanner. Leave them unless they are an explicit VIOLATIONS.md-listed violation for this file.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <done>
    - `commands/gsd/graphify.md`: STOP section deleted; 3 "Display and stop" replacements applied; scanner passes
    - `agents/gsd-debug-session-manager.md`: "Load only debug file and project metadata" replacement applied; scanner passes
    - `agents/gsd-domain-researcher.md`: 2 affirmative replacements applied; scanner passes
    - `agents/gsd-ai-researcher.md`: 1 affirmative replacement applied; scanner passes
    - `get-shit-done/workflows/eval-review.md`: 1 affirmative replacement applied; scanner passes
    - `node --test tests/negative-framing-scan.test.cjs` shows no regressions from Task 1 edits
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm 5 files that pass scanner already (NEW-06 to NEW-10)</name>
  <files>
    get-shit-done/workflows/extract_learnings.md,
    get-shit-done/references/planner-antipatterns.md,
    get-shit-done/references/planner-source-audit.md,
    get-shit-done/references/ai-evals.md,
    get-shit-done/references/ai-frameworks.md
  </files>
  <action>
**For each file: Read the file, run targeted grep for scanner-flagged patterns, run the scanner test, and record the outcome. Edit only if the scanner actually fails.**

---

**FILE 1: `get-shit-done/workflows/extract_learnings.md` (NEW-06)**

Read the file. Locate around line 229: `Do not fabricate learnings — only extract what is explicitly documented in artifacts`

This line has an em-dash complement (`— only extract...`). Per RESEARCH.md the scanner's `hasPositiveComplement()` function returns true for em-dash patterns, so this line passes.

Run: `node --test tests/negative-framing-scan.test.cjs`
Expected: same pass count as after Task 1. If it passes, mark NEW-06 done — no edit required.

If the test somehow fails for this file (unexpected), apply: `Source learnings exclusively from explicitly documented artifacts`

---

**FILE 2: `get-shit-done/references/planner-antipatterns.md` (NEW-07)**

Read the file. Search for any `Do NOT` / `do NOT` / `NEVER` patterns.

The VIOLATIONS.md audit listed 1 violation. The likely text is: `Do not flag these as MISSING:` — note lowercase `n` in `not`. The scanner regex `\b(DO NOT|Do NOT|do NOT)\b` does NOT match `Do not` (capital D, lowercase n, lowercase t). So this likely already passes the scanner.

Run: `node --test tests/negative-framing-scan.test.cjs`
If it passes: mark NEW-07 done — no edit required.
If it fails (unexpected): replace `Do not flag these as MISSING:` with `Treat these as present — flag only items absent from both sources:`

---

**FILE 3: `get-shit-done/references/planner-source-audit.md` (NEW-08)**

Read the file. Locate around line 30: `Do not flag these as MISSING:` — same lowercase-n analysis as above.

Run: `node --test tests/negative-framing-scan.test.cjs`
If passes: mark NEW-08 done — no edit required.
If fails: replace `Do not flag these as MISSING:` with `Treat these as present — flag only items absent from both sources:`

---

**FILE 4: `get-shit-done/references/ai-evals.md` (NEW-09)**

Read the file. Locate:
- Around line 87: `don't build for hypothetical coverage` — editorial voice in informational reference (D-05). Not a directive. Lowercase `don't`. Scanner does not flag.
- Around line 151: `they don't; model evals are a filter` — same analysis.

Run: `node --test tests/negative-framing-scan.test.cjs`
Expected: passes. Mark NEW-09 done — no edit required (D-05 applies).

If the scanner somehow fails for this file: read the exact failing line from the test output, then apply D-08 only to the actual failing line.

---

**FILE 5: `get-shit-done/references/ai-frameworks.md` (NEW-10)**

Read the file. Locate:
- Around line 18: `"I don't know my requirements yet"` — quoted user speech (D-04). Preserve as-is.
- `Avoid if:` table entries — comparative table metadata, not prompt directives.

Run: `node --test tests/negative-framing-scan.test.cjs`
Expected: passes. Mark NEW-10 done — no edit required.

If the scanner fails: read the exact failing line, apply D-08 only to the actual failing line.

---

**Final verification for this task:**

After confirming all 5 files, run the full scanner one more time:
`node --test tests/negative-framing-scan.test.cjs`

This is the definitive pass/fail for Task 2 and the whole of Plan 02-01.
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <done>
    - `get-shit-done/workflows/extract_learnings.md`: confirmed pass, no edit needed (em-dash complement present)
    - `get-shit-done/references/planner-antipatterns.md`: confirmed pass or edits applied if scanner failed
    - `get-shit-done/references/planner-source-audit.md`: confirmed pass or edits applied if scanner failed
    - `get-shit-done/references/ai-evals.md`: confirmed pass — editorial voice preserved per D-05
    - `get-shit-done/references/ai-frameworks.md`: confirmed pass — quoted speech preserved per D-04
    - `node --test tests/negative-framing-scan.test.cjs` passes with no regressions across all Plan 02-01 work
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → prompt files | Edit tool modifies agent and workflow prompt files; incorrect edits corrupt AI behavior at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | agent YAML frontmatter | mitigate | Read each agent file before editing; confirm frontmatter `---` block unchanged after each edit; `npm test` catches frontmatter regressions |
| T-02-01-02 | Tampering | `Only use the Write tool` constraint | mitigate | After editing any file-writing agent, grep for this string to confirm it remains |
| T-02-01-03 | Tampering | graphify.md section deletion | mitigate | Delete only the STOP section (D-03); verify adjacent sections are intact by reading surrounding context before confirming edit |
| T-02-01-04 | Information Disclosure | None — no user data or secrets involved | accept | This is a prompt-file-only editing task with no PII, credentials, or sensitive data |

Note: These files are prompt content only — no authentication, no external services, no user-supplied input. Security threat surface is exclusively accidental file corruption.
</threat_model>

<verification>
1. `node --test tests/negative-framing-scan.test.cjs` — must pass (no regressions from pre-plan baseline)
2. For each file that received edits: re-read and confirm the affirmative replacement text is present and the old `Do NOT` / `Do not` text is gone
3. For graphify.md specifically: confirm the `STOP -- DO NOT READ THIS FILE` section no longer exists in the file
4. For ai-evals.md and ai-frameworks.md: confirm no edits were made (editorial/quoted content preserved per D-04, D-05)
5. `npm test -- --test-name-pattern agent-frontmatter` — must pass (YAML frontmatter intact in agent files)
</verification>

<success_criteria>
- All 10 new files (NEW-01 through NEW-10) pass the negative-framing scanner
- graphify.md's STOP section is deleted; remaining directives are affirmative
- ai-evals.md and ai-frameworks.md contain unmodified editorial/quoted content (D-04, D-05 honored)
- No test regressions introduced
- YAML frontmatter in all agent files unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-01-SUMMARY.md` using the summary template. Record:
- For each file: what was changed (or confirmed-as-passing with no change)
- Final scanner test result
- Any files where the violation was different from what RESEARCH.md predicted (test vs. human-audit mismatch)
</output>
