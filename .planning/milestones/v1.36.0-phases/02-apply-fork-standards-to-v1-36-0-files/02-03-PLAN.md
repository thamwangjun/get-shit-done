---
phase: 02-apply-fork-standards-to-v1-36-0-files
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-code-fixer.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-doc-verifier.md
  - agents/gsd-doc-writer.md
  - agents/gsd-executor.md
  - agents/gsd-code-reviewer.md
  - agents/gsd-intel-updater.md
  - agents/gsd-integration-checker.md
  - agents/gsd-eval-auditor.md
  - agents/gsd-nyquist-auditor.md
  - agents/gsd-pattern-mapper.md
  - agents/gsd-security-auditor.md
autonomous: true
requirements:
  - NEW-11

must_haves:
  truths:
    - "The string `Do NOT load full AGENTS.md files` does not appear in any file under `agents/`"
    - "All 13 agent files pass the negative-framing scanner with 0 new failures after replacement"
    - "YAML frontmatter in all 13 agent files is unchanged"
    - "The string `Only use the Write tool` is preserved in all file-writing agents that contained it"
  artifacts:
    - path: "agents/gsd-code-fixer.md"
      provides: "Boilerplate replaced on line 38"
    - path: "agents/gsd-codebase-mapper.md"
      provides: "Boilerplate replaced on line 53"
    - path: "agents/gsd-debugger.md"
      provides: "Boilerplate replaced on line 86"
    - path: "agents/gsd-doc-verifier.md"
      provides: "Boilerplate replaced on line 44"
    - path: "agents/gsd-doc-writer.md"
      provides: "Boilerplate replaced on line 48"
    - path: "agents/gsd-executor.md"
      provides: "Boilerplate replaced on line 70"
    - path: "agents/gsd-code-reviewer.md"
      provides: "Boilerplate replaced on line 36"
    - path: "agents/gsd-intel-updater.md"
      provides: "Boilerplate replaced on line 29"
    - path: "agents/gsd-integration-checker.md"
      provides: "Boilerplate replaced on line 35"
    - path: "agents/gsd-eval-auditor.md"
      provides: "Boilerplate replaced on line 29"
    - path: "agents/gsd-nyquist-auditor.md"
      provides: "Boilerplate replaced on line 36"
    - path: "agents/gsd-pattern-mapper.md"
      provides: "Boilerplate replaced on line 41"
    - path: "agents/gsd-security-auditor.md"
      provides: "Boilerplate replaced on line 48"
  key_links:
    - from: "agents/*.md (13 files)"
      to: "tests/negative-framing-scan.test.cjs"
      via: "node --test"
      pattern: "Do NOT load full"
---

<objective>
Replace the recurring boilerplate `Do NOT load full AGENTS.md files (100KB+ context cost)` with its positive equivalent `Load specific agent files only` across all 13 affected agent files.

Purpose: This boilerplate appears identically in 13 agents and is the single highest-frequency violation in the codebase. Sweeping it globally in one plan (02-03) before the per-file passes (02-01, 02-02) avoids double-touching lines in agents that also have other violations (per D-02). Running this first ensures plans 02-01 and 02-02 operate on clean files.

Output: 13 agent files with the boilerplate replaced; all 13 pass the negative-framing scanner; YAML frontmatter untouched.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/VIOLATIONS.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-CONTEXT.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-RESEARCH.md

<interfaces>
<!-- Locked decision from CONTEXT.md D-01 -->
<!-- Old text (exact string to replace): Do NOT load full `AGENTS.md` files (100KB+ context cost) -->
<!-- New text (exact replacement, per D-01): Load specific agent files only -->

<!-- Files and violation lines (from RESEARCH.md Architecture Patterns section): -->
agents/gsd-code-fixer.md        — line 38
agents/gsd-codebase-mapper.md   — line 53
agents/gsd-executor.md          — line 70
agents/gsd-doc-writer.md        — line 48
agents/gsd-doc-verifier.md      — line 44
agents/gsd-debugger.md          — line 86
agents/gsd-code-reviewer.md     — line 36
agents/gsd-intel-updater.md     — line 29
agents/gsd-integration-checker.md — line 35
agents/gsd-eval-auditor.md      — line 29
agents/gsd-nyquist-auditor.md   — line 36
agents/gsd-pattern-mapper.md    — line 41
agents/gsd-security-auditor.md  — line 48

<!-- Test command: node --test tests/negative-framing-scan.test.cjs -->
<!-- Current baseline: 34/34 pass -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace boilerplate in all 13 agent files</name>
  <files>
    agents/gsd-code-fixer.md,
    agents/gsd-codebase-mapper.md,
    agents/gsd-debugger.md,
    agents/gsd-doc-verifier.md,
    agents/gsd-doc-writer.md,
    agents/gsd-executor.md,
    agents/gsd-code-reviewer.md,
    agents/gsd-intel-updater.md,
    agents/gsd-integration-checker.md,
    agents/gsd-eval-auditor.md,
    agents/gsd-nyquist-auditor.md,
    agents/gsd-pattern-mapper.md,
    agents/gsd-security-auditor.md
  </files>
  <action>
**Before any edits, establish a baseline:**
Run `node --test tests/negative-framing-scan.test.cjs` and note the result. This confirms the starting state (expected: 34/34 pass).

**For each of the 13 files listed below, in order:**

1. Use the Read tool to read the file. This is mandatory — never edit without reading first.
2. Locate the exact line containing: `Do NOT load full \`AGENTS.md\` files (100KB+ context cost)`
   - The approximate line numbers are given (use these as a guide, not an absolute — the file may have shifted slightly): code-fixer:38, codebase-mapper:53, executor:70, doc-writer:48, doc-verifier:44, debugger:86, code-reviewer:36, intel-updater:29, integration-checker:35, eval-auditor:29, nyquist-auditor:36, pattern-mapper:41, security-auditor:48
3. Use the Edit tool (not Write) to replace that one line. The replacement must preserve surrounding indentation and list formatting. The old text and new text for the Edit tool:
   - Old: `Do NOT load full \`AGENTS.md\` files (100KB+ context cost)`
   - New: `Load specific agent files only`
   - If the line starts with a list marker (e.g., `4. ` or `- `), preserve the list marker — only replace the text content after the marker.
4. After editing the file, visually verify the frontmatter (`---` block at top of file) is unchanged.
5. If the file had `Only use the Write tool` anywhere before the edit, confirm it is still present after the edit.

**Files to edit (in this order):**
- `agents/gsd-code-fixer.md` (line ~38)
- `agents/gsd-codebase-mapper.md` (line ~53)
- `agents/gsd-debugger.md` (line ~86)
- `agents/gsd-doc-verifier.md` (line ~44)
- `agents/gsd-doc-writer.md` (line ~48)
- `agents/gsd-executor.md` (line ~70)
- `agents/gsd-code-reviewer.md` (line ~36)
- `agents/gsd-intel-updater.md` (line ~29)
- `agents/gsd-integration-checker.md` (line ~35)
- `agents/gsd-eval-auditor.md` (line ~29)
- `agents/gsd-nyquist-auditor.md` (line ~36)
- `agents/gsd-pattern-mapper.md` (line ~41)
- `agents/gsd-security-auditor.md` (line ~48)

**After all 13 edits are complete:**
Run `node --test tests/negative-framing-scan.test.cjs` again. The result must still be 34/34 pass (or better if the scanner has been updated to catch `Do NOT` patterns).

**Safety checks — STOP if any of these are true:**
- The Edit tool reports that the old text was not found in a file — read the file again and locate the actual current text before retrying
- The frontmatter block (`---`) appears to have changed in any file — revert with git and investigate
- The string `Only use the Write tool` has disappeared from any file-writing agent (code-fixer, doc-writer, doc-verifier, executor, codebase-mapper) — revert and re-apply the targeted edit only
  </action>
  <verify>
    <automated>node --test tests/negative-framing-scan.test.cjs</automated>
  </verify>
  <done>
    - `grep -r "Do NOT load full" agents/` returns 0 matches
    - `node --test tests/negative-framing-scan.test.cjs` returns 34/34 pass (no regressions)
    - YAML frontmatter in all 13 files unchanged (spot-check with `npm test -- --test-name-pattern agent-frontmatter` if uncertain)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → file system | Edit tool writes directly to agent prompt files; incorrect edits corrupt agent behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | agent YAML frontmatter | mitigate | Read each file before editing; verify frontmatter block unchanged after edit; run npm test agent-frontmatter check if uncertain |
| T-02-03-02 | Tampering | `Only use the Write tool` string in file-writing agents | mitigate | After editing any file-writing agent, grep for the string to confirm presence |
| T-02-03-03 | Denial of Service | Test baseline | accept | Scan test currently at 34/34; no PII or secrets involved; lowest possible risk |

Note: This plan involves no user data, no authentication, no external services, and no secrets. Threat surface is limited to accidental file corruption during editing.
</threat_model>

<verification>
1. `grep -r "Do NOT load full" agents/` — must return zero lines
2. `node --test tests/negative-framing-scan.test.cjs` — must pass 34/34 (no regressions from baseline)
3. `grep -r "Load specific agent files only" agents/` — must return exactly 13 matches (one per file)
4. `npm test -- --test-name-pattern agent-frontmatter` — must pass (YAML frontmatter intact in all agents)
</verification>

<success_criteria>
- All 13 agent files have `Load specific agent files only` in place of the old boilerplate
- Zero test regressions
- YAML frontmatter in all 13 files intact
- Plans 02-01 and 02-02 can proceed without encountering this boilerplate violation in any of the 13 files
</success_criteria>

<output>
After completion, create `.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-03-SUMMARY.md` using the summary template. Record:
- Which 13 files were modified
- Confirmation that grep shows 0 remaining instances of the old boilerplate
- Test result (pass count)
- Any files where the line number differed from the expected approximate line
</output>
