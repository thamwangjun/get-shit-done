---
phase: 02-apply-fork-standards-to-v1-36-0-files
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements:
  - NEW-02
gap_closure: true

must_haves:
  truths:
    - "node --test tests/agent-frontmatter.test.cjs passes 135/135 tests"
    - "discuss-phase.md declares gsd-advisor-researcher in an <available_agent_types> block"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "<available_agent_types> section listing gsd-advisor-researcher"
      contains: "<available_agent_types>"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "tests/agent-frontmatter.test.cjs"
      via: "SPAWN suite test #1357 — any workflow spawning a named agent must declare it in <available_agent_types>"
      pattern: "available_agent_types"
---

<objective>
Close the single UAT gap from Phase 02: `discuss-phase.md` spawns `gsd-advisor-researcher` via `subagent_type=` but has no `<available_agent_types>` section, causing test #1357 of the SPAWN suite to fail (134/135).

Purpose: Bring the agent-frontmatter test suite to 135/135 so Phase 02 is fully verified and Phase 03 can proceed.
Output: `discuss-phase.md` with the `<available_agent_types>` block inserted; all 135 frontmatter tests green.
</objective>

<execution_context>
@/Users/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert &lt;available_agent_types&gt; block into discuss-phase.md</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Read `get-shit-done/workflows/discuss-phase.md` in full.

Locate the closing `</required_reading>` tag (currently at line 21). Insert the following block on a new line immediately after `</required_reading>` and before `<downstream_awareness>`:

```xml
<available_agent_types>
Valid GSD subagent types (use exact names — do not fall back to 'general-purpose'):
- gsd-advisor-researcher — Researches gray areas and returns structured comparison tables
</available_agent_types>
```

The result must have this exact ordering:
1. `</required_reading>`
2. blank line
3. `<available_agent_types>` block (3 lines as above)
4. blank line
5. `<downstream_awareness>`

Write the full file back with this change and no other modifications.
  </action>
  <verify>
    <automated>node --test tests/agent-frontmatter.test.cjs 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Output shows "135 passing" (or equivalent passing count), zero failures. The string `&lt;available_agent_types&gt;` is present in `get-shit-done/workflows/discuss-phase.md` between `&lt;/required_reading&gt;` and `&lt;downstream_awareness&gt;`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow file → orchestrator | Workflow content is read by orchestrator at runtime — a malformed section could cause agent resolution to fail silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | discuss-phase.md | mitigate | Write the full file back after reading it — do not patch in-place; verify no other lines changed by diffing before/after |
| T-02-04-02 | Denial | agent-frontmatter test suite | accept | Test is deterministic; single-file change; low blast radius |
</threat_model>

<verification>
Run after the task completes:

```bash
node --test tests/agent-frontmatter.test.cjs
```

Expected: 135 passing, 0 failing.

Secondary check — confirm placement is correct:

```bash
grep -n "available_agent_types\|required_reading\|downstream_awareness" get-shit-done/workflows/discuss-phase.md | head -10
```

Expected output (line numbers approximate):
```
21: </required_reading>
23: <available_agent_types>
27: </available_agent_types>
29: <downstream_awareness>
```
</verification>

<success_criteria>
- `node --test tests/agent-frontmatter.test.cjs` reports 135/135 passing
- `get-shit-done/workflows/discuss-phase.md` contains `<available_agent_types>` block listing `gsd-advisor-researcher`
- Block is positioned between `</required_reading>` and `<downstream_awareness>`
- No other lines in `discuss-phase.md` were altered
</success_criteria>

<output>
After completion, create `.planning/phases/02-apply-fork-standards-to-v1-36-0-files/02-04-SUMMARY.md`
</output>
