---
phase: 68-pre-merge-inventory-backup-sdk-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/config
  - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff
  - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-DECISIONS.md
autonomous: true
requirements: [MERGE-01]
must_haves:
  truths:
    - "git branch --list pre-merge-v1.3.1-backup returns the branch, anchored at pre-merge HEAD"
    - "git config diff.renameLimit and git config merge.renameLimit both read 5000"
    - "A durable, committed, non-empty fork-edit inventory diff exists in the phase directory (per D-01, D-02)"
    - "68-DECISIONS.md records both pre-made decisions with rationale and requirement IDs (per D-05)"
    - "D-01: inventory is durable inside the phase dir and committed to git — never /tmp"
    - "D-02: inventory form is raw git diff output, not a curated summary"
  artifacts:
    - path: ".planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff"
      provides: "Raw git-diff fork-edit baseline over fork-owned paths (audit baseline for Phases 70-71)"
    - path: ".planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-DECISIONS.md"
      provides: "Both pre-made architecture decisions (PATCH-02 KEEP, SDK-01->SDK-02 ACCEPT)"
  key_links:
    - from: "pre-merge-v1.3.1-backup branch"
      to: "current HEAD (pre-merge)"
      via: "git branch pointing at pre-merge HEAD"
      pattern: "pre-merge-v1.3.1-backup"
---

<objective>
Create every recovery anchor and pre-merge baseline artifact for the v1.3.1 merge: a backup branch anchoring pre-merge HEAD, the raised git renameLimit config, a durable raw fork-edit inventory diff, and the architecture-decision record — all on disk and committed before any destructive merge runs in Phase 69.

Purpose: MERGE-01 demands recovery anchors and a fork-edit audit baseline exist before the merge so patch survival can be verified in Phases 70-71 and recovery is possible if the merge goes wrong.
Output: backup branch, git config (renameLimit=5000), 68-FORK-EDIT-INVENTORY.diff, 68-DECISIONS.md.
</objective>

<context>
!`cat .planning/STATE.md`
!`cat .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md`
</context>

<threat_model>
## Trust Boundaries

This phase performs local git operations and writes documentation files into the repo. No secrets, no network calls, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | backup branch anchor | mitigate | Create `pre-merge-v1.3.1-backup` with `git branch` (no checkout, no working-tree change); verify it resolves to the current pre-merge HEAD with `git rev-parse` before continuing — a wrong anchor makes recovery impossible if the later merge fails |
| T-68-02 | Repudiation | inventory durability | mitigate | Per D-01 the inventory is committed to git inside the phase dir (not /tmp), giving a tamper-evident, reboot-surviving audit baseline for Phases 70-71 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create backup branch and raise git renameLimit</name>
  <files>.git/config</files>
  <read_first>
    - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md (D-01..D-05, fixed constants, "do not switch branches" constraint)
    - .planning/research/STACK.md (renameLimit=5000 rationale, merge-base fa4bba47)
  </read_first>
  <action>
    Capture the current pre-merge HEAD: run `git rev-parse HEAD` and record it as PRE_MERGE_HEAD.
    Create the recovery anchor WITHOUT switching branches or touching the working tree: `git branch pre-merge-v1.3.1-backup` (plain `git branch`, not `checkout -b` and not `switch`). This anchors the new branch at the current pre-merge HEAD.
    Raise both rename limits per research (renameLimit=5000 rationale in STACK.md): `git config diff.renameLimit 5000` and `git config merge.renameLimit 5000`.
    Do NOT fetch upstream `--tags` (tag namespace is contaminated). Do NOT re-derive the merge-base — `fa4bba47` is a fixed input. This task runs no merge.
  </action>
  <acceptance_criteria>
    - `git branch --list pre-merge-v1.3.1-backup` outputs the branch name (non-empty)
    - `git rev-parse pre-merge-v1.3.1-backup` equals `git rev-parse HEAD` (anchor is at pre-merge HEAD)
    - `git config diff.renameLimit` outputs `5000`
    - `git config merge.renameLimit` outputs `5000`
    - `git rev-parse --abbrev-ref HEAD` is unchanged (still on the original branch — branch creation did not switch)
  </acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse pre-merge-v1.3.1-backup)" = "$(git rev-parse HEAD)" && test "$(git config diff.renameLimit)" = "5000" && test "$(git config merge.renameLimit)" = "5000"</automated>
  </verify>
  <done>Backup branch exists at pre-merge HEAD, both renameLimits read 5000, working branch unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Capture durable raw fork-edit inventory diff</name>
  <files>.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff</files>
  <read_first>
    - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md (D-01 durable in phase dir; D-02 raw diff form)
    - .planning/research/SUMMARY.md (§"Phase 1": exact `git diff fa4bba47..HEAD` command and fork-owned path set)
    - .planning/research/ARCHITECTURE.md ("Fork-authored files that must be migrated manually" table — defines what the inventory must capture)
  </read_first>
  <action>
    Per D-01 (durable, NOT /tmp — overrides the research draft's `/tmp/fork-edit-inventory.txt`) and D-02 (raw git diff output, no curated summary), generate the fork-edit baseline by redirecting raw diff output to the phase directory:
    `git diff fa4bba47..HEAD -- agents/ commands/gsd/ get-shit-done/workflows/ get-shit-done/references/ bin/install.js hooks/ > .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff`
    Use the exact fork-owned path set from research: `agents/`, `commands/gsd/`, `get-shit-done/workflows/`, `get-shit-done/references/`, `bin/install.js`, `hooks/`. Use the fixed merge-base `fa4bba47` (per D-02) — do not re-derive it. This file is the survival-audit baseline consumed by Phase 70 (fork-patch restoration) and Phase 71 (verification), so it must be committed with the phase, not left ephemeral.
  </action>
  <acceptance_criteria>
    - The file `68-FORK-EDIT-INVENTORY.diff` exists in the phase directory (NOT /tmp): `test -f` succeeds
    - The file is non-empty: `test -s` succeeds (the diff range produces >10000 lines)
    - The file contains raw diff markers (greppable): `grep -q '^diff --git'` succeeds
    - The file is tracked after the phase commit (appears in `git ls-files`)
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff && grep -q '^diff --git' .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-FORK-EDIT-INVENTORY.diff</automated>
  </verify>
  <done>Durable, non-empty raw fork-edit diff exists in the phase directory and is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Record both pre-made architecture decisions</name>
  <files>.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-DECISIONS.md</files>
  <read_first>
    - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md (D-05: single self-contained doc; decision/rationale/req-IDs greppable)
    - .planning/ROADMAP.md (milestone goal paragraph at ~line 299 — the two pre-made decisions verbatim; Phase 68 success criterion 4)
    - .planning/REQUIREMENTS.md (PATCH-02 wording; SDK-01, SDK-02 gating relationship)
  </read_first>
  <action>
    Per D-05, write a single self-contained `68-DECISIONS.md` in the phase directory recording BOTH pre-made architecture decisions. No project-wide ADR convention is introduced — this is phase-local only.
    Required sections (state each decision, its rationale, and the requirement IDs it satisfies so a grep confirms presence):
    - Decision 1 — KEEP the fork's SHA-based update-check worker (`isNewer()` via GitHub Commits API) over upstream's semver/npm approach. Rationale: the fork deliberately tracks commits by SHA, not published npm releases. Satisfies/encodes: PATCH-02 (re-applied in Phase 70). State that upstream's semver/npm worker is NOT adopted.
    - Decision 2 — ACCEPT upstream's `sdk/` deletion, GATED on SDK-01 documentation existing first. Rationale: the merge deletes sdk/ (305 files); the capability must be documented (SDK-01, this phase, Plan 02) before the deletion is accepted (SDK-02) in Phase 69. State the gating direction explicitly: SDK-01 -> SDK-02.
    The literal requirement tokens `PATCH-02`, `SDK-01`, and `SDK-02` MUST appear in the doc so the success-criterion grep passes.
  </action>
  <acceptance_criteria>
    - `test -f 68-DECISIONS.md` succeeds in the phase directory
    - `grep -q 'PATCH-02' 68-DECISIONS.md` succeeds (Decision 1 requirement ID present)
    - `grep -q 'SDK-01' 68-DECISIONS.md` AND `grep -q 'SDK-02' 68-DECISIONS.md` both succeed (Decision 2 gating IDs present)
    - `grep -qi 'isNewer\|SHA-based\|update-check' 68-DECISIONS.md` succeeds (Decision 1 substance present)
    - `grep -qi 'sdk/ deletion\|accept' 68-DECISIONS.md` succeeds (Decision 2 substance present)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-DECISIONS.md; test -f "$F" && grep -q 'PATCH-02' "$F" && grep -q 'SDK-01' "$F" && grep -q 'SDK-02' "$F"</automated>
  </verify>
  <done>68-DECISIONS.md records both decisions with rationale and requirement IDs; all grep tokens present.</done>
</task>

</tasks>

<verification>
- `git branch --list pre-merge-v1.3.1-backup` returns the branch; `git rev-parse pre-merge-v1.3.1-backup == git rev-parse HEAD`
- `git config diff.renameLimit` and `git config merge.renameLimit` both output `5000`
- `68-FORK-EDIT-INVENTORY.diff` exists in the phase dir, is non-empty, contains `diff --git` markers, is committed
- `68-DECISIONS.md` exists; greps for `PATCH-02`, `SDK-01`, `SDK-02` all succeed
- Working branch unchanged (no checkout/switch occurred)
</verification>

<success_criteria>
- MERGE-01 recovery anchors and audit baseline exist on disk and are committed before any merge
- Backup branch anchors pre-merge HEAD exactly; renameLimits set to 5000
- Durable raw fork-edit inventory diff captured per D-01/D-02
- Both pre-made architecture decisions recorded per D-05 with greppable requirement IDs
</success_criteria>

<output>
Create `.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-01-SUMMARY.md` when done.
</output>
