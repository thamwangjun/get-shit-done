---
phase: 68-pre-merge-inventory-backup-sdk-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md
autonomous: true
requirements: [SDK-01]
must_haves:
  truths:
    - "The fork's sdk/ capability is documented in .planning/ restoration-grade (per D-03), self-sufficient without the original source the merge deletes"
    - "Each named module (session-runner.ts, config.ts, model-catalog.ts, ws-transport.ts) appears literally in the doc (per D-04 — the success criterion greps each)"
    - "Supporting modules in sdk/src/ are enumerated and covered by subsystem (per D-04)"
    - "Per module/subsystem: purpose, public surface (exports + signatures), runtime behavior, integration points, external dependencies are captured"
    - "D-03: documentation is restoration-grade — detailed enough for a future SDKR-01 milestone to rebuild WITHOUT the original source"
  artifacts:
    - path: ".planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md"
      provides: "Restoration-grade documentation of the fork sdk/ capability; gates SDK-02 in Phase 69"
      contains: "session-runner.ts"
  key_links:
    - from: "68-SDK-CAPABILITY.md"
      to: "sdk/src/ named + supporting modules"
      via: "restoration-grade capture before the merge deletes sdk/"
      pattern: "session-runner|config|model-catalog|ws-transport"
---

<objective>
Produce restoration-grade documentation of the fork's `sdk/` capability into `.planning/` BEFORE the merge in Phase 69 deletes `sdk/`. Capture purpose, public surface (exports + signatures), runtime behavior, integration points, and external dependencies for the four named modules and the supporting `sdk/src/` tree — detailed enough that a future SDKR-01 milestone can rebuild the feature without the original source.

Purpose: SDK-01 gates SDK-02. The merge deletes sdk/ (~305 files); this doc is the only surviving record of what the SDK did, and Phase 69 must not accept the deletion until it exists.
Output: 68-SDK-CAPABILITY.md.
</objective>

<context>
!`cat .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md`
!`find sdk/src -name '*.ts' ! -name '*.test.ts' ! -name '*.integration.test.ts' | sort`
!`command grep -n '"name"\|"version"\|@anthropic-ai/claude-agent-sdk\|"ws"' sdk/package.json`
</context>

<threat_model>
## Trust Boundaries

Read-only documentation work: reads fork source under `sdk/src/` and writes one markdown file into `.planning/`. No secrets, no network calls, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | Information disclosure | restoration completeness | mitigate | Per D-03 the doc must be self-sufficient before the merge deletes the source; incompleteness is the integrity risk, not leakage. Acceptance criteria grep each named module literally and require the public-surface sections, so an under-documented capability is caught before SDK-02 proceeds |
| T-68-04 | Tampering | SDK-01 -> SDK-02 gate | accept | The gate is enforced procedurally by Phase 69 (it must not accept the sdk/ deletion until this doc exists); no automated lock — low risk in a single-maintainer fork |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document the four named SDK modules restoration-grade</name>
  <files>.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md</files>
  <read_first>
    - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md (D-03 restoration-grade; D-04 each named module must appear literally)
    - sdk/src/session-runner.ts (orchestrates Agent SDK query() for plan execution — read in full)
    - sdk/src/config.ts (loads .planning/config.json, merges defaults — read in full)
    - sdk/src/model-catalog.ts (model/effort resolution, runtime-tier table, parseModelEffort/EFFORT_SET — read in full)
    - sdk/src/ws-transport.ts (WebSocketServer broadcasting GSD events — read in full)
    - sdk/src/types.ts (shared types: GSDEvent, ParsedPlan, PlanResult, SessionOptions — read to capture signatures the four modules expose)
    - sdk/src/index.ts (the package public surface — what is re-exported)
  </read_first>
  <action>
    Create `68-SDK-CAPABILITY.md` in the phase directory. Start with a short header: package identity (`@opengsd/gsd-sdk`, version from sdk/package.json), the restoration purpose (this doc gates SDK-02; the merge in Phase 69 deletes sdk/), and external dependencies (`@anthropic-ai/claude-agent-sdk` powering `query()`, `ws` for the WebSocket server).
    Then write one restoration-grade section per named module. EACH named module token (`session-runner.ts`, `config.ts`, `model-catalog.ts`, `ws-transport.ts`) MUST appear literally as a section heading — the success criterion greps for each. Per module document (per D-03/D-04):
    - Purpose — what capability it provides
    - Public surface — every exported function/class/interface/const with its signature (parameters and return type) and a one-line description
    - Runtime behavior — what it does at call time (model resolution priority, config merge order, event serialization, server lifecycle, etc.)
    - Integration points — which other modules and external SDKs it depends on / is consumed by
    - External dependencies — npm packages and node builtins used
    Write enough that a future SDKR-01 milestone can rebuild each module compatibly WITHOUT the original source. Capture signatures and behavior in prose/tables — do NOT paste full implementation source.
  </action>
  <acceptance_criteria>
    - `test -f 68-SDK-CAPABILITY.md` succeeds in the phase directory
    - All four named tokens appear literally: `grep -q 'session-runner.ts'`, `grep -q 'config.ts'`, `grep -q 'model-catalog.ts'`, `grep -q 'ws-transport.ts'` all succeed
    - Each named-module section documents its public surface: `grep -qi 'public surface\|exports' 68-SDK-CAPABILITY.md` succeeds
    - The external deps are named: `grep -q '@anthropic-ai/claude-agent-sdk' 68-SDK-CAPABILITY.md` AND `grep -qi '\bws\b\|WebSocket' 68-SDK-CAPABILITY.md` succeed
    - `grep -qi 'restoration\|rebuild\|restore' 68-SDK-CAPABILITY.md` succeeds (restoration purpose stated)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md; test -f "$F" && grep -q 'session-runner.ts' "$F" && grep -q 'config.ts' "$F" && grep -q 'model-catalog.ts' "$F" && grep -q 'ws-transport.ts' "$F"</automated>
  </verify>
  <done>All four named modules documented restoration-grade with purpose, public surface, behavior, integration points, and external deps; all grep tokens present.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate and cover the supporting sdk/src/ tree</name>
  <files>.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md</files>
  <read_first>
    - .planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-CONTEXT.md (D-04 supporting modules must be covered; planner enumerates the actual tree)
    - sdk/src/index.ts (top-level re-exports — the surface to group around)
    - sdk/src/cli.ts and sdk/src/cli-transport.ts (CLI entry surface)
    - sdk/src/query/registry.ts and sdk/src/query/command-manifest.ts (the query/ command subsystem assembly — the largest subdir, ~112 modules)
    - sdk/src/handlers/init/index.ts (init handler subsystem)
    - sdk/src/runtime-bridge-sync/index.ts, sdk/src/workstream/builder.ts, sdk/src/event-stream.ts, sdk/src/prompt-builder.ts (representative supporting subsystems)
  </read_first>
  <action>
    Append a "Supporting modules" section to `68-SDK-CAPABILITY.md` covering the rest of the `sdk/src/` tree per D-04. The tree is large (~182 non-test modules); the named four are documented in Task 1 — here, enumerate and cover the remainder by SUBSYSTEM, not file-by-file restoration-grade. Group the actual directories enumerated from the live tree:
    - top-level `sdk/src/*.ts` (session/phase/init/milestone runners, event-stream, prompt-builder, context-engine, plan-parser, gsd-transport, logger, types, etc.)
    - `sdk/src/query/` (~112 modules — the command registry/manifest/dispatch/fallback subsystem; query handlers for state/roadmap/phase/verify/validate/config/commit/etc.)
    - `sdk/src/handlers/` (init, phase, phases, roadmap, state, validate, verify handler subtrees)
    - `sdk/src/config/`, `sdk/src/dispatch/`, `sdk/src/errors/`, `sdk/src/golden/`, `sdk/src/manifest/`, `sdk/src/runtime/`, `sdk/src/runtime-bridge-sync/`, `sdk/src/state/`, `sdk/src/workstream/`
    For each subsystem capture: its responsibility, the key entry-point modules, the public surface it exposes (exported entry points / what consumes it), and how it integrates with the four named modules. The goal is that a restorer reading this section knows every subsystem that existed, what each did, and where the detail lives — sufficient for SDKR-01 to plan a compatible rebuild. Reference the full file enumeration (from the `find sdk/src` context command) so no subsystem is silently dropped.
  </action>
  <acceptance_criteria>
    - The doc names every sdk/src subdirectory as a covered subsystem: `grep -q 'query/'`, `grep -q 'handlers/'`, `grep -q 'workstream'`, `grep -q 'runtime'`, `grep -q 'golden'`, `grep -q 'dispatch'`, `grep -q 'manifest'`, `grep -q 'errors'`, `grep -q 'state' 68-SDK-CAPABILITY.md` all succeed
    - A "Supporting modules" (or equivalent) heading exists: `grep -qi 'supporting' 68-SDK-CAPABILITY.md` succeeds
    - The query subsystem is acknowledged as the largest: `grep -qi 'registry\|command-manifest\|dispatch' 68-SDK-CAPABILITY.md` succeeds
    - No subsystem from the live tree is omitted (manual cross-check against `find sdk/src -type d` enumeration)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-SDK-CAPABILITY.md; grep -qi 'supporting' "$F" && grep -q 'query/' "$F" && grep -q 'handlers/' "$F" && grep -qi 'workstream' "$F" && grep -qi 'golden' "$F"</automated>
  </verify>
  <done>Supporting sdk/src/ subsystems enumerated and covered; every live subdirectory acknowledged with responsibility and entry points.</done>
</task>

</tasks>

<verification>
- `68-SDK-CAPABILITY.md` exists in the phase directory
- Grep for each named module (`session-runner.ts`, `config.ts`, `model-catalog.ts`, `ws-transport.ts`) succeeds
- Public-surface, integration-points, and external-dependency content present (greppable)
- Every live `sdk/src/` subsystem (query/, handlers/, config/, dispatch/, errors/, golden/, manifest/, runtime/, runtime-bridge-sync/, state/, workstream/) acknowledged
- Restoration purpose stated (self-sufficient without original source)
</verification>

<success_criteria>
- SDK-01 satisfied: the fork's sdk/ capability is documented restoration-grade in .planning/ before the merge
- Each named module appears literally and is documented in full (purpose, surface, behavior, integration, deps)
- Supporting tree enumerated and covered by subsystem per D-04
- Doc is self-sufficient for a future SDKR-01 rebuild and gates SDK-02 in Phase 69
</success_criteria>

<output>
Create `.planning/milestones/v2.3.1-a-phases/68-pre-merge-inventory-backup-sdk-capture/68-02-SUMMARY.md` when done.
</output>
