---
phase: "45"
plan: "05"
type: execute
wave: 5
depends_on: ["45-04"]
files_modified:
  - tests/import-command.test.cjs
  - tests/ingest-docs.test.cjs
autonomous: true
requirements: [PIPE-45-TEST]
gap_closure: true

must_haves:
  truths:
    - "npm test passes with no new failures from Phase 45"
    - "Both test files accept Eta include tag form AND @-notation form for all 5 converted assertions"
  artifacts:
    - path: "tests/import-command.test.cjs"
      provides: "Dual-accept assertion for import workflow reference"
      contains: "hasLegacyRef || hasEtaRef"
    - path: "tests/ingest-docs.test.cjs"
      provides: "Dual-accept assertions for ingest-docs workflow and conflict-engine references"
      contains: "hasLegacyRef || hasEtaRef"
  key_links:
    - from: "tests/import-command.test.cjs"
      to: "commands/gsd/import.md"
      via: "content.includes check replaced with dual regex test"
      pattern: "hasLegacyRef || hasEtaRef"
    - from: "tests/ingest-docs.test.cjs"
      to: "commands/gsd/ingest-docs.md"
      via: "content.includes checks replaced with dual regex tests"
      pattern: "hasLegacyRef || hasEtaRef"
---

<objective>
Close the gap where Phase 45-02 converted 2 command files from @-notation to Eta include tags but did not update 5 test assertions that still check for the old @-notation form exclusively.

Purpose: Restore green `npm test` by making 5 assertions in 2 test files dual-accept both notations, following the established pattern from `tests/few-shot-calibration.test.cjs` (Phase 45-04).
Output: Updated `tests/import-command.test.cjs` and `tests/ingest-docs.test.cjs` with dual-accept assertions; clean test run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/few-shot-calibration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 5 dual-accept assertions in import-command.test.cjs and ingest-docs.test.cjs</name>
  <files>tests/import-command.test.cjs, tests/ingest-docs.test.cjs</files>
  <action>
Apply the dual-accept pattern from `tests/few-shot-calibration.test.cjs` lines 115-117 to 5 assertions across 2 test files. Each updated assertion must pass when the file contains EITHER the legacy @-notation OR the Eta include tag form.

The reference pattern (from few-shot-calibration.test.cjs):
```
const hasLegacyRef = /@~\/\.claude\/get-shit-done\/.../.test(content);
const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/...['"]\)\s*%\}/.test(content);
assert.ok(hasLegacyRef || hasEtaRef, '...');
```

**File 1: tests/import-command.test.cjs**

Replace the single assertion at line 56-59 in the `'references the import workflow'` test:

Current (lines 55-60):
```js
  test('references the import workflow', () => {
    assert.ok(
      content.includes('@~/.claude/get-shit-done/workflows/import.md'),
      'command should reference the workflow via @~/.claude/get-shit-done/workflows/import.md'
    );
  });
```

Replace with:
```js
  test('references the import workflow', () => {
    // Accept both legacy @-notation and Eta include tag form (Phase 45 converted bare-line refs)
    const hasLegacyRef = content.includes('@~/.claude/get-shit-done/workflows/import.md');
    const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/workflows\/import\.md['"]\)\s*%\}/.test(content);
    assert.ok(hasLegacyRef || hasEtaRef, 'command must reference the import workflow (@-notation or Eta include form)');
  });
```

**File 2: tests/ingest-docs.test.cjs**

There are 4 assertions to update across 2 describe blocks.

**Block A: `ingest-docs command references` (lines 78-96)**

Replace the 3 `content.includes('@~/.claude/...')` assertions:

Assertion 1 — `'references the ingest-docs workflow'` (lines 78-83):
Current:
```js
  test('references the ingest-docs workflow', () => {
    assert.ok(
      content.includes('@~/.claude/get-shit-done/workflows/ingest-docs.md'),
      'command must @-reference its workflow'
    );
  });
```
Replace with:
```js
  test('references the ingest-docs workflow', () => {
    // Accept both legacy @-notation and Eta include tag form (Phase 45 converted bare-line refs)
    const hasLegacyRef = content.includes('@~/.claude/get-shit-done/workflows/ingest-docs.md');
    const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/workflows\/ingest-docs\.md['"]\)\s*%\}/.test(content);
    assert.ok(hasLegacyRef || hasEtaRef, 'command must reference the ingest-docs workflow (@-notation or Eta include form)');
  });
```

Assertion 2 — `'references the doc-conflict-engine'` (lines 84-89):
Current:
```js
  test('references the doc-conflict-engine', () => {
    assert.ok(
      content.includes('@~/.claude/get-shit-done/references/doc-conflict-engine.md'),
      'command must load the shared conflict-engine contract'
    );
  });
```
Replace with:
```js
  test('references the doc-conflict-engine', () => {
    // Accept both legacy @-notation and Eta include tag form (Phase 45 converted bare-line refs)
    const hasLegacyRef = content.includes('@~/.claude/get-shit-done/references/doc-conflict-engine.md');
    const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%\}/.test(content);
    assert.ok(hasLegacyRef || hasEtaRef, 'command must load the shared conflict-engine contract (@-notation or Eta include form)');
  });
```

Assertion 3 — `'references gate-prompts'` (lines 90-95):
Current:
```js
  test('references gate-prompts', () => {
    assert.ok(
      content.includes('@~/.claude/get-shit-done/references/gate-prompts.md'),
      'command must load gate-prompts for AskUserQuestion patterns'
    );
  });
```
Replace with:
```js
  test('references gate-prompts', () => {
    // Accept both legacy @-notation and Eta include tag form (Phase 45 converted bare-line refs)
    const hasLegacyRef = content.includes('@~/.claude/get-shit-done/references/gate-prompts.md');
    const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/references\/gate-prompts\.md['"]\)\s*%\}/.test(content);
    assert.ok(hasLegacyRef || hasEtaRef, 'command must load gate-prompts for AskUserQuestion patterns (@-notation or Eta include form)');
  });
```

**Block B: `import command adopts shared conflict-engine` (lines 293-313)**

Assertion 4 — `'import command loads doc-conflict-engine reference'` (lines 297-301):
Current:
```js
  test('import command loads doc-conflict-engine reference', () => {
    assert.ok(
      cmdContent.includes('@~/.claude/get-shit-done/references/doc-conflict-engine.md'),
      '/gsd-import must load the shared conflict-engine contract'
    );
  });
```
Replace with:
```js
  test('import command loads doc-conflict-engine reference', () => {
    // Accept both legacy @-notation and Eta include tag form (Phase 45 converted bare-line refs)
    const hasLegacyRef = cmdContent.includes('@~/.claude/get-shit-done/references/doc-conflict-engine.md');
    const hasEtaRef = /\{%~\s*include\(['"]get-shit-done\/references\/doc-conflict-engine\.md['"]\)\s*%\}/.test(cmdContent);
    assert.ok(hasLegacyRef || hasEtaRef, '/gsd-import must load the shared conflict-engine contract (@-notation or Eta include form)');
  });
```
  </action>
  <verify>
    <automated>cd /home/thamw/development/remote-dev/get-shit-done && node --test tests/import-command.test.cjs && node --test tests/ingest-docs.test.cjs</automated>
  </verify>
  <done>Both test files pass with 0 failures. All 5 updated assertions accept the Eta include tag form present in the actual command files.</done>
</task>

<task type="auto">
  <name>Task 2: Run full npm test and commit atomically</name>
  <files>tests/import-command.test.cjs, tests/ingest-docs.test.cjs</files>
  <action>
Run the full test suite to confirm no regressions beyond the 5 targeted assertions. Then commit both test files in a single atomic commit.

Run: `npm test`

If npm test passes, commit with:
```
git add tests/import-command.test.cjs tests/ingest-docs.test.cjs
git commit -m "test(45-05): dual-accept Eta include tags in import and ingest-docs test assertions"
```

The commit message must reference phase 45-05 and describe the dual-accept change, not just "fix tests".
  </action>
  <verify>
    <automated>cd /home/thamw/development/remote-dev/get-shit-done && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>npm test exits 0. Git log shows the 45-05 commit containing exactly 2 files: tests/import-command.test.cjs and tests/ingest-docs.test.cjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → production file | Tests read source files via fs.readFileSync; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Tampering | test assertions | accept | Test-only change; no production code modified; reviewed in PR |
</threat_model>

<verification>
1. `node --test tests/import-command.test.cjs` — all tests pass
2. `node --test tests/ingest-docs.test.cjs` — all tests pass
3. `npm test` — full suite passes with 0 new failures
4. `git log --oneline -1` — shows the 45-05 commit
</verification>

<success_criteria>
- 5 previously-failing assertions now pass
- No existing passing tests are broken
- Both test files accept {%~ include('get-shit-done/...') %} as a valid form alongside @~/.claude/... form
- npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/45-pipeline-integration/45-05-SUMMARY.md` when done.
</output>
