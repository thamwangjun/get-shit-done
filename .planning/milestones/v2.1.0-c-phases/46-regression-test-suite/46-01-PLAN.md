---
phase: 46-regression-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - commands/gsd/*.md
  - agents/*.md
  - get-shit-done/workflows/*.md
  - get-shit-done/references/*.md
autonomous: true
requirements:
  - INTG-01

must_haves:
  truths:
    - "D-01: Eta constructor in bin/install.js uses default delimiters — lines containing `tags: ['{%', '%}']` and `parse: { raw: '~' }` are absent"
    - "D-02: All source files use `<%~ include(` — zero occurrences of `{%~ include(` survive in commands/gsd/, agents/, or get-shit-done/"
    - "D-03: npm test passes with zero new failures after the delimiter switch and source conversion"
  artifacts:
    - path: "bin/install.js"
      provides: "Eta constructor without custom delimiter config"
      contains: "const eta = new Eta({"
  key_links:
    - from: "bin/install.js Eta constructor"
      to: "Eta default delimiter behavior"
      via: "removal of tags and parse.raw overrides"
      pattern: "new Eta\\(\\{[^}]*useWith"
---

<objective>
Remove the custom Eta delimiter configuration (`tags: ['{%', '%}']` and `parse: { raw: '~' }`) from the Eta constructor in `bin/install.js`, then convert all source files from `{%~ include(` to `<%~ include(` (Eta's default). Verify zero survivors and confirm npm test passes.

Purpose: Eta 4.6.0 defaults to `<%`/`%>` delimiters and `~` raw prefix — the explicit custom config added in Phase 45 is redundant. Removing it eliminates unnecessary configuration drift and aligns source files with the Eta default convention.

Output: Updated `bin/install.js` (3 lines removed from Eta constructor); 83 source `.md` files converted; npm test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-regression-test-suite/46-CONTEXT.md
@.planning/phases/46-regression-test-suite/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove custom delimiter config from Eta constructor</name>
  <files>bin/install.js</files>
  <action>
Read `bin/install.js` lines 1750–1768 to confirm the exact Eta constructor block. Remove the two custom delimiter lines (per D-01):

- Remove: `tags: ['{%', '%}'],`
- Remove: `parse: { raw: '~' },`

The constructor after removal must be:

```javascript
const eta = new Eta({
  views: _etaSourceRoot,
  useWith: true,
  autoEscape: false,
});
```

The `eta.resolvePath` override at lines 1764–1767 must be preserved unchanged. Do not touch any other lines in the file.

Eta 4.6.0 defaults are `tags: ['<%', '%>']` and `parse.raw: '~'` — the removal produces identical runtime behavior (per D-01, VERIFIED in RESEARCH.md by live Eta config inspection).
  </action>
  <verify>
    <automated>grep -n "tags: \['{%" bin/install.js; echo "exit: $?" && grep -n "parse: { raw:" bin/install.js; echo "exit: $?"</automated>
  </verify>
  <done>Neither `tags: ['{%'` nor `parse: { raw: '~' }` appears in `bin/install.js`. The Eta constructor retains `views`, `useWith`, and `autoEscape` lines.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all source files from {%~ to <%~ delimiter</name>
  <files>commands/gsd/*.md, agents/*.md, get-shit-done/workflows/*.md, get-shit-done/references/*.md</files>
  <action>
Run the following sed one-liner to atomically replace all Eta include tags across the 83 source files (per D-02):

```bash
find commands/gsd agents get-shit-done -name '*.md' | xargs sed -i 's/{%~ include(/<%~ include(/g; s/ %}/ %>/g'
```

Then run the post-conversion verification grep:

```bash
grep -r '{%~ include' commands/ agents/ get-shit-done/
```

Zero output from the grep confirms complete conversion. If any survivors appear, read those files and apply the replacement manually.

Note on the sed pattern: `s/ %}/ %>/g` is safe because source files exclusively use `{%` / `%}` for Eta tags — no other prose uses this exact pattern. The post-conversion broader safety check runs in the verify step.
  </action>
  <verify>
    <automated>grep -r '{%~ include' commands/ agents/ get-shit-done/ && echo "SURVIVORS FOUND" || echo "Zero survivors — conversion complete"</automated>
  </verify>
  <done>Zero occurrences of `{%~ include(` in any `.md` file under `commands/gsd/`, `agents/`, or `get-shit-done/`. Post-conversion grep returns no output.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm npm test passes with zero new failures</name>
  <files></files>
  <action>
Run the full test suite to confirm the delimiter switch and source conversion introduced no regressions (per D-03):

```bash
npm test
```

All tests that passed before this plan must continue to pass. The Eta constructor change is a no-op at runtime (defaults are identical to the removed explicit config), so zero test failures are expected.

If any test fails, read the failure output carefully. Failures in `install-eta-regression.test.cjs` are expected to not exist yet (Plan 02 creates that file). Any failure in other test files is a regression introduced by this plan — investigate and fix before proceeding to Plan 02.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>`npm test` exits 0. No new test failures beyond any pre-existing baseline. The delimiter removal and source file conversion are confirmed non-regressive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sed one-liner → source .md files | Bulk in-place edit; incorrect pattern could corrupt include tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | sed one-liner over 83 source files | mitigate | Post-conversion grep (`grep -r '{%~ include'`) confirms zero survivors; `npm test` catches any rendering breakage introduced by incorrect substitution |
| T-46-02 | Tampering | bin/install.js Eta constructor edit | mitigate | Verify constructor shape after edit via grep; `npm test` catches any Eta config regression |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -n "tags: \['{%" bin/install.js` — returns no output
2. `grep -n "parse: { raw:" bin/install.js` — returns no output
3. `grep -r '{%~ include' commands/ agents/ get-shit-done/` — returns no output
4. `npm test` — exits 0, no new failures
</verification>

<success_criteria>
- Eta constructor in `bin/install.js` uses default delimiters (no custom `tags` or `parse.raw` config)
- All 83 source `.md` files use `<%~ include(` instead of `{%~ include(`
- `npm test` passes with zero new failures
- INTG-01 custom delimiter config is eliminated; default behavior is preserved
</success_criteria>

<output>
Create `.planning/phases/46-regression-test-suite/46-01-SUMMARY.md` when done.
</output>
