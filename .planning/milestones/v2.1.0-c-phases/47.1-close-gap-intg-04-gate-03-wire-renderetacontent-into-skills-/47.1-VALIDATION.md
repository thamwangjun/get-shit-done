---
phase: "47.1"
slug: close-gap-intg-04-gate-03-wire-renderetacontent-into-skills
status: complete
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-29
audited: 2026-05-29
---

# Phase 47.1 — Validation Strategy

> Per-phase validation contract — Nyquist audit completed post-execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `--test` runner |
| **Config file** | none — native runner, no config file |
| **Quick run command** | `node --test tests/install-eta-regression.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~300ms (quick); ~5–10 min (full suite) |

---

## Sampling Rate

- **After every task commit:** `node --test tests/install-eta-regression.test.cjs && node --test tests/runtime-artifact-layout.test.cjs`
- **After every plan wave:** `npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds (quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 47.1-01-01 | 01 | 1 | INTG-04 | T-47.1-01 | renderEtaContent processes only repo source content — no user input reaches Eta renderer | integration | `node --test tests/install-eta-regression.test.cjs` | ✅ | ✅ green |
| 47.1-01-02 | 01 | 1 | GATE-03 | T-47.1-01 | No `<%~` directive survives in any installed .md file — full Claude runtime install scanned | integration | `node --test tests/install-eta-regression.test.cjs` | ✅ | ✅ green |
| 47.1-01-03 | 01 | 1 | INTG-04 | — | skills `stage()` returns SKILL.md files (structure check) | unit | `node --test tests/runtime-artifact-layout.test.cjs` | ✅ | ✅ green |
| 47.1-01-04 | 01 | 1 | INTG-04 | — | Full npm test suite — no regressions introduced | regression | `npm test` | ✅ | ✅ green |
| 47.1-02-01 | 02 | 2 | INTG-04, GATE-03 | — | REQUIREMENTS.md annotated; bookkeeping only — no behavioral change | manual | `grep -c "phase 47.1" .planning/REQUIREMENTS.md` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.*

No test stubs were needed — the existing test files (`install-eta-regression.test.cjs`, `runtime-artifact-layout.test.cjs`) were already in place. Plan 01 added a second `test()` inside the existing `TEST-01` describe block.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| REQUIREMENTS.md + ROADMAP.md bookkeeping annotations | INTG-04, GATE-03 | Planning artifact edits have no executable behavior — correctness verified by grep assertions in SUMMARY.md self-check | `grep -c "phase 47.1" .planning/REQUIREMENTS.md` → ≥2; `grep "Plans: 2 plans" .planning/ROADMAP.md` → match |

---

## Gap Analysis (Nyquist Audit — 2026-05-29)

| Requirement | Coverage Source | Status | Notes |
|-------------|----------------|--------|-------|
| INTG-04: Eta renderer wired into skills pipeline | `install-eta-regression.test.cjs` TEST-01 (E2E `<%~` scan) + `runtime-artifact-layout.test.cjs` stage tests | COVERED | E2E test walks all installed .md files; unit test verifies SKILL.md file structure. Content-level `<%~` check is in the E2E test. |
| GATE-03: No unrendered directives in installed output | `install-eta-regression.test.cjs` TEST-01 (line 158) | COVERED | Dedicated test: full Claude runtime install, walks all .md files, fails on any `<%~` match. 6/6 pass. |

**Gaps found: 0** — No missing or partial coverage. All success criteria have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are manual bookkeeping
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 not required — existing infrastructure covered all requirements
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-29

---

## Validation Audit 2026-05-29

| Metric | Count |
|--------|-------|
| Requirements audited | 2 (INTG-04, GATE-03) |
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |
| Test files checked | 2 (`install-eta-regression.test.cjs`, `runtime-artifact-layout.test.cjs`) |
| Tests passing | 6/6 (`install-eta-regression`) + 31/31 (`runtime-artifact-layout`) |
