---
phase: "47.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "47.1-01"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INTG-04
  - GATE-03

must_haves:
  truths:
    - "INTG-04 and GATE-03 updated to reflect full satisfaction (skills path now wired)"
    - "ROADMAP.md Phase 47.1 goal and success criteria updated with actual plan count"
    - "STATE.md advanced to reflect phase 47.1 completion"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "INTG-04 and GATE-03 annotated as fully satisfied"
      contains: "skills path"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 47.1 plans populated"
      contains: "47.1-01"
---

<objective>
Close INTG-04 and GATE-03 in `.planning/REQUIREMENTS.md` as fully satisfied (skills path now wired), update ROADMAP.md Phase 47.1 plans, and advance STATE.md.

This plan runs after 47.1-01 confirms all tests pass.

Output:
- `.planning/REQUIREMENTS.md`: INTG-04 and GATE-03 annotated with the skills-path fix from phase 47.1
- `.planning/ROADMAP.md`: Phase 47.1 plan list updated
- `.planning/STATE.md`: phase 47.1 recorded
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.1.0-c-MILESTONE-AUDIT.md
@.planning/phases/47.1-close-gap-intg-04-gate-03-wire-renderetacontent-into-skills-/47.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update INTG-04 and GATE-03 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` to locate the current INTG-04 and GATE-03 entries.

For **INTG-04**, the current entry reads:
```
- [x] **INTG-04**: Eta renderer wired into `copyWithPathReplacement()` as the first transform step ...
```
Append the phase 47.1 closure note inline:
```
- [x] **INTG-04**: Eta renderer wired into `copyWithPathReplacement()` as the first transform step ... (skills path: `renderEtaContent` wired into `wrappedConverter` in `runtime-artifact-layout.cjs` — closed phase 47.1)
```

For **GATE-03**, the current entry reads:
```
- [x] **GATE-03**: `grep -r '@~/.claude/' <install-dir>` on a fresh install ... closed 2026-05-29)
```
Append the phase 47.1 note:
```
- [x] **GATE-03**: `grep -r '@~/.claude/' <install-dir>` on a fresh install ... closed 2026-05-29); skills-path `<%~ include()` gap closed phase 47.1 — TEST-01 now also detects unrendered Eta directives)
```

Preserve all other content exactly as-is. Write only the lines that changed.
  </action>
  <verify>
    <automated>command grep -c "phase 47.1" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md updated: INTG-04 and GATE-03 entries annotated with phase 47.1 closure. File parses as valid Markdown.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 47.1 section</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read `.planning/ROADMAP.md` and locate the Phase 47.1 section. Update it to reflect:
- **Goal:** Wired `renderEtaContent` into skills staging pipeline; expanded TEST-01 to detect unrendered Eta directives. Closes INTG-04/GATE-03 for skills-based runtimes.
- **Requirements**: INTG-04, GATE-03
- **Plans:** 2 plans

Replace the placeholder plan line:
```
- [ ] TBD (run /gsd-plan-phase 47.1 to break down)
```
With:
```
- [x] 47.1-01: Wire renderEtaContent into wrappedConverter + expand TEST-01
- [x] 47.1-02: Close INTG-04/GATE-03 bookkeeping
```

Update `**Plans:** 0 plans` → `**Plans:** 2 plans`.
Update `**Goal:** [Urgent work - to be planned]` → `**Goal:** Wire renderEtaContent into skills staging pipeline; expand TEST-01 to detect unrendered Eta directives. Closes INTG-04/GATE-03 for skills-based runtimes (BLOCKER-01 from v2.1.0-c audit).`
Update `**Requirements**: TBD` → `**Requirements**: INTG-04, GATE-03`.
  </action>
  <verify>
    <automated>command grep -A 10 "Phase 47.1" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 47.1 section updated with goal, requirements, and 2 plan entries marked complete.</done>
</task>

<task type="auto">
  <name>Task 3: Record phase 47.1 in STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
Use `gsd-tools.cjs` to record phase 47.1 completion:

```bash
node get-shit-done/bin/gsd-tools.cjs state patch --phase 47.1 --status complete 2>&1 || true
```

If the above command is unavailable or errors, manually update STATE.md to record that phase 47.1 is complete. Read STATE.md first, then append or update the phase 47.1 entry to show `status: complete`.
  </action>
  <verify>
    <automated>command grep -i "47.1" .planning/STATE.md || echo "STATE.md: phase 47.1 entry check"</automated>
  </verify>
  <done>STATE.md records phase 47.1 as complete.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47.1-B01 | Tampering | REQUIREMENTS.md edits | accept | Read before write; append-only annotation to existing entries |
| T-47.1-B02 | Tampering | ROADMAP.md edits | accept | Read before write; surgical replacement of placeholder lines |
| T-47.1-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan |
</threat_model>

<verification>
After all tasks complete:

1. `command grep -c "phase 47.1" .planning/REQUIREMENTS.md` — must return ≥2 (INTG-04 + GATE-03)
2. `command grep "Plans: 2" .planning/ROADMAP.md` — must match
3. `command grep -i "47.1" .planning/STATE.md` — must match
</verification>

<success_criteria>
- SC #1: INTG-04 and GATE-03 annotated in REQUIREMENTS.md with phase 47.1 closure
- SC #2: ROADMAP.md Phase 47.1 section complete with goal, requirements, and 2 plan entries
- SC #3: STATE.md records phase 47.1
</success_criteria>

<output>
Create `.planning/phases/47.1-close-gap-intg-04-gate-03-wire-renderetacontent-into-skills-/47.1-02-SUMMARY.md` when done.
</output>
