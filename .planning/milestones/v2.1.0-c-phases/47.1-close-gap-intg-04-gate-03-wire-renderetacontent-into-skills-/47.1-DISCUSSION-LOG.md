# Phase 47.1: Close gap INTG-04/GATE-03 — wire renderEtaContent into skills install path - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 47.1-close-gap-intg-04-gate-03-wire-renderetacontent-into-skills-
**Areas discussed:** Fix location, Test upgrade

---

## Fix location

| Option | Description | Selected |
|--------|-------------|----------|
| wrappedConverter in runtime-artifact-layout.cjs | Add renderEtaContent to the wrappedConverter factory via getInstallExports(). Minimal, localized — no new function parameters. etaViewsRoot derived from srcCommandsDir. | ✓ |
| stageSkillsForRuntimeAsSkills in install-profiles.cjs | Add optional renderFn parameter to stageSkillsForRuntimeAsSkills and pass renderEtaContent from the call site. More explicit API but touches the function signature and all callers. | |
| Let Claude decide | Claude picks whichever is cleaner. | |

**User's choice:** Asked which option produces cleaner, more coherent, and understandable code — deferred to Claude. Claude chose wrappedConverter in runtime-artifact-layout.cjs (content transformation already composed there, no new parameters, consistent with existing getInstallExports() pattern).
**Notes:** The audit identified install-profiles.cjs lines 384–394 as the primary fix location, but also noted the wrappedConverter factory as an alternative. The wrappedConverter approach is preferred because it avoids adding a parameter to stageSkillsForRuntimeAsSkills and keeps all content transformation logic in one place.

---

## Test upgrade

| Option | Description | Selected |
|--------|-------------|----------|
| Expand TEST-01 to also check for <%~ include | Add a second assertion in TEST-01 that scans installed SKILL.md files for unrendered Eta directives. Same full-install walk. | ✓ |
| Add TEST-06 as a dedicated new test | Keep TEST-01 as the @~/.claude/ ref check, add TEST-06 specifically for unrendered Eta tag detection. | |
| Let Claude decide | Claude picks the right test structure. | |

**User's choice:** Expand TEST-01 to also check for `<%~ include` tags.
**Notes:** No ALLOWED_ETA_PATTERNS allowlist needed — any surviving `<%~` in installed output is always a bug (unlike `@~/.claude/` refs which can appear in intentional prose instructions).

---

## Claude's Discretion

- Exact failure message wording for the new `<%~ include` detection
- Whether to fold the check into the existing TEST-01 describe block or add a sibling assertion
- Regression test name / description string for the new check
- etaViewsRoot verification (confirm `path.join(srcCommandsDir, '..', '..')` resolves to same value as `_etaSourceRoot` in install.js)

## Deferred Ideas

None.
