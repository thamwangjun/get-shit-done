---
phase: 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
plan: 04
type: execute
wave: 3
depends_on: ["55.2-02"]
files_modified:
  - sdk/src/query/intel.ts
  - sdk/src/golden/read-only-golden-rows.ts
  - sdk/src/query/query-error-taxonomy.ts
  - sdk/src/golden/golden-policy.ts
  - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-203: the intel JSON-parity rows (intel.status/diff/validate/query/extract-exports) now dispatch through the registry (registered by Plan 02) and their SDK output byte-matches the intel.cjs oracle — SDK fixed to match, assertions unrelaxed (D-201)"
    - "The meta/policy failures are reconciled: executeForCjs native_failure classification reflects that intel.update/verify.codebase-drift are now registered (no longer unknown-command), and the golden coverage policy ('every canonical registry command is integration-covered or excepted') passes for the newly-registered canonicals"
    - "D-205: every remaining failing test (intel parity rows + meta/policy) has its VERIFICATION row set to a confirmed cause"
  artifacts:
    - path: .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
      provides: "intel parity + meta/policy cluster classified"
      contains: "native_failure"
  key_links:
    - from: "intel parity rows in read-only-golden-rows.ts"
      to: "registered intel.* handlers (Plan 02)"
      via: "registry.dispatch resolves; sdkResult.data toEqual intel.cjs oracle"
      pattern: "intel\\.(status|diff|validate|query|extract-exports)"
---

<objective>
Close the intel JSON-parity cluster (coupled to Plan 02's intel registration) and the meta/policy
cluster. With intel.* registered, the read-only parity rows for intel.status/diff/validate/query/
extract-exports now dispatch — fix any residual divergence so SDK byte-matches the intel.cjs oracle
(D-203). Reconcile the executeForCjs native_failure classification (the registered commands no
longer throw unknown-command) and confirm the golden coverage policy passes for the registered
canonicals. Finalize all remaining VERIFICATION cause rows.

Purpose: intel parity (depends on Plan 02 registration) + meta/policy reconciliation.
Output: passing intel parity + policy tests; fully-classified VERIFICATION cause column.
</objective>

<context>
!`cd /home/thamw/development/remote-dev/get-shit-done && cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel" 2>&1 | tee /tmp/gsd-55-2-intelparity.txt | command grep -E "Tests .*(passed|failed)|expected|received" | head -40`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile intel.status/diff/validate/query/extract-exports JSON parity</name>
  <files>sdk/src/query/intel.ts, sdk/src/golden/read-only-golden-rows.ts</files>
  <read_first>
    - sdk/src/query/intel.ts (intel.status/diff/validate/query/extract-exports handler bodies)
    - get-shit-done/bin/lib/intel.cjs (canonical oracle output per D-203)
    - sdk/src/golden/read-only-golden-rows.ts (intel parity rows: intel.status line 35, diff 36, validate 37, query 38, extract-exports 39-44)
    - sdk/src/golden/read-only-parity.integration.test.ts (harness: captureGsdToolsOutput → registry.dispatch → toEqual, lines 26-28)
    - 55.2-02-SUMMARY.md (confirm intel.* was registered so these rows dispatch instead of throwing)
  </read_first>
  <behavior>
    - registry.dispatch for each intel parity canonical resolves (registered by Plan 02) and sdkResult.data toEqual the intel.cjs oracle
    - assertions unrelaxed (D-201); SDK fixed to match CJS (D-203)
  </behavior>
  <action>
    For each intel parity row (intel.status/diff/validate/query/extract-exports), run the read-only
    parity harness, capture the intel.cjs oracle, and diff the SDK handler output. Per D-203 fix the
    SDK handler bodies in intel.ts to byte-match the canonical CJS output; never relax the assertion
    (D-201). If a divergence is a confirmed legitimate CLI change, regenerate the golden row's
    expectation from confirmed-correct CLI output only (D-201/D-204). These rows depend on Plan 02's
    intel registration to dispatch — confirm registration landed before fixing. Set each intel
    parity row's VERIFICATION cause.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel" 2>&1 | tee /tmp/gsd-55-2-intelfix.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - intel.status/diff/validate/query/extract-exports parity tests pass (`failed` 0 in the filtered summary line)
    - SDK handlers fixed to match intel.cjs; no assertion relaxed
    - VERIFICATION rows for the five intel parity tests carry a confirmed cause
  </acceptance_criteria>
  <done>intel JSON-parity rows green against intel.cjs oracle; causes recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile meta/policy — native_failure classification + golden coverage policy</name>
  <files>sdk/src/query/query-error-taxonomy.ts, sdk/src/golden/golden-policy.ts, .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</files>
  <read_first>
    - sdk/src/query/query-error-taxonomy.ts (nativeFailureError lines 21-35 — the native_failure classification path)
    - sdk/src/runtime-bridge-sync/index.ts (executeForCjs; native_failure consumed line 64)
    - sdk/src/golden/golden-policy.ts (verifyGoldenPolicyComplete lines 103-120; exception-row format lines 25-62)
    - sdk/src/golden/registry-canonical-commands.ts (getCanonicalRegistryCommands lines 9-32 — registered canonicals the policy gate enforces)
    - the failing meta tests: query-error-taxonomy.test.ts, query-dispatch.test.ts, runtime-bridge-sync/index.test.ts, and the golden coverage-policy test
  </read_first>
  <behavior>
    - executeForCjs native_failure classification reflects that intel.update/verify.codebase-drift are registered (no longer unknown-command)
    - verifyGoldenPolicyComplete passes: every canonical from getCanonicalRegistryCommands() is integration-covered or excepted with rationale
  </behavior>
  <action>
    Reconcile the meta/policy cluster against the post-Plan-02 registry. For the executeForCjs >
    native_failure classification test: confirm whether it expected intel.update/verify.codebase-drift
    to classify as native_failure *because unregistered* — now that they are registered (D-204), update
    the expectation to "registered + parity" rather than relaxing it (D-201); fix source only if a real
    classification regression is found. For the golden coverage policy
    (verifyGoldenPolicyComplete): the newly-registered intel.* + verify.codebase-drift canonicals must
    each be in GOLDEN_PARITY_INTEGRATION_COVERED or carry a GOLDEN_PARITY_EXCEPTIONS rationale row
    (mirror the exception-row format at golden-policy.ts:26-27) — add coverage/exception rows as needed
    so the policy test passes. Set the meta/policy test rows in VERIFICATION to their confirmed cause.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/query/query-error-taxonomy.test.ts src/runtime-bridge-sync/index.test.ts src/golden/golden-policy.test.ts 2>&1 | tee /tmp/gsd-55-2-meta.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - native_failure classification tests pass with expectations updated to the registered state (not relaxed)
    - verifyGoldenPolicyComplete passes — registered intel.*/verify.codebase-drift canonicals covered or excepted with rationale
    - VERIFICATION rows for the meta/policy tests carry a confirmed cause
  </acceptance_criteria>
  <done>native_failure classification + golden coverage policy green for the registered canonicals; causes recorded.</done>
</task>

</tasks>

<threat_model>
No new attack surface — test-reconciliation phase; changes are golden-fixture/handler parity and
coverage-policy bookkeeping for newly-registered native handlers within the existing trust
boundary. No network, auth, user input, or data handling introduced.
</threat_model>

<verification>
intel parity rows + native_failure classification + golden coverage policy tests pass (unrelaxed);
VERIFICATION cause column fully populated for these tests.
</verification>

<success_criteria>
Intel JSON-parity green against intel.cjs (D-203, coupling to Plan 02 resolved); meta/policy
reconciled to the registered state without relaxing assertions (D-201); all remaining causes
recorded (D-205).
</success_criteria>

<output>
Create .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-04-SUMMARY.md when done.
</output>
