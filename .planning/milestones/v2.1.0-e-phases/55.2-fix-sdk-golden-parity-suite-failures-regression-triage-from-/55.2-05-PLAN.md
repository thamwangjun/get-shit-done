---
phase: 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
plan: 05
type: execute
wave: 4
depends_on: ["55.2-02", "55.2-03", "55.2-04"]
files_modified:
  - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The SDK suite reports 0 failures, confirmed from the `Tests N passed / N failed` summary line in /tmp/gsd-55-2-sdk.txt — NOT the tee/$? exit code"
    - "The ROOT suite stays green (fail 0), confirmed from the summary line in /tmp/gsd-55-2-root.txt — no regression introduced by Plans 02-04"
    - "D-205: 55.2-VERIFICATION.md classifies all 17 failing tests with a confirmed cause (missing-handler / stale-fixture / real-divergence) and names the introducing commit; D-204 register-vs-exception choice and D-203 oracle policy are recorded; no assertion was relaxed and no new skip was added to hide a failure (D-201)"
  artifacts:
    - path: .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
      provides: "final SDK-green + ROOT-green evidence and the finalized 17-test classification + introducing commit"
      contains: "introducing commit"
  key_links:
    - from: "SDK suite + ROOT suite summary lines"
      to: "55.2-VERIFICATION.md"
      via: "both report fail 0, recorded in verification"
      pattern: "failed"
---

<objective>
Terminal whole-suite gate, mirroring 55.1-04. Run BOTH suites once, read from the piped summary
lines (not exit codes), and confirm SDK fail 0 + ROOT stays green. Finalize 55.2-VERIFICATION.md:
all 17 tests classified with confirmed causes, the introducing commit named, the D-204 and D-203
decisions recorded, and confirmation that no assertion was relaxed and no new skip was added.
Runs last (Wave 4) after Plans 02-04 land.

Purpose: D-205 end-state — SDK green without masking, ROOT unregressed, full classification.
Output: finalized 55.2-VERIFICATION.md with pass evidence.
</objective>

<context>
!`cd /home/thamw/development/remote-dev/get-shit-done && cd sdk && npm test 2>&1 | tee /tmp/gsd-55-2-sdk.txt | command grep -E "Tests .*(passed|failed)" | tail -5`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run SDK + ROOT suites; confirm fail 0 from summary lines; finalize VERIFICATION</name>
  <files>.planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md (the skeleton + cause rows filled by Plans 01-04 — finalize, do not overwrite the recorded causes)
    - .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-VERIFICATION.md (sibling terminal-gate format to mirror)
    - CLAUDE.md (test discipline: read pass/fail from the `Tests N passed / N failed` summary line, not tee/$?; use command grep)
    - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-CONTEXT.md (D-201 no-relax / no-new-skip; D-205 classify-all-17 + name-commit)
  </read_first>
  <action>
    Run the SDK suite once: `cd sdk && npm test 2>&1 | tee /tmp/gsd-55-2-sdk.txt`, then read the file
    (do not re-run to re-read). Confirm fail 0 from the `Tests N passed / N failed` summary line, NOT
    the tee/$? exit code. Run the ROOT suite once: `npm test 2>&1 | tee /tmp/gsd-55-2-root.txt` and
    confirm ROOT stays green (fail 0) from its summary line. Confirm no new `.skip(`/`.todo(` was added
    by Plans 02-04 (git log -p over the edited SDK files) to hide a failure (D-201). If any SDK or ROOT
    failure remains, root-cause it against the relevant cluster verdict and fix it (do not skip);
    re-run only the affected suite. Finalize 55.2-VERIFICATION.md: confirm all 17 rows have a confirmed
    cause (missing-handler / stale-fixture / real-divergence), the introducing commit/surface is named
    (from Plan 01), the D-204 register-vs-exception choice and D-203 oracle policy are recorded, and add
    a final section capturing SDK pass/fail counts, ROOT pass/fail counts, and the no-relax / no-new-skip
    confirmation. Mirror 55.1-VERIFICATION.md structure.
  </action>
  <verify>
    <automated>command grep -E "Tests .*(passed|failed)" /tmp/gsd-55-2-sdk.txt | command grep -vE "[1-9][0-9]* failed" &amp;&amp; command grep -cE "missing-handler|stale-fixture|real-divergence" .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/gsd-55-2-sdk.txt summary line shows 0 failed (no `[1-9]N failed`)
    - /tmp/gsd-55-2-root.txt summary line shows ROOT fail 0 (no regression)
    - no new `.skip(`/`.todo(` added by Plans 02-04 (verified via git log -p)
    - 55.2-VERIFICATION.md classifies all 17 tests with a confirmed cause, names the introducing commit, and records the D-204 + D-203 decisions
  </acceptance_criteria>
  <done>SDK fail 0 and ROOT fail 0 (from summary lines); no assertion relaxed; no new skip; 55.2-VERIFICATION.md finalized with all 17 classified + introducing commit named.</done>
</task>

</tasks>

<threat_model>
No new attack surface — test-reconciliation phase; this terminal plan only runs test suites and
finalizes a planning doc within the existing trust boundary. No network, auth, user input, or
data handling introduced.
</threat_model>

<verification>
SDK suite → fail 0 and ROOT suite → fail 0 (both from summary lines); no new skips; 55.2-VERIFICATION.md
classifies all 17 + names the introducing commit.
</verification>

<success_criteria>
SDK green without masking and ROOT unregressed (both confirmed from summary lines, not exit codes);
no assertion relaxed and no new skip (D-201); VERIFICATION classifies all 17 and names the
introducing commit (D-205).
</success_criteria>

<output>
Create .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-05-SUMMARY.md when done.
</output>
