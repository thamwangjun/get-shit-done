# Phase 55.2: Fix SDK golden-parity suite failures (regression triage from phase 55) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
**Areas discussed:** Bisect rigor, Parity oracle default, Handler vs exception, Plan slicing

---

## Bisect rigor

| Option | Description | Selected |
|--------|-------------|----------|
| Best-effort (suspect-first) | Confirm regression, root-cause from named suspects e4a841cd/abfcbf2b, record evidence; full multi-checkout bisect only if suspects don't explain it | ✓ |
| Hard requirement | Run SDK suite at successive checkouts until exact commit pinned, regardless of cost | |

**User's choice:** Best-effort
**Notes:** Repeated full SDK-suite runs are costly; introducing surface already narrowed → D-202.

---

## Parity oracle default

| Option | Description | Selected |
|--------|-------------|----------|
| gsd-tools.cjs canonical | CLI is reference oracle; fix SDK to match unless evidence shows the CLI regressed | ✓ |
| Decide fully per-case | No default lean; every mismatch adjudicated with no presumption | |

**User's choice:** gsd-tools.cjs canonical
**Notes:** → D-203. Stale fixtures regenerated only from confirmed-correct CLI output.

---

## Handler vs exception

| Option | Description | Selected |
|--------|-------------|----------|
| Implement, exception as fallback | Default to registering native handlers (mirror intel.status / verify.schema-drift); coverage exception acceptable only if proven CLI-only, documented in VERIFICATION | ✓ |
| Always implement | Both must get native handlers; no exception permitted | |

**User's choice:** Implement, exception as fallback
**Notes:** → D-204.

---

## Plan slicing

| Option | Description | Selected |
|--------|-------------|----------|
| Bisect gate, then by cluster | Plan 1 bisect/root-cause gate; cluster plans; final whole-suite gate mirroring 55.1-04 | |
| Let planner decide | Leave decomposition to /gsd-plan-phase; capture constraints only | ✓ |

**User's choice:** Let planner decide
**Notes:** Captured as guidance (bisect-first gating, cluster grouping, final whole-suite gate) but planner owns wave structure.

## Claude's Discretion

- Plan/wave decomposition deferred to `/gsd-plan-phase`.

## Deferred Ideas

None — discussion stayed within phase scope.
