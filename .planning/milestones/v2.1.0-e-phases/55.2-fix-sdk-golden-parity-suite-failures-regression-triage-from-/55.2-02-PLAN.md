---
phase: 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
plan: 02
type: execute
wave: 2
depends_on: ["55.2-01"]
files_modified:
  - sdk/src/handlers/verify/index.ts
  - sdk/src/query/verify.ts
  - sdk/src/query/command-manifest.verify.ts
  - sdk/src/query/command-static-catalog-domain.ts
  - sdk/src/query/command-manifest.non-family.ts
  - sdk/src/golden/golden-integration-covered.ts
  - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-204: intel.update and verify.codebase-drift native handlers are registered by default so registry.dispatch resolves them and the hard 'no native handler registered' failures clear — exception-allowlist fallback used only if investigation proves CLI-only, with rationale recorded in VERIFICATION"
    - "D-203: gsd-tools.cjs is canonical — the registered handlers' output byte-matches the CJS oracle (intel.cjs intelUpdate; drift.cjs codebase-drift) with no relaxed assertions (D-201)"
    - "Registering the intel.* family makes the golden-integration-covered.ts intel.update entry valid and unblocks intel JSON-parity rows (coupling resolved here for Plan 04)"
    - "D-205: VERIFICATION rows for intel.update and verify.codebase-drift are set to their confirmed cause and the D-204 register-vs-exception choice is recorded with rationale"
  artifacts:
    - path: sdk/src/handlers/verify/index.ts
      provides: "verify.codebase-drift registered in VERIFY_FAMILY_HANDLERS"
      contains: "verify.codebase-drift"
    - path: sdk/src/query/command-static-catalog-domain.ts
      provides: "intel.* family registered (intel.update reachable via registry)"
      contains: "intel.update"
  key_links:
    - from: "sdk/src/golden/golden.integration.test.ts intel.update / verify.codebase-drift describe blocks"
      to: "registered native handlers"
      via: "registry.dispatch resolves instead of throwing Unknown command"
      pattern: "intel\\.update|verify\\.codebase-drift"
---

<objective>
Resolve the two hard "no native handler registered" failures by registering the native
handlers per D-204 register-by-default: intel.update (handler already exists at intel.ts:394,
byte-matching the CJS oracle) and verify.codebase-drift (new handler mirroring verifySchemaDrift).
Registering the intel.* family also unblocks the intel JSON-parity rows that dispatch through
the registry (coupling flagged in PATTERNS) — Plan 04 then fixes any intel divergence. Record
the D-204 choice and these two classifications in VERIFICATION.

Purpose: D-204 missing-handler resolution + intel-registration coupling unlock.
Output: registered intel.* + verify.codebase-drift handlers; cleared hard failures; VERIFICATION rows.
</objective>

<context>
!`cd /home/thamw/development/remote-dev/get-shit-done && command grep -n "intel.*NOT imported\|intel.* entries removed\|intentionally omitted\|intentionally NOT" sdk/src/query/command-static-catalog-domain.ts sdk/src/query/command-manifest.non-family.ts sdk/src/handlers/verify/index.ts sdk/src/query/command-manifest.verify.ts`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register verify.codebase-drift native handler (mirror verify.schema-drift)</name>
  <files>sdk/src/query/verify.ts, sdk/src/handlers/verify/index.ts, sdk/src/query/command-manifest.verify.ts</files>
  <read_first>
    - sdk/src/query/verify.ts (verifySchemaDrift — the exact analog handler body to mirror)
    - sdk/src/handlers/verify/index.ts (VERIFY_FAMILY_HANDLERS map; schema-drift entry line 15, codebase-drift omitted comment line 16)
    - sdk/src/query/command-manifest.verify.ts (schema-drift row line 13; codebase-drift omission comment line 14)
    - get-shit-done/bin/lib/drift.cjs (CJS oracle for `verify codebase-drift` — output the SDK handler must toEqual per D-203)
    - sdk/src/golden/golden.integration.test.ts (verify.codebase-drift describe ~line 943 — the assertion that must pass unrelaxed)
  </read_first>
  <behavior>
    - registry.dispatch('verify.codebase-drift', [], REPO_ROOT) resolves (no "Unknown command" throw)
    - SDK output toEqual the gsd-tools.cjs `verify codebase-drift` oracle (D-203), assertions unrelaxed (D-201)
  </behavior>
  <action>
    Add a verifyCodebaseDrift handler to sdk/src/query/verify.ts mirroring verifySchemaDrift in
    role and data flow, producing output that byte-matches the get-shit-done/bin/lib/drift.cjs
    `codebase-drift` branch (D-203 — CJS canonical; fix SDK to match, never relax the assertion
    per D-201). Register it: import + add `'verify.codebase-drift': verifyCodebaseDrift,` to
    VERIFY_FAMILY_HANDLERS in sdk/src/handlers/verify/index.ts (mirroring the schema-drift line 15)
    and remove the "intentionally omitted" comment. Add the matching manifest row in
    command-manifest.verify.ts mirroring the schema-drift row line 13 (and its command-definition
    handler_key if required by the buildAliasGroup chain). Do not relax the golden assertion.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "verify.codebase-drift" 2>&1 | tee /tmp/gsd-55-2-cdrift.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/handlers/verify/index.ts contains `'verify.codebase-drift'` registered to verifyCodebaseDrift
    - sdk/src/query/command-manifest.verify.ts contains a verify.codebase-drift row
    - The verify.codebase-drift golden test passes with no assertion relaxed (summary line `failed` count for the filtered run is 0)
  </acceptance_criteria>
  <done>verify.codebase-drift registered and dispatching; golden test green against the drift.cjs oracle; no assertion relaxed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the intel.* family so intel.update dispatches; record D-204 choice</name>
  <files>sdk/src/query/command-static-catalog-domain.ts, sdk/src/query/command-manifest.non-family.ts, sdk/src/golden/golden-integration-covered.ts, .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</files>
  <read_first>
    - sdk/src/query/intel.ts (intelUpdate already at line 394, byte-matching the oracle; intelStatus at 121 as the registered-handler shape)
    - sdk/src/query/command-static-catalog-domain.ts (intel import-site marker lines 16-20, entry-site marker lines 94-95 — exact insertion points)
    - sdk/src/query/command-manifest.non-family.ts (non-family manifest shape; intel removal comment lines 74-76)
    - get-shit-done/bin/lib/intel.cjs (intelUpdate oracle lines 315-322 — output to match per D-203)
    - sdk/src/golden/golden-integration-covered.ts (intel.update allowlist entry line 21 — becomes valid once registered)
    - sdk/src/golden/golden-policy.ts (coverage gate — registered canonicals must be covered or excepted)
    - sdk/src/golden/golden.integration.test.ts (intel.update describe ~line 882)
  </read_first>
  <behavior>
    - registry.dispatch('intel.update', [], REPO_ROOT) resolves and intel.status/diff/validate/query/extract-exports also dispatch (unblocks Plan 04 parity rows)
    - intel.update SDK output toEqual the intel.cjs intelUpdate oracle (D-203), unrelaxed (D-201)
  </behavior>
  <action>
    Per D-204 register-by-default: register the intel.* family in the SDK seam. Import the existing
    intelUpdate (and the other intel handlers intel.status/diff/validate/query/extract-exports needed
    to unblock Plan 04 parity rows) at the import-site marker in command-static-catalog-domain.ts
    (lines 16-20) and add static-catalog entries at the entry-site marker (lines 94-95) mirroring
    sibling non-family read rows; reverse the phase-55 ADR-3524 omission comments. Add matching rows
    to command-manifest.non-family.ts (lines 74-76 region) following a registered non-family read-row
    pattern. The intelUpdate body already matches intel.cjs:315-322 byte-for-byte, so registration —
    not authoring — clears the hard failure; do not relax the golden assertion (D-201). Confirm the
    existing golden-integration-covered.ts intel.update entry (line 21) is now valid under the
    golden-policy coverage gate (or add coverage/exception rows for any newly-registered intel
    canonical so the policy test stays green). Record in 55.2-VERIFICATION.md: intel.update and
    verify.codebase-drift causes (missing-handler → registered), and the explicit D-204 choice
    (register-by-default taken; exception fallback not used) with rationale.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "intel.update" 2>&1 | tee /tmp/gsd-55-2-intelreg.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/query/command-static-catalog-domain.ts registers intel.update (and the intel parity-row canonicals)
    - registry.dispatch('intel.update', ...) no longer throws "Unknown command"; intel.update golden test passes unrelaxed
    - golden-policy coverage gate stays green (intel.update entry valid or excepted with rationale)
    - 55.2-VERIFICATION.md records the D-204 register-vs-exception decision with rationale and sets the intel.update + verify.codebase-drift rows to missing-handler
  </acceptance_criteria>
  <done>intel.* registered; intel.update + verify.codebase-drift dispatch and pass against CJS oracles; coverage gate green; D-204 choice recorded in VERIFICATION.</done>
</task>

</tasks>

<threat_model>
No new attack surface — test-reconciliation phase; changes are native-handler registration
(intel.* + verify.codebase-drift) and golden-fixture parity within the existing trust boundary.
No network, auth, user input, or data handling introduced.
</threat_model>

<verification>
verify.codebase-drift and intel.update golden tests pass (unrelaxed) against their CJS oracles;
golden-policy coverage gate green; VERIFICATION records D-204 choice + the two missing-handler rows.
</verification>

<success_criteria>
The two hard "no native handler registered" failures cleared by registration (D-204); intel.*
family registered (unblocks Plan 04 intel parity); no assertion relaxed (D-201); oracle parity
held (D-203); VERIFICATION updated (D-205).
</success_criteria>

<output>
Create .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-02-SUMMARY.md when done.
</output>
