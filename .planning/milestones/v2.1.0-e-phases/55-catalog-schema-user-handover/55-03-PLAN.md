---
phase: 55-catalog-schema-user-handover
plan: 03
type: execute
wave: 3
depends_on: [55-02]
files_modified:
  - .planning/phases/55-catalog-schema-user-handover/HANDOVER.md
autonomous: false
requirements: [CATALOG-02]
must_haves:
  truths:
    - "HANDOVER.md lists all 33 agents with their current slot value and heuristic recommendation (heavy→high, standard→medium, light→none/low)"
    - "D-05: HANDOVER.md states the heuristic is advisory not enforced, higher is not monotonically better, and inherit stays effort-free"
    - "HANDOVER.md instructs the user to hand-edit sdk/shared/model-catalog.json agent slots (and the TS mirror is type-ready) — Claude does NOT pre-fill values (CATALOG-02 is user-owned)"
    - "HANDOVER.md gives the exact completeness-check command from Plan 02"
    - "The handover step is a blocking checkpoint:human-verify that pauses execution until the user confirms CATALOG-02 assignment is complete (Pitfall 5)"
    - "After user confirmation, the completeness check (node check-completeness.js) exits 0 — all 33 agents resolve a non-null effort through the Phase 53 resolver (CATALOG-02 success criterion 4)"
  artifacts:
    - path: ".planning/phases/55-catalog-schema-user-handover/HANDOVER.md"
      provides: "User handover instructions: 33-agent table, heuristic, edit target, verification command"
      contains: "model;effort"
  key_links:
    - from: "HANDOVER.md"
      to: "sdk/shared/model-catalog.json agents"
      via: "instructions to assign model;effort per agent slot"
      pattern: "model;effort"
    - from: "HANDOVER.md"
      to: "check-completeness.js"
      via: "documented run command for post-assignment verification"
      pattern: "check-completeness"
---

<objective>
Execute the USER-HANDOVER boundary for CATALOG-02: produce a handover document that gives the user everything needed to hand-assign per-agent `model;effort` values across all 33 agents, then pause for the user to do the assignment, then verify completeness via the Plan 02 script.

Purpose: CATALOG-02 is explicitly user-owned — Claude builds the schema/type (Plan 01) and the verification tooling (Plan 02) but does NOT pre-fill effort values. This plan delivers the handover artifact and the blocking checkpoint that makes the manual assignment an explicit, verified step rather than a silent gap. After the user confirms, the completeness check proves the hand-assigned values flow through the Phase 53 resolver.

Output: `HANDOVER.md` with the 33-agent table and guidance, a blocking human-verify checkpoint, and a passing completeness check.
</objective>

<context>
!`cat .planning/phases/55-catalog-schema-user-handover/55-CONTEXT.md`
@.planning/phases/55-catalog-schema-user-handover/55-RESEARCH.md
@.planning/phases/55-catalog-schema-user-handover/55-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write HANDOVER.md with the 33-agent table and assignment guidance</name>
  <files>.planning/phases/55-catalog-schema-user-handover/HANDOVER.md</files>
  <read_first>
    - .planning/phases/55-catalog-schema-user-handover/55-RESEARCH.md ("All 33 Agents — Enumerated for Completeness Check" table — agent → routingTier → heuristic mapping: 9 heavy, 13 standard, 11 light)
    - sdk/shared/model-catalog.json (current per-agent slot values to populate the "current value" column)
    - .planning/phases/55-catalog-schema-user-handover/55-CONTEXT.md (D-05 guidance heuristic wording)
  </read_first>
  <action>
    Create `.planning/phases/55-catalog-schema-user-handover/HANDOVER.md`. It MUST contain: (1) a one-paragraph framing that CATALOG-02 is a user-owned hand-assignment — Claude widened the type (Plan 01) and built the check (Plan 02) but does not pre-fill values; (2) a table of all 33 agents with columns Agent | routingTier | current slot value (read from sdk/shared/model-catalog.json) | heuristic recommendation — using the routingTier→heuristic mapping from 55-RESEARCH.md (heavy→high for the 9 heavy agents: gsd-planner, gsd-roadmapper, gsd-debugger, gsd-assumptions-analyzer, gsd-debug-session-manager, gsd-eval-planner, gsd-framework-selector, gsd-security-auditor, gsd-user-profiler; standard→medium for the 13 standard; light→none/low for the 11 light); (3) per D-05, an explicit advisory note that the heuristic is advisory not enforced, higher effort is NOT monotonically better (documented overthinking regressions), and the `inherit` profile stays effort-free; (4) edit instructions: assign values by appending `;<effort>` to the chosen profile slot(s) in `sdk/shared/model-catalog.json` (e.g. change `"balanced": "sonnet"` to `"balanced": "sonnet;medium"`) using the `model;effort` syntax — the `;` delimiter, not `:`; the TS mirror in sdk/src/model-catalog.ts is already type-ready (Plan 01) so no TS edit is required; (5) the exact post-assignment verification command `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` run from the project root, with a note that it must print `PASS: all 33 agents have assigned effort values.` Do NOT write any effort values into the catalog yourself.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-catalog-schema-user-handover/HANDOVER.md && command grep -o 'gsd-[a-z-]*' .planning/phases/55-catalog-schema-user-handover/HANDOVER.md | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/55-catalog-schema-user-handover/HANDOVER.md` exists
    - HANDOVER.md mentions all 33 agent names — `command grep -o 'gsd-[a-z-]*' HANDOVER.md | sort -u | wc -l` returns 33
    - HANDOVER.md contains the string `model;effort` and the literal `check-completeness.js`
    - HANDOVER.md contains the advisory wording: `not monotonically better` (or equivalent) and an `inherit` effort-free note (per D-05)
    - sdk/shared/model-catalog.json agent slots remain bare — `node -e "const c=require('./sdk/shared/model-catalog.json'); process.exit(JSON.stringify(c.agents).includes(';')?1:0)"` exits 0 (Claude did not pre-fill)
  </acceptance_criteria>
  <done>HANDOVER.md exists with the full 33-agent table, D-05 advisory guidance, edit instructions, and the verification command; catalog still bare.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: USER HANDOVER — assign per-agent model;effort values (CATALOG-02)</name>
  <files>sdk/shared/model-catalog.json</files>
  <action>
    This is a blocking USER-HANDOVER checkpoint, not a Claude-executed action (Pitfall 5). Claude pauses here and the user hand-assigns the per-agent `model;effort` values in `sdk/shared/model-catalog.json` (CATALOG-02 is user-owned — Claude must NOT pre-fill). Present the `<what-built>`, `<how-to-verify>`, and `<resume-signal>` content below and wait for the user's "approved" signal before advancing to Task 3. Do not write any effort values into the catalog on the user's behalf.
  </action>
  <what-built>
    The catalog type is widened to accept `model;effort` strings (Plan 01), a completeness-check script is ready (Plan 02), and HANDOVER.md lists all 33 agents with a recommended effort per the heavy→high / standard→medium / light→none-or-low heuristic. Claude has NOT assigned any effort values — CATALOG-02 is yours to own.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/55-catalog-schema-user-handover/HANDOVER.md` and review the 33-agent table and the advisory heuristic (per D-05: the heuristic is advisory, higher is not monotonically better, `inherit` stays effort-free).
    2. Edit `sdk/shared/model-catalog.json`: for each agent, append `;<effort>` to the chosen profile slot value(s) using the `model;effort` syntax (semicolon delimiter), e.g. `"balanced": "sonnet"` → `"balanced": "sonnet;medium"`. The TS mirror is already type-ready — no TS edit needed.
    3. Run the completeness check from the project root: `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js`
    4. Confirm it prints `PASS: all 33 agents have assigned effort values.`
  </how-to-verify>
  <verify>
    <human-check>User confirms `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` prints `PASS: all 33 agents have assigned effort values.`</human-check>
  </verify>
  <resume-signal>Type "approved" once the check prints PASS, or describe any agents the check still reports as missing.</resume-signal>
  <done>User has assigned model;effort values across all 33 agents and confirmed the completeness check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Post-handover completeness verification</name>
  <files>.planning/phases/55-catalog-schema-user-handover/HANDOVER.md</files>
  <read_first>
    - .planning/phases/55-catalog-schema-user-handover/check-completeness.js (the script run here)
    - .planning/phases/55-catalog-schema-user-handover/HANDOVER.md (to append the verification result)
  </read_first>
  <action>
    After the user confirms CATALOG-02 assignment (Task 2 checkpoint approved), run the completeness check `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` from the project root and confirm it exits 0 with `PASS: all 33 agents have assigned effort values.` — this satisfies CATALOG-02 success criterion 4 (every agent resolves a non-null effort through the Phase 53 resolver). Also run `node --test tests/feat-53-unified-effort-resolver.test.cjs` to confirm the now-populated catalog does not regress the resolver suite, and `node get-shit-done/bin/gsd-tools.cjs query resolve-model gsd-planner` to confirm the model field is the bare alias (no `;`) while effort is now populated (proving the Plan 01 suffix-strip fix works end-to-end with live values). Append a short `## Completeness Check Result` section to HANDOVER.md recording the PASS line and the resolve-model output for gsd-planner. If the check FAILs, list the missing agents and return to the user (do not silently proceed).
  </action>
  <verify>
    <automated>node .planning/phases/55-catalog-schema-user-handover/check-completeness.js && node get-shit-done/bin/gsd-tools.cjs query resolve-model gsd-planner</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` exits 0 and prints `PASS: all 33 agents have assigned effort values.`
    - `node --test tests/feat-53-unified-effort-resolver.test.cjs` exits 0 (no regression with populated catalog)
    - `node get-shit-done/bin/gsd-tools.cjs query resolve-model gsd-planner` model field contains no `;` and reports a non-null effort
    - HANDOVER.md contains a `## Completeness Check Result` section with the PASS line
  </acceptance_criteria>
  <done>Completeness check passes for all 33 agents; resolver suite green; resolve-model returns bare alias + populated effort; result recorded in HANDOVER.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-edited catalog → resolver | User assigns `model;effort` values; `parseModelEffort` validates effort tokens at resolution time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Tampering | user-assigned catalog effort values | accept | Values are project-owned config edited by the user during handover; `parseModelEffort` rejects/warns on malformed effort tokens (CONFIG-04, Phase 53). The completeness check confirms all 33 resolve. No elevated-privilege operation, no network, no external input. |

This plan produces a documentation artifact and runs read-only verification. The only state mutation (catalog effort values) is performed by the user, validated by the existing parser. Security risk: low.
</threat_model>

<verification>
- HANDOVER.md lists all 33 agents with heuristic guidance and the verification command (CATALOG-02 handover)
- Blocking checkpoint pauses for user assignment (Pitfall 5)
- Post-handover: completeness check exits 0 for all 33 agents (CATALOG-02 success criterion 4)
- resolve-model gsd-planner returns bare alias + non-null effort (Plan 01 fix validated with live values)
</verification>

<success_criteria>
- HANDOVER.md complete with 33-agent table, D-05 advisory heuristic, edit instructions, verification command
- Catalog left bare by Claude (no pre-filled effort)
- User assignment captured via blocking checkpoint
- All 33 agents resolve non-null effort post-handover; resolver suite green
</success_criteria>

<output>
Create `.planning/phases/55-catalog-schema-user-handover/55-03-SUMMARY.md` when done
</output>
