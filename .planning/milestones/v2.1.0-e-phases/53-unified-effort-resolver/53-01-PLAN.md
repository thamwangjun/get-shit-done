---
phase: 53-unified-effort-resolver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/model-catalog.cjs
  - get-shit-done/bin/lib/core.cjs
  - tests/issue-2517-runtime-aware-profiles.test.cjs
  - tests/feat-3023-model-phase-types.test.cjs
  - tests/feat-53-unified-effort-resolver.test.cjs
autonomous: true
requirements: [RESOLVE-01, RESOLVE-02, RESOLVE-03, RESOLVE-04, RESOLVE-05, RESOLVE-06, CONFIG-01, CONFIG-04]
must_haves:
  truths:
    - "With an effort suffix in a profile/phase-type slot, resolveReasoningEffortInternal emits that effort for the claude runtime (RESOLVE-01)"
    - "Effort precedence mirrors resolveModelInternal: per-agent override (parseModelEffort.effort) -> _resolveAgentSlot slot effort -> Codex per-tier reasoning_effort fallback -> null (RESOLVE-02)"
    - "Profile/phase-type slot effort overrides Codex per-tier reasoning_effort; per-tier value is fallback-only when the slot carries no ;effort (RESOLVE-03)"
    - "A model_overrides.<agent> value carrying ;effort emits that effort inside the {claude,codex} gate; a bare override (no ;) omits effort (CONFIG-01, D-01)"
    - "Runtimes outside {claude,codex} hard no-op even with an override ;effort suffix (RESOLVE-05, D-02)"
    - "inherit profile, resolved inherit tier, and bare adaptive entries omit effort (RESOLVE-06)"
    - "Malformed effort token in a slot/override degrades to effort:null with a one-time warning via parseModelEffort, no separate reject pass (CONFIG-04, D-05)"
    - "max emits verbatim on the claude path (no Claude clamp); the resolver returns canonical effort and never clamps max->xhigh (D-03)"
    - "On the codex runtime a slot carrying ;max returns max VERBATIM from resolveReasoningEffortInternal — the resolver never clamps; max->xhigh is the downstream emit boundary's job (RESOLVE-04, D-03)"
    - "On a bare catalog/config every slot resolves to effort:null (back-compat invariant holds)"
  artifacts:
    - path: get-shit-done/bin/lib/model-catalog.cjs
      provides: "Static RUNTIMES_WITH_REASONING_EFFORT = new Set(['claude','codex'])"
      contains: "new Set(['claude', 'codex'])"
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Rewritten resolveReasoningEffortInternal single precedence chain on _resolveAgentSlot"
    - path: tests/feat-53-unified-effort-resolver.test.cjs
      provides: "New-behavior tests: claude emits slot/override effort, codex slot-over-per-tier, allowlist gate, malformed degrade, max verbatim (claude + codex)"
  key_links:
    - from: get-shit-done/bin/lib/core.cjs::resolveReasoningEffortInternal
      to: _resolveAgentSlot
      via: "const tier = _resolveAgentSlot(cwd, agentType)"
      pattern: "_resolveAgentSlot\\(cwd, agentType\\)"
    - from: get-shit-done/bin/lib/core.cjs::resolveReasoningEffortInternal
      to: parseModelEffort
      via: "override path and slot path both take .effort"
      pattern: "parseModelEffort\\("
    - from: get-shit-done/bin/lib/core.cjs::resolveReasoningEffortInternal
      to: RUNTIMES_WITH_REASONING_EFFORT
      via: "outermost allowlist gate"
      pattern: "RUNTIMES_WITH_REASONING_EFFORT\\.has"
---

<objective>
Rewrite `resolveReasoningEffortInternal` (core.cjs) into a unified effort resolver for `{claude, codex}` that mirrors `resolveModelInternal`'s precedence chain on the shared `_resolveAgentSlot`, and replace the data-derived `RUNTIMES_WITH_REASONING_EFFORT` (model-catalog.cjs) with a static `{claude, codex}` allowlist.

Purpose: Lift the Claude effort gate (per D-07/RESOLVE-01), structurally align model+effort on the same slot (#3023 guard, D-08 same-slot derivation), accept `model;effort` in `model_overrides.<agent>` (CONFIG-01/D-01), and keep the back-compat invariant — bare configs still resolve to `effort: null`.
Output: Rewritten resolver + static allowlist + new behavior tests; updated comments on two existing tests whose intent shifts from "Claude never emits" to "bare-config Claude still omits".
</objective>

<context>
!`cat .planning/phases/53-unified-effort-resolver/53-CONTEXT.md`
!`cat .planning/phases/53-unified-effort-resolver/53-PATTERNS.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace data-derived allowlist with static {claude, codex} Set</name>
  <files>get-shit-done/bin/lib/model-catalog.cjs, tests/issue-2517-runtime-aware-profiles.test.cjs, tests/feat-3023-model-phase-types.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/model-catalog.cjs (lines 75-136 — the data-derived `RUNTIMES_WITH_REASONING_EFFORT` at 87-91 and the `module.exports` block at 121-136)
    - tests/issue-2517-runtime-aware-profiles.test.cjs (lines 100-200 — the "reasoning_effort is null on Claude (never leaks)" test at 147-150 and the codex tier tests)
    - tests/feat-3023-model-phase-types.test.cjs (lines 256-365 — the resolveReasoningEffortInternal suite, especially the claude test at 345-352)
  </read_first>
  <action>
    In `model-catalog.cjs`, replace the data-derived `RUNTIMES_WITH_REASONING_EFFORT` expression (lines 87-91, the `new Set(Object.entries(catalog.runtimeTierDefaults).filter(...).map(...))`) with the static literal `new Set(['claude', 'codex'])` per D-07/RESOLVE-01. Keep the `const RUNTIMES_WITH_REASONING_EFFORT =` name and its presence in the `module.exports` block unchanged (it stays a `Set<string>`; call sites use `.has(config.runtime)`).
    Do NOT touch `RUNTIME_PROFILE_MAP` or `KNOWN_RUNTIMES`. Per D-07/RESOLVE-01 the data-derived scan is the exact anti-pattern to remove — the static literal is the capability allowlist that does not auto-admit future runtimes that gain an effort suffix.
    In the two existing test files, locate any assertion that depends on the OLD codex-only membership of `RUNTIMES_WITH_REASONING_EFFORT`. Per the PATTERNS map these live in `issue-2517-runtime-aware-profiles.test.cjs` and `feat-3023-model-phase-types.test.cjs`. Note: the bare-config "claude returns null" assertions (issue-2517:147-150 and feat-3023:345-352) stay GREEN unchanged because the bare `claude` catalog tiers carry no `reasoning_effort` and no `;effort` suffix — update only their comment intent to "bare-config Claude still omits effort (the allowlist now admits claude, but the slot carries no effort)" so the renamed expectation is clear. Do not weaken or delete these tests.
  </action>
  <verify>
    <automated>node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-3023-model-phase-types.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -n "new Set(\['claude', 'codex'\])" get-shit-done/bin/lib/model-catalog.cjs` returns the `RUNTIMES_WITH_REASONING_EFFORT` line
    - `command grep -c "filter(\[, tiers\]) => Object.values(tiers).some" get-shit-done/bin/lib/model-catalog.cjs` returns 0 (data-derived form gone)
    - `RUNTIMES_WITH_REASONING_EFFORT` still appears in the `module.exports` block of model-catalog.cjs
    - `node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-3023-model-phase-types.test.cjs` exits 0
  </acceptance_criteria>
  <done>The allowlist is a static `{claude, codex}` Set still exported; both existing suites pass with comment intent updated, no test weakened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite resolveReasoningEffortInternal as a unified precedence chain</name>
  <files>get-shit-done/bin/lib/core.cjs, tests/feat-53-unified-effort-resolver.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs (lines 1242-1265 `parseModelEffort`; 1295-1361 `resolveTierEntry`/`_resolveRuntimeTier`/`_resolveAgentSlot`; 1363-1433 `resolveModelInternal` — the exact analog; 1523-1583 the current `resolveReasoningEffortInternal` to rewrite)
    - get-shit-done/bin/lib/model-catalog.cjs (the static allowlist from Task 1; `AGENT_TO_PHASE_TYPE`, `MODEL_PROFILES`)
    - .planning/phases/53-unified-effort-resolver/53-PATTERNS.md (the per-step mirror excerpts for steps 1-3 + the allowlist gate)
    - tests/feat-3023-model-phase-types.test.cjs (lines 256-365 — existing effort suite the new tests sit beside)
  </read_first>
  <behavior>
    - claude + model_profile=quality + a profile/phase-type slot carrying `;high` (simulate via models.<phase-type> = "opus;high" routed through _resolveAgentSlot) → effort "high"
    - claude + bare config (no ;effort anywhere) → effort null (back-compat invariant)
    - claude + model_overrides[agent] = "opus;max" → effort "max" emitted verbatim (no clamp, D-03)
    - claude + model_overrides[agent] = "openai/gpt-5.4" (bare, no ;) → effort null (D-01)
    - codex + model_profile=quality, slot carries no ;effort → per-tier fallback "xhigh" (gpt-5.4 opus row) (RESOLVE-03 fallback)
    - codex + models.<phase-type> = "opus;low" → slot effort "low" WINS over per-tier xhigh (RESOLVE-03 slot-over-per-tier)
    - codex + models.<phase-type> = "opus;max" → resolveReasoningEffortInternal returns "max" VERBATIM (proves the resolver does NOT clamp max→xhigh; the Codex max→xhigh clamp is the downstream emit boundary's job, not the resolver's) (RESOLVE-04, D-03)
    - opencode (or any non-{claude,codex}) + model_overrides[agent] = "opus;high" → effort null (RESOLVE-05/D-02 absolute gate)
    - no runtime set → defaults to claude path; bare config → null
    - resolved tier === 'inherit' (model_profile=inherit, no phase-type override) → null (RESOLVE-06)
    - phase-type tier === 'inherit' → null (RESOLVE-06)
    - malformed slot "opus;hihg" → effort null with a one-time stderr warning (via parseModelEffort) (CONFIG-04/D-05)
  </behavior>
  <action>
    First write `tests/feat-53-unified-effort-resolver.test.cjs` covering every case in `<behavior>` using `node --test` (`describe`/`test`) and the project's `tests/helpers.cjs` (`createTempProject`/`writeConfig`/`cleanup` or the equivalents used in feat-3023). Assert exact effort strings with `assert.strictEqual` (NEVER `indexOf`/substring — Pitfall 6: `medium`/`high` collide with Codex defaults). For the malformed-warning case, assert `effort === null` and capture stderr to confirm a one-time warning fires. For the codex `;max` resolver-contract case, set `config.runtime = 'codex'` and `models.<phase-type> = "opus;max"`, then assert `resolveReasoningEffortInternal(...) === "max"` — this positively pins RESOLVE-04's resolver-side contract by proving the ABSENCE of a clamp (per D-03 clamping lives at the downstream Phase 54 Codex emit boundary, NEVER in the resolver). Run the new test and confirm RED before implementing.
    Then rewrite `resolveReasoningEffortInternal` (core.cjs ~1533-1583) as a single precedence chain mirroring `resolveModelInternal` (1363-1433):
    1. Load config. Apply the outermost allowlist gate FIRST: `if (!config.runtime || !RUNTIMES_WITH_REASONING_EFFORT.has(config.runtime)) return null;` — per D-02 this is absolute and runs BEFORE the override emit so a non-{claude,codex} install with `model_overrides.x = "model;high"` still returns null.
    2. Per-agent override (highest precedence within the gate): `const override = config.model_overrides?.[agentType]; if (override) return parseModelEffort(override).effort;` — this REMOVES the old early-`null` at the former core.cjs:1545 (per D-01). A bare override yields `.effort === null` naturally.
    3. Shared slot: `const tier = _resolveAgentSlot(cwd, agentType);` then apply the resolver's own opt-out AFTER the call (since `_resolveAgentSlot`'s VALID_TIERS includes 'inherit'): `if (!tier || tier === 'inherit') return null;` (RESOLVE-06). Delete the duplicated inline phase-type lookup block (former 1563-1577 that re-derived `phaseType`/`phaseTypeTier`/`VALID_TIERS`) — per D-08.
    4. Slot effort (same-slot invariant): run `parseModelEffort(tier).effort`; if non-null, return it (slot effort wins, RESOLVE-03). Per D-03 return canonical effort verbatim (`low|medium|high|xhigh|max`) — do NOT clamp `max`→`xhigh` here on the codex path either; the resolver's contract is to surface the canonical slot effort, and the codex `;max`→`max` verbatim return proves it (RESOLVE-04).
    5. Codex per-tier fallback ONLY when the slot carried no effort: `const entry = _resolveRuntimeTier(config, parseModelEffort(tier).model); return entry?.reasoning_effort || null;` (RESOLVE-03 fallback / D-06 step 3). Pass the parsed `.model` (bare tier alias) to `_resolveRuntimeTier`, not the raw `;effort` slot.
    Per D-03 do NOT clamp `max`→`xhigh` anywhere in this resolver — return canonical effort verbatim (`low|medium|high|xhigh|max`); the Codex `max`→`xhigh` clamp is the emit boundary's job in a downstream phase. Keep the function exported exactly as before.
  </action>
  <verify>
    <automated>node --test tests/feat-53-unified-effort-resolver.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c "_resolveAgentSlot(cwd, agentType)" get-shit-done/bin/lib/core.cjs` returns 2 (resolveModelInternal + the rewritten effort resolver both consume it)
    - `command grep -n "config.model_overrides?.\[agentType\]) return null" get-shit-done/bin/lib/core.cjs` returns nothing (old early-null at former 1545 removed)
    - The rewritten `resolveReasoningEffortInternal` body contains no inline `const phaseType = AGENT_TO_PHASE_TYPE[agentType]` re-derivation (D-08; that lookup now lives only inside `_resolveAgentSlot`)
    - `tests/feat-53-unified-effort-resolver.test.cjs` includes a codex `;max` case asserting `resolveReasoningEffortInternal(...) === "max"` verbatim (resolver does NOT clamp; pins RESOLVE-04 resolver-side contract per D-03)
    - `node --test tests/feat-53-unified-effort-resolver.test.cjs` exits 0 with all `<behavior>` cases asserted via `assert.strictEqual`
    - `node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-3023-model-phase-types.test.cjs` still exits 0 (no regression)
  </acceptance_criteria>
  <done>resolveReasoningEffortInternal is a single precedence chain on _resolveAgentSlot honoring the {claude,codex} gate as outermost, override `.effort`, slot effort over Codex per-tier fallback, inherit/unknown → null, max verbatim on both claude and codex paths (no resolver clamp); new behavior suite green; existing suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → resolver | User-authored `.planning/config.json` slot/override strings cross into `resolveReasoningEffortInternal`. These are local, developer-owned config, not network/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | malformed effort token in config slot/override | mitigate | All slot/override strings route through `parseModelEffort`, which strips to base model + `effort: null` + one-time stderr warning (D-05/CONFIG-04); resolver never throws on bad input |
| T-53-02 | Elevation of Privilege | effort leak to unsupported runtime via override ;effort | mitigate | Static `{claude, codex}` allowlist is the outermost gate (D-02/D-07); runs before any override emit so the 8 null-tier runtimes hard no-op |
| T-53-03 | Tampering | `;` in a model;effort token reaching a raw shell | accept | The combined string is parsed into separate fields inside `parseModelEffort` and never emitted to a shell on this resolver path; no spawn/install path is touched in this plan |

No untrusted-input or network surface exists on this path; this is internal config resolution.
</threat_model>

<verification>
- `node --test tests/feat-53-unified-effort-resolver.test.cjs` exits 0
- `node --test tests/issue-2517-runtime-aware-profiles.test.cjs tests/feat-3023-model-phase-types.test.cjs` exits 0 (no regression on existing resolver behavior)
- `command grep -n "new Set(\['claude', 'codex'\])" get-shit-done/bin/lib/model-catalog.cjs` confirms the static allowlist
</verification>

<success_criteria>
- RUNTIMES_WITH_REASONING_EFFORT is a static `{claude, codex}` Set (RESOLVE-01, D-07)
- resolveReasoningEffortInternal follows the unified precedence chain on _resolveAgentSlot (RESOLVE-02, D-06, D-08)
- Slot effort overrides the Codex per-tier reasoning_effort fallback (RESOLVE-03)
- max emits verbatim on the claude path, no Claude clamp; on the codex path a slot `;max` returns `max` verbatim from the resolver, proving the resolver never clamps (RESOLVE-04, D-03)
- Non-{claude,codex} runtimes hard no-op even with an override ;effort (RESOLVE-05, D-02)
- inherit profile / resolved inherit tier / bare adaptive → null (RESOLVE-06)
- model_overrides ;effort accepted; bare override omits (CONFIG-01, D-01)
- malformed effort token warns + degrades via parseModelEffort, no separate reject pass (CONFIG-04, D-05)
- Bare-config back-compat invariant holds (effort: null everywhere)
</success_criteria>

<output>
Create `.planning/phases/53-unified-effort-resolver/53-01-SUMMARY.md` when done
</output>
</tasks>
