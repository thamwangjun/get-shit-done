---
phase: 55.1-update-old-tests-found-failing-due-to-phase-55-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - tests/parse-model-effort.test.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-103/D-101: cluster #1 (codex runtime model) is root-caused against SOURCE before any test edit"
    - "resolveModelInternal(runtime:'codex', 'gsd-executor') returns 'gpt-5.3-codex', not 'sonnet'"
    - "The codex model regression is fixed in core.cjs (the model resolver parses the bare tier before runtime lookup), mirroring resolveReasoningEffortInternal"
    - "D-104: no golden snapshot is blindly accepted — the codex fixture is fixed at SOURCE, not by realigning the expected value to the buggy output"
    - "tests/parse-model-effort.test.cjs passes with 0 failures (node --test exits 0)"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "resolveModelInternal runtime block strips ;effort from slot before _resolveRuntimeTier"
    - path: tests/parse-model-effort.test.cjs
      provides: "stale _resolveAgentSlot literal updated to hand-assigned slot output"
  key_links:
    - from: "resolveModelInternal step 3 (core.cjs ~1441)"
      to: "_resolveRuntimeTier"
      via: "parseModelEffort(tier).model passed instead of raw tier"
      pattern: "_resolveRuntimeTier\\(config, parseModelEffort"
---

<objective>
Root-cause and fix SUSPECT CLUSTER #1 (CONTEXT D-103.1): a bare tier alias
('sonnet') leaks out of resolveModelInternal for the codex runtime where
'gpt-5.3-codex' is expected. Then update the one genuinely-stale `_resolveAgentSlot`
literal in the same test file. This plan runs FIRST (Wave 1) because the verdict
(real regression vs stale literal) reshapes how the bulk-update plans treat any
other runtime/codex assertion.

Purpose: Per D-101 each cluster gets a per-cluster verdict; per D-104 golden output
is never blindly accepted. The codex model leak is a REGRESSION introduced by the
;effort slot parsing — fix the SOURCE, keep the test's intent.

Output: core.cjs runtime-block fix + parse-model-effort.test.cjs green.
</objective>

<context>
!`command grep -n "non-claude runtime returns runtime model\|_resolveAgentSlot returns a single slot string" tests/parse-model-effort.test.cjs`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause and FIX the codex model leak at SOURCE (cluster #1)</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs lines 1405–1485 (resolveModelInternal — the step-3 runtime block at ~1441)
    - get-shit-done/bin/lib/core.cjs lines 1598–1637 (resolveReasoningEffortInternal — the CORRECT pattern: it parses parseModelEffort(tier).model at ~1634 before _resolveRuntimeTier)
    - get-shit-done/bin/lib/core.cjs lines 1330–1336 (_resolveRuntimeTier — looks up runtimeTierDefaults[runtime][tier], returns undefined for an unrecognized key like 'sonnet;medium')
    - sdk/shared/model-catalog.json lines 16–20 (codex tier defaults — keyed by bare alias 'sonnet', not 'sonnet;medium')
  </read_first>
  <action>
    Root cause (per D-101 verdict: REGRESSION, not stale literal): in resolveModelInternal
    step 3 the runtime block passes the raw slot `tier` (now `'sonnet;medium'` after the
    hand-assigned catalog) straight to `_resolveRuntimeTier`. That function looks up
    `runtimeTierDefaults.codex['sonnet;medium']` which does not exist (keys are bare aliases),
    returns undefined, and resolution falls through to the Claude alias `'sonnet'` — the leak.
    The effort resolver already avoids this by parsing the bare alias first (per D-08 same-slot
    handling at ~1634).
    Fix: at the runtime block (~1441), pass `parseModelEffort(tier).model` to `_resolveRuntimeTier`
    instead of the raw `tier`, so the bare alias 'sonnet' is looked up. Keep the existing
    `tier !== 'inherit'` guard semantics. Add a brief inline comment citing this as the
    same-slot ;effort fix paralleling resolveReasoningEffortInternal. Do NOT change the
    effort resolver. (per D-104 — fixing SOURCE, never realigning the expected value to the
    buggy 'sonnet' output.)
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort.test.cjs 2>&1 | command grep -E "non-claude runtime returns runtime model" | command grep -qv "✖" &amp;&amp; echo CODEXFIXED</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const c=require('./get-shit-done/bin/lib/core.cjs'); const os=require('os'),fs=require('fs'),p=require('path'); const d=fs.mkdtempSync(p.join(os.tmpdir(),'t')); fs.mkdirSync(p.join(d,'.planning')); fs.writeFileSync(p.join(d,'.planning/config.json'),JSON.stringify({runtime:'codex'})); console.log(c.resolveModelInternal(d,'gsd-executor'))"` prints `gpt-5.3-codex`
    - core.cjs runtime block calls `_resolveRuntimeTier(config, parseModelEffort(tier).model)` (or equivalent bare-alias parse) — verified by `command grep -n "parseModelEffort(tier).model" get-shit-done/bin/lib/core.cjs` returning the runtime-block line in addition to the effort-resolver line
  </acceptance_criteria>
  <done>resolveModelInternal returns 'gpt-5.3-codex' for codex+gsd-executor; effort resolver untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update the stale _resolveAgentSlot literal (cluster #1 stale half)</name>
  <files>tests/parse-model-effort.test.cjs</files>
  <read_first>
    - tests/parse-model-effort.test.cjs lines 119–160 (_resolveAgentSlot assertions)
    - sdk/shared/model-catalog.json lines 87–122 (gsd-executor balanced = "sonnet;medium")
  </read_first>
  <action>
    Per D-102 the hand-assigned slot `gsd-executor` balanced = `"sonnet;medium"` is INTENDED.
    The assertion at ~line 124 expecting `_resolveAgentSlot(d,'gsd-executor') === 'sonnet'`
    is a STALE literal — `_resolveAgentSlot` deliberately returns the raw slot string
    WITH the ;effort suffix (so the effort resolver can read it). Update the expected value
    to `'sonnet;medium'`. Read the live catalog value rather than guessing. Do NOT weaken
    the assertion to a substring/indexOf check — keep strict equality (per D-105 anti-false-pass).
    The codex assertion at ~226 is fixed by Task 1's SOURCE change; leave its expected value
    `'gpt-5.3-codex'` as-is.
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/parse-model-effort.test.cjs` exits 0 (0 failures)
    - the updated assertion uses `assert.strictEqual(..., 'sonnet;medium')` — confirmed by `command grep -n "sonnet;medium" tests/parse-model-effort.test.cjs`
  </acceptance_criteria>
  <done>tests/parse-model-effort.test.cjs passes with 0 failures.</done>
</task>

</tasks>

<verification>
node --test tests/parse-model-effort.test.cjs exits 0.
node --test tests/feat-53-config-sites-and-golden.test.cjs shows the #3023 codex
fixture (lines ~293–322) no longer failing on the model assertion (Plan 02 closes the rest).
</verification>

<success_criteria>
Cluster #1 root-caused with an explicit REGRESSION verdict; codex model leak fixed at
SOURCE in core.cjs; stale _resolveAgentSlot literal updated; parse-model-effort.test.cjs green.
</success_criteria>

<output>
Create .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-01-SUMMARY.md when done.
</output>
