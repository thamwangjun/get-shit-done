---
status: passed
phase: 55.1-update-old-tests-found-failing-due-to-phase-55-work
scope: ROOT suite (SDK greenness delegated to phase 55.2)
verified: 2026-06-03
plans_verified: ["55.1-01", "55.1-02", "55.1-03", "55.1-04"]
---

# Phase 55.1 Verification — Whole-Suite Gate (D-105)

## ROOT suite — PASS

`npm test 2>&1 | tee /tmp/gsd-55-1-final.txt` (run once, read from file per CLAUDE.md):

```
tests 8605
suites 1328
pass 8594
fail 0
skipped 11
todo 0
duration_ms 76494
```

- **fail 0** — D-105 ROOT end-state achieved.
- **skipped 11** — pre-existing baseline. Verified `git log -p --grep="55.1-0[123]" -- 'tests/**' 'sdk/**'` introduced **no** new `.skip(`/`.todo(`. The "no masking" invariant (D-105) holds — Plans 01–03 fixed sources/expectations, they did not hide failures behind skips.

## Cluster verdicts (Plans 01–02)

- **Cluster #1 — SOURCE regression:** Phase-55 catalog/effort work leaked stale model literals and a tier/effort precedence bug into `get-shit-done/bin/lib/core.cjs`. Fixed at source (`63561e14` strip `;effort` before `_resolveRuntimeTier` in `resolveModelInternal`; plus the codex model-leak fix). Tests were updated to the corrected literals only after the source was right.
- **Cluster #2 — stale-premise expectations:** Tests asserted pre-D-08 effort values. Realigned to the hand-assigned catalog slot effort, preserving the D-08 same-slot invariant. Per the resolved wave-2 merge decision: **catalog hand-assigned slot effort WINS over the codex runtime built-in tier effort**; a bare `model_profile_overrides` (no `;effort`) changes only the model, slot effort persists (`resolveReasoningEffortInternal` orders catalog same-slot effort before `_resolveRuntimeTier`). Two contradicting assertions in `tests/feat-53-config-sites-and-golden.test.cjs` moved `'xhigh'` → `'low'`. All 9 model/effort test files green (599/599) at merge.

## SDK suite — OUT OF SCOPE (delegated to phase 55.2)

`cd sdk && npm test` reports **17 failures** — all golden native-handler parity checks:
- Missing native handlers: `intel.update`, `verify.codebase-drift` ("Unknown command … No native handler registered").
- JSON-parity mismatches vs `gsd-tools.cjs`: `intel.status/diff/validate/query/extract-exports`, `history.digest`, `phase-plan-index`, `init.quick/todos/manager/map-codebase`, `audit-open`, `validate.health`.
- Meta/policy: `executeForCjs > native_failure classification`, `golden policy > every canonical registry command is integration-covered or excepted`.

**These are not in 55.1's scope.** Evidence:
1. Phase 55.1's entire SDK footprint is one test file, `sdk/src/query/config-query.test.ts` (passes **31/31**). Reverting 55.1 cannot affect the failing golden handler tests.
2. **Zero** of the 17 failures reference model/effort/profile — they are SDK↔CLI registry parity gaps inherited from phase 55 (catalog-schema/effort SDK work).

Rescoped per user decision: SDK greenness is owned by **phase 55.2 — Fix SDK golden-parity suite failures (regression triage from phase 55)**. Captured failure log: `/tmp/gsd-55-1-sdk.txt`.

## Verdict

Phase 55.1 goal (reconcile ROOT tests broken by phase-55 model/effort work, without masking) is **ACHIEVED**. ROOT suite green, no new skips, sources fixed at root, D-08 invariant preserved. SDK parity failures are a separate, pre-existing concern tracked in 55.2.
