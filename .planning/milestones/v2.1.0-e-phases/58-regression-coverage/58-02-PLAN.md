---
phase: 58-regression-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/parse-model-effort.json
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "The parser fixture covers multi-colon provider IDs (e.g. bedrock:us.anthropic...:0) → effort null"
    - "The parser fixture covers a colon provider ID WITH a valid effort suffix (openrouter:anthropic/claude-opus;high) → model stripped of suffix, effort 'high'"
    - "The existing parity harness (parse-model-effort-parity.test.cjs) exercises the extended fixture automatically — single source, no new fixture file"
    - "All pre-existing fixture cases remain unchanged (additive only)"
  artifacts:
    - path: "tests/fixtures/parse-model-effort.json"
      provides: "Extended parser fixture: existing 12 cases + 2 colon-provider-ID gap cases for TEST-02"
      contains: "bedrock:us.anthropic"
  key_links:
    - from: "tests/parse-model-effort-parity.test.cjs"
      to: "tests/fixtures/parse-model-effort.json"
      via: "readFileSync of the fixture, iterated through parseModelEffort"
      pattern: "parse-model-effort.json"
---

<objective>
Close the TEST-02 parser-fixture gaps by extending the existing committed fixture with the two colon-containing provider-ID edge cases the current fixture omits. This is additive — the existing parity test already reads this file, so coverage extends to both the CJS and SDK parity harnesses without a new file.

Purpose: Prove `parseModelEffort` never treats a colon inside a provider ID as an effort delimiter (it splits on `lastIndexOf(';')`, never `:`), including the multi-colon and colon-plus-valid-suffix cases.
Output: Two new rows appended to `tests/fixtures/parse-model-effort.json`.
</objective>

<context>
@.planning/phases/58-regression-coverage/58-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/fixtures/parse-model-effort.json with colon-provider-ID gap cases</name>
  <files>tests/fixtures/parse-model-effort.json</files>
  <read_first>
    - tests/fixtures/parse-model-effort.json (the 12 existing cases — confirm shape {input, expectedModel, expectedEffort}; the new rows must match exactly and be additive)
    - tests/parse-model-effort-parity.test.cjs (confirm it readFileSync's this exact fixture and iterates each row through parseModelEffort — so appending rows automatically expands coverage)
    - tests/parse-model-effort.test.cjs (the unit suite — confirm parseModelEffort splits on lastIndexOf(';') and strips only exact-member effort tokens {low,medium,high,xhigh,max}; this is what the new rows assert against)
    - .planning/phases/58-regression-coverage/58-PATTERNS.md (§tests/fixtures/parse-model-effort.json — the two exact cases to ADD)
  </read_first>
  <action>
    Append exactly two rows to the JSON array in `tests/fixtures/parse-model-effort.json`, preserving all existing rows verbatim and keeping the file valid JSON (array form):
    1. Multi-colon provider ID with NO semicolon: input `bedrock:us.anthropic.claude-3-5-sonnet-20241022-v2:0` → `expectedModel` identical to input, `expectedEffort: null`. Proves colons are never effort delimiters.
    2. Colon provider ID WITH a valid `;effort` suffix: input `openrouter:anthropic/claude-opus;high` → `expectedModel: openrouter:anthropic/claude-opus`, `expectedEffort: "high"`. Proves the suffix is split on the LAST `;` while colons in the model are preserved.

    Do not modify, reorder, or remove any existing fixture row. Do not create a separate fixture file — the parity harness reads this single source.
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort.test.cjs tests/parse-model-effort-parity.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/parse-model-effort.json` parses as valid JSON and contains 14 rows (12 existing + 2 new)
    - `grep -c "bedrock:us.anthropic" tests/fixtures/parse-model-effort.json` returns 1
    - A row exists with input `openrouter:anthropic/claude-opus;high`, expectedModel `openrouter:anthropic/claude-opus`, expectedEffort `high`
    - `node --test tests/parse-model-effort.test.cjs tests/parse-model-effort-parity.test.cjs` exits 0 (both new rows pass through both CJS and parity harnesses)
  </acceptance_criteria>
  <done>The fixture gains the two colon-provider-ID cases; the existing parity harness validates them; all prior cases untouched and still green.</done>
</task>

</tasks>

<threat_model>
No threats — test-only phase: this plan appends two rows to a committed JSON test fixture. No runtime behavior, no input surfaces, no secrets. No source-code edits.
</threat_model>

<verification>
- `node --test tests/parse-model-effort.test.cjs tests/parse-model-effort-parity.test.cjs` exits 0.
- Fixture is valid JSON with 14 rows; the two new colon cases present with correct expected values.
</verification>

<success_criteria>
- Parser fixture covers multi-colon provider IDs and colon-provider-plus-valid-suffix.
- Existing parity harness exercises the new cases; no new fixture file; no regressions.
</success_criteria>

<output>
Create `.planning/phases/58-regression-coverage/58-02-SUMMARY.md` when done.
</output>
