---
phase: 56-spawn-template-wiring
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - get-shit-done/workflows/audit-milestone.md
  - get-shit-done/workflows/debug.md
  - get-shit-done/workflows/docs-update.md
  - get-shit-done/workflows/scan.md
  - get-shit-done/workflows/secure-phase.md
  - get-shit-done/workflows/ui-phase.md
  - get-shit-done/workflows/ui-review.md
  - get-shit-done/workflows/validate-phase.md
  - get-shit-done/workflows/discuss-phase/modes/advisor.md
  - agents/gsd-debug-session-manager.md
autonomous: true
requirements: [SPAWN-01, SPAWN-02, SPAWN-03]
must_haves:
  truths:
    - "Every Group B spawn site gains a resolve-model-effort gsd-<agent> --raw capture line adjacent to its existing resolve-model line"
    - "Every Group B Agent() block carries the pre-built {<agent>_model_effort_arg} token adjacent to its model= line"
    - "Legacy jq-form sites (debug.md, gsd-debug-session-manager.md) use the --raw form for the new effort line regardless of the model line form"
    - "docs-update.md is wired via a standalone resolve-model-effort call (Option A) — docs.cjs is NOT modified"
    - "Fork gates unchanged: agent-frontmatter 155/155 (covers the agent file edit), negative-framing 99/99, step-numbering 632/632, cross-file-refs 219/219, eta-include pass"
  artifacts:
    - path: "get-shit-done/workflows/audit-milestone.md"
      provides: "integration_checker effort capture + Agent() token"
      contains: "resolve-model-effort"
    - path: "agents/gsd-debug-session-manager.md"
      provides: "debugger effort capture (--raw) + Agent() token"
      contains: "resolve-model-effort"
    - path: "get-shit-done/workflows/discuss-phase/modes/advisor.md"
      provides: "advisor effort capture + Agent() token (site missed by discuss-time grep)"
      contains: "resolve-model-effort"
  key_links:
    - from: "Group B Agent() blocks"
      to: "resolve-model-effort SDK query (Plan 01)"
      via: "adjacent --raw capture line + interpolated {<agent>_model_effort_arg}"
      pattern: "resolve-model-effort gsd-"
---

<objective>
Wire resolved effort into the 9 standalone-resolve (Group B) workflow files plus the 1 agent-side spawn site. These use explicit `resolve-model` shell calls (not init). For each: add a `resolve-model-effort gsd-<agent> --raw` capture line adjacent to the existing resolve-model line, and add the `{<agent>_model_effort_arg}` token adjacent to each `model=` line in the Agent() block.

Purpose: SPAWN-01/02 — resolved effort reaches every Group B spawned agent via the Plan 01 query and the carrier confirmed in Plan 01 (D-02). SPAWN-03 — adding one resolve line + one Agent() arg never renumbers steps. Includes advisor.md (the site missed by discuss-time grep) and the gsd-debug-session-manager.md agent orchestrator.
Output: 10 spawn-site files carrying the per-agent effort token; full fork gate suite green (incl. agent-frontmatter for the agent file edit).
</objective>

<context>
!`cat .planning/phases/56-spawn-template-wiring/56-PATTERNS.md`
@.planning/phases/56-spawn-template-wiring/56-RESEARCH.md
@.planning/phases/56-spawn-template-wiring/56-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries
Internal markdown/agent-template editing; no external attack surface. Interpolated token is the enum-constrained `effort="<enum>"` / `""` string from Plan 01's query.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-B1 | Tampering | effort token in Agent() pseudocode + agent file | accept | Enum-constrained, pre-built by Plan 01 query; agent frontmatter untouched (body-only edit). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire --raw-form Group B workflows (audit-milestone, scan, secure-phase, ui-phase, ui-review, validate-phase, advisor)</name>
  <files>get-shit-done/workflows/audit-milestone.md, get-shit-done/workflows/scan.md, get-shit-done/workflows/secure-phase.md, get-shit-done/workflows/ui-phase.md, get-shit-done/workflows/ui-review.md, get-shit-done/workflows/validate-phase.md, get-shit-done/workflows/discuss-phase/modes/advisor.md</files>
  <read_first>
    - get-shit-done/workflows/audit-milestone.md (lines 37–98 — the complete resolve + Agent() block analog)
    - get-shit-done/workflows/scan.md, secure-phase.md, ui-phase.md, ui-review.md, validate-phase.md, discuss-phase/modes/advisor.md (each file's resolve-model line + Agent() block)
    - .planning/phases/56-spawn-template-wiring/56-PATTERNS.md (§"Group B workflows — standalone resolve pattern" and §"Shared Patterns")
    - .planning/phases/56-spawn-template-wiring/56-RESEARCH.md (§"D-07" Group B table — model vars per file)
  </read_first>
  <action>For each file, per the Group B pattern: (1) directly below each existing `<var>=$($GSD_SDK query resolve-model gsd-<agent> --raw)` line, add `<var>_effort_arg=$($GSD_SDK query resolve-model-effort gsd-<agent> --raw)`. (2) In the Agent() block, add the `{<var>_effort_arg}` line adjacent to the existing `model="{<var>}"` line. ui-phase.md has two agents (UI_RESEARCHER_MODEL, UI_CHECKER_MODEL) — wire both. advisor.md (ADVISOR_MODEL) is the site missed by the discuss-time grep — wire it. Use the `{existing_model_var}_effort_arg` naming convention verbatim (PATTERNS.md table). Per D-04 — the token is `effort="medium"` or `""`; pass it unconditionally, empty renders nothing. Per SPAWN-03 — add only the resolve line + the Agent() arg line; do NOT add or renumber any `Step N.M` heading, alter frontmatter, or add @-refs.</action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Each file has a `resolve-model-effort gsd-<agent> --raw` line adjacent to every `resolve-model gsd-<agent>` line
    - Each Agent() `model="{X}"` line has an adjacent `{X_effort_arg}` line
    - ui-phase.md has 2 resolve-model-effort lines (researcher + checker); advisor.md has 1
    - `npm test` green; step-numbering 632/632, cross-file-refs 219/219, negative-framing 99/99, agent-frontmatter 155/155 unchanged
  </acceptance_criteria>
  <done>All seven --raw-form sites carry the effort capture + token; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire legacy-jq + docs-update sites (debug.md, gsd-debug-session-manager.md, docs-update.md)</name>
  <files>get-shit-done/workflows/debug.md, agents/gsd-debug-session-manager.md, get-shit-done/workflows/docs-update.md</files>
  <read_first>
    - get-shit-done/workflows/debug.md (the legacy `resolve-model ... | jq -r '.model'` line + Agent() block)
    - agents/gsd-debug-session-manager.md (lines ~86–96 — the debugger resolve + model= spawn; this is an agent FILE, frontmatter must stay untouched)
    - get-shit-done/workflows/docs-update.md (the docs-init / doc_writer_model resolve + the ~10 Agent() model= sites)
    - .planning/phases/56-spawn-template-wiring/56-PATTERNS.md (§"For legacy jq-form sites" + §"Group B")
    - .planning/phases/56-spawn-template-wiring/56-RESEARCH.md (§"D-07" legacy-jq list + §"Pattern 3: docs.cjs gap" — use Option A)
  </read_first>
  <action>debug.md + gsd-debug-session-manager.md (legacy jq form): leave the existing `debugger_model=$(... | jq -r '.model' ...)` line unchanged; add an adjacent `debugger_model_effort_arg=$($GSD_SDK query resolve-model-effort gsd-debugger --raw 2>/dev/null || echo "")` line (always --raw regardless of the model line form), and add `{debugger_model_effort_arg}` adjacent to `model="{debugger_model}"` in the Agent() block. For gsd-debug-session-manager.md — edit only the spawn-block BODY at ~86–96; do NOT touch the agent's YAML frontmatter (agent-frontmatter gate 155/155 must hold). docs-update.md (Option A — docs.cjs NOT modified): add a standalone `doc_writer_model_effort_arg=$($GSD_SDK query resolve-model-effort gsd-doc-writer --raw)` capture adjacent to the doc_writer_model resolution, and add `{doc_writer_model_effort_arg}` adjacent to `model="{doc_writer_model}"` in each of the ~10 Agent() blocks. Per Pitfall 4 — do NOT wire docs-update via the init path (docs.cjs emits no doc_writer_effort); the standalone query call is authoritative. Per SPAWN-03 — no step renumbering, no frontmatter changes.</action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - debug.md + gsd-debug-session-manager.md: `debugger_model_effort_arg` capture (--raw) present; jq `.model` line unchanged; Agent() block carries the token
    - `git diff agents/gsd-debug-session-manager.md` shows no change above the body spawn block (frontmatter untouched)
    - docs-update.md: a single `resolve-model-effort gsd-doc-writer --raw` capture; every `model="{doc_writer_model}"` Agent() line has an adjacent `{doc_writer_model_effort_arg}` line; docs.cjs unchanged (`git status` shows no docs.cjs edit)
    - `npm test` green; agent-frontmatter 155/155 + all other fork gates unchanged
  </acceptance_criteria>
  <done>Three legacy/docs sites carry the effort token; docs.cjs and all frontmatter untouched; gates green.</done>
</task>

</tasks>

<verification>
- `grep -rn "resolve-model-effort gsd-" get-shit-done/workflows/{audit-milestone,debug,docs-update,scan,secure-phase,ui-phase,ui-review,validate-phase}.md get-shit-done/workflows/discuss-phase/modes/advisor.md agents/gsd-debug-session-manager.md` — every spawn site has an effort capture
- `git status get-shit-done/bin/lib/docs.cjs` — no change (Option A)
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` — full suite green; fork gates hold (agent-frontmatter 155/155, negative-framing 99/99, step-numbering 632/632, cross-file-refs 219/219, eta-include)
</verification>

<success_criteria>
- All 10 Group B spawn sites (9 workflows + 1 agent) carry the per-agent effort capture + token
- advisor.md (discuss-time-missed site) and gsd-debug-session-manager.md (agent orchestrator) both wired
- docs-update via standalone query (docs.cjs untouched); no step renumbered; no frontmatter altered
- Full npm test green with no new regressions
</success_criteria>

<output>
Create `.planning/phases/56-spawn-template-wiring/56-03-SUMMARY.md` when done.
</output>
