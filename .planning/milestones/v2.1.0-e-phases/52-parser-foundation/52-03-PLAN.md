---
phase: 52-parser-foundation
plan: 03
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - sdk/src/model-catalog.ts
  - tests/fixtures/parse-model-effort.json
  - sdk/src/parse-model-effort.test.ts
  - tests/parse-model-effort-parity.test.cjs
autonomous: true
requirements: [PARSE-04]
must_haves:
  truths:
    - "sdk/src/model-catalog.ts exports parseModelEffort with semantics identical to the core.cjs implementation"
    - "one shared JSON fixture of {input, expectedModel, expectedEffort} cases is loaded by BOTH the node --test parity suite and the vitest suite"
    - "the fixture covers: bare model, opus;high, every valid effort token, the provider-colon ID openrouter:anthropic/claude-opus (unchanged, effort null), and the opus;hihg typo (base + null)"
    - "the CJS parseModelEffort and the TS parseModelEffort return identical {model,effort} for every shared-fixture case"
    - "D4: parity is driven from a single shared fixture so the two case-lists cannot drift"
  artifacts:
    - path: sdk/src/model-catalog.ts
      provides: "TS parseModelEffort mirror (exported)"
      contains: "export function parseModelEffort"
    - path: tests/fixtures/parse-model-effort.json
      provides: "Shared {input, expectedModel, expectedEffort} parity fixture (DECISION 4 cases)"
      contains: "expectedEffort"
    - path: sdk/src/parse-model-effort.test.ts
      provides: "vitest parity suite loading the shared fixture (PARSE-04)"
      contains: "parseModelEffort"
    - path: tests/parse-model-effort-parity.test.cjs
      provides: "node --test CJS parity suite loading the same fixture"
      contains: "parseModelEffort"
  key_links:
    - from: sdk/src/parse-model-effort.test.ts
      to: tests/fixtures/parse-model-effort.json
      via: "path.resolve(__dirname, '../../tests/fixtures/parse-model-effort.json') read"
      pattern: "parse-model-effort.json"
    - from: tests/parse-model-effort-parity.test.cjs
      to: tests/fixtures/parse-model-effort.json
      via: "path.join read of the same fixture"
      pattern: "parse-model-effort.json"
---

<objective>
Mirror `parseModelEffort` verbatim into `sdk/src/model-catalog.ts`, create one shared JSON fixture of parity cases, and drive both a vitest suite and a node --test CJS parity suite from that single fixture (per D4). This proves CJS and TS parsers return identical shapes — PARSE-04.

Purpose: Single source of truth for parity cases so the two language implementations cannot silently drift (the exact failure PARSE-04 prevents).
Output: TS `parseModelEffort` mirror + `tests/fixtures/parse-model-effort.json` + `sdk/src/parse-model-effort.test.ts` + `tests/parse-model-effort-parity.test.cjs`.
</objective>

<context>
@.planning/phases/52-parser-foundation/52-CONTEXT.md
@.planning/phases/52-parser-foundation/52-RESEARCH.md
@.planning/phases/52-parser-foundation/52-PATTERNS.md
@.planning/phases/52-parser-foundation/52-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared parity fixture and mirror parseModelEffort into the SDK</name>
  <files>tests/fixtures/parse-model-effort.json, sdk/src/model-catalog.ts</files>
  <read_first>
    - sdk/src/model-catalog.ts lines 1-70 (existing exported helpers getAgentToModelMapForProfile/resolveRuntimeTierDefault/runtimesWithReasoningEffort, the `../shared/model-catalog.json` read pattern, and the export style to match)
    - .planning/phases/52-parser-foundation/52-RESEARCH.md lines 240-258 (the canonical TS mirror — reimplement verbatim with identical semantics)
    - .planning/phases/52-parser-foundation/52-PATTERNS.md lines 102-127 (TS mirror analog + fixture shape + required cases + location decision)
    - .planning/phases/52-parser-foundation/52-01-PLAN.md Task 1 action (the CJS algorithm and warn-once contract the TS mirror must match exactly)
  </read_first>
  <action>
    Create `tests/fixtures/parse-model-effort.json` as an array of `{input, expectedModel, expectedEffort}` objects covering exactly these cases (per D4 / CONTEXT line 48): bare model `opus` to `{opus, null}`; `opus;high` to `{opus, high}`; one case per valid token `opus;low`/`opus;medium`/`opus;high`/`opus;xhigh`/`opus;max` where effort equals the token; provider-colon ID `openrouter:anthropic/claude-opus` to `{openrouter:anthropic/claude-opus, null}` (colon never a delimiter); typo `opus;hihg` to `{opus, null}`. Then add `export function parseModelEffort(label: string): { model: string; effort: string | null }` to `sdk/src/model-catalog.ts` alongside the existing exported helpers. Reimplement the CJS algorithm verbatim: module-level `const EFFORT_TOKENS = new Set(['low','medium','high','xhigh','max'])` and `const _warnedEffortLabels = new Set<string>()`; non-string returns `{model: label, effort: null}`; `idx = label.lastIndexOf(';')`; `idx === -1` returns `{model: label, effort: null}`; valid suffix returns `{model: base, effort: suffix}`; otherwise emit a one-time `process.stderr.write` `gsd: warning — ` prefixed message (Set-gated, side channel only) and return `{model: base, effort: null}`. Match the warn message intent of the CJS version; warnings never appear in the returned shape. Use `lastIndexOf(';')` — never greedy `split(';')`.
  </action>
  <verify>
    <automated>node -e "const f=require('./tests/fixtures/parse-model-effort.json'); const a=require('assert'); a.ok(Array.isArray(f)&&f.length>=8); a.ok(f.some(c=>c.input==='openrouter:anthropic/claude-opus'&&c.expectedEffort===null)); a.ok(f.some(c=>c.input==='opus;hihg'&&c.expectedModel==='opus'&&c.expectedEffort===null)); console.log('OK')" && command grep -n "export function parseModelEffort" sdk/src/model-catalog.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/parse-model-effort.json` exists, parses as a JSON array, and includes the provider-colon case (effort null) and the `opus;hihg` typo case (model `opus`, effort null)
    - the fixture contains one entry per token: `opus;low`,`opus;medium`,`opus;high`,`opus;xhigh`,`opus;max`
    - `command grep -n "export function parseModelEffort" sdk/src/model-catalog.ts` returns exactly one match
    - the inline node -e fixture-shape check above exits 0 (prints OK)
    - `command grep -n "split(';')" sdk/src/model-catalog.ts` returns no match (lastIndexOf is used, not greedy split)
  </acceptance_criteria>
  <done>The shared fixture exists with all D4 cases and the TS parseModelEffort mirror is exported from sdk/src/model-catalog.ts with semantics matching the CJS implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add vitest and node --test parity suites driven by the shared fixture</name>
  <files>sdk/src/parse-model-effort.test.ts, tests/parse-model-effort-parity.test.cjs</files>
  <read_first>
    - sdk/src/config.test.ts lines 1-20 (vitest import + describe/it/expect style, fileURLToPath/readFileSync usage)
    - tests/feat-3023-model-phase-types.test.cjs lines 1-50 (node --test header, GSD_TEST_MODE, requires, assert/strict)
    - vitest.config.ts (root: './sdk', include `src/**/*.test.ts` — the vitest file MUST live under sdk/src/ to be collected; the fixture is read via fs, not collected, so an out-of-root data file resolves fine)
    - .planning/phases/52-parser-foundation/52-PATTERNS.md lines 160-171 (vitest parity-test analog)
  </read_first>
  <action>
    Create `sdk/src/parse-model-effort.test.ts`: import `parseModelEffort` from `./model-catalog.js`, load the shared fixture via `readFileSync(path.resolve(fileURLToPath(import.meta.url), '../../../tests/fixtures/parse-model-effort.json'), 'utf8')` (climb from `sdk/src/` to repo root then into `tests/fixtures/`; verify the relative depth empirically), JSON.parse it, and `describe`/`it.each` over the cases asserting `expect(parseModelEffort(c.input)).toEqual({ model: c.expectedModel, effort: c.expectedEffort })`. Create `tests/parse-model-effort-parity.test.cjs`: `'use strict'`, `process.env.GSD_TEST_MODE='1'`, require `parseModelEffort` from `../get-shit-done/bin/lib/core.cjs`, load the SAME fixture via `path.join(__dirname, 'fixtures', 'parse-model-effort.json')`, and for each case `assert.deepStrictEqual(parseModelEffort(c.input), { model: c.expectedModel, effort: c.expectedEffort })`. Both suites assert deep-equality on `{model,effort}` only — never `indexOf` truthiness or `includes` (Pitfall 4). After writing both, run each runner and confirm the fixture loads from both paths (the explicit both-runner reachability check per RESEARCH A2 / Pitfall 5); if the vitest relative path fails to resolve, that is the trigger to fall back to a `sdk/shared/`-hosted fixture — adjust both readers to the fallback path.
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort-parity.test.cjs 2>&1 | tail -5 && cd sdk && npx vitest run src/parse-model-effort.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/parse-model-effort.test.ts` and `tests/parse-model-effort-parity.test.cjs` both exist and both load `tests/fixtures/parse-model-effort.json` (empirically confirmed: both runner commands exit 0)
    - `node --test tests/parse-model-effort-parity.test.cjs` exits 0 (CJS parser matches every fixture case)
    - `cd sdk && npx vitest run src/parse-model-effort.test.ts` exits 0 (TS parser matches every fixture case — PARSE-04 parity)
    - `command grep -nE "indexOf|\.includes\(" sdk/src/parse-model-effort.test.ts tests/parse-model-effort-parity.test.cjs` returns no assertion-as-boolean usage
    - `npm test 2>&1 | tail -5` shows no new failures vs baseline
  </acceptance_criteria>
  <done>Both the CJS node --test suite and the SDK vitest suite load the single shared fixture and pass, proving CJS/TS parseModelEffort parity across every D4 case (PARSE-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared fixture JSON → both test runners | Parity cases cross from one on-disk JSON file into the CJS and TS test suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-DRIFT2 | Tampering | CJS vs TS parser semantic drift | mitigate | A single shared fixture (D4) drives both suites; a divergent edge case fails one suite immediately. No duplicated case-lists to drift. |
| T-52-VAL | Tampering | arbitrary effort token in fixture inputs | accept | Fixture is a test artifact under version control; the parser's `{low,medium,high,xhigh,max}` allowlist validation is exercised by the typo case, not bypassed. |
</threat_model>

<verification>
- `node --test tests/parse-model-effort-parity.test.cjs` exits 0.
- `cd sdk && npx vitest run src/parse-model-effort.test.ts` exits 0.
- Both suites read the identical fixture file.
- `npm test` shows no new failures vs baseline.
</verification>

<success_criteria>
- `parseModelEffort` exported from `sdk/src/model-catalog.ts` with identical semantics.
- One shared fixture drives both parity suites (D4).
- CJS and TS parsers return identical `{model,effort}` for every fixture case (PARSE-04).
</success_criteria>

<output>
Create `.planning/phases/52-parser-foundation/52-03-SUMMARY.md` when done.
</output>
