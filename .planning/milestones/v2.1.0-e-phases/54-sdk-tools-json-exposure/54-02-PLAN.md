---
phase: 54-sdk-tools-json-exposure
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - sdk/src/model-catalog.ts
  - sdk/src/query/config-query.ts
  - sdk/src/handlers/init/composer.ts
  - sdk/src/handlers/init/complex.ts
  - sdk/src/golden/read-only-golden-rows.ts
  - sdk/src/golden/init-golden-normalize.ts
  - sdk/src/golden/read-only-parity.integration.test.ts
autonomous: true
requirements: [EXPOSE-03]
must_haves:
  truths:
    - "SDK resolveModel emits a canonical `effort` field (always present, ?? null) matching the CLI cmdResolveModel shape"
    - "SDK init handlers emit a *_effort sibling for every *_model, mirroring the CLI init builders (same agent)"
    - "SDK runtimesWithReasoningEffort uses the static {claude, codex} allowlist, not the data-derived scan (parity source of truth, Pitfall 1)"
    - "The existing resolve-model golden parity row passes with both sides emitting `effort: null` on a bare catalog (EXPOSE-03 for resolve-model)"
    - "A NEW init-builder golden row enforces *_effort sibling parity, with volatile keys stripped before toEqual (EXPOSE-03 for init)"
    - "SDK no longer emits `reasoning_effort` from resolveModel (renamed to effort, D-05 parity)"
  artifacts:
    - path: "sdk/src/model-catalog.ts"
      provides: "static {claude, codex} runtimesWithReasoningEffort allowlist"
      contains: "claude"
    - path: "sdk/src/query/config-query.ts"
      provides: "ported effort precedence chain + always-emit `effort`"
      contains: "effort"
    - path: "sdk/src/handlers/init/composer.ts"
      provides: "getEffort helper + *_effort siblings"
    - path: "sdk/src/handlers/init/complex.ts"
      provides: "*_effort siblings"
    - path: "sdk/src/golden/read-only-golden-rows.ts"
      provides: "new init-builder parity row"
  key_links:
    - from: "sdk/src/query/config-query.ts resolveModel"
      to: "core.cjs resolveReasoningEffortInternal precedence chain"
      via: "ported 4-step precedence mirror"
      pattern: "effort"
    - from: "sdk/src/handlers/init composer.ts/complex.ts"
      to: "resolveModel effort"
      via: "getEffort helper reading data.effort"
      pattern: "getEffort"
---

<objective>
Port the minimal effort-resolution logic into the SDK so it emits identical `effort` / `*_effort` JSON shapes to the CLI (EXPOSE-03 / D-07), and reuse the existing read-only golden parity harness to enforce SDK↔CLI parity (D-08). Mirror the CLI precedence chain in `config-query.ts resolveModel`, reconcile the data-derived SDK allowlist to the static `{claude, codex}` set, add `*_effort` siblings to the SDK init handlers, and extend the golden rows with an init-builder row.

Purpose: Guarantee the SDK and CLI produce byte-identical model+effort shapes for the same inputs — the cross-language parity gate that Phase 58's regression suite and downstream runtimes depend on.
Output: Modified SDK src (model-catalog.ts, config-query.ts, composer.ts, complex.ts) + extended golden harness rows/normalize; parity suite green.
</objective>

<execution_context>
<required_reading>
Read STATE.md and config.json before any operation.
Consult @$HOME/.claude/get-shit-done/references/git-integration.md before creating any commit.
</required_reading>
</execution_context>

<context>
@.planning/phases/54-sdk-tools-json-exposure/54-CONTEXT.md
@.planning/phases/54-sdk-tools-json-exposure/54-RESEARCH.md
@.planning/phases/54-sdk-tools-json-exposure/54-PATTERNS.md
@.planning/phases/54-sdk-tools-json-exposure/54-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog/config → SDK JSON | resolved effort values (constrained to `{low,medium,high,xhigh,max}` or `null`) cross into SDK query output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | Tampering | SDK effort emission | accept | SDK mirrors the CLI resolver over already-validated tokens (parseModelEffort/EFFORT_TOKENS, PARSE-04 mirror); no new untrusted input parsing. |
| T-54-04 | Spoofing/Parity | SDK↔CLI allowlist divergence | mitigate | Reconcile SDK `runtimesWithReasoningEffort` to the static `{claude, codex}` set so the two surfaces cannot silently diverge after Phase 55 (Pitfall 1); the golden `toEqual` parity row is the enforcing tripwire. |

**Honest assessment:** No new high/medium security threat surface — additive read-only JSON exposure with no new input handling, I/O sinks, or auth/network surface. The one `mitigate` (T-54-04) is a correctness/parity concern, not a security vulnerability: it guards against the SDK and CLI drifting once live effort values are assigned in Phase 55.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port effort resolution into SDK resolveModel + static allowlist (EXPOSE-03 / D-07)</name>
  <files>sdk/src/model-catalog.ts, sdk/src/query/config-query.ts</files>
  <read_first>
    - sdk/src/model-catalog.ts (lines 60-70 runtimesWithReasoningEffort data-derived scan; line ~82 EFFORT_TOKENS; line ~89 parseModelEffort)
    - sdk/src/query/config-query.ts (resolveModel ~174-314, esp. 195 resolveRuntimeTier, 208-210 delete-guard, 280-287 current conditional reasoning_effort emit)
    - get-shit-done/bin/lib/core.cjs (resolveReasoningEffortInternal ~1596-1635 — the canonical 4-step precedence chain to mirror; RUNTIMES_WITH_REASONING_EFFORT static set)
    - .planning/phases/54-sdk-tools-json-exposure/54-PATTERNS.md (the config-query.ts and model-catalog.ts assignment sections — port requirements + the 4 precedence steps)
    - .planning/phases/54-sdk-tools-json-exposure/54-01-SUMMARY.md (the final CLI `effort` shape this must match byte-for-byte)
  </read_first>
  <behavior>
    - `runtimesWithReasoningEffort()` returns the static `{claude, codex}` set (not a data-derived scan of runtimeTierDefaults).
    - SDK `resolveModel(['gsd-planner'])` returns `data.effort` always present, value `null` on a bare catalog — matching the CLI `resolve-model gsd-planner` output exactly.
    - SDK no longer emits `reasoning_effort` (renamed to `effort`, D-05 parity).
    - Effort follows all 4 CLI precedence steps: allowlist gate → per-agent override (parseModelEffort) → shared slot effort (parseModelEffort) → Codex per-tier fallback → null; runtimes outside `{claude, codex}` always resolve null.
    - The config-query.ts:208-210 Codex delete-guard still behaves correctly once the allowlist is static.
  </behavior>
  <action>
    In sdk/src/model-catalog.ts, replace the data-derived body of `runtimesWithReasoningEffort()` (lines 64-70) with a static `new Set(['claude', 'codex'])` matching the CLI `RUNTIMES_WITH_REASONING_EFFORT` (per D-07 / Pitfall 1 — the parity source of truth). Reuse the existing `parseModelEffort` (line 89) and `EFFORT_TOKENS` (line 82); do not re-implement either. In sdk/src/query/config-query.ts `resolveModel`, port the canonical 4-step precedence chain from `core.cjs resolveReasoningEffortInternal`: (1) allowlist gate — if `!config.runtime || !runtimesWithReasoningEffort().has(runtime)` resolve effort to `null`; (2) per-agent override → `parseModelEffort(override).effort`; (3) shared resolved slot → `parseModelEffort(tier).effort`; (4) Codex per-tier fallback via `resolveRuntimeTier(...).reasoning_effort` only when the slot carries no suffix. Replace the conditional `if (runtimeTier.reasoning_effort) result.reasoning_effort = ...` (lines 283-285) with always-emit canonical `result.effort = <resolvedEffort> ?? null` (D-03/D-04/D-05) across ALL return paths of resolveModel so the `effort` key is present on every path (bare, override, codex), matching the CLI. Verify the line 208-210 delete-guard still behaves once the set is static `{claude, codex}` (adjust if the guard relied on the data-derived membership).
  </action>
  <verify>
    <automated>npm --prefix sdk test -- read-only-parity</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -n "claude" sdk/src/model-catalog.ts` shows `runtimesWithReasoningEffort` returning a static set containing `claude` and `codex`
    - `command grep -c "reasoning_effort" sdk/src/query/config-query.ts` reflects only the Codex per-tier FALLBACK reads (not an emitted output key) — the emitted output field is `effort`, never `reasoning_effort`
    - config-query.ts resolveModel emits `effort` on every return path (bare/override/codex), value `null` when unresolved
    - `npm --prefix sdk test -- read-only-parity` passes — the existing `resolve-model gsd-planner` row `toEqual` now matches with both sides emitting `effort: null`
    - SDK compiles: `npm --prefix sdk run build` (or tsc) succeeds with no type errors
  </acceptance_criteria>
  <done>SDK resolveModel mirrors the CLI 4-step effort precedence, emits canonical always-present `effort`, uses the static `{claude, codex}` allowlist; the existing resolve-model golden row enforces parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDK init *_effort siblings + new init-builder golden parity row (EXPOSE-03 / D-08)</name>
  <files>sdk/src/handlers/init/composer.ts, sdk/src/handlers/init/complex.ts, sdk/src/golden/read-only-golden-rows.ts, sdk/src/golden/init-golden-normalize.ts, sdk/src/golden/read-only-parity.integration.test.ts</files>
  <read_first>
    - sdk/src/handlers/init/composer.ts (getModelAlias helper 43-47; builder *_model sites at 449-450, 556-558, 648-650, 713-716, 788-789, 1115)
    - sdk/src/handlers/init/complex.ts (resolveModel usage ~53; *_model sites at 317-319, 492-493)
    - sdk/src/golden/read-only-golden-rows.ts (line 21 resolve-model row; line ~45 init.list-workspaces row — the row shape to follow)
    - sdk/src/golden/init-golden-normalize.ts (lines 7-15 INIT_QUICK_VOLATILE_KEYS + omitInitQuickVolatile — the strip pattern to mirror)
    - sdk/src/golden/read-only-parity.integration.test.ts (line ~26 the toEqual equality gate; how rows are run and normalized)
    - .planning/phases/54-sdk-tools-json-exposure/54-RESEARCH.md (SDK Mirror Inventory table + Open Question 2 on init-builder parity coverage)
  </read_first>
  <behavior>
    - Each SDK init builder emits a `<role>_effort` sibling for every `<role>_model`, derived from the SAME agentType (mirrors the CLI init builders 1:1).
    - On a bare catalog every SDK `*_effort` value is `null`.
    - A new golden parity row for one init builder that emits `*_model` (e.g. an `init` execute-phase-family command with deterministic args) passes `toEqual` against the CLI output after volatile keys are stripped.
    - The new init-builder row enforces that SDK and CLI emit identical `*_effort` siblings (EXPOSE-01 parity, previously uncovered).
  </behavior>
  <action>
    Add a `getEffort(agentType, projectDir)` helper to composer.ts (and complex.ts as needed) mirroring `getModelAlias` (composer.ts:43-47) but reading `data.effort` from the ported `resolveModel` result (now emitting `effort` after Task 1). At every SDK `*_model` site (composer.ts 449-450, 556-558, 648-650, 713-716, 788-789, 1115; complex.ts 317-319, 492-493), add a `<role>_effort` field next to the `<role>_model`, using the SAME agentType as the adjacent `*_model` (same-slot invariant, per the RESEARCH SDK Mirror Inventory). Then add a new row to `READ_ONLY_JSON_PARITY_ROWS` (read-only-golden-rows.ts) for an init builder that emits `*_model`/`*_effort` with deterministic args, following the existing row shape. Since init builders carry volatile fields, add a new `*_VOLATILE_KEYS` const + `omit*` strip helper to init-golden-normalize.ts (mirroring INIT_QUICK_VOLATILE_KEYS/omitInitQuickVolatile) for that builder's volatile keys, and wire the strip into the row's normalize step in read-only-parity.integration.test.ts before `toEqual`. If no init builder can be invoked deterministically in-process, fall back to a dedicated effort-parity fixture (D-08 fallback clause) — but prefer the harness extension.
  </action>
  <verify>
    <automated>npm --prefix sdk test -- read-only-parity</automated>
  </verify>
  <acceptance_criteria>
    - composer.ts and complex.ts each emit a `*_effort` sibling for every `*_model` site (count matches: composer 6 builder regions, complex 2)
    - Each `_effort` sibling uses the SAME agentType as its adjacent `_model`
    - A `getEffort` helper exists reading `data.effort` from resolveModel
    - read-only-golden-rows.ts has a NEW init-builder row that emits *_model/*_effort
    - init-golden-normalize.ts has a new volatile-key strip mirroring omitInitQuickVolatile, wired into the new row's normalize
    - `npm --prefix sdk test -- read-only-parity` passes (both resolve-model and the new init-builder row green)
    - SDK compiles with no type errors
  </acceptance_criteria>
  <done>SDK init handlers emit same-agent *_effort siblings; a new init-builder golden row with volatile-key strip enforces SDK↔CLI *_effort parity green.</done>
</task>

</tasks>

<verification>
- `npm --prefix sdk test -- read-only-parity` green (resolve-model row + new init-builder row)
- `npm --prefix sdk run build` / tsc succeeds with no type errors
- Full gate: `npm test 2>&1 | tee /tmp/gsd-test-output.txt` shows zero new CLI regressions; SDK vitest green
- `command grep` confirms SDK `runtimesWithReasoningEffort` is the static `{claude, codex}` set and the emitted SDK field is `effort` (not `reasoning_effort`)
</verification>

<success_criteria>
- EXPOSE-03: SDK and CLI emit byte-identical model+effort shapes — enforced by the golden resolve-model row (effort) + new init-builder row (*_effort siblings)
- D-07: effort precedence ported into the SDK; static {claude, codex} allowlist reconciled
- D-08: parity verified by reusing/extending the existing golden harness, not a parallel mechanism
- Bare-catalog inertness preserved cross-language (all SDK effort values null)
</success_criteria>

<output>
Create `.planning/phases/54-sdk-tools-json-exposure/54-02-SUMMARY.md` when done.
</output>
