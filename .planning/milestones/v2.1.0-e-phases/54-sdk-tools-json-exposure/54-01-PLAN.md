---
phase: 54-sdk-tools-json-exposure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/commands.cjs
  - tests/init.test.cjs
  - tests/commands.test.cjs
  - tests/core.test.cjs
autonomous: true
requirements: [EXPOSE-01, EXPOSE-02]
must_haves:
  truths:
    - "Every *_model field in every init.cjs builder has a sibling *_effort key (1:1, same agent)"
    - "cmdResolveModel always emits a canonical `effort` field (never omitted)"
    - "On a bare catalog every exposed *_effort / effort value is null (SC#4 inertness)"
    - "D-05: reasoning_effort is no longer emitted from cmdResolveModel output (renamed to effort, no alias)"
    - "D-06: each *_effort sibling derives from the SAME agent slot as its adjacent *_model (no #3023 divergence)"
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "*_effort siblings for all 20 *_model sites + resolveReasoningEffortInternal import"
      contains: "executor_effort"
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "cmdResolveModel always-emit effort field"
      contains: "result.effort = reasoningEffort ?? null"
    - path: "tests/init.test.cjs"
      provides: "1:1 *_model/*_effort pairing + bare-catalog null assertions"
    - path: "tests/commands.test.cjs"
      provides: "always-emit effort assertions; reasoning_effort absence"
    - path: "tests/core.test.cjs"
      provides: "bare-catalog inertness loop over all agent slots"
  key_links:
    - from: "init.cjs *_effort siblings"
      to: "core.cjs resolveReasoningEffortInternal"
      via: "same-agent call with ?? null"
      pattern: "resolveReasoningEffortInternal\\(cwd, '[^']+'\\) \\?\\? null"
    - from: "commands.cjs cmdResolveModel"
      to: "result.effort"
      via: "always-emit ?? null"
      pattern: "result\\.effort = reasoningEffort \\?\\? null"
---

<objective>
Expose resolved reasoning effort in the CLI init JSON and the resolve-model JSON surface. Add a `*_effort` sibling for every `*_model` field across all init.cjs builders (EXPOSE-01 / D-06 blanket coverage), and flip `cmdResolveModel` from omit-when-falsy `reasoning_effort` to always-emit canonical `effort` (EXPOSE-02 / D-03/D-04/D-05).

Purpose: Make Phase 53's resolved effort observable to workflows and the resolve-model surface, with a uniform self-documenting explicit-null contract (D-01) — the plumbing that Phase 56 spawn-templates will consume.
Output: Modified init.cjs + commands.cjs; new/updated unit tests proving 1:1 sibling coverage, always-emit, and bare-catalog inertness.
</objective>

<execution_context>
<required_reading>
Read STATE.md and config.json before any operation.
Consult @$HOME/.claude/get-shit-done/references/git-integration.md before creating any commit.
</required_reading>
</execution_context>

<context>
@.planning/phases/54-sdk-tools-json-exposure/54-CONTEXT.md
@.planning/phases/54-sdk-tools-json-exposure/54-RESEARCH.md
@.planning/phases/54-sdk-tools-json-exposure/54-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/catalog → init JSON | resolved effort values (already constrained to `{low,medium,high,xhigh,max}` or `null`) cross into JSON output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | effort value injected into JSON | accept | No new untrusted input: effort tokens are validated upstream by `parseModelEffort`/`EFFORT_TOKENS` (Phase 52); this phase emits already-resolved values, never raw user strings. The `model;effort` combined form never reaches a shell. |
| T-54-02 | Information Disclosure | additive null keys in JSON | accept | New keys are null-valued on a bare catalog; no secret or PII exposure; output shape is internal workflow plumbing. |

**Honest assessment:** This phase introduces no new high/medium threat surface. It is pure additive read-only JSON exposure — no new input handling, no I/O sinks, no auth/network/shell surface. On a bare catalog all new keys are explicit `null` with zero behavior change. No threat warrants a `mitigate` disposition.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add *_effort siblings to all init.cjs builders (EXPOSE-01 / D-06)</name>
  <files>get-shit-done/bin/lib/init.cjs, tests/init.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/init.cjs (lines 1-12 import block; and each builder region at 197-198, 343-345, 530-532, 583-585, 640-643, 762-763, 1096, 1552-1553)
    - get-shit-done/bin/lib/core.cjs (resolveReasoningEffortInternal ~1596-1635 and its export block — confirm it is exported)
    - tests/init.test.cjs (lines 36-51 executor_model override test; lines ~1352-1354 "field present" idiom)
    - .planning/phases/54-sdk-tools-json-exposure/54-RESEARCH.md (the "Validated *_model Field Inventory" table — canonical agent-slot mapping for each sibling)
  </read_first>
  <behavior>
    - For every init builder, `init <command> --raw` JSON output contains a `<role>_effort` key for each `<role>_model` key (1:1 pairing, blanket coverage D-06).
    - On this repo's bare catalog, each `*_effort` value is exactly `null` (SC#4 inertness).
    - Existing `*_model` values are UNCHANGED (additive-superset, D-02 — assert existing values identical, not whole-JSON byte-equality).
    - The sibling for `executor_model` resolves effort for `gsd-executor`, `verifier_model` for `gsd-verifier`, etc. — the SAME agent as the adjacent `*_model` (no #3023 divergence).
  </behavior>
  <action>
    Add `resolveReasoningEffortInternal` to the existing destructured `require('./core.cjs')` at init.cjs:8 (alongside `resolveModelInternal`). Then at each of the 20 `*_model` sites (lines 197-198, 343-345, 530-532, 583-585, 640-643, 762-763, 1096, 1552-1553 per the RESEARCH inventory table), insert a sibling line immediately after each `<role>_model: resolveModelInternal(cwd, '<agent>'),` of the form `<role>_effort: resolveReasoningEffortInternal(cwd, '<agent>') ?? null,` — deriving the `_effort` key by replacing the `_model` suffix (D-04) and using the EXACT SAME agent string as the adjacent `*_model` (per D-06; the inventory table is canonical — do not invent agent slots). Keep `?? null` per D-01's explicit-null contract (belt-and-suspenders guard against an undefined leak; the resolver already returns null on a bare catalog). Then add/extend assertions in tests/init.test.cjs mirroring the init.test.cjs:36-51 analog: for at least one builder per region, parse `--raw` JSON and assert that for every key ending `_model` a matching `_effort` key exists and is `null` on the bare catalog.
  </action>
  <verify>
    <automated>node --test tests/init.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c "resolveReasoningEffortInternal(cwd," get-shit-done/bin/lib/init.cjs` returns 20 (one sibling per *_model site)
    - init.cjs:8 import line contains `resolveReasoningEffortInternal`
    - Every sibling uses `?? null` — `command grep -c "resolveReasoningEffortInternal(cwd, '[^']*') ?? null" get-shit-done/bin/lib/init.cjs` returns 20
    - For each builder, the `_effort` agent string equals the adjacent `_model` agent string (verify by reading paired lines — e.g. executor_effort → 'gsd-executor')
    - `node --test tests/init.test.cjs` passes; new assertions confirm 1:1 *_model/*_effort pairing and null values on bare catalog
  </acceptance_criteria>
  <done>All 20 init.cjs *_model sites have a same-agent *_effort sibling defaulting to null; the import is added; tests assert 1:1 pairing and bare-catalog null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cmdResolveModel always-emit canonical effort + inertness test (EXPOSE-02 / D-03/D-04/D-05 / SC#4)</name>
  <files>get-shit-done/bin/lib/commands.cjs, tests/commands.test.cjs, tests/core.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/commands.cjs (cmdResolveModel, lines 236-251 — current omit-when-falsy emit at line 250)
    - tests/commands.test.cjs (lines 1138-1187 — the THREE existing tests that assert OLD `reasoning_effort` name and the omit/absence contract; and lines 1082-1090 JSON-shape idiom)
    - get-shit-done/bin/lib/core.cjs (resolveReasoningEffortInternal — confirm it returns null for all agents on a bare catalog)
    - .planning/phases/54-sdk-tools-json-exposure/54-PATTERNS.md (the commands.test.cjs assignment section — the exact assertion flips required)
  </read_first>
  <behavior>
    - `resolve-model gsd-planner --raw` JSON always contains an `effort` key (value `null` on bare catalog), per D-01/D-03.
    - The JSON NEVER contains `reasoning_effort` (renamed, no alias — D-05).
    - When effort IS resolved (e.g. a config override carrying a valid suffix), `effort` holds the verbatim value; `max` is emitted verbatim on the Claude path (no clamp — inherited Phase 53 D-03).
    - Inertness: looping `resolveReasoningEffortInternal` over all resolvable agent slots on a bare catalog returns `null` for every one (SC#4).
  </behavior>
  <action>
    In `cmdResolveModel` (commands.cjs:250), replace the omit-when-falsy line `if (reasoningEffort) result.reasoning_effort = reasoningEffort;` with the always-emit canonical-name line `result.effort = reasoningEffort ?? null;` (per D-03 always-emit, D-04 canonical `effort`, D-05 rename — do NOT keep `reasoning_effort` alongside or as an alias). `resolveReasoningEffortInternal` is already in scope at line 244 — no new import. Then UPDATE the three existing tests at commands.test.cjs:1138-1187 (do not merely add new ones): flip the `output.reasoning_effort` value assertion to `output.effort` under the new name, and flip the two `assert.ok(!hasOwnProperty(output,'reasoning_effort'))` absence assertions to `assert.strictEqual(output.effort, null)` plus `assert.ok('effort' in output)` (per D-01 always-present). Add an inertness test in tests/core.test.cjs that loops `resolveReasoningEffortInternal(cwd, agent)` over the resolvable agent slots (gsd-executor, gsd-verifier, gsd-planner, gsd-plan-checker, gsd-phase-researcher, gsd-project-researcher, gsd-research-synthesizer, gsd-roadmapper, gsd-codebase-mapper) and asserts `null` for each (SC#4 proof — mirrors RESEARCH "Verifying inertness on a bare catalog").
  </action>
  <verify>
    <automated>node --test tests/commands.test.cjs tests/core.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - commands.cjs contains `result.effort = reasoningEffort ?? null`
    - `command grep -c "reasoning_effort" get-shit-done/bin/lib/commands.cjs` returns 0 (rename complete, no alias — D-05)
    - The three updated tests at commands.test.cjs:1138-1187 assert `output.effort` (not `output.reasoning_effort`) and assert `effort` present-with-null where they previously asserted absence
    - tests/core.test.cjs has an inertness loop asserting `null` for all 9 listed agent slots on a bare catalog
    - `node --test tests/commands.test.cjs tests/core.test.cjs` passes
  </acceptance_criteria>
  <done>cmdResolveModel always emits canonical `effort` (null on bare catalog), `reasoning_effort` fully removed; existing tests updated to the new contract; inertness loop proves SC#4.</done>
</task>

</tasks>

<verification>
- `node --test tests/init.test.cjs tests/commands.test.cjs tests/core.test.cjs` all green
- `command grep -c "resolveReasoningEffortInternal(cwd," get-shit-done/bin/lib/init.cjs` == 20
- `command grep -c "reasoning_effort" get-shit-done/bin/lib/commands.cjs` == 0
- `node get-shit-done/bin/gsd-tools.cjs resolve-model gsd-planner --raw` JSON contains key `effort` with value `null`, and no key `reasoning_effort`
</verification>

<success_criteria>
- EXPOSE-01: every init builder *_model has a same-agent *_effort sibling (20 siblings, blanket coverage)
- EXPOSE-02: cmdResolveModel always emits canonical `effort`; `reasoning_effort` removed
- SC#4: bare-catalog inertness proven — all siblings + effort field resolve to null
- D-02: additive-superset — existing *_model values unchanged
</success_criteria>

<output>
Create `.planning/phases/54-sdk-tools-json-exposure/54-01-SUMMARY.md` when done.
</output>
