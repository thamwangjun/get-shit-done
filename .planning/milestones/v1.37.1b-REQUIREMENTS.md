# Requirements Archive: v1.37.1b Fork Tag Corpus Tests

**Archived:** 2026-04-29
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: GSD — Prompt-Engineered Fork (v1.37.1b)

**Defined:** 2026-04-28
**Core Value:** Every prompt file on `thamw-main` meets the fork's prompt engineering quality bar before it ships — upstream content additions are modified, not accepted verbatim.

## v1.37.1b Requirements

Requirements for milestone v1.37.1b: Fork Tag Corpus Tests. Each maps to roadmap phases.

### Persona Tag Tests

- [x] **PERSONA-01**: `tests/fork-persona-tag.test.cjs` exists and passes — scans all 31 `agents/gsd-*.md` files, asserts each contains `<persona>` tag, asserts none contain `<role>` as a persona XML tag (code-fence content excluded from check)

### Intent Tag Tests

- [x] **INTENT-01**: `tests/fork-intent-tag.test.cjs` exists and passes — scans all `commands/gsd/*.md` files, asserts each contains `<intent>` block, asserts none use bare `<task>` as the outermost primary directive block

### Test Suite Gate

- [x] **TEST-GATE-01**: Full test suite (`npm test`) passes with both new test files integrated — no regressions introduced, all existing subtests green

## Intent Tag Conversion

### v1.37.1b Completion — Objective Tag Removal

- [x] **CONVERT-01**: All 79 `commands/gsd/*.md` files pass `fork-intent-tag.test.cjs` (79/79 subtests pass, 0 failures) — closes the loop on INTENT-01 from Phase 18 by converting the 33 remaining `<objective>` files to `<intent>`

## Future Requirements

Items not in this milestone but acknowledged:

- **GAP-03**: Statusline `isDevInstall` semver logic absence test (static analysis)
- **GAP-04**: Fork reference files integrity tests (`.planning/references/PROMPT_ENGINEERING_GUIDE_V09.md`, `.planning/references/PROMPT_IMPROVEMENT_GUIDE_V01.md` existence + minimum size)
- **GAP-05**: Canonical `plans/` playbook existence tests

## Out of Scope

| Feature | Reason |
|---------|--------|
| GAP-3 statusline test | Low risk: statusline already has tests; `isDevInstall` absence is a static-analysis concern for a future milestone |
| GAP-4 reference file tests | Low risk: upstream can't touch these files; accidental deletion is unlikely without git showing it |
| GAP-5 plans/ existence tests | Low risk: plans/ is fork-owned and upstream has no path to delete it |
| Any new prompt file edits | This milestone is test-only — no changes to prompt content |
| Updating UPSTREAM_TO_FORK_CHANGES_GUIDE | The guide's stale "155 subtests" claim will be corrected implicitly when the new test files are in place |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| PERSONA-01 | Phase 18 | Complete |
| INTENT-01 | Phase 18 | Complete |
| TEST-GATE-01 | Phase 18 | Complete |
| CONVERT-01 | Phase 19 | Complete |

**Coverage:**
- v1.37.1b requirements: 4 total
- Mapped to phases: 4
- Unmapped: 0 ✓

---
*Requirements defined: 2026-04-28*
*Last updated: 2026-04-29 after Phase 18 & 19 completion — all requirements marked complete*
