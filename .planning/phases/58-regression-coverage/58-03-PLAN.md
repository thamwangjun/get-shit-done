---
phase: 58-regression-coverage
plan: 03
type: execute
wave: 2
depends_on: ["58-01", "58-02"]
files_modified:
  - tests/feat-58-regression.test.cjs
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04, TEST-05]
must_haves:
  truths:
    - "tests/feat-58-regression.test.cjs reads the committed golden fixture and asserts strictEqual(live resolution, literal) for every row — the resolver cannot shift the expected (D-01)"
    - "Per-runtime omit contract: resolveReasoningEffortInternal returns null for each of the 13 non-effort runtimes"
    - "translateEffortForCodex(max) === 'xhigh' and pass-through/null cases hold — the max→xhigh boundary is tested separately from the resolver golden (TEST-03)"
    - "An antipattern guard lints tests/*.cjs and fails on indexOf-as-boolean on effort tokens and bare includes('medium'|'high') in assert.ok (TEST-04)"
    - "D-G1: the antipattern guard does NOT flag feat-57's assert.ok(toml.includes('model_reasoning_effort = \"xhigh\"')) — a full structured key-value string is safe, only bare-token matches are flagged"
    - "D-04: each new assertion is mutation-verified RED-before-fix (invert logic → confirm RED → revert), documented per-assertion in SUMMARY"
    - "Full npm test passes with zero new regressions and ≥70% line coverage on get-shit-done/bin/lib/*.cjs maintained (TEST-05)"
  artifacts:
    - path: "tests/feat-58-regression.test.cjs"
      provides: "Milestone regression suite: static golden (TEST-01) + omit/translate contract (TEST-03) + antipattern guard (TEST-04)"
      contains: "golden-effort-snapshot.json"
      min_lines: 80
  key_links:
    - from: "tests/feat-58-regression.test.cjs"
      to: "tests/fixtures/golden-effort-snapshot.json"
      via: "readFileSync + iterate rows asserting strictEqual against literals"
      pattern: "golden-effort-snapshot"
    - from: "tests/feat-58-regression.test.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "resolveModelInternal / resolveReasoningEffortInternal / translateEffortForCodex"
      pattern: "translateEffortForCodex"
    - from: "tests/feat-58-regression.test.cjs"
      to: "tests/*.cjs"
      via: "readdir + readFileSync + regex lint for antipattern detection"
      pattern: "readdirSync"
---

<objective>
Build the milestone regression test file that consumes the committed golden fixture (from plan 01) with strict-equality assertions, asserts the per-runtime omit/translate contract, and adds a committed antipattern guard that structurally prevents the `indexOf`-as-boolean and `medium`/`high` substring-collision false-pass classes from recurring. Then run the full suite + coverage gate.

Purpose: This is the discoverable home for the cross-cutting v2.1.0-e (Phases 52–57) regression contract. It freezes the post-D-08 answer, proves each assertion actually bites (mutation RED-before-fix per D-04), and closes the milestone with a green suite and maintained coverage.
Output: `tests/feat-58-regression.test.cjs` covering TEST-01 (assertion), TEST-03, TEST-04; plus the TEST-05 full-suite/coverage gate.
</objective>

<context>
!`cat .planning/phases/58-regression-coverage/58-PATTERNS.md`

@.planning/phases/58-regression-coverage/58-RESEARCH.md
@.planning/phases/58-regression-coverage/58-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write feat-58-regression.test.cjs — static golden (TEST-01) + omit/translate contract (TEST-03)</name>
  <files>tests/feat-58-regression.test.cjs</files>
  <read_first>
    - tests/feat-53-config-sites-and-golden.test.cjs (the EXACT analog — imports block lines 1-51, writeConfig + rmr helpers lines 43-65, the DYNAMIC golden loop lines 243-308 to INVERT into a static one; note line ~271 computes expectedEffort FROM the resolver — the pitfall to avoid)
    - tests/fixtures/golden-effort-snapshot.json (the committed fixture from plan 01 — the literal source of truth; iterate rows[] and omitContract[])
    - get-shit-done/bin/lib/core.cjs (signatures of resolveModelInternal, resolveReasoningEffortInternal, translateEffortForCodex, _resetEffortWarningCacheForTests; confirm translateEffortForCodex(max)==='xhigh', null/undefined→null, pass-through for low/medium/high)
    - get-shit-done/bin/lib/model-catalog.cjs (KNOWN_RUNTIMES, RUNTIMES_WITH_REASONING_EFFORT for the non-effort runtime list)
    - tests/feat-57-install-translation.test.cjs (existing translateEffortForCodex + codex-TOML coverage — so TEST-03 here adds the cross-cutting 13-runtime omit contract without duplicating feat-57)
    - .planning/phases/58-regression-coverage/58-PATTERNS.md (§tests/feat-58-regression.test.cjs — static golden test pattern, shared patterns: GSD_TEST_MODE, assert/strict, _resetEffortWarningCacheForTests, section banners)
  </read_first>
  <action>
    Create `tests/feat-58-regression.test.cjs`. Set `process.env.GSD_TEST_MODE = '1'` before any core.cjs require (per PATTERNS §shared). Use `require('node:assert/strict')` (avoids the loose-equal trap). Import `resolveModelInternal`, `resolveReasoningEffortInternal`, `translateEffortForCodex`, `_resetEffortWarningCacheForTests` from core.cjs, and `KNOWN_RUNTIMES`, `RUNTIMES_WITH_REASONING_EFFORT` from model-catalog.cjs. Copy the `writeConfig` + `rmr` helpers and the `makeTmp` createTempDir wrapper from the feat-53 analog.

    TEST-01 (static golden) — section banner `// ─── TEST-01: Static golden snapshot ───`: read `tests/fixtures/golden-effort-snapshot.json`. For each `row` in `fixture.rows`, emit a `test('golden: ${agent}/${profile}/${runtime}')` that creates a temp dir, calls `_resetEffortWarningCacheForTests()`, writes `{ runtime, model_profile: profile }` config, then asserts `assert.strictEqual(resolveModelInternal(d, row.agent), row.expectedModel)` AND `assert.strictEqual(resolveReasoningEffortInternal(d, row.agent), row.expectedEffort)` against the LITERAL fixture values (never compute expected from the resolver — that is the feat-53 pitfall this file exists to cover). Wrap in try/finally with rmr cleanup. For each `row` in `fixture.omitContract`, emit a test asserting `resolveReasoningEffortInternal(d, row.sampleAgent)` strictEqual null with that runtime configured.

    TEST-03 (translate boundary) — section banner `// ─── TEST-03: Per-runtime omit/translate contract ───`: add direct unit assertions on `translateEffortForCodex`: `strictEqual(translateEffortForCodex('max'), 'xhigh')`, pass-through for `'low'|'medium'|'high'`, and `null`/`undefined` → `null`. Add a derived-list omit assertion: for `nonEffortRuntimes = [...KNOWN_RUNTIMES].filter(r => !RUNTIMES_WITH_REASONING_EFFORT.has(r))`, assert each resolves effort null for a sample agent — complementing (not duplicating) feat-57's codex-TOML coverage. Keep the resolver golden (TEST-01) storing `max` and this boundary test asserting `max→xhigh` separately (per plan 01 D-A2 / research Open Question 2).

    Do not edit any `get-shit-done/bin/lib/*.cjs`. If the golden fixture is missing or empty, fail loudly — it is a hard dependency from plan 01.
  </action>
  <verify>
    <automated>node --test tests/feat-58-regression.test.cjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/feat-58-regression.test.cjs` exits 0
    - The file reads `golden-effort-snapshot.json` (grep confirms the filename string is present) and asserts via `assert.strictEqual` against `row.expectedEffort` — it does NOT call `_resolveAgentSlot`/`parseModelEffort` to derive expected values (grep confirms no expected-from-resolver computation in the TEST-01 block)
    - A `strictEqual(translateEffortForCodex('max'), 'xhigh')` assertion is present
    - The omit contract iterates `[...KNOWN_RUNTIMES].filter(...)` (catalog-derived, not a hard-coded 13-element array)
  </acceptance_criteria>
  <done>The static golden asserts live resolution against committed literals across the full matrix; the omit/translate contract holds per runtime with the max→xhigh boundary tested separately from the resolver golden.</done>
</task>

<task type="auto">
  <name>Task 2: Add the antipattern guard (TEST-04) + mutation RED-before-fix verification</name>
  <files>tests/feat-58-regression.test.cjs</files>
  <read_first>
    - tests/feat-58-regression.test.cjs (the file from Task 1 — append the guard section here)
    - tests/feat-57-install-translation.test.cjs (lines ~189-191 — assert.ok(toml.includes('model_reasoning_effort = "xhigh"')) — this SAFE full-key-value usage must NOT be flagged by the guard; use it as the negative test case)
    - .planning/phases/58-regression-coverage/58-RESEARCH.md (§Q5 — antipattern guard form, the two regexes, and the scoping note that the guard targets bare effort-token matches not full structured lines)
    - .planning/phases/58-regression-coverage/58-VALIDATION.md (§Manual-Only Verifications — the mutation protocol; document each per-assertion in SUMMARY)
  </read_first>
  <action>
    Append a TEST-04 section to `tests/feat-58-regression.test.cjs` — banner `// ─── TEST-04: Antipattern guard ───`. Read every `tests/*.test.cjs` via `fs.readdirSync(__dirname).filter(f => f.endsWith('.test.cjs'))` and for each file source check two classes:
    1. `indexOf`-as-boolean on effort tokens: flag `assert.ok(...\.indexOf\s*\(\s*['"](?:low|medium|high|xhigh|max)['"]\s*\))` — `indexOf` result used as a truthy boolean (false-passes when token is anywhere but index 0, and even index 0 is falsy). Assert no matches.
    2. bare `includes('medium'|'high')` substring collision: flag `assert\.ok\([^)]*\.includes\s*\(\s*['"](?:medium|high)['"]\s*\)` — collides on `'xhigh'` containing `'high'` and on `'medium'` prefixes. Assert no matches. Scope the regex to BARE single-token matches so feat-57's full structured `includes('model_reasoning_effort = "xhigh"')` is NOT flagged (D-G1 — verify by asserting the guard reports zero violations on the current suite, which contains that safe line).

    Run the guard against the live suite and confirm it reports zero violations (the current suite is clean by construction). Then, for the mutation RED-before-fix evidence (D-04), per the research §Q5 mutation table: temporarily invert each new assertion class one at a time — (a) flip a `medium` literal to `null` in one golden fixture row; (b) make `translateEffortForCodex` pass-through; (c) add a runtime to `RUNTIMES_WITH_REASONING_EFFORT`; (d) loosen one guard regex — confirm the corresponding test goes RED, then revert. Record each mutation, the RED observation, and the revert per-assertion in the SUMMARY's Manual Verification section. None of these mutations are committed.
  </action>
  <verify>
    <automated>node --test tests/feat-58-regression.test.cjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The TEST-04 guard tests pass (zero violations) against the current suite including feat-57's safe `includes('model_reasoning_effort = "xhigh"')` line
    - The guard reads `tests/*.test.cjs` via `readdirSync` (grep confirms) and scans for both the `indexOf`-as-boolean and bare `includes('medium'|'high')` patterns
    - `node --test tests/feat-58-regression.test.cjs` exits 0
    - SUMMARY will document the per-assertion mutation RED-before-fix evidence (recorded at execution time, mutations reverted)
  </acceptance_criteria>
  <done>A committed guard fails on the two false-pass classes without flagging safe structured-string assertions; each new assertion is shown to bite via reverted mutation, documented in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + coverage gate (TEST-05)</name>
  <files>tests/feat-58-regression.test.cjs</files>
  <read_first>
    - .planning/phases/58-regression-coverage/58-VALIDATION.md (§Sampling Rate — the full-suite + coverage commands and the ≥70% threshold)
    - package.json (confirm `test` and `test:coverage` scripts; coverage measured against get-shit-done/bin/lib/*.cjs at ≥70% lines)
    - CLAUDE.md (test discipline: run once, pipe to /tmp, read the file — do not re-run to re-read)
  </read_first>
  <action>
    Run the full suite once piping to a temp file: `npm test 2>&1 | tee /tmp/gsd-58-full.txt`, then read the file and confirm zero new failures versus the pre-milestone baseline (read the `Tests N passed / N failed` summary line, not the tee exit code). Then run `npm run test:coverage 2>&1 | tee /tmp/gsd-58-cov.txt` and read it to confirm ≥70% line coverage on `get-shit-done/bin/lib/*.cjs` is maintained (the new feat-58 file exercises additional core.cjs paths, so coverage should hold or rise). If any new failure appears, root-cause and fix within the test files only (no resolver/source edits — the milestone behavior is frozen). Record both summary lines in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-58-full.txt | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/gsd-58-full.txt` summary line shows zero new failures versus the pre-milestone baseline
    - `npm run test:coverage` reports ≥70% line coverage on `get-shit-done/bin/lib/*.cjs`
    - No edits were made to any `get-shit-done/bin/lib/*.cjs` or `bin/install.js` during this plan (test-only)
  </acceptance_criteria>
  <done>Full `npm test` green with zero new regressions and ≥70% line coverage maintained; both summary lines recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
No threats — test-only phase: this plan adds one test file that reads committed fixtures and lints test sources. No runtime behavior change, no input surfaces, no secrets, no env vars, no network. No edits to `get-shit-done/bin/lib/*.cjs` or `bin/install.js`.
</threat_model>

<verification>
- `node --test tests/feat-58-regression.test.cjs` exits 0 (golden + omit/translate + antipattern guard all green).
- Guard reports zero violations against the current suite including feat-57's safe structured-string assertion.
- `npm test` summary line shows zero new failures; `npm run test:coverage` shows ≥70% on get-shit-done/bin/lib/*.cjs.
- SUMMARY documents per-assertion mutation RED-before-fix evidence (mutations reverted).
</verification>

<success_criteria>
- Static golden asserts live resolution against committed literals across the matrix (TEST-01).
- Per-runtime omit + max→xhigh translate contract holds (TEST-03).
- Committed antipattern guard catches indexOf-as-boolean and medium/high substring collisions without flagging safe structured strings; each assertion mutation-verified RED (TEST-04).
- Full suite green, ≥70% coverage maintained (TEST-05).
</success_criteria>

<output>
Create `.planning/phases/58-regression-coverage/58-03-SUMMARY.md` when done.
</output>
