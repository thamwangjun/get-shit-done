---
phase: 62-rubric-inlining-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/debug-session-management.test.cjs
autonomous: true
requirements:
  - RIC-01
must_haves:
  truths:
    - "tests/debug-session-management.test.cjs contains a describe block named 'phase-62: rubric inlining coverage'"
    - "The describe block contains three separate assert.ok() calls targeting gsd-user-profiler.md"
    - "Assertion D-02 checks content.includes('<step name=\"load_rubric\">')"
    - "Assertion D-03 checks content.includes('user-profiling.md')"
    - "Assertion D-04 checks content.includes('included above in the `<reference>` block')"
    - "npm test passes with 0 new failures after the append"
  artifacts:
    - path: "tests/debug-session-management.test.cjs"
      provides: "phase-62 rubric inlining coverage describe block"
      contains: "phase-62: rubric inlining coverage"
  key_links:
    - from: "tests/debug-session-management.test.cjs"
      to: "agents/gsd-user-profiler.md"
      via: "fs.readFileSync(path.join(process.cwd(), 'agents', 'gsd-user-profiler.md'), 'utf8')"
      pattern: "gsd-user-profiler\\.md"
---

<objective>
Append a new describe block to tests/debug-session-management.test.cjs that asserts gsd-user-profiler.md references the Eta-inlined rubric via a `<reference>` block rather than a bare file read instruction. Three separate assert.ok() calls give unambiguous failure attribution for each of the three observable tokens.

Purpose: Close RIC-01 — the only missing coverage gap for Phase 62. Guards against a future regression where the load_rubric step is reverted to a bare file-read instruction.
Output: One new describe block (one test with three assertions) appended to tests/debug-session-management.test.cjs.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.

Consult `$HOME/.claude/get-shit-done/references/git-integration.md` before creating any commit or branch during plan execution: it defines the atomic-commit sequence, commit-message conventions, and branch/worktree handling rules this workflow must follow. Read it at execution start so every commit conforms; skip it only for read-only steps that create no commits.

@$HOME/.claude/get-shit-done/references/git-integration.md
</required_reading>
</execution_context>

<context>
!`cat .planning/PROJECT.md`
!`cat .planning/STATE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append phase-62 rubric inlining coverage describe block</name>
  <files>tests/debug-session-management.test.cjs</files>

  <read_first>
  - tests/debug-session-management.test.cjs — read the full file to locate the final closing brace of the last describe block (line 194 `});`) so the new block is appended immediately after; also confirm that `fs`, `path`, `describe`, `test`, and `assert` are already imported (lines 8–11) — no new requires needed.
  - agents/gsd-user-profiler.md — read lines 40–60 to confirm the three assertion tokens are present before writing assertions that depend on them: line 41 contains `user-profiling.md` inside the Eta include tag; line 54 contains `<step name="load_rubric">`; line 55 contains `included above in the \`<reference>\` block`.
  </read_first>

  <action>
  Append the following describe block to tests/debug-session-management.test.cjs immediately after the closing `});` of the existing `describe('debug skill dispatch and sub-orchestrator (#2148, #2151)', ...)` block (which ends at line 194 of the current file). Do not modify any existing content.

  The new block structure (per D-01, D-02, D-03, D-04):

  - Describe label: `'phase-62: rubric inlining coverage'`
  - One `test()` named `'gsd-user-profiler load_rubric step references Eta-inlined rubric'`
  - Inside the test: read gsd-user-profiler.md using `fs.readFileSync(path.join(process.cwd(), 'agents', 'gsd-user-profiler.md'), 'utf8')` (per D-01 — mirrors existing file-read pattern in the file)
  - Three separate `assert.ok()` calls in this exact order:
    1. (per D-02): `assert.ok(content.includes('<step name="load_rubric">'), 'gsd-user-profiler.md missing load_rubric step')` — confirms the load_rubric step exists (SC-1)
    2. (per D-03): `assert.ok(content.includes('user-profiling.md'), 'gsd-user-profiler.md missing rubric filename reference')` — confirms the specific rubric filename is referenced (SC-2)
    3. (per D-04): `assert.ok(content.includes('included above in the \`<reference>\` block'), 'gsd-user-profiler.md load_rubric step missing inlining phrase')` — confirms the step body uses the inlining phrase, distinguishing it from a bare-read regression (RIC-01)

  Use single-quote string delimiters for the describe and test labels (consistent with all existing code in the file). The backtick in the D-04 assertion string is inside a single-quoted JS string so no escaping is required for JS, but the backtick itself must appear literally in the included string so that `content.includes(...)` matches the literal backtick in the source file.
  </action>

  <acceptance_criteria>
  - tests/debug-session-management.test.cjs contains the string `phase-62: rubric inlining coverage`
  - tests/debug-session-management.test.cjs contains `content.includes('<step name="load_rubric">')`
  - tests/debug-session-management.test.cjs contains `content.includes('user-profiling.md')`
  - tests/debug-session-management.test.cjs contains `included above in the` (confirming the D-04 inlining phrase assertion is present)
  - tests/debug-session-management.test.cjs contains `gsd-user-profiler.md` in the new readFileSync call
  - `node --test tests/debug-session-management.test.cjs` exits 0 with the new test passing (not skipped)
  - `npm test 2>&1 | tee /tmp/gsd-62-test-output.txt` exits 0 with 0 new failures
  </acceptance_criteria>

  <verify>
    <automated>node --test tests/debug-session-management.test.cjs 2>&1 | tail -20</automated>
  </verify>

  <done>
  `node --test tests/debug-session-management.test.cjs` reports the new test passing. `npm test 2>&1 | tee /tmp/gsd-62-test-output.txt` shows 0 new failures versus the pre-phase baseline (8,243 pass). The three assert.ok() calls are individually identifiable in the failure output if any single token is missing from gsd-user-profiler.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → agent source | Test reads agent file from disk via fs.readFileSync; no network or external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | tests/debug-session-management.test.cjs | accept | File is version-controlled; test-only change with no production surface |
| T-62-SC | Tampering | npm package installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
node --test tests/debug-session-management.test.cjs 2>&1 | grep -E '(pass|fail|skip|phase-62)'
npm test 2>&1 | tee /tmp/gsd-62-test-output.txt | tail -5
</verification>

<success_criteria>
- tests/debug-session-management.test.cjs contains the three assert.ok() calls for D-02, D-03, D-04
- `node --test tests/debug-session-management.test.cjs` exits 0 with the new test passing (not skipped)
- `npm test` shows 0 new failures versus the pre-phase baseline
- RIC-01 is satisfied: the test guards the load_rubric step's inlining phrase against regression
</success_criteria>

<output>
Create `.planning/phases/62-rubric-inlining-coverage/62-01-SUMMARY.md` when done.
</output>
