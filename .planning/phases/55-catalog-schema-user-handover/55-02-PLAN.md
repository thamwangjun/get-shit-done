---
phase: 55-catalog-schema-user-handover
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - .planning/phases/55-catalog-schema-user-handover/check-completeness.js
autonomous: true
requirements: [CATALOG-01]
must_haves:
  truths:
    - "check-completeness.js exists and runs without error from the project root"
    - "The script enumerates all 33 agents via Object.keys(catalog.agents) — no hardcoded agent list"
    - "D-04: The script writes a temp config.json with {runtime:'claude', model_profile:'balanced'} before calling resolveReasoningEffortInternal, so the {claude,codex} allowlist gate is satisfied (Pitfall 2)"
    - "On the current bare catalog the script reports all 33 agents as missing effort (PASS only after CATALOG-02 assignment) — confirming the check is wired to the live Phase 53 resolver"
    - "The script exits non-zero and lists missing agents when any agent resolves to null effort; exits 0 with a PASS line when all 33 resolve"
  artifacts:
    - path: ".planning/phases/55-catalog-schema-user-handover/check-completeness.js"
      provides: "Post-handover completeness check reusing the Phase 53 resolveReasoningEffortInternal"
      contains: "resolveReasoningEffortInternal"
  key_links:
    - from: "check-completeness.js"
      to: "get-shit-done/bin/lib/core.cjs resolveReasoningEffortInternal"
      via: "require + per-agent call in a temp claude-runtime dir"
      pattern: "resolveReasoningEffortInternal"
    - from: "check-completeness.js"
      to: "sdk/shared/model-catalog.json agents"
      via: "Object.keys(catalog.agents)"
      pattern: "Object\\.keys\\(catalog\\.agents\\)"
---

<objective>
Write the post-handover completeness-check script (D-04) that the user runs after assigning per-agent `model;effort` values (CATALOG-02). The script reads the filled catalog and asserts that, for every one of the 33 agents, the Phase 53 resolver returns a non-null effort.

Purpose: CATALOG-02 success criterion 4 requires confirmation that all 33 agents carry an assigned effort that flows through the resolver. This script is the auditable, no-new-test-file mechanism for that confirmation — it reuses the existing `resolveReasoningEffortInternal` rather than duplicating resolution logic.

Output: A single Node.js script at the phase directory that exits 0 when all 33 agents resolve effort, non-zero (listing the gaps) otherwise. On the current bare catalog it correctly reports all 33 as missing — proving it is wired to live resolution before CATALOG-02 lands.
</objective>

<context>
!`cat .planning/phases/55-catalog-schema-user-handover/55-CONTEXT.md`
@.planning/phases/55-catalog-schema-user-handover/55-PATTERNS.md
@.planning/phases/55-catalog-schema-user-handover/55-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write check-completeness.js using the Phase 53 resolver</name>
  <files>.planning/phases/55-catalog-schema-user-handover/check-completeness.js</files>
  <read_first>
    - .planning/phases/55-catalog-schema-user-handover/55-PATTERNS.md (the verified completeness-check script pattern under "Completeness check script (post-handover, D-04)" — exact structure including the temp-dir config write)
    - get-shit-done/bin/lib/core.cjs (resolveReasoningEffortInternal signature `(cwd, agentType)` at line 1596, exported at line 2014; the outermost runtime-allowlist gate that returns null without runtime in {claude,codex})
    - sdk/shared/model-catalog.json (agents key — source of the 33 agent names)
  </read_first>
  <action>
    Create `.planning/phases/55-catalog-schema-user-handover/check-completeness.js` as a CommonJS Node script. It MUST: (1) resolve `projectRoot = process.cwd()` and `require(path.join(projectRoot, 'get-shit-done/bin/lib/core.cjs'))` to get `resolveReasoningEffortInternal`; (2) read and JSON.parse `sdk/shared/model-catalog.json` from the project root; (3) per D-04 and Pitfall 2, create a temp dir via `fs.mkdtempSync` and write a minimal `config.json` containing `{ "runtime": "claude", "model_profile": "balanced" }` into it — this is mandatory because the effort resolver's outermost gate returns null for any runtime outside the {claude,codex} allowlist, so a temp dir with no config would report 33 false failures; (4) enumerate agents via `Object.keys(catalog.agents)` (never a hardcoded list — the catalog is the single source of truth); (5) compute `missing = agents.filter(a => resolveReasoningEffortInternal(tmpDir, a) === null)`; (6) clean up the temp dir with `fs.rmSync(tmpDir, {recursive: true})`; (7) if `missing.length > 0`, print `FAIL: <n> agents missing effort assignment:` followed by each missing agent name and `process.exit(1)`; otherwise print `PASS: all <count> agents have assigned effort values.` and exit 0. Use the exact verified pattern from 55-PATTERNS.md. The run command (documented in a header comment) is `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` from the project root.
  </action>
  <verify>
    <automated>node .planning/phases/55-catalog-schema-user-handover/check-completeness.js; test $? -eq 1 && echo EXPECTED_FAIL_ON_BARE_CATALOG</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/55-catalog-schema-user-handover/check-completeness.js` exists
    - The file contains `resolveReasoningEffortInternal`, `Object.keys(catalog.agents)`, and `"runtime": "claude"` (or equivalent `runtime: 'claude'` in the written config object)
    - The file contains `fs.mkdtempSync` and `fs.rmSync`
    - On the current bare catalog, running the script exits 1 and prints `FAIL:` listing 33 agents (proves it is wired to the live resolver and the allowlist gate is satisfied — every bare slot resolves effort:null)
    - The script does NOT contain a hardcoded agent-name array literal (agents come only from the catalog)
  </acceptance_criteria>
  <done>Script exists, enumerates all 33 agents from the catalog, satisfies the runtime allowlist via a temp config, and correctly reports all-missing on the bare catalog (the post-CATALOG-02 PASS path is exercised in Plan 03 after handover).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog JSON → check script | Script reads project-owned catalog and calls the existing resolver; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Tampering | temp config.json written by the check | accept | Written to an OS temp dir created with mkdtempSync and removed after the check; contents are a fixed `{runtime:'claude', model_profile:'balanced'}` literal, no user input. No elevated-privilege operation, no network. |

This plan writes a single read-only diagnostic script. No authentication, no persistent state mutation outside an ephemeral temp dir, no network. Security risk: low.
</threat_model>

<verification>
- `node .planning/phases/55-catalog-schema-user-handover/check-completeness.js` exits 1 with a `FAIL:` listing on the bare catalog (CATALOG-01 infrastructure — the check is live-wired)
- Script enumerates agents from the catalog, not a hardcoded list
</verification>

<success_criteria>
- check-completeness.js reuses resolveReasoningEffortInternal (no duplicated resolution logic)
- Temp claude-runtime config satisfies the allowlist gate (Pitfall 2 avoided)
- All 33 agents enumerated from the catalog
- Correct FAIL-on-bare / PASS-when-complete behavior
</success_criteria>

<output>
Create `.planning/phases/55-catalog-schema-user-handover/55-02-SUMMARY.md` when done
</output>
