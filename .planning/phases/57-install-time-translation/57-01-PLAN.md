---
phase: 57-install-time-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/feat-57-install-translation.test.cjs]
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "A new test file tests/feat-57-install-translation.test.cjs exists and runs under node --test"
    - "The file contains failing (RED) assertions for translateEffortForCodex, the resolver haiku exclusion, and Codex TOML emit behavior — confirming the tests detect absent behavior"
    - "D-01: Every assertion uses exact string equality (assert.strictEqual/deepStrictEqual) — never indexOf or substring matching on effort tokens"
    - "D-02: The resolver haiku-exclusion test covers the explicit-override path (model_overrides.x = 'haiku;high' resolves to null), not only the bare-tier path"
  artifacts:
    - path: "tests/feat-57-install-translation.test.cjs"
      provides: "RED test stubs for INSTALL-01 and INSTALL-02"
      contains: "translateEffortForCodex"
  key_links:
    - from: "tests/feat-57-install-translation.test.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require core.cjs — resolveReasoningEffortInternal, _resetEffortWarningCacheForTests"
      pattern: "resolveReasoningEffortInternal"
    - from: "tests/feat-57-install-translation.test.cjs"
      to: "bin/install.js"
      via: "require install.js — generateCodexAgentToml, readGsdRuntimeProfileResolver (GSD_TEST_MODE exports)"
      pattern: "generateCodexAgentToml"
---

<objective>
Create the Wave 0 RED test file `tests/feat-57-install-translation.test.cjs` covering all INSTALL-01 / INSTALL-02 behaviors before any source change. The tests MUST fail against the current (unmodified) source — confirming they detect the absence of `translateEffortForCodex`, the haiku exclusion, and the redirected Codex emit path.

Purpose: Establishes the Nyquist feedback loop (per 57-VALIDATION.md) — every downstream task verifies against this file in <5s. RED-first prevents indexOf-as-boolean false-passes (Pitfall 4 / TEST-04).
Output: One new test file with unit + integration assertions, all currently failing for the right reason.
</objective>

<context>
!`cat .planning/phases/57-install-time-translation/57-VALIDATION.md`
@.planning/phases/57-install-time-translation/57-RESEARCH.md
@.planning/phases/57-install-time-translation/57-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit-test stubs for translateEffortForCodex and the resolver haiku exclusion</name>
  <files>tests/feat-57-install-translation.test.cjs</files>
  <read_first>
    - tests/feat-53-unified-effort-resolver.test.cjs (Analog A: GSD_TEST_MODE header, createTempDir, writeConfig planting .planning/config.json, _resetEffortWarningCacheForTests in beforeEach, afterEach cleanup, assert.strictEqual style)
    - tests/parse-model-effort.test.cjs (Analog C: loop-over-allowlist + exact-equality pure-fn parity shape)
    - tests/helpers.cjs (createTempDir signature)
    - get-shit-done/bin/lib/core.cjs lines 1610-1696 (resolveReasoningEffortInternal: override branch at 1626, bareTier at 1639, floor at 1695) and the module.exports tail near 2080
  </read_first>
  <behavior>
    - translateEffortForCodex('max') === 'xhigh'
    - translateEffortForCodex('low'|'medium'|'high') passes through unchanged (loop over tokens)
    - translateEffortForCodex(null) === null and translateEffortForCodex(undefined) === null
    - typeof core.translateEffortForCodex === 'function' (export check)
    - resolver Claude-form-neutral: resolveReasoningEffortInternal never returns 'xhigh' for any opus/sonnet slot assigned 'max' on claude or codex — it returns 'max' verbatim
    - haiku → null (bare): a bare haiku-tier slot resolves to null on BOTH claude and codex runtimes (no medium floor)
    - haiku → null (explicit override, A1 resolved): model_overrides set to 'haiku;high' resolves to null (haiku exclusion wins over the explicit ;effort suffix)
  </behavior>
  <action>
    Set process.env.GSD_TEST_MODE='1' at the top, then require node:test, node:assert/strict, path, fs, ./helpers.cjs (createTempDir), and ../get-shit-done/bin/lib/core.cjs (destructure resolveReasoningEffortInternal, translateEffortForCodex, _resetEffortWarningCacheForTests). Mirror the feat-53 local writeConfig(projectDir, config) helper that writes .planning/config.json. In beforeEach call _resetEffortWarningCacheForTests(); in afterEach clean the temp dir. Write the behaviors above as exact-equality assertions only (assert.strictEqual / assert.deepStrictEqual) per D-01 — no indexOf or substring on 'medium'/'high'. For the resolver tests, plant a codex (and a claude) config via writeConfig with a model_overrides or models entry that produces the target tier, asserting per D-02 that 'haiku;high' as an explicit override still yields null. These assertions reference translateEffortForCodex and the haiku exclusion which do NOT yet exist — the file MUST fail RED.
  </action>
  <verify>
    <automated>node --test tests/feat-57-install-translation.test.cjs 2>&1 | tee /tmp/gsd-57-01-unit.txt; command grep -E "fail [1-9]|tests [0-9]" /tmp/gsd-57-01-unit.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/feat-57-install-translation.test.cjs` runs (file parses) and reports failures (RED), NOT a parse/require crash with zero tests collected
    - The failures are attributable to missing `translateEffortForCodex` export and the missing haiku exclusion (assertion failures / undefined-not-a-function), confirmed by reading /tmp/gsd-57-01-unit.txt
    - `command grep -c "assert.strictEqual\|assert.deepStrictEqual" tests/feat-57-install-translation.test.cjs` returns >= 6
    - `command grep -c "indexOf\|includes(" tests/feat-57-install-translation.test.cjs` applied to effort-token assertions returns 0 (no substring matching on effort)
    - The string `haiku;high` appears in the file (A1 explicit-override coverage, D-02)
  </acceptance_criteria>
  <done>The unit portion of the test file exists, parses, references translateEffortForCodex + haiku exclusion + the A1 explicit-override case, and fails RED for the right reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Codex TOML emit integration-test stubs</name>
  <files>tests/feat-57-install-translation.test.cjs</files>
  <read_first>
    - tests/codex-config.test.cjs (Analog B: GSD_TEST_MODE, optional hooks/dist before() hook, import generateCodexAgentToml + readGsdRuntimeProfileResolver from ../bin/install.js, assert on model_reasoning_effort line presence/absence in returned TOML string)
    - tests/bug-3427-3433-codex-install-shape.test.cjs (Codex install shape assertions)
    - bin/install.js lines 2719-2761 (generateCodexAgentToml + emit seam 2748-2749) and 11402/11412 (GSD_TEST_MODE exports)
  </read_first>
  <behavior>
    - Codex install emits `model_reasoning_effort = "xhigh"` for an opus-tier agent whose slot is assigned 'max'
    - Codex install emits `model_reasoning_effort = "medium"` for a bare opus/sonnet slot (D-08 floor, translated)
    - Codex install emits NO `model_reasoning_effort` line for a haiku-tier agent (D-03)
    - A non-{claude,codex} install path emits no effort; the Claude install TOML path is unchanged (D-04 — Codex-emit-only)
  </behavior>
  <action>
    Add a describe block to the same test file for Codex TOML emit. Import generateCodexAgentToml and readGsdRuntimeProfileResolver from ../bin/install.js (GSD_TEST_MODE exports). Build a temp targetDir with a .planning/config.json (runtime 'codex') via the writeConfig helper from Task 1, construct the runtimeResolver with readGsdRuntimeProfileResolver(targetDir), call generateCodexAgentToml(name, content, modelOverrides, runtimeResolver), and assert exact presence (`model_reasoning_effort = "xhigh"`, `"medium"`) or absence of the `model_reasoning_effort` line in the returned TOML string using exact substring presence checks on the full line — NOT token substring matching. For the haiku case assert the line is absent. These will fail RED because today the emit sources catalog `medium` for haiku and does not route through translateEffortForCodex. Mirror codex-config.test.cjs for any hooks/dist build prerequisite only if full install is exercised — prefer calling generateCodexAgentToml directly to avoid a full install.
  </action>
  <verify>
    <automated>node --test tests/feat-57-install-translation.test.cjs 2>&1 | tee /tmp/gsd-57-01-int.txt; command grep -E "fail [1-9]" /tmp/gsd-57-01-int.txt</automated>
  </verify>
  <acceptance_criteria>
    - The integration assertions run and fail RED (haiku currently emits `medium`; opus `max` is not translated to `xhigh`), confirmed in /tmp/gsd-57-01-int.txt
    - `command grep -c "model_reasoning_effort" tests/feat-57-install-translation.test.cjs` returns >= 3
    - The file asserts at least one ABSENT case (haiku emits no `model_reasoning_effort` line)
    - The exact strings `"xhigh"` and `"medium"` appear in emit assertions
    - No new fixtures created — only createTempDir + writeConfig used
  </acceptance_criteria>
  <done>The integration portion exists in the same test file, exercises generateCodexAgentToml with a codex temp config, and fails RED for opus-max-not-xhigh and haiku-still-medium.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only file; reads/writes temp dirs via createTempDir. No external input, network, auth, or persisted state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | npm/test deps | accept | No new packages installed; uses built-in node --test runner and existing tests/helpers.cjs. No supply-chain surface introduced. |

Honest assessment: this is a test-stub plan with no external input, no auth, no network, no new data surface. No mitigations beyond the existing test harness are warranted.
</threat_model>

<verification>
- `node --test tests/feat-57-install-translation.test.cjs` parses and runs (no require crash), reporting RED failures.
- Failures are attributable to missing source behavior (translateEffortForCodex export, haiku exclusion, redirected emit), not test bugs.
</verification>

<success_criteria>
- New file tests/feat-57-install-translation.test.cjs exists with unit + integration assertions for INSTALL-01 and INSTALL-02.
- All new assertions fail RED against unmodified source for the correct reasons.
- All assertions use exact equality (D-01); explicit-override haiku case present (D-02 / A1).
</success_criteria>

<output>
Create `.planning/phases/57-install-time-translation/57-01-SUMMARY.md` when done.
</output>
