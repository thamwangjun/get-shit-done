---
phase: 57-install-time-translation
plan: 03
type: execute
wave: 3
depends_on: ["57-02"]
files_modified: [bin/install.js]
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "D-01: install.js sources per-agent canonical effort from the core resolver (resolveReasoningEffortInternal), never re-deriving it from the per-tier catalog slot"
    - "D-02: install.js routes the resolved Claude-form effort through translateEffortForCodex (max→xhigh) only at the Codex TOML emit seam"
    - "D-04: Changes are confined to the Codex emit path (generateCodexAgentToml); the Codex-native model field stays sourced from runtimeResolver.resolve(); the Claude install path is untouched"
    - "Codex install emits model_reasoning_effort='xhigh' for an opus slot assigned max, 'medium' for a bare opus/sonnet slot, and NO model_reasoning_effort line for a haiku-tier agent"
    - "All integration assertions in tests/feat-57-install-translation.test.cjs pass GREEN and the full npm test suite stays green with fork quality gates intact"
  artifacts:
    - path: "bin/install.js"
      provides: "Codex effort emit redirected through floored core resolver + translateEffortForCodex; resolveEffort sibling on the runtime resolver"
      contains: "translateEffortForCodex"
  key_links:
    - from: "bin/install.js generateCodexAgentToml (emit seam ~2748-2749)"
      to: "core.cjs resolveReasoningEffortInternal + translateEffortForCodex"
      via: "runtimeResolver.resolveEffort(agentName) → gsdTranslateEffortForCodex(...)"
      pattern: "resolveEffort"
    - from: "bin/install.js readGsdRuntimeProfileResolver (~1498-1511)"
      to: "core.cjs resolveReasoningEffortInternal"
      via: "resolveEffort sibling closing over the probed project dir"
      pattern: "gsdResolveReasoningEffort"
---

<objective>
Redirect the Codex TOML effort source at the single emit seam: source `reasoning_effort` from the floored core resolver (Plan 02) and translate it via `translateEffortForCodex`, while the Codex-native `model` field stays sourced from `runtimeResolver.resolve()`. The Claude install path does nothing (D-04).

Purpose: Completes INSTALL-01 (translate only at the Codex boundary; resolver stays format-neutral) and INSTALL-02 (effort materializes correctly per runtime — Claude preserved, Codex translated, haiku omits, unsupported runtimes omit). This is the seam Phase 57 exists to fix: today the emit uses the per-tier catalog value (which wrongly emits haiku=medium and never translates max).
Output: Import additions, a `resolveEffort` sibling on the install resolver, and the redirected emit conditional. Integration tests from Plan 01 go GREEN.
</objective>

<context>
@.planning/phases/57-install-time-translation/57-RESEARCH.md
@.planning/phases/57-install-time-translation/57-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Import core helpers and add a resolveEffort sibling on the install resolver</name>
  <files>bin/install.js</files>
  <read_first>
    - bin/install.js lines 159-162 (import destructure: RUNTIME_PROFILE_MAP, resolveTierEntry with gsd* rename convention)
    - bin/install.js lines 1449-1512 (readGsdRuntimeProfileResolver: project-dir probe walk-up 1460-1472, returned resolver object 1498-1511 with resolve(agentName))
    - get-shit-done/bin/lib/core.cjs (exports of resolveReasoningEffortInternal + translateEffortForCodex added in Plan 02)
  </read_first>
  <behavior>
    - install.js imports resolveReasoningEffortInternal and translateEffortForCodex from core.cjs under the gsd* rename convention
    - readGsdRuntimeProfileResolver returns an object that, in addition to resolve(agentName), exposes resolveEffort(agentName) → Claude-form effort | null
    - resolveEffort closes over the SAME probed project dir used by the existing walk-up (Pitfall 3: a wrong/empty cwd makes the resolver a silent null no-op)
  </behavior>
  <action>
    Per D-01/A2: extend the import block (install.js ~159-162) to also destructure `resolveReasoningEffortInternal: gsdResolveReasoningEffort` and `translateEffortForCodex: gsdTranslateEffortForCodex` using the existing gsd* rename convention. In readGsdRuntimeProfileResolver, capture the probed project dir from the existing walk-up loop (install.js ~1460-1472) into a stable variable, then add a sibling method `resolveEffort(agentName)` to the returned resolver object (~1498-1511) that returns `gsdResolveReasoningEffort(probeDir, agentName)`. Mirror the existing resolve(agentName) shape; thread the canonical agent key the same way resolve does (e.g. the gsd-<agentName> form the SDK uses) so the resolver's allowlist/precedence reads the same config. Avoid Pitfall 3 by reusing the existing probe rather than a new walk-up. No call-site signature change (the resolver object is folded, install.js ~5018-5019 stays as-is).
  </action>
  <verify>
    <automated>node -e "process.env.GSD_TEST_MODE='1'; const i=require('./bin/install.js'); const r=i.readGsdRuntimeProfileResolver(process.cwd()); console.log(typeof r.resolveEffort)" 2>&1 | tee /tmp/gsd-57-03-t1.txt; command grep -q function /tmp/gsd-57-03-t1.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c "gsdResolveReasoningEffort\|gsdTranslateEffortForCodex" bin/install.js` returns >= 2 (both imports present)
    - `command grep -c "resolveEffort" bin/install.js` returns >= 2 (definition + emit-site use comes in Task 2)
    - The resolver object returned by readGsdRuntimeProfileResolver exposes resolveEffort as a function (node probe prints `function`)
    - resolveEffort reuses the existing probed project dir — no second walk-up loop added (`command grep -c "find.*\.planning/config" bin/install.js` is unchanged from baseline)
    - bin/install.js still parses: `node -e "process.env.GSD_TEST_MODE='1';require('./bin/install.js')"` exits 0
  </acceptance_criteria>
  <done>install.js imports the two core helpers and the install resolver exposes a working resolveEffort(agentName) bound to the probed project dir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redirect the Codex emit seam to the floored resolver + translateEffortForCodex</name>
  <files>bin/install.js</files>
  <read_first>
    - bin/install.js lines 2719-2761 (generateCodexAgentToml; emit seam 2745-2751 where model AND reasoning_effort both come from the per-tier entry)
    - tests/feat-57-install-translation.test.cjs (the RED integration assertions: opus max→xhigh, bare opus/sonnet→medium, haiku→absent, non-{claude,codex}→absent)
    - bin/install.js around line 5018-5019 (call site — confirm no signature change needed)
  </read_first>
  <behavior>
    - Codex emit: model continues to come from entry.model (runtimeResolver.resolve()) — Pitfall 2: do not drop the Codex-native model
    - Codex emit: reasoning_effort = translateEffortForCodex(runtimeResolver.resolveEffort(agentName)); emitted only when truthy
    - opus slot assigned 'max' → `model_reasoning_effort = "xhigh"`; bare opus/sonnet → `"medium"`; haiku-tier → no line (resolver already returns null)
    - non-{claude,codex} install → resolver returns null → no line; Claude install path untouched (D-04)
  </behavior>
  <action>
    At the emit seam (install.js ~2745-2751): keep `model = ${JSON.stringify(entry.model)}` sourced from runtimeResolver.resolve() unchanged (Pitfall 2 — two values, two sources). Replace the effort source: compute `const codexEffort = gsdTranslateEffortForCodex(runtimeResolver.resolveEffort(resolvedName) ?? runtimeResolver.resolveEffort(agentName))` and emit `model_reasoning_effort = ${JSON.stringify(codexEffort)}` only when codexEffort is truthy (per D-01 source from the floored resolver, D-02 translate at the boundary). Remove the old `entry.reasoning_effort` as the effort source so the catalog per-tier value is no longer used for effort (anti-pattern D-01 rejects). Guard against resolveEffort being absent on an older resolver shape only if needed, but the resolver always provides it now. This makes haiku emit nothing (resolver returns null), opus max emit xhigh, and bare slots emit medium.
  </action>
  <verify>
    <automated>node --test tests/feat-57-install-translation.test.cjs tests/codex-config.test.cjs 2>&1 | tee /tmp/gsd-57-03-t2.txt; command grep -E "fail [0-9]" /tmp/gsd-57-03-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - All integration assertions in tests/feat-57-install-translation.test.cjs pass GREEN (opus max→`"xhigh"`, bare opus/sonnet→`"medium"`, haiku→no `model_reasoning_effort` line, non-{claude,codex}→no line)
    - tests/codex-config.test.cjs and tests/bug-3427-3433-codex-install-shape.test.cjs still pass (0 failures) — no Codex TOML regression
    - `command grep -c "entry.reasoning_effort" bin/install.js` returns 0 (catalog per-tier value no longer the effort source)
    - The emit uses resolveEffort + gsdTranslateEffortForCodex: `command grep -c "gsdTranslateEffortForCodex(.*resolveEffort" bin/install.js` returns >= 1
    - `model = ` still emitted from entry.model (Codex-native model not dropped) — Pitfall 2 assertion in the test passes
  </acceptance_criteria>
  <done>The Codex emit seam sources effort from the floored resolver and translates via translateEffortForCodex; model is preserved; all Phase 57 integration tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + coverage gate</name>
  <files>bin/install.js</files>
  <read_first>
    - .planning/phases/57-install-time-translation/57-VALIDATION.md (Phase gate: npm run test:coverage green, ≥70% on bin/lib/*.cjs)
    - CLAUDE.md (test discipline: run once, pipe to /tmp, read the file; use command grep)
  </read_first>
  <action>
    Run the full suite once piped to a temp file per CLAUDE.md discipline. Confirm 0 NEW failures versus the pre-milestone baseline and that the fork quality gates stay green (agent-frontmatter, negative-framing, step-numbering, cross-file-refs — unaffected since this phase touches only .js/.cjs). Then run coverage and confirm ≥70% line coverage on get-shit-done/bin/lib/*.cjs. If any new failure appears, fix it within this plan before finalizing (no new test files needed; this is a gate task). Do not relax assertions to hide divergence.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-57-03-full.txt; command grep -E "tests [0-9]+|fail [0-9]+|pass [0-9]+" /tmp/gsd-57-03-full.txt | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` reports 0 new failures vs the pre-milestone baseline (read the summary line in /tmp/gsd-57-03-full.txt, not the exit code)
    - Fork quality gates green: agent-frontmatter, negative-framing, step-numbering, cross-file-refs subtests all pass
    - `npm run test:coverage` reports ≥70% line coverage on get-shit-done/bin/lib/*.cjs
    - tests/feat-57-install-translation.test.cjs is fully GREEN within the full run
  </acceptance_criteria>
  <done>Full suite green with no new failures, fork gates intact, coverage ≥70% on bin/lib/*.cjs — Phase 57 behavior locked in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent frontmatter name → Codex TOML | Agent names already flow into generateCodexAgentToml and are JSON.stringify-emitted into the TOML; this is a pre-existing boundary unchanged by Phase 57. |
| config.json → install-time resolver | resolveEffort reads .planning/config.json via the probed project dir; same existing input surface as the model resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Injection | model_reasoning_effort value in TOML | mitigate | The emitted effort is one of a fixed token set ({medium,high,xhigh,low}) or omitted (null), and is JSON.stringify-quoted into the TOML line — no free-form string reaches the emit. translateEffortForCodex only ever returns an allowlist token or null. |
| T-57-05 | Tampering | npm installs | accept | No new packages; pure in-repo .js edits. |
| T-57-06 | Information disclosure | install-time config read | accept | resolveEffort reads the same local .planning/config.json the model resolver already reads; no new file or network read introduced. |

Honest assessment: install-time effort-string translation with fixed-token, JSON-quoted output and no new external input or network. Agent-name-into-TOML injection already exists and is unchanged. Threat surface is minimal/low.
</threat_model>

<verification>
- `node --test tests/feat-57-install-translation.test.cjs` — all integration assertions GREEN.
- `node --test tests/codex-config.test.cjs tests/bug-3427-3433-codex-install-shape.test.cjs` — 0 failures (no Codex TOML regression).
- `npm test` — 0 new failures; fork quality gates green.
- `npm run test:coverage` — ≥70% on get-shit-done/bin/lib/*.cjs.
</verification>

<success_criteria>
- Codex emit sources effort from the floored core resolver and translates only at the boundary (INSTALL-01).
- Effort materializes correctly per runtime: Claude untouched, Codex translated (max→xhigh), haiku omits, unsupported runtimes omit (INSTALL-02).
- Codex-native model field preserved (Pitfall 2); catalog per-tier value no longer used for effort (D-01).
- Full suite + coverage gates green; fork quality gates intact.
</success_criteria>

<output>
Create `.planning/phases/57-install-time-translation/57-03-SUMMARY.md` when done.
</output>
