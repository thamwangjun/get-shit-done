---
phase: 57-install-time-translation
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [get-shit-done/bin/lib/core.cjs]
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "D-03: A slot resolving to the haiku tier returns null effort on every runtime ({claude,codex}) — bare haiku never floors to medium, never inherits the catalog haiku reasoning_effort"
    - "D-03 + A1: The haiku exclusion wins even over an explicit per-agent override (model_overrides.x = 'haiku;high' returns null)"
    - "D-02: A shared translateEffortForCodex(effort) helper exists in core.cjs, exported, translating max→xhigh with low/medium/high pass-through and null→null"
    - "D-01: resolveReasoningEffortInternal stays Claude-form-neutral — returns max verbatim, never xhigh; haiku omit and floor exclusion both live in the resolver"
    - "All unit assertions in tests/feat-57-install-translation.test.cjs pass GREEN"
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "haiku exclusion (both override and bareTier paths) + translateEffortForCodex helper + export"
      contains: "function translateEffortForCodex"
  key_links:
    - from: "get-shit-done/bin/lib/core.cjs (module.exports)"
      to: "consumers (install.js, SDK, tests)"
      via: "named export translateEffortForCodex alongside resolveTierEntry/parseModelEffort"
      pattern: "translateEffortForCodex,"
---

<objective>
Make `core.cjs` the single source of truth for the haiku exclusion and add the shared `translateEffortForCodex` helper, so by the time the Codex install boundary (Plan 03) reads a value, haiku is already `null` and only the `max`→`xhigh` translation remains at the boundary.

Purpose: Implements D-01 (single source of truth), D-02 (boundary translation as one unit-testable function), and D-03 + A1 (haiku omits effort on every runtime AND every path, including explicit override).
Output: Two edits in core.cjs — the haiku exclusion (override branch + bareTier branch) and the new exported helper. Unit tests from Plan 01 go GREEN.
</objective>

<context>
@.planning/phases/57-install-time-translation/57-RESEARCH.md
@.planning/phases/57-install-time-translation/57-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tier-based haiku exclusion to resolveReasoningEffortInternal (override path + bareTier path)</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs lines 1610-1696 (resolveReasoningEffortInternal: allowlist gate 1618, override branch 1624-1627, _resolveAgentSlotFromConfig 1633, inherit short-circuit 1636, bareTier 1639, step-5 fallback 1689-1690, D-08 floor 1692-1695)
    - get-shit-done/bin/lib/core.cjs lines 1242-1265 (parseModelEffort — used to extract .model from the override and tier)
    - tests/feat-57-install-translation.test.cjs (the RED unit assertions this task must satisfy)
  </read_first>
  <behavior>
    - A bare haiku-tier slot resolves to null on claude AND codex (no medium floor)
    - A haiku catalog slot carrying reasoning_effort='medium' still resolves to null (guard precedes step 5)
    - Per A1: model_overrides.<agent> = 'haiku;high' resolves to null — the haiku exclusion wins over the explicit ;effort override
    - Non-haiku behavior is unchanged: opus 'max' still returns 'max' verbatim; bare opus/sonnet still floors to 'medium'
  </behavior>
  <action>
    Make two insertions so haiku omits on every path. (1) Override path: in the per-agent override branch (core.cjs ~1624-1627), before returning parseModelEffort(override).effort, compute parseModelEffort(override).model and, per A1, if it === 'haiku' return null — so 'haiku;high' as an explicit override resolves to null instead of 'high'. (2) bareTier path: immediately after bareTier is computed (core.cjs ~1639), per D-03 insert `if (bareTier === 'haiku') return null;` BEFORE steps 3/3a/4/5 and the D-08 floor — because the catalog codex.haiku entry carries reasoning_effort='medium' and step 5 would otherwise emit it (Pitfall 1). Add a concise comment on each citing D-03 (haiku omits on every runtime, no medium floor) and D-08 (this overrides the floor for haiku). Do not alter the allowlist gate, the floor for non-haiku tiers, or the max-verbatim behavior. Keep CommonJS + existing comment-banner style.
  </action>
  <verify>
    <automated>node --test tests/feat-57-install-translation.test.cjs tests/feat-53-unified-effort-resolver.test.cjs 2>&1 | tee /tmp/gsd-57-02-t1.txt; command grep -E "fail [0-9]" /tmp/gsd-57-02-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - The haiku unit assertions in tests/feat-57-install-translation.test.cjs pass GREEN (bare haiku → null on claude and codex; 'haiku;high' override → null)
    - tests/feat-53-unified-effort-resolver.test.cjs still passes (0 failures) — no regression to non-haiku precedence/floor
    - `command grep -c "bareTier === 'haiku'" get-shit-done/bin/lib/core.cjs` returns 1 (the bareTier guard)
    - The override branch contains a haiku check: `command grep -c "=== 'haiku'" get-shit-done/bin/lib/core.cjs` returns >= 2 (override path + bareTier path)
    - opus 'max' still resolves to 'max' (never 'xhigh') — confirmed by the Claude-form-neutral assertion passing
  </acceptance_criteria>
  <done>resolveReasoningEffortInternal returns null for any haiku tier on every runtime and every path; non-haiku precedence and the D-08 floor are unchanged; haiku unit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and export translateEffortForCodex helper</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs lines 1240-1265 (parseModelEffort — JSDoc-first, single-function, null-passthrough, no-throw shape to mirror)
    - get-shit-done/bin/lib/core.cjs module.exports tail near 2073-2082 (resolveTierEntry, parseModelEffort, EFFORT_TOKENS listing)
    - tests/feat-57-install-translation.test.cjs (translateEffortForCodex unit assertions)
  </read_first>
  <behavior>
    - translateEffortForCodex('max') === 'xhigh'
    - translateEffortForCodex('low'|'medium'|'high') returns the input unchanged
    - translateEffortForCodex(null) === null; translateEffortForCodex(undefined) === null
    - Exported as a named member of module.exports
  </behavior>
  <action>
    Add a JSDoc-first single function translateEffortForCodex(effort) near the other effort helpers, mirroring parseModelEffort's shape: if effort == null return null; return effort === 'max' ? 'xhigh' : effort (per D-02 — only the max→xhigh clamp differs; low/medium/high pass through; resolver stays Claude-form-neutral). Because the resolver (Task 1) already returns null for haiku, this helper only ever sees opus/sonnet effort. Add `translateEffortForCodex,` to the single trailing module.exports block alongside resolveTierEntry / parseModelEffort. Do not throw; no behavior change to existing exports.
  </action>
  <verify>
    <automated>node --test tests/feat-57-install-translation.test.cjs 2>&1 | tee /tmp/gsd-57-02-t2.txt; command grep -E "fail [0-9]" /tmp/gsd-57-02-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - The translateEffortForCodex unit assertions pass GREEN (max→xhigh; low/medium/high pass-through; null/undefined → null; export is a function)
    - `command grep -c "function translateEffortForCodex" get-shit-done/bin/lib/core.cjs` returns 1
    - `command grep -c "translateEffortForCodex," get-shit-done/bin/lib/core.cjs` returns >= 1 (export entry)
    - `node -e "console.log(typeof require('./get-shit-done/bin/lib/core.cjs').translateEffortForCodex)"` prints `function`
    - The helper returns 'xhigh' only for 'max' input — no other token is altered (exact equality)
  </acceptance_criteria>
  <done>translateEffortForCodex exists, is exported, and all its unit assertions pass GREEN; helper never sees haiku (already null from the resolver).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → resolver | A project .planning/config.json supplies runtime + model overrides consumed by resolveReasoningEffortInternal. Already-existing trust boundary; this plan adds no new input parsing — it only adds a tier comparison and a pure string clamp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02 | Tampering | npm installs | accept | No new packages; pure in-repo .cjs edits. |
| T-57-03 | Injection | translateEffortForCodex output | accept | Output is one of a fixed token set ({low,medium,high,xhigh}) or null; it is JSON.stringify-emitted into TOML downstream (Plan 03), so no string-injection surface. Effort never derives from free-form user text — only from the parsed effort allowlist. |

Honest assessment: pure resolver + helper change, fixed-token output, no external input or network. Low/no real threat surface.
</threat_model>

<verification>
- `node --test tests/feat-57-install-translation.test.cjs` — all unit assertions GREEN.
- `node --test tests/feat-53-unified-effort-resolver.test.cjs` — 0 failures (no regression to existing precedence/floor).
</verification>

<success_criteria>
- core.cjs returns null for haiku on every runtime AND every path (override + bareTier) per D-03 + A1.
- translateEffortForCodex exists, exported, max→xhigh with pass-through/null behavior per D-02.
- Resolver stays Claude-form-neutral (max verbatim, never xhigh) per D-01.
- Unit portion of the Phase 57 test file is GREEN; feat-53 suite unregressed.
</success_criteria>

<output>
Create `.planning/phases/57-install-time-translation/57-02-SUMMARY.md` when done.
</output>
