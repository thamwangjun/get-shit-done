---
phase: 76-spec-07-citation-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/07-citation-guard/SPEC.md
  - .planning/spec/INDEX.md
autonomous: true
requirements: [SPEC-07, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md specifies the citation-cleanup guard tests/no-issue-citations.test.cjs as a single corpus scanner: three-form detection (inline #NNN, parenthetical (#NNN) via paren-context within one INLINE_RE pass, feat-form feat-NNNN 3+ digits via FEAT_FORM_RE — github-style owner/repo#NNN is a deliberate POSITIVE detected by its trailing #NNN), the TWO-TIER allowlist distinguished by SCOPE OF EXEMPTION (PLACEHOLDER_DIGITS exempt corpus-wide vs FILE_ALLOWLIST exempt per-file and flagged everywhere else), the two exclusion state machines (line-1-only YAML frontmatter per D-09; triple-backtick code fences per D-10), and the five-directory SCAN_DIRS scope (ROADMAP SC1 / SPEC-07, per D-01/D-02)"
    - "SPEC.md states five numbered EARS invariants 07-INV-1..07-INV-5 (citation detection; PLACEHOLDER_DIGITS global tier; FILE_ALLOWLIST per-file tier WITH the test-backing clause per D-03; exclusion state machines; five-directory SCAN_DIRS scope) with RFC 2119 strength, each citing tests/no-issue-citations.test.cjs as tier-1, an Acceptance Tests traceability table keyed on 07-INV-M mapping each MUST to a VERBATIM real describe/test name, NO unflagged [MISSING] rows (07-INV-3 and 07-INV-5 cite the corpus describe block 'corpus scan — no issue citations' / 'no citations in <file>' as the functional oracle), and the two allowlist tiers stated as SEPARATE invariants with distinct per-tier semantics (ROADMAP SC2 / QUAL-01,02,04, per D-01/D-02/D-03)"
    - "SPEC.md records the hex-color deliberate-false-positive tradeoff (D-04) as a SETTLED Key Decision PLUS an Out-of-scope Scope bullet (NOT an invariant), records the two-tier-allowlist refactor (D-05, ROADMAP SC3) and the FILE_ALLOWLIST test-backing requirement (D-03) as settled Key Decisions with consequence-of-reopening; marks every concrete enumeration (five SCAN_DIRS, PLACEHOLDER_DIGITS members {1,2,123}, the three FILE_ALLOWLIST file keys / four allowlisted digits and their four backing tests, INLINE_RE/FEAT_FORM_RE) as dated 'current as of 2026-06-12' advisory under ## Code Context with <!-- advisory --> (shape normative, values dated per D-06; QUAL-03, QUAL-05)"
    - "SPEC.md advances frontmatter Status Draft -> Ready with Confidence + Specced 2026-06-12 set, gated on QUAL-01..05, preserving the H1, the Depends on: SPEC-08 edge and the tier-1 evidence line tests/no-issue-citations.test.cjs; and the INDEX.md SPEC-07 Feature-Status row is flipped Draft -> Ready, preserving the SPEC-08 -> SPEC-07 dependency edge"
  artifacts:
    - path: ".planning/spec/07-citation-guard/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-07 (Purpose, Scope, five 07-INV-M invariants, Acceptance Tests, three Key Decisions, advisory Code Context) with Status: Ready"
      contains: "07-INV-1"
    - path: ".planning/spec/INDEX.md"
      provides: "SPEC-07 Feature-Status row advanced Draft -> Ready"
      contains: "SPEC-07"
  key_links:
    - from: ".planning/spec/07-citation-guard/SPEC.md (Acceptance Tests table)"
      to: "the tier-1 test file tests/no-issue-citations.test.cjs"
      via: "verbatim describe/test subtest names keyed on 07-INV-M"
      pattern: "tests/no-issue-citations\\.test\\.cjs"
    - from: ".planning/spec/07-citation-guard/SPEC.md (Invariants + section order)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme + status vocabulary)"
      via: "section order + invariant ID format conformance + Draft->Ready transition"
      pattern: "07-INV-[1-5]"
    - from: ".planning/spec/INDEX.md (SPEC-07 Feature-Status row)"
      to: ".planning/spec/07-citation-guard/SPEC.md (frontmatter Status)"
      via: "status reconciliation — both read Ready after this phase"
      pattern: "SPEC-07 .*Ready"
---

<objective>
Author the full body of `.planning/spec/07-citation-guard/SPEC.md` — a behavioral-contract
specification of the citation-cleanup corpus guard `tests/no-issue-citations.test.cjs` — so a
reimplementer can rebuild the guard on a refactored upstream without reading the current source.
The phase fills an existing stub (frontmatter + empty 7-section skeleton from Phase 68); it does
NOT modify `tests/no-issue-citations.test.cjs`, the corpus, the `PLACEHOLDER_DIGITS` / `FILE_ALLOWLIST`
contents, or the four sibling backing tests — it NARRATES the one passing tier-1 test file into the
locked 7-section contract and advances Status `Draft -> Ready`. The guard is detection-only: there
is NO remediation/normalizer CLI to spec (unlike SPEC-05), so the Acceptance Tests table is fully
direct — every invariant maps to a real subtest cluster with no scanner-GREEN-as-oracle indirection.

Purpose: Capture the citation-guard contract (a single corpus scanner that detects issue/PR-number
citations in three forms — inline `#NNN`, parenthetical `(#NNN)`, feat-form `feat-NNNN`; applies a
two-tier allowlist distinguished by SCOPE — `PLACEHOLDER_DIGITS` exempt corpus-wide vs `FILE_ALLOWLIST`
exempt per-file and re-flagged elsewhere, every FILE_ALLOWLIST entry backed by a sibling test; applies
two exclusion state machines — line-1-only YAML frontmatter and triple-backtick code fences; scans the
five prompt-content directories) as a durable, move-proof contract (shape normative; the five SCAN_DIRS,
the PLACEHOLDER_DIGITS members, the FILE_ALLOWLIST entries and backing tests, and both regexes are
advisory `current as of 2026-06-12`), satisfying SPEC-07 and the shared QUAL-01..05 bars so the spec
reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, five invariants (`07-INV-1`..`07-INV-5`; detection,
PLACEHOLDER_DIGITS global tier, FILE_ALLOWLIST per-file tier with test-backing clause, exclusion state
machines, five-directory scope — keep INV-5 standalone per RESEARCH §Open-Question-1, or fold into
INV-1 only if it reads cleanly as one falsifiable claim, Claude's Discretion per D-01), an Acceptance
Tests traceability table citing real verbatim subtests in the single tier-1 file with no unflagged
`[MISSING]` rows, three settled Key Decisions (FILE_ALLOWLIST test-backing per D-03, hex-color tradeoff
per D-04, two-tier-allowlist refactor per D-05), advisory Code Context, frontmatter Status `Ready`
(Confidence + Specced 2026-06-12), and the INDEX.md SPEC-07 row advanced to `Ready`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-76 source artifacts (locked decisions + narration map + locked template + the stub)
@.planning/phases/76-spec-07-citation-guard/76-CONTEXT.md
@.planning/phases/76-spec-07-citation-guard/76-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/07-citation-guard/SPEC.md
@.planning/spec/INDEX.md

# Tier-1 normative source the spec narrates (read VERBATIM for subtest names + detection shapes + constants)
@tests/no-issue-citations.test.cjs

# Closest worked reference — scanner-style spec (section shape, EARS phrasing, advisory marking,
# rule-normative-list-advisory pattern, lettered Key Decisions, QUAL pattern)
@.planning/spec/05-step-numbering/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose / the INDEX.md status
cell — no code/test symbols are created; all system symbols are PRE-EXISTING and appear only under
advisory Code Context):

- **Invariant IDs:** `07-INV-1` (citation detection — inline `#NNN`, parenthetical `(#NNN)` via
  paren-context within one `INLINE_RE` pass, feat-form `feat-NNNN` 3+ digits via `FEAT_FORM_RE`;
  github-style `owner/repo#NNN` is a deliberate POSITIVE detected by its trailing `#NNN`),
  `07-INV-2` (PLACEHOLDER_DIGITS GLOBAL tier — a member is exempt EVERYWHERE in the corpus),
  `07-INV-3` (FILE_ALLOWLIST PER-FILE tier — a digit is exempt ONLY in its listed file(s) and MUST
  remain flagged in every other corpus file, WITH the D-03 test-backing clause: every FILE_ALLOWLIST
  entry MUST be backed by a sibling test requiring the cited digit's continued presence),
  `07-INV-4` (exclusion state machines — line-1-only YAML frontmatter per D-09, and triple-backtick
  code fences per D-10, MUST NOT be scanned; a later `---` is a thematic break and content after it
  IS scanned), `07-INV-5` (five-directory SCAN_DIRS scope — the corpus scanned is the fixed
  five-element SCAN_DIRS set; the shape is normative, the literal list advisory). INV-5 stays
  STANDALONE per RESEARCH §Open-Question-1 recommendation — fold into INV-1 only if it reads cleanly
  as a single falsifiable claim (Claude's Discretion per D-01). INV-2 and INV-3 are kept SEPARATE
  with the scope-of-exemption (global vs per-file) as the normative axis (ROADMAP lock per D-02).
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** SPEC.md frontmatter `**Status:**` advances `Draft -> Ready`;
  `**Confidence:**` (High recommended per RESEARCH §Metadata — Claude's Discretion per D-06/CONTEXT)
  and `**Specced:** 2026-06-12` set. The `**Depends on:** SPEC-08` line and the
  `**Reimplementation evidence (tier-1 test):** tests/no-issue-citations.test.cjs` line are
  PRESERVED, not changed. The INDEX.md SPEC-07 Feature-Status row cell advances `Draft -> Ready`;
  the `SPEC-08 -> SPEC-07` dependency edge and the `Depends On` cell (`SPEC-08`) are PRESERVED.

PRE-EXISTING (advisory, NOT produced here — list under Code Context only, per RESEARCH §"Code Context";
the Phase-77 source-grounding pass MUST exclude these from drift verification):
`tests/no-issue-citations.test.cjs` symbols — `SCAN_DIRS` (five-element array: `commands`,
`get-shit-done/workflows`, `agents`, `get-shit-done/references`, `get-shit-done/templates`),
`PLACEHOLDER_DIGITS` (`new Set([1, 2, 123])`), `FILE_ALLOWLIST` (three file keys / four allowlisted
digits: `commands/gsd/config.md` -> {2439}, `get-shit-done/references/thinking-partner.md` -> {1729},
`agents/gsd-executor.md` -> {2924, 3542}), `INLINE_RE` (`/#(\d+)\b/g`; hex lookbehind removed in
260610-gku), `FEAT_FORM_RE` (`/\bfeat-(\d{3,})\b/g`), `scanContent(content, relPath)` (paren-context
disambiguation; PLACEHOLDER_DIGITS + `FILE_ALLOWLIST[relPath]` exemption; frontmatter + code-fence
state machines), `collectMarkdownFiles(dir)` (ENOENT-tolerant recursive `.md` collector), `ALL_FILES`.
The four advisory backing-test cross-references (D-03): `commands/gsd/config.md` #2439 <-
`tests/bug-2439-set-profile-gsd-sdk-preflight.test.cjs`; `get-shit-done/references/thinking-partner.md`
#1729 <- `tests/thinking-partner.test.cjs`; `agents/gsd-executor.md` #2924 <-
`tests/worktree-cleanup.test.cjs`, #3542 <- `tests/bug-3542-executor-git-stash-prohibition.test.cjs`.
NOTE: there is NO dedicated FILE_ALLOWLIST unit test and NO dedicated SCAN_DIRS-scope unit test — the
corpus describe block `'corpus scan — no issue citations'` (`'no citations in <file>'`, one subtest per
file) is the FUNCTIONAL oracle for 07-INV-3 and 07-INV-5, so those are NOT `[MISSING]` rows. There is
NO remediation/normalizer CLI for this guard (detection-only).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, five 07-INV-M invariants, Acceptance Tests table</name>
  <files>.planning/spec/07-citation-guard/SPEC.md</files>
  <read_first>
    - .planning/spec/07-citation-guard/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose;
      preserve frontmatter, H1 `# SPEC-07: Citation Cleanup Guard`, `**Depends on:** SPEC-08`, and
      the tier-1 evidence line `tests/no-issue-citations.test.cjs`)
    - tests/no-issue-citations.test.cjs (TIER-1 source — narrate VERBATIM. The detection regexes:
      `INLINE_RE = /#(\d+)\b/g`, `FEAT_FORM_RE = /\bfeat-(\d{3,})\b/g`. The constants:
      `SCAN_DIRS` five-element array, `PLACEHOLDER_DIGITS = new Set([1, 2, 123])`, `FILE_ALLOWLIST`
      three file keys / four digits. The paren-context disambiguation: `category = (charBefore === '('
      && charAfter === ')') ? 'parenthetical' : 'inline'` WITHIN the same `INLINE_RE` pass. The
      frontmatter state machine: opening `---` activates frontmatter ONLY when `lineNumber === 1`;
      a later `---` is a thematic break. The code-fence state machine: `/^```/` toggles `inCodeBlock`.
      The describe blocks and their subtests, VERBATIM:
      describe `'scanContent() — inline citation detection'` with subtests
        `'INLINE_RE positive: #3097 produces one inline hit'`,
        `'github-style citation: owner/repo#NNN is detected'`,
        `'parenthetical category: (#3097) produces one parenthetical hit'`,
        `'FEAT_FORM_RE positive: feat-3347 produces one feat-form hit'`,
        `'hex color exemption (D-11): #e8c170 produces zero hits'`,
        `'PLACEHOLDER_DIGITS exemption (D-04): #1, #2, #123 produce zero hits'`;
      describe `'scanContent() — exclusion state machines'` with subtests
        `'heading marker exemption: ## Section and ### Subsection produce zero hits'`,
        `'frontmatter exclusion (D-09): color: "#A78BFA" inside frontmatter produces zero hits'`,
        `'code fence exclusion (D-10): #3456 inside fence produces zero hits'`,
        `'non-line-1 --- is not frontmatter: #3456 after thematic break produces one hit'`;
      describe `'corpus scan — no issue citations'` with the dynamic per-file subtest
        `'no citations in ${relPath}'` (one per file in ALL_FILES; relPath forward-slash normalized))
    - .planning/phases/76-spec-07-citation-guard/76-RESEARCH.md (THE narration map — §"SPEC-07
      Proposed Invariant → Subtest Mapping" the five invariant→subtest clusters; §"Verbatim Source
      Extraction" the constants/regexes/describe-test names; §"How SPEC-05 Structures Each Section";
      §"Common Pitfalls" 1-6 — esp. Pitfall 2 (FILE_ALLOWLIST has THREE file keys / FOUR digits, NOT
      four file keys), Pitfall 6 (07-INV-3 MUST carry the D-03 test-backing clause); §"Open Questions"
      1 (keep INV-5 standalone) and 2 (07-INV-3 oracle = three corpus file subtests, no [MISSING] row))
    - .planning/phases/76-spec-07-citation-guard/76-CONTEXT.md (LOCKED decisions D-01..D-06 — esp.
      D-01 five role-based invariants, D-02 scope-of-exemption as the normative axis for INV-2 vs
      INV-3, D-03 FILE_ALLOWLIST test-backing clause, D-06 enumerations-are-advisory)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template §1, NN-INV-M scheme §2 — `07-INV-M`,
      EARS patterns + RFC 2119 strength §4, the `01-INV-1` example invariant shape, the Acceptance
      Tests table columns `Invariant | Test File | Subtest / Assertion Shape` §5, source-of-truth
      hierarchy §4 tier-1=test)
    - .planning/spec/05-step-numbering/SPEC.md (CLOSEST worked reference — invariant prose +
      "Consequence of violating this invariant:" closer, the "regex is advisory supporting detail,
      current as of 2026-06-12" rule-as-supporting-list pattern, the Acceptance Tests
      multi-row-per-invariant layout, and the corpus-describe-block row pattern
      `corpus describe 'no <X> in <file>' (one subtest per SCAN_FILE)`)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (state the correct behavior directly; the spec sits beside the
    fork's affirmative-framing standard and models it). Do NOT inline fenced code blocks or full file
    contents; name identifiers, subtest strings, regex names, and section headers in prose.

    Purpose (one paragraph): state the "why" of the citation-cleanup guard. The guard is a single
    corpus scanner that detects issue/PR-number citations across the five prompt-content directories
    and keeps illustrative example digits and functional cross-references distinguishable from
    un-cleaned citations. State the three detection forms (inline `#NNN`, parenthetical `(#NNN)`,
    feat-form `feat-NNNN` 3+ digits) and note the github-style `owner/repo#NNN` case is a DELIBERATE
    positive — its trailing `#NNN` matches. State the two-tier allowlist semantic up front
    (PLACEHOLDER_DIGITS exempt corpus-wide; FILE_ALLOWLIST exempt only in its listed file and flagged
    elsewhere, every entry backed by a sibling test). State the two exclusion state machines (line-1-only
    YAML frontmatter; triple-backtick code fences). State what breaks if absent or wrong: without the
    guard, issue/PR citations to ephemeral tracker IDs silently accumulate across upstream merges and
    ship in prompt content that no longer has access to the referenced issue; a wrong allowlist either
    over-exempts a real citation in the wrong file (a functional digit slips through corpus-wide) or
    flags a legitimate illustrative placeholder. Cite `tests/no-issue-citations.test.cjs` as the
    behavioral authority (source-of-truth tier 1 per 00-CONVENTIONS.md §4). This guard is detection-only
    — there is NO remediation/normalizer CLI (Phase 66 did the one-time cleanup manually); say so to
    pre-empt a reader expecting a repair tool.

    Scope: two bullet lists.
      In scope (one bullet per behavioral role, derived from the five invariants): three-form citation
      detection (inline / parenthetical via paren-context within one regex pass / feat-form, with
      github-style `owner/repo#NNN` detected); the PLACEHOLDER_DIGITS GLOBAL exemption tier; the
      FILE_ALLOWLIST PER-FILE exemption tier WITH the test-backing requirement; the two exclusion state
      machines (line-1-only frontmatter, triple-backtick code fences); the five-directory SCAN_DIRS
      corpus scope. State the explicit `SCAN_DIRS` scope (`commands/`, `get-shit-done/workflows/`,
      `agents/`, `get-shit-done/references/`, `get-shit-done/templates/`) as a dated advisory enumeration
      marked `current as of 2026-06-12` (per D-06) — the normative claim is the SCOPE shape (a fixed
      five-directory set), not the literal directory list. Mark the PLACEHOLDER_DIGITS members and the
      FILE_ALLOWLIST entries the same dated-advisory way where they appear.
      Out of scope (each with a one-line rationale): (1) **the hex-color deliberate-false-positive
      policy** — a hex tail in BARE PROSE whose first character after `#` is a digit (e.g. `#8170ff`)
      is accepted-as-flagged BY DESIGN, because false negatives (a missed real citation like
      `owner/repo#NNN`) are worse than false positives; the inline regex deliberately has no hex
      lookbehind (removed in 260610-gku). This is a deliberate NON-behavior; its full settled rationale
      lives in Key Decision (b) — the at-a-glance Out-of-scope bullet keeps a reader from misreading the
      silence as a detection gap. (Hex INSIDE frontmatter/fences IS protected — that guarantee is covered
      by 07-INV-4's exclusion state machines, not duplicated here.) (2) **remediation / citation cleanup**
      — the guard is detection-only; there is no normalizer or repair CLI to spec (Phase 66 cleanup was
      manual). (3) **directories outside SCAN_DIRS** — `tests/`, `sdk/`, and any path not in the
      five-directory set are not part of the prompt corpus this guard governs.

    Invariants: author FIVE numbered EARS statements with RFC 2119 strength, IDs `07-INV-1`..`07-INV-5`
    (NN-INV-M format per 00-CONVENTIONS.md §2). Each is a single falsifiable claim mapping to a real
    subtest cluster (RESEARCH §"SPEC-07 Proposed Invariant → Subtest Mapping"). Use the per-invariant
    EARS pattern that fits (Claude's Discretion per D-01 — Event-driven / Ubiquitous / Unwanted-behavior),
    provided each is a single testable claim.
      - 07-INV-1 Citation detection (three forms): when the scanner processes a non-frontmatter,
        non-code-fence line, the system MUST flag issue/PR-number citations in three forms as a single
        detection family — inline `#NNN`, parenthetical `(#NNN)` (the SAME `INLINE_RE` match categorized
        `parenthetical` when immediately wrapped by `(` and `)`, not a separate regex), and feat-form
        `feat-NNNN` with a 3+ digit tracker ID (`FEAT_FORM_RE`). State EXPLICITLY that the github-style
        `owner/repo#NNN` form IS detected (its trailing `#NNN` matches) — a deliberate positive, not an
        exemption. Mark `INLINE_RE` (`/#(\d+)\b/g`) and `FEAT_FORM_RE` (`/\bfeat-(\d{3,})\b/g`) as
        advisory supporting detail `current as of 2026-06-12`. (If you fold INV-5 here per Claude's
        Discretion, add the scope clause; RESEARCH recommends keeping INV-5 standalone.)
      - 07-INV-2 PLACEHOLDER_DIGITS GLOBAL exemption: a digit that is a member of `PLACEHOLDER_DIGITS`
        MUST be exempt EVERYWHERE in the corpus — global, illustrative example digits with no real
        referent. State the GLOBAL scope as the normative claim (this is the scope-axis contrast with
        INV-3 per D-02). Mark the current members (`{1, 2, 123}`) as advisory `current as of 2026-06-12`
        — the rule (global exemption) is normative; the member values are dated.
      - 07-INV-3 FILE_ALLOWLIST PER-FILE exemption (with test-backing clause): a digit listed in
        `FILE_ALLOWLIST` for a given file MUST be exempt ONLY in that listed file and MUST remain a
        flagged violation in every OTHER corpus file. State the PER-FILE scope as the normative claim
        (the global-vs-per-file contrast with INV-2 is the D-02 normative axis). Include the D-03
        test-backing sub-clause as a normative MUST: every `FILE_ALLOWLIST` entry MUST be backed by a
        sibling test that requires the cited digit's continued presence in that file (an entry without a
        backing test is indistinguishable from an un-cleaned citation and becomes a silent permanent
        exemption). Mark the current entries (THREE file keys — `commands/gsd/config.md`,
        `get-shit-done/references/thinking-partner.md`, `agents/gsd-executor.md` — covering FOUR
        allowlisted digits {2439}, {1729}, {2924, 3542}) and the four backing tests as advisory
        `current as of 2026-06-12`. (Pitfall 2: it is THREE files / FOUR digits, NOT four file keys.)
      - 07-INV-4 Exclusion state machines: content inside an excluded region MUST NOT be scanned for
        citations. Two regions: (a) YAML frontmatter — recognized ONLY when the opening `---` is on
        LINE 1 (`lineNumber === 1`); a `---` on any LATER line is a thematic break and content after it
        IS scanned (per D-09); and (b) triple-backtick code fences — a line matching `/^```/` toggles the
        fence and fenced content is skipped (per D-10). State the line-1-only frontmatter rule and the
        thematic-break consequence explicitly. (This invariant also delivers the hex-inside-protected-
        context guarantee referenced by the D-04 Out-of-scope bullet.)
      - 07-INV-5 Five-directory SCAN_DIRS scope: the corpus the guard scans MUST be the fixed
        five-directory `SCAN_DIRS` set; files outside that set are not scanned. State the SCOPE shape (a
        fixed five-element set) as the normative claim and mark the literal directory list (`commands/`,
        `get-shit-done/workflows/`, `agents/`, `get-shit-done/references/`, `get-shit-done/templates/`)
        as advisory `current as of 2026-06-12` (per D-06). Keep STANDALONE per RESEARCH §Open-Question-1
        (a reader scanning invariants should see the scope constraint as its own claim) — fold into INV-1
        only if it reads cleanly as a single falsifiable claim (Claude's Discretion per D-01).
      End each invariant with a "Consequence of violating this invariant: <what breaks>" clause.

    Acceptance Tests: a traceability table keyed on `07-INV-M`, columns EXACTLY
    `Invariant | Test File | Subtest / Assertion Shape`. The Test File column carries
    `tests/no-issue-citations.test.cjs` for EVERY row (single tier-1 file). Lift REAL verbatim subtest
    names from the test file (cross-check against RESEARCH §"SPEC-07 Proposed Invariant → Subtest
    Mapping"). Every MUST invariant traces to a real subtest: NO unflagged `[MISSING — write test first]`
    rows. For 07-INV-3 and 07-INV-5 there is no dedicated unit subtest — cite the corpus describe block
    `'corpus scan — no issue citations'` rows as the FUNCTIONAL oracle (NOT a [MISSING] row, per
    RESEARCH §Open-Question-2 — the FILE_ALLOWLIST entries and the SCAN_DIRS scope must be correct or the
    corpus pass would fail RED). Include at least these literal verbatim subtest names:
      - 07-INV-1: `'INLINE_RE positive: #3097 produces one inline hit'`,
        `'github-style citation: owner/repo#NNN is detected'`,
        `'parenthetical category: (#3097) produces one parenthetical hit'`,
        `'FEAT_FORM_RE positive: feat-3347 produces one feat-form hit'`, plus the corpus row
        `'corpus scan — no issue citations'` -> `'no citations in <file>'` (one subtest per file in
        ALL_FILES).
      - 07-INV-2: `'PLACEHOLDER_DIGITS exemption (D-04): #1, #2, #123 produce zero hits'`.
      - 07-INV-3: the corpus describe block `'corpus scan — no issue citations'` rows for the three
        allowlisted files — `'no citations in commands/gsd/config.md'`,
        `'no citations in get-shit-done/references/thinking-partner.md'`,
        `'no citations in agents/gsd-executor.md'` — cited as the functional per-file-exemption oracle
        (note the per-file exemption is exercised by the corpus pass; no dedicated FILE_ALLOWLIST unit
        test exists, and this is real indirect coverage, not a missing test).
      - 07-INV-4: `'heading marker exemption: ## Section and ### Subsection produce zero hits'`,
        `'frontmatter exclusion (D-09): color: "#A78BFA" inside frontmatter produces zero hits'`,
        `'code fence exclusion (D-10): #3456 inside fence produces zero hits'`,
        `'non-line-1 --- is not frontmatter: #3456 after thematic break produces one hit'`.
      - 07-INV-5: the corpus describe block `'corpus scan — no issue citations'` ->
        `'no citations in <file>'` (one subtest per file across the five SCAN_DIRS) as the scope oracle.
    Precede the table with a one-sentence narrative explaining that 07-INV-3 and 07-INV-5 cite the
    corpus describe block as the functional oracle (the guard has no dedicated FILE_ALLOWLIST or
    SCAN_DIRS unit test) — indirect coverage, not missing tests.
  </action>
  <verify>
    <automated>grep -c '07-INV-' .planning/spec/07-citation-guard/SPEC.md  # >=5 invariant-ID occurrences</automated>
    <automated>grep -q 'tests/no-issue-citations.test.cjs' .planning/spec/07-citation-guard/SPEC.md && echo OK  # tier-1 file cited</automated>
    <automated>grep -q 'INLINE_RE positive: #3097 produces one inline hit' .planning/spec/07-citation-guard/SPEC.md && grep -q 'github-style citation: owner/repo#NNN is detected' .planning/spec/07-citation-guard/SPEC.md && grep -q 'non-line-1 --- is not frontmatter: #3456 after thematic break produces one hit' .planning/spec/07-citation-guard/SPEC.md && grep -q 'PLACEHOLDER_DIGITS exemption (D-04): #1, #2, #123 produce zero hits' .planning/spec/07-citation-guard/SPEC.md && echo OK  # verbatim anchor subtests present</automated>
    <automated>grep -q 'no citations in commands/gsd/config.md' .planning/spec/07-citation-guard/SPEC.md && grep -q 'current as of 2026-06-12' .planning/spec/07-citation-guard/SPEC.md && echo OK  # per-file corpus oracle row + dated-advisory marker</automated>
    <automated>grep -v '^#' .planning/spec/07-citation-guard/SPEC.md | grep -c 'MISSING — write test first'  # ==0, every MUST invariant has a real subtest (INV-3/INV-5 cite corpus oracle)</automated>
    <automated>grep -c 'to be filled' .planning/spec/07-citation-guard/SPEC.md  # decreasing — first four placeholder comments replaced</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '07-INV-' SPEC.md` returns >=5 (five invariant IDs `07-INV-1`..`07-INV-5` present,
      matching `07-INV-[1-5]`).
    - The single tier-1 test file appears in the Acceptance Tests table:
      `grep -q 'tests/no-issue-citations.test.cjs'` succeeds.
    - The table contains the verbatim anchor subtest names — at minimum
      `'INLINE_RE positive: #3097 produces one inline hit'`,
      `'github-style citation: owner/repo#NNN is detected'`,
      `'PLACEHOLDER_DIGITS exemption (D-04): #1, #2, #123 produce zero hits'`, and
      `'non-line-1 --- is not frontmatter: #3456 after thematic break produces one hit'` — lifted
      exactly from `tests/no-issue-citations.test.cjs`.
    - 07-INV-3 cites at least one corpus per-file oracle row
      (`grep -q 'no citations in commands/gsd/config.md'` succeeds), and at least one dated advisory
      marker is present (`grep -q 'current as of 2026-06-12'` succeeds).
    - 07-INV-3 states the D-03 test-backing requirement (a clause requiring every FILE_ALLOWLIST entry
      to be backed by a sibling test) and INV-2 vs INV-3 state the global-vs-per-file scope contrast
      (`grep -q 'everywhere'` and `grep -q 'per-file'` or equivalent scope language succeed).
    - `grep -v '^#' SPEC.md | grep -c 'MISSING — write test first'` returns 0 (no unflagged MISSING
      rows in body content — INV-3/INV-5 cite the corpus describe block as the functional oracle).
    - The four sections appear under the exact headers `## Purpose`, `## Scope`, `## Invariants`,
      `## Acceptance Tests` in that order, with no `<!-- to be filled -->` comment remaining in them.
  </acceptance_criteria>
  <done>
    Purpose, Scope, Invariants (five `07-INV-M` EARS statements), and the Acceptance Tests
    traceability table are authored; ROADMAP success criteria 1 and 2 (invariant + traceability half)
    are satisfied; three-form detection (with github-style positive), the PLACEHOLDER_DIGITS global
    tier, the FILE_ALLOWLIST per-file tier with the D-03 test-backing clause, the two exclusion state
    machines (line-1 frontmatter + code fence), and the five-directory SCAN_DIRS scope are all
    specified; the two allowlist tiers are distinguished as SEPARATE invariants by scope of exemption;
    every MUST invariant traces to a real subtest in the single tier-1 file with no unflagged MISSING
    rows; and the regexes / member values / SCAN_DIRS are marked advisory `current as of 2026-06-12`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author three settled Key Decisions + advisory Code Context, advance SPEC.md Status Draft -> Ready</name>
  <files>.planning/spec/07-citation-guard/SPEC.md</files>
  <read_first>
    - .planning/spec/07-citation-guard/SPEC.md (now carrying the Task 1 normative core; fill the
      remaining `## Key Decisions` and `## Code Context` sections and update the frontmatter block)
    - .planning/phases/76-spec-07-citation-guard/76-RESEARCH.md (§"How SPEC-05 Structures Each Section"
      -> "Key Decisions" the three required Key Decisions (a) FILE_ALLOWLIST test-backing (D-03),
      (b) hex-color tradeoff (D-04), (c) two-tier-allowlist refactor (D-05); -> "Code Context" the
      advisory symbol inventory; §"Verbatim Source Extraction" the constants/backing-test cross-refs;
      §"Note on hex color test" the precise hex mechanics — `#e8c170` produces zero hits because `e` is
      not a digit, NOT because of inline code; a hex whose first post-`#` char IS a digit WOULD match —
      this is the accepted false positive; §"Stub State Verification" the frontmatter transition
      (Status Ready, Confidence, Specced 2026-06-12))
    - .planning/phases/76-spec-07-citation-guard/76-CONTEXT.md (D-03 FILE_ALLOWLIST test-coupling,
      D-04 hex-policy as Key-Decision-plus-Out-of-scope NOT invariant, D-05 two-tier-refactor settled,
      D-06 shape-normative-values-dated)
    - tests/no-issue-citations.test.cjs (advisory symbol names for Code Context ONLY: `SCAN_DIRS`,
      `PLACEHOLDER_DIGITS`, `FILE_ALLOWLIST`, `INLINE_RE`, `FEAT_FORM_RE`, `scanContent(content,
      relPath)`, `collectMarkdownFiles(dir)`, `ALL_FILES`; the module-header comment documenting the
      allowlist policy and the hex tradeoff. NO normative claim rests here)
    - .planning/spec/00-CONVENTIONS.md (§1 Section 6/7 shape; §3 status vocabulary
      Draft|Ready|Implemented|Verified and the "MUST NOT advance to Ready unless QUAL-01..05 satisfied"
      rule; §4 advisory-marker rule)
    - .planning/spec/05-step-numbering/SPEC.md (CLOSEST worked reference — the `### (a)`/`### (b)`/`### (c)`
      lettered Key Decision format with "Settled — do not reopen. Consequence of reopening:" footer, and
      the advisory Code Context layout opening with `<!-- advisory -->` + "current as of 2026-06-12"
      caveat + symbol table `Symbol | What it does | Line (advisory)`)
  </read_first>
  <action>
    Fill the final two sections and update the frontmatter block; keep authoring affirmative-framed
    prose. Do NOT inline fenced code blocks; name symbols in prose/tables.

    Key Decisions (`## Key Decisions`): record the THREE settled decisions from RESEARCH, each as
    `### (a)`/`### (b)`/`### (c)` with statement + one-sentence rationale + "Settled — do not reopen.
    Consequence of reopening: <what breaks>":
      (a) FILE_ALLOWLIST test-backing requirement (D-03): every `FILE_ALLOWLIST` entry exists ONLY
          because another contract (a sibling test) depends on the cited digit's continued presence in
          that file; remove the dependency and the citation must go. The allowlist exempts a citation
          only because another test requires it — so every entry MUST be backed by a sibling test.
          Rationale: an entry without a backing test is indistinguishable from an un-cleaned citation
          and would become a silent permanent exemption. Name the four current backings as advisory
          `current as of 2026-06-12`: `commands/gsd/config.md` #2439 <-
          `tests/bug-2439-set-profile-gsd-sdk-preflight.test.cjs`;
          `get-shit-done/references/thinking-partner.md` #1729 <- `tests/thinking-partner.test.cjs`;
          `agents/gsd-executor.md` #2924 <- `tests/worktree-cleanup.test.cjs`, #3542 <-
          `tests/bug-3542-executor-git-stash-prohibition.test.cjs`. Consequence of reopening: an
          unbacked allowlist entry lets a real un-cleaned citation hide permanently behind a per-file
          exemption that no test guards, defeating the guard's purpose for that file.
      (b) Hex-color deliberate-false-positive tradeoff — inline regex hex lookbehind removed (D-04):
          the guard ACCEPTS hex-color false positives in plain prose BY DESIGN, because false negatives
          (a missed real citation like `owner/repo#NNN`) are worse than false positives (a flagged hex
          tail an author moves to a fence or allowlists). The inline regex deliberately has NO hex
          lookbehind (removed in 260610-gku). State the precise mechanics (RESEARCH §"Note on hex color
          test"): `#e8c170` produces zero hits because the character after `#` (`e`) is not a digit —
          NOT because of any hex-specific exemption; a hex whose first post-`#` character IS a digit
          (e.g. `#8170ff`) WOULD match and is accepted-as-flagged. Hex INSIDE frontmatter (D-09) and
          code fences (D-10) IS protected — that protection is delivered by 07-INV-4's exclusion state
          machines, not by any hex-specific rule. Rationale: a citation scanner that misses real
          citations to avoid hex false positives fails its primary job. Consequence of reopening:
          re-adding a hex lookbehind to suppress prose hex false positives reintroduces the false
          negative it was removed to fix — real `owner/repo#NNN`-style citations slip through undetected.
      (c) Two-tier-allowlist refactor as settled design (D-05, ROADMAP SC3): the single flat allowlist
          was split into a GLOBAL `PLACEHOLDER_DIGITS` tier and a PER-FILE `FILE_ALLOWLIST` tier because
          the two exemption needs are different in KIND — illustrative-everywhere placeholders vs
          functional-here-only cross-references. Rationale: collapsing them back to one flat list either
          over-exempts functional digits corpus-wide (a real citation in the wrong file slips through)
          or forces per-file scoping onto illustrative placeholders (needless churn). Consequence of
          reopening: a single flat allowlist cannot express "exempt everywhere" and "exempt only here"
          at once — re-flattening either leaks functional cross-reference digits into every file or
          burdens illustrative placeholders with per-file entries, and INV-2/INV-3's scope contrast
          collapses.

    Code Context (`## Code Context`): mark the section `<!-- advisory -->` and open with a dated caveat
    in the SPEC-05 style ("The items below are current as of 2026-06-12. All file paths, function names,
    regex bodies, constant values, and line numbers are advisory and will shift on any test edit or
    upstream refactor. No normative invariant depends on these paths or symbols."). List the PRE-EXISTING
    current symbols ONLY here, with no normative claim resting on them (Pitfall 3 — every sentence is
    advisory description, not a requirement), in a single advisory subsection for the tier-1 source plus
    the advisory backing-test cross-references:
      1. `tests/no-issue-citations.test.cjs` (tier-1 normative source) — a symbol table with columns
         `Symbol | What it does | Line (advisory)`: `SCAN_DIRS` (five-element corpus directory array),
         `PLACEHOLDER_DIGITS` (`new Set([1, 2, 123])`, global tier), `FILE_ALLOWLIST` (per-file map:
         three file keys / four digits), `INLINE_RE` (`/#(\d+)\b/g`; no hex lookbehind), `FEAT_FORM_RE`
         (`/\bfeat-(\d{3,})\b/g`), `scanContent(content, relPath)` (paren-context disambiguation;
         PLACEHOLDER_DIGITS + `FILE_ALLOWLIST[relPath]` exemption; frontmatter + code-fence state
         machines), `collectMarkdownFiles(dir)` (ENOENT-tolerant recursive `.md` collector), `ALL_FILES`.
      2. The four advisory FILE_ALLOWLIST backing-test cross-references (D-03), as a small table:
         `commands/gsd/config.md` #2439 <- `tests/bug-2439-set-profile-gsd-sdk-preflight.test.cjs`;
         `get-shit-done/references/thinking-partner.md` #1729 <- `tests/thinking-partner.test.cjs`;
         `agents/gsd-executor.md` #2924 <- `tests/worktree-cleanup.test.cjs`, #3542 <-
         `tests/bug-3542-executor-git-stash-prohibition.test.cjs`. State these are advisory
         cross-references for the FILE_ALLOWLIST entries — the test-backing REQUIREMENT is normative
         (07-INV-3 / Key Decision (a)); these specific test paths are advisory and shift on any rename.
      State that all line numbers and constant values are advisory and shift on any edit (move-proofing
      for the upstream refactor), and that there is NO dedicated FILE_ALLOWLIST or SCAN_DIRS unit test
      (the corpus describe block is the functional oracle) and NO remediation/normalizer CLI.

    Frontmatter transition (block-header frontmatter, NOT YAML): set `**Status:** Ready` (from Draft),
    set `**Confidence:**` to a stated value (High recommended per RESEARCH §Metadata — Claude's
    Discretion per CONTEXT.md), and set `**Specced:** 2026-06-12`. PRESERVE `**Depends on:** SPEC-08`
    and the `**Reimplementation evidence (tier-1 test):** tests/no-issue-citations.test.cjs` line.
    Advancing to Ready asserts QUAL-01 (numbered falsifiable EARS invariants), QUAL-02 (traceability
    maps each MUST to a subtest, no unflagged MISSING rows), QUAL-03 (advisory-marked paths/symbols,
    move-proof), QUAL-04 (the tier-1 test cited), QUAL-05 (three Key Decisions with do-not-reopen +
    consequence) are all satisfied. Do NOT modify INDEX.md in THIS task — the SPEC-07 row flip is
    Task 3.
  </action>
  <verify>
    <automated>grep -q '\*\*Status:\*\* Ready' .planning/spec/07-citation-guard/SPEC.md && grep -q '\*\*Specced:\*\* 2026-06-12' .planning/spec/07-citation-guard/SPEC.md && echo OK</automated>
    <automated>grep -q '\*\*Depends on:\*\* SPEC-08' .planning/spec/07-citation-guard/SPEC.md && grep -q 'tests/no-issue-citations.test.cjs' .planning/spec/07-citation-guard/SPEC.md && echo OK  # SPEC-08 edge + tier-1 evidence preserved</automated>
    <automated>grep -c 'Settled — do not reopen' .planning/spec/07-citation-guard/SPEC.md  # >=3 Key Decisions (a,b,c)</automated>
    <automated>grep -q 'PLACEHOLDER_DIGITS' .planning/spec/07-citation-guard/SPEC.md && grep -q 'FILE_ALLOWLIST' .planning/spec/07-citation-guard/SPEC.md && grep -q 'bug-2439-set-profile-gsd-sdk-preflight.test.cjs' .planning/spec/07-citation-guard/SPEC.md && grep -q '260610-gku' .planning/spec/07-citation-guard/SPEC.md && echo OK  # allowlist constants, a backing test, hex-removal provenance</automated>
    <automated>grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' .planning/spec/07-citation-guard/SPEC.md  # ==6, locked section set, none renamed/added/reordered</automated>
    <automated>grep -c 'to be filled' .planning/spec/07-citation-guard/SPEC.md  # ==0, no placeholder comments remain</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` contains literal `**Status:** Ready` and `**Specced:** 2026-06-12`; `Confidence` is set
      to a non-placeholder value; `**Depends on:** SPEC-08` and the tier-1 evidence line
      `tests/no-issue-citations.test.cjs` are preserved unchanged.
    - `grep -c 'Settled — do not reopen' SPEC.md` returns >=3 (the three Key Decisions a, b, c).
    - All three Key Decisions are present and identifiable: FILE_ALLOWLIST test-backing (names a backing
      test, e.g. `grep -q 'bug-2439-set-profile-gsd-sdk-preflight.test.cjs'`), the hex-color tradeoff
      (names the regex hex-lookbehind removal provenance `grep -q '260610-gku'`), and the
      two-tier-allowlist refactor — each with a consequence-of-reopening sentence.
    - The `## Code Context` section is marked `<!-- advisory -->` and lists `SCAN_DIRS`,
      `PLACEHOLDER_DIGITS`, `FILE_ALLOWLIST`, `INLINE_RE`, `FEAT_FORM_RE`, `scanContent`,
      `collectMarkdownFiles`, and the four backing-test cross-references; no normative invariant text
      appears in Code Context.
    - The six body section headers appear exactly once each in the locked order
      (`grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$'`
      returns 6) — no section renamed/added/removed vs 00-CONVENTIONS.md §1.
    - `grep -c 'to be filled' SPEC.md` returns 0 (every placeholder comment replaced).
  </acceptance_criteria>
  <done>
    Three settled Key Decisions (FILE_ALLOWLIST test-backing, hex-color deliberate-false-positive
    tradeoff with regex-lookbehind-removal provenance, two-tier-allowlist refactor) and advisory Code
    Context are authored; frontmatter Status is `Ready` with Confidence + Specced 2026-06-12 set and the
    `Depends on: SPEC-08` + tier-1 evidence lines preserved; ROADMAP success criterion 3 (advisory paths
    + two-tier-refactor-settled half) is satisfied; and the spec conforms exactly to the locked
    7-section template (no drift for Phase 77).
  </done>
</task>

<task type="auto">
  <name>Task 3: Advance INDEX.md SPEC-07 Feature-Status row Draft -> Ready</name>
  <files>.planning/spec/INDEX.md</files>
  <read_first>
    - .planning/spec/INDEX.md (the Feature-Status Table — the SPEC-07 row currently reads
      `| SPEC-07 | Citation Guard | [07-citation-guard/SPEC.md](07-citation-guard/SPEC.md) | Draft | SPEC-08 |`;
      change ONLY the Status cell `Draft -> Ready`. Preserve the Feature name, the link, the `Depends On`
      cell `SPEC-08`, the Dependency Graph `SPEC-08 -> SPEC-07` edge, and the Wave-2 Build Order mapping —
      none of those change)
    - .planning/spec/07-citation-guard/SPEC.md (confirm the SPEC.md frontmatter now reads
      `**Status:** Ready` from Task 2 before flipping the INDEX row — the two MUST agree)
    - .planning/phases/76-spec-07-citation-guard/76-RESEARCH.md (§"INDEX.md Consistency Check" — the
      exact row transition `| SPEC-07 | ... | Draft | SPEC-08 |` -> `| SPEC-07 | ... | Ready | SPEC-08 |`)
  </read_first>
  <action>
    In `.planning/spec/INDEX.md`, edit the SPEC-07 row of the Feature-Status Table, changing ONLY the
    Status column value from `Draft` to `Ready`. The row's Feature name (`Citation Guard`), the SPEC.md
    link, and the `Depends On` cell (`SPEC-08`) are PRESERVED unchanged. Do NOT touch any other row, the
    Dependency Graph section, the `SPEC-08 -> SPEC-07` arrow, the Build Order / Wave-2 mapping, or the
    Excluded-from-Scope section — Phase 77 reconciles the full graph; this task only advances the one
    status cell so INDEX.md agrees with the SPEC.md frontmatter Status set in Task 2.
  </action>
  <verify>
    <automated>grep -E '^\| SPEC-07 \|.*\| Ready \| SPEC-08 \|$' .planning/spec/INDEX.md && echo OK  # SPEC-07 row Status==Ready, Depends On==SPEC-08 preserved</automated>
    <automated>grep -cE '^\| SPEC-07 \|.*\| Draft \|' .planning/spec/INDEX.md  # ==0, no Draft cell remains on the SPEC-07 row</automated>
    <automated>grep -q 'SPEC-08 → SPEC-07' .planning/spec/INDEX.md && echo OK  # dependency edge preserved</automated>
  </verify>
  <acceptance_criteria>
    - The SPEC-07 row of the Feature-Status Table reads `Ready` in the Status column and `SPEC-08` in the
      Depends On column: `grep -E '^\| SPEC-07 \|.*\| Ready \| SPEC-08 \|$' INDEX.md` succeeds.
    - No `Draft` value remains on the SPEC-07 row: `grep -cE '^\| SPEC-07 \|.*\| Draft \|' INDEX.md`
      returns 0.
    - The `SPEC-08 → SPEC-07` dependency edge is preserved: `grep -q 'SPEC-08 → SPEC-07' INDEX.md`
      succeeds.
    - The INDEX.md SPEC-07 status agrees with the SPEC.md frontmatter (both `Ready`).
  </acceptance_criteria>
  <done>
    The INDEX.md SPEC-07 Feature-Status row is advanced `Draft -> Ready`, the `Depends On: SPEC-08` cell
    and the `SPEC-08 -> SPEC-07` dependency edge are preserved, and the manifest status agrees with the
    SPEC.md frontmatter Status — leaving the spec set internally consistent for the Phase 77 review.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This phase authors a static Markdown documentation artifact (`SPEC.md`) and edits one status cell in another Markdown file (`INDEX.md`). There is no untrusted input, no code execution, no runtime surface, no network call, and no secret handled. The citation guard the spec describes runs at test time over the committed corpus (file read + regex match, no file write — it is detection-only), but specifying it in prose introduces none of that surface — standard STRIDE injection/auth/DoS categories do not apply to a prose deliverable. At ASVS L1 there are no exploitable threats for a spec-authoring phase; this block is honest about that rather than inventing threats. Non-blocking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Information disclosure / Tampering (spec INACCURACY) | SPEC.md normative claims (Invariants + Acceptance Tests) | mitigate | Every invariant and Acceptance Tests row is traced to a real VERBATIM subtest in the single tier-1 file `tests/no-issue-citations.test.cjs` (per QUAL-02/QUAL-04 / D-01 — no unflagged `[MISSING]` rows; 07-INV-3 and 07-INV-5 cite the corpus describe block `'corpus scan — no issue citations'` as the functional oracle per RESEARCH §Open-Question-2, which is real indirect coverage, not a planted false `[MISSING]` and not a planted false coverage claim — the FILE_ALLOWLIST entries and SCAN_DIRS scope must be correct or the corpus pass fails RED); no normative claim rests on an advisory path/symbol/regex/constant the upstream refactor could move (per QUAL-03 advisory-marking under Code Context); every enumeration (five SCAN_DIRS, PLACEHOLDER_DIGITS members, FILE_ALLOWLIST entries + four backing tests, both regexes) is narrated as `current as of 2026-06-12` SHAPE, not durable values (per D-06); the two allowlist tiers are distinguished by scope of exemption (global vs per-file) so a reimplementer does not collapse them; the github-style `owner/repo#NNN` positive and the hex deliberate-false-positive tradeoff are stated explicitly so silence is not misread as a detection gap. |
| T-76-02 | Tampering (advisory-claim drift) | SPEC.md Code Context paths/symbols/constants | accept | Current paths, symbol names (`scanContent`, `collectMarkdownFiles`, `SCAN_DIRS`, `PLACEHOLDER_DIGITS`, `FILE_ALLOWLIST`, `INLINE_RE`, `FEAT_FORM_RE`, `ALL_FILES`), regex bodies, the PLACEHOLDER_DIGITS / FILE_ALLOWLIST member values, the four backing-test paths, and line numbers will shift on any source edit or upstream refactor; this is accepted because they are confined to `## Code Context` and marked `<!-- advisory -->`, so no contract depends on them. |
| T-76-03 | Tampering (manifest/spec status disagreement) | INDEX.md SPEC-07 Status cell vs SPEC.md frontmatter Status | mitigate | Task 3 flips the INDEX.md SPEC-07 Status cell to `Ready` ONLY after Task 2 sets the SPEC.md frontmatter `**Status:** Ready`, and the Task-3 verify gate asserts both read `Ready` with the `SPEC-08` Depends-On cell and the `SPEC-08 -> SPEC-07` edge preserved — the two artifacts cannot diverge, which is exactly what the Phase 77 reconciliation checks. |

No package-manager installs occur in this phase, so the `T-76-SC` supply-chain row and the
package-legitimacy checkpoint do not apply. No high-severity threats are present; this documentation
phase is non-blocking at ASVS L1.
</threat_model>

<verification>
Overall phase checks (run after all three tasks):
- `grep -c '07-INV-' SPEC.md` >= 5 (five invariants `07-INV-1`..`07-INV-5` present, matching `07-INV-[1-5]`).
- `grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' SPEC.md` == 6 (locked section set, exact order, no drift vs 00-CONVENTIONS.md §1).
- The single tier-1 file is cited: `grep -q 'tests/no-issue-citations.test.cjs' SPEC.md` succeeds.
- `grep -v '^#' SPEC.md | grep -c 'MISSING — write test first'` == 0 (every MUST invariant has a real subtest — 07-INV-3/07-INV-5 cite the corpus describe block as the functional oracle).
- `grep -c 'Settled — do not reopen' SPEC.md` >= 3 (Key Decisions a, b, c — QUAL-05); `grep -q 'PLACEHOLDER_DIGITS' SPEC.md` and `grep -q 'FILE_ALLOWLIST' SPEC.md` both succeed (two-tier allowlist named).
- `grep -q '260610-gku' SPEC.md` succeeds (hex-lookbehind-removal provenance — Key Decision (b)).
- `grep -q 'bug-2439-set-profile-gsd-sdk-preflight.test.cjs' SPEC.md` succeeds (a FILE_ALLOWLIST backing test cross-reference — D-03 / Key Decision (a)).
- `grep -q 'current as of 2026-06-12' SPEC.md` succeeds (dated advisory enumerations — D-06).
- `grep -q '\*\*Status:\*\* Ready' SPEC.md`, `grep -q '\*\*Specced:\*\* 2026-06-12' SPEC.md`, and `grep -q '\*\*Depends on:\*\* SPEC-08' SPEC.md` all succeed.
- `grep -c 'to be filled' SPEC.md` == 0 (no placeholder comments remain).
- `grep -E '^\| SPEC-07 \|.*\| Ready \| SPEC-08 \|$' INDEX.md` succeeds and `grep -cE '^\| SPEC-07 \|.*\| Draft \|' INDEX.md` == 0 (INDEX status flipped, Depends-On preserved).
</verification>

<success_criteria>
Measurable completion — maps to the three ROADMAP Phase 76 success criteria:
1. `07-citation-guard/SPEC.md` specifies the `no-issue-citations.test.cjs` detection (inline /
   parenthetical / feat-form, with github-style `owner/repo#NNN` as a deliberate positive), the
   two-tier allowlist (`PLACEHOLDER_DIGITS` global vs `FILE_ALLOWLIST` per-file) with distinct per-tier
   semantics, the two exclusion state machines (line-1 frontmatter + code fence), and the 5-directory
   detection scope. [SPEC-07]
2. Invariants are five numbered EARS statements citing `tests/no-issue-citations.test.cjs` as the
   tier-1 source; the two allowlist tiers are distinguished as SEPARATE invariants by scope of
   exemption; the Acceptance Tests traceability table maps each MUST to a real verbatim subtest with no
   unflagged MISSING rows (07-INV-3/07-INV-5 cite the corpus describe block as the functional oracle).
   [QUAL-01, QUAL-02, QUAL-04]
3. Current paths/symbols/regexes/member-values are advisory-marked `current as of 2026-06-12` (shape
   normative, values dated); the FILE_ALLOWLIST test-backing requirement (D-03), the hex-color
   deliberate-false-positive tradeoff (D-04), and the two-tier-allowlist refactor (D-05) are recorded as
   settled Key Decisions with consequence-of-reopening; SPEC.md Status advanced `Draft -> Ready` and the
   INDEX.md SPEC-07 row advanced `Draft -> Ready` (Depends-On + dependency edge preserved).
   [QUAL-03, QUAL-05]
</success_criteria>

<output>
Create `.planning/phases/76-spec-07-citation-guard/76-01-SUMMARY.md` when done.
</output>
