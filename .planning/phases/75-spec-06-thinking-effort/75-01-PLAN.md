---
phase: 75-spec-06-thinking-effort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/06-thinking-effort/SPEC.md
  - .planning/spec/INDEX.md
autonomous: true
requirements: [SPEC-06, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md specifies the full per-agent thinking-effort surface as one connected behavior: the parseModelEffort semicolon parser (model;effort splits on lastIndexOf(';'); an EFFORT_TOKENS member populates effort; an unrecognized ;-suffix is a typo stripped from model with effort null and a once-only warning; a bare label yields {model: label, effort: null}; a colon is never a delimiter); the resolveReasoningEffortInternal unified precedence chain (outermost runtime-allowlist gate -> per-agent override via parseModelEffort -> haiku-omits-effort carve-out -> shared/phase-type slot effort -> user reasoning_effort override -> catalog slot effort -> runtime-tier fallback); the D-08 medium floor; the static RUNTIMES_WITH_REASONING_EFFORT={claude,codex} allowlist as an absolute outer gate overrides cannot bypass; the per-runtime omit/translate contract (13 non-effort runtimes resolve to null; translateEffortForCodex maps max->xhigh and passes low|medium|high through, null|undefined->null); the catalog reasoning_effort schema; the 20 *_effort init siblings; spawn-template wiring; and the install.js Codex emit seam (ROADMAP SC1 / SPEC-06, per D-01/D-07)"
    - "SPEC.md states 6-8 numbered role-based EARS invariants 06-INV-1..06-INV-N with RFC 2119 strength, each a single falsifiable claim mapping to a real subtest cluster, with the D-08 medium floor stated as a MUST-level invariant (bare {claude,codex} slots floor to medium; carve-outs haiku-exempt / inherit-unknown->null / non-effort-runtime->null in the SAME invariant) carrying the consequence of omission (without the floor every such agent silently drops to the provider default, erasing the per-agent effort dimension the milestone exists to add), and the static-allowlist gate kept SEPARATE from the precedence-chain invariant as a distinct MUST with a distinct failure consequence (ROADMAP SC2 / QUAL-01, per D-01/D-03)"
    - "SPEC.md carries an Acceptance Tests traceability table keyed on 06-INV-M mapping each MUST-level invariant to a VERBATIM real describe/test name routed to its CLOSEST tier-1 oracle (parser->parse-model-effort.test.cjs + parse-model-effort-parity.test.cjs; resolver precedence + allowlist no-op->feat-53-unified-effort-resolver.test.cjs; D-08 floor->feat-53-config-sites-and-golden.test.cjs; omit/translate->feat-58-regression.test.cjs TEST-03; catalog+siblings golden->feat-58-regression.test.cjs TEST-01; wiring->phase-56-effort-wiring.test.cjs; Codex emit->feat-57-install-translation.test.cjs) with NO unflagged [MISSING] rows; the 330-row golden snapshot is cited as the tier-1 oracle and init.cjs's resolveReasoningEffortInternal as the tier-2 implementation, and the snapshot STRUCTURE is described ({generated, description, rows[], omitContract[]}; each row {agent, profile, runtime, expectedModel, expectedEffort}; omitContract asserts expectedEffort:null for the 13 non-effort runtimes) (ROADMAP SC2,SC3 / QUAL-02,04, per D-05)"
    - "SPEC.md records the Codex emit fix as a SETTLED Key Decision naming the REAL install.js seam (readGsdRuntimeProfileResolver -> resolveEffort -> the WR-03 modelEmitted guard gating the model_reasoning_effort= TOML line on the model line's emission; max->xhigh translates at the emit seam via translateEffortForCodex while the resolver returns max verbatim) with the linchpin status and consequence-of-reopening, explicitly noting the ROADMAP rawSlotForRuntime label is a paraphrase with NO literal symbol; records the other settled decisions (D-08 floor, allowlist-as-outermost-gate, max-verbatim-then-translate, haiku-omits-effort, shape-not-count) marked 'Settled — do not reopen' with consequence-of-reopening (ROADMAP SC2,SC3 / QUAL-05, per D-04/D-03/D-07)"
    - "SPEC.md marks EVERY concrete enumeration as dated 'current as of 2026-06-12' advisory under ## Code Context with <!-- advisory --> (EFFORT_TOKENS members, the {claude,codex} allowlist, the 13 non-effort runtimes, the 20/21 init siblings, the 330 snapshot rows, the precedence-step numbers, and every path/symbol/line-number: parseModelEffort core.cjs~1242 / EFFORT_TOKENS~1221 / translateEffortForCodex~1278 / resolveReasoningEffortInternal~1628 with D-08 floor~1719 / RUNTIMES_WITH_REASONING_EFFORT model-catalog.cjs~97 / readGsdRuntimeProfileResolver install.js~1456 + resolveEffort~1520-1529 + WR-03 guard~2760-2787 + translate~2795) — the normative claim is always the SHAPE, never the literal values (shape normative, values dated per D-07; QUAL-03)"
    - "SPEC.md advances frontmatter Status Draft -> Ready with Confidence + Specced 2026-06-12 set, gated on QUAL-01..05, preserving the H1, the Depends on: SPEC-08 edge, and the tier-1 evidence line tests/feat-58-regression.test.cjs; and the INDEX.md SPEC-06 Feature-Status row is flipped Draft -> Ready, preserving the SPEC-08 -> SPEC-06 dependency edge"
  artifacts:
    - path: ".planning/spec/06-thinking-effort/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-06 (Purpose, Scope, 6-8 role-based 06-INV-M invariants, Acceptance Tests, settled Key Decisions incl. Codex emit linchpin, advisory Code Context) with Status: Ready"
      contains: "06-INV-1"
    - path: ".planning/spec/INDEX.md"
      provides: "SPEC-06 Feature-Status row advanced Draft -> Ready"
      contains: "SPEC-06"
  key_links:
    - from: ".planning/spec/06-thinking-effort/SPEC.md (Acceptance Tests table)"
      to: "the tier-1 oracle tests/feat-58-regression.test.cjs and the sibling unit oracles"
      via: "verbatim describe/test subtest names keyed on 06-INV-M routed to the closest oracle"
      pattern: "tests/feat-58-regression\\.test\\.cjs"
    - from: ".planning/spec/06-thinking-effort/SPEC.md (Invariants + section order)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme + status vocabulary)"
      via: "section order + invariant ID format conformance + Draft->Ready transition"
      pattern: "06-INV-[1-8]"
    - from: ".planning/spec/06-thinking-effort/SPEC.md (Key Decisions — Codex emit linchpin)"
      to: "bin/install.js WR-03 modelEmitted guard (advisory Code Context)"
      via: "named real seam readGsdRuntimeProfileResolver -> resolveEffort -> WR-03 gate (not the rawSlotForRuntime paraphrase)"
      pattern: "modelEmitted"
    - from: ".planning/spec/INDEX.md (SPEC-06 Feature-Status row)"
      to: ".planning/spec/06-thinking-effort/SPEC.md (frontmatter Status)"
      via: "status reconciliation — both read Ready after this phase"
      pattern: "SPEC-06 .*Ready"
---

<objective>
Author the full body of `.planning/spec/06-thinking-effort/SPEC.md` — a behavioral-contract
specification of GSD's per-agent thinking-effort feature, the most complex spec surface in the
v2.1.0-h milestone — so a reimplementer can rebuild the parser/resolver/catalog/wiring/Codex-emit
on a refactored upstream without reading the current source. The phase fills an existing stub
(frontmatter + empty 7-section skeleton from Phase 68); it does NOT modify `parseModelEffort`,
`resolveReasoningEffortInternal`, the catalog, the 20 `*_effort` init siblings, spawn-template wiring,
`tests/feat-58-regression.test.cjs`, or the 330-row golden snapshot — it NARRATES the shipped, GREEN
feature into the locked 7-section contract and advances Status `Draft -> Ready`. Unlike the single-oracle
sibling scanner specs (SPEC-05, SPEC-07), SPEC-06 has MULTIPLE tier-1 oracles: the parser, resolver,
D-08 floor, translate boundary, and wiring each have a dedicated unit test in addition to the golden
snapshot, so the Acceptance Tests table routes each invariant to its CLOSEST oracle rather than forcing
everything through the 330-row corpus.

Purpose: Capture the per-agent thinking-effort contract — the `parseModelEffort` semicolon parser
with its `EFFORT_TOKENS` allowlist and warn-once typo path; the unified `resolveReasoningEffortInternal`
precedence chain (allowlist gate -> per-agent override -> haiku carve-out -> slot effort -> user override
-> catalog slot -> runtime-tier fallback); the D-08 medium floor for bare `{claude,codex}` slots; the
static `RUNTIMES_WITH_REASONING_EFFORT={claude,codex}` allowlist as the absolute outermost gate; the
per-runtime omit/translate contract (13 non-effort runtimes -> null; `translateEffortForCodex` maps
`max -> xhigh`); the catalog `reasoning_effort` schema; the 20 `*_effort` init siblings; spawn-template
wiring; and the install.js Codex emit seam that ties the emitted effort line to the same resolution
source as the model line (WR-03, no silent divergence) — as a durable, move-proof contract (shape
normative; every literal set advisory `current as of 2026-06-12`), satisfying SPEC-06 and the shared
QUAL-01..05 bars so the spec reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, 6-8 role-based invariants (`06-INV-1`..`06-INV-N`;
7-role axis — parser / resolver precedence / D-08 floor as MUST / static allowlist gate / omit-translate
/ catalog+siblings / wiring+Codex-emit; final count and exact role boundaries Claude's Discretion per
D-01, keeping INV-floor and INV-gate SEPARATE from INV-precedence per D-03), an Acceptance Tests
traceability table routing each invariant to its closest real tier-1 oracle with verbatim subtest names
and no unflagged `[MISSING]` rows, settled Key Decisions (Codex emit linchpin per D-04, D-08 floor,
allowlist-as-gate, max-verbatim, haiku-omits-effort, shape-not-count), advisory Code Context with the
pinned line numbers, frontmatter Status `Ready` (Confidence + Specced 2026-06-12), and the INDEX.md
SPEC-06 row advanced to `Ready`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-75 source artifacts (locked decisions + narration map + locked template + the stub)
@.planning/phases/75-spec-06-thinking-effort/75-CONTEXT.md
@.planning/phases/75-spec-06-thinking-effort/75-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/06-thinking-effort/SPEC.md
@.planning/spec/INDEX.md

# Tier-1 normative source the spec narrates (read VERBATIM for subtest names + snapshot structure)
@tests/feat-58-regression.test.cjs
@tests/fixtures/golden-effort-snapshot.json

# Sibling unit oracles (read VERBATIM for closest-oracle describe/test names per invariant)
@tests/parse-model-effort.test.cjs
@tests/parse-model-effort-parity.test.cjs
@tests/feat-53-unified-effort-resolver.test.cjs
@tests/feat-53-config-sites-and-golden.test.cjs
@tests/phase-56-effort-wiring.test.cjs
@tests/feat-57-install-translation.test.cjs

# Closest worked reference — resolver/scanner-style spec (section shape, EARS phrasing, advisory marking,
# rule-normative-list-advisory pattern, lettered Key Decisions, multi-row-per-invariant Acceptance Tests)
@.planning/spec/07-citation-guard/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose / the INDEX.md status
cell — no code/test symbols are created; all system symbols are PRE-EXISTING and appear only under
advisory Code Context). The Phase-77 source-grounding pass MUST exclude these from drift verification:

- **Invariant IDs:** `06-INV-1`..`06-INV-N` (6-8 role-based invariants, final count and exact role
  boundaries Claude's Discretion per D-01). The 7-role axis (RESEARCH §"Per-Invariant Oracle Routing"
  recommends 7 as the natural fit, one per role):
  1. **parseModelEffort semicolon contract** — `model;effort` splits on `lastIndexOf(';')`; an
     `EFFORT_TOKENS` member populates `effort`; a non-token `;`-suffix is a typo (stripped from
     `model`, `effort` null, warn once); a bare label yields `{model: label, effort: null}`; a colon
     is never a delimiter.
  2. **Resolver precedence chain** — the ordered precedence in `resolveReasoningEffortInternal`
     (allowlist gate -> per-agent override -> haiku carve-out -> phase-type slot effort -> user
     `reasoning_effort` override -> catalog slot effort -> runtime-tier fallback); the resolver returns
     `max` VERBATIM (no clamp).
  3. **D-08 medium floor (MUST)** — bare `{claude,codex}` slots floor to `medium`; carve-outs
     (haiku exempt; `inherit`/unknown -> null; non-effort runtime -> null) belong to the SAME
     invariant. KEPT SEPARATE from INV-2 per D-03 (distinct MUST, distinct failure consequence).
  4. **Static runtime allowlist gate** — `RUNTIMES_WITH_REASONING_EFFORT={claude,codex}` is the
     absolute OUTERMOST gate; a non-`{claude,codex}` install with a `;effort` override still returns
     null — overrides CANNOT bypass it. KEPT SEPARATE from INV-2 per D-01.
  5. **Per-runtime omit/translate contract** — the 13 non-effort runtimes resolve to null (the
     `omitContract` rows); `translateEffortForCodex` maps `max -> xhigh`, passes `low|medium|high`
     through, `null|undefined -> null`. (D-04 testable boundary, traced to feat-58 TEST-03.)
  6. **Catalog schema + init siblings** — the catalog carries `reasoning_effort`; the 20 `*_effort`
     init siblings expose resolved effort to workflows via `resolveReasoningEffortInternal(cwd, agentType)`.
  7. **Spawn-template + Codex emit wiring** — resolved effort reaches `Agent()` spawns; the install.js
     Codex emit seam ties the effort line to the same source as the model line (WR-03).
  Claude's Discretion per D-01: final count (6/7/8); whether INV-6 splits catalog-schema from
  init-siblings; whether INV-7 splits spawn-wiring from Codex-emit; exact EARS pattern per invariant.
  HARD CONSTRAINT per D-03: INV-3 (floor) and INV-4 (gate) MUST stay SEPARATE from INV-2 (precedence).
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** SPEC.md frontmatter `**Status:**` advances `Draft -> Ready`; `**Confidence:**`
  (HIGH recommended per RESEARCH §Metadata — Claude's Discretion per D-01/CONTEXT) and
  `**Specced:** 2026-06-12` set. The `**Depends on:** SPEC-08` line and the
  `**Reimplementation evidence (tier-1 test):** tests/feat-58-regression.test.cjs` line are PRESERVED,
  not changed. The INDEX.md SPEC-06 Feature-Status row cell advances `Draft -> Ready`; the
  `SPEC-08 -> SPEC-06` dependency edge and the `Depends On` cell (`SPEC-08`) are PRESERVED.

PRE-EXISTING (advisory, NOT produced here — list under Code Context only):
`get-shit-done/bin/lib/core.cjs` — `EFFORT_TOKENS` (~line 1221, `new Set(['low','medium','high','xhigh','max'])`),
`parseModelEffort` (~line 1242), `translateEffortForCodex` (~line 1278, `max->xhigh`; pass-through;
`null|undefined->null`), `resolveReasoningEffortInternal` (~line 1628; D-08 floor `return 'medium'` at
~line 1719; haiku bareTier check `if (bareTier === 'haiku') return null` precedes the floor).
`get-shit-done/bin/lib/model-catalog.cjs` — `RUNTIMES_WITH_REASONING_EFFORT` (~line 97,
`new Set(['claude','codex'])`), `KNOWN_RUNTIMES` (~line 91, data-derived), the static-allowlist
rationale comment (~lines 93-96). `get-shit-done/bin/lib/init.cjs` — the `*_effort` siblings
(21 call sites / ~8-20 unique key names per RESEARCH §init.cjs; shape normative, count advisory).
`bin/install.js` — `readGsdRuntimeProfileResolver` (~line 1456) returning `resolveEffort(agentName)`
(~lines 1520-1529, WR-01 gate on `merged.runtime && probedProjectDir`); the WR-03 guard (~lines
2760-2787): `let modelEmitted = false`, and the `model_reasoning_effort =` line emitted ONLY when
`modelEmitted && runtimeResolver?.resolveEffort && runtimeResolver.runtime === 'codex'`;
`gsdTranslateEffortForCodex(...)` translation at ~line 2795; aliases `gsdTranslateEffortForCodex`
(~line 163) and `gsdResolveReasoningEffort` (~line 162). The golden snapshot
`tests/fixtures/golden-effort-snapshot.json` — `{generated, description, rows[] (330 entries:
{agent, profile, runtime, expectedModel, expectedEffort}), omitContract[] (13 entries:
{runtime, sampleProfile, sampleAgent}, asserting expectedEffort:null)}`.
NOTE per D-04 / RESEARCH Pitfall 2: the ROADMAP `rawSlotForRuntime` label is a PARAPHRASE — there is
NO literal `rawSlotForRuntime` symbol in source. The Codex emit Key Decision names the REAL seam
(`readGsdRuntimeProfileResolver` / `resolveEffort` / WR-03 `modelEmitted` guard) and explicitly says
the ROADMAP label is a paraphrase. Per D-05 / RESEARCH §"Per-Invariant Oracle Routing": the parser,
translate boundary, and wiring have dedicated unit oracles (parse-model-effort, feat-57, phase-56) and
need NOT route through the 330-row snapshot, so those are NOT `[MISSING]` rows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, 6-8 role-based 06-INV-M invariants, Acceptance Tests table</name>
  <files>.planning/spec/06-thinking-effort/SPEC.md</files>
  <read_first>
    - .planning/spec/06-thinking-effort/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose;
      PRESERVE frontmatter, H1 `# SPEC-06: Per-Agent Thinking Effort`, `**Depends on:** SPEC-08`, and
      the tier-1 evidence line `tests/feat-58-regression.test.cjs`)
    - tests/feat-58-regression.test.cjs (TIER-1 oracle — narrate VERBATIM. The describe/test names,
      confirmed in 75-RESEARCH §"Tier-1 and Sibling Oracle Test Names":
      describe `'TEST-01: static golden snapshot — post-D-08 resolver values'` with dynamic subtests
        `golden: ${row.agent}/${row.profile}/${row.runtime}` (one per row) and
        `omit contract: runtime=${row.runtime} → effort null` (one per omitContract entry);
      describe `'TEST-03: translateEffortForCodex boundary + per-runtime omit contract'` with subtests
        `'translateEffortForCodex("max") === "xhigh"'`,
        `'translateEffortForCodex passes through "low"'`, `'...passes through "medium"'`,
        `'...passes through "high"'`, `'translateEffortForCodex(null) === null'`,
        `'translateEffortForCodex(undefined) === null'`, and nested describe
        `'non-effort runtimes return null from resolveReasoningEffortInternal'` with dynamic subtest
        `resolveReasoningEffortInternal returns null for runtime=${runtime}` (one per non-effort runtime);
      describe `'TEST-04: antipattern guard — indexOf-as-boolean and bare includes substring collision'`
        with subtests for the indexOf-as-boolean and bare-includes guards (advisory; see Open Q2))
    - tests/fixtures/golden-effort-snapshot.json (TIER-1 oracle STRUCTURE — describe it, do NOT inline:
      `{generated, description, rows[], omitContract[]}`; each `rows` entry
      `{agent, profile, runtime, expectedModel, expectedEffort}` (post-D-08 values); `omitContract`
      asserts `expectedEffort: null` for each non-effort runtime. Counts: 330 rows / 13 omitContract,
      both dated advisory. omitContract runtimes (advisory): gemini, qwen, opencode, copilot, hermes,
      kilo, cline, cursor, windsurf, augment, trae, codebuddy, antigravity)
    - tests/parse-model-effort.test.cjs + tests/parse-model-effort-parity.test.cjs (INV-1 closest
      oracles — narrate VERBATIM top-level test names: `'parseModelEffort splits model;effort on the
      effort allowlist'`, `'...treats every allowlist token as a valid effort suffix'`, `'...never
      treats a colon as a delimiter'`, `'...returns bare model with null effort (backward-compatible)'`,
      `'...splits on lastIndexOf(";") so embedded semicolons stay in model'`; parity describes
      `'parseModelEffort CJS parity (shared fixture)'`, `'...allowlist parity'`,
      `'...warning-path parity (WR-02)'`)
    - tests/feat-53-unified-effort-resolver.test.cjs (INV-2 precedence + INV-4 allowlist-gate oracles —
      narrate VERBATIM describe names: `'Phase 53: claude runtime emits slot effort'`,
      `'Phase 53: codex runtime — slot effort over per-tier fallback (RESOLVE-03)'`,
      `'Phase 53: non-{claude,codex} runtime hard no-op (RESOLVE-05, D-02)'` (the INV-4 gate oracle),
      `'Phase 53: no runtime set → claude path; bare config → null'`,
      `'Phase 53: inherit paths → null (RESOLVE-06)'`,
      `'Phase 53: malformed effort token degrades gracefully (CONFIG-04/D-05)'`)
    - tests/feat-53-config-sites-and-golden.test.cjs (INV-3 D-08 floor oracle — narrate VERBATIM
      describe `'D-08: cross-resolver golden snapshot — bare config back-compat + same-slot invariant'`
      and `'#3023 same-slot fixture: model_profile=inherit + models.execution=opus on codex'`)
    - tests/phase-56-effort-wiring.test.cjs (INV-6/INV-7 wiring oracle — narrate VERBATIM describe
      `'phase-56 GAP A: Group A init-fed workflows carry effort token variables'`,
      `'phase-56 GAP B: Group B standalone-resolve sites carry resolve-model-effort capture lines'`,
      `'phase-60 Group B effort wiring: newly-covered workflows'`)
    - tests/feat-57-install-translation.test.cjs (INV-5/INV-7 install-emit oracle — narrate VERBATIM
      describe `'Phase 57: translateEffortForCodex translations (INSTALL-01)'`,
      `'Phase 57: resolver returns max verbatim on both runtimes (D-03 / RESOLVE-04)'`,
      `'Phase 57: haiku tier omits effort entirely (INSTALL-02 / D-03)'`,
      `'Phase 57: Codex TOML emit via generateCodexAgentToml (INSTALL-02)'`)
    - .planning/phases/75-spec-06-thinking-effort/75-RESEARCH.md (THE narration map — §"Pinned Source
      Seams" the exact symbols/line numbers; §"Pattern: Per-Invariant Oracle Routing" the seven
      invariant->oracle clusters; §"Tier-1 and Sibling Oracle Test Names" the verbatim describe/test
      names; §"Common Pitfalls" 1-5 — esp. Pitfall 1 (haiku carve-out PRECEDES the floor), Pitfall 3
      (D-08 floor is MUST not SHOULD), Pitfall 5 (resolver returns max VERBATIM; max->xhigh happens
      only at the install.js emit boundary); §"Open Questions" 1 (7 invariants natural fit) and 2
      (TEST-04 antipattern guard placement is Claude's Discretion — advisory note or fold into wiring))
    - .planning/phases/75-spec-06-thinking-effort/75-CONTEXT.md (LOCKED decisions D-01..D-07 — esp.
      D-01 6-8 role-based invariants, D-03 D-08-floor-as-MUST kept separate from precedence,
      D-05 golden snapshot tier-1 / init.cjs tier-2 with structure described, D-07 shape-normative)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template §1, NN-INV-M scheme §2 — `06-INV-M`,
      EARS patterns + RFC 2119 strength §4, the `01-INV-1` example invariant shape, the Acceptance
      Tests table columns `Invariant | Test File | Subtest / Assertion Shape` §5, source-of-truth
      hierarchy §4 tier-1=test)
    - .planning/spec/07-citation-guard/SPEC.md (CLOSEST worked reference — invariant prose +
      "Consequence of violating this invariant:" closer, the "X is advisory supporting detail, current
      as of 2026-06-12" rule-as-supporting-list pattern, the Acceptance Tests multi-row-per-invariant
      layout, and the dynamic-per-file corpus-describe-row pattern — SPEC-06 mirrors the dynamic-row
      pattern for the golden snapshot's per-row/per-omitContract subtests)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (state the correct behavior directly; the spec sits beside the
    fork's affirmative-framing standard and models it). Do NOT inline fenced code blocks or full file
    contents; name identifiers, subtest strings, symbol names, and section headers in prose. EARS
    "Unwanted-behavior" invariants (`If X then the system SHALL NOT Y`) are acceptable normative spec
    language and are not prompt directives.

    Purpose (one paragraph): state the "why" of per-agent thinking effort. The feature gives each
    spawned agent a runtime-appropriate reasoning-effort level resolved from a single source so model
    and effort never diverge. State the connected surface at a high level: the `parseModelEffort`
    semicolon parser, the unified `resolveReasoningEffortInternal` precedence chain with the D-08 medium
    floor, the static `{claude,codex}` runtime allowlist, the per-runtime omit/translate contract, the
    catalog `reasoning_effort` schema, the `*_effort` init siblings, spawn-template wiring, and the
    install.js Codex emit seam. State what breaks if absent or wrong: without the D-08 floor, bare
    `{claude,codex}` slots emit no effort and silently drop every such agent to the provider default,
    erasing the per-agent effort dimension the milestone exists to add; without the allowlist gate,
    effort leaks to runtimes that cannot consume it; without the WR-03 emit tie, the Codex
    `model_reasoning_effort` TOML line can resolve from a different source than the model line and the
    two drift. Cite `tests/feat-58-regression.test.cjs` as the behavioral authority (source-of-truth
    tier 1 per 00-CONVENTIONS.md §4) and note `init.cjs`'s `resolveReasoningEffortInternal` is the
    tier-2 implementation it pins. Note this phase SPECIFIES a shipped, GREEN feature — no behavior
    change.

    Scope: two bullet lists.
      In scope (one bullet per behavioral role): the `parseModelEffort` semicolon parser (input/output/
      error modes, including the warn-once typo path and the colon-is-never-a-delimiter rule); the
      unified resolver precedence chain (allowlist gate -> per-agent override -> haiku carve-out ->
      slot effort -> user override -> catalog slot -> runtime-tier fallback) with the resolver returning
      `max` verbatim; the D-08 medium floor; the static `RUNTIMES_WITH_REASONING_EFFORT` allowlist as
      the absolute outermost gate; the per-runtime omit/translate contract (non-effort runtimes -> null;
      `translateEffortForCodex` `max -> xhigh`); the catalog `reasoning_effort` schema; the `*_effort`
      init siblings; spawn-template wiring; and the install.js Codex emit seam. Mark every literal set
      (the EFFORT_TOKENS members, the `{claude,codex}` allowlist, the 13 non-effort runtimes, the
      ~20 init siblings, the 330 snapshot rows, the precedence-step numbers) as a dated advisory
      enumeration `current as of 2026-06-12` (per D-07) where it appears — the normative claim is the
      SHAPE (a fixed effort-token allowlist, a two-runtime effort gate, a per-runtime omit contract,
      a complete-coverage init-sibling set, a full agent×profile×runtime matrix), not the literal values.
      Out of scope (each with a one-line rationale): (1) **CATALOG-02 user-handover boundary** — how
      users author `model;effort` labels in their own catalog / the handover UX is an adjacent concern
      placed out of scope per ROADMAP and D-06; narrate the feature's behavior, not the authoring UX.
      (2) **behavior change** — this phase specifies the shipped, GREEN feature; it does not alter the
      parser/resolver/catalog/wiring. (3) **the TEST-04 antipattern guard as a feature behavior** —
      TEST-04 guards test-code correctness, not feature behavior; reference it as supporting evidence
      under Code Context or fold it into the wiring invariant (Claude's Discretion per RESEARCH Open Q2),
      not as a standalone feature invariant.

    Invariants: author 6-8 numbered role-based EARS statements with RFC 2119 strength, IDs
    `06-INV-1`..`06-INV-N` (NN-INV-M format per 00-CONVENTIONS.md §2). 7 invariants is the natural fit
    (one per role, per RESEARCH Open Q1) — Claude's Discretion to use 6, 7, or 8 and to merge/split
    INV-6 (catalog vs siblings) and INV-7 (wiring vs Codex-emit). HARD CONSTRAINT per D-03: keep the
    D-08 floor invariant and the static-allowlist-gate invariant SEPARATE from the precedence-chain
    invariant — each is a distinct MUST with a distinct failure consequence and a distinct subtest. Each
    invariant is a single falsifiable claim mapping to a real subtest cluster (RESEARCH §"Per-Invariant
    Oracle Routing"). Each invariant carries a "Consequence of violating this invariant:" closer
    (mirror SPEC-07). Author the role axis:
      - **06-INV-1 parseModelEffort semicolon contract** — when the resolver parses a `model;effort`
        label, the system MUST split on the LAST `;`, populate `effort` only when the suffix is a member
        of the effort-token allowlist, strip an unrecognized `;`-suffix to recover a bare model with
        `effort` null and warn once, return `{model: label, effort: null}` for a bare label, and MUST
        NOT treat a colon as a delimiter. Mark `EFFORT_TOKENS`/`parseModelEffort` advisory.
      - **06-INV-2 resolver precedence chain** — the system MUST resolve effort by an ordered precedence
        (allowlist gate -> per-agent override -> haiku carve-out -> phase-type slot -> user override ->
        catalog slot -> runtime-tier fallback) and MUST return `max` VERBATIM with no resolver clamp
        (per Pitfall 5; the `max -> xhigh` translation is INV-5's emit-boundary behavior, NOT the
        resolver's). The haiku carve-out PRECEDES the floor (Pitfall 1).
      - **06-INV-3 D-08 medium floor (MUST)** — when a bare `{claude,codex}` slot resolves no effort
        through steps 1-5, the system MUST floor it to `medium`; haiku is exempt, and `inherit`/unknown
        slots and non-effort runtimes MUST still return null (carve-outs in the SAME invariant per D-03).
        Consequence of omission stated explicitly (every such agent silently drops to the provider
        default, erasing the per-agent effort dimension the milestone exists to add — per D-03/Pitfall 3).
      - **06-INV-4 static runtime allowlist gate** — the system MUST treat the static
        `RUNTIMES_WITH_REASONING_EFFORT` set as the absolute outermost gate; if the install runtime is
        not in the set then the system MUST return null even when a `;effort` override is present —
        overrides cannot bypass the gate.
      - **06-INV-5 per-runtime omit/translate contract** — the system MUST resolve every non-effort
        runtime to null (the `omitContract`), and at the Codex emit boundary MUST translate `max` to
        `xhigh`, pass `low|medium|high` through unchanged, and map `null|undefined` to null.
      - **06-INV-6 catalog schema + init siblings** — the catalog MUST carry `reasoning_effort` per
        effort-supporting runtime/tier entry, and the init layer MUST expose resolved effort to
        workflows via the `*_effort` siblings calling `resolveReasoningEffortInternal(cwd, agentType)`.
      - **06-INV-7 spawn-template + Codex emit wiring** — resolved effort MUST reach agent spawns, and
        the install.js Codex emit MUST gate the `model_reasoning_effort` TOML line on the model line's
        emission (WR-03) so the two lines share one resolution source and cannot diverge.

    Acceptance Tests: author the traceability table with the locked columns `Invariant | Test File |
    Subtest / Assertion Shape`, keyed on `06-INV-M`. Route EACH invariant to its CLOSEST tier-1 oracle
    (RESEARCH §"Per-Invariant Oracle Routing"), using VERBATIM describe/test strings:
      - 06-INV-1 -> tests/parse-model-effort.test.cjs (the five top-level parser tests) +
        tests/parse-model-effort-parity.test.cjs (the three parity describes).
      - 06-INV-2 -> tests/feat-53-unified-effort-resolver.test.cjs (the slot-effort + inherit-path +
        malformed-degrades describes) + tests/feat-57-install-translation.test.cjs
        (`'...resolver returns max verbatim on both runtimes (D-03 / RESOLVE-04)'` for the max-verbatim
        claim).
      - 06-INV-3 -> tests/feat-53-config-sites-and-golden.test.cjs (the D-08 cross-resolver golden
        describe) + tests/feat-58-regression.test.cjs TEST-01 (golden rows carry post-D-08 floored
        values).
      - 06-INV-4 -> tests/feat-53-unified-effort-resolver.test.cjs
        (`'Phase 53: non-{claude,codex} runtime hard no-op (RESOLVE-05, D-02)'`) + the TEST-01
        omitContract rows.
      - 06-INV-5 -> tests/feat-58-regression.test.cjs TEST-03 (the six translate subtests + the nested
        non-effort-runtime-returns-null dynamic subtest, one per non-effort runtime).
      - 06-INV-6 -> tests/phase-56-effort-wiring.test.cjs (GAP A/GAP B describes) +
        tests/feat-58-regression.test.cjs TEST-01 (the golden agent×profile×runtime matrix).
      - 06-INV-7 -> tests/feat-57-install-translation.test.cjs (the Codex TOML emit describe) +
        tests/phase-56-effort-wiring.test.cjs (spawn-template wiring describes).
    Describe the golden snapshot STRUCTURE in a sentence above or beside the table (per D-05/SC3):
    `{generated, description, rows[], omitContract[]}`; each `rows` entry `{agent, profile, runtime,
    expectedModel, expectedEffort}`; `omitContract` asserts `expectedEffort: null` for each non-effort
    runtime. State that the 330-row count and 13-omitContract count are advisory `current as of
    2026-06-12`. Every MUST-level invariant has at least one real subtest row — there MUST be NO
    unflagged `[MISSING]` rows (the parser/translate/wiring each have dedicated unit oracles per D-05,
    so none route through the snapshot as a fallback). Optionally cite TEST-04 as supporting evidence
    for whichever invariant covers the effort-token surface (RESEARCH Open Q2).
  </action>
  <acceptance_criteria>
    - SPEC.md `## Purpose` is one paragraph stating the per-agent thinking-effort "why", names the
      connected surface, states what breaks without the D-08 floor / allowlist gate / WR-03 emit tie,
      and cites tests/feat-58-regression.test.cjs as tier-1 + init.cjs as tier-2.
    - SPEC.md `## Scope` has In-scope and Out-of-scope bullet lists; Out of scope includes the
      CATALOG-02 user-handover boundary (per D-06) with a one-line rationale; every literal set is
      marked dated-advisory `current as of 2026-06-12` (shape normative per D-07).
    - SPEC.md `## Invariants` states between 6 and 8 numbered EARS invariants with IDs matching
      `06-INV-[1-8]` (NN-INV-M format), each with RFC 2119 MUST/SHOULD/MAY strength and a
      "Consequence of violating this invariant:" closer.
    - The D-08 medium floor is a MUST-level invariant stating the omission consequence, and the
      static-allowlist gate is a SEPARATE invariant from the precedence-chain invariant (per D-03):
      grep shows three distinct invariant IDs covering precedence, floor, and gate respectively.
    - SPEC.md `## Acceptance Tests` has a table keyed on `06-INV-M` with the columns
      `Invariant | Test File | Subtest / Assertion Shape`; every MUST-level invariant has at least one
      row; there are NO unflagged `[MISSING]` rows.
    - Every cited subtest/describe string is present VERBATIM in its named test file:
      `grep -F '<subtest string>' tests/<file>.cjs` returns a hit for each Acceptance Tests row
      (spot-check at minimum the TEST-01/TEST-03 strings in tests/feat-58-regression.test.cjs and the
      `'Phase 53: non-{claude,codex} runtime hard no-op (RESOLVE-05, D-02)'` string in
      tests/feat-53-unified-effort-resolver.test.cjs).
    - The golden-snapshot STRUCTURE ({generated, description, rows[] with
      {agent,profile,runtime,expectedModel,expectedEffort}, omitContract[] asserting null}) is described
      in prose, not just cited.
    - No fenced code block (```) appears in the authored sections; identifiers/regex names/subtest
      strings are named in prose or inline code spans only.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE '06-INV-[1-8]' .planning/spec/06-thinking-effort/SPEC.md) -ge 6 && grep -F 'TEST-03: translateEffortForCodex boundary' tests/feat-58-regression.test.cjs && grep -F 'Phase 53: non-{claude,codex} runtime hard no-op' tests/feat-53-unified-effort-resolver.test.cjs && ! grep -q '\[MISSING' .planning/spec/06-thinking-effort/SPEC.md && echo PASS</automated>
  </verify>
  <done>
    `## Purpose`, `## Scope`, `## Invariants` (6-8 `06-INV-M` EARS invariants with the D-08 floor and
    the allowlist gate as separate MUSTs from precedence), and `## Acceptance Tests` (table keyed on
    `06-INV-M`, every MUST routed to a verbatim real subtest, no unflagged `[MISSING]` rows, golden
    snapshot structure described) are authored in the locked order with no fenced code blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Key Decisions + advisory Code Context, flip Status Draft -> Ready, sync INDEX.md</name>
  <files>.planning/spec/06-thinking-effort/SPEC.md, .planning/spec/INDEX.md</files>
  <read_first>
    - .planning/spec/06-thinking-effort/SPEC.md (the Purpose/Scope/Invariants/Acceptance-Tests sections
      authored in Task 1 — author `## Key Decisions` and `## Code Context` BELOW them in the locked
      order; then flip the frontmatter `**Status:**` and set `**Confidence:**` + `**Specced:**`)
    - .planning/phases/75-spec-06-thinking-effort/75-RESEARCH.md (§"D-04 Linchpin — The Real Codex Emit
      Seam" the WR-03 `modelEmitted` guard at install.js ~2784-2787 gating the `model_reasoning_effort =`
      line on `modelEmitted && runtimeResolver?.resolveEffort && runtimeResolver.runtime === 'codex'`,
      `gsdTranslateEffortForCodex` at ~2795, aliases at ~162-163; §"core.cjs — Exact Line Numbers" the
      pinned symbols; §"State of the Art" the five settled design transitions; §"Common Pitfalls" 2
      (rawSlotForRuntime is a paraphrase — no literal symbol) and 5 (resolver returns max verbatim))
    - .planning/phases/75-spec-06-thinking-effort/75-CONTEXT.md (LOCKED decisions — D-04 Codex linchpin
      as Key Decision + INV-5 testable boundary, D-03 D-08 floor consequence, D-07 shape-not-count;
      §"Specific Ideas" the four non-obvious surfaces (D-08 floor as deliberate amendment; allowlist
      gate outermost+absolute; haiku omits effort and precedes floor; max translates to xhigh at emit))
    - .planning/spec/00-CONVENTIONS.md (§6 Key Decisions entry shape — decision statement + one-sentence
      rationale + "Settled — do not reopen." + consequence of reopening; §7 Code Context ADVISORY-ONLY
      with `<!-- advisory -->` marker; §3 status vocabulary Draft|Ready|Implemented|Verified and the
      "MUST NOT advance to Ready unless QUAL-01..05 satisfied" rule)
    - .planning/spec/07-citation-guard/SPEC.md (CLOSEST worked reference — the lettered Key Decision
      format `### (a) ... (D-NN)` with Rationale + "Settled — do not reopen." + "Consequence of
      reopening:" closer; the Code Context layout — `<!-- advisory -->` section header, the dated
      "current as of 2026-06-12" preamble, the symbol/what-it-does/line tables, and the advisory
      cross-reference table)
    - .planning/spec/INDEX.md (the SPEC-06 Feature-Status row to flip Draft -> Ready; PRESERVE the
      `SPEC-08 -> SPEC-06` dependency edge and the SPEC-06 `Depends On` cell `SPEC-08`; mirror how the
      SPEC-07 row was advanced to Ready)
  </read_first>
  <action>
    Author `## Key Decisions` then `## Code Context` (in the locked order, below the Task-1 sections),
    then flip the frontmatter Status and sync INDEX.md. Affirmative framing; no fenced code blocks in
    the spec body (name symbols/line numbers in prose or inline code spans).

    Key Decisions — author settled decisions in the lettered format from SPEC-07
    (`### (a) <title> (D-NN)` with a decision statement, a one-sentence Rationale, "Settled — do not
    reopen.", and a "Consequence of reopening:" closer). Author at minimum:
      (a) **Codex emit fix — correctness linchpin (D-04).** The decision: the Codex emit MUST tie the
        emitted `reasoning_effort` line to the SAME resolution source as the model line (WR-03, no
        silent divergence), and `max` translates to the Codex-specific `xhigh` tier at the emit seam.
        Name the REAL seam: `readGsdRuntimeProfileResolver` returns `resolveEffort(agentName)`; the WR-03
        guard tracks `modelEmitted` and emits `model_reasoning_effort =` ONLY when
        `modelEmitted && runtimeResolver?.resolveEffort && runtimeResolver.runtime === 'codex'`;
        `gsdTranslateEffortForCodex` applies `max -> xhigh` at the boundary. State EXPLICITLY that the
        ROADMAP `rawSlotForRuntime` label is a PARAPHRASE with NO literal symbol in source (per D-04 /
        RESEARCH Pitfall 2). Mark this the correctness linchpin. Consequence of reopening: model and
        effort emit paths resolve from different slots and silently diverge, producing a
        `model_reasoning_effort` TOML line that does not match the emitted model.
      (b) **D-08 medium floor (D-03 / D-08 amendment 2026-06-04).** The decision: bare `{claude,codex}`
        slots floor to `medium` (a deliberate milestone amendment, not a provider default). Consequence
        of reopening: every bare-slot agent silently drops to the provider default, erasing the
        per-agent effort dimension.
      (c) **Static allowlist as the outermost absolute gate (RESOLVE-01/D-07).** The decision:
        `RUNTIMES_WITH_REASONING_EFFORT` is a static literal, not a data-derived scan, and is the
        absolute outer gate. Consequence of reopening: a data-derived scan auto-admits any runtime that
        gains a `reasoning_effort` field, silently leaking effort to unsupported runtimes.
      (d) **`max` returned verbatim by the resolver; translated only at the emit boundary
        (D-03/RESOLVE-04).** Consequence of reopening: clamping `max` in the resolver loses the GSD/Codex
        `max` tier before the emit seam can translate it.
      (e) **Haiku omits effort on every runtime, and the carve-out precedes the floor (Pitfall 1).**
        Consequence of reopening: a haiku slot with a catalog `reasoning_effort` value would emit effort
        the feature intends to suppress.
      (f) **Shape is normative; concrete enumerations are dated advisory (D-07).** Consequence of
        reopening: the spec rots on every catalog/runtime/agent/test edit and stops being move-proof.
    Mark every ROADMAP-mandated decision "Settled — do not reopen."

    Code Context — author under a `<!-- advisory -->` section with a dated "current as of 2026-06-12"
    preamble stating no normative claim depends on these paths/symbols (move-proofing for the upstream
    refactor). Provide symbol/what-it-does/line tables (mirror SPEC-07's Code Context tables):
      - `get-shit-done/bin/lib/core.cjs` — `EFFORT_TOKENS` (~1221), `parseModelEffort` (~1242),
        `translateEffortForCodex` (~1278), `resolveReasoningEffortInternal` (~1628) with the D-08 floor
        `return 'medium'` (~1719) and the haiku bareTier check that precedes it.
      - `get-shit-done/bin/lib/model-catalog.cjs` — `RUNTIMES_WITH_REASONING_EFFORT` (~97,
        `{claude,codex}`), `KNOWN_RUNTIMES` (~91), the static-allowlist rationale comment (~93-96), and
        the catalog `reasoning_effort` schema (per effort-supporting runtime/tier entry).
      - `get-shit-done/bin/lib/init.cjs` — the `*_effort` siblings calling
        `resolveReasoningEffortInternal(cwd, agentType)` (the "20 init siblings" / ~21 call sites; mark
        the count advisory, the complete-coverage SHAPE normative).
      - `bin/install.js` — `readGsdRuntimeProfileResolver` (~1456), `resolveEffort` (~1520-1529, WR-01
        gate), the WR-03 `modelEmitted` guard (~2760-2787), `gsdTranslateEffortForCodex` translation
        (~2795), aliases `gsdTranslateEffortForCodex` (~163) / `gsdResolveReasoningEffort` (~162).
      - `tests/fixtures/golden-effort-snapshot.json` — the `{generated, description, rows[],
        omitContract[]}` structure; 330 rows / 13 omitContract entries (dated advisory).
      - Dated advisory enumerations: EFFORT_TOKENS `{low,medium,high,xhigh,max}`, the `{claude,codex}`
        allowlist, the 13 non-effort runtimes (gemini, qwen, opencode, copilot, hermes, kilo, cline,
        cursor, windsurf, augment, trae, codebuddy, antigravity), and the precedence-step numbering —
        all marked `current as of 2026-06-12`, with the SHAPE called out as the normative claim. You may
        abbreviate the 20-sibling / 330-row enumerations to representative classes (Claude's Discretion
        per D-07).
      - Optionally a one-line advisory note for TEST-04 (antipattern guard, governs test-code
        correctness not feature behavior — RESEARCH Open Q2).

    Status + Confidence + Specced: in the SPEC.md frontmatter, advance `**Status:** Draft` to
    `**Status:** Ready`, set `**Confidence:** High` (or your justified value per RESEARCH §Metadata),
    and set `**Specced:** 2026-06-12`. PRESERVE the H1, `**ID:** 06`, `**Requirement:** SPEC-06`,
    `**Depends on:** SPEC-08`, and `**Reimplementation evidence (tier-1 test):**
    tests/feat-58-regression.test.cjs`. Confirm QUAL-01..05 are met before flipping (numbered EARS
    invariants; complete traceability table; advisory markers on Code Context; tier-1 test cited; Key
    Decisions settled).

    INDEX.md: advance the SPEC-06 Feature-Status row cell `Draft -> Ready` (mirror how the SPEC-07 row
    reads Ready). PRESERVE the `SPEC-08 -> SPEC-06` dependency edge and the SPEC-06 `Depends On` cell
    (`SPEC-08`). Change ONLY the SPEC-06 status cell.
  </action>
  <acceptance_criteria>
    - SPEC.md `## Key Decisions` contains a settled decision naming the REAL Codex emit seam: it
      mentions `readGsdRuntimeProfileResolver`, `resolveEffort`, and the WR-03 `modelEmitted` guard, and
      explicitly states the ROADMAP `rawSlotForRuntime` label is a paraphrase with no literal symbol.
    - SPEC.md `## Key Decisions` records the D-08 floor, the static-allowlist-as-outermost-gate, the
      max-verbatim-then-translate, the haiku-omits-effort, and the shape-not-count decisions; every
      entry carries "Settled — do not reopen." and a consequence-of-reopening clause.
    - SPEC.md `## Code Context` is marked `<!-- advisory -->`, carries a dated "current as of
      2026-06-12" preamble, and lists the pinned symbols with their advisory line numbers (core.cjs
      ~1221/~1242/~1278/~1628/~1719; model-catalog.cjs ~97; install.js ~1456/~2760-2787/~2795); every
      concrete enumeration is marked dated-advisory and the SHAPE is named as the normative claim.
    - SPEC.md frontmatter `**Status:**` reads `Ready`; `**Confidence:**` and `**Specced:** 2026-06-12`
      are set; the H1, `**Depends on:** SPEC-08`, and the tier-1 evidence line are unchanged.
    - The six locked section headers appear exactly once each in the locked order (Purpose, Scope,
      Invariants, Acceptance Tests, Key Decisions, Code Context) — no section drift.
    - INDEX.md SPEC-06 Feature-Status row reads `Ready`; the `SPEC-08 -> SPEC-06` edge and the SPEC-06
      `Depends On: SPEC-08` cell are preserved (grep confirms both the Ready status and the SPEC-08 edge).
    - No fenced code block (```) appears in the authored SPEC.md sections.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^\*\*Status:\*\* Ready' .planning/spec/06-thinking-effort/SPEC.md && grep -q 'modelEmitted' .planning/spec/06-thinking-effort/SPEC.md && grep -q 'rawSlotForRuntime' .planning/spec/06-thinking-effort/SPEC.md && grep -q '<!-- advisory -->' .planning/spec/06-thinking-effort/SPEC.md && grep -q '\*\*Depends on:\*\* SPEC-08' .planning/spec/06-thinking-effort/SPEC.md && grep -qE 'SPEC-06.*Ready' .planning/spec/INDEX.md && echo PASS</automated>
  </verify>
  <done>
    `## Key Decisions` (Codex emit linchpin naming the real WR-03 seam and flagging the rawSlotForRuntime
    paraphrase, plus the D-08 floor / allowlist-gate / max-verbatim / haiku / shape-not-count decisions,
    all "Settled — do not reopen." with consequences) and `## Code Context` (advisory, dated, pinned line
    numbers, enumerations marked advisory) are authored; SPEC.md Status reads Ready with Confidence +
    Specced 2026-06-12 and preserved Depends-on/H1/tier-1 lines; INDEX.md SPEC-06 row reads Ready with
    the SPEC-08 edge preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase authors a single Markdown specification file under `.planning/spec/` and flips two status cells. It adds no executable code, parses no input, opens no network/file-system/auth surface, and changes no shipped behavior. The narrated feature (`parseModelEffort`/resolver/install-emit) already ships and is GREEN; the spec describes it but does not run it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | `.planning/spec/06-thinking-effort/SPEC.md` content (advisory line numbers / enumerations drift from source) | accept | Low impact, non-executable: every path/symbol/count is explicitly marked `<!-- advisory -->` "current as of 2026-06-12" per D-07, so a stale value is informational only and no normative claim depends on it; Phase 77 cross-spec review re-validates. |
| T-75-02 | Information Disclosure | secrets/PII in authored prose | accept | The spec narrates public source symbols and test names only; no credentials, tokens, env-var values, or PII are introduced. The Codex emit Key Decision references symbol names and TOML key names, not secret values. |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No package installs occur in this phase (spec authoring only); RESEARCH §"Package Legitimacy Audit" confirms "Not applicable — this phase installs no external packages." No `T-75-SC` legitimacy checkpoint is required. |

No `high`-severity threats are introduced: authoring a non-executable Markdown spec creates no input,
network, auth, or supply-chain surface. ASVS L1 categories do not apply (RESEARCH §"Security Domain":
SKIPPED — spec-authoring phase with no security-relevant code changes).
</threat_model>

<verification>
- `## Purpose`, `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`
  appear exactly once each in the locked order in `.planning/spec/06-thinking-effort/SPEC.md`
  (no section drift — Phase 77 rejects drift).
- 6-8 invariants with IDs matching `06-INV-[1-8]`; the D-08 floor and the static-allowlist gate are
  distinct MUST invariants, SEPARATE from the precedence-chain invariant (per D-03).
- The Acceptance Tests table is keyed on `06-INV-M` with columns `Invariant | Test File | Subtest /
  Assertion Shape`, has no unflagged `[MISSING]` rows, and every cited subtest string is present
  verbatim in its named test file (`grep -F` confirms the TEST-01/TEST-03 strings in
  `tests/feat-58-regression.test.cjs` and the resolver/parser/wiring strings in their files).
- The golden-snapshot structure (`{generated, description, rows[], omitContract[]}`) is described, the
  330-row / 13-omitContract counts are dated advisory, and the snapshot is named tier-1 with init.cjs
  named tier-2 (per SC3).
- Key Decisions names the real WR-03 `modelEmitted` seam and flags `rawSlotForRuntime` as a paraphrase;
  every Key Decision is "Settled — do not reopen." with a consequence-of-reopening clause.
- Code Context is `<!-- advisory -->`, dated `current as of 2026-06-12`, and every enumeration/path/
  symbol/line-number is advisory with the SHAPE called out as the normative claim (per D-07; QUAL-03).
- SPEC.md frontmatter Status reads `Ready` with Confidence + Specced 2026-06-12; H1, `Depends on:
  SPEC-08`, and the tier-1 evidence line are preserved. INDEX.md SPEC-06 row reads `Ready` with the
  `SPEC-08 -> SPEC-06` edge preserved.
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` is GREEN (the spec body must not break any existing
  test; this phase touches only Markdown).
- No fenced code blocks appear in the authored SPEC.md sections.
</verification>

<success_criteria>
SC1 (SPEC-06): `06-thinking-effort/SPEC.md` specifies the full per-agent thinking-effort surface —
parseModelEffort semicolon parser, the unified `{claude,codex}` resolver precedence chain (override ->
slot -> D-08 medium floor), the static runtime allowlist, the per-runtime behavior matrix, the catalog
schema, the `*_effort` init siblings, spawn-template wiring, the install.js Codex emit seam, the Codex
fix linchpin, and the CATALOG-02 user-handover boundary placed out of scope.

SC2 (SPEC-06, QUAL-01): the D-08 medium floor is a MUST-level invariant (not optional) with the
consequence of omission stated; the Codex emit fix is named as the correctness linchpin (the real
WR-03 seam, with `rawSlotForRuntime` flagged as a paraphrase).

SC3 (QUAL-02-05): invariants cite the 330-row golden snapshot as tier-1 and `init.cjs` as tier-2; the
snapshot's structure is described, not just its existence; the traceability table is complete; paths
are advisory-marked; Key Decisions are settled. The spec satisfies QUAL-01..05 and advances Status
`Draft -> Ready`; INDEX.md SPEC-06 row matches.
</success_criteria>

<output>
Create `.planning/phases/75-spec-06-thinking-effort/75-01-SUMMARY.md` when done.
</output>
