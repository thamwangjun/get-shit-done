---
phase: 66-citation-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/checkpoints.md
  - get-shit-done/references/git-integration.md
  - get-shit-done/references/model-profiles.md
  - get-shit-done/references/mvp-concepts.md
  - get-shit-done/references/planner-graphify-auto-update.md
  - get-shit-done/references/planner-human-verify-mode.md
  - get-shit-done/references/planning-config.md
  - get-shit-done/references/scout-codebase.md
  - get-shit-done/references/thinking-partner.md
  - get-shit-done/references/worktree-path-safety.md
autonomous: true
requirements: [CITE-06, CITE-07, CITE-08, CITE-09]

must_haves:
  truths:
    - "D-01: Per-file executor edits — no regex script is created"
    - "D-08: YAML frontmatter blocks (between leading --- delimiters) are untouched"
    - "D-09: Fenced code blocks (triple-backtick) are untouched"
    - "D-10: Allowlisted tokens (#1, #2, #45, #123, hex colors, heading markers) are preserved"
    - "All 10 get-shit-done/references/ files are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences"
    - "Cleaned prose contains no double spaces, empty parentheses, or dangling connectors at sentence boundaries"
    - "Guard test reports zero ✖ lines for get-shit-done/references/ files after cleanup"
  artifacts:
    - path: "get-shit-done/references/checkpoints.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/git-integration.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/model-profiles.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/mvp-concepts.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/planner-graphify-auto-update.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/planner-human-verify-mode.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/planning-config.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/scout-codebase.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/thinking-partner.md"
      provides: "Cleaned reference file"
    - path: "get-shit-done/references/worktree-path-safety.md"
      provides: "Cleaned reference file"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "get-shit-done/references/*.md"
      via: "directory scan + per-file ✖ enumeration"
      pattern: "get-shit-done/references/.*\\.md"
---

<objective>
Remove every issue/PR citation from the 10 failing files in `get-shit-done/references/` and repair the surrounding prose. References are prompt fragments injected into workflows via Eta `include()`, so the cleaned output is what downstream agents actually see at runtime.

Purpose: Close CITE-06 / CITE-07 / CITE-08 / CITE-09 for the `get-shit-done/references/` directory so the Phase 65 guard test goes GREEN for this slice.

Output: 10 modified reference `.md` files committed atomically. Guard test reports zero ✖ lines for `get-shit-done/references/` paths.
</objective>

<context>
!`cat .planning/phases/66-citation-cleanup/66-CONTEXT.md`
!`cat .planning/REQUIREMENTS.md`

# Phase 65 guard test (success gate)
@tests/no-issue-citations.test.cjs

# Phase 64 findings (citation inventory)
@.planning/phases/64-citation-pattern-exploration/64-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean citations from 10 get-shit-done/references/ files</name>
  <files>get-shit-done/references/checkpoints.md, get-shit-done/references/git-integration.md, get-shit-done/references/model-profiles.md, get-shit-done/references/mvp-concepts.md, get-shit-done/references/planner-graphify-auto-update.md, get-shit-done/references/planner-human-verify-mode.md, get-shit-done/references/planning-config.md, get-shit-done/references/scout-codebase.md, get-shit-done/references/thinking-partner.md, get-shit-done/references/worktree-path-safety.md</files>
  <read_first>
    - get-shit-done/references/checkpoints.md
    - get-shit-done/references/git-integration.md
    - get-shit-done/references/model-profiles.md
    - get-shit-done/references/mvp-concepts.md
    - get-shit-done/references/planner-graphify-auto-update.md
    - get-shit-done/references/planner-human-verify-mode.md
    - get-shit-done/references/planning-config.md
    - get-shit-done/references/scout-codebase.md
    - get-shit-done/references/thinking-partner.md
    - get-shit-done/references/worktree-path-safety.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    For each of the 10 files in `<files>`, work file-by-file (per D-01: per-file agent edits, no regex script per D-02):

    1. Read the file once. Identify the YAML frontmatter range (first `---` line through the matching closing `---`) — note that many reference files have NO frontmatter and start directly with content; in that case there is no frontmatter to skip. Also identify every fenced code block. Skip citation tokens inside frontmatter (if present) and inside code fences per D-08 / D-09.

    2. Outside those zones, remove every citation token of these forms: bare `#NNN` (3+ digits), parenthetical `(#NNN)`, and inline `feat-NNNN`. Preserve allowlisted tokens `#1`, `#2`, `#45`, `#123`, hex color literals (e.g. `#e8c170`), and markdown heading markers (`##`, `###`, etc.) per D-10.

    3. Apply contextual prose repair at the exact point of each removed citation per D-05/D-06/D-07:
       - Drop citation-only clauses (e.g. `(see #1486)`, `— #2015`) with their leading connector (`—`, `,`, `see`, `by`, `in`, `from`, `per`) and any wrapping parentheses (D-06).
       - For citations attached to substantive prose (e.g. "prevents destructive HEAD-on-master self-recovery path (#2924)"), strip only the citation token plus immediately surrounding parentheses/whitespace — keep the explanatory clause (D-07).
       - Collapse any resulting double space to a single space, remove any space directly before a comma/period/semicolon/colon, delete any empty parentheses `()`.

    4. Do NOT touch any line inside a YAML frontmatter block or inside any fenced code block. References are Eta-included into other files; modifying a code-fence example would change the prompt content downstream agents see at runtime.

    5. After all 10 files are edited, verify the references/ slice of the guard test is clean:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/references/" || echo "CLEAN: references/"`
       Expected stdout: `CLEAN: references/`. If any `✖` line referencing `get-shit-done/references/` appears, re-read the offending file and re-edit until the slice is clean.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/references/" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0`.
    - `grep -rEn '(^|[^&#])#[0-9]{3,}' get-shit-done/references/ | grep -vE '#(1|2|45|123)([^0-9]|$)' | wc -l | tr -d ' '` outputs exactly `0` (no unallowlisted bare `#NNN` of 3+ digits remain).
    - `grep -rEn 'feat-[0-9]{3,}' get-shit-done/references/ | wc -l | tr -d ' '` outputs exactly `0`.
    - `git diff --stat get-shit-done/references/` shows changes to all 10 files listed in `<files>`.
    - For each modified file that has a YAML frontmatter block, that block (line 1 through second `---`) is byte-identical to its pre-edit version.
  </acceptance_criteria>
  <done>
    All 10 references/ files are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences. Allowlisted tokens preserved. Surrounding prose flows naturally. Guard test produces zero ✖ lines for get-shit-done/references/ paths.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "get-shit-done/references/"` returns no matches.
- `node --test tests/agent-frontmatter.test.cjs` exits 0 (no agent files were touched, but global frontmatter suite should still be GREEN).
</verification>

<success_criteria>
- 10 get-shit-done/references/ files modified, all citations outside frontmatter/code-fences removed (CITE-06, CITE-07).
- Sentences read naturally (CITE-08).
- YAML frontmatter (where present) byte-identical to pre-edit state (CITE-09).
- Guard test produces zero ✖ lines for get-shit-done/references/ paths.
</success_criteria>

<output>
Create `.planning/phases/66-citation-cleanup/66-04-SUMMARY.md` when done, citing the post-edit guard test output for the references/ slice.
</output>
