---
phase: 66-citation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/config.md
  - commands/gsd/graphify.md
  - commands/gsd/ns-context.md
  - commands/gsd/ns-ideate.md
  - commands/gsd/ns-manage.md
  - commands/gsd/ns-project.md
  - commands/gsd/ns-review.md
  - commands/gsd/ns-workflow.md
  - commands/gsd/plan-phase.md
autonomous: true
requirements: [CITE-06, CITE-07, CITE-08, CITE-09]

must_haves:
  truths:
    - "D-01: Per-file executor edits — no regex script is created"
    - "D-08: YAML frontmatter blocks (between leading --- delimiters) are untouched"
    - "D-09: Fenced code blocks (triple-backtick) are untouched"
    - "D-10: Allowlisted tokens (#1, #2, #45, #123, hex colors, heading markers) are preserved"
    - "All 9 commands/gsd/ files are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences"
    - "Cleaned prose contains no double spaces, empty parentheses, or dangling connectors (— , see, by, in, from) at sentence boundaries"
    - "Guard test reports zero ✖ lines for commands/gsd/ files after cleanup"
  artifacts:
    - path: "commands/gsd/config.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/graphify.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-context.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-ideate.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-manage.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-project.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-review.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/ns-workflow.md"
      provides: "Cleaned command file — citations removed, prose intact"
    - path: "commands/gsd/plan-phase.md"
      provides: "Cleaned command file — citations removed, prose intact"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "commands/gsd/*.md"
      via: "directory scan + per-file ✖ enumeration"
      pattern: "commands/gsd/.*\\.md"
---

<objective>
Remove every issue/PR citation from the 9 failing files in `commands/gsd/` and repair the surrounding prose so each sentence reads as if the citation was never there. Frontmatter and fenced code blocks must not be modified.

Purpose: Close CITE-06 / CITE-07 / CITE-08 / CITE-09 for the `commands/gsd/` directory so the Phase 65 guard test goes GREEN for this slice and the next plan can run in parallel without file contention.

Output: 9 modified `.md` files committed atomically. Guard test reports zero ✖ lines for `commands/gsd/` paths.
</objective>

<context>
!`cat .planning/phases/66-citation-cleanup/66-CONTEXT.md`
!`cat .planning/REQUIREMENTS.md`

# Phase 65 guard test (success gate — reference for detection rules and allowlist)
@tests/no-issue-citations.test.cjs

# Phase 64 findings (citation inventory)
@.planning/phases/64-citation-pattern-exploration/64-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean citations from 9 commands/gsd/ files</name>
  <files>commands/gsd/config.md, commands/gsd/graphify.md, commands/gsd/ns-context.md, commands/gsd/ns-ideate.md, commands/gsd/ns-manage.md, commands/gsd/ns-project.md, commands/gsd/ns-review.md, commands/gsd/ns-workflow.md, commands/gsd/plan-phase.md</files>
  <read_first>
    - commands/gsd/config.md
    - commands/gsd/graphify.md
    - commands/gsd/ns-context.md
    - commands/gsd/ns-ideate.md
    - commands/gsd/ns-manage.md
    - commands/gsd/ns-project.md
    - commands/gsd/ns-review.md
    - commands/gsd/ns-workflow.md
    - commands/gsd/plan-phase.md
    - tests/no-issue-citations.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    For each of the 9 files listed in `<files>`, work file-by-file using the Read and Edit tools (per D-01: per-file agent edits, no regex script per D-02):

    1. Read the entire file once to see all citations in context. Identify the YAML frontmatter range (first `---` line through the matching closing `---`) and every fenced code block (lines from an opening triple-backtick fence through its closing fence). Skip all citation tokens inside those two zones per D-08 and D-09.

    2. Outside those zones, remove every citation token of these forms: bare `#NNN` (3+ digits), parenthetical `(#NNN)`, and inline `feat-NNNN`. Preserve allowlisted tokens `#1`, `#2`, `#45`, `#123`, hex color literals (e.g. `#e8c170`), and markdown heading markers (`##`, `###`, etc.) per D-10.

    3. Apply contextual prose repair per D-05/D-06/D-07 at the exact point of each removed citation:
       - If the citation token is the only informational content in its clause (e.g. `(see #1486)`, `— #2015`, `(#2924)` standing alone after a sentence), drop the entire clause including its leading connector (`—`, `,`, `see`, `by`, `in`, `from`, `per`) and any wrapping parentheses (per D-06).
       - If the clause carries independent meaning (e.g. "prevents destructive HEAD-on-master recovery (#2924)"), keep the prose and strip only the citation token plus immediately surrounding parentheses/whitespace so the sentence still reads naturally (per D-07).
       - After every removal, normalise whitespace at the edit site: collapse any resulting double space to a single space, remove any space that now sits directly before a comma/period/semicolon/colon, and delete any empty parentheses `()` left behind.

    4. Do NOT touch any line inside the YAML frontmatter block or inside any fenced code block, even if it contains a `#NNN` token. The guard test exempts those zones; modifying them risks breaking frontmatter validation (per D-09 / `agent-frontmatter.test.cjs`).

    5. After all 9 files are edited, run the guard test once to confirm zero `✖` lines reference any `commands/gsd/` path:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "commands/gsd/" || echo "CLEAN: commands/gsd/"`
       The expected stdout line is exactly `CLEAN: commands/gsd/`. If any `✖` line referencing `commands/gsd/` appears, re-read the offending file and apply the same removal + repair procedure until the directory is clean.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "commands/gsd/" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "commands/gsd/" | wc -l | tr -d ' '` outputs exactly `0`.
    - `grep -rEn '(^|[^&#])#[0-9]{3,}' commands/gsd/ | grep -vE '#(1|2|45|123)([^0-9]|$)' | wc -l | tr -d ' '` outputs exactly `0` (no unallowlisted bare `#NNN` citations of 3+ digits remain in commands/gsd/).
    - `grep -rEn 'feat-[0-9]{3,}' commands/gsd/ | wc -l | tr -d ' '` outputs exactly `0` (no hyphen-form `feat-NNNN` citations remain).
    - `git diff --stat commands/gsd/` shows changes to all 9 files listed in `<files>`.
    - For each modified file, the YAML frontmatter block is byte-identical to its pre-edit version when sliced from line 1 to the second `---` delimiter (no frontmatter mutation per D-08 / CITE-09).
  </acceptance_criteria>
  <done>
    All 9 files in commands/gsd/ are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences. Allowlisted tokens are preserved. Surrounding prose flows naturally with no double spaces, empty parentheses, or dangling connectors. The Phase 65 guard test produces zero `✖` lines for commands/gsd/ paths.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "commands/gsd/"` returns no matches (exit-status pipeline result empty).
- `node --test tests/agent-frontmatter.test.cjs` exits 0 (frontmatter untouched).
</verification>

<success_criteria>
- 9 commands/gsd/ files modified, all citations outside frontmatter/code-fences removed (CITE-06, CITE-07).
- Sentences read naturally — no double spaces, dangling connectors, or empty parentheses (CITE-08).
- YAML frontmatter blocks are byte-identical to pre-edit state (CITE-09).
- Guard test produces zero ✖ lines for commands/gsd/ paths.
</success_criteria>

<output>
Create `.planning/phases/66-citation-cleanup/66-01-SUMMARY.md` when done, citing the post-edit guard test output for the commands/gsd/ slice.
</output>
