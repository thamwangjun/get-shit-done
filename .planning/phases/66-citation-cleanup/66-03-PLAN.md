---
phase: 66-citation-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-code-fixer.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-executor.md
  - agents/gsd-intel-updater.md
  - agents/gsd-plan-checker.md
  - agents/gsd-verifier.md
autonomous: true
requirements: [CITE-06, CITE-07, CITE-08, CITE-09]

must_haves:
  truths:
    - "D-01: Per-file executor edits — no regex script is created"
    - "D-08: YAML frontmatter blocks (between leading --- delimiters) are untouched — agents/ is the highest-risk slice for this rule because agent-frontmatter.test.cjs validates every agent file"
    - "D-09: Fenced code blocks (triple-backtick) are untouched"
    - "D-10: Allowlisted tokens (#1, #2, #45, #123, hex colors, heading markers) are preserved"
    - "All 6 agents/ files are free of bare #NNN, parenthetical (#NNN), and feat-NNNN citations outside frontmatter and code fences"
    - "Guard test reports zero ✖ lines for agents/ files after cleanup"
    - "agent-frontmatter.test.cjs passes with the same count as before this plan ran (CITE-09)"
  artifacts:
    - path: "agents/gsd-code-fixer.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
    - path: "agents/gsd-codebase-mapper.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
    - path: "agents/gsd-executor.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
    - path: "agents/gsd-intel-updater.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
    - path: "agents/gsd-plan-checker.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
    - path: "agents/gsd-verifier.md"
      provides: "Cleaned agent file — citations removed, frontmatter untouched"
  key_links:
    - from: "tests/no-issue-citations.test.cjs"
      to: "agents/*.md"
      via: "directory scan + per-file ✖ enumeration"
      pattern: "agents/.*\\.md"
    - from: "tests/agent-frontmatter.test.cjs"
      to: "agents/*.md"
      via: "YAML frontmatter validator (name, description, tools, color, hooks)"
      pattern: "front-matter validation count"
---

<objective>
Remove every issue/PR citation from the 6 failing agent files in `agents/` and repair the surrounding prose. The agents/ directory carries the strictest frontmatter contract in the repo — `agent-frontmatter.test.cjs` validates every agent file on every `npm test`, so any accidental frontmatter edit will surface immediately.

Purpose: Close CITE-06 / CITE-07 / CITE-08 / CITE-09 for the `agents/` directory so the Phase 65 guard test goes GREEN for this slice without disturbing the frontmatter test count.

Output: 6 modified agent `.md` files committed atomically. Guard test reports zero ✖ lines for `agents/` paths and `agent-frontmatter.test.cjs` test count is unchanged.
</objective>

<context>
!`cat .planning/phases/66-citation-cleanup/66-CONTEXT.md`
!`cat .planning/REQUIREMENTS.md`

# Phase 65 guard test (success gate)
@tests/no-issue-citations.test.cjs

# Frontmatter validator (must remain GREEN with unchanged test count)
@tests/agent-frontmatter.test.cjs

# Phase 64 findings (citation inventory)
@.planning/phases/64-citation-pattern-exploration/64-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline agent-frontmatter test count</name>
  <files>(none — read-only baseline capture)</files>
  <read_first>
    - tests/agent-frontmatter.test.cjs
  </read_first>
  <action>
    Before touching any agent file, run the frontmatter validator and record the test summary line so the post-cleanup verification can compare against a real baseline (per D-08 / CITE-09):

    `node --test tests/agent-frontmatter.test.cjs 2>&1 | tee /tmp/gsd-66-03-frontmatter-baseline.txt | grep -E '^(# tests|# pass|# fail|# skipped|# todo|# duration_ms)' || true`

    The baseline must show `# fail 0`. Note the `# tests` and `# pass` numbers — Task 3 will assert they are unchanged.

    If `# fail` is non-zero at baseline, halt and report: the frontmatter suite is already broken and this plan cannot proceed without false-positive risk.
  </action>
  <verify>
    <automated>grep -E '^# fail' /tmp/gsd-66-03-frontmatter-baseline.txt | head -1 | grep -E 'fail 0$' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/gsd-66-03-frontmatter-baseline.txt` exists.
    - The verify command outputs exactly `1` (the baseline shows `# fail 0`).
    - The file contains a `# tests N` and `# pass N` line that Task 3 will reference.
  </acceptance_criteria>
  <done>
    Baseline frontmatter test counts are captured to /tmp/gsd-66-03-frontmatter-baseline.txt with 0 failures. Test 3 has a real comparison target.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean citations from 6 agents/ files</name>
  <files>agents/gsd-code-fixer.md, agents/gsd-codebase-mapper.md, agents/gsd-executor.md, agents/gsd-intel-updater.md, agents/gsd-plan-checker.md, agents/gsd-verifier.md</files>
  <read_first>
    - agents/gsd-code-fixer.md
    - agents/gsd-codebase-mapper.md
    - agents/gsd-executor.md
    - agents/gsd-intel-updater.md
    - agents/gsd-plan-checker.md
    - agents/gsd-verifier.md
    - tests/no-issue-citations.test.cjs
    - tests/agent-frontmatter.test.cjs
    - .planning/phases/66-citation-cleanup/66-CONTEXT.md
  </read_first>
  <action>
    For each of the 6 agent files in `<files>`, work file-by-file (per D-01 / D-02):

    1. Read the file once. Identify the YAML frontmatter range: lines 1 through the second `---` delimiter. This block typically contains `name:`, `description:`, `tools:`, `color:`, optional `hooks:`, and (in some agents) multi-line descriptions. Every line in this range is OFF LIMITS for editing per D-08 — including any `#NNN` token that happens to appear inside a multi-line description. The frontmatter validator hashes / counts these fields and will fail if structure changes.

    2. Identify every fenced code block (lines from an opening triple-backtick fence through its closing fence) and skip citation tokens inside per D-09.

    3. Outside those two zones, remove every citation token of these forms: bare `#NNN` (3+ digits), parenthetical `(#NNN)`, and inline `feat-NNNN`. Preserve allowlisted tokens `#1`, `#2`, `#45`, `#123`, hex color literals, and markdown heading markers per D-10.

    4. Apply contextual prose repair at the exact point of each removed citation per D-05/D-06/D-07:
       - Drop citation-only clauses (e.g. `(see #1486)`, `— #2015`) with their leading connector (`—`, `,`, `see`, `by`, `in`, `from`, `per`) and any wrapping parentheses (D-06).
       - For citations attached to substantive prose (e.g. "prevents destructive HEAD-on-master self-recovery path (#2924)"), strip only the parenthetical citation plus surrounding whitespace — keep the explanatory clause (D-07).
       - Collapse double spaces, remove space-before-punctuation, delete empty `()`.

    5. After all 6 files are edited, verify the agents/ slice of the guard test is clean:
       `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "agents/" || echo "CLEAN: agents/"`
       Expected stdout: `CLEAN: agents/`. If any `✖` line referencing `agents/` appears, re-read the offending file and re-edit.
  </action>
  <verify>
    <automated>node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "agents/" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0`.
    - `git diff --stat agents/` shows changes to all 6 files listed in `<files>`.
    - For each modified file, the YAML frontmatter block (line 1 through second `---`) is byte-identical to its pre-edit version. Verify per file with: `git diff agents/<file> | grep -E '^\\+' | grep -E '(^\\+name:|^\\+description:|^\\+tools:|^\\+color:|^\\+hooks:|^\\+---)' | wc -l | tr -d ' '` outputs exactly `0` (no added lines start with a frontmatter key or the delimiter).
    - `grep -rEn '(^|[^&#])#[0-9]{3,}' agents/ | grep -vE '#(1|2|45|123)([^0-9]|$)' | wc -l | tr -d ' '` outputs exactly `0`.
    - `grep -rEn 'feat-[0-9]{3,}' agents/ | wc -l | tr -d ' '` outputs exactly `0`.
  </acceptance_criteria>
  <done>
    All 6 agents/ files are free of citations outside frontmatter and code fences. Frontmatter blocks are byte-identical to pre-edit state. Allowlisted tokens preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run agent-frontmatter validator and confirm unchanged test count</name>
  <files>(none — verification only)</files>
  <read_first>
    - /tmp/gsd-66-03-frontmatter-baseline.txt
    - tests/agent-frontmatter.test.cjs
  </read_first>
  <action>
    Run the frontmatter validator post-cleanup and compare against the baseline captured in Task 1 (per D-08 / CITE-09):

    `node --test tests/agent-frontmatter.test.cjs 2>&1 | tee /tmp/gsd-66-03-frontmatter-after.txt | grep -E '^(# tests|# pass|# fail|# skipped|# todo)' || true`

    Extract the `# tests` and `# pass` values from both the baseline and the after file. They MUST match exactly. The `# fail` value in the after file MUST be `0`.

    If counts differ or any failure appears, the cleanup has mutated a frontmatter block — re-open the file(s) flagged in the test output and restore the original frontmatter lines verbatim from `git show HEAD:<path>`, then re-run this verification.
  </action>
  <verify>
    <automated>diff <(grep -E '^# (tests|pass|fail)' /tmp/gsd-66-03-frontmatter-baseline.txt | sort) <(grep -E '^# (tests|pass|fail)' /tmp/gsd-66-03-frontmatter-after.txt | sort) | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs exactly `0` (the `# tests`, `# pass`, and `# fail` summary lines are identical between baseline and after).
    - `grep -E '^# fail 0$' /tmp/gsd-66-03-frontmatter-after.txt | wc -l | tr -d ' '` outputs exactly `1` (post-cleanup frontmatter suite has 0 failures).
    - `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "agents/" | wc -l | tr -d ' '` still outputs `0` (guard test agents/ slice remained clean during this verification step).
  </acceptance_criteria>
  <done>
    agent-frontmatter test count is byte-identical to pre-cleanup baseline; 0 frontmatter failures; agents/ slice of the guard test is clean. CITE-09 is satisfied with measurable proof, not just assertion.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/no-issue-citations.test.cjs 2>&1 | grep "✖" | grep "agents/"` returns no matches.
- `node --test tests/agent-frontmatter.test.cjs` exits 0 with the same `# tests` / `# pass` counts as baseline.
</verification>

<success_criteria>
- 6 agents/ files modified, all citations outside frontmatter/code-fences removed (CITE-06, CITE-07).
- Sentences read naturally (CITE-08).
- YAML frontmatter blocks are byte-identical to pre-edit state; agent-frontmatter test count unchanged (CITE-09).
- Guard test produces zero ✖ lines for agents/ paths.
</success_criteria>

<output>
Create `.planning/phases/66-citation-cleanup/66-03-SUMMARY.md` when done, citing the baseline vs. after frontmatter test summary lines and the post-edit guard test output for the agents/ slice.
</output>
