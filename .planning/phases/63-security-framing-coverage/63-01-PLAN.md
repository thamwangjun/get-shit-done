---
phase: 63-security-framing-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/debug-session-management.test.cjs]
autonomous: true
requirements: [SFC-01]

must_haves:
  truths:
    - "The gsd-debugger security test at lines 99–101 executes unconditionally (no skip option)"
    - "The test asserts gsd-debugger.md contains 'untrusted user input'"
    - "The test asserts gsd-debugger.md contains 'evidence data only'"
    - "The stale DATA_START assertion is gone from this test"
    - "The line-133 sessionManager DATA_START/DATA_END test is unchanged (per D-01)"
    - "npm test passes with 0 new failures; debug-session-management.test.cjs skip count drops by exactly 1 (per D-06)"
  artifacts:
    - path: "tests/debug-session-management.test.cjs"
      provides: "Active fork-hardened security framing assertions on gsdDebugger"
      contains: "untrusted user input"
  key_links:
    - from: "tests/debug-session-management.test.cjs"
      to: "agents/gsd-debugger.md (lines 32–33)"
      via: "gsdDebugger module-level variable .includes()"
      pattern: "gsdDebugger\\.includes\\('untrusted user input'\\)"
---

<objective>
Flip the single skipped unit test at lines 99–101 of `tests/debug-session-management.test.cjs` to active, replacing its stale upstream `DATA_START` assertion with two assertions on the fork's hardened security language in `agents/gsd-debugger.md`.

Purpose: Close gap SFC-01 — the fork's hardened security paragraph ("untrusted user input", "evidence data only") is now actively guarded against regression instead of being suppressed by a skip marker.

Output: One modified test file. No source/agent content changes — the asserted strings already exist in `gsd-debugger.md` lines 32–33, so the rewritten test passes immediately.
</objective>

<context>
!`cat .planning/phases/63-security-framing-coverage/63-CONTEXT.md`
</context>

<threat_model>
## Trust Boundaries

None. This phase modifies test-assertion code only — no runtime code, no I/O, no new attack surface.

## STRIDE Threat Register

No applicable threats — test assertion change only. The change adds two read-only string assertions against an already-on-disk agent file and removes a skip marker. No package installs, no network, no new data flow.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite skipped gsd-debugger security test to assert fork hardened framing</name>
  <files>tests/debug-session-management.test.cjs</files>
  <read_first>
    - tests/debug-session-management.test.cjs (the target is the SKIPPED test at lines 99–101 only)
    - agents/gsd-debugger.md (lines 32–33 contain the asserted strings — DO NOT modify this file)
  </read_first>
  <action>
    Edit ONLY the test at lines 99–101 (title `'gsd-debugger contains security note about DATA_START'`, currently skipped). Make these three changes in one edit (per D-02, D-03, D-05):

    1. Remove the `{ skip: 'fork intentionally diverges from upstream contract' }` option so the test executes unconditionally (per D-02).
    2. Rename the test title to `'gsd-debugger asserts fork hardened security framing'` (per D-05).
    3. Replace the single `assert.ok(gsdDebugger.includes('DATA_START'), ...)` body entirely with two assertions (per D-03):
       - `assert.ok(gsdDebugger.includes('untrusted user input'), 'gsd-debugger.md must contain fork hardened framing: untrusted user input');`
       - `assert.ok(gsdDebugger.includes('evidence data only'), 'gsd-debugger.md must contain fork scope restriction: evidence data only');`

    Reuse the existing module-level `gsdDebugger` variable (line 19) — no new file reads. Follow the sibling `assert.ok(gsdDebugger.includes('...'), 'message')` pattern.

    No negative DATA_START assertion is needed (per D-04) — the criterion only requires the DATA_START assertion be absent/replaced.

    CRITICAL (per D-01): Leave the UNSKIPPED test at lines 133–139 (`'gsd-debug-session-manager uses DATA_START/DATA_END for checkpoint responses'`) completely untouched. It asserts on `sessionManager`, not `gsdDebugger`, and its DATA_START/DATA_END usage is legitimate fork behavior for a different agent. Do NOT touch `agents/gsd-debugger.md` either.
  </action>
  <acceptance_criteria>
    - `tests/debug-session-management.test.cjs` no longer contains the string `skip: 'fork intentionally diverges from upstream contract'`
    - `tests/debug-session-management.test.cjs` contains `assert.ok(gsdDebugger.includes('untrusted user input')`
    - `tests/debug-session-management.test.cjs` contains `assert.ok(gsdDebugger.includes('evidence data only')`
    - The string `gsdDebugger.includes('DATA_START')` is absent from the file (the line-133 test uses `sessionManager`, not `gsdDebugger`, so this confirms only the target test changed)
    - The line-133 test `'gsd-debug-session-manager uses DATA_START/DATA_END for checkpoint responses'` is still present and references `sessionManager.includes('DATA_START')`
    - `agents/gsd-debugger.md` is unmodified (`git diff --quiet agents/gsd-debugger.md` exits 0)
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/debug-session-management.test.cjs 2>&1 | grep -E 'fork hardened security framing|# skipped'</automated>
  </verify>
  <done>The target test executes (not skipped), passes, and asserts both fork strings; line-133 test and gsd-debugger.md are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite gate — 0 new failures, skip count drops by exactly 1</name>
  <files>tests/debug-session-management.test.cjs</files>
  <read_first>
    - tests/debug-session-management.test.cjs (verify the Task 1 edit landed correctly)
  </read_first>
  <action>
    Run the full suite per D-06: `npm test 2>&1 | tee /tmp/gsd-test-output.txt`.

    Confirm from the piped output (NOT the tee exit code) that there are 0 new failures versus baseline, and that `debug-session-management.test.cjs` reports exactly ONE FEWER skipped test than before this phase (the skip count for the prior baseline included the now-active test). Inspect the `# skipped` / `# pass` / `# fail` summary lines.

    If any failure traces to this edit, fix it in `tests/debug-session-management.test.cjs` (do not touch `agents/gsd-debugger.md`). No source/agent changes are permitted in this phase.
  </action>
  <acceptance_criteria>
    - `npm test 2>&1 | tee /tmp/gsd-test-output.txt` reports 0 new failures versus baseline (read from the `# fail` summary line in the piped file)
    - `node --test tests/debug-session-management.test.cjs` reports 0 skipped tests in the `debug session management implementation` describe block (skip count dropped by exactly 1)
    - `node --test tests/debug-session-management.test.cjs` reports the test named `gsd-debugger asserts fork hardened security framing` as passing
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/debug-session-management.test.cjs 2>&1 | grep -E '# (pass|fail|skipped)'</automated>
  </verify>
  <done>Full suite green with 0 new failures; debug-session-management.test.cjs skip count is exactly 1 lower than before (per D-06).</done>
</task>

</tasks>

<verification>
1. `node --test tests/debug-session-management.test.cjs` — target test active and passing, 0 skipped in the first describe block.
2. `git diff --quiet agents/gsd-debugger.md` exits 0 (agent file unchanged).
3. `npm test 2>&1 | tee /tmp/gsd-test-output.txt` — 0 new failures.
</verification>

<success_criteria>
- Test at lines 99–101 has no `{ skip: '...' }` option (executes unconditionally).
- Test asserts `gsdDebugger.includes('untrusted user input')` and `gsdDebugger.includes('evidence data only')`.
- Stale `gsdDebugger.includes('DATA_START')` assertion is replaced entirely.
- Line-133 `sessionManager` DATA_START/DATA_END test is untouched; `agents/gsd-debugger.md` is untouched.
- `npm test` passes with 0 new failures; `debug-session-management.test.cjs` skip count drops by exactly 1.
</success_criteria>

<output>
Create `.planning/phases/63-security-framing-coverage/63-01-SUMMARY.md` when done.
</output>
