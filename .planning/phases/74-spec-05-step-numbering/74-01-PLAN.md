---
phase: 74-spec-05-step-numbering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/05-step-numbering/SPEC.md
autonomous: true
requirements: [SPEC-05, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md specifies the whole-integer step-numbering system as a three-layer contract — scanner (decimal Pattern A/B incl. Step N.0, Pattern D ordered-list items cols 0-2, letter-suffix Step 7a, AND out-of-order reversed/gap detection with ##/### counter reset), the normalize-step-numbers.cjs cross-file-aware idempotent CLI, and the cross-file-step-refs scanner — with the Pattern C exclusion (## N.N. section headings in plan-phase.md/new-milestone.md/new-project.md) preserved as an INTENTIONAL exclusion in BOTH an Out-of-scope Scope bullet AND a settled Key Decision (ROADMAP SC1 / SPEC-05, per D-01/D-02)"
    - "SPEC.md states the scanner -> normalizer -> cross-file-ref-scanner internal ordering explicitly as a stated contract (scanner detects = the gate, normalizer fixes in-place = the remediation, cross-file-ref scanner validates remediation did not strand prose refs) — in the Purpose narrative and/or a dedicated Key Decision (ROADMAP SC2 / SPEC-05, per D-04)"
    - "SPEC.md states five numbered EARS invariants 05-INV-1..05-INV-5 (decimal detection; out-of-order; cross-file-ref integrity with basename-collision tolerance; normalizer idempotent renumber; normalizer cross-file-ref update) with RFC 2119 strength, each citing tests/step-numbering-scan.test.cjs or tests/cross-file-step-refs.test.cjs as tier-1, an Acceptance Tests traceability table keyed on 05-INV-M mapping each MUST to a verbatim real subtest, NO [MISSING] rows (INV-4/INV-5 cite scanner-GREEN per D-03), and the symmetric fence skip stated as a normative clause (ROADMAP SC3 / QUAL-01,02,04, per D-01/D-03)"
    - "SPEC.md records corpus counts (SCAN_DIRS scope, the three Pattern C files, historical violating-file count) as dated 'current as of 2026-06-12' advisory enumerations with the SHAPE normative not the count (per D-05); marks every current path/symbol/regex/function under ## Code Context as <!-- advisory --> (QUAL-03); records the four settled Key Decisions with consequence-of-reopening (QUAL-05); and advances frontmatter Status Draft -> Ready with Confidence + Specced 2026-06-12 set, gated on QUAL-01..05, preserving Depends on: SPEC-08 and the tier-1 evidence line"
  artifacts:
    - path: ".planning/spec/05-step-numbering/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-05 (Purpose, Scope, five 05-INV-M invariants, Acceptance Tests, four Key Decisions, advisory Code Context) with Status: Ready"
      contains: "05-INV-1"
  key_links:
    - from: ".planning/spec/05-step-numbering/SPEC.md (Acceptance Tests table)"
      to: "the two tier-1 test files tests/step-numbering-scan.test.cjs and tests/cross-file-step-refs.test.cjs"
      via: "verbatim describe/test subtest names keyed on 05-INV-M"
      pattern: "tests/(step-numbering-scan|cross-file-step-refs)\\.test\\.cjs"
    - from: ".planning/spec/05-step-numbering/SPEC.md (Invariants + section order)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme + status vocabulary)"
      via: "section order + invariant ID format conformance + Draft->Ready transition"
      pattern: "05-INV-[1-5]"
---

<objective>
Author the full body of `.planning/spec/05-step-numbering/SPEC.md` — a behavioral-contract
specification of the whole-integer step-numbering system as a three-layer contract (corpus
scanner -> idempotent normalizer CLI -> cross-file-ref scanner) — so a reimplementer can rebuild
the system on a refactored upstream without reading the current source. The phase fills an
existing stub (frontmatter + empty 7-section skeleton from Phase 68); it does NOT modify
`tests/step-numbering-scan.test.cjs`, `tests/cross-file-step-refs.test.cjs`,
`scripts/normalize-step-numbers.cjs`, or the corpus — it NARRATES the two passing tier-1 test
files into the locked 7-section contract, narrates the advisory normalizer CLI into Code Context,
and advances Status `Draft -> Ready`.

Purpose: Capture the step-numbering contract (the corpus uses whole-integer `Step N` labels;
decimal `Step N.M`/`Step N.0`, ordered-list `N.M.` items, and letter-suffix `Step Na` labels plus
reversed/gapped sequences are violations a scanner catches; an idempotent normalizer renumbers to
sequential whole integers per section and updates cross-file prose refs; a cross-file-ref scanner
validates no prose ref is stranded; `## N.N.` section headings in three plan-phase files are an
intentional Pattern C exclusion) as a durable, move-proof contract (shape normative; counts,
SCAN_DIRS, Pattern C file list, regexes, function names advisory), satisfying SPEC-05 and the
shared QUAL-01..05 bars so the spec reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, five invariants (`05-INV-1`..`05-INV-5`;
INV-1/INV-2 and INV-4/INV-5 stay split per RESEARCH §4 recommendation — merge only if a pair reads
as one falsifiable claim), an Acceptance Tests traceability table citing real subtests in the two
tier-1 files with no `[MISSING]` rows, four settled Key Decisions, advisory Code Context, and
frontmatter Status `Ready` (Confidence + Specced 2026-06-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-74 source artifacts (locked decisions + narration map + locked template + the stub)
@.planning/phases/74-spec-05-step-numbering/74-CONTEXT.md
@.planning/phases/74-spec-05-step-numbering/74-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/05-step-numbering/SPEC.md
@.planning/spec/INDEX.md

# Tier-1 normative sources the spec narrates (read verbatim for subtest names + detection shapes)
@tests/step-numbering-scan.test.cjs
@tests/cross-file-step-refs.test.cjs

# Advisory implementation file (function names for Code Context only; no normative claim rests here)
@scripts/normalize-step-numbers.cjs

# Sibling Wave-1/Wave-2 worked references (section shape, EARS phrasing, advisory marking, QUAL pattern)
@.planning/spec/04-eta-materialization/SPEC.md
@.planning/spec/01-positive-framing/SPEC.md
@.planning/spec/02-sha-versioning/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose — no code/test
symbols are created; all system symbols are PRE-EXISTING and appear only under advisory Code
Context):

- **Invariant IDs:** `05-INV-1` (decimal step-label detection — Pattern A/B incl. `Step N.0`,
  Pattern D `N.M.` items cols 0-2, letter-suffix `Step 7a`; code-fence exclusion; `Step 0` valid),
  `05-INV-2` (out-of-order detection — reversed AND gaps; per-section counter reset on `##`/`###`;
  list/blockquote marker stripping; code-fence exclusion; `Step 0` valid start), `05-INV-3`
  (cross-file-ref integrity — `<file>.md step N` / `step N in <file>.md` point at a real Step N;
  same-file refs excluded; symmetric fence skip; basename-collision tolerance), `05-INV-4`
  (normalizer idempotent renumber — sequential whole integers per section; converges with second
  run "No changes needed"; `--dry-run` exits 0 without writing; traces to scanner-GREEN per D-03),
  `05-INV-5` (normalizer cross-file-ref update — dynamically discovered, no pre-built manifest;
  traces to cross-file-ref-scanner-GREEN per D-03). INV-1/INV-2 and INV-4/INV-5 stay SPLIT per
  RESEARCH §4 — merge only if a pair reads cleanly as a single falsifiable claim (Claude's
  Discretion per D-01).
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** frontmatter `**Status:**` advances `Draft -> Ready`; `**Confidence:**`
  (High recommended per RESEARCH §1 Section 1) and `**Specced:** 2026-06-12` set. The
  `**Depends on:** SPEC-08` line and the `**Reimplementation evidence (tier-1 test):**` line (which
  already names both scanner test files) are PRESERVED, not changed.

PRE-EXISTING (advisory, NOT produced here — list under Code Context only, per RESEARCH §7; the
Phase-77 source-grounding pass MUST exclude these from drift verification):
`tests/step-numbering-scan.test.cjs` symbols — `SCAN_DIRS`, `PATTERN_C_EXCLUDES`,
`STEP_DECIMAL_RE`, `scanContent()` (returns `{ patternAB, patternD }`), `scanForOutOfOrder()`,
`ALL_FILES`/`SCAN_FILES`, `collectMarkdownFiles()`;
`tests/cross-file-step-refs.test.cjs` symbols — `XREF_PATTERNS` (two word-order variants),
`FILES_BY_BASENAME`, `extractStepSet()`, `findCrossFileRefs()`;
`scripts/normalize-step-numbers.cjs` symbols — `buildRenameMap()`, `applyRenameMap()`,
`discoverCrossFileRefs()`, `processFile()`, `STEP_DECIMAL_RE`, `PATTERN_D_RE`, `XREF_PATTERNS`,
the `--dry-run` flag, and the "No changes needed." idempotency report. NOTE: there is NO dedicated
normalizer test (`normalize*.test.cjs` is ABSENT — research grep-confirmed); the scanner IS the
normalizer's acceptance oracle (D-03), so INV-4/INV-5 are NOT `[MISSING]` rows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, five 05-INV-M invariants, Acceptance Tests table</name>
  <files>.planning/spec/05-step-numbering/SPEC.md</files>
  <read_first>
    - .planning/spec/05-step-numbering/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose;
      preserve frontmatter, H1, `**Depends on:** SPEC-08`, and the tier-1 evidence line)
    - tests/step-numbering-scan.test.cjs (TIER-1 source — narrate verbatim: `scanContent()`
      decimal-detection describe with subtests `'flags Pattern A/B "**Step 2.5**" heading'`,
      `'flags Pattern A/B "Step 7.0" (zero fractional digit, D-08)'`,
      `'flags Pattern A/B with indentation (D-05: no indentation guard)'`,
      `'flags letter-suffix step (Step 7a) as violation'`,
      `'does not flag whole-integer step (Step 7)'`,
      `'flags Pattern D ordered-list decimal "5.5. text"'`,
      `'does not flag Pattern D inside code block'`; the `scanForOutOfOrder()` synthetic-content
      describe with `'flags reversed sequence Step 1, Step 3, Step 2'`,
      `'flags gap Step 1, Step 3 (missing Step 2)'`,
      `'does not flag sequence Step 0, Step 1, Step 2'`, `'resets sequence on ## heading'`,
      `'resets sequence on ### heading'`, `'ignores Step references inside code blocks'`,
      `'detects out-of-order steps preceded by dash list markers'`,
      `'detects out-of-order steps preceded by blockquote markers'`; and the three corpus describe
      blocks `'no decimal Pattern A/B labels in <file>'`, `'no decimal Pattern D items in <file>'`,
      `'no out-of-order step numbering in <file>'`)
    - tests/cross-file-step-refs.test.cjs (TIER-1 source — narrate verbatim: `extractStepSet()`
      describe with `'detects whole-integer **Step N:** heading'`, `'detects ## Step N heading'`,
      `'detects Pattern D ordered-list item at column 0'`,
      `'skips Pattern D inside code fence (per D-05)'`,
      `'skips **Step N:** inside code fence (per D-05 — symmetric)'`,
      `'skips decimal labels (whole-integer only)'`,
      `'skips letter-suffix labels (whole-integer only)'`; `findCrossFileRefs()` describe with
      `'detects filename.md step N variant'`, `'detects step N in filename.md variant'`,
      `'skips same-file refs (D-04)'`,
      `'skips refs inside code fences (per D-05 — symmetric source-side)'`; the corpus describe
      `'no stale cross-file step refs in <file>'`; and the RED test
      `'detects a stale cross-file ref pointing at a nonexistent step'`. Note the
      basename-collision rule at FILES_BY_BASENAME and the ALL_FILES-vs-SCAN_FILES source/target
      asymmetry — Pattern C files are skipped as SOURCES but read as TARGETS, RESEARCH §Landmine 7)
    - .planning/phases/74-spec-05-step-numbering/74-RESEARCH.md (THE narration map — §"Section 4"
      the five invariant claims with EARS patterns + consequence clauses; §"Section 5" the draft
      traceability table; §"Four Violation Shapes" the precise Pattern A/B vs Pattern D vs
      letter-suffix vs out-of-order statements and the Step-0-is-valid rule; §"Symmetric fence
      skip" the shared normative clause; §Landmines 1-8)
    - .planning/phases/74-spec-05-step-numbering/74-CONTEXT.md (LOCKED decisions D-01..D-05)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template §1, NN-INV-M scheme §2, EARS
      patterns + RFC 2119 strength §4, the `01-INV-1` example invariant shape, source-of-truth
      hierarchy §4 tier-1=test)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference — invariant prose +
      "Consequence of violating this invariant:" closer, the "branch enumeration below is advisory"
      rule-as-supporting-list pattern, and the Acceptance Tests multi-row-per-invariant layout)
    - .planning/spec/04-eta-materialization/SPEC.md (sibling worked reference — the shared-evidence
      Acceptance Tests note pattern for invariants whose coverage is indirect, directly applicable
      to INV-4/INV-5 scanner-GREEN framing)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (state the correct behavior directly; the spec models the
    fork's affirmative-framing standard it sits beside). Do NOT inline fenced code blocks or full
    file contents; name identifiers, subtest strings, regex names, and section headers in prose.

    Purpose (one paragraph): state the "why" of the whole-integer step-numbering system. The fork
    corpus uses whole-integer `Step N` labels across `agents/`, `get-shit-done/workflows/`, and
    `commands/gsd/`; decimal variants (`Step N.M`, `Step N.0`), ordered-list `N.M.` items,
    letter-suffix `Step Na` labels, and out-of-order (reversed or gapped) sequences are violations.
    State the three-layer pipeline AND the D-04 internal ordering explicitly here (this is the
    natural home per RESEARCH §"Section 2" — the executor may ALSO place it in a Key Decision per
    D-04): the scanner detects violations (the CI gate), the `normalize-step-numbers.cjs` CLI fixes
    them in-place (the remediation), and the cross-file-ref scanner validates that remediation did
    not strand prose references (the completeness check). State what breaks if absent: without the
    scanner, decimal/out-of-order labels silently accumulate across upstream merges; without the
    normalizer, they must be fixed manually; without the cross-file-ref scanner, a normalizer run
    can strand existing prose references to old step numbers. Include the Pattern C sentence: the
    `## N.N.` section headings in three plan-phase files are intentionally outside scanner scope —
    document structure, not step sequences. Cite the two tier-1 test files as the behavioral
    authority (source-of-truth tier 1 per 00-CONVENTIONS.md §4).

    Scope: two bullet lists.
      In scope (one bullet per behavioral role, derived from the three SCAN_DIRS and detection
      shapes): Pattern A/B detection (`**Step N.M**`/`Step N.M` incl. `Step N.0`, and `Step Na`
      letter-suffix); Pattern D detection (`N.M.` ordered-list items at columns 0-2); out-of-order
      detection (per-section reversed sequences and gaps, counter reset on `##`/`###`, list and
      blockquote marker stripping before matching); symmetric code-fence exclusion (fenced
      steps/refs excluded on BOTH source-scan and target-extract sides across all three layers);
      the normalizer's idempotent per-section renumber (`--dry-run` behavior, "No changes needed."
      on a clean corpus); dynamic cross-file ref discovery (greps the whole corpus each run, no
      pre-built manifest); cross-file ref integrity (every ref points at a real Step N; same-file
      refs excluded; basename-collision tolerance — a ref is valid if Step N exists in ANY file
      sharing the basename). State the explicit `SCAN_DIRS` scope (`agents/`,
      `get-shit-done/workflows/`, `commands/gsd/`) as a dated advisory enumeration marked
      `current as of 2026-06-12` (per D-05) — the normative claim is the SCOPE shape, not the
      literal directory count.
      Out of scope (each with a one-line rationale): (1) **the Pattern C exclusion** —
      `## N.N.` section-heading numbering (no "Step" keyword) in `get-shit-done/workflows/plan-phase.md`,
      `get-shit-done/workflows/new-milestone.md`, and `get-shit-done/workflows/new-project.md` are
      document structure, not step sequences, deliberately excluded from all corpus scans (this is
      the D-02 dual-framing Out-of-scope half — its full settled rationale lives in Key Decision
      (a)); mark the three-file list `current as of 2026-06-12` advisory; (2)
      `get-shit-done/references/` and `get-shit-done/templates/` — outside SCAN_DIRS (see INDEX.md
      Excluded from Scope); (3) step-numbering enforcement in `tests/`, SDK source, or any directory
      outside SCAN_DIRS; (4) the install/runtime conversion pipeline — orthogonal to step numbering.

    Invariants: author FIVE numbered EARS statements with RFC 2119 strength, IDs `05-INV-1`..
    `05-INV-5` (NN-INV-M format per 00-CONVENTIONS.md §2). Each is a single falsifiable claim
    mapping to a real subtest cluster (RESEARCH §"Section 4"). Keep INV-1/INV-2 and INV-4/INV-5
    SPLIT (RESEARCH recommendation) — merge a pair only if it reads cleanly as one falsifiable claim
    (Claude's Discretion per D-01). Use the per-invariant EARS pattern recommended in RESEARCH.
      - 05-INV-1 Decimal step-label detection (Event-driven MUST): when the corpus scanner processes
        a source file, the system MUST flag three violation shapes as requiring renumbering to whole
        integers — (a) `**Step N.M**`/`Step N.M` bold or plain headings INCLUDING `Step N.0`
        (Pattern A/B), (b) `N.M.` ordered-list items at columns 0-2 (Pattern D), and (c) `Step Na`
        letter-suffix labels — and MUST NOT flag content inside triple-backtick code fences on any
        shape. State explicitly that `Step N.0` IS a violation and a whole-integer `Step N` is NOT.
        State the symmetric fence-skip clause as normative. Mark the detection regex
        (`STEP_DECIMAL_RE`) and the Pattern D column guard as advisory `current as of 2026-06-12`.
      - 05-INV-2 Out-of-order step detection (Event-driven MUST): when the scanner processes a
        section, the system MUST flag out-of-order whole-integer sequences — BOTH reversed sequences
        AND gaps — resetting the per-section counter at every `##` or `###` heading; list markers
        (`-`, `*`, `+`, `1.`) and blockquote markers (`>`) MUST be stripped before matching;
        code-fenced steps MUST NOT affect the counter; `Step 0` is a valid sequence start (a
        `Step 0, Step 1, Step 2` sequence MUST NOT be flagged).
      - 05-INV-3 Cross-file-ref integrity (Event-driven MUST): when the cross-file-ref scanner
        processes a source file, every prose cross-file step reference of the form `<file>.md step N`
        (Pattern 1) or `step N in <file>.md` (Pattern 2) in SCAN_DIRS MUST point at a real Step N
        heading or Pattern D ordered-list item in the target file. Same-file refs MUST be excluded;
        code-fenced refs MUST be excluded on the source-ref side AND code-fenced steps on the
        target-step side (symmetric fence skip). State the basename-collision tolerance EXPLICITLY:
        a ref is valid if Step N exists in ANY file sharing the target basename (e.g. a thin
        `commands/gsd/execute-phase.md` delegating to the canonical
        `get-shit-done/workflows/execute-phase.md`) — do NOT assume one file per basename. Mark the
        two `XREF_PATTERNS` advisory `current as of 2026-06-12`.
      - 05-INV-4 Normalizer idempotent renumber (Event-driven MUST): when the
        `normalize-step-numbers.cjs` CLI runs on a corpus containing decimal or letter-suffix step
        labels, it MUST renumber each to a sequential whole integer per section AND MUST converge —
        a second run on a corpus the first run processed reports "No changes needed." and exits 0
        (idempotent); a `--dry-run` invocation MUST report without writing and exit 0. State the
        D-03 acceptance oracle: after a correct run, `tests/step-numbering-scan.test.cjs` corpus
        subtests report zero violations (scanner-GREEN). Add the shared-evidence note (SPEC-04
        precedent): this invariant has no dedicated normalizer test; its observable evidence is the
        scanner running GREEN on the post-normalization corpus — this is indirect coverage, NOT a
        missing test.
      - 05-INV-5 Normalizer cross-file-ref update (Event-driven MUST): when the normalizer renumbers
        a step label in a file, it MUST update every cross-file prose reference to that label across
        the corpus — dynamically discovered on each run by grepping the corpus for `XREF_PATTERNS`,
        NOT from a pre-built manifest — so that after normalization
        `tests/cross-file-step-refs.test.cjs` corpus subtests report zero stale refs (scanner-GREEN
        per D-03). State explicitly there is NO persisted cross-file map consumed at runtime
        (RESEARCH §Landmine 1). Add the same shared-evidence/indirect-coverage note.
      End each invariant with a "Consequence of violating this invariant: <what breaks>" clause
      (consequences supplied per-invariant in RESEARCH §"Section 4").

    Acceptance Tests: a traceability table keyed on `05-INV-M`, columns EXACTLY
    `Invariant | Test File | Subtest / Assertion Shape`. The Test File column carries
    `tests/step-numbering-scan.test.cjs` for INV-1, INV-2, INV-4 rows and
    `tests/cross-file-step-refs.test.cjs` for INV-3, INV-5 rows. Lift REAL verbatim subtest names
    from the two test files (cross-check against RESEARCH §"Section 5"). Every MUST invariant traces
    to a real subtest: NO `[MISSING — write test first]` rows. Precede the table with a one-sentence
    narrative (SPEC-04 INV-3 precedent) explaining that INV-4 and INV-5 cite the scanner corpus
    subtests as the acceptance oracle per D-03 — indirect coverage, not missing tests. Include at
    least these literal subtest names verbatim:
      - 05-INV-1: `'flags Pattern A/B "**Step 2.5**" heading'`,
        `'flags Pattern A/B "Step 7.0" (zero fractional digit, D-08)'`,
        `'flags letter-suffix step (Step 7a) as violation'`,
        `'flags Pattern D ordered-list decimal "5.5. text"'`,
        `'does not flag whole-integer step (Step 7)'`, plus the corpus rows
        `no decimal Pattern A/B labels in <file>` and `no decimal Pattern D items in <file>`.
      - 05-INV-2: `'flags reversed sequence Step 1, Step 3, Step 2'`,
        `'flags gap Step 1, Step 3 (missing Step 2)'`,
        `'does not flag sequence Step 0, Step 1, Step 2'`, `'resets sequence on ## heading'`,
        `'ignores Step references inside code blocks'`, plus the corpus row
        `no out-of-order step numbering in <file>`.
      - 05-INV-3: `'detects whole-integer **Step N:** heading'`,
        `'detects filename.md step N variant'`, `'detects step N in filename.md variant'`,
        `'skips same-file refs (D-04)'`,
        `'skips **Step N:** inside code fence (per D-05 — symmetric)'`, the corpus row
        `no stale cross-file step refs in <file>`, and the RED test
        `'detects a stale cross-file ref pointing at a nonexistent step'`.
      - 05-INV-4: the scanner corpus rows `no decimal Pattern A/B labels in <file>` +
        `no decimal Pattern D items in <file>` from `tests/step-numbering-scan.test.cjs` —
        scanner-GREEN after a normalizer run (per D-03).
      - 05-INV-5: the corpus row `no stale cross-file step refs in <file>` from
        `tests/cross-file-step-refs.test.cjs` — scanner-GREEN after a normalizer run (per D-03).
  </action>
  <verify>
    <automated>grep -c '05-INV-' .planning/spec/05-step-numbering/SPEC.md  # >=5 invariant-ID occurrences</automated>
    <automated>grep -q 'tests/step-numbering-scan.test.cjs' .planning/spec/05-step-numbering/SPEC.md && grep -q 'tests/cross-file-step-refs.test.cjs' .planning/spec/05-step-numbering/SPEC.md && echo OK  # both tier-1 files cited</automated>
    <automated>grep -q 'flags Pattern A/B "Step 7.0"' .planning/spec/05-step-numbering/SPEC.md && grep -q 'does not flag sequence Step 0, Step 1, Step 2' .planning/spec/05-step-numbering/SPEC.md && grep -q 'detects a stale cross-file ref pointing at a nonexistent step' .planning/spec/05-step-numbering/SPEC.md && grep -q 'skips same-file refs (D-04)' .planning/spec/05-step-numbering/SPEC.md && echo OK  # verbatim anchor subtests present</automated>
    <automated>grep -q 'basename' .planning/spec/05-step-numbering/SPEC.md && grep -q 'No changes needed' .planning/spec/05-step-numbering/SPEC.md && grep -q 'current as of 2026-06-12' .planning/spec/05-step-numbering/SPEC.md && echo OK  # basename-collision tolerance, idempotency report, dated-advisory marker</automated>
    <automated>grep -c 'MISSING — write test first' .planning/spec/05-step-numbering/SPEC.md  # ==0, every MUST invariant has a real subtest (D-01/D-03)</automated>
    <automated>grep -c 'to be filled' .planning/spec/05-step-numbering/SPEC.md  # decreasing — first four placeholder comments replaced</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-INV-' SPEC.md` returns >=5 (five invariant IDs `05-INV-1`..`05-INV-5` present).
    - Both tier-1 test files appear in the Acceptance Tests table:
      `grep -q 'tests/step-numbering-scan.test.cjs'` and `grep -q 'tests/cross-file-step-refs.test.cjs'`
      both succeed.
    - The table contains the verbatim anchor subtest names listed in the action — at minimum
      `'flags Pattern A/B "Step 7.0" (zero fractional digit, D-08)'`,
      `'does not flag sequence Step 0, Step 1, Step 2'`, `'skips same-file refs (D-04)'`, and
      `'detects a stale cross-file ref pointing at a nonexistent step'` — lifted exactly from the
      two test files.
    - The basename-collision tolerance is stated (`grep -q 'basename'` succeeds), the normalizer
      idempotency report is named (`grep -q 'No changes needed'` succeeds), and at least one dated
      advisory marker is present (`grep -q 'current as of 2026-06-12'` succeeds).
    - `grep -c 'MISSING — write test first' SPEC.md` returns 0 (no MISSING rows — INV-4/INV-5 cite
      scanner-GREEN per D-03).
    - The four sections appear under the exact headers `## Purpose`, `## Scope`, `## Invariants`,
      `## Acceptance Tests` in that order, with no `<!-- to be filled -->` comment remaining in them.
    - The symmetric fence-skip clause appears as normative text in at least INV-1 and INV-3
      (`grep -q 'fence'` or `grep -q 'code fence'` succeeds in those invariant bodies).
  </acceptance_criteria>
  <done>
    Purpose, Scope, Invariants (five `05-INV-M` EARS statements), and the Acceptance Tests
    traceability table are authored; ROADMAP success criteria 1, 2, and 3 (invariant half) are
    satisfied; decimal detection, out-of-order detection, cross-file-ref integrity with
    basename-collision tolerance, normalizer idempotent renumber, and normalizer cross-file-ref
    update are all specified; the scanner -> normalizer -> cross-file-ref-scanner ordering is stated;
    every MUST invariant traces to a real subtest across the two tier-1 files with no MISSING rows;
    and the symmetric fence skip is a normative clause.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author four settled Key Decisions + advisory Code Context, advance Status Draft -> Ready</name>
  <files>.planning/spec/05-step-numbering/SPEC.md</files>
  <read_first>
    - .planning/spec/05-step-numbering/SPEC.md (now carrying the Task 1 normative core; fill the
      remaining `## Key Decisions` and `## Code Context` sections and update the frontmatter block)
    - .planning/phases/74-spec-05-step-numbering/74-RESEARCH.md (§"Section 6" the four ready-to-insert
      Key Decisions (a) Pattern C exclusion, (b) scanner->normalizer->cross-file-ref ordering,
      (c) normalizer-traceability scanner-GREEN, (d) dynamic cross-file ref discovery — each with
      decision statement + rationale + consequence-of-reopening; §"Section 7" the full advisory
      symbol inventory across the two test files and the normalizer CLI; §"Section 1" the
      frontmatter transition (Status Ready, Confidence High, Specced 2026-06-12))
    - .planning/phases/74-spec-05-step-numbering/74-CONTEXT.md (D-02 Pattern C dual-framing,
      D-03 normalizer-traceability, D-04 internal ordering, D-05 shape-normative-count-dated)
    - scripts/normalize-step-numbers.cjs (advisory function names for Code Context ONLY:
      `buildRenameMap`, `applyRenameMap`, `discoverCrossFileRefs`, `processFile`; constants
      `STEP_DECIMAL_RE`, `PATTERN_D_RE`, `XREF_PATTERNS`, `SCAN_DIRS`, `PATTERN_C_EXCLUDES`; the
      `--dry-run` flag and the "No changes needed." report. NO normative claim rests here — per
      D-03 normalizer invariants trace to scanner-GREEN, not to this file)
    - .planning/spec/00-CONVENTIONS.md (§1 Section 6/7 shape; §3 status vocabulary
      Draft|Ready|Implemented|Verified and the "MUST NOT advance to Ready unless QUAL-01..05
      satisfied" rule; §4 advisory-marker rule)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference — the
      `### (a)`/`### (b)` lettered Key Decision format with "Settled — do not reopen. Consequence of
      reopening:" footer, and the advisory Code Context layout opening with `<!-- advisory -->` +
      "current as of 2026-06-12" caveat)
    - .planning/spec/04-eta-materialization/SPEC.md (sibling worked reference — the Code Context
      symbol-table layout and the "all line numbers are advisory" caveat)
  </read_first>
  <action>
    Fill the final two sections and update the frontmatter block; keep authoring affirmative-framed
    prose. Do NOT inline fenced code blocks; name symbols in prose/tables.

    Key Decisions (`## Key Decisions`): record the FOUR settled decisions from RESEARCH §"Section 6",
    each as `### (a)`/`### (b)`/`### (c)`/`### (d)` with statement + one-sentence rationale +
    "Settled — do not reopen. Consequence of reopening: <what breaks>":
      (a) Pattern C exclusion — `## N.N.` section headings are not step labels (D-02): the
          `## N.N.` numbered section headings (no "Step" keyword) in `plan-phase.md`,
          `new-milestone.md`, and `new-project.md` are document structure, deliberately excluded
          from all corpus scans and from the normalizer's scan set. State the rule is NORMATIVE and
          the literal three-file list (`PATTERN_C_EXCLUDES`) is a DATED advisory enumeration
          `current as of 2026-06-12`. Consequence of reopening: removing the exclusion mass-flags
          every `## N.N.` section heading in those three files as a false-positive violation, and
          the normalizer would attempt to renumber them as step labels, corrupting document
          structure and blocking CI on every merge touching those files. (This is the settled half
          of the D-02 dual framing; the Out-of-scope Scope bullet is the at-a-glance half.)
      (b) Scanner -> normalizer -> cross-file-ref-scanner internal ordering (D-04): the three-layer
          pipeline is ordered — the scanner detects violations (the gate, run in CI), the normalizer
          fixes them in-place (the remediation, run as a maintenance pass), and the cross-file-ref
          scanner validates that remediation did not strand prose refs (the completeness check, run
          in CI alongside the scanner). A reimplementer MUST preserve this sequencing. Rationale:
          the scanner IS the normalizer's acceptance oracle (per (c)) and the cross-file-ref scanner
          IS its completeness oracle; these relationships define the order. Consequence of
          reopening: reversing or conflating the layers breaks the CI feedback loop — a broken
          normalizer run can pass CI silently. (Satisfies ROADMAP SC2 as an explicit stated
          contract — the Purpose narrative states the same ordering; this Key Decision is the
          durable record per D-04's "Purpose and/or Key Decision" discretion.)
      (c) Normalizer traceability — scanner-GREEN as acceptance oracle (D-03): NO dedicated
          normalizer test exists (`normalize*.test.cjs` is absent) and none is required; the
          normalizer's correctness is fully observable via the two scanner test files running GREEN
          on a post-normalization corpus, so the Acceptance Tests rows for INV-4 and INV-5 cite the
          scanner corpus subtests and are NOT flagged `[MISSING]`. Rationale: a dedicated normalizer
          test would duplicate the scanner contract or assert refactor-fragile internals.
          Consequence of reopening: a dedicated test creates a duplicate oracle that rots
          independently, or asserts implementation internals that break on any refactor; and
          flagging INV-4/INV-5 `[MISSING]` makes Phase 77 demand a test for a gap that does not
          exist.
      (d) Dynamic cross-file ref discovery — no pre-built manifest (D-01 of v2.1.0-d): the
          normalizer discovers cross-file prose references by grepping the entire corpus on every
          run via `XREF_PATTERNS`; there is no pre-built manifest read at startup. Rationale: a
          persisted manifest would need maintenance and could go stale silently. Consequence of
          reopening: a persisted manifest requires a separate "update manifest" step before every
          run; if stale, the normalizer misses cross-file ref updates silently (stranding refs the
          cross-file-ref scanner then flags).

    Code Context (`## Code Context`): mark the section `<!-- advisory -->` and open with a dated
    caveat in the sibling style ("The items below are current as of 2026-06-12. All file paths,
    function names, regex bodies, and line numbers are advisory and will shift on any test edit or
    upstream refactor. No normative invariant depends on these paths or symbols."). List the
    PRE-EXISTING current symbols ONLY here, with no normative claim resting on them, grouped into
    three advisory subsections (RESEARCH §"Section 7"):
      1. `tests/step-numbering-scan.test.cjs` (tier-1; also the normalizer acceptance oracle):
         `SCAN_DIRS`, `PATTERN_C_EXCLUDES`, `STEP_DECIMAL_RE`, `scanContent()` (returns
         `{ patternAB, patternD }`; code-fence toggle), `scanForOutOfOrder()` (per-section counter
         reset on `##`/`###`; list/blockquote-marker strip).
      2. `tests/cross-file-step-refs.test.cjs` (tier-1): `XREF_PATTERNS` (two word-order variants),
         `FILES_BY_BASENAME` (Map keyed by basename -> array of paths; basename-collision
         tolerance), `extractStepSet()` (whole-integer heading + Pattern D; symmetric fence skip),
         `findCrossFileRefs()` (same-file skip).
      3. `scripts/normalize-step-numbers.cjs` (advisory implementation, NOT tier-1):
         `buildRenameMap()` (per-section sequential renumber; `Step 0` preserved), `applyRenameMap()`
         (line-by-line rewrite; symmetric fence skip), `discoverCrossFileRefs()` (dynamic
         corpus-wide ref discovery; merged basename rename maps), `processFile()` (idempotency write
         gate; `--dry-run`), and the `--dry-run` flag + "No changes needed." report. State that the
         normalizer's behavioral contract is narrated in §Invariants (05-INV-4, 05-INV-5) and that
         NO normative claim rests on these paths/function names (per D-03). State that all line
         numbers are advisory and shift on any edit (move-proofing for the upstream refactor).

    Frontmatter transition (block-header frontmatter, NOT YAML): set `**Status:** Ready` (from
    Draft), set `**Confidence:**` to a stated value (High recommended per RESEARCH §"Section 1" —
    Claude's discretion per CONTEXT.md), and set `**Specced:** 2026-06-12`. PRESERVE
    `**Depends on:** SPEC-08` and the `**Reimplementation evidence (tier-1 test):**` line that
    already names both scanner test files (no expansion needed). Advancing to Ready asserts QUAL-01
    (numbered falsifiable EARS invariants), QUAL-02 (traceability maps each MUST to a subtest, no
    MISSING rows), QUAL-03 (advisory-marked paths, move-proof), QUAL-04 (the two tier-1 tests cited),
    QUAL-05 (four Key Decisions with do-not-reopen + consequence) are all satisfied. Do NOT modify
    INDEX.md — the SPEC-05 row Status and the SPEC-08 -> SPEC-05 edge are reconciled by Phase 77.
  </action>
  <verify>
    <automated>grep -q '\*\*Status:\*\* Ready' .planning/spec/05-step-numbering/SPEC.md && grep -q '\*\*Specced:\*\* 2026-06-12' .planning/spec/05-step-numbering/SPEC.md && echo OK</automated>
    <automated>grep -q '\*\*Depends on:\*\* SPEC-08' .planning/spec/05-step-numbering/SPEC.md && echo OK  # SPEC-08 edge preserved</automated>
    <automated>grep -c 'Settled — do not reopen' .planning/spec/05-step-numbering/SPEC.md  # >=4 Key Decisions (a,b,c,d)</automated>
    <automated>grep -q 'Pattern C' .planning/spec/05-step-numbering/SPEC.md && grep -q 'PATTERN_C_EXCLUDES' .planning/spec/05-step-numbering/SPEC.md && grep -q 'discoverCrossFileRefs' .planning/spec/05-step-numbering/SPEC.md && grep -q 'normalize-step-numbers.cjs' .planning/spec/05-step-numbering/SPEC.md && echo OK</automated>
    <automated>grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' .planning/spec/05-step-numbering/SPEC.md  # ==6, locked section set, none renamed/added/reordered</automated>
    <automated>grep -c 'to be filled' .planning/spec/05-step-numbering/SPEC.md  # ==0, no placeholder comments remain</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` contains literal `**Status:** Ready` and `**Specced:** 2026-06-12`; `Confidence` is
      set to a non-placeholder value; `**Depends on:** SPEC-08` is preserved unchanged.
    - `grep -c 'Settled — do not reopen' SPEC.md` returns >=4 (the four Key Decisions a, b, c, d).
    - All four Key Decisions are present and identifiable: Pattern C exclusion (`grep -q 'Pattern C'`),
      the scanner->normalizer->cross-file-ref ordering, scanner-GREEN traceability, and dynamic
      cross-file ref discovery each appear with a consequence-of-reopening sentence.
    - The `## Code Context` section is marked `<!-- advisory -->` and lists `PATTERN_C_EXCLUDES`,
      `scanContent`, `scanForOutOfOrder`, `extractStepSet`, `findCrossFileRefs`, `buildRenameMap`,
      `discoverCrossFileRefs`, and `normalize-step-numbers.cjs`; no normative invariant text appears
      in Code Context.
    - The six body section headers appear exactly once each in the locked order
      (`grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$'`
      returns 6) — no section renamed/added/removed vs 00-CONVENTIONS.md §1.
    - `grep -c 'to be filled' SPEC.md` returns 0 (every placeholder comment replaced).
  </acceptance_criteria>
  <done>
    Four settled Key Decisions (Pattern C exclusion, internal ordering, scanner-GREEN traceability,
    dynamic ref discovery) and advisory Code Context are authored; frontmatter Status is `Ready` with
    Confidence + Specced set and the `Depends on: SPEC-08` + tier-1 evidence lines preserved; ROADMAP
    success criteria 1 (Pattern-C-intentional half), 2, and 3 (advisory/Key-Decisions half) are
    satisfied; and the spec conforms exactly to the locked 7-section template (no drift for Phase 77).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This phase authors a static Markdown documentation artifact (`SPEC.md`). There is no untrusted input, no code execution, no runtime surface, no network call, and no secret handled. The step-numbering scanners and normalizer the spec describes run at test/maintenance time over the committed corpus (file read + regex match + optional file write), but specifying them in prose introduces none of that surface — standard STRIDE injection/auth/DoS categories do not apply to a prose deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Information disclosure / Tampering (spec INACCURACY) | SPEC.md normative claims (Invariants + Acceptance Tests) | mitigate | Every invariant and Acceptance Tests row is traced to a real verbatim subtest in the two tier-1 files `tests/step-numbering-scan.test.cjs` and `tests/cross-file-step-refs.test.cjs` (per QUAL-02/QUAL-04 / D-01 — no `[MISSING]` rows; INV-4/INV-5 cite scanner-GREEN as the acceptance oracle per D-03, which is real indirect coverage, not a planted false `[MISSING]` and not a planted false coverage claim); no normative claim rests on an advisory path/symbol/regex/function the upstream refactor could move (per QUAL-03 advisory-marking under Code Context); corpus counts (SCAN_DIRS, the three Pattern C files, the historical violating-file count) are narrated as `current as of 2026-06-12` shape, not durable counts (per D-05); the basename-collision tolerance and symmetric fence skip are stated explicitly so a reimplementer does not assume one-file-per-basename or asymmetric fence handling. |
| T-74-02 | Tampering (advisory-claim drift) | SPEC.md Code Context paths/symbols | accept | Current paths, symbol names (`scanContent`, `scanForOutOfOrder`, `extractStepSet`, `findCrossFileRefs`, `buildRenameMap`, `applyRenameMap`, `discoverCrossFileRefs`, `processFile`, `STEP_DECIMAL_RE`, `PATTERN_D_RE`, `XREF_PATTERNS`, `SCAN_DIRS`, `PATTERN_C_EXCLUDES`), regex bodies, and line numbers will shift on any source edit or upstream refactor; this is accepted because they are confined to `## Code Context` and marked `<!-- advisory -->`, so no contract depends on them. |

No package-manager installs occur in this phase, so the `T-{phase}-SC` supply-chain row and the
package-legitimacy checkpoint do not apply. No high-severity threats are present.
</threat_model>

<verification>
Overall phase checks (run after both tasks):
- `grep -c '05-INV-' SPEC.md` >= 5 (five invariants present).
- `grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' SPEC.md` == 6 (locked section set, exact order, no drift vs 00-CONVENTIONS.md §1).
- Both tier-1 files are cited: `grep -q 'tests/step-numbering-scan.test.cjs' SPEC.md` and `grep -q 'tests/cross-file-step-refs.test.cjs' SPEC.md` both succeed.
- `grep -c 'MISSING — write test first' SPEC.md` == 0 (every MUST invariant has a real subtest — INV-4/INV-5 cite scanner-GREEN per D-03).
- `grep -c 'Settled — do not reopen' SPEC.md` >= 4 (Key Decisions a, b, c, d — QUAL-05); `grep -q 'Pattern C' SPEC.md` succeeds (intentional exclusion dual-framed — D-02).
- `grep -q 'basename' SPEC.md` succeeds (cross-file-ref basename-collision tolerance — INV-3).
- `grep -q 'No changes needed' SPEC.md` succeeds (normalizer idempotency report — INV-4).
- `grep -q 'current as of 2026-06-12' SPEC.md` succeeds (dated advisory enumerations — D-05).
- `grep -q '\*\*Status:\*\* Ready' SPEC.md`, `grep -q '\*\*Specced:\*\* 2026-06-12' SPEC.md`, and `grep -q '\*\*Depends on:\*\* SPEC-08' SPEC.md` all succeed.
- `grep -c 'to be filled' SPEC.md` == 0 (no placeholder comments remain).
</verification>

<success_criteria>
Measurable completion — maps to the three ROADMAP Phase 74 success criteria:
1. SPEC.md specifies the scanner (decimal + letter-suffix + out-of-order detection), the
   `normalize-step-numbers.cjs` cross-file-aware idempotent CLI, the cross-file-step-refs scanner,
   and the explicit Pattern C exclusion as an INTENDED exclusion (dual-framed: Out-of-scope Scope
   bullet + settled Key Decision). [SPEC-05]
2. The scanner -> normalizer -> cross-file-ref-scanner internal ordering is stated explicitly within
   the spec (Purpose narrative and a dedicated Key Decision). [SPEC-05]
3. Invariants are five numbered EARS statements citing `tests/step-numbering-scan.test.cjs` and
   `tests/cross-file-step-refs.test.cjs` as tier-1; corpus counts recorded `current as of
   2026-06-12` with the shape (not the count) normative; the Acceptance Tests traceability table
   maps each MUST to a real subtest with no MISSING rows; paths/symbols advisory-marked; four Key
   Decisions settled with consequence-of-reopening; Status advanced `Draft -> Ready`.
   [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]
</success_criteria>

<output>
Create `.planning/phases/74-spec-05-step-numbering/74-01-SUMMARY.md` when done.
</output>
