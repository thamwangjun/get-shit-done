---
phase: 49-survey-and-normalization
plan: "12"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/quick.md
  - tests/quick-branching.test.cjs
  - tests/bug-2432-quick-plan-predispatch-commit.test.cjs
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/quick.md contains '**Step 3: Handle quick-task branching**' (not Step 2.5)"
    - "get-shit-done/workflows/quick.md contains '**Step 9: Plan-checker loop**' (not Step 5.5)"
    - "get-shit-done/workflows/quick.md step sequence goes to Step 15 (was Step 8)"
    - "tests/quick-branching.test.cjs assertion updated to reference 'Step 3: Handle quick-task branching'"
    - "tests/bug-2432-quick-plan-predispatch-commit.test.cjs indexOf calls updated to 'Step 9', 'Step 10', 'Step 11:'"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "get-shit-done/workflows/quick.md"
      provides: "Renamed step labels — 15 sequential whole-integer steps"
      contains: "Step 3: Handle quick-task branching"
    - path: "tests/quick-branching.test.cjs"
      provides: "Updated assertion for Step 2.5 → Step 3"
      contains: "Step 3: Handle quick-task branching"
    - path: "tests/bug-2432-quick-plan-predispatch-commit.test.cjs"
      provides: "Updated indexOf calls for steps 5.5→9, 5.6→10, 6→11"
      contains: "Step 9"
  key_links:
    - from: "get-shit-done/workflows/quick.md"
      to: "tests/quick-branching.test.cjs"
      via: "co-located assertion must match new step label (Step 3)"
      pattern: "Step 2.5"
    - from: "get-shit-done/workflows/quick.md"
      to: "tests/bug-2432-quick-plan-predispatch-commit.test.cjs"
      via: "3 co-located indexOf assertions must use new step numbers"
      pattern: "Step 5.5|Step 5.6|Step 6:"
---

<objective>
Rename all 7 decimal step violations in `get-shit-done/workflows/quick.md` to sequential whole integers (full 15-step renumber from Step 1 to Step 15). Update same-file prose cross-references and TWO co-located test files in the same commit.

Purpose: NORM-01. Seven decimal steps (2.5, 4.5, 4.75, 5.5, 5.6, 6.25, 6.5) require a full 15-step renumber. Co-located test assertions in two test files MUST be updated in the same commit (truthy -1 pitfall warning).

Output: `get-shit-done/workflows/quick.md` with steps 1-15; `tests/quick-branching.test.cjs` and `tests/bug-2432-quick-plan-predispatch-commit.test.cjs` with updated assertions.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all steps in quick.md and update two co-located test files</name>
  <files>get-shit-done/workflows/quick.md, tests/quick-branching.test.cjs, tests/bug-2432-quick-plan-predispatch-commit.test.cjs</files>
  <read_first>
    - get-shit-done/workflows/quick.md (read the full file — see all 15 step labels, their exact format, and any same-file prose cross-references)
    - tests/quick-branching.test.cjs (read the full file — find the regex and indexOf assertion for "Step 2.5: Handle quick-task branching")
    - tests/bug-2432-quick-plan-predispatch-commit.test.cjs (read the full file — find the three indexOf calls: 'Step 5.5', 'Step 5.6', 'Step 6:')
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A8: get-shit-done/workflows/quick.md" for exact 15-step renaming map and exact test assertion updates)
  </read_first>
  <action>
Apply the full renaming map to `get-shit-done/workflows/quick.md` (per RESEARCH.md Section A8):

Rename these step labels (old → new), preserving all text after the step number:
- Step 1 → Step 1 (no change — Parse arguments)
- Step 2 → Step 2 (no change — Initialize)
- `**Step 2.5: Handle quick-task branching**` → `**Step 3: Handle quick-task branching**`
- Step 3 → Step 4 (Create task directory)
- Step 4 → Step 5 (Create quick task directory)
- `**Step 4.5: Discussion phase**` → `**Step 6: Discussion phase**`
- `**Step 4.75: Research phase**` → `**Step 7: Research phase**`
- Step 5 → Step 8 (Spawn planner)
- `**Step 5.5: Plan-checker loop**` → `**Step 9: Plan-checker loop**`
- `**Step 5.6: Pre-dispatch plan commit**` → `**Step 10: Pre-dispatch plan commit**`
- Step 6 → Step 11 (Spawn executor)
- `**Step 6.25: Code review**` → `**Step 12: Code review**`
- `**Step 6.5: Verification**` → `**Step 13: Verification**`
- Step 7 → Step 14 (Update STATE.md)
- Step 8 → Step 15 (Final commit)

Apply renames from BOTTOM to TOP (Step 8→15 first, then downward to Step 2.5→3 last) to prevent conflicts.

After renaming headings/labels, grep for same-file prose cross-references:
`command grep -n "step [0-9]\+\(\.[0-9]\+\)\?\|Step [0-9]\+\(\.[0-9]\+\)\?" get-shit-done/workflows/quick.md`

Update any prose references found (e.g., "see step 5.5" → "see step 9").

**Update tests/quick-branching.test.cjs** (per RESEARCH.md):
- Find the regex: `/^\*\*Step 2\.5:\s*Handle quick-task branching\*\*\s*$/` → change to `/^\*\*Step 3:\s*Handle quick-task branching\*\*\s*$/`
- Find: `content.indexOf('Step 2.5: Handle quick-task branching')` → change to `content.indexOf('Step 3: Handle quick-task branching')`
- WARNING: The indexOf call is the truthy -1 pitfall — the old string returns -1 after rename. Verify the new assertion passes.

**Update tests/bug-2432-quick-plan-predispatch-commit.test.cjs** (per RESEARCH.md):
- Find: `content.indexOf('Step 5.5')` → change to `content.indexOf('Step 9')`
- Find: `content.indexOf('Step 5.6')` → change to `content.indexOf('Step 10')`
- Find: `content.indexOf('Step 6:')` → change to `content.indexOf('Step 11:')`
- WARNING: All three are truthy -1 pitfall risk — run `npm test` to verify these pass after updating.

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 2\.5\|Step 4\.5\|Step 4\.75\|Step 5\.5\|Step 5\.6\|Step 6\.25\|Step 6\.5" get-shit-done/workflows/quick.md` returns no output
    - `command grep -n "Step 3: Handle quick-task branching" get-shit-done/workflows/quick.md` returns a match
    - `command grep -n "Step 9: Plan-checker loop" get-shit-done/workflows/quick.md` returns a match
    - `command grep -n "Step 15" get-shit-done/workflows/quick.md` returns a match (old Step 8 → Step 15)
    - `command grep -n "Step 3.*Handle quick-task" tests/quick-branching.test.cjs` returns a match
    - `command grep -n "Step 2\.5" tests/quick-branching.test.cjs` returns no output
    - `command grep -n "Step 9\b" tests/bug-2432-quick-plan-predispatch-commit.test.cjs` returns a match
    - `command grep -n "Step 5\.5\|Step 5\.6" tests/bug-2432-quick-plan-predispatch-commit.test.cjs` returns no output
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 2\.5\|Step 4\.5\|Step 4\.75\|Step 5\.5\|Step 5\.6\|Step 6\.25\|Step 6\.5" get-shit-done/workflows/quick.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `quick.md` has steps 1-15 as sequential whole integers; no decimal step labels remain
    - `tests/quick-branching.test.cjs` references "Step 3: Handle quick-task branching" (not Step 2.5)
    - `tests/bug-2432-quick-plan-predispatch-commit.test.cjs` indexOf calls updated to Step 9, Step 10, Step 11
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a large markdown workflow file and two test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-12-01 | Tampering | truthy -1 pitfall in quick-branching.test.cjs and bug-2432.test.cjs | mitigate | Run npm test immediately after each test file update; check assertion format — if using assert.ok(indexOf(...)), convert to assert.ok(content.includes(...)) for clarity |
| T-49-12-02 | Tampering | Bottom-up rename required for 15-step sequence | mitigate | Apply Step 8→15 first, then downward to Step 2.5→3 to prevent renaming conflicts |
| T-49-12-03 | Tampering | Step 4.75 mid-sequence creates extra cascade steps | mitigate | RESEARCH.md Section A8 provides the complete verified map; follow it exactly |
</threat_model>

<verification>
- `command grep -c "Step 2\.5\|Step 4\.5\|Step 4\.75\|Step 5\.5\|Step 5\.6\|Step 6\.25\|Step 6\.5" get-shit-done/workflows/quick.md` outputs `0`
- `command grep "Step 3: Handle quick-task" tests/quick-branching.test.cjs` returns a match
- `npm test` passes
</verification>

<success_criteria>
`quick.md` has sequential whole-integer steps 1-15; two co-located test files updated; scanner subtest for this file passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-12-SUMMARY.md` when done.
</output>
