---
phase: 49-survey-and-normalization
plan: "03"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - agents/gsd-phase-researcher.md
  - tests/agent-frontmatter.test.cjs
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "agents/gsd-phase-researcher.md contains '## Step 6: Environment Availability Audit' (not Step 2.6)"
    - "agents/gsd-phase-researcher.md contains '## Step 2: Load Graph Context' (not Step 1.3)"
    - "agents/gsd-phase-researcher.md contains '## Step 3: Architectural Responsibility Mapping' (not Step 1.5)"
    - "tests/agent-frontmatter.test.cjs assertion updated from 'Step 2.6: Environment Availability Audit' to 'Step 6: Environment Availability Audit' (per D-02)"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "agents/gsd-phase-researcher.md"
      provides: "Renamed step headings — all 12 steps are sequential whole integers"
      contains: "Step 6: Environment Availability Audit"
    - path: "tests/agent-frontmatter.test.cjs"
      provides: "Updated assertion for gsd-phase-researcher.md step label"
      contains: "Step 6: Environment Availability Audit"
  key_links:
    - from: "agents/gsd-phase-researcher.md"
      to: "tests/agent-frontmatter.test.cjs"
      via: "co-located assertion must be updated in same commit per D-02"
      pattern: "Step 2.6"
---

<objective>
Rename 4 decimal step violations in `agents/gsd-phase-researcher.md` to sequential whole integers (full 12-step renumber), update the same-file prose reference on line 657, and update the co-located test assertion in `tests/agent-frontmatter.test.cjs` in the same commit.

Purpose: NORM-01 + D-02 (locked). Four decimal steps (1.3, 1.5, 2.5, 2.6) require sequential renumbering of all 12 steps. The agent-frontmatter test assertion for "Step 2.6: Environment Availability Audit" is a co-located assertion — it MUST be updated in the same commit.

NOTE: Plans 03 and 04 both touch `tests/agent-frontmatter.test.cjs`. Plan 04 depends on Plan 03 completing first (wave 3) to avoid file conflicts. This plan (03) is wave 2.

Output: `agents/gsd-phase-researcher.md` with 12 sequential whole-integer steps; `tests/agent-frontmatter.test.cjs` with updated assertion.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all steps in gsd-phase-researcher.md and update co-located test assertion</name>
  <files>agents/gsd-phase-researcher.md, tests/agent-frontmatter.test.cjs</files>
  <read_first>
    - agents/gsd-phase-researcher.md (read the full file — see current step sequence, same-file prose ref on line 657 "continue to Step 1.5 without graph context", and all heading text)
    - tests/agent-frontmatter.test.cjs (read lines referencing 'Step 2.6' to find the exact assertion string and surrounding context)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A2: agents/gsd-phase-researcher.md" for exact renaming map)
  </read_first>
  <action>
Apply the full renaming map to `agents/gsd-phase-researcher.md` (per RESEARCH.md Section A2):

Rename these headings in order (old → new), preserving all heading text after the step number:
- `## Step 1` → `## Step 1` (no change — Receive Scope and Load Context)
- `## Step 1.3` → `## Step 2` (Load Graph Context)
- `## Step 1.5` → `## Step 3` (Architectural Responsibility Mapping)
- `## Step 2` → `## Step 4` (Identify Research Domains)
- `## Step 2.5` → `## Step 5` (Runtime State Inventory)
- `## Step 2.6` → `## Step 6` (Environment Availability Audit)
- `## Step 3` → `## Step 7` (Execute Research Protocol)
- `## Step 4` → `## Step 8` (Validation Architecture Research)
- `## Step 5` → `## Step 9` (Quality Check)
- `## Step 6` → `## Step 10` (Write RESEARCH.md)
- `## Step 7` → `## Step 11` (Commit Research)
- `## Step 8` → `## Step 12` (Return Structured Result)

Apply renames from BOTTOM to TOP of the file so renaming Step 7→11 does not conflict with renaming Step 1.3→2. Start at the end (Step 8→12) and work upward.

After renaming headings, update the same-file prose reference at line ~657: the text "continue to Step 1.5 without graph context" must become "continue to Step 3 without graph context".

Use `command grep -n "step 1\.[35]\|step 2\.[56]\|step [2-8]\b" agents/gsd-phase-researcher.md` to verify no prose references were missed.

Then update `tests/agent-frontmatter.test.cjs`:
- Find the assertion: `content.includes('Step 2.6: Environment Availability Audit')`
- Replace with: `content.includes('Step 6: Environment Availability Audit')`
- Use `command grep -n "Step 2\.6" tests/agent-frontmatter.test.cjs` to find the exact line.

Do NOT touch agent YAML frontmatter. Do NOT modify any other assertions in agent-frontmatter.test.cjs.

Run `npm test` and confirm it passes. The scanner subtest for `agents/gsd-phase-researcher.md` must now pass GREEN.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 1\.3\|Step 1\.5\|Step 2\.5\|Step 2\.6" agents/gsd-phase-researcher.md` returns no output
    - `command grep -n "Step 2: Load Graph Context" agents/gsd-phase-researcher.md` returns a match
    - `command grep -n "Step 6: Environment Availability Audit" agents/gsd-phase-researcher.md` returns a match
    - `command grep -n "Step 12" agents/gsd-phase-researcher.md` returns a match (old Step 8 became Step 12)
    - `command grep -n "Step 3 without graph context\|Step 3: Architectural" agents/gsd-phase-researcher.md` returns matches (prose ref updated)
    - `command grep -n "Step 6: Environment Availability Audit" tests/agent-frontmatter.test.cjs` returns a match
    - `command grep -n "Step 2\.6" tests/agent-frontmatter.test.cjs` returns no output (old assertion removed)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 1\.3\|Step 1\.5\|Step 2\.5\|Step 2\.6" agents/gsd-phase-researcher.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `agents/gsd-phase-researcher.md` has 12 sequential whole-integer steps (Step 1 through Step 12) with no decimal or letter-suffix labels
    - Same-file prose reference "Step 1.5" updated to "Step 3"
    - `tests/agent-frontmatter.test.cjs` assertion updated from "Step 2.6: Environment Availability Audit" to "Step 6: Environment Availability Audit"
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in markdown agent file and test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03-01 | Tampering | agent-frontmatter.test.cjs other assertions | mitigate | Read the file first; edit only the Step 2.6 assertion; npm test validates no other assertions broken |
| T-49-03-02 | Tampering | truthy -1 silent false-pass | mitigate | Run `npm test` immediately after editing; the updated assertion uses `content.includes()` which returns false (not -1) when the string is absent |
| T-49-03-03 | Tampering | bottom-to-top rename order | mitigate | Rename from Step 8 → 12 downward to Step 1.3 → 2 to avoid renaming a target before source is resolved |
</threat_model>

<verification>
- `command grep -c "Step 1\.3\|Step 1\.5\|Step 2\.5\|Step 2\.6" agents/gsd-phase-researcher.md` outputs `0`
- `command grep "Step 6: Environment Availability Audit" tests/agent-frontmatter.test.cjs` returns a match
- `npm test` passes
</verification>

<success_criteria>
`agents/gsd-phase-researcher.md` has sequential whole-integer steps 1-12; `tests/agent-frontmatter.test.cjs` references "Step 6: Environment Availability Audit"; scanner subtest for this file passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-03-SUMMARY.md` when done.
</output>
