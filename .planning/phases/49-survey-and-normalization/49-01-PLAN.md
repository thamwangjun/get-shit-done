---
phase: 49-survey-and-normalization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-survey-and-normalization/49-MAP-01.md
autonomous: true
requirements:
  - MAP-01

must_haves:
  truths:
    - "49-MAP-01.md exists at .planning/phases/49-survey-and-normalization/49-MAP-01.md before any renaming begins (per D-04)"
    - "The index records every prose reference matching 'step N.N' or 'step Na' patterns with source file, source line, target file, and target step number"
    - "No source files in agents/, commands/gsd/, or get-shit-done/workflows/ are modified by this plan"
  artifacts:
    - path: ".planning/phases/49-survey-and-normalization/49-MAP-01.md"
      provides: "Cross-file step reference index (MAP-01)"
      contains: "| Source File |"
  key_links:
    - from: "49-MAP-01.md"
      to: "plans 49-02 through 49-12"
      via: "consumed by cross-file refs plan (49-13) to know which old step numbers became which new numbers"
      pattern: "MAP-01"
---

<objective>
Produce the MAP-01 cross-file step reference index: a Markdown table enumerating every prose reference of the form "filename.md step N", "step N.N", or "step Na" across all in-scope prompt content files. This is a READ-ONLY plan — no source files are modified.

Purpose: D-04 (locked decision) mandates that the MAP-01 index is produced FIRST, before any step renaming begins. The index gives the cross-file refs plan (49-13) the information it needs to know which prose references point where and what the old step number maps to after renaming.

Output: `.planning/phases/49-survey-and-normalization/49-MAP-01.md` — Markdown table with columns: Source File | Source Line | Prose Text (verbatim) | Target File | Target Step | Step Description
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
!`cat .planning/phases/49-survey-and-normalization/49-CONTEXT.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce MAP-01 cross-file step reference index</name>
  <files>.planning/phases/49-survey-and-normalization/49-MAP-01.md</files>
  <read_first>
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "MAP-01: Cross-File Reference Index Format" and Section "B: Cross-File Refs Plan")
    - .planning/phases/49-survey-and-normalization/49-CONTEXT.md (D-04, D-05)
    - get-shit-done/workflows/execute-plan.md (lines 140-150, 366-375, 472-480 — prose refs to execute-phase.md step 5.5)
    - get-shit-done/workflows/autonomous.md (lines 403-415, 493-505, 780-790 — prose refs to step 3a, step 3a.5)
    - get-shit-done/workflows/profile-user.md (lines 60-70, 120-130, 150-160 — prose refs to step 4a, step 4b)
    - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md (line 60 — prose ref to step 5.8)
  </read_first>
  <action>
Survey all in-scope directories for cross-file prose step references, then write 49-MAP-01.md.

Step 1 — Run the survey grep (use `command grep` per CLAUDE.md):
```
command grep -rn "step [0-9]\+\.[0-9]\+\|step [0-9]\+[a-z]\|Step [0-9]\+\.[0-9]\+\|Step [0-9]\+[a-z]" agents/ get-shit-done/workflows/ commands/gsd/
```

Step 2 — For each match, determine whether the reference is:
  (a) A step HEADING in its own file (same-file heading — not a cross-file ref)
  (b) A same-file prose self-reference (e.g., "go back to step 5a" inside plan-review-convergence.md)
  (c) A cross-file prose reference (e.g., "execute-phase.md step 5.5" in execute-plan.md)

Only cross-file prose references (type c) are recorded in MAP-01.

Step 3 — For each cross-file reference identified, read the target file to confirm: (1) what the step heading says, and (2) what the new step number will be after the rename (use the renaming maps in RESEARCH.md Section "Violation Inventory with Step Renaming Maps").

Step 4 — Write 49-MAP-01.md as a Markdown table. Use this exact column structure:

```
| Source File | Source Line | Prose Text (verbatim) | Target File | Old Step | New Step (post-rename) |
```

Known cross-file references to include (verify line numbers by reading the files):
- `get-shit-done/workflows/execute-plan.md` lines 143, 369, 475 → references `execute-phase.md step 5.5` → after rename becomes item 7
- `get-shit-done/workflows/autonomous.md` lines 406, 496, 783 → references `step 3a` and/or `step 3a.5` → determine target file by reading the file; if autonomous.md has `<step name="3a">` elements, these are internal references (not cross-file); if they reference execute-phase.md's steps, record as cross-file
- `get-shit-done/workflows/profile-user.md` lines 63, 122, 153, 154 → references `step 4a`, `step 4b` → determine target file; if they reference gsd-verifier.md's old Step 4b (now Step 9), record accordingly
- `get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` line 60 → references `step 5.8` in execute-phase.md → after rename becomes item 10

Step 5 — Add a preamble section to 49-MAP-01.md explaining what the file contains and confirming it was produced before any renaming.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/49-survey-and-normalization/49-MAP-01.md` exits 0 (file exists)
    - `command grep "| Source File |" .planning/phases/49-survey-and-normalization/49-MAP-01.md` returns a match (table header present)
    - `command grep "execute-plan.md" .planning/phases/49-survey-and-normalization/49-MAP-01.md` returns a match (known cross-file ref included)
    - `command grep "post-merge-gate.md" .planning/phases/49-survey-and-normalization/49-MAP-01.md` returns a match (known cross-file ref included)
    - `git diff --name-only agents/ get-shit-done/workflows/ commands/gsd/` returns no output (no source files modified)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/49-survey-and-normalization/49-MAP-01.md && echo "MAP-01 file exists"</automated>
  </verify>
  <done>
    - `.planning/phases/49-survey-and-normalization/49-MAP-01.md` exists and contains a Markdown table with at least 4 data rows (the known cross-file references from execute-plan.md x3 + post-merge-gate.md x1)
    - Table headers include "Source File", "Source Line", "Target File"
    - No source file in agents/, commands/gsd/, or get-shit-done/workflows/ was modified
    - File confirms it was produced before any renaming (preamble note)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem read | All operations are read-only grep and file reads on local markdown files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | Information Disclosure | 49-MAP-01.md | accept | File contains only file paths and line numbers from the local repo — no secrets or PII |
| T-49-01-02 | Tampering | grep output | accept | grep output is deterministic on a stable corpus; plan is read-only so no writes occur except to the planning artifact |
</threat_model>

<verification>
- `.planning/phases/49-survey-and-normalization/49-MAP-01.md` exists
- File contains a Markdown table with at minimum these source files: execute-plan.md, post-merge-gate.md
- `command grep -rn "step [0-9]" agents/ get-shit-done/workflows/ commands/gsd/ | wc -l` returns output (grep ran successfully)
- No files in agents/, commands/gsd/, get-shit-done/workflows/ have been modified (git diff --name-only is empty for those dirs)
</verification>

<success_criteria>
MAP-01 cross-file step reference index exists at `.planning/phases/49-survey-and-normalization/49-MAP-01.md` and records at minimum the 4+ known cross-file prose references (execute-plan.md x3 + post-merge-gate.md x1), with old and new step numbers for each, produced before any source file renaming.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-01-SUMMARY.md` when done.
</output>
