---
phase: 49-survey-and-normalization
plan: "08"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/reapply-patches.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/reapply-patches.md no longer contains '**Step 5a:**' in prose text"
    - "The drift-check sub-label within Step 5 body is replaced with a non-matching label (no 'Step Na' pattern)"
    - "npm test passes with 0 new failures after this change"
  artifacts:
    - path: "get-shit-done/workflows/reapply-patches.md"
      provides: "Step 5a label removed — drift check uses a plain descriptive label"
      contains: "Step 5"
  key_links:
    - from: "get-shit-done/workflows/reapply-patches.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file"
      pattern: "Step 5a"
---

<objective>
Remove the single letter-suffix violation `**Step 5a:**` from `get-shit-done/workflows/reapply-patches.md` by replacing it with a plain descriptive label (no step number). This is a minimal label-removal change, not a full renumber.

The violation is a bold inline label `**Step 5a: drift check**` within the body of `## Step 5: Hunk Verification Gate`. Per RESEARCH.md Section A9, the recommended fix is to remove the "Step 5a" prefix and use plain descriptive text: `**drift check:**`.

Also update line 377's `Step 5b verification` reference if it refers to this label, in the same commit.

Purpose: NORM-01 + D-01. The `### 5a:` and `### 5b:` headings (which omit the word "Step") are NOT violations per the scanner. Only `**Step 5a:**` at line 298 matches `STEP_DECIMAL_RE`.

Output: `get-shit-done/workflows/reapply-patches.md` with the `**Step 5a:**` label replaced by `**drift check:**` (or similar plain label).
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace **Step 5a:** label with plain descriptive label in reapply-patches.md</name>
  <files>get-shit-done/workflows/reapply-patches.md</files>
  <read_first>
    - get-shit-done/workflows/reapply-patches.md (read the full file — see exact format of `**Step 5a:**` at line ~298, whether `### 5a:` and `### 5b:` headings exist and are confirmed non-violations, and the line ~377 "Step 5b verification" reference)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A9: get-shit-done/workflows/reapply-patches.md" — notes on the three fix options and the line 377 follow-up)
  </read_first>
  <action>
Read `get-shit-done/workflows/reapply-patches.md` first to see the exact context of `**Step 5a:**`. Then:

1. Replace `**Step 5a:**` (at line ~298) with `**drift check:**` — remove only the "Step 5a" prefix and keep the rest of the label text unchanged.

If the format is `**Step 5a: drift check** — ...`, replace with `**drift check** — ...` (removing "Step 5a: " entirely).

2. Check line ~377 for `Step 5b verification`:
   - Use `command grep -n "Step 5b\|5b" get-shit-done/workflows/reapply-patches.md` to find the line
   - If "Step 5b" appears in prose and refers to the drift-check context, replace with the plain label form (e.g., "drift check verification" or descriptive text matching what the step does)

3. Confirm the `### 5a:` and `### 5b:` headings are NOT changed — they do NOT contain the word "Step" and are not scanner violations.

After editing, run `command grep -n "Step 5[a-z]" get-shit-done/workflows/reapply-patches.md` to confirm 0 violations remain.

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "\*\*Step 5a" get-shit-done/workflows/reapply-patches.md` returns no output (bold Step 5a label gone)
    - `command grep -n "Step 5a\|step 5a" get-shit-done/workflows/reapply-patches.md` returns no output (no Step Na prose either)
    - `command grep -n "### 5a:\|### 5b:" get-shit-done/workflows/reapply-patches.md` returns matches (these headings are unchanged — they are not violations)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 5[a-z]" get-shit-done/workflows/reapply-patches.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `reapply-patches.md` has no `Step Na` labels (Step 5a is gone); `### 5a:` and `### 5b:` headings remain (non-violations)
    - Any prose reference to "Step 5b" at line ~377 updated to use a plain descriptive label
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown workflow file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-08-01 | Tampering | Accidentally changing non-violation ### 5a: headings | mitigate | Read file first to confirm their exact format; only change `**Step 5a:**` (with the word "Step") |
| T-49-08-02 | Tampering | Line 377 Step 5b ref left stale | mitigate | grep for Step 5b explicitly; update in same commit if found |
</threat_model>

<verification>
- `command grep -c "Step 5[a-z]" get-shit-done/workflows/reapply-patches.md` outputs `0`
- `command grep "### 5a:\|### 5b:" get-shit-done/workflows/reapply-patches.md` returns matches (unchanged)
- `npm test` passes
</verification>

<success_criteria>
`reapply-patches.md` has no `Step Na` labels matching the scanner pattern; `### 5a:` and `### 5b:` headings unchanged; scanner subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-08-SUMMARY.md` when done.
</output>
