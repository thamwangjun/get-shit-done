---
phase: 49-survey-and-normalization
plan: "06"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/plan-review-convergence.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/plan-review-convergence.md contains 'step 7' (not 'step 5a') in the 'review again' prose"
    - "get-shit-done/workflows/plan-review-convergence.md step sequence is sequential whole integers (1 through 10)"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "get-shit-done/workflows/plan-review-convergence.md"
      provides: "Renamed step labels — all 10 steps sequential whole integers"
      contains: "Step 7"
  key_links:
    - from: "get-shit-done/workflows/plan-review-convergence.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file"
      pattern: "step 5a"
---

<objective>
Rename all step labels in `get-shit-done/workflows/plan-review-convergence.md` to sequential whole integers (Steps 1-10), eliminating the decimal step (Step 1.5) and letter-suffix steps (Step 5a, 5b, 5c, 5d). Update same-file prose cross-references in the same commit.

Purpose: NORM-01 + D-01. The file has Step 1.5 (decimal) and Steps 5a/5b/5c/5d (letter-suffix) — both are violations. After renaming, Steps 5a→7, 5b→8, 5c→9, 5d→10.

Output: `get-shit-done/workflows/plan-review-convergence.md` with sequential whole-integer steps 1-10.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all step labels in plan-review-convergence.md to sequential whole integers</name>
  <files>get-shit-done/workflows/plan-review-convergence.md</files>
  <read_first>
    - get-shit-done/workflows/plan-review-convergence.md (read the full file — see all step labels, whether they are ## headings or bold inline labels, and all same-file prose cross-references particularly at line 107 "skip to step 5" and line 322 "step 5a")
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A6: get-shit-done/workflows/plan-review-convergence.md" for exact renaming map)
  </read_first>
  <action>
Apply the full renaming map to `get-shit-done/workflows/plan-review-convergence.md` (per RESEARCH.md Section A6):

Rename these step labels (old → new), preserving all text after the step number:
- Step 1 → Step 1 (no change)
- Step 1.5 → Step 2
- Step 2 → Step 3
- Step 3 → Step 4
- Step 4 → Step 5
- Step 5 → Step 6
- Step 5a → Step 7
- Step 5b → Step 8
- Step 5c → Step 9
- Step 5d → Step 10

Apply renames from BOTTOM to TOP of the file (Step 5d→10 first, then upward to Step 1.5→2 last).

After renaming headings/labels, update same-file prose cross-references:
- Line 107: "skip to step 5" prose — after rename, old Step 5 (skip to convergence) becomes Step 6 → update to "skip to step 6"
- Line 322: "go back to **step 5a** (review again)" → update to "go back to **step 7** (review again)"
- Use `command grep -n "step [0-9]\+[a-z]\?\|step [0-9]\+\.[0-9]" get-shit-done/workflows/plan-review-convergence.md` to find all prose references

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 1\.5\|step 1\.5\|Step 5[abcd]\|step 5[abcd]" get-shit-done/workflows/plan-review-convergence.md` returns no output
    - `command grep -n "step 7\|Step 7" get-shit-done/workflows/plan-review-convergence.md` returns a match (old step 5a)
    - `command grep -n "step 6\|Step 6" get-shit-done/workflows/plan-review-convergence.md` returns a match (old step 5)
    - `command grep -n "step 5a" get-shit-done/workflows/plan-review-convergence.md` returns no output (prose ref updated)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 1\.5\|Step 5[abcd]" get-shit-done/workflows/plan-review-convergence.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `plan-review-convergence.md` has steps 1-10 as sequential whole integers with no decimal or letter-suffix labels
    - Prose reference "step 5a" updated to "step 7"; "skip to step 5" updated to "skip to step 6"
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown workflow file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06-01 | Tampering | Bottom-up rename order prevents conflicts | mitigate | Apply Step 5d→10, then 5c→9, 5b→8, 5a→7, 5→6, 4→5, 3→4, 2→3, 1.5→2 in that order |
| T-49-06-02 | Tampering | Prose "skip to step 5" accidentally maps to wrong target | mitigate | Identify what Step 5 represents before and after rename; the old Step 5 (skip target) becomes Step 6 |
</threat_model>

<verification>
- `command grep -c "Step 1\.5\|step 1\.5\|[Ss]tep 5[abcd]" get-shit-done/workflows/plan-review-convergence.md` outputs `0`
- `npm test` passes
</verification>

<success_criteria>
`plan-review-convergence.md` has sequential whole-integer steps 1-10; no decimal or letter-suffix steps remain; scanner subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-06-SUMMARY.md` when done.
</output>
