---
phase: 49-survey-and-normalization
plan: "09"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - commands/gsd/graphify.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "commands/gsd/graphify.md contains '### Step 3 -- Query' (not Step 2a)"
    - "commands/gsd/graphify.md contains '### Step 4 -- Status' (not Step 2b)"
    - "commands/gsd/graphify.md contains '### Step 5 -- Diff' (not Step 2c)"
    - "commands/gsd/graphify.md step sequence ends at Step 6 (old Step 3 renamed)"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "commands/gsd/graphify.md"
      provides: "Renamed step headings — letter-suffix Steps 2a/2b/2c renamed to Steps 3/4/5"
      contains: "Step 3 -- Query"
  key_links:
    - from: "commands/gsd/graphify.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file"
      pattern: "Step 2a|Step 2b|Step 2c"
---

<objective>
Rename the three letter-suffix step violations in `commands/gsd/graphify.md`: Step 2a (Query), Step 2b (Status), Step 2c (Diff) → Steps 3, 4, 5. The existing Step 3 (Build inline) becomes Step 6.

Purpose: NORM-01 + D-01 (letter-suffix steps are violations). The `### Step 2a` through `### Step 2c` headings are sub-sections of Step 2 (Parse Argument). After renaming, the full step sequence is: Step 0, Step 1, Step 2, Step 3 (Query), Step 4 (Status), Step 5 (Diff), Step 6 (Build).

Output: `commands/gsd/graphify.md` with sequential whole-integer steps 0-6.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Step 2a/2b/2c to Step 3/4/5 in graphify.md (and Step 3 → Step 6)</name>
  <files>commands/gsd/graphify.md</files>
  <read_first>
    - commands/gsd/graphify.md (read the full file — see exact heading format for Step 2a/2b/2c and Step 3, and any same-file prose references to these steps)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A10: commands/gsd/graphify.md" for exact renaming map)
  </read_first>
  <action>
Apply the full renaming map to `commands/gsd/graphify.md` (per RESEARCH.md Section A10):

Rename these headings (old → new), preserving all text after the step number:
- `## Step 0` → `## Step 0` (no change — Banner)
- `## Step 1` → `## Step 1` (no change — Config Gate)
- `## Step 2` → `## Step 2` (no change — Parse Argument)
- `### Step 2a -- Query` → `### Step 3 -- Query`
- `### Step 2b -- Status` → `### Step 4 -- Status`
- `### Step 2c -- Diff` → `### Step 5 -- Diff`
- `## Step 3` → `## Step 6` (Build Inline — old Step 3 becomes Step 6)

Apply renames from BOTTOM to TOP (Step 3→6 first, then Step 2c→5, 2b→4, 2a→3 last).

After renaming headings, grep for any prose that references step 2a, 2b, 2c, or step 3 by number:
`command grep -n "step 2[abc]\|step 3\|Step 2[abc]\|Step 3" commands/gsd/graphify.md`

Update any prose references found (e.g., "see step 2a" → "see step 3").

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 2[abc]\|step 2[abc]" commands/gsd/graphify.md` returns no output
    - `command grep -n "Step 3 -- Query" commands/gsd/graphify.md` returns a match
    - `command grep -n "Step 4 -- Status" commands/gsd/graphify.md` returns a match
    - `command grep -n "Step 5 -- Diff" commands/gsd/graphify.md` returns a match
    - `command grep -n "Step 6" commands/gsd/graphify.md` returns a match (old Step 3 Build Inline)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 2[abc]" commands/gsd/graphify.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `graphify.md` has sequential whole-integer steps 0-6; no letter-suffix steps remain
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown command file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-09-01 | Tampering | Bottom-up rename prevents Step 3→6 conflicting with Step 2a→3 | mitigate | Rename Step 3→6 first, then Step 2c→5, 2b→4, 2a→3 |
| T-49-09-02 | Tampering | Prose refs to steps 2a/2b/2c missed | mitigate | command grep after editing to verify 0 remaining occurrences |
</threat_model>

<verification>
- `command grep -c "Step 2[abc]" commands/gsd/graphify.md` outputs `0`
- `npm test` passes
</verification>

<success_criteria>
`graphify.md` has sequential whole-integer steps 0-6; no letter-suffix steps remain; scanner subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-09-SUMMARY.md` when done.
</output>
