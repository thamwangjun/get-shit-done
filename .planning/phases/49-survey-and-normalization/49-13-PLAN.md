---
phase: 49-survey-and-normalization
plan: "13"
type: execute
wave: 4
depends_on:
  - "49-02"
  - "49-03"
  - "49-04"
  - "49-05"
  - "49-06"
  - "49-07"
  - "49-08"
  - "49-09"
  - "49-10"
  - "49-11"
  - "49-12"
files_modified:
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/autonomous.md
  - get-shit-done/workflows/profile-user.md
  - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/execute-plan.md no longer contains 'execute-phase.md step 5.5' (updated to new item number after execute-phase.md rename)"
    - "get-shit-done/workflows/autonomous.md no longer contains 'step 3a' or 'step 3a.5' in prose that matches STEP_DECIMAL_RE"
    - "get-shit-done/workflows/profile-user.md no longer contains 'step 4a' or 'step 4b' in prose that matches STEP_DECIMAL_RE"
    - "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md line 60 '(same as step 5.8)' updated to reflect renamed ordered-list item (5.8 → 10)"
    - "node --test tests/step-numbering-scan.test.cjs passes GREEN with 0 subtest failures"
    - "npm test passes with 0 new failures"
  artifacts:
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Updated cross-file prose refs to execute-phase.md (was step 5.5, now item 7)"
    - path: "get-shit-done/workflows/autonomous.md"
      provides: "Updated prose refs — step 3a, step 3a.5 resolved per MAP-01 index"
    - path: "get-shit-done/workflows/profile-user.md"
      provides: "Updated prose refs — step 4a, step 4b resolved per MAP-01 index"
    - path: "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md"
      provides: "Updated cross-file prose ref (same as step 5.8) → (same as step 10)"
  key_links:
    - from: "get-shit-done/workflows/execute-plan.md"
      to: "get-shit-done/workflows/execute-phase.md"
      via: "prose reference to old step 5.5 (now item 7 after Plan 11 rename)"
      pattern: "step 5.5"
    - from: "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md"
      to: "get-shit-done/workflows/execute-phase.md"
      via: "prose reference to old step 5.8 (now item 10 after Plan 11 rename)"
      pattern: "step 5.8"
---

<objective>
Update all cross-file prose references that still contain decimal or letter-suffix step numbers after all source file renames are complete. This is the FINAL plan in Phase 49 — it runs only after all 11 source rename plans (49-02 through 49-12) have completed.

Per D-03 (locked decision): Cross-file prose reference updates are in a DIFFERENT commit from source file renames. This plan's commit is that separate final commit.

Files to update:
1. `get-shit-done/workflows/execute-plan.md` — 3 prose refs to "execute-phase.md step 5.5" (now item 7)
2. `get-shit-done/workflows/autonomous.md` — prose refs to "step 3a" and "step 3a.5" (resolve per MAP-01)
3. `get-shit-done/workflows/profile-user.md` — prose refs to "step 4a" and "step 4b" (resolve per MAP-01)
4. `get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` — line 60 "(same as step 5.8)" → "(same as step 10)"

After this plan, `node --test tests/step-numbering-scan.test.cjs` must show 0 failures across all corpus subtests.

Output: All four files updated; scanner at 0 failures.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
!`cat .planning/phases/49-survey-and-normalization/49-MAP-01.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cross-file prose refs in execute-plan.md, autonomous.md, profile-user.md, and post-merge-gate.md</name>
  <files>get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/autonomous.md, get-shit-done/workflows/profile-user.md, get-shit-done/workflows/execute-phase/steps/post-merge-gate.md</files>
  <read_first>
    - .planning/phases/49-survey-and-normalization/49-MAP-01.md (the cross-file reference index produced by Plan 01 — use this as the authority on what each prose reference targets and what its new step number is after rename)
    - get-shit-done/workflows/execute-plan.md (read lines 140-150, 366-375, 472-480 to see exact prose text for the three "execute-phase.md step 5.5" references)
    - get-shit-done/workflows/autonomous.md (read lines 403-415, 493-505, 780-790 to see "step 3a" and "step 3a.5" prose; also read the file's step structure to determine if these are internal references to the file's own `<step name="...">` elements or cross-file references)
    - get-shit-done/workflows/profile-user.md (read lines 60-70, 120-130, 150-160 to see "step 4a" and "step 4b" prose; determine if these reference gsd-verifier.md's old Step 4b, now Step 9)
    - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md (read line 60 to see exact "(same as step 5.8)" text; after Plan 11, step 5.8 → item 10)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "B: Cross-File Refs Plan" — lists all known files and their old→new step targets; Section "Open Questions" — autonomous.md and profile-user.md require MAP-01 to resolve)
  </read_first>
  <action>
Read `49-MAP-01.md` first — it contains the authoritative mapping of each prose reference to its target file and new step number.

**File 1: get-shit-done/workflows/execute-plan.md** (3 references)

Per RESEARCH.md Section B, execute-plan.md has three prose references to `execute-phase.md step 5.5` at lines 143, 369, 475. After Plan 11, execute-phase.md's ordered-list item 5.5 became item 7.

- Use `command grep -n "step 5\.5\|5\.5\." get-shit-done/workflows/execute-plan.md` to find all occurrences
- Replace each `execute-phase.md step 5.5` (or equivalent prose) with the new item reference. The exact replacement text should match the surrounding prose style — e.g., "execute-phase.md step 7" or "execute-phase.md item 7". Use whichever form the existing prose uses (e.g., if it says "See execute-phase.md step 5.5", replace "step 5.5" with "step 7").

**File 2: get-shit-done/workflows/autonomous.md** (step 3a, step 3a.5)

Per RESEARCH.md Open Question 2: autonomous.md may use `<step name="...">` XML tags. Read the file structure first.

- If autonomous.md has `<step name="3a">` elements and the prose references are internal (within autonomous.md's own step structure), then these are NOT cross-file references — they reference the file's own steps. In that case, the fix is: rename autonomous.md's own `<step name="3a">` to `<step name="...">` with a non-decimal/non-letter-suffix name, OR replace the `step 3a` prose with a plain name reference.
- If autonomous.md's "step 3a" references execute-phase.md's old Step 3a (which became a sequential step after Plan 11), update to the new step number.
- Use MAP-01.md to determine the correct target. Update the prose to eliminate any `step 3a` or `step 3a.5` text that matches `STEP_DECIMAL_RE`.

After updating, run `command grep -n "step 3a\|step 3a\.5" get-shit-done/workflows/autonomous.md` to confirm 0 remaining scanner violations.

**File 3: get-shit-done/workflows/profile-user.md** (step 4a, step 4b)

Per RESEARCH.md Open Question 1: profile-user.md references "step 4b" (lines 63, 122, 153) and "step 4a" (line 154).

- Read MAP-01.md to determine whether these reference gsd-verifier.md's old Step 4b (now Step 9 after Plan 04), or another file's steps, or profile-user.md's own steps.
- Update the prose to use the new step number or a plain descriptive reference that doesn't match `STEP_DECIMAL_RE`.

After updating, run `command grep -n "step 4[ab]\|step 4a\|step 4b" get-shit-done/workflows/profile-user.md` to confirm 0 remaining violations.

**File 4: get-shit-done/workflows/execute-phase/steps/post-merge-gate.md** (step 5.8)

Plan 05 renamed Step A/Step B but explicitly did NOT change line 60. Now update it:
- Find `(same as step 5.8)` at line 60
- After Plan 11, execute-phase.md's ordered-list item 5.8 became item 10
- Replace with `(same as step 10)`

After updating, run `command grep -n "5\.8" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` to confirm it no longer contains this decimal step reference.

**Final verification:**

Run `node --test tests/step-numbering-scan.test.cjs` and confirm ALL subtests pass GREEN (0 failures).

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "step 5\.5\|step 5\.8" get-shit-done/workflows/execute-plan.md` returns no output
    - `command grep -n "step 3[a-z]\|step 3a\|step 3a\.5" get-shit-done/workflows/autonomous.md` returns no output
    - `command grep -n "step 4[ab]" get-shit-done/workflows/profile-user.md` returns no output
    - `command grep -n "step 5\.8" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns no output
    - `command grep -n "step 10" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns a match (updated cross-file ref)
    - `node --test tests/step-numbering-scan.test.cjs 2>&1 | command grep -c "not ok"` outputs `0` (all subtests pass)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/step-numbering-scan.test.cjs; npm test</automated>
  </verify>
  <done>
    - All four Category B files updated: execute-plan.md, autonomous.md, profile-user.md, post-merge-gate.md
    - No prose references matching STEP_DECIMAL_RE remain in any of the four files
    - `node --test tests/step-numbering-scan.test.cjs` passes GREEN with 0 subtest failures
    - `npm test` exits 0 with 0 new failures
    - Phase 49 scanner goal is achieved: all decimal and letter-suffix step labels eliminated from agents/, commands/gsd/, get-shit-done/workflows/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Prose text substitution in 4 local markdown workflow files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-13-01 | Tampering | autonomous.md step 3a may be internal self-reference, not cross-file | mitigate | Read MAP-01.md to determine target; read autonomous.md's step structure; if internal, rename the internal step element, not a cross-file target |
| T-49-13-02 | Tampering | profile-user.md step 4b target unclear until MAP-01 is read | mitigate | MAP-01 was produced in Plan 01 before any renaming — read it first; it contains the target file for each reference |
| T-49-13-03 | Tampering | execute-plan.md prose replacement uses wrong new item number | mitigate | Confirm Plan 11's execute-phase.md outcome first: ordered-list item 5.5 → item 7. Then replace "step 5.5" with "step 7" in execute-plan.md. |
| T-49-13-04 | Tampering | Accidentally editing source file headings instead of just cross-file prose refs | mitigate | All source file heading renames are done; this plan touches only prose text, not headings. Verify git diff after editing. |
</threat_model>

<verification>
- `node --test tests/step-numbering-scan.test.cjs` passes with 0 failures
- `command grep -rn "step [0-9]\+\.[0-9]\+\|step [0-9]\+[a-z]" get-shit-done/workflows/execute-plan.md get-shit-done/workflows/autonomous.md get-shit-done/workflows/profile-user.md get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns no output
- `npm test` passes
</verification>

<success_criteria>
All four Category B files have their cross-file prose references updated to use new step numbers; `node --test tests/step-numbering-scan.test.cjs` shows 0 failures (all corpus subtests GREEN); `npm test` exits 0 — Phase 49 NORM-01 goal achieved.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-13-SUMMARY.md` when done.
</output>
