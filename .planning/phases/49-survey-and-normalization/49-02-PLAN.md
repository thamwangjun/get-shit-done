---
phase: 49-survey-and-normalization
plan: "02"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - agents/gsd-intel-updater.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "agents/gsd-intel-updater.md contains '### Step 7: Self-Check' (not Step 6.5)"
    - "agents/gsd-intel-updater.md contains '### Step 8: Snapshot' (not Step 7)"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "agents/gsd-intel-updater.md"
      provides: "Renamed step headings (6.5 → 7, 7 → 8)"
      contains: "Step 7: Self-Check"
  key_links:
    - from: "agents/gsd-intel-updater.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file"
      pattern: "Step 6.5"
---

<objective>
Rename the single decimal step violation in `agents/gsd-intel-updater.md`: Step 6.5 (Self-Check) → Step 7, and the existing Step 7 (Snapshot) → Step 8. Update any same-file prose references to these step numbers in the same commit.

Purpose: NORM-01 requires all decimal step labels to be renamed to sequential whole integers. This is an atomic, self-contained rename for one file. Per D-05, each source file gets its own plan.

Output: `agents/gsd-intel-updater.md` with Step 6.5 renamed to Step 7 and old Step 7 renamed to Step 8.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Step 6.5 → Step 7 and Step 7 → Step 8 in gsd-intel-updater.md</name>
  <files>agents/gsd-intel-updater.md</files>
  <read_first>
    - agents/gsd-intel-updater.md (read the full file to see the current step sequence and any same-file prose references to "step 6.5" or "step 7")
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A1: agents/gsd-intel-updater.md" for the exact renaming map)
  </read_first>
  <action>
Apply the following exact renames to `agents/gsd-intel-updater.md`:

1. Find the heading `### Step 6.5: Self-Check` (at line ~259) and rename it to `### Step 7: Self-Check`
2. Find the heading `### Step 7` (or `### Step 7:`) (Snapshot) and rename it to `### Step 8` (preserving the heading text after the colon)

After renaming headings, grep the file for any prose that references "step 6.5", "Step 6.5", "step 7", "Step 7" by number and update them to match the new numbering. Use `command grep -n "step 6\.5\|step 7\|Step 6\.5\|Step 7" agents/gsd-intel-updater.md` to find all occurrences before editing.

Do NOT touch agent YAML frontmatter (`name`, `description`, `tools`, `color`, `hooks`).

After editing, run `npm test` to confirm 0 new failures. The step-numbering scanner subtest for `agents/gsd-intel-updater.md` must now pass.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 6\.5" agents/gsd-intel-updater.md` returns no output (Step 6.5 is gone)
    - `command grep -n "Step 7:" agents/gsd-intel-updater.md | command grep -i "self.check"` returns a match (Step 7: Self-Check exists)
    - `command grep -n "Step 8" agents/gsd-intel-updater.md` returns a match (old Step 7 renamed to Step 8)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 6\.5" agents/gsd-intel-updater.md || echo "0 occurrences — PASS"; npm test</automated>
  </verify>
  <done>
    - `agents/gsd-intel-updater.md` contains `Step 7: Self-Check` and `Step 8` (Snapshot), with no remaining `Step 6.5` occurrences
    - `npm test` passes with 0 new failures
    - The step-numbering scanner no longer flags this file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown agent file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Tampering | agents/gsd-intel-updater.md YAML frontmatter | mitigate | Explicitly do not touch YAML frontmatter; `agent-frontmatter.test.cjs` validates all agents on every npm test run |
| T-49-02-02 | Tampering | npm test silent false-pass (truthy -1) | mitigate | No co-located indexOf assertions for this file; still run npm test to confirm scanner passes |
</threat_model>

<verification>
- `command grep -c "Step 6\.5" agents/gsd-intel-updater.md` outputs `0`
- `command grep "Step 7: Self-Check" agents/gsd-intel-updater.md` returns a match
- `npm test` passes
</verification>

<success_criteria>
`agents/gsd-intel-updater.md` contains no decimal step labels; scanner subtest for this file passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-02-SUMMARY.md` when done.
</output>
