---
phase: 49-survey-and-normalization
plan: "11"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - tests/execute-phase-step-5-5-deviation-doc.test.cjs
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/execute-phase.md ordered list contains item '3.' followed by '4.' (not '2.5.' followed by '3.')"
    - "get-shit-done/workflows/execute-phase.md ordered list item for worktree cleanup is now '7.' (was '5.5.')"
    - "get-shit-done/workflows/execute-phase.md Step 7.0/7.1/7.2/7.3 bold labels renamed to non-decimal forms"
    - "tests/execute-phase-step-5-5-deviation-doc.test.cjs updated — indexOf calls use new item numbers"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Renamed Pattern A/B and Pattern D violations — sequential ordered-list items and non-decimal step labels"
      contains: "7. **"
    - path: "tests/execute-phase-step-5-5-deviation-doc.test.cjs"
      provides: "Updated test assertions using new ordered-list item numbers"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "tests/execute-phase-step-5-5-deviation-doc.test.cjs"
      via: "co-located test uses content.indexOf for list item 5.5 and 5.6 — must be updated"
      pattern: "5.5.|5.6."
---

<objective>
Rename all Pattern A/B and Pattern D violations in `get-shit-done/workflows/execute-phase.md`:
- Pattern D: 5 ordered-list items (2.5., 5.5., 5.6., 5.7., 5.8.) renumbered with cascading update of all subsequent items
- Pattern A/B: 4 bold inline labels (Step 7.0–7.3) renamed to non-decimal labels

Also update same-file prose cross-references and the co-located test file `tests/execute-phase-step-5-5-deviation-doc.test.cjs` in the same commit.

This is the most complex rename in Phase 49 due to the cascading Pattern D renumber and the test file that may also need renaming.

Purpose: NORM-01. Per D-03, cross-file references from execute-plan.md and post-merge-gate.md are updated in the SEPARATE final plan (49-13), not here.

Output: `get-shit-done/workflows/execute-phase.md` with clean ordered-list items and non-decimal step labels; updated test file.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Pattern D ordered-list items and Pattern A/B Step 7.x labels in execute-phase.md</name>
  <files>get-shit-done/workflows/execute-phase.md, tests/execute-phase-step-5-5-deviation-doc.test.cjs</files>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (read the full file — see all ordered-list items in the wave execution step starting at line ~510, the Step 7.x bold labels at lines ~925-949, and same-file prose refs at lines ~527, 660, 809, 811, 813)
    - tests/execute-phase-step-5-5-deviation-doc.test.cjs (read the full file — see extractStep55Block() function with indexOf('\n5.5.') and indexOf('\n5.6.'), and the function name to determine if it needs renaming)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A4: get-shit-done/workflows/execute-phase.md" for exact renaming maps for both Pattern D and Pattern A/B)
  </read_first>
  <action>
**Part 1: Pattern D ordered-list renumber**

The ordered list in the wave execution section currently has items: 1, 2, 2.5, 3, 4, 5, 5.5, 5.6, 5.7, 5.8, 6

Apply this cascading rename (RESEARCH.md Section A4 — Ordered-list renaming map):
- 2.5 → 3
- 3 → 4
- 4 → 5
- 5 → 6
- 5.5 → 7
- 5.6 → 8
- 5.7 → 9
- 5.8 → 10
- 6 → 11

Rename from BOTTOM to TOP (item 6→11 first, then 5.8→10, 5.7→9, 5.6→8, 5.5→7, 5→6, 4→5, 3→4, 2.5→3 last) to avoid conflicts.

**Part 2: Pattern A/B Step 7.x label rename**

Find the bold inline labels inside the Step 7 failure-handling section (around lines 925-949):
- `**Step 7.0 — classify before branching (#3095):**` — read the exact text; this is an intro/classifier label. Options:
  - If it serves as an intro to Step 7's sub-classification, rename it to `**Step 7 — classify before branching (#3095):**` (merging 7.0 into the parent Step 7 label) OR label it as `**Step 7a — classify before branching:**` and then 7.1/7.2/7.3 → Steps 7b/7c/7d
  - PREFERRED approach (matches RESEARCH.md suggestion): Rename to `**Step 7 — classify before branching:**` (removing the `.0`), then rename 7.1→Step 8, 7.2→Step 9, 7.3→Step 10 as SEPARATE sequential steps
  - Read the actual file structure to determine which approach is least disruptive to the existing step flow

After reading the file, apply whichever approach results in no decimal labels remaining. Ensure the chosen Step numbers for 7.1/7.2/7.3 do not conflict with any existing `## Step N` headings in the file.

**Part 3: Same-file prose cross-reference updates**

After all renames, update same-file prose refs:
- Lines mentioning "step 2.5" → update to new item number (3)
- Lines mentioning "step 5.5", "step 5.6" in prose → update to new item numbers (7, 8)
- Use `command grep -n "step [0-9]\+\.[0-9]\+" get-shit-done/workflows/execute-phase.md` to find all

**Part 4: Update co-located test file**

Read `tests/execute-phase-step-5-5-deviation-doc.test.cjs`:
- Find `extractStep55Block()` function using `content.indexOf('\n5.5.')` and `content.indexOf('\n5.6.')`
- Replace `'\n5.5.'` with `'\n7.'` (new item number)
- Replace `'\n5.6.'` with `'\n8.'` (new item number)
- Rename the function from `extractStep55Block` to `extractStep7Block` (or similar name reflecting new item number 7)
- Check if the TEST FILE ITSELF should be renamed: RESEARCH.md says rename it to reflect the new step; check `node --test` file discovery pattern. If the test is auto-discovered by glob, rename the file from `tests/execute-phase-step-5-5-deviation-doc.test.cjs` to `tests/execute-phase-step-7-deviation-doc.test.cjs`. Verify with `npm test` that the renamed file is still discovered.

Run `npm test` to confirm 0 new failures after all changes.
  </action>
  <acceptance_criteria>
    - `command grep -n "2\.5\.\|5\.5\.\|5\.6\.\|5\.7\.\|5\.8\." get-shit-done/workflows/execute-phase.md` returns no output (all Pattern D violations gone)
    - `command grep -n "Step 7\.0\|Step 7\.1\|Step 7\.2\|Step 7\.3" get-shit-done/workflows/execute-phase.md` returns no output (all Pattern A/B Step 7.x labels gone)
    - `command grep -n "^\s*7\." get-shit-done/workflows/execute-phase.md` returns a match (item 7 exists in the ordered list)
    - `command grep -n "^\s*11\." get-shit-done/workflows/execute-phase.md` returns a match (old item 6 became item 11)
    - `command grep -rn "5\.5\." tests/execute-phase-step-5-5-deviation-doc.test.cjs tests/execute-phase-step-7-deviation-doc.test.cjs 2>/dev/null` returns no output OR the old test file no longer exists (was renamed)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "2\.5\.\|5\.5\.\|5\.6\.\|5\.7\.\|5\.8\." get-shit-done/workflows/execute-phase.md || echo "0 Pattern D — PASS"; command grep -c "Step 7\.[0-3]" get-shit-done/workflows/execute-phase.md || echo "0 Pattern AB — PASS"; npm test</automated>
  </verify>
  <done>
    - `execute-phase.md` has no decimal ordered-list items or decimal Step labels
    - Ordered list items cascade correctly: old 2.5→3, 5.5→7, 5.6→8, 5.7→9, 5.8→10, old 6→11
    - Step 7.x labels renamed (no decimal/letter-suffix forms remain)
    - Same-file prose refs updated
    - Co-located test file updated with new indexOf values and function name; test file renamed if applicable
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a large markdown workflow file and a test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-11-01 | Tampering | Cascading Pattern D renumber creates numbering conflicts if not done bottom-up | mitigate | Always rename from item 6→11 first, down to 2.5→3 last |
| T-49-11-02 | Tampering | truthy -1 pitfall in test asserting indexOf('\n5.5.') | mitigate | Run npm test immediately; the test will catch stale assertions because the assertion uses -1 check (but check test format first — if it uses assert.ok(indexOf(...)) it could false-pass; fix assertion format too) |
| T-49-11-03 | Tampering | Test file rename breaks discovery | mitigate | Verify `npm test` runs the renamed test file; if not discovered, check glob pattern in scripts |
| T-49-11-04 | Tampering | Cross-file refs to step 5.5 in execute-plan.md accidentally updated here | mitigate | Do NOT touch execute-plan.md in this plan; those are cross-file refs for plan 49-13 (D-03) |
</threat_model>

<verification>
- `command grep -c "2\.5\.\|5\.5\.\|5\.6\.\|5\.7\.\|5\.8\." get-shit-done/workflows/execute-phase.md` outputs `0`
- `command grep -c "Step 7\.[0-3]" get-shit-done/workflows/execute-phase.md` outputs `0`
- `npm test` passes with the deviation-doc test still running (check that test appears in output)
</verification>

<success_criteria>
`execute-phase.md` has no decimal ordered-list items (Pattern D) or decimal/letter-suffix step labels (Pattern A/B); co-located test file assertions updated; scanner subtests for this file pass GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-11-SUMMARY.md` when done.
</output>
