---
phase: 49-survey-and-normalization
plan: "04"
type: execute
wave: 3
depends_on:
  - "49-03"
files_modified:
  - agents/gsd-verifier.md
  - tests/agent-frontmatter.test.cjs
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "agents/gsd-verifier.md contains '## Step 9: Data-Flow Trace' (not Step 4b)"
    - "agents/gsd-verifier.md contains '## Step 13: Behavioral Spot-Checks' (not Step 7b)"
    - "agents/gsd-verifier.md contains steps 0 through 18 as sequential whole integers"
    - "tests/agent-frontmatter.test.cjs assertion updated from 'Step 4b: Data-Flow Trace' to 'Step 9: Data-Flow Trace' (per D-02)"
    - "tests/agent-frontmatter.test.cjs assertion updated from 'Step 7b: Behavioral Spot-Checks' to 'Step 13: Behavioral Spot-Checks' (per D-02)"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "agents/gsd-verifier.md"
      provides: "Renamed step headings — 8 letter-suffix steps renamed to sequential whole integers (0-18)"
      contains: "Step 9: Data-Flow Trace"
    - path: "tests/agent-frontmatter.test.cjs"
      provides: "Updated assertions for gsd-verifier.md step labels (both Step 9 and Step 13)"
      contains: "Step 13: Behavioral Spot-Checks"
  key_links:
    - from: "agents/gsd-verifier.md"
      to: "tests/agent-frontmatter.test.cjs"
      via: "two co-located assertions must be updated in same commit per D-02"
      pattern: "Step 4b|Step 7b"
---

<objective>
Rename 8 letter-suffix step violations in `agents/gsd-verifier.md` to sequential whole integers (full 19-step renumber from Step 0 to Step 18), update same-file prose cross-references, and update TWO co-located test assertions in `tests/agent-frontmatter.test.cjs` in the same commit.

Purpose: NORM-01 + D-01 (letter-suffix steps are violations) + D-02 (locked — co-located assertions must update in same commit). This plan DEPENDS ON plan 49-03 because both plans touch `tests/agent-frontmatter.test.cjs` and they must not run concurrently.

Output: `agents/gsd-verifier.md` with 19 sequential whole-integer steps (0-18); `tests/agent-frontmatter.test.cjs` with two updated assertions for gsd-verifier.md.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all letter-suffix steps in gsd-verifier.md and update co-located test assertions</name>
  <files>agents/gsd-verifier.md, tests/agent-frontmatter.test.cjs</files>
  <read_first>
    - agents/gsd-verifier.md (read the full file — see all step headings including Step 2a/2b/2c as bold inline labels, Step 3b/4b/7b/7c/9b as ## headings, and all same-file prose refs at lines 148-149 and 628)
    - tests/agent-frontmatter.test.cjs (read current state — Plan 03 already updated it; find the Step 4b and Step 7b assertions by searching for them)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A3: agents/gsd-verifier.md" for exact 19-step renaming map)
  </read_first>
  <action>
Apply the full renaming map to `agents/gsd-verifier.md` (per RESEARCH.md Section A3). The file has 19 steps (0 through 18 after rename):

Rename these headings/labels (old → new), preserving all text after the step number:
- Step 0 → Step 0 (no change — Check for Previous Verification)
- Step 1 → Step 1 (no change — Load Context)
- Step 2 → Step 2 (no change — Establish Must-Haves)
- `**Step 2a:** (or **Step 2a: Always load...)` → `**Step 3:**` (Always load ROADMAP Success Criteria)
- `**Step 2b:**` → `**Step 4:**` (Load PLAN frontmatter must-haves)
- `**Step 2c:**` → `**Step 5:**` (Merge must-haves)
- `## Step 3` → `## Step 6` (Verify Observable Truths)
- `## Step 3b` → `## Step 7` (Check Verification Overrides)
- `## Step 4` → `## Step 8` (Verify Artifacts)
- `## Step 4b` → `## Step 9` (Data-Flow Trace)
- `## Step 5` → `## Step 10` (Verify Key Links)
- `## Step 6` → `## Step 11` (Check Requirements Coverage)
- `## Step 7` → `## Step 12` (Scan for Anti-Patterns)
- `## Step 7b` → `## Step 13` (Behavioral Spot-Checks)
- `## Step 7c` → `## Step 14` (Probe Execution)
- `## Step 8` → `## Step 15` (Identify Human Verification Needs)
- `## Step 9` → `## Step 16` (Determine Overall Status)
- `## Step 9b` → `## Step 17` (Filter Deferred Items)
- `## Step 10` → `## Step 18` (Structure Gap Output)

Apply renames from BOTTOM to TOP of the file (Step 10→18 first, then upward to Step 2a→3 last) to prevent conflicts.

After renaming headings, update same-file prose references:
- Lines 148-149 reference "Step 2a" and "Step 2b" by name → update to "Step 3" and "Step 4"
- Line 628 references "Step 9b" → update to "Step 17"
- Use `command grep -n "step 2[abc]\|step 3b\|step 4b\|step 7[bc]\|step 9b" agents/gsd-verifier.md` to find all prose references

Then update `tests/agent-frontmatter.test.cjs` (Plan 03 has already run — do not disturb its changes):
- Find `content.includes('Step 4b: Data-Flow Trace')` → change to `content.includes('Step 9: Data-Flow Trace')`
- Find `content.includes('Step 7b: Behavioral Spot-Checks')` → change to `content.includes('Step 13: Behavioral Spot-Checks')`
- Use `command grep -n "Step 4b\|Step 7b" tests/agent-frontmatter.test.cjs` to find both lines

Do NOT touch agent YAML frontmatter. Do NOT modify the Step 6 assertion (set by Plan 03).

Run `npm test` and confirm it passes with 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 2[abc]\|Step 3b\|Step 4b\|Step 7[bc]\|Step 9b" agents/gsd-verifier.md` returns no output
    - `command grep -n "Step 9: Data-Flow Trace" agents/gsd-verifier.md` returns a match
    - `command grep -n "Step 13: Behavioral Spot-Checks" agents/gsd-verifier.md` returns a match
    - `command grep -n "Step 17: Filter Deferred Items\|Step 17" agents/gsd-verifier.md` returns a match
    - `command grep -n "Step 9: Data-Flow Trace" tests/agent-frontmatter.test.cjs` returns a match
    - `command grep -n "Step 13: Behavioral Spot-Checks" tests/agent-frontmatter.test.cjs` returns a match
    - `command grep -n "Step 4b\|Step 7b" tests/agent-frontmatter.test.cjs` returns no output (old assertions gone)
    - `command grep -n "Step 6: Environment Availability Audit" tests/agent-frontmatter.test.cjs` still returns a match (Plan 03 assertion untouched)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 2[abc]\|Step 3b\|Step 4b\|Step 7[bc]\|Step 9b" agents/gsd-verifier.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `agents/gsd-verifier.md` has steps 0-18 as sequential whole integers with no letter-suffix labels
    - Same-file prose references to old step names updated (Step 2a → Step 3, Step 2b → Step 4, Step 9b → Step 17)
    - `tests/agent-frontmatter.test.cjs` has "Step 9: Data-Flow Trace" and "Step 13: Behavioral Spot-Checks" assertions; old "Step 4b" and "Step 7b" assertions removed
    - Plan 03's "Step 6: Environment Availability Audit" assertion is still intact
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in markdown agent file and shared test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Tampering | Plan 03 changes in agent-frontmatter.test.cjs | mitigate | Read file after Plan 03 commits; verify Step 6 assertion still present before saving |
| T-49-04-02 | Tampering | Bold inline Step 2a/2b/2c labels (not ## headings) | mitigate | Use command grep to find exact format; may be `**Step 2a:**` or `**Step 2a: text**` — match actual format in file |
| T-49-04-03 | Tampering | truthy -1 silent false-pass in test assertions | mitigate | Both assertions use content.includes(); run npm test immediately after to catch any -1 returns |
</threat_model>

<verification>
- `command grep -c "Step [0-9]\+[abc]" agents/gsd-verifier.md` outputs `0`
- `command grep "Step 9: Data-Flow Trace" tests/agent-frontmatter.test.cjs` returns a match
- `command grep "Step 13: Behavioral Spot-Checks" tests/agent-frontmatter.test.cjs` returns a match
- `npm test` passes
</verification>

<success_criteria>
`agents/gsd-verifier.md` has sequential whole-integer steps 0-18; `tests/agent-frontmatter.test.cjs` references "Step 9: Data-Flow Trace" and "Step 13: Behavioral Spot-Checks"; scanner subtest for this file passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-04-SUMMARY.md` when done.
</output>
