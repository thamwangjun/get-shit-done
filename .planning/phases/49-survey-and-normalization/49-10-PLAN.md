---
phase: 49-survey-and-normalization
plan: "10"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/discuss-phase-assumptions.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/discuss-phase-assumptions.md has a '###' section heading between the two independent Step 1/2/3 sequences"
    - "The scanner out-of-order subtest for discuss-phase-assumptions.md now passes GREEN"
    - "Neither of the two Step 1/2/3 sequences is renumbered — only a separator heading is added"
    - "npm test passes with 0 new failures after this change"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase-assumptions.md"
      provides: "Added ### section heading before second Step 1 group so scanner counter resets"
      contains: "###"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase-assumptions.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner out-of-order detection resets counter on ### headings"
      pattern: "out-of-order"
---

<objective>
Fix the out-of-order scanner failure in `get-shit-done/workflows/discuss-phase-assumptions.md` by adding a `###` section heading between the two independent Step 1/2/3 sequences. This is a minimal structural fix per D-06 — NO step renumbering required.

The scanner's `scanForOutOfOrder()` function resets its per-section step counter on any `##` or `###` heading. The file has two separate Step 1/2/3 sequences (one about project files, one about codebase maps) without a heading separator — the scanner sees Step 1 again after Step 3 and reports out-of-order. Adding a `###` heading before the second sequence is sufficient to reset the counter.

Heading text is Claude's choice per D-06. Recommended: `### Codebase Context`

Output: `get-shit-done/workflows/discuss-phase-assumptions.md` with one new `###` heading before the second Step 1 group.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### section heading before second Step 1 group in discuss-phase-assumptions.md</name>
  <files>get-shit-done/workflows/discuss-phase-assumptions.md</files>
  <read_first>
    - get-shit-done/workflows/discuss-phase-assumptions.md (read lines 140-250 to see both Step 1/2/3 sequences and the exact location where the heading should be inserted; confirm line ~221 is where the second "Step 1: Check for existing codebase maps" begins)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A11: get-shit-done/workflows/discuss-phase-assumptions.md" — D-06 fix instructions)
  </read_first>
  <action>
Insert a `###` section heading immediately before the second Step 1 group in `get-shit-done/workflows/discuss-phase-assumptions.md`.

The second Step 1 starts at approximately line 221: `**Step 1: Check for existing codebase maps**`

Insert the heading on a new line immediately before this second Step 1 occurrence. Use:
```
### Codebase Context
```

(The heading text is Claude's choice per D-06. "Codebase Context" clearly describes the second group's purpose.)

Do NOT renumber any steps. Do NOT modify any other content. The change is exactly: one new line added.

After inserting the heading, use `command grep -n "Step 1:" get-shit-done/workflows/discuss-phase-assumptions.md` to confirm both Step 1 occurrences are still present (neither was deleted).

Run `node --test tests/step-numbering-scan.test.cjs` to confirm the out-of-order subtest now passes. Then run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "### Codebase Context" get-shit-done/workflows/discuss-phase-assumptions.md` returns a match (heading added)
    - `command grep -c "Step 1:" get-shit-done/workflows/discuss-phase-assumptions.md` returns `2` (both Step 1 occurrences present)
    - `node --test tests/step-numbering-scan.test.cjs 2>&1 | command grep -i "discuss-phase-assumptions"` shows pass or no failure for this file
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "### Codebase Context" get-shit-done/workflows/discuss-phase-assumptions.md; npm test</automated>
  </verify>
  <done>
    - `discuss-phase-assumptions.md` has one new `###` section heading before the second Step 1 group
    - Neither Step 1/2/3 sequence is renumbered
    - Scanner out-of-order subtest passes GREEN for this file
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Single line insertion in a local markdown workflow file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10-01 | Tampering | Inserting heading in wrong location (before first Step 1 instead of second) | mitigate | Read file to identify the SECOND Step 1 occurrence at line ~221; command grep -n "Step 1:" to count occurrences before editing |
| T-49-10-02 | Tampering | Heading level ## instead of ### causes wrong counter reset scope | mitigate | Use `###` exactly — both ## and ### reset the counter, but ### is more semantically appropriate for a sub-section separator |
</threat_model>

<verification>
- `command grep "### Codebase Context" get-shit-done/workflows/discuss-phase-assumptions.md` returns a match
- `command grep -c "Step 1:" get-shit-done/workflows/discuss-phase-assumptions.md` returns `2`
- `npm test` passes
</verification>

<success_criteria>
`discuss-phase-assumptions.md` has a `###` heading before the second Step 1 group; scanner out-of-order subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-10-SUMMARY.md` when done.
</output>
