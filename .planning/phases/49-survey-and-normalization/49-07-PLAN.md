---
phase: 49-survey-and-normalization
plan: "07"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/progress.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/progress.md contains 'Step 2: Check for unaddressed UAT gaps' (not Step 1.5)"
    - "get-shit-done/workflows/progress.md contains 'Step 3: Cross-phase health check' (not Step 1.6)"
    - "get-shit-done/workflows/progress.md step sequence is sequential whole integers 1-5"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "get-shit-done/workflows/progress.md"
      provides: "Renamed step labels — 5 sequential whole-integer steps"
      contains: "Step 2: Check for unaddressed UAT gaps"
  key_links:
    - from: "get-shit-done/workflows/progress.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file"
      pattern: "Step 1.5|Step 1.6"
---

<objective>
Rename the two decimal step violations in `get-shit-done/workflows/progress.md`: Step 1.5 and Step 1.6 require a full 5-step renumber (Steps 1-5). Update same-file prose cross-references in the same commit.

Purpose: NORM-01. Steps 1.5 and 1.6 are decimal violations. The full step sequence becomes: Step 1 (no change), Step 1.5→Step 2, Step 1.6→Step 3, Step 2→Step 4, Step 3→Step 5.

Output: `get-shit-done/workflows/progress.md` with sequential whole-integer steps 1-5.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Step 1.5 and Step 1.6 in progress.md (full 5-step renumber)</name>
  <files>get-shit-done/workflows/progress.md</files>
  <read_first>
    - get-shit-done/workflows/progress.md (read the full file — see all step headings and any prose cross-references, particularly the routing table at line ~242 that references "Step 3")
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A7: get-shit-done/workflows/progress.md" for exact renaming map)
  </read_first>
  <action>
Apply the full renaming map to `get-shit-done/workflows/progress.md` (per RESEARCH.md Section A7):

Rename these step labels (old → new), preserving all text after the step number:
- Step 1 → Step 1 (no change — first step)
- `**Step 1.5: Check for unaddressed UAT gaps**` → `**Step 2: Check for unaddressed UAT gaps**`
- `**Step 1.6: Cross-phase health check**` → `**Step 3: Cross-phase health check**`
- Step 2 → Step 4 (Identify Research Domains or whatever the step 2 heading says)
- Step 3 → Step 5 (final step)

Apply renames from BOTTOM to TOP of the file (Step 3→5 first, then Step 2→4, then Step 1.6→3, then Step 1.5→2 last).

After renaming headings, update same-file prose cross-references:
- The routing table at line ~242 references "Step 3" — after rename, old Step 3 becomes Step 5. Update that reference to "Step 5".
- Use `command grep -n "step 1\.[56]\|step [23]\b\|Step 1\.[56]\|Step [23]\b" get-shit-done/workflows/progress.md` to find all prose references

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step 1\.5\|Step 1\.6" get-shit-done/workflows/progress.md` returns no output
    - `command grep -n "Step 2: Check for unaddressed UAT gaps" get-shit-done/workflows/progress.md` returns a match
    - `command grep -n "Step 3: Cross-phase health check" get-shit-done/workflows/progress.md` returns a match
    - `command grep -n "Step 5" get-shit-done/workflows/progress.md` returns a match (old Step 3)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step 1\.5\|Step 1\.6" get-shit-done/workflows/progress.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `progress.md` has steps 1-5 as sequential whole integers; no decimal steps remain
    - Same-file prose references updated (old Step 3 → Step 5 in routing table)
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown workflow file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07-01 | Tampering | Bottom-up rename order prevents Step 2→4 conflicting with 1.5→2 | mitigate | Rename Step 3→5 first, then Step 2→4, then Step 1.6→3, then Step 1.5→2 |
| T-49-07-02 | Tampering | Routing table prose ref to "Step 3" missed | mitigate | command grep for "Step 3" and "Step [23]" in prose context before finishing edits |
</threat_model>

<verification>
- `command grep -c "Step 1\.5\|Step 1\.6" get-shit-done/workflows/progress.md` outputs `0`
- `npm test` passes
</verification>

<success_criteria>
`progress.md` has sequential whole-integer steps 1-5; no decimal steps remain; scanner subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-07-SUMMARY.md` when done.
</output>
