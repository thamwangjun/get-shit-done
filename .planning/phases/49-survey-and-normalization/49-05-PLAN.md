---
phase: 49-survey-and-normalization
plan: "05"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md
autonomous: true
requirements:
  - NORM-01

must_haves:
  truths:
    - "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md contains '**Step 1' (the Build gate, not Step A)"
    - "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md contains '**Step 2' (the Test gate, not Step B)"
    - "Line 60 '(same as step 5.8)' is NOT changed in this plan — that cross-file reference is handled by plan 49-13"
    - "npm test passes with 0 new failures after this rename"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md"
      provides: "Renamed Step A → Step 1 and Step B → Step 2"
      contains: "Step 1"
  key_links:
    - from: "get-shit-done/workflows/execute-phase/steps/post-merge-gate.md"
      to: "tests/step-numbering-scan.test.cjs"
      via: "scanner must no longer flag this file for Step A/Step B"
      pattern: "Step A|Step B"
---

<objective>
Rename the two letter-suffix step violations in `get-shit-done/workflows/execute-phase/steps/post-merge-gate.md`: Step A (Build gate) → Step 1, and Step B (Test gate) → Step 2.

Important constraint: Line 60 contains `(same as step 5.8)` — this is a CROSS-FILE prose reference to `execute-phase.md` ordered-list item 5.8. Per D-03, it must NOT be updated in this plan. It will be updated in the cross-file refs plan (49-13) after execute-phase.md is renamed.

Purpose: NORM-01 + D-01 (letter-suffix steps are violations). This is a simple two-heading rename.

Output: `get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` with Step A → Step 1 and Step B → Step 2.
</objective>

<context>
!`cat .planning/phases/49-survey-and-normalization/49-RESEARCH.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Step A → Step 1 and Step B → Step 2 in post-merge-gate.md</name>
  <files>get-shit-done/workflows/execute-phase/steps/post-merge-gate.md</files>
  <read_first>
    - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md (read the full file to see exact heading formats and confirm line 60 contains the cross-file ref to NOT touch)
    - .planning/phases/49-survey-and-normalization/49-RESEARCH.md (Section "A5: get-shit-done/workflows/execute-phase/steps/post-merge-gate.md")
  </read_first>
  <action>
Apply the following exact renames to `get-shit-done/workflows/execute-phase/steps/post-merge-gate.md`:

1. Find `**Step A — Build gate:**` (or similar format) and rename to `**Step 1 — Build gate:**`
2. Find `**Step B — Test gate:**` (or similar format) and rename to `**Step 2 — Test gate:**`

After renaming, grep the file for any other prose references to "Step A", "Step B", "step A", "step B" and update them:
```
command grep -n "step A\|step B\|Step A\|Step B" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md
```

CRITICAL: Do NOT touch line 60 containing `(same as step 5.8)` or any other `step 5.8` references. That is a cross-file reference handled by plan 49-13. Use `command grep -n "5\.8" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` before editing to confirm the exact line.

Run `npm test` to confirm 0 new failures.
  </action>
  <acceptance_criteria>
    - `command grep -n "Step A\|Step B" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns no output
    - `command grep -n "Step 1" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns a match (Build gate)
    - `command grep -n "Step 2" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` returns a match (Test gate)
    - `command grep -n "5\.8" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` still returns a match (NOT changed)
    - `npm test` exits 0 with no new failures
  </acceptance_criteria>
  <verify>
    <automated>command grep -c "Step A\|Step B" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md || echo "0 — PASS"; npm test</automated>
  </verify>
  <done>
    - `post-merge-gate.md` contains Step 1 (Build gate) and Step 2 (Test gate); no Step A or Step B remains
    - `(same as step 5.8)` at line 60 is unchanged
    - `npm test` passes with 0 new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem edit | Text substitution in a local markdown workflow file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05-01 | Tampering | Cross-file ref "step 5.8" accidentally changed | mitigate | Explicitly grep for 5.8 before editing; leave it unchanged; acceptance criterion verifies it persists |
| T-49-05-02 | Tampering | Other prose references to Step A/B missed | mitigate | Run command grep on Step A/Step B after editing to verify 0 remaining occurrences |
</threat_model>

<verification>
- `command grep -c "Step A\|Step B" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` outputs `0`
- `command grep "5\.8" get-shit-done/workflows/execute-phase/steps/post-merge-gate.md` still returns a match
- `npm test` passes
</verification>

<success_criteria>
`post-merge-gate.md` has Step 1 (Build gate) and Step 2 (Test gate); no letter-suffix steps remain; `(same as step 5.8)` cross-file reference is untouched; scanner subtest passes GREEN; `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/49-survey-and-normalization/49-05-SUMMARY.md` when done.
</output>
