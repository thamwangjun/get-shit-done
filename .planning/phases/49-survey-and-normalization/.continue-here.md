# Phase 49 Continuation Handoff

Wave 2 (plans 49-06 through 49-12) completed and merged to `dev` on 2026-05-30.
Three tests newly regressed from the renumbering. Fix these before dispatching wave 3.

## Critical Anti-Patterns

| # | anti-pattern | severity | caused-by | what-to-fix |
|---|-------------|----------|-----------|-------------|
| 1 | Test asserts old step numbers in execute-phase.md heartbeat positions | blocking | Plan 49-11 renumbered execute-phase.md steps; `tests/bug-2410-stream-checkpoint-heartbeats.test.cjs` still asserts the old step names/numbers for plan-start and wave-complete heartbeat locations | Read execute-phase.md to find new step numbers/names for the heartbeat lines, then update the test assertions to match |
| 2 | Test asserts `Step 8` references deferred-items.md in quick.md | blocking | Plan 49-12 renumbered quick.md from ~8 steps to 15 steps; `tests/bug-2523-quick-deferred-items.test.cjs` still looks for "Step 8" | Read quick.md to find which step number now references deferred-items.md, then update the test assertion |
| 3 | Test asserts `Step 5a` drift-check section exists in reapply-patches.md | blocking | Plan 49-08 removed the `**Step 5a:**` label; `tests/bug-3657` (Finding 2 subtest) still greps for `Step 5a source contains drift-check section` | Read reapply-patches.md to confirm what the section is now called, then update the test assertion to match the new label |

## Current Phase State

- Plans complete (SUMMARY.md on dev): 49-01, 49-02, 49-03, 49-05, 49-06, 49-07, 49-08, 49-09, 49-10, 49-11, 49-12
- Plans remaining: **49-04** (wave 3), **49-13** (wave 4)
- Wave 3 and 4 must NOT start until all 3 blocking regressions above are fixed and `npm test` shows no new failures

## Fix Approach

Fix the 3 test files listed above as a pre-wave commit before dispatching 49-04:
```
fix(49): update test assertions broken by wave 2 step renumbering
```
Then verify `npm test` passes (ignoring the expected corpus-scan failures for
`gsd-verifier.md`, `autonomous.md`, `execute-plan.md`, `profile-user.md`,
`post-merge-gate.md` — those clear when waves 3 and 4 complete).
