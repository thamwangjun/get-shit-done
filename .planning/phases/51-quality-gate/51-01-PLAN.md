---
phase: 51-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-quality-gate/51-VERIFICATION.md
  - .planning/phases/51-quality-gate/51-01-SUMMARY.md
autonomous: true
requirements:
  - GATE-01

must_haves:
  truths:
    - "D-01: `npm test` is executed exactly once during this plan; the observed pass count is locked in VERIFICATION.md as the new baseline (the v2.1.0-c baseline of 7459 pass / 49 fail is superseded because Phases 48–50 added new tests, including 632 step-numbering-scan subtests and 219 cross-file-step-refs subtests)"
    - "D-02: Zero regressions means the failure count stays at 49 — no new failures beyond the pre-existing 49"
    - "D-03 + D-04: New failures introduced by Phase 48–50 work are inline-fixed only if the fix touches a single file and changes fewer than 10 lines; anything larger halts execution and is escalated as a new fix phase"
    - "D-05: Phase produces both `51-VERIFICATION.md` (gate documentation) and `51-01-SUMMARY.md` (phase close-out), mirroring the format of `.planning/phases/48-tdd-red-gate/48-VERIFICATION.md`"
    - "D-06: SUMMARY.md contains no milestone-prep note; milestone close-out is a separate `/gsd-complete-milestone` workflow step"
    - "Negative-framing scanner `tests/negative-framing-scan.test.cjs` reports its full subtest count green (99 subtests per ROADMAP §Phase 51 success criterion 2 — the documented gate value; if the live count differs from 99, the actual observed value is recorded in VERIFICATION.md and the discrepancy is flagged for user decision per D-03/D-04 escalation rules)"
    - "`tests/step-numbering-scan.test.cjs` passes GREEN inside the full `npm test` run (632/632 per Phase 50 VALIDATION.md baseline)"
    - "`tests/cross-file-step-refs.test.cjs` passes GREEN inside the full `npm test` run (219/219 per Phase 50 VALIDATION.md baseline)"
    - "The test run captured output file `/tmp/gsd-test-output.txt` is referenced verbatim in VERIFICATION.md (totals line, fail line, and the negative-framing subtest count line) so the gate result is reproducible from the captured artifact"
  artifacts:
    - path: ".planning/phases/51-quality-gate/51-VERIFICATION.md"
      provides: "Phase 51 gate verification report — documents the observed npm test totals, the per-scanner pass counts (negative-framing, step-numbering, cross-file-step-refs), the regression check vs the 49-fail baseline, and the requirement coverage for GATE-01"
      contains: "# Phase 51: Quality Gate"
    - path: ".planning/phases/51-quality-gate/51-01-SUMMARY.md"
      provides: "Phase 51 Plan 01 close-out summary — frontmatter with phase metadata and metrics; body documenting what was verified, observed pass/fail counts, and any inline regression fixes (D-03/D-04 scope) or escalation"
      contains: "phase: 51-quality-gate"
  key_links:
    - from: "Task 1 test-run capture (`/tmp/gsd-test-output.txt`)"
      to: "Task 2 VERIFICATION.md `## Test Run Results` table"
      via: "VERIFICATION.md cites the totals line and fail line verbatim from the captured output file"
      pattern: "tests" 
    - from: "Task 2 VERIFICATION.md gate status"
      to: "Task 3 SUMMARY.md `## Self-Check` section"
      via: "SUMMARY.md self-check confirms VERIFICATION.md exists, frontmatter `status: passed`, and re-cites the observed totals so the SUMMARY can be read standalone"
      pattern: "Self-Check"
    - from: "`tests/negative-framing-scan.test.cjs`, `tests/step-numbering-scan.test.cjs`, `tests/cross-file-step-refs.test.cjs`"
      to: "VERIFICATION.md per-scanner status rows"
      via: "Each scanner's subtest count and pass/fail status is parsed from the captured output and recorded as a discrete row, satisfying ROADMAP §Phase 51 success criteria 1, 2, 3"
      pattern: "negative-framing-scan|step-numbering-scan|cross-file-step-refs"
---

<objective>
Phase 51 closes the v2.1.0-d milestone by running the full test suite once, confirming zero regressions vs the 49-fail baseline, confirming the three required scanners stay green, and producing the standard close-out artifacts (`51-VERIFICATION.md` + `51-01-SUMMARY.md`). Per GATE-01.

Purpose: Lock the milestone deliverable. After Phases 48 (TDD red gate scanner), 49 (corpus normalization), and 50 (maintenance script + cross-file scanner), Phase 51 is the gate that confirms the milestone shipped clean — no new failures, all three target scanners green, all v2.1.0-d work integrated cleanly into the test suite.

Output:
1. `.planning/phases/51-quality-gate/51-VERIFICATION.md` — gate report
2. `.planning/phases/51-quality-gate/51-01-SUMMARY.md` — phase close-out

Inline-fix scope (per D-03 + D-04): If `npm test` reveals NEW failures (count rises above 49), the fix is applied within this plan only when it touches a single file and changes fewer than 10 lines. Larger fixes halt the plan and escalate per the failure-response protocol; the SUMMARY records the escalation and the verifier addresses it as a new fix phase.
</objective>

<context>
@.planning/phases/51-quality-gate/51-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-tdd-red-gate/48-VERIFICATION.md
@.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm test once, capture output, classify regressions</name>
  <files>/tmp/gsd-test-output.txt</files>
  <read_first>
    - CLAUDE.md (the "Running tests efficiently" section — required pattern: `npm test 2>&1 | tee /tmp/gsd-test-output.txt; echo "Exit: $?"`, run ONCE, then analyze from file; per D-01 no re-runs to refresh output)
    - .planning/phases/51-quality-gate/51-CONTEXT.md (full file — D-01 lock-the-count rule, D-02 zero-regression definition, D-03/D-04 inline-fix scope, D-05/D-06 close-out artifact rules)
    - .planning/ROADMAP.md (§Phase 51 — the three success criteria: 0 new failures vs baseline, negative-framing 99/99, both new test files pass)
    - tests/negative-framing-scan.test.cjs (lines 1-50 only — confirm the file exists and identify the `describe` block names that the captured output will use; do NOT read the full file)
    - tests/step-numbering-scan.test.cjs (lines 1-50 only — confirm file exists; identify describe-block names so the captured output can be parsed)
    - tests/cross-file-step-refs.test.cjs (lines 1-50 only — confirm file exists; identify describe-block names)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-VALIDATION.md (the §Validation Audit 2026-05-31 section — documents the post-Phase-50 green baseline numbers: step-numbering 632/632, cross-file-step-refs 219/219)
  </read_first>
  <action>
    Execute the gate test run exactly once per D-01 and the CLAUDE.md "Running tests efficiently" protocol. After the run, classify any regressions per D-02/D-03/D-04 — without re-running the suite.

    Operational steps:

    1. Verify the working tree is clean and on branch `dev` so the gate result reflects shipped state. Run `git status --porcelain` and confirm empty output; record HEAD commit SHA via `git rev-parse HEAD` for the VERIFICATION.md `## Test Run Results` row in Task 2.

    2. Run the full suite ONCE via the exact CLAUDE.md pattern:

           `npm test 2>&1 | tee /tmp/gsd-test-output.txt; echo "Exit: $?"`

       Per D-01, do NOT re-run `npm test` to refresh the capture. Subsequent analysis reads `/tmp/gsd-test-output.txt`.

    3. From the captured file, extract these data points (use `Read` on `/tmp/gsd-test-output.txt`, then `command grep` for targeted extraction):
       - Top-level totals: `# tests`, `# pass`, `# fail`, `# skipped` (node test runner summary, usually at the end of the captured file)
       - Negative-framing scanner subtest count: search for the describe block name from `tests/negative-framing-scan.test.cjs` (typically `describe('negative-framing scan'...` or similar — extract the actual name from the file's first 50 lines per `<read_first>`). Record the per-scanner pass/fail/skip count by counting `ok N - ...` lines under that describe block, or by reading the TAP suite summary that node prints per file.
       - Step-numbering scanner subtest count: same procedure against `tests/step-numbering-scan.test.cjs` describe block names.
       - Cross-file-step-refs scanner subtest count: same procedure against `tests/cross-file-step-refs.test.cjs` describe block names.
       - Exit code (from the `echo "Exit: $?"` line at the end of the capture).

    4. Compute the regression classification per D-02:
       - If `# fail` equals `49`: regression count is `0` — gate passes the regression criterion, proceed to Task 2 without escalation.
       - If `# fail` is greater than `49`: regression count is `# fail - 49`. List the new failures (extract failing test names from the captured file). For EACH new failure, before proceeding, evaluate the D-03 / D-04 inline-fix scope:
           - If a fix touches exactly one file AND changes fewer than 10 lines: apply the fix in this task (single Edit call), then re-run only that one test file (`node --test tests/<that-file>.test.cjs`) — NOT the full suite — to confirm the fix takes. Record the fix in the deviation log for Task 3's SUMMARY.md.
           - Otherwise: halt the plan. Do NOT attempt the fix. Record the unresolved regression in `/tmp/gsd-test-output.txt`'s companion notes, and proceed to Task 2 to document the escalation in VERIFICATION.md (`status: gaps_found`); Task 3 records the escalation in SUMMARY.md and the user creates a new fix phase per D-03.
       - If `# fail` is less than `49`: regressions count is `0` and the pre-existing failure count has decreased (likely because Phase 48–50 work fixed an unrelated pre-existing issue). Record the new lower baseline in VERIFICATION.md without escalating.

    5. Compute the negative-framing scanner status per ROADMAP §Phase 51 criterion 2:
       - The documented expectation is 99/99 subtests pass.
       - If the live count differs from 99 (e.g., upstream merges between v1.38.6 and now may have added or removed scanner subtests), record the actual observed count in VERIFICATION.md and surface the discrepancy in the gate status — the gate passes only if all live subtests pass (0 fail under the scanner). A subtest-count drift from 99 is not by itself a regression; only a fail count > 0 is.

    6. Per D-01, persist the captured output verbatim — do NOT delete `/tmp/gsd-test-output.txt` between Task 1 and Task 3. Tasks 2 and 3 cite it as the canonical source of truth.

    Anti-patterns to avoid:
    - Do NOT re-run `npm test` to "refresh" the output (D-01 — run once, lock the count).
    - Do NOT manually verify any failing test in isolation BEFORE classifying scope (D-03/D-04 are size-based — single file + <10 lines — not "obviously mechanical"; assess scope first).
    - Do NOT modify any file in `tests/`, `agents/`, `commands/`, `get-shit-done/`, `scripts/`, or `bin/` unless step 4's inline-fix scope is met.
    - Do NOT skip the `git status --porcelain` clean-tree check — a dirty tree contaminates the gate baseline.
  </action>
  <verify>
    <automated>[ -f /tmp/gsd-test-output.txt ] && command grep -E '^# (tests|pass|fail|skipped)' /tmp/gsd-test-output.txt | tee /tmp/gsd-test-summary.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/gsd-test-output.txt` exists as a regular file (verify with `[ -f /tmp/gsd-test-output.txt ] && echo OK` returns `OK`)
    - The captured output contains the node test-runner summary block (verify with `command grep -c '^# tests' /tmp/gsd-test-output.txt` returns at least `1`)
    - The captured output contains a `# pass` line (verify with `command grep -c '^# pass' /tmp/gsd-test-output.txt` returns at least `1`)
    - The captured output contains a `# fail` line (verify with `command grep -c '^# fail' /tmp/gsd-test-output.txt` returns at least `1`)
    - The captured output contains an `Exit:` line from the `echo "Exit: $?"` invocation (verify with `command grep -c '^Exit:' /tmp/gsd-test-output.txt` returns at least `1`)
    - `tests/negative-framing-scan.test.cjs` ran in this capture (verify with `command grep -c 'negative-framing-scan' /tmp/gsd-test-output.txt` returns at least `1`)
    - `tests/step-numbering-scan.test.cjs` ran in this capture (verify with `command grep -c 'step-numbering-scan' /tmp/gsd-test-output.txt` returns at least `1`)
    - `tests/cross-file-step-refs.test.cjs` ran in this capture (verify with `command grep -c 'cross-file-step-refs' /tmp/gsd-test-output.txt` returns at least `1`)
    - The repo working tree is clean (verify with `git status --porcelain` returns empty) — except when D-03/D-04 inline fixes were applied; in that case the diff is constrained to a single file with fewer than 10 changed lines (verify with `git diff --stat` showing at most one entry and the line count delta below 10)
    - If `# fail` is greater than `49`, the new failures have been classified into inline-fix vs escalation buckets and the determination is captured in the task's commit message (verify with `git log -1 --format=%B` containing either `inline-fix scope: applied` or `escalation: new fix phase required`)
    - If `# fail` is `49` or below, no new files were modified and no escalation note was added (verify the git status is clean per the prior criterion)
  </acceptance_criteria>
  <done>
    `/tmp/gsd-test-output.txt` captures the single `npm test` invocation verbatim, the four totals (`# tests`, `# pass`, `# fail`, `# skipped`) are extracted, the three required scanners are confirmed present in the run, and any regressions vs the 49-fail baseline are either inline-fixed within D-03/D-04 scope or flagged for escalation. The capture is preserved for Tasks 2 and 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 51-VERIFICATION.md mirroring Phase 48's VERIFICATION format</name>
  <files>.planning/phases/51-quality-gate/51-VERIFICATION.md</files>
  <read_first>
    - /tmp/gsd-test-output.txt (the capture from Task 1 — the canonical source of truth for all numbers cited in VERIFICATION.md; per D-01 no re-run is allowed)
    - .planning/phases/48-tdd-red-gate/48-VERIFICATION.md (full file — the format template to mirror: frontmatter shape, `## Goal Achievement` section, Observable Truths / Required Artifacts / Key Link Verification / Requirements Coverage / Behavioral Spot-Checks / Anti-Patterns / Known Limitation tables)
    - .planning/REQUIREMENTS.md (§GATE-01 — the single requirement this VERIFICATION must document as VERIFIED or `gaps_found`)
    - .planning/ROADMAP.md (§Phase 51 — the three success criteria that map to Observable Truths rows)
    - .planning/phases/51-quality-gate/51-CONTEXT.md (§decisions D-01 through D-06 — the implementation choices that shape what gets recorded)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-VALIDATION.md (§Validation Audit 2026-05-31 — the baseline numbers for step-numbering 632/632 and cross-file-step-refs 219/219; cross-reference these against the Task 1 capture to confirm the scanners stayed green inside the full suite)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/51-quality-gate/51-VERIFICATION.md` in a single pass. Required structure (mirror `.planning/phases/48-tdd-red-gate/48-VERIFICATION.md` per D-05):

    1. YAML frontmatter with these keys exactly:
       - `phase: 51-quality-gate`
       - `verified:` an ISO-8601 UTC timestamp captured at write time
       - `status:` either `passed` (when fail count ≤ 49 AND all three target scanners green) or `gaps_found` (when new regressions remain after Task 1's inline-fix pass)
       - `score:` formatted as `N/M` where M is the total Observable Truths rows (see below) and N is the rows verified
       - `overrides_applied: 0`
       - Optional `regressions_inline_fixed:` list (empty if none) — file name + line count for each D-03/D-04 fix applied in Task 1
       - Optional `regressions_escalated:` list (empty if none) — short description for each regression flagged for a new fix phase

    2. Top-level heading: `# Phase 51: Quality Gate — Verification Report` followed by:
       - `**Phase Goal:** The full test suite passes with 0 regressions and the negative-framing scanner remains at 99/99 after all v2.1.0-d changes`
       - `**Verified:** <iso-timestamp>`
       - `**Status:** <status>`
       - `**Test Run Capture:** /tmp/gsd-test-output.txt` (cite the captured artifact verbatim per the must-have key_link from Task 1 to Task 2)
       - `**HEAD commit:** <sha>` (the SHA captured by Task 1 step 1)

    3. `## Goal Achievement` section with these subsections (mirroring Phase 48):

       a. `### Observable Truths` table — exactly these rows, populated from the Task 1 capture:
          | # | Truth | Status | Evidence |
          1. `npm test` exit code is 0
          2. `# fail` count equals 49 (or lower) — no regressions vs the v2.1.0-c baseline
          3. `tests/negative-framing-scan.test.cjs` reports 0 fail (subtest count recorded verbatim)
          4. `tests/step-numbering-scan.test.cjs` reports 0 fail (subtest count recorded verbatim — Phase 50 VALIDATION baseline is 632/632; record actual observed count)
          5. `tests/cross-file-step-refs.test.cjs` reports 0 fail (subtest count recorded verbatim — Phase 50 VALIDATION baseline is 219/219; record actual observed count)
          6. The new expected `# pass` count is recorded as the new baseline per D-01 (higher than v2.1.0-c's 7459; cite the actual number from the Task 1 capture)

          For each row, fill `Status` with `VERIFIED` or `GAPS_FOUND` and `Evidence` with the verbatim line from `/tmp/gsd-test-output.txt` (e.g., `# pass 7892` or `subtest count from negative-framing-scan describe block: 99/99 pass`).

       b. `### Required Artifacts` table with these rows:
          - `/tmp/gsd-test-output.txt` exists and contains the run capture
          - `.planning/phases/51-quality-gate/51-VERIFICATION.md` exists (self-reference — VERIFIED upon write)
          - `.planning/phases/51-quality-gate/51-01-SUMMARY.md` (status: `PENDING — written by Task 3`)

       c. `### Key Link Verification` table with one row:
          - From: `tests/negative-framing-scan.test.cjs`, `tests/step-numbering-scan.test.cjs`, `tests/cross-file-step-refs.test.cjs`
          - To: `npm test` full-suite invocation
          - Via: `scripts/run-tests.cjs` auto-discovery (no per-test wiring; the three files run automatically as part of the full suite)
          - Status: `VERIFIED` (confirmed by Task 1 capture showing all three files ran)

       d. `### Requirements Coverage` table with one row for GATE-01:
          - Requirement: `GATE-01`
          - Source: `.planning/REQUIREMENTS.md`
          - Description: `Full npm test passes at 0 regressions after all changes; negative-framing scanner remains at 99/99`
          - Status: `VERIFIED` or `GAPS_FOUND`
          - Evidence: cite the `# pass`, `# fail`, and per-scanner subtest counts from the Task 1 capture

       e. `### Behavioral Spot-Checks` table with at least these rows, each citing a verbatim line or count from `/tmp/gsd-test-output.txt`:
          - `npm test exits 0` → cite the `Exit: 0` line
          - `# fail equals 49 (or lower)` → cite the `# fail N` line
          - `negative-framing-scan green` → cite the subtest count or summary line for that file
          - `step-numbering-scan green` → cite the subtest count for that file (expected 632/632)
          - `cross-file-step-refs green` → cite the subtest count for that file (expected 219/219)
          - `regression count (fail - 49)` → 0 when fail is 49 or below; otherwise the delta and the list of new failing test names

       f. `### Anti-Patterns Found` table — empty body if no inline fixes were required; one row per inline fix when D-03/D-04 scope was applied (each row records the failing test name, the file changed, and the line-count delta to prove the <10-lines criterion).

       g. `### Known Limitation (Not a Gap)` section if Task 1 step 4 escalated any non-trivial regression — copy Phase 48's `G-01` documentation style. Empty section if no escalations.

       h. `### Human Verification Required` section: `None. All acceptance criteria are programmatically verifiable and verified above.` (when the gate passes); or list the escalated regressions for human review (when `status: gaps_found`).

    4. Footer block (mirroring Phase 48 — italicized lines at bottom):
       - `_Verified: <iso-timestamp>_`
       - `_Verifier: Claude (gsd-executor under /gsd-execute-phase 51)_`

    Anti-patterns to avoid:
    - Do NOT invent numbers. Every count, subtest total, and pass/fail figure must come verbatim from `/tmp/gsd-test-output.txt`. If the capture is missing a number, surface it as an observable gap rather than fabricating.
    - Do NOT include a milestone-prep note (per D-06 — that's `/gsd-complete-milestone`'s job, not the SUMMARY's; and the VERIFICATION format from Phase 48 has no such section).
    - Do NOT mark `status: passed` if any of the three target scanners show ≥1 fail in the capture — the milestone gate is non-negotiable on those three.
  </action>
  <verify>
    <automated>[ -f .planning/phases/51-quality-gate/51-VERIFICATION.md ] && command grep -E '^(phase:|status:|score:|verified:)' .planning/phases/51-quality-gate/51-VERIFICATION.md | tee /tmp/gsd-verification-frontmatter.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/51-quality-gate/51-VERIFICATION.md` exists (verify with `[ -f .planning/phases/51-quality-gate/51-VERIFICATION.md ] && echo OK` returns `OK`)
    - Frontmatter contains `phase: 51-quality-gate` (verify with `command grep -c '^phase: 51-quality-gate$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - Frontmatter contains `status:` set to either `passed` or `gaps_found` (verify with `command grep -cE '^status: (passed|gaps_found)$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - Frontmatter contains `verified:` ISO-8601 timestamp (verify with `command grep -cE '^verified: [0-9]{4}-[0-9]{2}-[0-9]{2}T' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - Frontmatter contains `score:` in `N/M` form (verify with `command grep -cE '^score: [0-9]+/[0-9]+$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - Frontmatter contains `overrides_applied: 0` (verify with `command grep -c '^overrides_applied: 0$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - Top-level heading is `# Phase 51: Quality Gate — Verification Report` (verify with `command grep -c '^# Phase 51: Quality Gate' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`)
    - The doc cites the test-run capture path (verify with `command grep -c '/tmp/gsd-test-output.txt' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`)
    - The doc contains `## Goal Achievement` section (verify with `command grep -c '^## Goal Achievement$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - The doc contains the Observable Truths sub-heading (verify with `command grep -c '^### Observable Truths$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1`)
    - The doc contains the Requirements Coverage sub-heading and references `GATE-01` (verify with `command grep -c '^### Requirements Coverage$' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `1` AND `command grep -c 'GATE-01' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`)
    - The doc references the three scanners by file name (verify each: `command grep -c 'negative-framing-scan' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`; same for `step-numbering-scan` and `cross-file-step-refs`)
    - The doc references the `49` regression baseline (verify with `command grep -c ' 49' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`)
    - The doc references the v2.1.0-c baseline `7459` from CONTEXT.md (verify with `command grep -c '7459' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`)
    - The doc does NOT mention `/gsd-complete-milestone` or a milestone-prep note per D-06 (verify with `command grep -c 'gsd-complete-milestone' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns `0`)
    - The numbers in VERIFICATION.md match the capture (sample check: extract the `# pass` line from `/tmp/gsd-test-output.txt` via `command grep '^# pass' /tmp/gsd-test-output.txt | head -1` and confirm the same numeric value appears in VERIFICATION.md via `command grep <value> .planning/phases/51-quality-gate/51-VERIFICATION.md`)
  </acceptance_criteria>
  <done>
    `51-VERIFICATION.md` exists with Phase 48-style frontmatter (`phase`, `verified`, `status`, `score`, `overrides_applied`), Observable Truths table populated verbatim from `/tmp/gsd-test-output.txt`, GATE-01 requirements coverage row, and a per-scanner spot-check table. The gate status accurately reflects the Task 1 capture: `passed` when fail count ≤ 49 and all three scanners green; `gaps_found` otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 51-01-SUMMARY.md closing the phase per D-05/D-06</name>
  <files>.planning/phases/51-quality-gate/51-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/51-quality-gate/51-VERIFICATION.md (the document Task 2 just created — the SUMMARY's `## What Was Verified` section cites it verbatim; the self-check confirms it exists with `status: passed` or records the escalation)
    - /tmp/gsd-test-output.txt (still preserved — cite the same totals so the SUMMARY can be read standalone without flipping back to VERIFICATION.md)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-03-SUMMARY.md (full file — the SUMMARY format template to mirror: frontmatter shape with `phase`, `plan`, `subsystem`, `tags`, `dependency_graph`, `tech_stack`, `key_files`, `decisions`, `metrics`; body sections `## What Was Built` / `## Design Decisions` / `## Deviations from Plan` / `## Threat Flags` / `## Self-Check`)
    - .planning/phases/51-quality-gate/51-CONTEXT.md (§decisions D-05 + D-06 — close-out artifact rules; D-06 prohibits milestone-prep note)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/51-quality-gate/51-01-SUMMARY.md` in a single pass. Mirror the Phase 50 Plan 03 SUMMARY frontmatter shape and body layout (per D-05).

    Required structure:

    1. YAML frontmatter with these keys (mirroring `50-03-SUMMARY.md`):
       - `phase: 51-quality-gate`
       - `plan: "01"` (quoted to preserve leading zero)
       - `subsystem: quality-gate` (since this phase is a milestone gate, not a feature subsystem)
       - `tags: [milestone-gate, npm-test, regression-check, v2.1.0-d, gate-01]`
       - `dependency_graph:`
         - `requires: [50-01, 50-02, 50-03]` (Phase 50 plans; the milestone gate depends on the entire prior phase chain)
         - `provides: [v2.1.0-d-gate-pass]`
         - `affects: []` (the gate produces documentation only; no code is affected when the gate passes cleanly)
       - `tech_stack:`
         - `added: []`
         - `patterns: [run-once-capture-once, size-bounded-inline-fix]` (the two procedural patterns this phase ratifies)
       - `key_files:`
         - `created: [.planning/phases/51-quality-gate/51-VERIFICATION.md, .planning/phases/51-quality-gate/51-01-SUMMARY.md]`
         - `modified: []` (the list is empty when the gate passes; populated with the single inline-fixed file when D-03/D-04 scope was used)
       - `decisions:` populated with verbatim copies of CONTEXT.md D-01 through D-06 (one bullet each, prefixed with the D-id) so the SUMMARY captures the full decision context inline
       - `requirements_completed: [GATE-01]`
       - `metrics:`
         - `duration:` rough wall-clock estimate for the plan (e.g., `~5 minutes` — the gate is a single test run plus two docs)
         - `completed:` ISO date at write time
         - `tasks_completed: 3`
         - `files_changed:` count (`0` when gate passes clean; `1` when a D-03/D-04 inline fix was applied)

    2. Top-level heading: `# Phase 51 Plan 01: Quality Gate Summary`

    3. Opening paragraph (1–3 sentences) — a substantive one-liner per the SUMMARY template:
       Example shape: `v2.1.0-d milestone quality gate executed: `npm test` reports <N pass> / <M fail> (0 regressions vs the 49-fail baseline), the three required scanners green (negative-framing <X/X>, step-numbering 632/632, cross-file-step-refs 219/219), and the milestone is ready for /gsd-complete-milestone.`
       Substitute the actual observed numbers from `/tmp/gsd-test-output.txt`. NOT a generic "phase complete" line per the one-liner-rules.

    4. `## Performance` section with these bullets:
       - `Duration:` from metrics
       - `Completed:` from metrics
       - `Tasks:` 3
       - `Files changed:` 0 (or 1 if inline fix)

    5. `## What Was Verified` section — bulleted list of the three success criteria from ROADMAP §Phase 51, each followed by the observed result cited verbatim from the capture:
       - 0 new failures vs the v2.1.0-c 49-fail baseline → result
       - negative-framing scanner 99/99 → actual observed count
       - both new test files pass in the full suite → actual observed counts (632/632 and 219/219)

    6. `## Test Run Results` table summarizing the capture (same numbers as VERIFICATION.md's Observable Truths, but in a compact form):
       | Metric | Value | Source |
       Rows: `# tests`, `# pass`, `# fail`, `# skipped`, `Exit code`, `negative-framing pass count`, `step-numbering pass count`, `cross-file-step-refs pass count`.
       Source column: `/tmp/gsd-test-output.txt:<line>` or the describe-block name where the count appears.

    7. `## Files Created` section listing the two artifacts created in this plan with one-line descriptions.

    8. `## Decisions Made` section — copy each D-id from CONTEXT.md with a one-line note on how the plan honored it.

    9. `## Deviations from Plan` section. If Task 1 step 4 did NOT apply any inline fixes: write `None — plan executed exactly as written.`. If inline fixes were applied: enumerate each per the SUMMARY template (Rule N - Category, file modified, line count, fix description, verification command). If escalations occurred: enumerate each with the escalation reason and the recommendation for a new fix phase.

    10. `## Issues Encountered` section — `None` when the gate passes cleanly; otherwise list escalated regressions.

    11. `## Threat Flags` section — `None — this plan reads test output and writes two documentation files; no code execution beyond `npm test`; no network access; no user input beyond the gate invocation.` (Mirrors Phase 50 Plan 03's Threat Flags wording.)

    12. `## Next Phase Readiness` section — DO NOT include a milestone-prep note per D-06. Acceptable wording: `Phase 51 closes the v2.1.0-d milestone scope. No subsequent phase is planned in this milestone.` (Without mentioning `/gsd-complete-milestone` or any milestone close-out workflow.)

    13. `## Self-Check` checklist with at least these items, each verifying a concrete state:
        - `[x] /tmp/gsd-test-output.txt exists with the captured npm test run`
        - `[x] .planning/phases/51-quality-gate/51-VERIFICATION.md exists with status: passed (or gaps_found)`
        - `[x] # fail count equals 49 (or the new lower baseline)`
        - `[x] tests/negative-framing-scan.test.cjs reports 0 fail`
        - `[x] tests/step-numbering-scan.test.cjs reports 0 fail`
        - `[x] tests/cross-file-step-refs.test.cjs reports 0 fail`
        - `[x] requirements_completed includes GATE-01`
        - `[x] No milestone-prep note appears in this SUMMARY (per D-06)`

    14. Footer: `## Self-Check: PASSED` (or `## Self-Check: FAILED` with the specific failing line if any) — exactly the pattern used in Phase 50 SUMMARYs.

    Anti-patterns to avoid:
    - Do NOT mention `/gsd-complete-milestone`, `milestone close-out`, `milestone prep`, or any similar phrase anywhere in the SUMMARY (per D-06 — that workflow is the user's separate step).
    - Do NOT use a generic one-liner like "Phase 51 complete" or "Quality gate finished" — the opening paragraph must cite the actual numbers from the capture.
    - Do NOT re-run `npm test` to populate the SUMMARY — read `/tmp/gsd-test-output.txt` from Task 1 (per D-01).
    - Do NOT omit the `requirements_completed: [GATE-01]` frontmatter field — it is required for ROADMAP `gsd-sdk query requirements.mark-complete` to wire the GATE-01 row to `Complete`.
  </action>
  <verify>
    <automated>[ -f .planning/phases/51-quality-gate/51-01-SUMMARY.md ] && command grep -E '^(phase:|plan:|requirements_completed:)' .planning/phases/51-quality-gate/51-01-SUMMARY.md | tee /tmp/gsd-summary-frontmatter.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/51-quality-gate/51-01-SUMMARY.md` exists (verify with `[ -f .planning/phases/51-quality-gate/51-01-SUMMARY.md ] && echo OK` returns `OK`)
    - Frontmatter contains `phase: 51-quality-gate` (verify with `command grep -c '^phase: 51-quality-gate$' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - Frontmatter contains `plan: "01"` (verify with `command grep -c '^plan: \"01\"$' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - Frontmatter contains `requirements_completed: [GATE-01]` (verify with `command grep -c 'requirements_completed: \[GATE-01\]' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - Frontmatter contains the `dependency_graph` block with `requires: [50-01, 50-02, 50-03]` (verify with `command grep -c 'requires: \[50-01, 50-02, 50-03\]' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - Top-level heading is `# Phase 51 Plan 01: Quality Gate Summary` (verify with `command grep -c '^# Phase 51 Plan 01: Quality Gate Summary$' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - The doc contains `## What Was Verified`, `## Test Run Results`, `## Files Created`, `## Decisions Made`, `## Deviations from Plan`, `## Issues Encountered`, `## Threat Flags`, `## Next Phase Readiness`, `## Self-Check` sections (verify each heading appears via individual `command grep -c '^## <heading>$' ...` returning `1`)
    - The doc references the three scanners (verify each: `command grep -c 'negative-framing-scan' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns at least `1`; same for `step-numbering-scan` and `cross-file-step-refs`)
    - The doc references VERIFICATION.md (verify with `command grep -c '51-VERIFICATION.md' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns at least `1`)
    - The doc does NOT mention `/gsd-complete-milestone`, `milestone close-out`, `milestone prep`, or `complete-milestone` (per D-06 — verify with `command grep -ciE 'complete-milestone|milestone close-out|milestone prep' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `0`)
    - The doc ends with `## Self-Check: PASSED` or `## Self-Check: FAILED` line (verify with `command grep -cE '^## Self-Check: (PASSED|FAILED)$' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `1`)
    - The numeric counts in the SUMMARY match the capture (sample check: the `# pass` value in `/tmp/gsd-test-output.txt` appears verbatim in the SUMMARY; verify with `PASS_VALUE=$(command grep '^# pass' /tmp/gsd-test-output.txt | head -1 | command grep -oE '[0-9]+'); command grep -c "$PASS_VALUE" .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns at least `1`)
    - All six `D-NN` decision IDs from CONTEXT.md appear in the `## Decisions Made` section (verify with `command grep -cE 'D-0[1-6]' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns at least `6`)
  </acceptance_criteria>
  <done>
    `51-01-SUMMARY.md` exists with Phase 50-style frontmatter, a substantive one-liner citing the observed `npm test` numbers, all required sections present, no milestone-prep note (per D-06), `requirements_completed: [GATE-01]` set so the ROADMAP wiring marks the requirement complete, and a Self-Check footer reflecting the actual state of the artifacts created in Tasks 1 and 2.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-filesystem → `.planning/phases/51-quality-gate/` | Writes go to two new documentation files; no executable code is produced |
| test-runner → captured output file (`/tmp/gsd-test-output.txt`) | `npm test` output is treated as authoritative input for the gate report; the capture file is the trust root for all numeric assertions in VERIFICATION.md and SUMMARY.md |
| dependency tree → `npm test` invocation | The test runner pulls in the project's `node_modules`; any compromised package in the suite could influence the observed pass/fail count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | `/tmp/gsd-test-output.txt` overwritten between Task 1 and Task 2 by a concurrent process | accept | `/tmp` is per-user on this dev box; gate runs on `dev` branch in a controlled environment; if the capture is tampered with, Task 2's acceptance criteria (numbers match capture, exit-line present, all three test files referenced) will catch the discrepancy at write time |
| T-51-02 | Tampering | Inline regression fix introduces a hidden behavior change beyond the documented D-03/D-04 scope | mitigate | Task 1 step 4 enforces the size bound (single file, <10 lines) via `git diff --stat` check before commit; Task 3 SUMMARY's `## Deviations from Plan` section records the exact file and line count, providing post-hoc auditability |
| T-51-03 | Repudiation | Gate marked `passed` despite a new regression | mitigate | Task 2 acceptance criteria require `status: passed` only when the captured `# fail` is ≤ 49 AND all three target scanners report 0 fail; the verifier (downstream `/gsd-verify-work` step) re-checks the numbers against the capture |
| T-51-04 | Information Disclosure | `/tmp/gsd-test-output.txt` contains environment paths or local secrets | accept | `npm test` output for this repo contains only test names, pass/fail counts, and node stack traces; no secrets are read by tests; `/tmp` files are user-readable on this dev box only and are not committed to git |
| T-51-05 | Denial of Service | `npm test` hangs or fails to terminate, blocking the gate | accept | Test suite is bounded (~5–30s typical runtime per CLAUDE.md "Estimated runtime"); if the suite hangs, the executor's standard timeout fires and the plan halts; Task 1's `Exit:` line capture proves termination |
| T-51-06 | Elevation of Privilege | Test execution exploits a malicious test fixture to escape sandbox | accept | Tests are all local CommonJS files under the project's tree; no new dependencies were installed in this phase; the `npm` install pinned the lockfile during prior phases |
| T-51-SC | Tampering | Supply chain — `npm test` triggers any package install or post-install hook | accept | Phase 51 does not run `npm install`; the `npm test` invocation runs the existing `node_modules`; the lockfile (`package-lock.json`) was last touched in earlier phases; package legitimacy was audited during the original milestone planning (Phases 48–50 RESEARCH.md) |

</threat_model>

<verification>
- Run the validate step before committing: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify plan-structure .planning/phases/51-quality-gate/51-01-PLAN.md`
- After execution, confirm both artifacts exist: `[ -f .planning/phases/51-quality-gate/51-VERIFICATION.md ] && [ -f .planning/phases/51-quality-gate/51-01-SUMMARY.md ] && echo OK`
- Confirm GATE-01 wiring: `command grep -c 'GATE-01' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns at least `1` AND `command grep -c 'GATE-01' .planning/phases/51-quality-gate/51-VERIFICATION.md` returns at least `1`
- Confirm D-06 compliance: `command grep -ciE 'complete-milestone|milestone close-out|milestone prep' .planning/phases/51-quality-gate/51-01-SUMMARY.md` returns `0`
</verification>

<success_criteria>
- `/tmp/gsd-test-output.txt` exists, captures the single `npm test` run, and is referenced by both VERIFICATION.md and SUMMARY.md (per D-01 single-capture rule)
- `.planning/phases/51-quality-gate/51-VERIFICATION.md` exists with `status: passed` and all Observable Truths VERIFIED — OR `status: gaps_found` with escalated regressions documented (per D-03/D-04 size-bounded inline-fix rule)
- `.planning/phases/51-quality-gate/51-01-SUMMARY.md` exists with `requirements_completed: [GATE-01]`, substantive one-liner citing actual numbers, all required sections present, and no milestone-prep note (per D-05 + D-06)
- All three target scanners green inside the full suite per ROADMAP §Phase 51 success criteria:
  1. `npm test` `# fail` ≤ 49 (0 regressions vs v2.1.0-c baseline)
  2. `tests/negative-framing-scan.test.cjs` 0 fail
  3. `tests/step-numbering-scan.test.cjs` and `tests/cross-file-step-refs.test.cjs` both 0 fail
- Phase 51 commit history: 3 task commits + 1 plan-metadata commit, with the SUMMARY committed atomically per the executor's commit protocol
</success_criteria>

<output>
Create `.planning/phases/51-quality-gate/51-01-SUMMARY.md` and `.planning/phases/51-quality-gate/51-VERIFICATION.md` when all three tasks succeed. The SUMMARY closes Phase 51 and the v2.1.0-d milestone scope. The user's separate `/gsd-complete-milestone` step (out of scope for this plan per D-06) finalizes the milestone ROADMAP entry.
</output>
