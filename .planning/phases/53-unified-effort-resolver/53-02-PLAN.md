---
phase: 53-unified-effort-resolver
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - tests/feat-53-config-sites-and-golden.test.cjs
  - get-shit-done/bin/lib/config.cjs
autonomous: true
requirements: [CONFIG-02, CONFIG-03, CONFIG-04]
must_haves:
  truths:
    - "models.<phase-type> accepts a model;effort form: the ;effort survives _resolveAgentSlot and is parsed by parseModelEffort in the resolver (CONFIG-02)"
    - "model_profile_overrides.<runtime> accepts a model;effort form via the resolved tier entry path (CONFIG-03)"
    - "A malformed effort token in either of the two config sites degrades to effort:null with a one-time warning via parseModelEffort, no separate reject pass (CONFIG-04, D-05)"
    - "A behavior-preserving golden snapshot guards bare configs across BOTH resolvers: resolveModelInternal model and resolveReasoningEffortInternal effort derive from the SAME resolved slot for all agents across quality/balanced/budget/inherit (D-08)"
    - "The #3023 fixture {model_profile:'inherit', models:{execution:'opus'}} yields opus model AND opus-tier effort on codex (same-slot invariant)"
  artifacts:
    - path: tests/feat-53-config-sites-and-golden.test.cjs
      provides: "CONFIG-02/CONFIG-03 acceptance tests + cross-resolver golden snapshot (D-08)"
    - path: get-shit-done/bin/lib/config.cjs
      provides: "Confirmed routing (or minimal change) so the two remaining config sites reach parseModelEffort"
  key_links:
    - from: tests/feat-53-config-sites-and-golden.test.cjs
      to: get-shit-done/bin/lib/core.cjs::resolveReasoningEffortInternal
      via: "asserts effort from models.<phase-type> and model_profile_overrides slots"
      pattern: "resolveReasoningEffortInternal"
    - from: tests/feat-53-config-sites-and-golden.test.cjs
      to: get-shit-done/bin/lib/core.cjs::resolveModelInternal
      via: "golden snapshot pairs model+effort on the same slot"
      pattern: "resolveModelInternal"
---

<objective>
Prove the two remaining config override sites — `models.<phase-type>` (CONFIG-02) and `model_profile_overrides.<runtime>` (CONFIG-03) — accept a `model;effort` form and degrade malformed tokens via `parseModelEffort` (CONFIG-04), and add the D-08 behavior-preserving golden snapshot that guards bare configs across BOTH resolvers.

Purpose: Per D-05, CONFIG-02..04 are satisfied by routing each site's slot string through `parseModelEffort` (no separate hard-reject pass). The slot-string sites flow through `_resolveAgentSlot`/`_resolveRuntimeTier` already touched in Plan 01; this plan verifies that routing with tests and adds the cross-resolver golden snapshot that locks the same-slot invariant in place.
Output: A config-sites acceptance suite + a golden snapshot test; a minimal `config.cjs` change only if a site is found to bypass `parseModelEffort`.
</objective>

<context>
!`cat .planning/phases/53-unified-effort-resolver/53-CONTEXT.md`
!`cat .planning/phases/53-unified-effort-resolver/53-PATTERNS.md`
@.planning/phases/53-unified-effort-resolver/53-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config-site acceptance tests + confirm parseModelEffort routing</name>
  <files>tests/feat-53-config-sites-and-golden.test.cjs, get-shit-done/bin/lib/config.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/config.cjs (full file — confirm whether it does any model_overrides/reasoning_effort/effort handling; PATTERNS grep found zero matches)
    - get-shit-done/bin/lib/core.cjs (the rewritten resolveReasoningEffortInternal from Plan 01; `_resolveAgentSlot` ~1340; `_resolveRuntimeTier`/`resolveTierEntry` ~1295-1324)
    - .planning/phases/53-unified-effort-resolver/53-PATTERNS.md (the three-site routing table in the config.cjs section)
    - tests/helpers.cjs (createTempProject/writeConfig/cleanup helpers)
  </read_first>
  <behavior>
    - CONFIG-02: codex + models.<phase-type for executor> = "opus;low" → resolveReasoningEffortInternal returns "low" (slot ;effort survives _resolveAgentSlot and is parsed)
    - CONFIG-02: claude + models.<phase-type> = "sonnet;high" → effort "high"
    - CONFIG-03: codex + model_profile_overrides.codex.opus = "gpt-5-pro;high" (string shorthand) with a config resolving to the opus tier → resolved tier-entry path yields effort "high" (slot effort overrides the per-tier built-in)
    - CONFIG-04: models.<phase-type> = "opus;hihg" (typo) → effort null + one-time stderr warning, model still resolves to opus
    - CONFIG-04: model_profile_overrides slot with a malformed ;effort → effort null + warning
    - Bare forms (no ;) at both sites → effort null (back-compat)
  </behavior>
  <action>
    First read `config.cjs` end-to-end and confirm whether it performs any effort/override parsing. Per the PATTERNS grep it currently has none — CONFIG-02/03/04 are satisfied purely by the resolver routing each site through `parseModelEffort` (D-05: no separate hard-reject pass). If config.cjs does NOT bypass parseModelEffort (expected), make NO change to config.cjs and document that the routing is satisfied by the resolver. ONLY if you find a site that strips or validates effort before it reaches `parseModelEffort`, add the minimal change to route it through `parseModelEffort` instead — do not add a new validation pass.
    Then write `tests/feat-53-config-sites-and-golden.test.cjs` (Task 2 adds the golden snapshot to the same file). Cover every `<behavior>` case via `node --test` with `assert.strictEqual` on exact effort strings (never `indexOf`/substring — Pitfall 6). For the `model_profile_overrides` case, drive a config whose resolved slot maps to the overridden tier so the `;effort` rides the `resolveTierEntry` field-merge path. For the malformed cases assert `effort === null`, capture stderr, and assert one-time warning behavior. Reference the concrete identifiers: `resolveReasoningEffortInternal`, `resolveModelInternal`, `_resolveAgentSlot`, `parseModelEffort`, `_resolveRuntimeTier`.
  </action>
  <verify>
    <automated>node --test tests/feat-53-config-sites-and-golden.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/feat-53-config-sites-and-golden.test.cjs` exits 0 with CONFIG-02, CONFIG-03, CONFIG-04 cases asserted via `assert.strictEqual`
    - The CONFIG-02 case proves `models.<phase-type> = "opus;low"` yields effort "low" on codex
    - The CONFIG-03 case proves a `model_profile_overrides` slot `;effort` is honored
    - The CONFIG-04 cases prove a malformed token degrades to `effort === null` with a one-time warning, base model preserved
    - If config.cjs is changed, the diff routes through `parseModelEffort` and adds no separate reject pass; if unchanged, the SUMMARY states the routing is resolver-owned
  </acceptance_criteria>
  <done>Both remaining config sites are proven to accept model;effort and degrade malformed tokens through parseModelEffort; config.cjs is either unchanged (routing resolver-owned) or minimally routed through parseModelEffort.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-resolver golden snapshot guarding the same-slot invariant (D-08)</name>
  <files>tests/feat-53-config-sites-and-golden.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs (resolveModelInternal 1363-1433; rewritten resolveReasoningEffortInternal; parseModelEffort 1242-1265; _resolveAgentSlot 1340-1361)
    - get-shit-done/bin/lib/model-catalog.cjs (MODEL_PROFILES — the ~33 agent slot map; VALID_PROFILES)
    - .planning/phases/53-unified-effort-resolver/53-PATTERNS.md (the "Behavior-Preservation Guard (D-08)" section)
    - tests/feat-3023-model-phase-types.test.cjs (the existing #3023 fixture pattern to reuse)
  </read_first>
  <action>
    Add a golden-snapshot `describe` block to `tests/feat-53-config-sites-and-golden.test.cjs` per D-08. For every agent in `MODEL_PROFILES` (the ~33 agents) under each profile in {`quality`/`golden`, `balanced`, `budget`, `inherit`} on a BARE config (no `;effort` suffixes, no overrides), assert: (a) `resolveModelInternal(cwd, agent)` returns the expected bare model (snapshot the current values — this is the back-compat invariant: bare config → unchanged model), and (b) `resolveReasoningEffortInternal(cwd, agent)` returns `null` (bare config → no effort) AND the effort path derived from the SAME slot as the model path (assert via `parseModelEffort(_resolveAgentSlot(cwd, agent))` so both resolvers read one slot).
    Then add the explicit #3023 fixture case: config `{ runtime: 'codex', model_profile: 'inherit', models: { execution: 'opus' } }` must yield the opus model AND the opus-tier effort for an execution-phase agent (e.g. gsd-executor) — assert with `assert.strictEqual` on the parsed `{model, effort}` pair, NEVER `indexOf`/substring (Pitfall 6: `medium`/`high` collide with Codex defaults). Build the expected snapshot by reading current resolver output once at test setup so it pins behavior rather than hard-coding a model-id list that drifts with catalog edits — but assert effort `null` for all bare claude/codex slots explicitly.
  </action>
  <verify>
    <automated>node --test tests/feat-53-config-sites-and-golden.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The golden snapshot iterates all agents in `MODEL_PROFILES` across quality/balanced/budget/inherit and asserts bare-config `resolveReasoningEffortInternal` === null for each
    - The snapshot asserts model+effort derive from the SAME slot (`parseModelEffort(_resolveAgentSlot(...))`) for both resolvers
    - The #3023 fixture `{model_profile:'inherit', models:{execution:'opus'}}` on codex asserts opus model AND opus-tier effort via `assert.strictEqual`, no `indexOf`/substring
    - `node --test tests/feat-53-config-sites-and-golden.test.cjs` exits 0
  </acceptance_criteria>
  <done>A cross-resolver golden snapshot pins the bare-config back-compat invariant and the same-slot model+effort alignment across all agents and profiles, with the #3023 fixture explicitly guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → resolver | The two config override sites carry user-authored slot strings into the resolver. Local developer-owned config; no network/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Tampering | malformed effort token in models.<phase-type> or model_profile_overrides | mitigate | Both sites route through `parseModelEffort` → strip-to-base + `effort: null` + one-time warning (D-05/CONFIG-04); verified by the CONFIG-04 cases in Task 1 |
| T-53-05 | Repudiation | silent model/effort divergence (#3023 class) | mitigate | The D-08 golden snapshot asserts model+effort derive from the same `_resolveAgentSlot` result for all agents/profiles, locking the invariant against future drift |

This plan is test-and-verify over the Plan 01 resolver; no new untrusted-input or shell-emit surface is introduced.
</threat_model>

<verification>
- `node --test tests/feat-53-config-sites-and-golden.test.cjs` exits 0
- `npm test 2>&1 | tee /tmp/gsd-53-test.txt` passes with zero new regressions and ≥70% line coverage on `get-shit-done/bin/lib/*.cjs` maintained
</verification>

<success_criteria>
- models.<phase-type> accepts model;effort and degrades malformed tokens (CONFIG-02, CONFIG-04)
- model_profile_overrides.<runtime> accepts model;effort (CONFIG-03)
- CONFIG-04 satisfied by parseModelEffort routing, no separate reject pass (D-05)
- D-08 cross-resolver golden snapshot guards the bare-config back-compat invariant and the same-slot model+effort alignment
- Full npm test passes with no new regressions
</success_criteria>

<output>
Create `.planning/phases/53-unified-effort-resolver/53-02-SUMMARY.md` when done
</output>
