---
phase: 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-202: The SDK regression is confirmed — suite green before phase 55, failing at the phase-55 completion commit 30108028 — with the introducing commit (or narrowed surface) named from the suspects e4a841cd / abfcbf2b (phase 54 SDK 44c150b42 / f0107ba64 / 26679eb8c secondary)"
    - "D-205: A VERIFICATION skeleton enumerates all 17 failing SDK golden parity tests, each with a placeholder classification slot (missing-handler / stale-fixture / real-divergence)"
    - "D-203: VERIFICATION states the parity oracle policy — gsd-tools.cjs is canonical; SDK is fixed to match unless concrete CLI-regression evidence is recorded"
    - "The 17 failures are captured live to /tmp/gsd-55-2-sdk.txt and the count is read from the `Tests N passed / N failed` summary line, not the tee/$? exit code"
  artifacts:
    - path: .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
      provides: "investigation findings: confirmed regression + introducing commit + 17-test classification skeleton + oracle policy"
      contains: "Introducing commit"
  key_links:
    - from: "git bisect / suspect-checkout evidence"
      to: "55.2-VERIFICATION.md"
      via: "introducing commit recorded with strongest evidence"
      pattern: "30108028|e4a841cd|abfcbf2b"
---

<objective>
Investigation-first gate (D-202): confirm the SDK golden-parity regression and root-cause
the introducing surface from the named suspects BEFORE any fix lands. Capture the live
17-failure list, build the per-test classification skeleton in 55.2-VERIFICATION.md, and
record the parity-oracle policy (D-203). This plan gates Waves 2-4 — fixes must not begin
until the regression and its surface are established.

Purpose: D-202 bisect-first rigor and D-205 classification scaffold.
Output: 55.2-VERIFICATION.md with confirmed regression, introducing commit, and a
17-row classification skeleton.
</objective>

<context>
!`cd sdk && npm test 2>&1 | tee /tmp/gsd-55-2-sdk.txt | command grep -E "Tests .*(passed|failed)|FAIL " | tail -40`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the 17 failures and confirm the regression from named suspects</name>
  <files>.planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-CONTEXT.md (D-201..D-205, suspect commits, test discipline)
    - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-PATTERNS.md (orienting insight: intelUpdate already exists; intel.* + verify.codebase-drift intentionally unregistered)
    - sdk/src/golden/golden.integration.test.ts (the 17 failing assertions — intel.update ~line 882, verify.codebase-drift ~line 943, validate.health ~743, init.quick ~781)
    - .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-04-PLAN.md (sibling precedent — VERIFICATION shape, ROOT-deferral note)
  </read_first>
  <action>
    Read the captured /tmp/gsd-55-2-sdk.txt from the context block (do not re-run to re-read,
    per CLAUDE.md test discipline). Extract the exact 17 failing test names from the
    `Tests N passed / N failed` summary region and the per-test `FAIL` lines. Confirm the
    regression surface (per D-202): the failures are inherited from phase 55, present at the
    phase-55 completion commit 30108028 and absent before phase 55. Root-cause the introducing
    surface from the named suspects e4a841cd and abfcbf2b (phase 55-01) first; only escalate to
    phase-54 SDK commits 44c150b42 / f0107ba64 / 26679eb8c if those do not explain the failures.
    Use `git log --oneline` and targeted `git show --stat <suspect>` over sdk/src/query,
    sdk/src/handlers/verify, sdk/src/golden to attribute each cluster to a commit. Record the
    strongest evidence (best-effort, suspect-first — not exhaustive multi-checkout bisect per
    D-202). Confirm pass/fail counts from the summary line only.
  </action>
  <verify>
    <automated>command grep -cE "intel\.update|verify\.codebase-drift|validate\.health|init\.quick|audit-open|history\.digest|phase-plan-index" /tmp/gsd-55-2-sdk.txt</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/gsd-55-2-sdk.txt exists and its summary line shows a non-zero `failed` count
    - The 17 failing test names are extracted into VERIFICATION (no fewer, no more)
    - The introducing commit (or narrowed surface) is named with `git show`/`git log` evidence, suspect-first per D-202
  </acceptance_criteria>
  <done>The 17 failures captured from the summary line; introducing commit/surface attributed with evidence from the named suspects.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 17-test classification skeleton and oracle policy in VERIFICATION</name>
  <files>.planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-PATTERNS.md (cluster groupings: hard missing-handler; intel JSON-parity; non-intel JSON-parity; meta/policy)
    - .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-VERIFICATION.md (sibling VERIFICATION format to mirror)
  </read_first>
  <action>
    Write 55.2-VERIFICATION.md with: (1) a "Regression Confirmation" section recording the
    introducing commit/surface and evidence from Task 1; (2) a "Parity Oracle Policy" section
    stating gsd-tools.cjs is canonical (per D-203) and SDK is fixed to match unless concrete
    CLI-regression evidence is recorded; (3) a "Classification" table with one row per failing
    test (all 17), columns: test name, cluster, cause (placeholder: missing-handler /
    stale-fixture / real-divergence — TBD), resolution plan (02/03/04), notes. Pre-fill the
    cluster column from PATTERNS: hard missing-handler = intel.update, verify.codebase-drift;
    intel JSON-parity = intel.status/diff/validate/query/extract-exports; non-intel JSON-parity
    = history.digest, phase-plan-index, init.quick/todos/manager/map-codebase, audit-open,
    validate.health; meta/policy = executeForCjs native_failure classification, golden coverage
    policy. Leave the cause column as TBD for Waves 2-4 to fill. Note the D-204 register-by-default
    resolution choice is recorded by Plan 02. Mirror 55.1-VERIFICATION.md structure.
  </action>
  <verify>
    <automated>command grep -cE "missing-handler|stale-fixture|real-divergence" .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 55.2-VERIFICATION.md has a Classification table with 17 test rows
    - "Parity Oracle Policy" section names gsd-tools.cjs as canonical (D-203)
    - Each row carries a cluster label and a TBD cause placeholder for downstream waves
  </acceptance_criteria>
  <done>VERIFICATION skeleton exists with 17 classification rows, oracle policy stated, clusters pre-filled, causes TBD for fix waves.</done>
</task>

</tasks>

<threat_model>
No new attack surface — test-reconciliation phase; this plan only reads test output and git
history and writes a planning-doc skeleton within the existing trust boundary. No network,
auth, user input, or data handling introduced.
</threat_model>

<verification>
/tmp/gsd-55-2-sdk.txt captured; 17 failures enumerated; introducing commit named; VERIFICATION
skeleton with 17 classification rows + oracle policy written.
</verification>

<success_criteria>
Regression confirmed and attributed (D-202); 55.2-VERIFICATION.md skeleton classifies all 17
tests with cluster labels and TBD causes; oracle policy (D-203) recorded. Gates Waves 2-4.
</success_criteria>

<output>
Create .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-01-SUMMARY.md when done.
</output>
