---
phase: 55.2-fix-sdk-golden-parity-suite-failures
fixed_at: 2026-06-03T00:00:00Z
review_path: .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-REVIEW.md
iteration: 1
findings_in_scope: 7
fixed: 6
skipped: 1
status: partial
---

# Phase 55.2: Code Review Fix Report

**Fixed at:** 2026-06-03
**Source review:** .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 7
- Fixed: 6
- Skipped: 1

## Fixed Issues

### WR-01: Unparseable `_meta.updated_at` silently treated as "fresh" in intel staleness math

**Files modified:** `sdk/src/query/intel.ts`
**Commit:** c3062ac8
**Applied fix:** Verified against the CJS oracle (`get-shit-done/bin/lib/intel.cjs` lines 257-261 and 411-412) — the NaN-swallowing behavior is **identical** in the oracle, so this is parity-locked. Per the parity directive, the SDK keeps matching the oracle and the limitation is now documented inline at both `intelStatus` (line 174) and `intelValidate` (lines 254-258), noting a garbage timestamp reads as fresh and that the underlying oracle behavior is tracked separately. No behavioral divergence introduced.

### WR-02: `intelPatchMeta` shadows the outer `raw` binding

**Files modified:** `sdk/src/query/intel.ts`
**Commit:** c3062ac8
**Applied fix:** Renamed the inner file-content local from `raw` to `fileRaw` (and updated the `JSON.parse` call) so the outer arg binding `raw` is no longer shadowed. Behavior unchanged; intent clarified for future edits.

### WR-03: `intelExtractExports` brace-matching ignores `}` inside strings/comments

**Files modified:** `sdk/src/query/intel.ts`
**Commit:** c3062ac8
**Applied fix:** Verified the naive `{`/`}` depth-counting loop is identical in the CJS oracle (`intel.cjs` lines 522-528) — parity-locked. Per the parity directive, documented the heuristic limitation in the `intelExtractExports` JSDoc rather than diverging from the oracle. The block-boundary under-reporting risk is now explicit.

### WR-04: `verifySummary` commit-hash heuristic matches 7+ hex runs anywhere

**Files modified:** `sdk/src/query/verify.ts`
**Commit:** 84f06f62
**Applied fix:** Verified the `/\b[0-9a-f]{7,40}\b/g` pattern is identical in the CJS oracle (`verify.cjs` line 75) — parity-locked. Documented the false-positive risk inline above the pattern rather than tightening it, preserving byte-parity with the oracle.

### IN-01: Dead deprecated helpers in `complex.ts`

**Files modified:** `sdk/src/handlers/init/complex.ts`, `sdk/src/handlers/init/composer.ts`
**Commit:** 4e468562
**Applied fix:** Confirmed `getModelAlias` and `getEffort` had no call sites anywhere in `src/` (only definitions plus a stale doc reference). Deleted both `@deprecated` functions from `complex.ts`, and also from `composer.ts` where the same duplicate dead helpers existed unused — as the finding explicitly permitted. `tsc -p tsconfig.json` passes after removal, confirming no remaining references.

### IN-02: `validateConsistency` `errors` array is structurally always empty

**Files modified:** `sdk/src/query/validate.ts`
**Commit:** 9609cb3e
**Applied fix:** Confirmed the warnings-only design matches the CJS oracle (parity-locked). Past the early ROADMAP-not-found return, nothing pushes to `errors`, so `passed` is always `true`. Added an inline comment above the `passed` computation noting `errors` is intentionally never populated and instructing future authors to push to `errors` (not `warnings`) when a check must flip `passed`.

## Skipped Issues

### CR-01: Generated command-alias artifacts are stale — missing all 8 `intel.*` non-family entries

**File:** `sdk/src/query/command-aliases.generated.ts:107-147` and `get-shit-done/bin/lib/command-aliases.generated.cjs:563-806`
**Reason:** Already resolved — fixed in prior commit `31269ed6` ("fix(55.2): regenerate command alias artifacts for intel.* registration (CR-01)"). Verified in the current tree: both generated files now contain the intel entries (TS = 8 intel matches, CJS = 16), and after building the SDK (`tsc -p tsconfig.json`) the committed freshness gate `node scripts/check-command-aliases-fresh.mjs` prints `command alias artifacts are fresh` and exits 0. No further action required.
**Original issue:** The 8 `intel.*` entries added to `NON_FAMILY_COMMAND_MANIFEST` were absent from both generated `NON_FAMILY_COMMAND_ALIASES` arrays, which would have failed the byte-equality freshness check in CI and lost intel's mutation flags.

---

_Fixed: 2026-06-03_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
