---
phase: 55.2
slug: fix-sdk-golden-parity-suite-failures-regression-triage-from
status: passed
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-03
reconstructed_from: artifacts (State B — no prior VALIDATION.md)
---

# Phase 55.2 — Validation Strategy

> Per-phase validation contract, reconstructed retroactively from PLAN/SUMMARY/VERIFICATION artifacts.

This is a **regression-triage / test-reconciliation phase**: its entire deliverable was making 17
failing SDK golden-parity tests green *without masking* (no relaxed assertions, no new skips — D-201).
The validation surface IS the test suite — every behavior change is a fix to make a pre-existing,
named golden test pass against the canonical `gsd-tools.cjs` oracle (D-203). There are therefore no
net-new behaviors lacking a test; each row below is guarded by an automated golden/parity assertion.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (SDK, `sdk/`) + Node built-in `--test` (ROOT) |
| **Config file** | `sdk/vitest.config.ts` ; ROOT uses `node --test` |
| **Quick run command** | `cd sdk && npx vitest run src/golden/golden.integration.test.ts src/golden/read-only-parity.integration.test.ts src/golden/golden-policy.test.ts` |
| **Full suite command** | `cd sdk && npm test` (SDK) + `npm test` (ROOT) |
| **Estimated runtime** | ~10s (golden subset) · ~77s (ROOT full) |

---

## Sampling Rate

- **After every task commit:** Run the per-test filtered `npx vitest run … -t "<test name>"`
- **After every plan wave:** Run the SDK golden subset (quick command above)
- **Before `/gsd-verify-work`:** Full SDK + ROOT suites must be green (fail 0 from summary line)
- **Max feedback latency:** ~10 seconds (golden subset)

---

## Per-Task Verification Map

All 17 failures are covered by automated golden-parity assertions (`toEqual` against the
`gsd-tools.cjs` oracle, volatile keys stripped via normalize helpers, never by weakening the
comparison). Confirmed green 2026-06-03: golden subset 125 passed | 1 todo.

| # | Test | Plan | Wave | Behavior Validated | Test File | Test Type | Automated Command | Status |
|---|------|------|------|--------------------|-----------|-----------|-------------------|--------|
| 1 | `intel.update` | 02 | 2 | `intelUpdate` registered + byte-matches CJS | `golden.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "intel.update"` | ✅ green |
| 2 | `verify.codebase-drift` | 02 | 2 | native TS port matches `drift.cjs` (no subprocess recursion) | `golden.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "verify.codebase-drift"` | ✅ green |
| 3 | `intel.status` | 04 | 3 | INTEL_FILES names + `timeAgo()` on `updated_at` | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel.status"` | ✅ green |
| 4 | `intel.diff` | 02 | 2 | dispatch resolves after registration (no divergence) | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel.diff"` | ✅ green |
| 5 | `intel.validate` | 04 | 3 | error/warning message formats match CJS + entries spot-check | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel.validate"` | ✅ green |
| 6 | `intel.query` | 04 | 3 | `searchJsonEntries` `{key,value}` algorithm matches CJS | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel.query"` | ✅ green |
| 7 | `intel.extract-exports` | 02 | 2 | dispatch resolves after registration (no divergence) | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "intel.extract-exports"` | ✅ green |
| 8 | `history.digest` | 03 | 2 | `normaliseProvides` array-of-objects parse parity | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "history.digest"` | ✅ green |
| 9 | `phase-plan-index` | 03 | 2 | archive fallback removed to match CJS active-only scan | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "phase-plan-index"` | ✅ green |
| 10 | `init.quick` | 03 | 2 | volatile `timestamp` stripped via normalize | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "init.quick"` | ✅ green |
| 11 | `init.todos` | 03 | 2 | volatile `timestamp` stripped via normalize | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "init.todos"` | ✅ green |
| 12 | `init.manager` | 03 | 2 | `deps_satisfied` all-ROADMAP scan + `queued_*` omission | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "init.manager"` | ✅ green |
| 13 | `init.map-codebase` | 03 | 2 | volatile `timestamp` stripped via normalize | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "init.map-codebase"` | ✅ green |
| 14 | `audit-open` | 03 | 2 | `scanQuickTasks` matches `*-SUMMARY.md` glob; `scanned_at` stripped | `read-only-parity.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/read-only-parity.integration.test.ts -t "audit-open"` | ✅ green |
| 15 | `validate.health` | 03 | 2 | ported W017/W018/W019 + current-milestone ROADMAP scoping | `golden.integration.test.ts` | golden parity | `cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "validate.health"` | ✅ green |
| 16 | `executeForCjs > native_failure classification` | 04 | 3 | fixture → `intel.extract-exports` ENOENT native_failure path | `runtime-bridge-sync/index.test.ts` | unit | `cd sdk && npx vitest run src/runtime-bridge-sync/index.test.ts -t "native_failure"` | ✅ green |
| 17 | `golden policy > canonical command integration-covered` | 02 | 2 | `intel.update` allowlist entry valid after registration | `golden-policy.test.ts` | policy | `cd sdk && npx vitest run src/golden/golden-policy.test.ts` | ✅ green |

*Coupled coverage:* `command-aliases.generated.{ts,cjs}` additions for `verify.codebase-drift`
(Plan 03 fix) are guarded by `command-seam-coverage.test.ts` and
`tests/feat-3598-generator-correctness.test.cjs`. Drift logic is additionally covered by
`tests/drift-detection.test.cjs` (ROOT).

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new framework, fixture, or stub files
were required — every failing test pre-existed; the phase fixed source/fixtures to make them pass.

---

## Manual-Only Verifications

All phase behaviors have automated verification. There are no manual-only checks — the phase's
acceptance criteria are entirely expressed as golden-parity / policy / unit assertions in the SDK
suite, and the terminal gate (Plan 05) confirmed SDK fail 0 + ROOT fail 0 from summary lines.

---

## Validation Sign-Off

- [x] All 17 failing tests have an `<automated>` verify command (golden/parity/policy/unit)
- [x] Sampling continuity: every task in Plans 02–04 carried a filtered `npx vitest run` verify
- [x] Wave 0 covers all MISSING references (none — existing infra sufficient)
- [x] No watch-mode flags (all commands use `vitest run`)
- [x] Feedback latency < 10s (golden subset)
- [x] No assertion relaxed, no new `.skip(`/`.todo(` added (D-201 confirmed via `git log -p`)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-03 (reconstructed retroactively; golden subset re-confirmed green 125 passed | 1 todo)

---

## Validation Audit 2026-06-03

| Metric | Count |
|--------|-------|
| Requirements / failing tests | 17 |
| COVERED (automated) | 17 |
| PARTIAL | 0 |
| MISSING | 0 |
| Gaps found | 0 |
| Resolved | 0 |
| Escalated to manual-only | 0 |

**Method:** State B reconstruction. Mapped all 17 classified failures (from 55.2-VERIFICATION.md)
to their per-test automated commands across `golden.integration.test.ts`,
`read-only-parity.integration.test.ts`, `golden-policy.test.ts`, and
`runtime-bridge-sync/index.test.ts`. Re-ran the golden subset to confirm coverage is live and green
(125 passed | 1 todo). No gaps — auditor spawn not required.
