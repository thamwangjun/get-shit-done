---
phase: 55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-
plan: 03
type: execute
wave: 2
depends_on: ["55.2-01"]
files_modified:
  - sdk/src/query/audit-open.ts
  - sdk/src/query/validate.ts
  - sdk/src/query/init.ts
  - sdk/src/query/init-complex.ts
  - sdk/src/golden/golden.integration.test.ts
  - .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-203: the non-intel JSON-parity handlers (history.digest, phase-plan-index, init.quick/todos/manager/map-codebase, audit-open, validate.health) byte-match the gsd-tools.cjs oracle — SDK fixed to match (or golden fixture regenerated only from confirmed-correct CLI output per D-201/D-204)"
    - "D-201: no assertion is relaxed to hide divergence; the audit-open volatile key scanned_at is stripped before toEqual using the established normalize helper, not by weakening the comparison"
    - "D-205: each of these tests' VERIFICATION rows is set to its confirmed cause (stale-fixture vs real-divergence) with the introducing-commit note"
  artifacts:
    - path: .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-VERIFICATION.md
      provides: "non-intel JSON-parity cluster classified (cause per test)"
      contains: "validate.health"
  key_links:
    - from: "non-intel SDK handlers (audit-open, validate, init)"
      to: "get-shit-done/bin/lib/*.cjs oracles"
      via: "sdkResult.data toEqual gsdToolsOutput (volatile keys excluded)"
      pattern: "toEqual"
---

<objective>
Fix the non-intel JSON-parity cluster: history.digest, phase-plan-index,
init.quick/todos/manager/map-codebase, audit-open (excluding volatile scanned_at), and
validate.health. Each is already registered/parity-rowed but its SDK output drifted from the
CJS oracle. Per D-203, fix the SDK side to match gsd-tools.cjs (or regenerate the golden fixture
only from confirmed-correct CLI output per D-201/D-204). This cluster is independent of intel/verify
registration, so it runs in parallel with Plan 02. Record each cause in VERIFICATION.

Purpose: cause-(b)/cause-(c) reconciliation for the non-intel cluster (D-203).
Output: passing non-intel parity tests; classified VERIFICATION rows.
</objective>

<context>
!`cd /home/thamw/development/remote-dev/get-shit-done && cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "validate.health" -t "init.quick" 2>&1 | tee /tmp/gsd-55-2-nonintel.txt | command grep -E "Tests .*(passed|failed)|expected|received" | head -40`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile audit-open + validate.health parity (volatile-key stripping)</name>
  <files>sdk/src/query/audit-open.ts, sdk/src/query/validate.ts, sdk/src/golden/golden.integration.test.ts</files>
  <read_first>
    - sdk/src/query/audit-open.ts (SDK handler under test)
    - sdk/src/query/validate.ts (validate.health handler)
    - get-shit-done/bin/lib audit + validate.health CJS oracle paths (the canonical output per D-203)
    - sdk/src/golden/golden.integration.test.ts (validate.health describe ~line 743; audit-open describe and its normalize use; the omitInitExecutePhaseVolatile / normalizeDocsInitPayload helpers ~lines 866-873 as the volatile-strip analog)
  </read_first>
  <behavior>
    - audit-open SDK output toEqual the CJS oracle after stripping scanned_at via the established normalize helper (not by weakening toEqual)
    - validate.health SDK output toEqual its CJS oracle, assertions unrelaxed (D-201)
  </behavior>
  <action>
    For audit-open and validate.health, capture the gsd-tools.cjs oracle output and diff against the
    SDK handler's JSON. Per D-203 fix the SDK handler (audit-open.ts / validate.ts) to match the
    canonical CJS shape; if the divergence is a legitimate CLI JSON change, regenerate the golden
    fixture from confirmed-correct CLI output only (D-201/D-204). For audit-open's volatile scanned_at
    key, exclude it before toEqual using the existing normalize helper analog (omitInitExecutePhaseVolatile /
    normalizeDocsInitPayload pattern) — never weaken the comparison to hide drift (D-201). Set the
    audit-open and validate.health rows in VERIFICATION to their confirmed cause (stale-fixture vs
    real-divergence).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "audit-open" -t "validate.health" 2>&1 | tee /tmp/gsd-55-2-audit.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - audit-open and validate.health golden tests pass with `failed` count 0 in the filtered run summary line
    - scanned_at is stripped via a normalize helper, not by relaxing toEqual
    - VERIFICATION rows for audit-open and validate.health carry a confirmed cause
  </acceptance_criteria>
  <done>audit-open + validate.health parity green against CJS oracle; volatile key handled by normalize; causes recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile history.digest, phase-plan-index, init.quick/todos/manager/map-codebase parity</name>
  <files>sdk/src/query/init.ts, sdk/src/query/init-complex.ts, sdk/src/golden/golden.integration.test.ts</files>
  <read_first>
    - sdk/src/query/init.ts and sdk/src/query/init-complex.ts (init.quick/todos/manager/map-codebase handlers)
    - the SDK handlers for history.digest and phase-plan-index (locate via command-manifest / handlers; their CJS oracles in get-shit-done/bin/lib)
    - get-shit-done/bin/lib/*.cjs (history-digest, phase-plan-index, init.* oracles — canonical per D-203)
    - sdk/src/golden/golden.integration.test.ts (init.quick describe ~line 781; history.digest and phase-plan-index describe blocks)
    - sdk/src/golden/init-golden-normalize.ts (init normalization analog)
  </read_first>
  <behavior>
    - history.digest, phase-plan-index, init.quick/todos/manager/map-codebase SDK outputs each toEqual their CJS oracle (D-203)
    - any volatile keys stripped via init-golden-normalize helper, assertions unrelaxed (D-201)
  </behavior>
  <action>
    For each of history.digest, phase-plan-index, init.quick, init.todos, init.manager, and
    init.map-codebase: capture the gsd-tools.cjs oracle output, diff against the SDK handler JSON,
    and per D-203 fix the SDK handler (init.ts / init-complex.ts / the history+phase-plan-index
    handlers) to match the canonical shape. Where the difference is a legitimate CLI JSON change,
    regenerate the golden fixture from confirmed-correct CLI output only (D-201/D-204). Use the
    existing init-golden-normalize helper for any volatile init keys rather than weakening toEqual.
    Set each test's row in VERIFICATION to its confirmed cause (stale-fixture vs real-divergence).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/golden.integration.test.ts -t "init.quick" -t "init.todos" -t "init.manager" -t "history.digest" -t "phase-plan-index" 2>&1 | tee /tmp/gsd-55-2-initcluster.txt | command grep -E "Tests .*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - history.digest, phase-plan-index, init.quick/todos/manager/map-codebase golden tests pass (`failed` 0 in the filtered summary line)
    - no assertion relaxed; volatile keys handled via init-golden-normalize
    - VERIFICATION rows for all six tests carry a confirmed cause
  </acceptance_criteria>
  <done>init/history/phase-plan-index parity green against CJS oracles; causes recorded in VERIFICATION.</done>
</task>

</tasks>

<threat_model>
No new attack surface — test-reconciliation phase; changes are golden-fixture parity fixes to
existing registered handlers within the existing trust boundary. No network, auth, user input,
or data handling introduced.
</threat_model>

<verification>
audit-open, validate.health, history.digest, phase-plan-index, init.quick/todos/manager/map-codebase
golden tests pass (unrelaxed) against CJS oracles; VERIFICATION rows classified.
</verification>

<success_criteria>
Non-intel JSON-parity cluster green against gsd-tools.cjs (D-203); no assertion relaxed (D-201);
volatile keys normalized not hidden; causes recorded (D-205).
</success_criteria>

<output>
Create .planning/phases/55.2-fix-sdk-golden-parity-suite-failures-regression-triage-from-/55.2-03-SUMMARY.md when done.
</output>
