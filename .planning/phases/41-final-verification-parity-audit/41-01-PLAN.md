---
phase: 41-final-verification-parity-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VALID-01
  - VALID-02

must_haves:
  truths:
    - "D-01/D-02: git diff backup-thamw-main-before-squash HEAD -- . ':!.planning' produces zero output, or only files in the D-03 allowlist (.claudeignore, .gitignore, scripts/stage-batch-*.cjs, Nyquist test files)"
    - "D-04: npm test exits 0 with 8300+ passing assertions and 0 failures"
    - "Negative-framing scanner subtest (negative-framing-scan.test.cjs) reports 0 violations and 0 warnings within the npm test run"
    - "D-05: 41-VERIFICATION.md exists at .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md and contains raw command output, pass/fail determination, and requirements coverage table"
    - "D-07: ROADMAP.md Phase 39 progress row shows 'Complete' with a completion date (not 'Pending')"
    - "STATE.md current_phase is 41, status is complete"
  artifacts:
    - path: ".planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md"
      provides: "Canonical parity audit record with embedded raw command output"
      contains: "VALID-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 39 progress row updated to Complete"
      contains: "Complete"
    - path: ".planning/STATE.md"
      provides: "Phase 41 marked complete"
      contains: "status: complete"
  key_links:
    - from: "41-VERIFICATION.md"
      to: "git diff output"
      via: "raw command capture"
      pattern: "backup-thamw-main-before-squash"
    - from: "41-VERIFICATION.md"
      to: "npm test output"
      via: "raw command capture"
      pattern: "pass.*fail"
---

<objective>
Run the final parity audit for the v1.41.5 milestone: diff consolidated HEAD against the pre-squash backup branch, execute the full test suite, and confirm the scanner subtest is clean. Record all raw output in the canonical 41-VERIFICATION.md artifact. Fix the Phase 39 ROADMAP.md progress row (planning-only artifact, D-07 inline fix allowed). Mark Phase 41 complete in STATE.md.

Purpose: Provide a reproducible audit record confirming that the 5-batch commit history refactor produced zero content divergence and zero test regressions.
Output: 41-VERIFICATION.md (canonical audit record), updated ROADMAP.md, updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-final-verification-parity-audit/41-CONTEXT.md
@.planning/phases/40-stage-and-commit-maintenance-logs-state/40-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run parity diff and npm test — capture output into 41-VERIFICATION.md</name>

  <read_first>
    - `.planning/phases/40-stage-and-commit-maintenance-logs-state/40-VERIFICATION.md` — reference format for VERIFICATION.md tables (Observable Truths, Required Artifacts, Behavioral Spot-Checks, Requirements Coverage sections)
    - `.planning/phases/41-final-verification-parity-audit/41-CONTEXT.md` — D-01 through D-08 decisions, expected allowlist (D-03), failure handling rules (D-06)
  </read_first>

  <files>.planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md</files>

  <action>
    Run all verification commands in order and capture raw output. Then write 41-VERIFICATION.md following the Phase 40 VERIFICATION.md format exactly.

    Step 1 — Parity diff (per D-01, D-02):
    Run: `git diff backup-thamw-main-before-squash HEAD -- . ':!.planning'`
    Capture the full raw output (or "(empty — no diff)" if output is blank).

    Step 2 — Apply D-03 allowlist filter:
    Inspect any filenames appearing in the diff output against the allowlist:
    - `.claudeignore`
    - `.gitignore`
    - `scripts/stage-batch-*.cjs` (any stage-batch-N.cjs file)
    - Nyquist test files (files added by gsd-validate-phase, typically in `tests/`)

    Any filename NOT in this allowlist is a real parity failure — per D-06, stop, record as FAILED in VERIFICATION.md, and do not proceed to npm test.

    Step 3 — Full test suite (per D-04):
    Run: `npm test`
    Capture the summary line (e.g., `NN pass, 0 fail, 1 skip`) and any failure output.
    Confirm pass count is 8300 or higher.

    Step 4 — Scanner subtest confirmation (per VALID-02, D-04):
    Within the npm test output, locate the `negative-framing-scan.test.cjs` subtest result.
    Confirm it reports 0 violations and 0 warnings (subtest passes).

    Step 5 — Write 41-VERIFICATION.md:
    Use the Phase 40 VERIFICATION.md structure:
    - YAML frontmatter: `phase`, `verified` (ISO timestamp), `status` (passed/failed), `score` (N/N must-haves verified), `overrides_applied: 0`
    - `## Goal Achievement` with `### Observable Truths` table — one row per must-have truth, columns: #, Truth, Status (✓ VERIFIED or ✗ FAILED), Evidence (exact command output excerpt)
    - `### Required Artifacts` table — rows for 41-VERIFICATION.md itself, ROADMAP.md Phase 39 row
    - `### Behavioral Spot-Checks` table — columns: Behavior, Command, Result, Status
    - `### Requirements Coverage` table — VALID-01 and VALID-02 rows, columns: Requirement, Source Plan, Description, Status, Evidence
    - `### Gaps Summary` section — "No gaps" if all truths verified, otherwise list failures

    Per D-06: if any row in the Observable Truths table is ✗ FAILED (due to unexpected diff output or test failures in refactored files), set frontmatter `status: failed`, write the artifact, and halt — do not proceed to Task 2 or Task 3.
  </action>

  <verify>
    <automated>test -f .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md && grep -q "backup-thamw-main-before-squash" .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md && grep -q "VALID-01" .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md && grep -q "VALID-02" .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `41-VERIFICATION.md` exists at `.planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md`
    - File contains the exact string `backup-thamw-main-before-squash` (proving the diff command was run and captured)
    - File contains rows for both `VALID-01` and `VALID-02` in the Requirements Coverage table
    - Each Observable Truth row has either `✓ VERIFIED` or `✗ FAILED` in the Status column — no blank or ambiguous status
    - The Behavioral Spot-Checks table contains at least one row for the parity diff and one row for npm test
    - If all truths are verified: frontmatter `status: passed` and `score: 5/5` (or equivalent count)
    - If D-06 triggered (failure found): frontmatter `status: failed`, artifact is written, execution halts after this task
  </acceptance_criteria>

  <done>41-VERIFICATION.md written with raw command output, VALID-01 and VALID-02 requirements covered, status set to passed or failed per D-06 rule.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Phase 39 ROADMAP.md progress row and update STATE.md</name>

  <read_first>
    - `.planning/ROADMAP.md` — locate Phase 39 progress row in the `## Progress` table; confirm it currently shows "Pending" in the Status column
    - `.planning/STATE.md` — read current frontmatter fields before updating
    - `.planning/phases/41-final-verification-parity-audit/41-CONTEXT.md` — D-07 and D-08 decision rules confirming this is a `.planning/`-only fix
  </read_first>

  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <action>
    This task executes only if Task 1 completed with `status: passed` in 41-VERIFICATION.md. If Task 1 set `status: failed`, halt here — do not modify ROADMAP.md or STATE.md.

    Fix 1 — ROADMAP.md Phase 39 progress row (per D-07, D-08 — planning-only artifact):
    In the `## Progress` table, find the row for Phase 39 (`39. Stage and Commit Tests & SDK Validation`).
    The current row reads: `| 39. Stage and Commit Tests & SDK Validation | v1.41.5 | 1/1 | Pending | - |`
    Change Status from `Pending` to `Complete` and the Completed date from `-` to `2026-05-22`.
    Batch 4 commit `3d1e663b` is the evidence (confirmed in git log per CONTEXT.md).
    Do not change any other rows in the progress table.

    Fix 2 — STATE.md phase completion:
    Update STATE.md frontmatter:
    - `status:` → `complete`
    - `stopped_at:` → `Phase 41 verification passed`
    - `last_updated:` → current ISO timestamp
    - `last_activity:` → current date in YYYY-MM-DD format
    In the `## Current Position` section body, update:
    - `Status:` → `Complete`
    - `Plan:` → `41-01 complete`
  </action>

  <verify>
    <automated>grep -q "Complete" .planning/ROADMAP.md && grep -A1 "39\." .planning/ROADMAP.md | grep -q "Complete" && grep -q "status: complete" .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - ROADMAP.md progress table Phase 39 row: Status column reads `Complete` (not `Pending`), Completed column reads `2026-05-22` (not `-`)
    - ROADMAP.md: no other rows in the progress table are modified
    - STATE.md frontmatter: `status: complete` (exact string)
    - STATE.md frontmatter: `stopped_at: Phase 41 verification passed`
    - STATE.md frontmatter: `last_updated` contains a valid ISO 8601 timestamp (format `YYYY-MM-DDTHH:MM:SS.sssZ`)
    - STATE.md `## Current Position` section: `Status: Complete`
  </acceptance_criteria>

  <done>ROADMAP.md Phase 39 row updated to Complete with date 2026-05-22. STATE.md status set to complete. Both files are .planning/-only changes (D-08 inline fix rule satisfied).</done>
</task>

<task type="auto">
  <name>Task 3: Commit planning artifacts and write phase SUMMARY</name>

  <read_first>
    - `.planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md` — confirm status: passed before committing
    - `.planning/STATE.md` — confirm status: complete before committing
    - `.planning/ROADMAP.md` — confirm Phase 39 row reads Complete
  </read_first>

  <files>.planning/phases/41-final-verification-parity-audit/41-01-SUMMARY.md</files>

  <action>
    Commit all modified planning artifacts, then write the phase SUMMARY.

    Step 1 — Verify preconditions:
    Run `git status` to confirm only `.planning/` files are modified (per D-08 rule). If any file outside `.planning/` appears in git status as modified, halt — do not commit.

    Step 2 — Stage and commit planning artifacts:
    Stage: `git add .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md .planning/ROADMAP.md .planning/STATE.md`
    Commit with message: `docs(phase-41): complete final verification and parity audit`

    Step 3 — Write 41-01-SUMMARY.md:
    Create `.planning/phases/41-final-verification-parity-audit/41-01-SUMMARY.md` using the standard GSD SUMMARY template.
    Key fields to populate:
    - `phase`: `41-final-verification-parity-audit`
    - `plan`: `01`
    - `status`: `complete`
    - `completed`: current date
    - Summary narrative: state that parity diff returned zero unexpected output (list any allowlisted files found), npm test passed with N assertions, scanner subtest confirmed 0 violations, Phase 39 ROADMAP row fixed as D-07 inline fix
    - Requirements satisfied: VALID-01 (parity diff), VALID-02 (npm test + scanner)
    - Decisions applied: D-01 (diff target), D-02 (pathspec), D-03 (allowlist), D-04 (inline commands), D-05 (VERIFICATION.md as audit record), D-06 (pure gate, not triggered), D-07/D-08 (ROADMAP.md inline fix)
  </action>

  <verify>
    <automated>test -f .planning/phases/41-final-verification-parity-audit/41-01-SUMMARY.md && git log --oneline -1 | grep -q "docs(phase-41)"</automated>
  </verify>

  <acceptance_criteria>
    - `git status` shows clean working tree after commit (no untracked or modified files in `.planning/phases/41-*`)
    - `git log --oneline -1` subject line is exactly: `docs(phase-41): complete final verification and parity audit`
    - `41-01-SUMMARY.md` exists and contains the string `VALID-01` and `VALID-02` (requirements coverage)
    - `41-01-SUMMARY.md` contains `D-07` (documents the ROADMAP inline fix decision)
    - Commit contains exactly three files: `41-VERIFICATION.md`, `ROADMAP.md`, `STATE.md` — no files outside `.planning/` in the commit (verify with `git show --name-only HEAD`)
  </acceptance_criteria>

  <done>Planning artifacts committed. 41-01-SUMMARY.md written. Phase 41 fully documented and closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git CLI output → VERIFICATION.md | Raw output captured verbatim; no user input involved |
| npm test output → VERIFICATION.md | Test runner output captured; no network calls in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | git diff output | accept | Read-only verification; diff compares committed git objects — output is deterministic and not user-influenced |
| T-41-02 | Information Disclosure | 41-VERIFICATION.md | accept | Contains only git history metadata and test counts; no secrets or PII; committed to the planning directory |
| T-41-03 | Repudiation | Commit authorship | accept | Standard git commit with conventional message; milestone audit trail is the VERIFICATION.md artifact itself |
</threat_model>

<verification>
After all tasks complete:

1. `test -f .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md` — exits 0
2. `grep -q 'status: passed' .planning/phases/41-final-verification-parity-audit/41-VERIFICATION.md` — exits 0
3. `grep -q 'status: complete' .planning/STATE.md` — exits 0
4. `grep 'Stage and Commit Tests' .planning/ROADMAP.md | grep -q 'Complete'` — exits 0
5. `git log --oneline -1 | grep -q 'docs(phase-41)'` — exits 0
6. `git show --name-only HEAD | grep -v '^$' | grep -v 'docs(phase-41)' | grep -v '^\.planning/'` — exits 1 (no non-.planning files in commit)
</verification>

<success_criteria>
- 41-VERIFICATION.md written with `status: passed`, raw diff output confirming zero unexpected divergence, npm test summary confirming 8300+ pass / 0 fail, scanner subtest confirmed clean
- ROADMAP.md Phase 39 row reads `Complete | 2026-05-22`
- STATE.md frontmatter `status: complete`
- Commit `docs(phase-41): complete final verification and parity audit` exists touching only `.planning/` files
- 41-01-SUMMARY.md exists documenting all decisions applied (D-01 through D-08)
</success_criteria>

<output>
After completion, `.planning/phases/41-final-verification-parity-audit/41-01-SUMMARY.md` is the deliverable. The canonical audit record is `41-VERIFICATION.md` in the same directory.
</output>
