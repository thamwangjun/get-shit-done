---
phase: 71-spec-04-eta-materialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/04-eta-materialization/SPEC.md
autonomous: true
requirements: [SPEC-04, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md specifies the Eta v4 install-time materialization contract: copy-path coverage across all three paths (commands/workflows install.js:6515, agents install.js:8731, and skills wrappedConverter in runtime-artifact-layout.cjs) with no surviving non-allowlisted @~/.claude/ ref and no surviving <%~ directive (ROADMAP criterion 1 / SPEC-04, per D-01/D-02); the ALLOWED_INLINE_REFS exception as a normative rule with the ~30 entries marked advisory (per D-04)"
    - "SPEC.md specifies <%~ include() inlining (04-INV-2), the observable engine config — autoEscape:false, raw <%~ output, resolvePath against views root (04-INV-3, per D-03), circular-include failure as a descriptive Error naming the path not a RangeError (04-INV-4), and missing-include failure as EtaFileResolutionError naming the file (04-INV-5)"
    - "SPEC.md states ~5 numbered EARS invariants in 04-INV-M format citing the single tier-1 test file tests/install-eta-regression.test.cjs, with an Acceptance Tests traceability table mapping each MUST invariant to a real subtest (TEST-01a/01b, TEST-03, TEST-04, TEST-05) and no [MISSING] rows (ROADMAP criterion 2 / QUAL-01, QUAL-02, QUAL-04, per D-01); current paths/symbols advisory-marked (QUAL-03)"
    - "SPEC.md records 'Eta v4 over custom resolveIncludes()' and 'Default Eta delimiters' as settled Key Decisions, each with the consequence of reopening stated (ROADMAP criterion 3 / QUAL-05, per D-05); frontmatter Status advances Draft -> Ready with Confidence and Specced (2026-06-12) set, gated on QUAL-01..05"
  artifacts:
    - path: ".planning/spec/04-eta-materialization/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-04 (Purpose, Scope, ~5 invariants, Acceptance Tests, Key Decisions, advisory Code Context) with Status: Ready"
      contains: "04-INV-1"
  key_links:
    - from: ".planning/spec/04-eta-materialization/SPEC.md (Acceptance Tests table)"
      to: "the single tier-1 test file tests/install-eta-regression.test.cjs (subtests TEST-01a/01b, TEST-03, TEST-04, TEST-05)"
      via: "verbatim subtest names keyed on 04-INV-M"
      pattern: "tests/install-eta-regression\\.test\\.cjs"
    - from: ".planning/spec/04-eta-materialization/SPEC.md (Invariants)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme)"
      via: "section order + invariant ID format conformance"
      pattern: "04-INV-[1-6]"
---

<objective>
Author the full body of `.planning/spec/04-eta-materialization/SPEC.md` — a behavioral-contract
specification of the Eta v4 install-time content materialization pipeline and the three copy
paths it runs on — so a reimplementer can rebuild the system on a refactored upstream without
reading the current source. The phase fills an existing stub (frontmatter + empty 7-section
skeleton from Phase 68); it does NOT modify `bin/install.js`, `renderEtaContent`, the copy
paths, or the skills `wrappedConverter` — it narrates the single tier-1 test file
`tests/install-eta-regression.test.cjs` into the locked 7-section contract and advances Status
`Draft -> Ready`.

Purpose: Capture the Eta materialization contract (Eta runs on every install copy path so no
unrendered `<%~` directive and no non-allowlisted `@~/.claude/` include survives; `<%~ include()`
inlines fragment content; the engine renders with `autoEscape: false` + raw `<%~` + views-root
`resolvePath`; circular includes throw a descriptive `Error` naming the path; missing includes
throw `EtaFileResolutionError` naming the file) as a durable, move-proof contract (shape
normative; counts/paths/regexes advisory), satisfying SPEC-04 and the shared QUAL-01..05 bars so
the spec reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, ~5 invariants (`04-INV-1`..`04-INV-5`; split
to `04-INV-6` is permitted per D-03 only if the engine-config invariant becomes overloaded), an
Acceptance Tests traceability table citing real subtests in the single tier-1 file, two settled
Key Decisions, advisory Code Context, and frontmatter Status `Ready` (Confidence + Specced
2026-06-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-71 source artifacts (locked decisions + narration map + locked template + the stub)
@.planning/phases/71-spec-04-eta-materialization/71-CONTEXT.md
@.planning/phases/71-spec-04-eta-materialization/71-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/04-eta-materialization/SPEC.md

# Sibling Wave-1 worked references (section shape, EARS phrasing, advisory marking, QUAL pattern)
@.planning/spec/01-positive-framing/SPEC.md
@.planning/spec/02-sha-versioning/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose — no code/test
symbols are created; all system symbols are PRE-EXISTING and appear only under advisory Code
Context):

- **Invariant IDs:** `04-INV-1` (copy-path materialization coverage across commands/workflows,
  agents, and skills `wrappedConverter` — no surviving non-allowlisted `@~/.claude/` ref and no
  surviving `<%~` directive; `ALLOWED_INLINE_REFS` rule folds in here), `04-INV-2` (`<%~ include()`
  inlining), `04-INV-3` (engine config observable — `autoEscape: false`, raw `<%~` output,
  views-root `resolvePath`), `04-INV-4` (circular-include failure — descriptive `Error` naming the
  path, never a raw `RangeError`), `04-INV-5` (missing-include failure — `EtaFileResolutionError`
  naming the missing file). A 6th invariant `04-INV-6` is created ONLY if the executor judges
  `04-INV-3` overloaded and splits the resolve-path clause out (Claude's Discretion per D-03 /
  RESEARCH Open Question 1).
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** frontmatter `**Status:**` advances `Draft -> Ready`; `**Confidence:**`
  (High recommended per RESEARCH §8) and `**Specced:** 2026-06-12` set. The
  `**Reimplementation evidence (tier-1 test):**` line already names the one tier-1 file correctly
  (`tests/install-eta-regression.test.cjs`) and needs no expansion (RESEARCH §8).

PRE-EXISTING (advisory, NOT produced here — list under Code Context only, per RESEARCH §5):
`bin/install.js` symbols — `renderEtaContent`, `_etaSourceRoot`, the
`new Eta({ views, useWith: true, autoEscape: false })` config, the `resolvePath` override, the
`RangeError`->descriptive-`Error` wrap, call sites `:6515` (commands/workflows) and `:8731`
(agents), the `module.exports` of `renderEtaContent`;
`get-shit-done/bin/lib/runtime-artifact-layout.cjs` symbols — `skillsKind`, `etaViewsRoot`, the
`wrappedConverter` closure (lines 198–201) that calls `renderEtaContent` before the per-runtime
skill converter, `stageSkillsForRuntimeAsSkills`; the upstream `EtaFileResolutionError` class
(imported from `eta`); and the test-file symbols `ALLOWED_INLINE_REFS` (30 entries), `walkDir`,
`walkDirEta`, `installRuntimeArtifacts`. NOTE: the historical symbol
`_copyCommandsAsSkillsViaConverter` is JSDoc-only and is NOT a current function — Code Context
names the real skills path as the `wrappedConverter` closure in `runtime-artifact-layout.cjs`
(RESEARCH §6 note 2).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, ~5 04-INV-M invariants, Acceptance Tests table</name>
  <files>.planning/spec/04-eta-materialization/SPEC.md</files>
  <read_first>
    - .planning/spec/04-eta-materialization/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose)
    - .planning/phases/71-spec-04-eta-materialization/71-RESEARCH.md (THE narration map — §1 the
      verbatim describe/test block names for TEST-01a/01b, TEST-03, TEST-04, TEST-05 with line
      numbers; §2 the ALLOWED_INLINE_REFS rule + 30-entry classification (prose vs conditional);
      §3 the five invariant->subtest mappings with EARS-phraseable claims, verbatim subtest names,
      and assertion shapes; §6 the concordance audit (TEST-02 is ABSENT — add no TEST-02 row; the
      skills path is the wrappedConverter closure, NOT _copyCommandsAsSkillsViaConverter))
    - .planning/phases/71-spec-04-eta-materialization/71-CONTEXT.md (LOCKED decisions D-01..D-05)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template, NN-INV-M scheme §2, EARS patterns
      + RFC 2119 strength §4, the example `01-INV-1` invariant shape)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference — invariant prose +
      consequence-clause shape, the "branch enumeration below is advisory" rule-as-supporting-list
      pattern for ALLOWED_INLINE_REFS, and Acceptance Tests table layout)
    - .planning/spec/02-sha-versioning/SPEC.md (sibling worked reference — multi-row-per-invariant
      traceability table and shared-subtest rows under multiple invariant IDs)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (state the correct behavior directly; the spec models the
    fork's affirmative-framing standard it sits beside).

    Purpose (one paragraph): state the "why" of the Eta v4 install-time materialization pipeline —
    every `.md` file copied to a target runtime is passed through `renderEtaContent` (Eta v4) so
    that `<%~ include('...') %>` directives, which reference shared fragments by `@~/.claude/`-style
    paths, are materialized (inlined) into the installed output before it reaches the runtime config
    directory. State what breaks if the contract is absent or wrong: an unrendered `<%~ include()`
    directive ships in an installed file as literal template syntax, or an `@~/.claude/` reference
    that should have been inlined survives as an unresolved path — both produce broken prompt
    content in the installed runtime; and a copy path that skips rendering (the skills path was
    found only via post-milestone audit) silently regresses coverage. Cite
    `tests/install-eta-regression.test.cjs` as the behavioral authority (source-of-truth tier 1 per
    00-CONVENTIONS.md §4).

    Scope: two bullet lists. In scope (each behavioral role): Eta rendering across all three copy
    paths (commands/workflows, agents, and the skills `wrappedConverter`) such that no unrendered
    `<%~` and no non-allowlisted `@~/.claude/` survives; the `ALLOWED_INLINE_REFS` exception rule;
    `<%~ include()` inlining of fragment content; the observable engine config (`autoEscape: false`,
    raw `<%~` output, views-root `resolvePath`); circular-include failure (descriptive `Error`
    naming the path); and missing-include failure (`EtaFileResolutionError` naming the file). Out of
    scope (each with a one-line rationale): (1) the literal `{{GSD_REPO}}` / `{{GSD_BRANCH}}` /
    `{{GSD_VERSION}}` regex placeholder substitution that wires repo identity and SHA into installed
    hooks — that is SPEC-02 (the explicit boundary: `<%~ include()` content materialization IS
    SPEC-04, `{{...}}` repo/branch/version substitution IS SPEC-02; both remain INDEX root nodes
    with no dependency edge added); (2) the bespoke `resolveIncludes()` stepping-stone resolver from
    Phase 44 — superseded by Eta v4 and listed in INDEX.md `## Excluded from Scope`; a reimplementer
    does not carry it forward (its supersession is recorded as a Key Decision, not an invariant);
    (3) the per-runtime skill *converter* functions (e.g. `convertClaudeCommandToClaudeSkill`) and
    the profile-staging functions `stageSkillsForProfile`/`stageSkillsForMode` — these run before or
    after Eta and are orthogonal to materialization (RESEARCH §6 note 3).

    Invariants: author ~5 numbered EARS statements with RFC 2119 strength, IDs `04-INV-1`..`04-INV-5`
    (NN-INV-M format per 00-CONVENTIONS.md §2). Each is a single falsifiable claim mapping to a real
    subtest cluster in RESEARCH §3. Use the per-invariant EARS pattern recommended in RESEARCH §3.
      - 04-INV-1 Copy-path materialization coverage (Ubiquitous MUST): the system MUST run
        `renderEtaContent` on every installed `.md` file across all three copy paths — commands/
        workflows, agents, and the skills `wrappedConverter` — such that no `@~/.claude/` reference
        outside `ALLOWED_INLINE_REFS` and no `<%~` Eta directive survives in any installed `.md`
        file. Name all three paths explicitly per D-02 (the skills `wrappedConverter` path was found
        only via post-milestone audit; naming it keeps the gap from silently reopening). Fold the
        D-04 `ALLOWED_INLINE_REFS` rule in HERE as a supporting normative RULE: an installed
        `@~/.claude/` reference is permitted only when it is an intentional AI-instruction prose ref
        or a `${...}`-preserved conditional; any other `@~/.claude/` occurrence (an include ref that
        should have been inlined) and any surviving `<%~` tag are violations. Render the ~30
        `ALLOWED_INLINE_REFS` entries as a dated supporting enumeration marked
        `current as of 2026-06-12` (table or bullets — Claude's Discretion per D-04 / RESEARCH Open
        Question 2; recommend grouping by `prose` vs `conditional` class), explicitly NOT as the
        contract. State that the surviving-`<%~` half needs no allowlist (any survivor is always a
        bug). Traces to BOTH TEST-01 subtests.
      - 04-INV-2 Include inlining (Event-driven MUST): when `renderEtaContent` processes a template
        containing `<%~ include('path') %>`, the system MUST resolve the referenced file relative to
        the views root and inline its full content into the rendered output. Traces to TEST-03
        (`gsd-executor.md` inlines "Mandatory Initial Read" from `mandatory-initial-read.md`).
      - 04-INV-3 Engine configuration observable (Ubiquitous MUST): the Eta engine MUST render with
        `autoEscape: false` so included content is emitted verbatim without HTML-entity escaping;
        `<%~` MUST emit raw (unescaped) output; and include paths MUST resolve by joining the views
        root with the template path. This invariant shares TEST-03 (inlining succeeds with content
        intact) and TEST-01b (all `<%~` consumed, engine active) evidence with 04-INV-2/04-INV-1 —
        there is no standalone engine-config test, which is acceptable (RESEARCH §6 note 5; QUAL-02
        requires each MUST trace to a real subtest, not a dedicated one). The design choices behind
        this observable contract are recorded as Key Decisions (D-03/D-05 — Task 2). Split the
        resolve-path clause to a separate `04-INV-6` ONLY if this invariant becomes overloaded
        (RESEARCH Open Question 1 — Claude's Discretion).
      - 04-INV-4 Circular-include failure (Unwanted-behavior MUST + MUST NOT): if `renderEtaContent`
        processes a file that directly or transitively includes itself, the system MUST throw a
        descriptive `Error` whose message names the offending source path, and MUST NOT propagate a
        raw `RangeError`. Traces to TEST-04. State the contract as "names the offending source path"
        (the absolute fixture path appears in the message), NOT the exact prefix text "Circular
        include detected in:" (that string is advisory — RESEARCH §1 / §3 04-INV-4).
      - 04-INV-5 Missing-include failure (Event-driven / Unwanted-behavior MUST): when
        `renderEtaContent` processes a file that includes a nonexistent path, the system MUST surface
        an `EtaFileResolutionError` — the upstream Eta v4 class imported from the `eta` package, NOT
        a custom error — whose message names the missing filename. Traces to TEST-05. Note the
        distinction from 04-INV-4: this error is NOT caught/rethrown by `renderEtaContent`; only
        `RangeError` is wrapped, so `EtaFileResolutionError` bubbles up naturally (RESEARCH §3
        04-INV-5).
      End each invariant with a "Consequence of violating: <what breaks>" clause per the
      00-CONVENTIONS.md §4 example shape (consequences supplied per-invariant in RESEARCH §3).

    Acceptance Tests: a traceability table keyed on `04-INV-M`, columns exactly
    `Invariant | Test File | Subtest / Assertion Shape`. The Test File column carries the single
    tier-1 file `tests/install-eta-regression.test.cjs` in every row (SPEC-04 has one tier-1 file).
    Lift REAL verbatim subtest names from RESEARCH §1/§3 for every MUST invariant — multiple rows
    where a cluster exists (04-INV-1 gets both TEST-01 subtests; 04-INV-3 shares the TEST-03 and
    TEST-01b rows — duplicate test-name rows under different invariant IDs are correct, RESEARCH §6
    note 3 / Open Question 3). Every MUST invariant traces to a real subtest (D-01): NO
    `[MISSING — write test first]` rows. Add NO TEST-02 row (TEST-02 does not exist — RESEARCH §6
    note 1). Include at least these literal subtest names verbatim:
      - 04-INV-1: `'Full Claude runtime install leaves no non-allowlisted @~/.claude/ refs in any installed .md file'`
        and `'Full Claude runtime install leaves no unrendered <%~ Eta directives in any installed .md file'`
      - 04-INV-2: `'Eta rendering of gsd-executor.md inlines "Mandatory Initial Read" from mandatory-initial-read.md'`
      - 04-INV-3: the same TEST-03 inlining row plus the TEST-01b surviving-`<%~` row (shared evidence)
      - 04-INV-4: `'Self-referencing include throws Error with fixture path in message'`
      - 04-INV-5: `'Include of nonexistent file throws EtaFileResolutionError naming the missing path'`
  </action>
  <verify>
    <automated>command grep -c '04-INV-' .planning/spec/04-eta-materialization/SPEC.md  # >=5 invariant-ID occurrences (>=6 if engine-config split)</automated>
    <automated>command grep -q 'wrappedConverter' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'ALLOWED_INLINE_REFS' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'EtaFileResolutionError' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'current as of 2026-06-12' .planning/spec/04-eta-materialization/SPEC.md && echo OK</automated>
    <automated>command grep -q "tests/install-eta-regression.test.cjs" .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'inlines "Mandatory Initial Read"' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'Self-referencing include throws Error with fixture path in message' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'EtaFileResolutionError naming the missing path' .planning/spec/04-eta-materialization/SPEC.md && echo OK</automated>
    <automated>command grep -c 'MISSING — write test first' .planning/spec/04-eta-materialization/SPEC.md  # ==0, every MUST invariant has a real subtest (D-01)</automated>
    <automated>command grep -c 'TEST-02' .planning/spec/04-eta-materialization/SPEC.md  # ==0, TEST-02 does not exist (RESEARCH §6 note 1)</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c '04-INV-' SPEC.md` returns >=5 (five invariant IDs `04-INV-1`..`04-INV-5`
      present; >=6 if the executor split the engine-config resolve-path clause to `04-INV-6`).
    - The single tier-1 test file `tests/install-eta-regression.test.cjs` appears in the Acceptance
      Tests table (it is the only tier-1 file; it appears in every row).
    - The table contains the verbatim anchor subtest names listed in the action: the two TEST-01
      subtest names (04-INV-1), the TEST-03 inlining name (04-INV-2, reused by 04-INV-3 with the
      TEST-01b row), the TEST-04 name (04-INV-4), and the TEST-05 name (04-INV-5), lifted exactly
      from RESEARCH §1/§3.
    - `wrappedConverter`, `ALLOWED_INLINE_REFS`, `EtaFileResolutionError`, and the literal marker
      `current as of 2026-06-12` all appear (the skills path is named, the allowlist rule is dated
      advisory, the missing-include error class is named).
    - `command grep -c 'MISSING — write test first' SPEC.md` returns 0 (no MISSING rows — D-01).
    - `command grep -c 'TEST-02' SPEC.md` returns 0 (no TEST-02 row — TEST-02 does not exist).
    - The four sections appear under the exact headers `## Purpose`, `## Scope`, `## Invariants`,
      `## Acceptance Tests` in that order, with no placeholder `<!-- to be filled -->` comment
      remaining in them.
  </acceptance_criteria>
  <done>
    Purpose, Scope, Invariants (~5 `04-INV-M` EARS statements), and the Acceptance Tests
    traceability table are authored; ROADMAP criteria 1 and 2 are satisfied; copy-path coverage
    across all three paths (commands/workflows, agents, skills `wrappedConverter`), `<%~ include()`
    inlining, the observable engine config, circular-include failure, and missing-include failure
    are all specified, and every MUST invariant traces to a real subtest in the single tier-1 file
    with no MISSING rows and no TEST-02 row.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author two settled Key Decisions + advisory Code Context, advance Status Draft -> Ready</name>
  <files>.planning/spec/04-eta-materialization/SPEC.md</files>
  <read_first>
    - .planning/spec/04-eta-materialization/SPEC.md (now carrying the Task 1 normative core; fill the
      remaining `## Key Decisions` and `## Code Context` sections and update the frontmatter block)
    - .planning/phases/71-spec-04-eta-materialization/71-RESEARCH.md (§4 the two ready-to-insert Key
      Decisions KD-A "Eta v4 over custom resolveIncludes()" and KD-B "Default Eta delimiters" with
      rationale + consequence-of-reopening + tier-1 evidence; §5 the full advisory symbol inventory
      across `bin/install.js`, `runtime-artifact-layout.cjs`, and the test file — including the
      `wrappedConverter` mechanics; §8 the frontmatter note that the one-file evidence line needs no
      expansion)
    - .planning/phases/71-spec-04-eta-materialization/71-CONTEXT.md (D-03 engine-config-is-also-a-KD,
      D-05 the two ROADMAP-mandated KDs verbatim)
    - .planning/spec/00-CONVENTIONS.md (§1 Section 6/7 shape; §3 status vocabulary Draft|Ready|
      Implemented|Verified and the "MUST NOT advance to Ready unless QUAL-01..05 satisfied" rule)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference for the settled/consequence
      footer format and the advisory Code Context layout)
    - .planning/spec/02-sha-versioning/SPEC.md (sibling worked reference — same)
  </read_first>
  <action>
    Fill the final two sections and update the frontmatter block; keep authoring affirmative-framed
    prose.

    Key Decisions (`## Key Decisions`): record the two settled decisions from RESEARCH §4 (D-05),
    each as statement + one-sentence rationale + "Settled — do not reopen. Consequence of reopening:
    <what breaks>" (use the consequences supplied in RESEARCH §4 verbatim or near-verbatim, and cite
    the tier-1 evidence given there):
      (a) KD-A "Eta v4 over custom `resolveIncludes()`" (D-05, ROADMAP-mandated): the install-time
          content materialization pipeline uses Eta v4 (`<%~ include() %>`, `renderEtaContent`)
          rather than the bespoke `resolveIncludes()` resolver written in Phase 44 as a
          stepping-stone (the `resolveIncludes()` stepping stone is listed in INDEX.md
          `## Excluded from Scope` — a reimplementer does not carry it forward); consequence of
          reopening — reintroduces the bespoke resolver Eta v4 superseded, losing circular-include
          detection (TEST-04), proper missing-file errors (TEST-05), and the zero-config include
          pipeline, and any source files converted from `@~/` refs to `<%~ include() %>` would need
          reconversion.
      (b) KD-B "Default Eta delimiters" (D-05, ROADMAP-mandated): the engine uses Eta's default
          delimiters (`<%`/`%>`) and raw-output marker (`<%~`) with no custom `tags` configuration;
          consequence of reopening — every `<%~ include() %>` directive in the source corpus would
          need rewriting to the new delimiter, the TEST-01b `<%~` survivor scan pattern would need
          updating, and the corpus-scanner expectations in `tests/install-eta-regression.test.cjs`
          would break.
    Note that KD-A and KD-B are the two ROADMAP-mandated settled decisions (ROADMAP criterion 3 /
    QUAL-05); KD-B is also the design intent paired with the observable engine-config invariant
    `04-INV-3` (D-03 — the observable behavior is the invariant, the delimiter choice is the
    decision). The `autoEscape: false` choice is observable in `04-INV-3` and need not be repeated as
    a third Key Decision; recording it as a one-line design note under KD-B is at Claude's Discretion.

    Code Context (`## Code Context`): mark the section `<!-- advisory -->` and open with a dated line
    in the sibling Phase 69/70 style (e.g. "The items below are current as of 2026-06-12. All file
    paths, function names, regex bodies, and line numbers are advisory and will shift on any test
    edit or upstream refactor."). List the PRE-EXISTING current paths/symbols ONLY here, with no
    normative claim resting on them, grouped by the three implementation areas (RESEARCH §5):
    `bin/install.js` (`renderEtaContent` ~6452, the `new Eta({ views, useWith: true, autoEscape:
    false })` config ~6453, the `resolvePath` override ~6458, the `RangeError`->descriptive-`Error`
    wrap ~6464, `_etaSourceRoot` ~1772, call site 1 `:6515` commands/workflows via
    `copyWithPathReplacement`, call site 2 `:8731` agents, the `renderEtaContent` `module.exports`
    ~11551); `get-shit-done/bin/lib/runtime-artifact-layout.cjs` (`skillsKind` ~184, `etaViewsRoot`
    ~197, the `wrappedConverter` closure ~198–201 that calls `renderEtaContent` before the
    per-runtime skill converter, `stageSkillsForRuntimeAsSkills` ~202, the Claude global layout
    `skillsKind(...)` ~239); and `tests/install-eta-regression.test.cjs` (`ALLOWED_INLINE_REFS` 30
    entries ~40–96, `walkDir`/`walkDirEta`, `installRuntimeArtifacts`, the `EtaFileResolutionError`
    import from `eta`). Add the explicit correction note that the historical symbol
    `_copyCommandsAsSkillsViaConverter` is JSDoc-only and is NOT the current skills path — the real
    path is the `wrappedConverter` closure (RESEARCH §6 note 2). State that all line numbers are
    advisory and shift on any source edit (move-proofing for the upstream refactor).

    Frontmatter transition (block-header frontmatter, NOT YAML): set `**Status:** Ready` (from
    Draft), set `**Confidence:**` to a stated value (High recommended per RESEARCH §8 — Claude's
    discretion per CONTEXT.md), and set `**Specced:** 2026-06-12`. Leave
    `**Reimplementation evidence (tier-1 test):**` as the one already-correct file
    `tests/install-eta-regression.test.cjs` (no expansion needed — SPEC-04 has one tier-1 file per
    RESEARCH §8). Advancing to Ready asserts QUAL-01 (numbered falsifiable EARS invariants), QUAL-02
    (traceability table maps each MUST to a subtest, no MISSING rows), QUAL-03 (advisory-marked
    paths, move-proof), QUAL-04 (the tier-1 test cited in every row), QUAL-05 (two Key Decisions with
    do-not-reopen + consequence) are all satisfied by the finished body.
  </action>
  <verify>
    <automated>command grep -q '\*\*Status:\*\* Ready' .planning/spec/04-eta-materialization/SPEC.md && command grep -q '\*\*Specced:\*\* 2026-06-12' .planning/spec/04-eta-materialization/SPEC.md && echo OK</automated>
    <automated>command grep -c 'Settled — do not reopen' .planning/spec/04-eta-materialization/SPEC.md  # >=2 Key Decisions (KD-A, KD-B)</automated>
    <automated>command grep -q 'resolveIncludes' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'Default Eta delimiters' .planning/spec/04-eta-materialization/SPEC.md && echo OK  # the two ROADMAP-mandated KDs present</automated>
    <automated>command grep -q 'advisory' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'renderEtaContent' .planning/spec/04-eta-materialization/SPEC.md && command grep -q 'runtime-artifact-layout.cjs' .planning/spec/04-eta-materialization/SPEC.md && echo OK</automated>
    <automated>command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' .planning/spec/04-eta-materialization/SPEC.md  # ==6, locked section set, none renamed/added/reordered</automated>
    <automated>command grep -c 'to be filled' .planning/spec/04-eta-materialization/SPEC.md  # ==0, no placeholder comments remain</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` contains the literal `**Status:** Ready` and `**Specced:** 2026-06-12`; `Confidence`
      is set to a non-placeholder value.
    - `command grep -c 'Settled — do not reopen'` returns >=2 (the two Key Decisions KD-A, KD-B).
    - Both ROADMAP-mandated decisions are present: `resolveIncludes` (KD-A) and
      `Default Eta delimiters` (KD-B) each appear with a consequence-of-reopening sentence.
    - The `## Code Context` section is marked `<!-- advisory -->` and lists `renderEtaContent`,
      `wrappedConverter`, `runtime-artifact-layout.cjs`, `ALLOWED_INLINE_REFS`, and the
      `_copyCommandsAsSkillsViaConverter`-is-JSDoc-only correction note; no normative invariant text
      appears in Code Context.
    - The six body section headers appear exactly once each in the locked order (`## Purpose`,
      `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`) —
      the section-header grep returns 6 and no section is renamed/added/removed versus
      00-CONVENTIONS.md §1.
    - `command grep -c 'to be filled' SPEC.md` returns 0 (every placeholder comment is replaced).
  </acceptance_criteria>
  <done>
    Two settled Key Decisions (KD-A, KD-B — the two ROADMAP-mandated ones) and advisory Code Context
    are authored; frontmatter Status is `Ready` with Confidence + Specced set and the one-file
    evidence line left correct; ROADMAP criterion 3 is satisfied and the spec conforms exactly to
    the locked 7-section template (no drift for Phase 77).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This phase authors a static Markdown documentation artifact (`SPEC.md`). There is no untrusted input, no code execution, no runtime surface, no network call, and no secret handled. The Eta materialization pipeline the spec describes runs at install time (file read + template render + file write), but specifying it in prose introduces none of that surface — standard STRIDE injection/auth/DoS categories do not apply to a prose deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Information disclosure / Tampering (spec INACCURACY) | SPEC.md normative claims (Invariants + Acceptance Tests) | mitigate | Every invariant and Acceptance Tests row is traced to a real subtest in the single tier-1 file `tests/install-eta-regression.test.cjs` with verbatim subtest names (per QUAL-02/QUAL-04 / D-01 — no MISSING rows, no TEST-02 row since TEST-02 does not exist); no normative claim rests on an advisory path/symbol/line-number the upstream refactor could move (per QUAL-03 advisory-marking); the `ALLOWED_INLINE_REFS` 30-entry enumeration is narrated as `current as of 2026-06-12` shape, not a durable magic count; the skills path is named as the verified `wrappedConverter` closure, not the JSDoc-only `_copyCommandsAsSkillsViaConverter`. |
| T-71-02 | Tampering (advisory-claim drift) | SPEC.md Code Context paths/symbols | accept | Current paths, symbol names (`renderEtaContent`, `wrappedConverter`, `EtaFileResolutionError`, `ALLOWED_INLINE_REFS`, `_etaSourceRoot`, `skillsKind`), regex bodies, and line numbers will shift on any source edit or upstream refactor; this is accepted because they are confined to `## Code Context` and marked `<!-- advisory -->`, so no contract depends on them. |

No package-manager installs occur in this phase, so the `T-{phase}-SC` supply-chain row and the
package-legitimacy checkpoint do not apply. No high-severity threats are present.
</threat_model>

<verification>
Overall phase checks (run after both tasks):
- `command grep -c '04-INV-' SPEC.md` >= 5 (five invariants present; >=6 if engine-config split).
- `command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' SPEC.md` == 6 (locked section set, exact order, no drift vs 00-CONVENTIONS.md §1).
- The single tier-1 file is cited: `command grep -q 'tests/install-eta-regression.test.cjs' SPEC.md` succeeds.
- `command grep -c 'MISSING — write test first' SPEC.md` == 0 (every MUST invariant has a real
  subtest — D-01).
- `command grep -c 'TEST-02' SPEC.md` == 0 (no TEST-02 row — TEST-02 does not exist, RESEARCH §6 note 1).
- `command grep -c 'Settled — do not reopen' SPEC.md` >= 2 (KD-A, KD-B); both `resolveIncludes` and
  `Default Eta delimiters` appear (the two ROADMAP-mandated decisions — QUAL-05).
- `command grep -q 'wrappedConverter' SPEC.md` succeeds (the audit-found skills path is named — D-02).
- `command grep -q '**Status:** Ready'` and `command grep -q '**Specced:** 2026-06-12'` both succeed.
- `command grep -c 'to be filled' SPEC.md` == 0 (no placeholder comments remain).
</verification>

<success_criteria>
Measurable completion — maps to the three ROADMAP Phase 71 success criteria:
1. SPEC.md specifies the engine configuration (default delimiters, `autoEscape: false`, `<%~` raw
   output), the `<%~ include() %>` conversion from `@~/` refs, the `ALLOWED_INLINE_REFS` exception
   list, and coverage of every copy path — including the skills `wrappedConverter` — stated as an
   explicit invariant. [SPEC-04]
2. Invariants are ~5 numbered EARS statements citing `tests/install-eta-regression.test.cjs` as the
   tier-1 source; the Acceptance Tests traceability table maps each MUST invariant to a real subtest
   (no MISSING rows, no TEST-02 row). [QUAL-01, QUAL-02, QUAL-04]
3. Current paths/symbols are advisory-marked; Key Decisions records the Eta-v4-over-custom-resolver
   and default-delimiter decisions as settled with the consequence of reopening; Status is advanced
   `Draft -> Ready`. [QUAL-03, QUAL-05]
</success_criteria>

<output>
Create `.planning/phases/71-spec-04-eta-materialization/71-01-SUMMARY.md` when done.
</output>
