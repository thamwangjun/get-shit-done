---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 06
type: execute
wave: 6
depends_on: ["69-05"]
files_modified:
  - gsd-core/
  - docs/
  - tests/
autonomous: true
requirements: [MERGE-02, MERGE-03]
must_haves:
  truths:
    - "Tier 6: all 513 added-by-them upstream files (incl. the additive gsd-core/ rename tree) are accepted as-is"
    - "Remaining AU/UU gsd-core/** entries are resolved by accepting upstream (theirs) — additive, NOT a rename adoption"
    - "MERGE-03: tier order honored (upstream additions land LAST); Tier 6 may batch-stage since no conflict markers"
    - "The final commit that clears MERGE_HEAD lands here, establishing the merge with 2nd parent == 1bb253c9 (MERGE-02)"
    - "Both get-shit-done/ and gsd-core/ trees coexist post-merge (rename sweep deferred to Phase 71)"
  artifacts:
    - path: gsd-core/
      provides: "Accepted additive upstream rename-target tree (coexists with get-shit-done/)"
  key_links:
    - from: "in-progress merge (MERGE_HEAD == 1bb253c9)"
      to: "final merge commit"
      via: "git commit closing the merge after all tiers resolved"
      pattern: "git log --merges -1 --format=%P"
---

<objective>
Resolve Tier 6 — accept all remaining new upstream additions (the ~513 added-by-them files, including the entire incoming `gsd-core/` rename-target tree, new docs/, new bug-* tests, and the sdk-derived `gsd-core/bin/shared/*.json` renames) — then land the FINAL commit that closes the in-progress merge, establishing the merge commit whose second parent is `1bb253c9` (MERGE-02). Tier order is honored: upstream additions go last.

Purpose: Complete the single stateful merge. After this plan, MERGE_HEAD is cleared, the merge parent is 1bb253c9, both `get-shit-done/` and `gsd-core/` trees coexist (rename sweep deferred to Phase 71), and the full structural phase gate passes.

Output: All upstream additions accepted; merge closed; structural phase gate green.

CRITICAL: This is the ONLY plan that closes the merge. After this plan, `git rev-parse MERGE_HEAD` MUST fail (merge complete) and `git log --merges -1 --format=%P` 2nd parent MUST be 1bb253c9. Accepting the gsd-core/ tree is ADDITIVE — both trees coexist; do NOT delete get-shit-done/ (Pitfall 1; Phase 71 owns the rename). Verification is structural only (VERIFY-02) — never gate on npm test.
</objective>

<execution_context>
On branch `dev`, merge in progress from 69-01..69-05 (all prior tiers resolved). Stay on `dev`. After Tiers 1-5, the only remaining unmerged paths should be the additive gsd-core/** entries (AU/UU rename-target) and any new-file add/adds. These carry the incoming upstream content; accept upstream side (theirs) for the gsd-core/ tree since it is the new additive tree, not a fork file.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Accept all Tier-6 upstream additions (gsd-core/ tree + new docs/tests), batch-staged</name>
  <files>gsd-core/, docs/, tests/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 6: 513 added-by-them; gsd-core/ accepted as additive; Pitfall 1 do-not-delete-get-shit-done)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (MERGE-02 rename not adopted; D-03 batch allowed for non-conflict groups)
  </read_first>
  <action>
    Confirm only additive/upstream entries remain unmerged: `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)'` should now contain only `gsd-core/**` (and any leftover pure-additions). For the incoming `gsd-core/` rename-target tree (AU/UU): accept upstream as additive — `git checkout --theirs <path>` + `git add <path>` for each conflicted gsd-core/ entry, OR for clean added-by-them entries simply `git add` them. This is ADDITIVE acceptance, NOT a rename adoption — do NOT delete or move any `get-shit-done/` file (Pitfall 1). The 7 sdk/shared/*.json -> gsd-core/bin/shared/*.json renames land here as part of the gsd-core/ tree. Since Tier 6 carries no conflict markers to hand-merge, batch-stage per group (allowed by D-03 for non-conflict additions) and commit in a few group commits tagged `[Tier 6]`, e.g. `git commit -m "merge(69-06): accept upstream gsd-core/ additive tree [Tier 6]"` and `git commit -m "merge(69-06): accept new upstream docs/tests [Tier 6]"`. After this task, `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)'` MUST be empty.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)'` is empty (no unresolved paths remain)
    - `ls gsd-core/` succeeds (additive upstream tree present)
    - `ls get-shit-done/bin/lib/core.cjs` STILL succeeds (fork tree NOT deleted — both coexist)
    - Tier-6 acceptance committed (group commits tagged `[Tier 6]`)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)')" && ls gsd-core/ >/dev/null 2>&1 && ls get-shit-done/bin/lib/core.cjs >/dev/null 2>&1</automated>
  </verify>
  <done>All upstream additions accepted additively; gsd-core/ and get-shit-done/ coexist; no unresolved paths remain; merge ready to close.</done>
</task>

<task type="auto">
  <name>Task 2: Close the merge and verify the full structural phase gate</name>
  <files>(merge commit — closes MERGE_HEAD)</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-VALIDATION.md (Structural Phase Gate table — the exact checks)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (merge mechanics; Validation Architecture phase gate)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (MERGE-02/03/04, SDK-02, PATCH-03)
  </read_first>
  <action>
    With all tiers resolved and staged, land the FINAL commit that clears MERGE_HEAD and establishes the merge parent (MERGE-02): `git commit -m "merge(69-06): land v1.3.1 merge (2nd parent 1bb253c9) — close all tiers"`. Because MERGE_HEAD is set, this records the merge with 1bb253c9 as the second parent while all prior per-file commits remain in the merge range (>1 resolution commit per MERGE-03). Then run the full structural phase gate from 69-VALIDATION.md and confirm every check passes:
    1. `git log --merges -1 --format=%P` 2nd parent == 1bb253c9 (MERGE-02)
    2. `git status --porcelain` shows no UU/UD/DU/AA codes (MERGE-02/03)
    3. `git log` over the merge range shows >1 resolution commit, no mega-commit (MERGE-03)
    4. `ls CLAUDE.md CATALOGUE.json .planning/` all present non-empty (PATCH-03)
    5. `ls get-shit-done/bin/lib/core.cjs` present (rename NOT adopted, MERGE-02)
    6. `ls sdk/` fails (SDK-02)
    7. `npm install` ×2 -> exit 0, no churn on 2nd run (MERGE-04)
    Do NOT run `npm test` as a gate (VERIFY-02). If any structural check fails before this final commit lands, per D-05 you may `git merge --abort` and restart; once the final commit lands, fix forward.
  </action>
  <acceptance_criteria>
    - `git rev-parse MERGE_HEAD` FAILS (merge is closed)
    - `git log --merges -1 --format=%P | command grep -q 1bb253c9` (2nd parent is upstream)
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)' | head -1` is empty
    - `ls CLAUDE.md CATALOGUE.json .planning/ get-shit-done/bin/lib/core.cjs` all succeed; `ls sdk/` fails
    - `git log` over the merge range contains more than one resolution commit (no mega-commit)
    - `npm install` exits 0 and a second `npm install` leaves `git diff --quiet package-lock.json`
  </acceptance_criteria>
  <verify>
    <automated>! git rev-parse MERGE_HEAD >/dev/null 2>&1 && git log --merges -1 --format=%P | command grep -q 1bb253c9 && ls CLAUDE.md CATALOGUE.json .planning/ get-shit-done/bin/lib/core.cjs >/dev/null 2>&1 && ! ls sdk/ >/dev/null 2>&1</automated>
  </verify>
  <done>The v1.3.1 merge is committed (2nd parent 1bb253c9), MERGE_HEAD cleared, all structural phase-gate checks pass, get-shit-done/ and gsd-core/ coexist, sdk/ gone, lockfile clean — Phase 69 complete.</done>
</task>

</tasks>

<verification>
Full structural phase gate (from 69-VALIDATION.md):
- `git log --merges -1 --format=%P` 2nd parent == 1bb253c9 (MERGE-02)
- `git status --porcelain` no conflict codes (MERGE-02/03)
- `git log` merge range >1 resolution commit (MERGE-03)
- `ls CLAUDE.md CATALOGUE.json .planning/` present non-empty (PATCH-03)
- `ls get-shit-done/bin/lib/core.cjs` present (rename not adopted)
- `ls sdk/` fails (SDK-02)
- `npm install` ×2 -> exit 0, no churn (MERGE-04)
- NO green-suite dependency (VERIFY-02)
</verification>

<success_criteria>
Tier 6 upstream additions accepted additively (gsd-core/ coexists with get-shit-done/), the merge is closed with second parent 1bb253c9, and every structural phase-gate check passes — Phase 69 complete.
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-06-SUMMARY.md` when done. After this plan the merge is CLOSED (MERGE_HEAD cleared) and Phase 69 is complete.
</output>
