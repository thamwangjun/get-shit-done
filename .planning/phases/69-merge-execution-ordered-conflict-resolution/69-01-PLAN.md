---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTEXT.md
  - CLAUDE.md
  - CATALOGUE.json
  - .planning/
  - get-shit-done/
  - sdk/
  - gsd-core/
  - package.json
  - bin/install.js
autonomous: true
requirements: [MERGE-02, MERGE-03, PATCH-03, SDK-02]
must_haves:
  truths:
    - "MERGE-02: a shared-history `-s ort` merge of 1bb253c9 is started without --allow-unrelated-histories and without pre-renaming any directory"
    - "D-06: the SINGLE merge commit is the FIRST successful commit of the phase — it is a MECHANICAL fork-preserving resolution that integrates NO upstream functional changes yet; it carries 2nd parent 1bb253c9 and clears MERGE_HEAD"
    - "D-07: every conflict is resolved per-path to the fork-preserving / additive state for the merge commit ONLY — fork-owned UU/UD kept OURS, sdk/ accepted deleted, gsd-core/ and added-by-them accepted additive — NOT via `-X theirs` or bulk `-X ours` passed to merge"
    - "MERGE-02: fork modules at get-shit-done/bin/lib/*.cjs are present after the merge commit (rename to gsd-core/ NOT adopted; both trees coexist)"
    - "SDK-02: ls sdk/ fails after the merge commit (all sdk/ delete/modify resolved in upstream's favor)"
    - "PATCH-03: CLAUDE.md, CATALOGUE.json, .planning/ survive populated (absent from upstream, auto-kept)"
    - "D-05: any resolution mistake caught BEFORE the merge commit lands triggers `git merge --abort` and restart from clean"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Fork core module preserved through the merge commit (rename deferred to Phase 71)"
    - path: CLAUDE.md
      provides: "Fork project instructions (auto-survives; verified present)"
    - path: CATALOGUE.json
      provides: "Fork catalogue (auto-survives; verified present)"
  key_links:
    - from: "git merge --no-commit --no-ff -s ort 1bb253c9"
      to: "single merge commit (2nd parent 1bb253c9)"
      via: "mechanical per-path fork-preserving resolution, then one git commit that clears MERGE_HEAD"
      pattern: "git log --merges -1 --format=%P"
---

<objective>
Execute **Step A** of the git-correct merge model: start the shared-history `-s ort` merge of upstream v1.3.1 (1bb253c9) and resolve EVERY one of the 311 conflicts MECHANICALLY to the fork-preserving / additive state — WITHOUT integrating any upstream functional changes yet — then land the SINGLE merge commit. This first commit is the merge commit: it gets 2nd parent 1bb253c9 and clears MERGE_HEAD.

Purpose: Git refuses to commit while ANY unmerged path remains, and the FIRST successful commit after a `--no-commit` merge IS the merge commit (it consumes MERGE_HEAD and records the 2nd parent). So the only git-correct path is to make the entire conflict set resolvable in one pass to a mechanical fork-preserving state, commit it as the merge commit, and then integrate upstream's functional changes as ordinary per-file follow-up commits in plans 69-02..69-05 (Step B). This plan delivers the merge commit and the structural anchors (PATCH-03 present, sdk/ gone, fork tree preserved).

Output: A landed merge commit on `dev` with 2nd parent 1bb253c9; MERGE_HEAD cleared; working tree = fork files unchanged + upstream additions present + sdk/ deleted.

CRITICAL: This is the ONE merge commit for the phase. It MUST be the first successful commit. The resolution here is MECHANICAL ONLY — keep fork side for fork-owned files (per-path `git checkout --ours` + `git add`, NOT `-X ours` passed to the merge, NOT `-X theirs`), accept sdk/ deletion, accept gsd-core/ and added-by-them additively. Upstream's functional changes are folded into fork files LATER (69-02..69-05) as ordinary follow-up commits. Per D-05, abort+restart from clean on any mistake before this commit lands.
</objective>

<execution_context>
This plan runs in the live working tree on branch `dev`. Stay on `dev` — do NOT create, switch, or rename branches. The recovery anchor `pre-merge-v1.3.1-backup` already exists.
</execution_context>

<merge_commit_mechanics>
Verified in a scratch repo: (a) `git commit` refuses while ANY unmerged path remains (exit 128: "Committing is not possible because you have unmerged files"); (b) the FIRST successful commit after `git merge --no-commit` IS the merge commit — it consumes MERGE_HEAD, records the 2nd parent (1bb253c9), and clears MERGE_HEAD immediately. Therefore per-file commits CANNOT interleave with an unmerged index, and there is no "merge in progress" state spanning multiple plans.

The git-correct two-step model:
- **Step A (this plan, 69-01):** Resolve ALL 311 conflicts mechanically to the fork-preserving / additive state (keep OURS for fork-owned files, `git rm` sdk/, accept gsd-core/ + added-by-them). Stage everything. Run ONE `git commit` → this is the merge commit (2nd parent 1bb253c9, MERGE_HEAD cleared). No upstream functional integration happens yet.
- **Step B (69-02..69-05):** Integrate upstream's functional changes INTO the fork files as ORDINARY single-parent per-file follow-up commits, in triage order (D-03). MERGE_HEAD is already gone; these are normal commits AFTER the merge.

Result over the phase range: 1 merge commit + N per-file follow-up commits = ">1 resolution commit, no mega-commit" (MERGE-03). The 2nd-parent check belongs to the merge commit (this plan); the ">1 resolution commit" check belongs to the overall `git log` over the phase range (69-06 final gate).
</merge_commit_mechanics>

<tasks>

<task type="auto">
  <name>Task 1: Start the -s ort merge and mechanically resolve fork-owned conflicts + sdk/ deletion to OURS/additive</name>
  <files>get-shit-done/, sdk/, agents/, commands/gsd/, bin/install.js, hooks/, scripts/, package.json, CONTEXT.md, gsd-core/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (conflict set; "Merge Mechanics — two-step model"; delete/modify resolution table; Pitfall 1 do-not-adopt-rename)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-05 abort-and-restart; D-06/D-07 merge-commit model)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-VALIDATION.md (structural phase gate)
  </read_first>
  <action>
    Confirm clean start: `git status --porcelain` empty, `git rev-parse --abbrev-ref HEAD` == `dev`, recovery anchor present (`git branch --list pre-merge-v1.3.1-backup` non-empty).
    Start the merge without committing so the index can be fully resolved before the single merge commit (per D-06): `git merge --no-commit --no-ff -s ort 1bb253c9`. It exits non-zero because the 311 conflicts stop it — expected, NOT a failure. Do NOT pass `--allow-unrelated-histories`; do NOT pre-rename (MERGE-02). Do NOT pass `-X ours` or `-X theirs` to the merge — resolution is per-path below (D-07).
    Resolve MECHANICALLY for the merge commit ONLY — integrate NO upstream functional changes yet:
    - Fork-owned `UU` content conflicts (agents/, commands/gsd/, get-shit-done/{workflows,references,templates}/, bin/install.js, hooks/, .gitignore, CI workflows, scripts/, root CONTEXT.md, package.json, READMEs, CONTRIBUTING.md, docs/) → keep FORK side per-path: `git checkout --ours <file> && git add <file>`. This is per-path index resolution to the fork state (NOT `-X ours` on the merge). Enumerate via `git status --porcelain | command grep '^UU '` and filter to fork-owned paths (exclude gsd-core/**).
    - Fork-owned `UD` (deleted-by-them via rename) get-shit-done/bin/lib/*.cjs and fork-only scripts → RESTORE: `git checkout --ours <file> && git add <file>` (Pitfall 1 — never `--theirs`). Enumerate: `git status --porcelain | command grep '^UD .*get-shit-done/'`.
    - All `sdk/**` (`UD` modify/delete + staged `D`) → ACCEPT deletion: `git rm <path>` for each `git status --porcelain | command grep -E '^(UD|DU) .*sdk/'`; the clean staged `D ` deletions stay staged (SDK-02).
    Per D-05: on any wrong-strategy or unexpected state before the commit lands, `git merge --abort` and restart from clean.
  </action>
  <acceptance_criteria>
    - `git rev-parse MERGE_HEAD` resolves and begins with `1bb253c9` (merge started, not yet committed)
    - `git rev-parse --abbrev-ref HEAD` == `dev`
    - `git status --porcelain | command grep -E '^(UU|UD) (agents/|commands/gsd/|get-shit-done/|bin/|hooks/|scripts/|docs/|README|CONTRIBUTING|CONTEXT.md|package.json)'` is empty (fork-owned conflicts resolved OURS)
    - `git status --porcelain | command grep -E '^(UD|DU) .*sdk/'` is empty (sdk/ delete/modify resolved via git rm)
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse MERGE_HEAD | command grep -q '^1bb253c9' && test -z "$(git status --porcelain | command grep '^UD .*get-shit-done/')" && test -z "$(git status --porcelain | command grep -E '^(UD|DU) .*sdk/')"</automated>
  </verify>
  <done>Merge started; all fork-owned UU/UD resolved to OURS (fork tree preserved) and all sdk/ delete/modify accepted via git rm — merge NOT yet committed.</done>
</task>

<task type="auto">
  <name>Task 2: Accept gsd-core/ + added-by-them additively, land the SINGLE merge commit, verify it</name>
  <files>gsd-core/, docs/, tests/, (merge commit)</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 6: 513 added-by-them; gsd-core/ additive; Pitfall 1 do-not-delete-get-shit-done; AA/DU resolution map)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-06 single merge commit; MERGE-02 rename not adopted)
  </read_first>
  <action>
    Resolve all remaining unmerged paths additively for the merge commit:
    - Incoming `gsd-core/**` (`UU`/`AU`) → accept upstream additively: `git checkout --theirs <path> && git add <path>` (or `git add` clean added-by-them). This is ADDITIVE — do NOT delete or move any get-shit-done/ file (Pitfall 1; rename is Phase 71).
    - Remaining `AA` add/adds (e.g. rollout-next-phase*.sh, .github/workflows, bug-* tests) and the single `DU` (tests/execute-phase-step-5-5-deviation-doc.test.cjs) → for the MERGE COMMIT, resolve mechanically to the fork-preserving state: `AA` keep OURS (`git checkout --ours` + add) where a fork side exists, accept theirs only for purely-upstream additions; `DU` keep the fork's deletion via `git rm` unless the fork still needs it. Any upstream add/add functional content is folded in later (Step B).
    - The 513 clean added-by-them files carry no markers → `git add -A` the remaining unstaged additions.
    Confirm NO unmerged paths remain: `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)'` empty.
    Land the SINGLE merge commit (D-06 — this is the FIRST successful commit and IS the merge commit): `git commit -m "merge(69): land v1.3.1 (2nd parent 1bb253c9) — mechanical fork-preserving resolution"`. This clears MERGE_HEAD and records 1bb253c9 as the 2nd parent.
    Then verify the merge-commit-level anchors: 2nd parent == 1bb253c9; fork tree preserved (`ls get-shit-done/bin/lib/core.cjs`); gsd-core/ present; sdk/ gone; PATCH-03 files present non-empty.
    Per D-05: if a check fails BEFORE this commit lands, `git merge --abort` and restart Task 1.
  </action>
  <acceptance_criteria>
    - `git rev-parse MERGE_HEAD` FAILS (the single merge commit landed and cleared it)
    - `git log --merges -1 --format=%P | command grep -q 1bb253c9` (2nd parent is upstream)
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)' | head -1` is empty (no unresolved paths)
    - `ls get-shit-done/bin/lib/core.cjs` succeeds AND `ls gsd-core/` succeeds (both trees coexist)
    - `! ls sdk/` (sdk/ deleted) AND `ls CLAUDE.md CATALOGUE.json .planning/` present, `test -s CLAUDE.md && test -s CATALOGUE.json`
  </acceptance_criteria>
  <verify>
    <automated>! git rev-parse MERGE_HEAD >/dev/null 2>&1 && git log --merges -1 --format=%P | command grep -q 1bb253c9 && test -z "$(git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)')" && ls get-shit-done/bin/lib/core.cjs gsd-core/ CLAUDE.md CATALOGUE.json .planning/ >/dev/null 2>&1 && ! ls sdk/ >/dev/null 2>&1</automated>
  </verify>
  <done>The single merge commit landed (2nd parent 1bb253c9, MERGE_HEAD cleared); fork tree preserved, gsd-core/ additive, sdk/ gone, PATCH-03 files present — ready for Step B per-file upstream integration (69-02).</done>
</task>

</tasks>

<verification>
- `git rev-parse MERGE_HEAD` fails (merge committed — single merge commit landed)
- `git log --merges -1 --format=%P` 2nd parent == 1bb253c9 (MERGE-02)
- No unresolved markers anywhere (`git status --porcelain` clean of UU/UD/DU/AA/AU)
- `ls get-shit-done/bin/lib/core.cjs` + `ls gsd-core/` both succeed (coexist, rename not adopted)
- `ls sdk/` fails (SDK-02); CLAUDE.md/CATALOGUE.json/.planning/ present non-empty (PATCH-03)
</verification>

<success_criteria>
The v1.3.1 merge is landed as a SINGLE merge commit (2nd parent 1bb253c9) via mechanical fork-preserving resolution: fork tree intact, gsd-core/ additive, sdk/ gone, PATCH-03 files present. No upstream functional changes integrated yet — that is Step B (69-02..69-05). Ready for Tier 2 follow-up integration.
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-01-SUMMARY.md` when done. After this plan the merge is CLOSED (MERGE_HEAD cleared); 69-02..69-05 add ordinary per-file follow-up commits.
</output>
</content>
</invoke>
