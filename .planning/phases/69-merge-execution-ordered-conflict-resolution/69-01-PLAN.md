---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTEXT.md
  - CLAUDE.md
  - CATALOGUE.json
  - .planning/
autonomous: true
requirements: [MERGE-02, MERGE-03, PATCH-03]
must_haves:
  truths:
    - "A non-fast-forward merge of 1bb253c9 is in progress (MERGE_HEAD set) without --allow-unrelated-histories"
    - "Tier 1 ours-side files (root CONTEXT.md) are resolved and committed before any other tier"
    - "Fork-only files upstream lacks (CLAUDE.md, CATALOGUE.json, .planning/) survive the merge populated"
    - "D-03: every Tier 1 resolution lands as its own per-file commit"
    - "D-05: any mistake before a resolution commit lands triggers git merge --abort and restart from clean"
  artifacts:
    - path: CLAUDE.md
      provides: "Fork project instructions (auto-survives; verified present)"
    - path: CATALOGUE.json
      provides: "Fork catalogue (auto-survives; verified present)"
  key_links:
    - from: "git merge --no-ff -s ort 1bb253c9"
      to: "MERGE_HEAD"
      via: "in-progress merge state established once, shared by all six plans"
      pattern: "MERGE_HEAD"
---

<objective>
Start the single stateful merge of upstream v1.3.1 (1bb253c9) and resolve Tier 1 of the locked triage order: `.planning/` + `CLAUDE.md` + root `CONTEXT.md` (resolve OURS). Verify the PATCH-03 fork-only files that upstream never had auto-survive.

Purpose: Establish the in-progress merge (MERGE_HEAD) that all subsequent plans (69-02..69-06) operate on, and land the first triage tier as per-file commits so the merge becomes multi-commit (MERGE-03), never a mega-commit.

Output: An in-progress `-s ort` merge of 1bb253c9; Tier 1 files resolved+committed; PATCH-03 presence verified.

CRITICAL: This is ONE stateful git merge across six sequential plans. This plan opens the merge; it does NOT land the final merge commit. MERGE_HEAD must remain set when this plan ends so 69-02 can continue. Do NOT run `git commit` of the merge itself, `git merge --continue`, or `git merge --abort` (except per D-05 recovery) at the end of this plan.
</objective>

<execution_context>
This plan runs in the live working tree on branch `dev`. Stay on `dev` — do NOT create, switch, or rename branches. The recovery anchor `pre-merge-v1.3.1-backup` already exists.
</execution_context>

<merge_commit_mechanics>
A standard merge produces ONE merge commit that clears MERGE_HEAD. To satisfy MERGE-03 ("multiple incremental commits in triage order") while keeping the merge's second parent == 1bb253c9, this phase uses this mechanic across all six plans:
1. Plan 69-01 starts `git merge --no-ff --no-commit -s ort 1bb253c9` (the `--no-commit` keeps MERGE_HEAD set so per-file commits can land WHILE the merge is in progress).
2. Plans 69-01..69-06 each `git add`/`git rm` resolved files for their tier and run per-file `git commit` calls. Because MERGE_HEAD is still set, these commits are recorded as part of the in-progress merge and the FINAL commit that clears MERGE_HEAD carries 1bb253c9 as its second parent.
3. The final tier plan (69-06) runs the LAST commit that clears MERGE_HEAD, establishing the merge parent. All earlier per-file commits remain in the merge range.

Net result: `git log --merges -1 --format=%P` 2nd parent == 1bb253c9 AND `git log` over the merge range shows >1 resolution commit.
</merge_commit_mechanics>

<tasks>

<task type="auto">
  <name>Task 1: Start the in-progress -s ort merge of 1bb253c9</name>
  <files>(git index / MERGE_HEAD — no tracked file content yet)</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (conflict set, merge mechanics, delete/modify resolution table)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (locked decisions D-01..D-05)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-VALIDATION.md (structural phase gate)
  </read_first>
  <action>
    Confirm clean starting state: `git status --porcelain` empty and `git rev-parse --abbrev-ref HEAD` == `dev`. Confirm the recovery anchor exists: `git branch --list pre-merge-v1.3.1-backup` non-empty.
    Start the merge WITHOUT committing so MERGE_HEAD stays set for per-file commits across all six plans (per D-03 and merge_commit_mechanics): `git merge --no-ff --no-commit -s ort 1bb253c9`. The command exits non-zero because conflicts stop it — that is expected, NOT a failure. Do NOT pass `--allow-unrelated-histories` and do NOT pre-rename any directory (MERGE-02).
    Confirm MERGE_HEAD now points at 1bb253c9: `git rev-parse MERGE_HEAD` == `1bb253c9...`.
    Per D-05: if at any point before a resolution commit lands you detect a wrong-strategy or unexpected state, run `git merge --abort` and restart this task from clean rather than untangling.
  </action>
  <acceptance_criteria>
    - `git rev-parse MERGE_HEAD` resolves and its value begins with `1bb253c9`
    - `git rev-parse --abbrev-ref HEAD` == `dev` (no branch switch occurred)
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)' | head -1` is non-empty (conflicts present, confirming the merge actually stopped mid-flight)
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse MERGE_HEAD | command grep -q '^1bb253c9'</automated>
  </verify>
  <done>The `-s ort` merge of 1bb253c9 is in progress (MERGE_HEAD set), on branch dev, with conflicts present and uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Tier 1 — root CONTEXT.md (OURS), per-file commit</name>
  <files>CONTEXT.md</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 1 table: root CONTEXT.md resolves OURS)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    Resolve the Tier-1 `UU` conflict on root `CONTEXT.md` in the fork's favor (per the locked triage order, Tier 1 = `.planning/`+`CLAUDE.md` resolved OURS): `git checkout --ours CONTEXT.md && git add CONTEXT.md`.
    Per D-03 (per-file commits everywhere), commit this single file on its own while MERGE_HEAD is still set: `git commit -m "merge(69-01): resolve CONTEXT.md (ours) [Tier 1]"`. Because MERGE_HEAD is set, this commit lands as part of the in-progress merge (see merge_commit_mechanics). Do NOT run `git merge --continue` or a bare `git commit` that would clear MERGE_HEAD — that is plan 69-06's job.
    If root `CONTEXT.md` shows no `UU` (e.g. not in conflict on this tree), skip the checkout and record that in the deviation notes; do not invent a conflict.
  </action>
  <acceptance_criteria>
    - `git status --porcelain CONTEXT.md` shows no `UU`/`UD`/`AA` code for CONTEXT.md
    - `git rev-parse MERGE_HEAD` still resolves (merge still in progress — this plan does not close it)
    - `git log --oneline -1 -- CONTEXT.md | command grep -q 'Tier 1'` (the per-file resolution commit landed)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain CONTEXT.md | command grep -E '^(UU|UD|DU|AA|AU)')" && git rev-parse MERGE_HEAD >/dev/null 2>&1</automated>
  </verify>
  <done>Root CONTEXT.md is resolved OURS and committed as its own per-file commit; merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 3: Verify PATCH-03 fork-only files auto-survive (CLAUDE.md, CATALOGUE.json, .planning/)</name>
  <files>CLAUDE.md, CATALOGUE.json, .planning/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Pitfall 2: PATCH-03 files are NOT conflicts — absent from upstream, verify via ls + non-empty)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-VALIDATION.md (Fork-only files restored check)
  </read_first>
  <action>
    Per PATCH-03 and Pitfall 2: `CLAUDE.md`, `CATALOGUE.json`, and `.planning/` are absent from the upstream tree, so they carry NO delete/modify conflict and auto-survive. Do NOT search for a conflict to resolve. Verify presence + non-empty content only:
    `ls CLAUDE.md CATALOGUE.json .planning/` succeeds; `test -s CLAUDE.md` and `test -s CATALOGUE.json` (non-empty); `.planning/` directory is non-empty.
    If any is unexpectedly missing or empty, STOP and escalate (this would indicate a merge-state corruption) — per D-05, prefer `git merge --abort` + restart over patching.
  </action>
  <acceptance_criteria>
    - `ls CLAUDE.md CATALOGUE.json .planning/` exits 0
    - `test -s CLAUDE.md && test -s CATALOGUE.json` exits 0 (both non-empty)
    - `test -n "$(ls -A .planning/)"` exits 0 (.planning/ non-empty)
  </acceptance_criteria>
  <verify>
    <automated>ls CLAUDE.md CATALOGUE.json .planning/ >/dev/null && test -s CLAUDE.md && test -s CATALOGUE.json && test -n "$(ls -A .planning/)"</automated>
  </verify>
  <done>PATCH-03 fork-only files confirmed present and populated; no conflict resolution was needed.</done>
</task>

</tasks>

<verification>
- `git rev-parse MERGE_HEAD` == 1bb253c9 (merge in progress, not yet closed)
- `git status --porcelain CONTEXT.md` has no conflict code for CONTEXT.md
- `ls CLAUDE.md CATALOGUE.json .planning/` all present and non-empty
- A per-file Tier-1 resolution commit exists in the in-progress merge
</verification>

<success_criteria>
The v1.3.1 merge is in progress on dev (MERGE_HEAD == 1bb253c9), Tier 1 (CONTEXT.md ours) is resolved and committed per-file, and PATCH-03 fork-only files are verified present — ready for Tier 2 (69-02).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-01-SUMMARY.md` when done. The merge MUST remain in progress (MERGE_HEAD set) for 69-02.
</output>
