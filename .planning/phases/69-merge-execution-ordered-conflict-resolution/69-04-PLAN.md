---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 04
type: execute
wave: 4
depends_on: ["69-03"]
files_modified:
  - agents/
  - commands/gsd/
  - get-shit-done/workflows/
  - get-shit-done/references/
  - get-shit-done/templates/
  - docs/
  - README.md
  - README.ja-JP.md
  - README.ko-KR.md
  - README.pt-BR.md
  - README.zh-CN.md
  - CONTRIBUTING.md
autonomous: true
requirements: [MERGE-03, D-01, D-02]
must_haves:
  truths:
    - "D-01: every prompt-content UU (agents/, commands/gsd/, get-shit-done/workflows|references|templates/) is HAND-merged per-file — never bulk -X theirs"
    - "D-02: hand-merge integrates upstream functional changes while preserving all fork patches; NO positive-framing/quality-bar conformance applied (deferred milestone)"
    - "D-03: every resolved prompt-content file lands as its own per-file commit"
    - "Fork agent frontmatter is preserved exactly (no skills: key introduced; Write-tool agents keep their guardrail string)"
    - "Incoming gsd-core/ tree is NOT resolved here — it is accepted additively in Tier 6 (69-06)"
  artifacts:
    - path: agents/gsd-executor.md
      provides: "Hand-merged fork executor agent with upstream functional changes integrated"
  key_links:
    - from: "agents/, commands/gsd/, get-shit-done/{workflows,references,templates}/ UU files"
      to: "fork file with upstream changes integrated"
      via: "per-file hand-merge (D-01), per-file commit (D-03)"
      pattern: "git commit -m \"merge(69-04)"
---

<objective>
Resolve Tier 4 (prompt content) of the in-progress merge: hand-merge every `UU` fork prompt-content file — `agents/gsd-*.md` (11), `commands/gsd/*.md` (~67), `get-shit-done/workflows|references|templates/*.md`, `docs/**`, the 5 `README*.md`, and `CONTRIBUTING.md` — integrating upstream's functional changes while preserving all fork patches (D-01/D-02). Per-file commits (D-03). Do NOT apply the fork's positive-framing/quality-bar conformance pass (deferred milestone). Do NOT resolve the incoming `gsd-core/` tree here — it is accepted additively in Tier 6 (69-06).

Purpose: This is the largest tier (~150+ per-file commits). Each fork prompt file gets upstream's functional changes folded in by hand without losing fork patches and without pre-adopting the rename or running conformance.

Output: All Tier-4 fork prompt-content conflicts hand-merged + committed per-file; merge still in progress.

CRITICAL: Merge still in progress (MERGE_HEAD set). Do NOT close it. NEVER pass `-X theirs` (D-01, Pitfall 4). Agent frontmatter must be preserved exactly — `agent-frontmatter.test.cjs` validates all agents; do NOT introduce a `skills:` key (breaks Gemini CLI) and keep the `Only use the Write tool` guardrail string in Write-tool agents. This is a large tier — the executor may need to work through it in batches across multiple invocations, but every file is a separate commit.
</objective>

<execution_context>
On branch `dev`, merge in progress. Stay on `dev`. The full per-file `UU` set is enumerated authoritatively at resolution time via `git status --porcelain | command grep '^UU '` filtered to the fork prompt-content paths below. The incoming `gsd-core/**` UU/AU entries are explicitly OUT of scope here (Tier 6 / 69-06).
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-merge fork agents/ and commands/gsd/ prompt content (per-file, D-01/D-02/D-03)</name>
  <files>agents/, commands/gsd/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 4 table; Pitfall 4 no -X theirs; Open Question on ~67 commands/gsd UU)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-01 hand-merge, D-02 preserve fork patches / no conformance, D-03 per-file commits)
    - CLAUDE.md (agent-frontmatter rules: no skills:, Write-tool guardrail string, valid-agent list)
  </read_first>
  <action>
    Enumerate the fork agent/command `UU` set: `git status --porcelain | command grep -E '^UU (agents/|commands/gsd/)'`. For EACH file, hand-merge per D-01/D-02 — integrate upstream's functional changes into the fork file while preserving every fork patch; do NOT run positive-framing/quality-bar conformance (deferred). Preserve agent frontmatter exactly: no `skills:` key, keep the `Only use the Write tool` guardrail in Write-tool agents, keep the fork's valid-agent membership. Resolve markers by hand, `git add <file>`, commit per-file (D-03): `git commit -m "merge(69-04): hand-merge <file> [Tier 4]"`. NEVER `-X theirs` (Pitfall 4). The 11 headline agents are: debugger, debug-session-manager, doc-writer, executor, intel-updater, phase-researcher, plan-checker, planner, research-synthesizer, user-profiler, verifier.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep -E '^UU (agents/|commands/gsd/)'` is empty (all resolved)
    - No conflict markers remain in agents/ or commands/gsd/ (grep for `<<<<<<<`/`>>>>>>>` empty)
    - No agent file contains a `skills:` frontmatter key
    - Each resolved file landed as its own commit tagged `[Tier 4]`
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^UU (agents/|commands/gsd/)')" && test -z "$(command grep -rlE '^(<<<<<<<|>>>>>>>)' agents/ commands/gsd/ 2>/dev/null)" && test -z "$(command grep -rl '^skills:' agents/ 2>/dev/null)"</automated>
  </verify>
  <done>All fork agents/ and commands/gsd/ UU conflicts hand-merged preserving fork patches and frontmatter, committed per-file; merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-merge fork get-shit-done/workflows, references, templates (per-file)</name>
  <files>get-shit-done/workflows/, get-shit-done/references/, get-shit-done/templates/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 4 table: get-shit-done/ UU paired with gsd-core/ rename target; accept gsd-core/ as Tier 6)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-01/D-02/D-03; MERGE-02 rename NOT adopted)
  </read_first>
  <action>
    Enumerate: `git status --porcelain | command grep -E '^UU get-shit-done/(workflows|references|templates)/'`. Hand-merge each fork file per D-01/D-02 — integrate upstream functional changes, preserve fork patches, no conformance pass. Keep the file at its `get-shit-done/` path (rename to gsd-core/ is Phase 71 — do NOT move it). The parallel incoming `gsd-core/workflows|references|templates/**` entries are Tier 6 additions (69-06) — do NOT touch them here. `git add` + per-file commit (D-03): `git commit -m "merge(69-04): hand-merge <file> [Tier 4]"`. NEVER `-X theirs`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep -E '^UU get-shit-done/(workflows|references|templates)/'` is empty
    - No conflict markers remain in get-shit-done/workflows|references|templates
    - Files remain at their `get-shit-done/` paths (no gsd-core/ move performed here)
    - `git rev-parse MERGE_HEAD` still resolves (merge still in progress)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^UU get-shit-done/(workflows|references|templates)/')" && ls get-shit-done/workflows >/dev/null 2>&1 && git rev-parse MERGE_HEAD >/dev/null 2>&1</automated>
  </verify>
  <done>All fork get-shit-done/ workflows/references/templates UU conflicts hand-merged at their original paths, committed per-file; merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-merge docs/, README*.md, CONTRIBUTING.md (per-file)</name>
  <files>docs/, README.md, README.ja-JP.md, README.ko-KR.md, README.pt-BR.md, README.zh-CN.md, CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 4 table: docs/**, 5 READMEs, CONTRIBUTING.md UU/AA)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    Enumerate remaining Tier-4 doc conflicts: `git status --porcelain | command grep -E '^(UU|AA) (docs/|README|CONTRIBUTING.md)'`. Hand-merge each (UU content conflicts and AA add/adds) integrating upstream changes while preserving fork-specific doc content; no conformance pass. `git add` + per-file commit (D-03): `git commit -m "merge(69-04): resolve <file> [Tier 4]"`. Leave any `gsd-core/`-pathed doc references to be handled by the Phase 71 rename sweep — do NOT pre-rename here.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep -E '^(UU|AA) (docs/|README|CONTRIBUTING.md)'` is empty
    - No conflict markers remain in docs/, READMEs, or CONTRIBUTING.md
    - Each file committed per-file tagged `[Tier 4]`
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^(UU|AA) (docs/|README|CONTRIBUTING.md)')" && test -z "$(command grep -lE '^(<<<<<<<|>>>>>>>)' README*.md CONTRIBUTING.md 2>/dev/null)"</automated>
  </verify>
  <done>All Tier-4 docs/README/CONTRIBUTING conflicts hand-merged and committed per-file; merge still in progress.</done>
</task>

</tasks>

<verification>
- No `UU`/`AA` remain for agents/, commands/gsd/, get-shit-done/{workflows,references,templates}/, docs/, READMEs, CONTRIBUTING.md
- No agent gained a `skills:` key; Write-tool guardrails preserved
- Files stay at `get-shit-done/` paths (rename deferred to Phase 71)
- `git rev-parse MERGE_HEAD` == 1bb253c9 (merge still open)
</verification>

<success_criteria>
All Tier-4 fork prompt-content conflicts are hand-merged per-file preserving fork patches and frontmatter (D-01/D-02), no conformance pass applied, every file committed per-file (D-03), gsd-core/ tree untouched — merge still in progress, ready for Tier 5 (69-05).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-04-SUMMARY.md` when done. The merge MUST remain in progress (MERGE_HEAD set) for 69-05.
</output>
