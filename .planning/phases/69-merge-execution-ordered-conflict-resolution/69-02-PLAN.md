---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - bin/install.js
  - package.json
  - package-lock.json
  - hooks/gsd-check-update-worker.js
  - hooks/gsd-statusline.js
  - .gitignore
  - .github/workflows/release.yml
  - .github/workflows/test.yml
  - .github/workflows/pr-target-validator.yml
  - scripts/run-tests.cjs
  - scripts/changeset/cli.cjs
  - scripts/setup-branch-protection.sh
  - scripts/lint-test-file-count.allowlist.json
  - rollout-next-phase1.sh
  - rollout-next-phase2.sh
autonomous: true
requirements: [MERGE-03, MERGE-04, D-04]
must_haves:
  truths:
    - "D-08: Step B — these are ORDINARY single-parent follow-up commits AFTER the merge commit (MERGE_HEAD already cleared by 69-01); none assert MERGE_HEAD is set"
    - "Tier 2 infrastructure upstream functional changes are integrated INTO the fork files preserving fork patches (D-01/D-02)"
    - "D-04: package.json keeps fork-sacred fields name=get-shit-done-cc, fork bin map, fork repository.url, fork version 1.1.0; takes upstream base otherwise"
    - "MERGE-04: package-lock.json regenerated cleanly — npm install exits 0 and second npm install produces zero lockfile churn"
    - "D-03: every Tier 2 file lands as its own per-file commit"
  artifacts:
    - path: package.json
      provides: "Reconciled upstream base + 4 sacred fork fields"
      contains: "get-shit-done-cc"
    - path: package-lock.json
      provides: "Cleanly regenerated lockfile, idempotent on second install"
  key_links:
    - from: package.json
      to: package-lock.json
      via: "npm install ×2 after package.json finalized"
      pattern: "npm install"
---

<objective>
Execute **Step B / Tier 2**: integrate upstream's functional infrastructure changes into the fork files as ORDINARY per-file follow-up commits AFTER the 69-01 merge commit. Fold upstream's functional changes into `bin/install.js`, hooks, `.gitignore`, CI workflows, and scripts while preserving fork patches (D-01/D-02); reconcile `package.json` to upstream base + 4 sacred fork fields (D-04); regenerate the lockfile cleanly (MERGE-04). Per-file commits (D-03).

Purpose: 69-01 landed the merge with the fork-side infrastructure files unchanged (mechanical OURS resolution). Now bring upstream's functional infra changes in, one file at a time, so the build/install/CI tier reflects upstream's improvements without losing fork patches. package.json must be finalized here so the lockfile regen is clean and stable.

Output: Tier 2 fork files updated with upstream functional changes via per-file follow-up commits; lockfile clean-regen verified.

CRITICAL: The merge is ALREADY CLOSED (69-01 cleared MERGE_HEAD). These are ordinary single-parent commits — do NOT assert MERGE_HEAD is set and do NOT run `git merge --continue`. To see what upstream changed, diff upstream against the merge-base: `git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- <path>`, then apply those functional changes to the fork file by hand. Phase 70 owns the full install.js/worker fork-patch re-apply and TypeScript port — THIS plan only folds in upstream's functional deltas, it does not re-implement fork patches.
</objective>

<execution_context>
On branch `dev`, merge already committed by 69-01 (MERGE_HEAD cleared). Stay on `dev`. The current fork files hold the fork side (OURS) from the merge commit; this plan edits them to integrate upstream functional changes.

Fork-sacred package.json values (read live from the merge commit / HEAD at integration time — current values):
- name: `get-shit-done-cc`
- version: `1.1.0`
- repository.url: `git+https://github.com/open-gsd/get-shit-done-redux.git`
- bin: `get-shit-done-redux`->`bin/install.js`, `gsd-sdk`->`bin/gsd-sdk.js`, `gsd-tools`->`bin/gsd-sdk.js`
Re-read these from `git show HEAD:package.json` in case HEAD moved; do NOT repoint coverage globs or bin paths to gsd-core/ (that is Phase 71).

To diff upstream's functional change for any file: `git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- <path>`.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile package.json (D-04 sacred fields) and regenerate lockfile cleanly (MERGE-04)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (package.json/package-lock.json reconciliation; Pitfall 3 lockfile churn; Assumption A1 read HEAD values)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-04 sacred fields)
    - package.json (current fork file from the merge commit)
  </read_first>
  <action>
    Reconcile `package.json` as an ordinary follow-up commit (D-08 — MERGE_HEAD already cleared). Start from upstream's content (`git show 1bb253c9:package.json`) and preserve the 4 fork-sacred fields per D-04, reading live fork values from `git show HEAD:package.json`: `name` (get-shit-done-cc), fork `bin` map (get-shit-done-redux->bin/install.js, gsd-sdk->bin/gsd-sdk.js, gsd-tools->bin/gsd-sdk.js), `repository.url` (fork URL), and `version` (1.1.0). Take upstream's genuinely-new dependencies/scripts/engines UNLESS they break the fork. Per D-04 — do NOT repoint c8 coverage `--include` globs or bin paths to `gsd-core/` (fork tools live at `get-shit-done/bin/` until Phase 71). `git add package.json` and commit per-file: `git commit -m "merge(69-02): reconcile package.json — upstream base + sacred fork fields [Tier 2]"`.
    Then regenerate the lockfile cleanly (mechanic is Claude's discretion per D-04): delete or take one side of `package-lock.json`, run `npm install` (must exit 0), run `npm install` a SECOND time and confirm zero churn (`git diff --quiet package-lock.json`). `git add package-lock.json` and commit per-file: `git commit -m "merge(69-02): regenerate package-lock.json clean [Tier 2]"`.
  </action>
  <acceptance_criteria>
    - `command grep -q '"name": "get-shit-done-cc"' package.json`
    - `command grep -q 'get-shit-done-redux' package.json` AND `command grep -q '"version": "1.1.0"' package.json`
    - `command grep -q 'get-shit-done-redux.git' package.json` (fork repository.url preserved)
    - `npm install` exits 0; a second `npm install` leaves `git diff --quiet package-lock.json` (no churn)
  </acceptance_criteria>
  <verify>
    <automated>command grep -q '"name": "get-shit-done-cc"' package.json && command grep -q '"version": "1.1.0"' package.json && npm install >/dev/null 2>&1 && npm install >/dev/null 2>&1 && git diff --quiet package-lock.json</automated>
  </verify>
  <done>package.json reconciled to upstream base with 4 sacred fork fields preserved; lockfile regenerated and idempotent on second install; both committed per-file as ordinary follow-up commits.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate upstream functional changes into bin/install.js and hooks, preserving fork patches</name>
  <files>bin/install.js, hooks/gsd-check-update-worker.js, hooks/gsd-statusline.js</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 2 table; PATCH-02 KEEP fork SHA worker note)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-01/D-02 integrate upstream, preserve fork patches, no conformance pass)
    - bin/install.js (current fork file from the merge commit)
    - hooks/gsd-check-update-worker.js (current fork file)
  </read_first>
  <action>
    For each file, diff upstream's functional change against the merge-base (`git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- <path>`) and apply those functional changes INTO the fork file per D-01/D-02 — preserve all fork patches; do NOT apply positive-framing/quality-bar conformance (deferred milestone). For `hooks/gsd-check-update-worker.js`: KEEP the fork's SHA-based update-check worker (PATCH-02) — fold in only upstream functional deltas that do not regress the fork SHA logic (full re-apply is Phase 70). For `bin/install.js`: keep fork patches; integrate upstream's functional changes (full port is Phase 70). `git add <file>` and commit each file on its own (D-03), e.g. `git commit -m "merge(69-02): integrate upstream into bin/install.js (fork patches preserved) [Tier 2]"`, repeat per file. These are ordinary commits — do NOT reference MERGE_HEAD.
  </action>
  <acceptance_criteria>
    - No conflict markers remain in the three files (grep for `<<<<<<<`/`=======`/`>>>>>>>` is empty)
    - `command grep -q` confirms the fork SHA worker logic still present in hooks/gsd-check-update-worker.js (PATCH-02 preserved)
    - Each file landed as its own commit tagged `[Tier 2]`
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(command grep -lE '^(<<<<<<<|>>>>>>>)' bin/install.js hooks/gsd-check-update-worker.js hooks/gsd-statusline.js 2>/dev/null)"</automated>
  </verify>
  <done>bin/install.js and both hooks integrated with upstream functional changes, fork patches preserved (PATCH-02 SHA worker kept), each committed per-file.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate upstream changes into .gitignore, CI workflows, scripts, rollout scripts (per-file)</name>
  <files>.gitignore, .github/workflows/release.yml, .github/workflows/test.yml, .github/workflows/pr-target-validator.yml, scripts/run-tests.cjs, scripts/changeset/cli.cjs, scripts/setup-branch-protection.sh, scripts/lint-test-file-count.allowlist.json, rollout-next-phase1.sh, rollout-next-phase2.sh</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 2 table: UU/AA infra files)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    For each Tier-2 infra file that upstream changed, diff upstream's functional delta against the merge-base (`git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- <path>`) and apply it into the fork file, keeping fork behavior where they diverge. Do NOT repoint any path to `gsd-core/` (Phase 71). `git add` then commit each file on its own (D-03): `git commit -m "merge(69-02): integrate upstream into <file> [Tier 2]"`. If upstream made no functional change to a listed file (fork side already correct), record that in deviations and skip — do not fabricate a change. Ordinary commits — no MERGE_HEAD reference.
  </action>
  <acceptance_criteria>
    - No conflict markers remain in any Tier-2 file
    - Files that upstream changed have a corresponding `[Tier 2]` per-file commit; skipped files are noted in deviations
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)'` is empty (no unmerged paths — merge stays closed)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU)')" && test -z "$(command grep -lE '^(<<<<<<<|>>>>>>>)' .gitignore rollout-next-phase1.sh rollout-next-phase2.sh 2>/dev/null)"</automated>
  </verify>
  <done>All Tier-2 infrastructure files integrated with upstream functional changes and committed per-file as ordinary follow-up commits.</done>
</task>

</tasks>

<verification>
- package.json: fork name/version/repo.url/bin map preserved; upstream base otherwise; no `gsd-core/` repointing
- `npm install` ×2 -> exit 0, no lockfile churn on 2nd run (MERGE-04)
- bin/install.js + hooks fork patches survive; no conflict markers in Tier-2 files
- Merge stays closed (no MERGE_HEAD, no unmerged paths) — all commits ordinary single-parent (D-08)
</verification>

<success_criteria>
All Tier-2 infrastructure upstream functional changes are integrated into the fork files preserving fork patches (D-01/D-02), package.json reconciled per D-04, lockfile cleanly regenerated per MERGE-04, every file committed per-file (D-03) as ordinary follow-up commits — ready for Tier 3 (69-03).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-02-SUMMARY.md` when done. The merge is already closed; these are ordinary follow-up commits.
</output>
</content>
