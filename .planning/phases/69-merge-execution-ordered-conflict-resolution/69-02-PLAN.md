---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - bin/install.js
  - package.json
  - package-lock.json
  - hooks/gsd-check-update-worker.js
  - hooks/gsd-statusline.js
  - .gitignore
  - .github/workflows/release.yml
  - .github/workflows/test.yml
  - .github/workflows/pr-target-validator.yml
  - scripts/run-tests.cjs
  - scripts/changeset/cli.cjs
  - scripts/setup-branch-protection.sh
  - scripts/lint-test-file-count.allowlist.json
  - rollout-next-phase1.sh
  - rollout-next-phase2.sh
autonomous: true
requirements: [MERGE-03, MERGE-04, D-04]
must_haves:
  truths:
    - "Tier 2 infrastructure conflicts are hand-merged preserving fork patches (D-02)"
    - "D-04: package.json keeps fork-sacred fields name=get-shit-done-cc, fork bin map, fork repository.url, fork version 1.1.0; takes upstream base otherwise"
    - "MERGE-04: package-lock.json regenerated cleanly — npm install exits 0 and second npm install produces zero lockfile churn"
    - "D-03: every Tier 2 file lands as its own per-file commit"
    - "bin/install.js and hooks fork patches survive (full re-apply/port is Phase 70 — here just keep them surviving)"
  artifacts:
    - path: package.json
      provides: "Reconciled upstream base + 4 sacred fork fields"
      contains: "get-shit-done-cc"
    - path: package-lock.json
      provides: "Cleanly regenerated lockfile, idempotent on second install"
  key_links:
    - from: package.json
      to: package-lock.json
      via: "npm install ×2 after package.json finalized"
      pattern: "npm install"
---

<objective>
Resolve Tier 2 of the triage order: infrastructure files — `bin/install.js`, `package.json`, `package-lock.json`, hooks, `.gitignore`, CI workflows, and scripts. Hand-merge each preserving fork patches (D-02), reconcile `package.json` to upstream base + 4 sacred fork fields (D-04), and regenerate the lockfile cleanly (MERGE-04). Per-file commits throughout (D-03).

Purpose: Land the build/install/CI infrastructure tier of the in-progress merge before fork-critical modules and prompt content. package.json must be finalized here so the lockfile regen is clean and stable.

Output: Tier 2 files resolved + per-file committed within the still-in-progress merge; lockfile clean-regen verified.

CRITICAL: The merge from 69-01 is still in progress (MERGE_HEAD set). Do NOT close it here (no `git merge --continue`, no bare merge commit). Per-file commits land while MERGE_HEAD is set. Phase 70 owns the full install.js/worker fork-patch re-apply and TypeScript port — THIS plan only keeps existing fork patches surviving the three-way merge, it does not re-implement them.
</objective>

<execution_context>
On branch `dev`, merge in progress from 69-01. Stay on `dev`. Per D-05, abort+restart only applies before a resolution commit lands; once Tier 1 commits exist, prefer fixing forward within the merge.

Fork-sacred package.json values (read live from HEAD at merge time — current values):
- name: `get-shit-done-cc`
- version: `1.1.0`
- repository.url: `git+https://github.com/open-gsd/get-shit-done-redux.git`
- bin: `get-shit-done-redux`->`bin/install.js`, `gsd-sdk`->`bin/gsd-sdk.js`, `gsd-tools`->`bin/gsd-sdk.js`
The executor MUST re-read these from `git show HEAD:package.json` (ours stage) at resolution time in case HEAD moved; do NOT repoint coverage globs or bin paths to gsd-core/ (that is Phase 71).
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile package.json (D-04 sacred fields) and regenerate lockfile cleanly (MERGE-04)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (package.json/package-lock.json reconciliation section; Pitfall 3 lockfile churn; Assumption A1 read HEAD values)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-04 sacred fields)
    - package.json (current fork file — ours stage)
  </read_first>
  <action>
    Resolve the `package.json` `UU`. Start from upstream base (`git show MERGE_HEAD:package.json`) and preserve the 4 fork-sacred fields per D-04, reading the live fork values from `git show HEAD:package.json`: `name` (get-shit-done-cc), the fork `bin` map (get-shit-done-redux->bin/install.js, gsd-sdk->bin/gsd-sdk.js, gsd-tools->bin/gsd-sdk.js), `repository.url` (fork URL git+https://github.com/open-gsd/get-shit-done-redux.git), and `version` (1.1.0). Take upstream's genuinely-new dependencies/scripts/engines UNLESS they break the fork. Per D-04 — do NOT repoint c8 coverage `--include` globs or bin paths to `gsd-core/`; the fork tools still live at `get-shit-done/bin/` until Phase 71. `git add package.json` and commit per-file: `git commit -m "merge(69-02): reconcile package.json — upstream base + sacred fork fields [Tier 2]"`.
    Then regenerate the lockfile cleanly (Claude's discretion on mechanic, per D-04): remove the conflicted `package-lock.json` (e.g. take one side or delete), run `npm install` (must exit 0), then run `npm install` a SECOND time and confirm zero churn (`git diff --quiet package-lock.json` after the 2nd run). Note `sdk/package.json`/`sdk/package-lock.json` are `UD` (upstream deleted sdk/) — those deletions are accepted in 69-03; no workspace lockfile to reconcile. `git add package-lock.json` and commit per-file: `git commit -m "merge(69-02): regenerate package-lock.json clean [Tier 2]"`.
  </action>
  <acceptance_criteria>
    - `command grep -q '"name": "get-shit-done-cc"' package.json`
    - `command grep -q 'get-shit-done-redux' package.json` AND `command grep -q '"version": "1.1.0"' package.json`
    - `command grep -q 'get-shit-done-redux.git' package.json` (fork repository.url preserved)
    - `npm install` exits 0; an immediately-following second `npm install` leaves `git diff --quiet package-lock.json` (no churn)
    - `git status --porcelain package.json package-lock.json` shows no `UU`
  </acceptance_criteria>
  <verify>
    <automated>command grep -q '"name": "get-shit-done-cc"' package.json && command grep -q '"version": "1.1.0"' package.json && npm install >/dev/null 2>&1 && npm install >/dev/null 2>&1 && git diff --quiet package-lock.json</automated>
  </verify>
  <done>package.json reconciled to upstream base with 4 sacred fork fields preserved; lockfile regenerated and idempotent on second install; both committed per-file. Merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-merge bin/install.js and hooks, preserving fork patches</name>
  <files>bin/install.js, hooks/gsd-check-update-worker.js, hooks/gsd-statusline.js</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 2 table; PATCH-02 KEEP fork SHA worker note)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-01/D-02 hand-merge, preserve fork patches, no conformance pass)
    - bin/install.js (ours stage)
    - hooks/gsd-check-update-worker.js (ours stage)
  </read_first>
  <action>
    Hand-merge each `UU` per D-01/D-02 — integrate upstream functional changes while preserving all fork patches; do NOT apply positive-framing/quality-bar conformance (deferred milestone). For `hooks/gsd-check-update-worker.js`: KEEP the fork's SHA-based update-check worker (PATCH-02 decision) — the full re-apply is Phase 70, so here just ensure the fork SHA logic survives rather than being overwritten by upstream's semver approach. For `bin/install.js`: keep fork patches surviving (full port is Phase 70). Resolve markers by hand, `git add <file>`, and commit each file on its own (D-03), e.g. `git commit -m "merge(69-02): hand-merge bin/install.js (fork patches preserved) [Tier 2]"`, repeat per file.
  </action>
  <acceptance_criteria>
    - `git status --porcelain bin/install.js hooks/gsd-check-update-worker.js hooks/gsd-statusline.js` shows no `UU`/`AA` codes
    - No conflict markers remain in those three files (grep for `<<<<<<<`/`=======`/`>>>>>>>` is empty)
    - Each file landed as its own commit (per-file commit messages tagged `[Tier 2]`)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain bin/install.js hooks/gsd-check-update-worker.js hooks/gsd-statusline.js | command grep -E '^(UU|AA|AU)')" && test -z "$(command grep -lE '^(<<<<<<<|>>>>>>>)' bin/install.js hooks/gsd-check-update-worker.js hooks/gsd-statusline.js 2>/dev/null)"</automated>
  </verify>
  <done>bin/install.js and both hooks hand-merged with fork patches preserved (PATCH-02 SHA worker kept), each committed per-file. Merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-merge/resolve .gitignore, CI workflows, scripts, and rollout add/adds</name>
  <files>.gitignore, .github/workflows/release.yml, .github/workflows/test.yml, .github/workflows/pr-target-validator.yml, scripts/run-tests.cjs, scripts/changeset/cli.cjs, scripts/setup-branch-protection.sh, scripts/lint-test-file-count.allowlist.json, rollout-next-phase1.sh, rollout-next-phase2.sh</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 2 table: UU/AA infra files; delete/modify resolution table)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    Resolve each remaining Tier-2 conflict per its class (per the RESEARCH delete/modify table): `UU` content conflicts -> hand-merge markers; `AA` add/add (`.github/workflows/*`, `scripts/*`, `rollout-next-phase*.sh`) -> hand-merge the two added versions, keeping fork behavior where they diverge. Do NOT repoint any path to `gsd-core/` (Phase 71). `git add` then commit each file on its own (D-03): `git commit -m "merge(69-02): resolve <file> [Tier 2]"`. If a listed file shows no conflict on this tree, record that in deviations and skip — do not fabricate a conflict.
  </action>
  <acceptance_criteria>
    - `git status --porcelain .gitignore .github/ scripts/ rollout-next-phase1.sh rollout-next-phase2.sh` shows no `UU`/`UD`/`DU`/`AA`/`AU` codes
    - No conflict markers remain in any resolved Tier-2 file
    - `git rev-parse MERGE_HEAD` still resolves (merge remains in progress for 69-03)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain .gitignore .github/ scripts/ rollout-next-phase1.sh rollout-next-phase2.sh | command grep -E '^(UU|UD|DU|AA|AU)')" && git rev-parse MERGE_HEAD >/dev/null 2>&1</automated>
  </verify>
  <done>All Tier-2 infrastructure conflicts resolved and committed per-file; merge still in progress.</done>
</task>

</tasks>

<verification>
- package.json: fork name/version/repo.url/bin map preserved; upstream base otherwise; no `gsd-core/` repointing
- `npm install` ×2 -> exit 0, no lockfile churn on 2nd run (MERGE-04)
- bin/install.js + hooks fork patches survive; no conflict markers in Tier-2 files
- `git rev-parse MERGE_HEAD` == 1bb253c9 (merge still open)
</verification>

<success_criteria>
All Tier-2 infrastructure conflicts are hand-merged preserving fork patches, package.json reconciled per D-04, lockfile cleanly regenerated per MERGE-04, every file committed per-file (D-03) — merge still in progress, ready for Tier 3 (69-03).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-02-SUMMARY.md` when done. The merge MUST remain in progress (MERGE_HEAD set) for 69-03.
</output>
