---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 05
type: execute
wave: 5
depends_on: ["69-04"]
files_modified:
  - tests/
autonomous: true
requirements: [MERGE-03]
must_haves:
  truths:
    - "Tier 5 test UU content conflicts are hand-merged per-file"
    - "Fork-only tests upstream deleted (UD) that exercise fork-only bin/lib modules are RESTORED via --ours"
    - "DU (execute-phase-step-5-5-deviation-doc) resolved per-file by judgment (keep deletion or take upstream)"
    - "AA bug-* tests resolved by hand-merging both added versions"
    - "D-03: each resolved test file lands as its own per-file commit"
    - "Tests are ALLOWED to fail — verification is structural, never green-suite (VERIFY-02)"
  artifacts:
    - path: tests/lint-shared-module-handsync.test.cjs
      provides: "Restored fork test for fork-only bin/lib module (require-path repair is Phase 70)"
  key_links:
    - from: "tests/*.test.cjs UD (fork-only module tests)"
      to: "restored HEAD test files"
      via: "git checkout --ours + git add"
      pattern: "checkout --ours"
---

<objective>
Resolve Tier 5 (tests) of the in-progress merge: hand-merge the ~12 `UU` content-conflict test files, RESTORE the 9 fork-only `UD` tests that exercise fork-only `bin/lib` modules, decide the single `DU` (`execute-phase-step-5-5-deviation-doc`) per-file, and resolve the 4 `AA` `bug-*` add/adds. Per-file commits (D-03). Tests are allowed to fail — verification here is structural (no conflict markers), NOT a green suite.

Purpose: Land the test tier in triage order before accepting new upstream additions. Fork-only-module tests must survive (Phase 70 repairs their require paths); upstream's new tests arrive in Tier 6.

Output: All Tier-5 test conflicts resolved + committed per-file; merge still in progress.

CRITICAL: Merge still in progress (MERGE_HEAD set). Do NOT close it. Do NOT make "npm test green" an acceptance criterion (Pitfall 5 / VERIFY-02) — tests referencing renamed paths WILL fail and that is deferred backlog (Phase 70/71). Restored fork-only-module tests will fail until Phase 70 repairs require paths — that is expected and acceptable.
</objective>

<execution_context>
On branch `dev`, merge in progress. Stay on `dev`. Resolution mechanics (RESEARCH delete/modify table):
- `UU` test content conflict -> hand-merge markers, `git add`.
- `UD` fork-only-module test that should STAY -> `git checkout --ours <file>` + `git add` (restore HEAD).
- `DU` (deleted by us, upstream kept) -> per-file: `git rm` to keep the fork deletion OR `git checkout --theirs <file>` + `git add` to take upstream.
- `AA` -> hand-merge both added versions, `git add`.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-merge UU test content conflicts + resolve DU and AA test cases (per-file)</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 5 table; Assumption A2 on DU execute-phase-step-5-5; Pitfall 5 tests-may-fail)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    Enumerate test conflicts: `git status --porcelain | command grep -E '^(UU|DU|AA) tests/'`. For each `UU` content-conflict test (e.g. plan-review-convergence, semver-compare, ultraplan-phase, windows-test-parity-guard, workflow-size-budget, workspace, worktree-cleanup, worktree-safety): hand-merge markers and `git add`. For the `DU` `tests/execute-phase-step-5-5-deviation-doc.test.cjs` (Assumption A2): decide per-file — if the fork no longer needs the deleted test, `git rm` to keep the deletion; otherwise `git checkout --theirs` + `git add`. For the 4 `AA` `bug-170`/`bug-17`/`bug-224`/`bug-33`: hand-merge the two added versions and `git add`. Commit each test file on its own (D-03): `git commit -m "merge(69-05): resolve <file> [Tier 5]"`. Do NOT gate on tests passing (VERIFY-02).
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep -E '^(UU|DU|AA) tests/'` is empty
    - No conflict markers remain in any resolved tests/ file
    - Each resolved test landed as its own commit tagged `[Tier 5]`
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep -E '^(UU|DU|AA) tests/')" && test -z "$(command grep -rlE '^(<<<<<<<|>>>>>>>)' tests/ 2>/dev/null)"</automated>
  </verify>
  <done>All UU/DU/AA test conflicts resolved and committed per-file; merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 2: RESTORE fork-only UD tests for fork-only bin/lib modules (--ours)</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 5: 9 UD fork-only tests; Phase 70 repairs require paths)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (MERGE-02 rename not adopted; D-03)
  </read_first>
  <action>
    Enumerate fork-only-module test `UD`s: `git status --porcelain | command grep '^UD tests/'`. These (bug-3751-init-local-agents, cjs-sdk-bridge-integration, config-schema-sdk-parity, configuration-generator, gen-staleness-check, lint-shared-module-handsync, project-root-generator, state-document-generator, workstream-inventory-builder-generator) test fork-only `bin/lib` modules that were RESTORED in 69-03, so the tests must STAY: `git checkout --ours <file> && git add <file>`. Their require paths still point at `get-shit-done/bin/lib/` and will be repaired in Phase 70 — they may fail until then, which is acceptable (VERIFY-02). Commit each per-file (D-03): `git commit -m "merge(69-05): restore <file> (require-path repair in P70) [Tier 5]"`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | command grep '^UD tests/' | head -1` is empty (all fork-only test UDs resolved)
    - The restored fork-only-module test files exist on disk (e.g. `ls tests/lint-shared-module-handsync.test.cjs` succeeds)
    - Each restored test committed per-file tagged `[Tier 5]`
    - `git rev-parse MERGE_HEAD` still resolves (merge still in progress)
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain | command grep '^UD tests/')" && git rev-parse MERGE_HEAD >/dev/null 2>&1</automated>
  </verify>
  <done>All fork-only-module UD tests restored to HEAD content and committed per-file; merge still in progress.</done>
</task>

</tasks>

<verification>
- No `UU`/`UD`/`DU`/`AA` remain for tests/
- Restored fork-only-module tests present on disk (require-path repair deferred to Phase 70)
- No green-suite dependency anywhere (VERIFY-02)
- `git rev-parse MERGE_HEAD` == 1bb253c9 (merge still open)
</verification>

<success_criteria>
All Tier-5 test conflicts resolved in triage order with per-file commits (D-03), fork-only-module tests restored, tests permitted to fail — merge still in progress, ready for Tier 6 (69-06).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-05-SUMMARY.md` when done. The merge MUST remain in progress (MERGE_HEAD set) for 69-06.
</output>
