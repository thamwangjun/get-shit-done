---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 05
type: execute
wave: 7
depends_on: ["69-04c"]
files_modified:
  - tests/
autonomous: true
requirements: [MERGE-03]
must_haves:
  truths:
    - "D-08: ordinary single-parent per-file follow-up commits AFTER the closed merge (no MERGE_HEAD)"
    - "Tier 5 test files that had UU content conflicts get upstream's functional test changes integrated per-file (D-01/D-02)"
    - "Fork-only tests upstream deleted (UD) that exercise fork-only bin/lib modules were RESTORED OURS by the 69-01 merge commit — confirmed present; require-path repair is Phase 70"
    - "The DU (execute-phase-step-5-5-deviation-doc) and AA bug-* tests were mechanically resolved in the merge commit; any upstream functional content folded in here per-file if needed"
    - "D-03: each integrated test file lands as its own per-file commit"
    - "Tests are ALLOWED to fail — verification is structural, never green-suite (VERIFY-02)"
  artifacts:
    - path: tests/lint-shared-module-handsync.test.cjs
      provides: "Restored fork test for fork-only bin/lib module (require-path repair is Phase 70)"
  key_links:
    - from: "fork tests/*.test.cjs (OURS / restored from merge commit)"
      to: "fork test with upstream functional test changes folded in"
      via: "per-file hand integration (D-01), per-file commit (D-03)"
      pattern: "git commit -m \"merge(69-05)"
---

<objective>
Execute **Step B / Tier 5**: integrate upstream's functional test changes into the fork test files as ordinary per-file follow-up commits after the closed merge, and confirm the fork-only `UD` tests restored by the 69-01 merge commit are present. The ~12 `UU` content-conflict tests were left at OURS by the merge commit; fold in upstream's functional test changes here. The 9 fork-only-module `UD` tests were restored OURS; confirm them. Per-file commits (D-03). Tests are allowed to fail — verification is structural, NOT a green suite.

Purpose: Land the test tier integration in triage order before final verification. Fork-only-module tests must survive (Phase 70 repairs their require paths); upstream's new tests already arrived additively in the merge commit (69-01 Tier 6 acceptance).

Output: All Tier-5 fork test files updated with upstream functional changes via per-file follow-up commits; fork-only test presence confirmed.

CRITICAL: Merge is CLOSED (MERGE_HEAD cleared in 69-01) — ordinary commits only; do NOT reference MERGE_HEAD. Do NOT make "npm test green" an acceptance criterion (Pitfall 5 / VERIFY-02) — tests referencing renamed paths WILL fail (deferred backlog Phase 70/71). To see upstream's functional test change: `git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- tests/<file>`. NEVER `-X theirs` on a fork test.
</objective>

<execution_context>
On branch `dev`, merge already committed by 69-01 (MERGE_HEAD cleared). Stay on `dev`. The UU content-conflict tests hold OURS content from the merge commit; the fork-only UD tests were restored OURS. Diff upstream's functional test change per file via `git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- tests/<file>`, then apply into the fork test.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate upstream functional test changes into UU content-conflict tests + finalize DU/AA (per-file)</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 5 table; Assumption A2 on DU execute-phase-step-5-5; Pitfall 5 tests-may-fail)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (D-03 per-file commits)
  </read_first>
  <action>
    For each test that had a `UU` content conflict (plan-review-convergence, semver-compare, ultraplan-phase, windows-test-parity-guard, workflow-size-budget, workspace, worktree-cleanup, worktree-safety, etc.), diff upstream's functional test change (`git diff $(git merge-base HEAD 1bb253c9) 1bb253c9 -- tests/<file>`) and apply it into the fork test per D-01/D-02 — preserve fork test intent, fold in upstream's functional changes. For the `DU` `tests/execute-phase-step-5-5-deviation-doc.test.cjs` (Assumption A2) and the 4 `AA` `bug-170`/`bug-17`/`bug-224`/`bug-33`: the merge commit already resolved them to the fork-preserving state; if upstream's added/kept version carries functional test content the fork should adopt, fold it in here. `git add` + per-file commit (D-03): `git commit -m "merge(69-05): integrate upstream into tests/<file> [Tier 5]"`. Do NOT gate on tests passing (VERIFY-02). NEVER `-X theirs`. Skip tests with no upstream functional change (note in deviations).
  </action>
  <acceptance_criteria>
    - No conflict markers remain in any tests/ file
    - `git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU) tests/'` is empty (merge stays closed)
    - Each integrated test landed as its own commit tagged `[Tier 5]`
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(command grep -rlE '^(<<<<<<<|>>>>>>>)' tests/ 2>/dev/null)" && test -z "$(git status --porcelain | command grep -E '^(UU|UD|DU|AA|AU) tests/')"</automated>
  </verify>
  <done>All UU/DU/AA test functional changes integrated and committed per-file as ordinary follow-up commits; merge stays closed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm fork-only UD tests for fork-only bin/lib modules are present (restored by merge commit)</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 5: 9 UD fork-only tests; Phase 70 repairs require paths)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (MERGE-02 rename not adopted; D-03)
  </read_first>
  <action>
    The fork-only-module test `UD`s (bug-3751-init-local-agents, cjs-sdk-bridge-integration, config-schema-sdk-parity, configuration-generator, gen-staleness-check, lint-shared-module-handsync, project-root-generator, state-document-generator, workstream-inventory-builder-generator) were RESTORED OURS by the 69-01 merge commit (they test fork-only bin/lib modules restored in the merge). Confirm they are present on disk and no fork test UD remains unresolved: `git status --porcelain | command grep '^UD tests/'` empty. Their require paths still point at `get-shit-done/bin/lib/` and will be repaired in Phase 70 — they may fail until then (acceptable, VERIFY-02). If any is unexpectedly absent, restore from the recovery anchor (`git checkout pre-merge-v1.3.1-backup -- tests/<file> && git add tests/<file>`) and commit as an ordinary follow-up: `git commit -m "merge(69-05): restore fork test tests/<file> (require-path repair in P70) [Tier 5]"`.
  </action>
  <acceptance_criteria>
    - `ls tests/lint-shared-module-handsync.test.cjs` succeeds (fork-only test present)
    - `git status --porcelain | command grep '^UD tests/' | head -1` is empty (no fork test UD unresolved)
    - Any restored test committed per-file tagged `[Tier 5]`
  </acceptance_criteria>
  <verify>
    <automated>ls tests/lint-shared-module-handsync.test.cjs >/dev/null 2>&1 && test -z "$(git status --porcelain | command grep '^UD tests/')"</automated>
  </verify>
  <done>All fork-only-module tests confirmed present (restored by merge commit; require-path repair deferred to Phase 70); merge stays closed.</done>
</task>

</tasks>

<verification>
- No conflict markers in tests/; no unmerged tests/ paths
- Restored fork-only-module tests present on disk (require-path repair deferred to Phase 70)
- No green-suite dependency anywhere (VERIFY-02)
- Merge stays closed (no MERGE_HEAD) — ordinary follow-up commits (D-08)
</verification>

<success_criteria>
All Tier-5 test functional changes integrated in triage order with per-file commits (D-03), fork-only-module tests confirmed present, tests permitted to fail — ready for final structural verification (69-06).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-05-SUMMARY.md` when done. Merge already closed; ordinary follow-up commits.
</output>
</content>
