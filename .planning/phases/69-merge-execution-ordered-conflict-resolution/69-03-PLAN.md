---
phase: 69-merge-execution-ordered-conflict-resolution
plan: 03
type: execute
wave: 3
depends_on: ["69-02"]
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/model-catalog.cjs
  - get-shit-done/bin/lib/command-routing-hub.cjs
  - get-shit-done/bin/lib/state-document.cjs
  - get-shit-done/bin/lib/configuration.generated.cjs
  - get-shit-done/bin/lib/project-root.generated.cjs
  - get-shit-done/bin/lib/state-document.generated.cjs
  - get-shit-done/bin/lib/workstream-inventory-builder.generated.cjs
  - scripts/lint-shared-module-handsync.cjs
  - scripts/shared-module-handsync-allowlist.json
  - sdk/
autonomous: true
requirements: [MERGE-02, MERGE-03, SDK-02]
must_haves:
  truths:
    - "MERGE-02: fork-only get-shit-done/bin/lib/*.cjs modules upstream deleted via rename are RESTORED (rename NOT adopted)"
    - "ls get-shit-done/bin/lib/core.cjs succeeds post-resolution (both trees coexist; gsd-core/ is additive Tier 6)"
    - "SDK-02: all sdk/ delete/modify (UD) conflicts resolved in upstream's favor via git rm; clean staged sdk/ deletions accepted"
    - "ls sdk/ fails after resolution (No such file or directory)"
    - "D-03: each restored fork module lands as its own per-file commit; sdk/ deletions may be batch-staged (no conflict markers)"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Restored fork core module (rename to gsd-core/ deferred to Phase 71)"
  key_links:
    - from: "git checkout --ours get-shit-done/bin/lib/*.cjs"
      to: "working tree restore"
      via: "UD deleted-by-them resolved by restoring HEAD version"
      pattern: "checkout --ours"
---

<objective>
Resolve Tier 3 (fork-critical) and the sdk/ deletion of the in-progress merge. RESTORE the fork-only `get-shit-done/bin/lib/*.cjs` modules and fork scripts that upstream "deleted" via its `get-shit-done/`->`gsd-core/` rename (MERGE-02 — do NOT adopt the rename), and ACCEPT upstream's full `sdk/` deletion (SDK-02). Per-file commits for restores (D-03); sdk/ deletions may be batch-staged since they carry no conflict markers, but tier order is honored.

Purpose: Keep the fork's live modules alive at `get-shit-done/` (Phase 71 owns the rename sweep) while accepting that upstream removed sdk/ entirely (SDK-01 documentation already captured in Phase 68 — dependency satisfied).

Output: Tier-3 fork modules restored + committed; all sdk/ paths removed; merge still in progress.

CRITICAL: The merge is still in progress (MERGE_HEAD set from 69-01/69-02). Do NOT close it here. Pitfall 1 (RESEARCH): never resolve `get-shit-done/bin/lib/*` `UD` by taking "theirs" — that silently deletes fork modules and pre-adopts the Phase 71 rename. Always RESTORE via `--ours`. The incoming `gsd-core/` tree is accepted as additive in Tier 6 (69-06), NOT here.
</objective>

<execution_context>
On branch `dev`, merge in progress. Stay on `dev`. Resolution mechanics from RESEARCH delete/modify table:
- `UD` (deleted by them), fork file should STAY: `git checkout --ours <file>` then `git add <file>` (restores HEAD version).
- `UD` (deleted by them), ACCEPT deletion: `git rm <file>` (used for ALL sdk/** paths).
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: RESTORE fork-only get-shit-done/bin/lib/*.cjs modules and fork scripts (UD -> --ours)</name>
  <files>get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/lib/phase.cjs, get-shit-done/bin/lib/model-catalog.cjs, get-shit-done/bin/lib/command-routing-hub.cjs, get-shit-done/bin/lib/state-document.cjs, get-shit-done/bin/lib/configuration.generated.cjs, get-shit-done/bin/lib/project-root.generated.cjs, get-shit-done/bin/lib/state-document.generated.cjs, get-shit-done/bin/lib/workstream-inventory-builder.generated.cjs, scripts/lint-shared-module-handsync.cjs, scripts/shared-module-handsync-allowlist.json</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Tier 3 table; Pitfall 1 do-not-adopt-rename; delete/modify resolution table)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (MERGE-02 forbids pre-rename; D-03 per-file commits)
  </read_first>
  <action>
    For every fork-owned `UD` (deleted-by-them) path that must STAY (the headline `get-shit-done/bin/lib/*.cjs` set plus fork scripts `scripts/lint-shared-module-handsync.cjs`, `scripts/shared-module-handsync-allowlist.json`), restore the HEAD version per MERGE-02 (rename NOT adopted — Pitfall 1): `git checkout --ours <file> && git add <file>`. Enumerate the full set authoritatively with `git status --porcelain | command grep '^UD ' | command grep '^UD .*get-shit-done/'` rather than relying solely on the list above (the 10 lib modules are the headline set; restore every fork-owned `get-shit-done/bin/lib/*` UD). Commit each restored file on its own (D-03): `git commit -m "merge(69-03): restore <file> (rename deferred to P71) [Tier 3]"`. Do NOT take `--theirs` on any `get-shit-done/` path. The `get-shit-done/workflows|references|templates` UU files are Tier 4 (69-04), not here.
  </action>
  <acceptance_criteria>
    - `ls get-shit-done/bin/lib/core.cjs` exits 0 (fork module present, rename NOT adopted)
    - `git status --porcelain | command grep '^UD .*get-shit-done/' | head -1` is empty (no fork get-shit-done/ UD left unresolved)
    - Each restored module landed as its own commit tagged `[Tier 3]`
    - `git rev-parse MERGE_HEAD` still resolves (merge still in progress)
  </acceptance_criteria>
  <verify>
    <automated>ls get-shit-done/bin/lib/core.cjs >/dev/null 2>&1 && test -z "$(git status --porcelain | command grep '^UD .*get-shit-done/')" && git rev-parse MERGE_HEAD >/dev/null 2>&1</automated>
  </verify>
  <done>All fork-only get-shit-done/bin/lib/*.cjs and fork scripts restored to HEAD content and committed per-file; rename NOT adopted; merge still in progress.</done>
</task>

<task type="auto">
  <name>Task 2: ACCEPT sdk/ deletion in upstream's favor (SDK-02)</name>
  <files>sdk/</files>
  <read_first>
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-RESEARCH.md (Separate: sdk/ deletion to ACCEPT; SDK-01 doc already committed at 68-SDK-CAPABILITY.md)
    - .planning/phases/69-merge-execution-ordered-conflict-resolution/69-CONTEXT.md (SDK-02 depends on SDK-01 — satisfied in Phase 68)
  </read_first>
  <action>
    Accept upstream's full sdk/ removal (SDK-02). SDK-01 restoration-grade documentation was already committed in Phase 68 (`68-SDK-CAPABILITY.md`), so the dependency is satisfied. Resolve the `UD` (modify/delete in upstream's favor) sdk/ paths with `git rm`: enumerate them via `git status --porcelain | command grep -E '^(UD|DU) .*sdk/'` and `git rm <path>` each (this covers the 62 UD modify/deletes plus sdk/package.json/sdk/package-lock.json). The 293 clean `D ` staged deletions are already staged — leave them. The 7 sdk/shared/*.json -> gsd-core/bin/shared/*.json renames are handled by the gsd-core/ acceptance in Tier 6. Since these carry no conflict markers after `git rm`, batch-commit them per D-03's allowance for non-conflict groups: `git commit -m "merge(69-03): accept sdk/ deletion (SDK-01 documented in P68) [Tier 3]"`. After: `ls sdk/` must fail.
  </action>
  <acceptance_criteria>
    - `ls sdk/` returns non-zero (No such file or directory)
    - `git status --porcelain | command grep ' sdk/' | command grep -E '^(UD|DU|UU)'` is empty (no unresolved sdk/ conflict)
    - The sdk/ deletion landed as a commit tagged `[Tier 3]`
  </acceptance_criteria>
  <verify>
    <automated>! ls sdk/ >/dev/null 2>&1 && test -z "$(git status --porcelain | command grep ' sdk/' | command grep -E '^(UD|DU|UU)')"</automated>
  </verify>
  <done>All sdk/ paths removed (UD resolved via git rm, clean deletions staged); ls sdk/ fails; deletion committed; merge still in progress.</done>
</task>

</tasks>

<verification>
- `ls get-shit-done/bin/lib/core.cjs` succeeds (rename not adopted, MERGE-02)
- `ls sdk/` fails (SDK-02)
- No `UD`/`DU` left for get-shit-done/ or sdk/ paths
- `git rev-parse MERGE_HEAD` == 1bb253c9 (merge still open)
</verification>

<success_criteria>
Tier-3 fork modules restored (rename deferred to Phase 71), sdk/ deletion accepted (SDK-02), all committed in triage order with per-file commits for restores — merge still in progress, ready for Tier 4 (69-04).
</success_criteria>

<output>
Create `.planning/phases/69-merge-execution-ordered-conflict-resolution/69-03-SUMMARY.md` when done. The merge MUST remain in progress (MERGE_HEAD set) for 69-04.
</output>
