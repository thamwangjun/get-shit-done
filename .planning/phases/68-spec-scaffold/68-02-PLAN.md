---
phase: 68-spec-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - .planning/spec/INDEX.md
autonomous: true
requirements: [SCAF-01, SCAF-03]
must_haves:
  truths:
    - "INDEX.md has a feature-status table with one row for each of SPEC-01..08 (columns: ID, Feature, Spec, Status, Depends On)"
    - "INDEX.md renders a dependency graph and a Wave 1 / Wave 2 build order that agree with ROADMAP Phases 69–77 and SUMMARY.md"
    - "INDEX.md has an 'Excluded from scope' section with a what/why/where entry for every PROJECT.md Out-of-Scope item PLUS the four SCAF-03-named floor items (XML tag hierarchy, resolveIncludes() stepping stone, parseV() semver block)"
    - "Each feature-status row's Spec link points at the matching NN-*/SPEC.md stub created in plan 68-01"
  artifacts:
    - path: ".planning/spec/INDEX.md"
      provides: "Spec-set manifest: feature-status table + dependency graph + wave build order + exclusion list"
      contains: "Excluded from"
  key_links:
    - from: ".planning/spec/INDEX.md"
      to: ".planning/spec/NN-*/SPEC.md stubs"
      via: "feature-status table Spec column links to each stub path"
      pattern: "0[1-8]-[a-z-]+/SPEC.md"
    - from: ".planning/spec/INDEX.md"
      to: ".planning/PROJECT.md Out of Scope + SCAF-03 floor items"
      via: "Excluded-from-scope what/why/where entries"
      pattern: "resolveIncludes|parseV|persona"
---

<objective>
Create the spec-set manifest `.planning/spec/INDEX.md`: a feature-status table keyed on the REQUIREMENTS.md handles SPEC-01..08, a dependency graph, a Wave 1 / Wave 2 build order, and an explicit "Excluded from scope" section. The exclusion list is an exhaustive sweep (D-03) — every PROJECT.md Out-of-Scope item plus the four SCAF-03-named floor items — with a what/why/where triad per entry.

Purpose: The manifest is the single index a reimplementer reads first; the dependency graph drives phase sequencing and the exclusion list is the primary guard against carrying forward abandoned work. This satisfies SCAF-01 and SCAF-03 and Phase 68 success criteria 1 and 3.
Output: `.planning/spec/INDEX.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-spec-scaffold/68-CONTEXT.md
@.planning/phases/68-spec-scaffold/68-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/spec/00-CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan (68-02) produces one NEW artifact:

**New file:**
- `.planning/spec/INDEX.md` — the spec-set manifest

**Manifest structure (three ordered sections + exclusion list):**
- Feature-status table — columns `ID | Feature | Spec | Status | Depends On`, one row per `SPEC-01..08`, keyed on REQUIREMENTS.md handles
- Dependency graph — Wave 1 (SPEC-01/02/04/08, independent) and Wave 2 (SPEC-03→02, SPEC-05/06/07→08)
- Build order — `**Wave N** *(blocked-on note)*` + bulleted spec lines
- "Excluded from scope" section — what/why/where triad per excluded item

**Convention tokens consumed (defined in 68-01's 00-CONVENTIONS.md):** the `SPEC-NN` whole-feature handles that key the table; the `Draft` status value; the em-dash `—` "none" glyph.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write INDEX.md (feature-status table + dependency graph + wave order + exclusion sweep)</name>
  <files>.planning/spec/INDEX.md</files>
  <read_first>
    - .planning/spec/INDEX.md (the file being created — does not yet exist)
    - .planning/spec/00-CONVENTIONS.md (the locked ID scheme + status vocabulary the table must key on — created in plan 68-01)
    - .planning/phases/68-spec-scaffold/68-PATTERNS.md (the "## .planning/spec/INDEX.md (manifest)" section: column set, wave-block render, exclusion what/why/where structure)
    - .planning/phases/68-spec-scaffold/68-CONTEXT.md (D-03 exhaustive exclusion sweep; D-01 dependency-graph render discretion)
    - .planning/PROJECT.md (§ "Out of Scope", lines ~167–175 — the authoritative source for the exclusion sweep)
    - .planning/ROADMAP.md (Phases 69–77 Depends-on lines — the dependency graph and waves MUST agree with these)
    - .planning/research/SUMMARY.md (§ "Dependency Graph and Build Order", lines ~104–116 — second authority the waves must match)
    - .planning/REQUIREMENTS.md (SPEC-01..08 handles + feature names for the table rows)
  </read_first>
  <action>
Create `.planning/spec/INDEX.md` using block-header frontmatter (H1 title, then a blank-line-separated bold-key block, then `---`) — NOT YAML. The H1 names the spec-set manifest for the v2.1.0-h Fork Feature Specification; the frontmatter block carries `**Manifest of:**`, `**Status:**`, and `**Authored:**` keys.

Write the three ordered sections, then the exclusion list.

(1) Feature-status table — GitHub-Markdown pipe table with header columns `ID | Feature | Spec | Status | Depends On` (column set fixed by SCAF-01). One row per feature keyed on the REQUIREMENTS.md handle (NOT the invariant ID), in directory order SPEC-01 through SPEC-08: SPEC-01 Positive Framing, SPEC-02 SHA Versioning, SPEC-03 Hooks Build, SPEC-04 Eta Materialization, SPEC-05 Step Numbering, SPEC-06 Thinking Effort, SPEC-07 Citation Guard, SPEC-08 Test Infrastructure. The Spec column is a relative Markdown link to each stub path (e.g. `[01-positive-framing/SPEC.md](01-positive-framing/SPEC.md)`). Status is `Draft` for all eight (matching the stub frontmatter). Depends On uses the em-dash `—` for the independent features (01, 02, 04, 08) and the REQUIREMENTS.md handle for the dependent ones (03 → SPEC-02; 05, 06, 07 → SPEC-08).

(2) Dependency graph — Claude's discretion on render (Mermaid, ASCII, or nested list per D-01), provided BOTH the dependency arrows and the Wave 1/Wave 2 split are legible. Whatever form is chosen, the arrows MUST be SPEC-02 → SPEC-03 and SPEC-08 → SPEC-05, SPEC-08 → SPEC-06, SPEC-08 → SPEC-07, with SPEC-01/02/04/08 as roots.

(3) Build order — use the repo's `**Wave N** *(parenthetical blocked-on note)*` pattern with bulleted spec lines. Wave 1 *(no dependencies — specced in parallel)*: SPEC-01, SPEC-02, SPEC-04, SPEC-08. Wave 2 *(blocked on Wave 1 completion)*: SPEC-03 (← SPEC-02), SPEC-05 (← SPEC-08), SPEC-06 (← SPEC-08), SPEC-07 (← SPEC-08). These memberships MUST match ROADMAP Phases 69–77 and SUMMARY.md lines ~104–116.

(4) "Excluded from scope" section (SCAF-03 + D-03 exhaustive sweep) — open with a one-line note that this lists superseded/abandoned work a reimplementer MUST NOT carry forward. Then one subsection per excluded item, each carrying a **what** (what the feature/artifact was), a **why** (why it is excluded), and a **where** (where it lived, so a reimplementer recognizes and skips it). The four SCAF-03-named floor items are mandatory: the XML tag hierarchy `<persona>`/`<intent>`/`<objective>`; the `resolveIncludes()` stepping stone; the `parseV()` semver block. (Note: SCAF-03 names the XML hierarchy as one item plus resolveIncludes and parseV — include all three of these plus the XML item.) On TOP of those, sweep EVERY item from PROJECT.md § "Out of Scope" (each as its own what/why/where entry): changing GSD core functionality/runtime behavior; maintaining a separate per-file changelog; applying fork standards to `get-shit-done/templates/`; applying fork standards to `get-shit-done/references/`; the em-dash complement pattern (`do not X — use Y`) deferral; fixing DO NOT violations in `sdk/`/`tests/` (out of `SCAN_DIRS` scope); the XML tag hierarchy conversion decision (2026-04-30). Where a PROJECT.md item and a SCAF-03 floor item describe the same exclusion (the XML tag hierarchy), record it once with a complete what/why/where. Use GitHub-Markdown formatting throughout with the em-dash glyph for empty cells.
  </action>
  <verify>
    <automated>f=.planning/spec/INDEX.md; fail=0; [ -f "$f" ] || exit 1; ! command grep -qE "^---$" <(head -1 "$f") || { echo "YAML frontmatter detected"; fail=1; }; for id in 01 02 03 04 05 06 07 08; do command grep -q "SPEC-$id" "$f" || { echo "missing SPEC-$id row"; fail=1; }; done; for link in 01-positive-framing/SPEC.md 02-sha-versioning/SPEC.md 03-hooks-build/SPEC.md 04-eta-materialization/SPEC.md 05-step-numbering/SPEC.md 06-thinking-effort/SPEC.md 07-citation-guard/SPEC.md 08-test-infrastructure/SPEC.md; do command grep -q "$link" "$f" || { echo "missing link $link"; fail=1; }; done; command grep -qiE "Excluded from" "$f" || { echo "no exclusion section"; fail=1; }; for x in resolveIncludes parseV persona; do command grep -q "$x" "$f" || { echo "missing exclusion $x"; fail=1; }; done; command grep -qiE "Wave 1" "$f" && command grep -qiE "Wave 2" "$f" || { echo "no wave blocks"; fail=1; }; command grep -qiE "what" "$f" && command grep -qiE "where" "$f" || { echo "no what/where triad"; fail=1; }; exit $fail</automated>
  </verify>
  <done>
`.planning/spec/INDEX.md` exists with block-header frontmatter (not YAML); a feature-status table with one `SPEC-01..08` row each (columns ID, Feature, Spec, Status, Depends On) whose Spec links resolve to the eight stub paths; a legible dependency graph and a Wave 1 / Wave 2 build order matching ROADMAP Phases 69–77 (Wave 1 = 01/02/04/08, Wave 2 = 03→02 and 05/06/07→08); and an "Excluded from scope" section with a what/why/where entry for every PROJECT.md Out-of-Scope item plus the four SCAF-03 floor items (XML tag hierarchy, resolveIncludes(), parseV()).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes a single Markdown file (`.planning/spec/INDEX.md`) in a developer-local planning directory. No untrusted input, executable code, network surface, or runtime is introduced. The one file path is a fixed literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | Tampering | INDEX.md file write | mitigate | Single fixed literal path `.planning/spec/INDEX.md`; no user-supplied or variable path interpolation, so path traversal is impossible. |
| T-68-04 | Information Disclosure | Exclusion-list and manifest content | accept | Content references only public feature names, test filenames, and milestone IDs already in the repo; no secrets or PII. |

No package-manager installs occur in this plan, so no Package Legitimacy Gate applies. No high-severity threats exist for this docs-only plan.
</threat_model>

<verification>
- `.planning/spec/INDEX.md` exists with block-header frontmatter (not YAML).
- Feature-status table has a row for each of SPEC-01..08 with columns ID, Feature, Spec, Status, Depends On; Spec links resolve to the eight stub paths created in 68-01.
- Dependency graph and Wave 1 / Wave 2 build order agree with ROADMAP Phases 69–77 and SUMMARY.md (Wave 1 = 01/02/04/08; Wave 2 = 03→02, 05/06/07→08).
- "Excluded from scope" section contains a what/why/where entry for resolveIncludes(), parseV(), the XML tag hierarchy, and every PROJECT.md Out-of-Scope item.
</verification>

<success_criteria>
- SCAF-01 satisfied: INDEX.md is the spec-set manifest with feature-status table, dependency graph, and Wave 1 / Wave 2 build order.
- SCAF-03 satisfied: INDEX.md "Excluded from scope" exhaustively sweeps PROJECT.md Out-of-Scope plus the four SCAF-03 floor items, each with a what/why/where triad.
- Phase 68 success criteria 1 and 3 are met; the manifest, per-stub depends-on, and exclusion list are all mechanically checkable for Phase 77's reconciliation.
</success_criteria>

<output>
Create `.planning/phases/68-spec-scaffold/68-02-SUMMARY.md` when done.
</output>
