---
phase: 68-spec-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/00-CONVENTIONS.md
  - .planning/spec/01-positive-framing/SPEC.md
  - .planning/spec/02-sha-versioning/SPEC.md
  - .planning/spec/03-hooks-build/SPEC.md
  - .planning/spec/04-eta-materialization/SPEC.md
  - .planning/spec/05-step-numbering/SPEC.md
  - .planning/spec/06-thinking-effort/SPEC.md
  - .planning/spec/07-citation-guard/SPEC.md
  - .planning/spec/08-test-infrastructure/SPEC.md
autonomous: true
requirements: [SCAF-02]
must_haves:
  truths:
    - "00-CONVENTIONS.md locks the canonical 7-section per-feature spec template in the exact D-01 order"
    - "00-CONVENTIONS.md defines the three-tier ID scheme: requirement IDs unchanged, feature-scoped NN-INV-M invariant IDs, Requirement back-reference"
    - "00-CONVENTIONS.md defines the Draft/Ready/Implemented/Verified status vocabulary and the tier-1..tier-4 source-of-truth hierarchy"
    - "Each of the 8 NN-*/SPEC.md stubs has populated block-header frontmatter (ID, Requirement, Status: Draft, depends-on, tier-1 test) and an empty 7-section skeleton matching the locked template"
    - "Every stub frontmatter and the CONVENTIONS use block-header style (H1 + bold-key block + ---), not YAML"
  artifacts:
    - path: ".planning/spec/00-CONVENTIONS.md"
      provides: "Meta-spec: canonical template, ID scheme, status vocabulary, source-of-truth hierarchy"
      contains: "Code Context"
    - path: ".planning/spec/01-positive-framing/SPEC.md"
      provides: "SPEC-01 stub: populated frontmatter + empty 7-section skeleton"
      contains: "Status: Draft"
    - path: ".planning/spec/02-sha-versioning/SPEC.md"
      provides: "SPEC-02 stub"
      contains: "Status: Draft"
    - path: ".planning/spec/03-hooks-build/SPEC.md"
      provides: "SPEC-03 stub (depends on SPEC-02)"
      contains: "Status: Draft"
    - path: ".planning/spec/04-eta-materialization/SPEC.md"
      provides: "SPEC-04 stub"
      contains: "Status: Draft"
    - path: ".planning/spec/05-step-numbering/SPEC.md"
      provides: "SPEC-05 stub (depends on SPEC-08)"
      contains: "Status: Draft"
    - path: ".planning/spec/06-thinking-effort/SPEC.md"
      provides: "SPEC-06 stub (depends on SPEC-08)"
      contains: "Status: Draft"
    - path: ".planning/spec/07-citation-guard/SPEC.md"
      provides: "SPEC-07 stub (depends on SPEC-08)"
      contains: "Status: Draft"
    - path: ".planning/spec/08-test-infrastructure/SPEC.md"
      provides: "SPEC-08 stub"
      contains: "Status: Draft"
  key_links:
    - from: ".planning/spec/01-positive-framing/SPEC.md"
      to: ".planning/spec/00-CONVENTIONS.md"
      via: "section headers match the locked canonical template order"
      pattern: "## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)"
    - from: ".planning/spec/00-CONVENTIONS.md"
      to: "REQUIREMENTS.md handles"
      via: "ID scheme states SCAF/SPEC are whole-feature handles, NN-INV-M are invariant IDs"
      pattern: "NN-INV-M"
---

<objective>
Create the meta-spec `.planning/spec/00-CONVENTIONS.md` and the eight numbered feature `SPEC.md` stubs under `.planning/spec/`. CONVENTIONS locks the canonical per-feature spec template, the ID scheme, the status vocabulary, and the source-of-truth hierarchy that every downstream feature-spec phase (69–76) inherits. The eight stubs ship now with populated frontmatter and an empty section skeleton matching the locked template, so each feature-spec author receives a ready-to-fill skeleton.

Purpose: Settle the template, ID scheme, and scope conventions BEFORE any feature is specced, so the eight feature-spec phases produce consistent specs with no per-spec section drift. This satisfies SCAF-02 and success criteria 2 and 4 of Phase 68.
Output: `.planning/spec/00-CONVENTIONS.md` + eight `NN-*/SPEC.md` stub files (the new `.planning/spec/` directory tree).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-spec-scaffold/68-CONTEXT.md
@.planning/phases/68-spec-scaffold/68-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
This plan (68-01) produces the following NEW artifacts. (INDEX.md is produced by plan 68-02.)

**New files:**
- `.planning/spec/00-CONVENTIONS.md` — the meta-spec
- `.planning/spec/01-positive-framing/SPEC.md` — stub
- `.planning/spec/02-sha-versioning/SPEC.md` — stub
- `.planning/spec/03-hooks-build/SPEC.md` — stub
- `.planning/spec/04-eta-materialization/SPEC.md` — stub
- `.planning/spec/05-step-numbering/SPEC.md` — stub
- `.planning/spec/06-thinking-effort/SPEC.md` — stub
- `.planning/spec/07-citation-guard/SPEC.md` — stub
- `.planning/spec/08-test-infrastructure/SPEC.md` — stub

**New directory layout:** `.planning/spec/` with `00-CONVENTIONS.md` at root and eight numbered feature subdirectories `01-positive-framing/` through `08-test-infrastructure/`, each holding a single `SPEC.md`.

**New convention tokens introduced (defined in 00-CONVENTIONS.md):**
- Invariant-ID format `NN-INV-M` (two-digit feature dir number + `-INV-` + sequence; e.g. `01-INV-1`)
- Status vocabulary terms: `Draft`, `Ready`, `Implemented`, `Verified`
- Frontmatter keys: `ID`, `Requirement`, `Status`, `Confidence`, `Specced`, `Reimplementation target`, `Depends on`, `Reimplementation evidence (tier-1 test)`
- Source-of-truth tiers: tier-1 (test assertions) → tier-2 (source behavior) → tier-3 (project history) → tier-4 (reference guides, known-stale)
- Canonical section names (the 7-section template): `Purpose`, `Scope`, `Invariants`, `Acceptance Tests`, `Key Decisions`, `Code Context` (preceded by the Frontmatter block)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write 00-CONVENTIONS.md meta-spec (template, ID scheme, vocabulary, hierarchy)</name>
  <files>.planning/spec/00-CONVENTIONS.md</files>
  <read_first>
    - .planning/spec/00-CONVENTIONS.md (the file being created — does not yet exist; create the parent dir .planning/spec/)
    - .planning/phases/68-spec-scaffold/68-CONTEXT.md (D-01 canonical 7-section order; D-04 ID scheme — both LOCKED)
    - .planning/phases/68-spec-scaffold/68-PATTERNS.md (block-header-frontmatter convention; the "## .planning/spec/00-CONVENTIONS.md (meta-spec)" section)
    - .planning/research/STACK.md (section-by-section template authority, lines ~45–162)
    - .planning/research/SUMMARY.md (source-of-truth hierarchy lines ~125–134; canonical template lines ~32–46)
    - .planning/REQUIREMENTS.md (line 5 source-of-truth hierarchy phrasing; SCAF/SPEC handles)
  </read_first>
  <action>
Create `.planning/spec/00-CONVENTIONS.md` using block-header frontmatter (H1 title, then a blank-line-separated block of `**Key:** value` lines, then a `---` rule) — NOT YAML frontmatter, because no `.planning/` doc uses YAML and YAML is reserved for agents/workflows. The H1 is the conventions title for the v2.1.0-h Fork Feature Specification spec set; the frontmatter block carries `**Defines:**`, `**Status:** Ready`, and `**Authored:**` keys.

Write four content sections. (1) Canonical per-feature spec template — reproduce the D-01 LOCKED order VERBATIM as the mandatory section order all eight specs follow: Frontmatter block, then `## Purpose`, `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context` (7 sections total, Frontmatter counted as section 1). For each section write a one-line description of its required contents mirroring the comment-placeholder style: Purpose = one paragraph "why"; Scope = explicit in-scope / out-of-scope bullets; Invariants = numbered falsifiable EARS statements with RFC 2119 strength, minimum 2 per spec, invariant IDs in `NN-INV-M` form; Acceptance Tests = invariant→test traceability table keyed on `NN-INV-M`; Key Decisions = settled decisions marked "settled — do not reopen" with consequence of reopening; Code Context = advisory-marked current paths/symbols that will not survive the upstream refactor. State explicitly that this 7-section order is the locked template and no per-spec section drift is permitted. (2) ID scheme (D-04, LOCKED) — state all three tiers: requirement IDs `SCAF-01..03` and `SPEC-01..08` stay unchanged as whole-feature handles that key the INDEX feature-status table; invariant IDs are feature-scoped `NN-INV-M` (two-digit dir number + `-INV-` + sequence, e.g. `01-INV-1`), globally unique, sortable, move-proof; each spec frontmatter carries a `Requirement:` back-reference to its REQUIREMENTS.md handle (e.g. `Requirement: SPEC-01`); traceability tables key on `NN-INV-M`, never a bare `INV-1`. State that this rejects the research-proposed `SPEC-NNN`-as-invariant scheme because `SPEC-NN` already denotes a whole feature in REQUIREMENTS.md. (3) Status vocabulary — define the pipe-separated enum `Draft | Ready | Implemented | Verified` and give a one-line meaning for each. (4) Source-of-truth hierarchy — restate the tier-1..tier-4 model: tier-1 = test assertions (the test IS the spec), tier-2 = source-code behavior, tier-3 = project history (PROJECT.md / PROJECT_HISTORY.md / MILESTONES.md), tier-4 = reference guides (background only, known-stale); state the hard rule that every spec MUST cite a tier-1 or tier-2 source. Also state the gap-to-address from SUMMARY: each feature spec harvests its `Requirement:` handle from REQUIREMENTS.md, and corpus counts are recorded "current as of" a date with the assertion shape — not the count — being normative. Use GitHub-Markdown pipe tables for any tabular content with the em-dash `—` glyph for empty cells. Do not pre-write any feature's invariants or decisions — CONVENTIONS defines structure only.
  </action>
  <verify>
    <automated>command grep -q "## Code Context" .planning/spec/00-CONVENTIONS.md && command grep -q "NN-INV-M" .planning/spec/00-CONVENTIONS.md && command grep -qiE "Draft \| Ready \| Implemented \| Verified" .planning/spec/00-CONVENTIONS.md && for s in Purpose Scope Invariants "Acceptance Tests" "Key Decisions"; do command grep -q "## $s" .planning/spec/00-CONVENTIONS.md || exit 1; done && ! command grep -qE "^---$" <(head -1 .planning/spec/00-CONVENTIONS.md)</automated>
  </verify>
  <done>
`.planning/spec/00-CONVENTIONS.md` exists with block-header frontmatter (first line is an H1, not a YAML `---` fence); lists the 7 canonical sections in D-01 order (Frontmatter + Purpose + Scope + Invariants + Acceptance Tests + Key Decisions + Code Context); defines the `NN-INV-M` invariant-ID scheme with the SCAF/SPEC handles unchanged and the `Requirement:` back-reference; defines the `Draft | Ready | Implemented | Verified` status vocabulary; and states the tier-1..tier-4 source-of-truth hierarchy with the "must cite tier-1 or tier-2" hard rule.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the eight NN-*/SPEC.md stubs (populated frontmatter + empty skeleton)</name>
  <files>.planning/spec/01-positive-framing/SPEC.md, .planning/spec/02-sha-versioning/SPEC.md, .planning/spec/03-hooks-build/SPEC.md, .planning/spec/04-eta-materialization/SPEC.md, .planning/spec/05-step-numbering/SPEC.md, .planning/spec/06-thinking-effort/SPEC.md, .planning/spec/07-citation-guard/SPEC.md, .planning/spec/08-test-infrastructure/SPEC.md</files>
  <read_first>
    - .planning/spec/00-CONVENTIONS.md (the canonical template just locked in Task 1 — stubs MUST match its 7-section order)
    - .planning/phases/68-spec-scaffold/68-PATTERNS.md (the per-stub frontmatter value table at lines ~133–144; the "## .planning/spec/NN-*/SPEC.md × 8 (stub)" section; comment-placeholder skeleton pattern)
    - .planning/phases/68-spec-scaffold/68-CONTEXT.md (D-02 stub depth = populated frontmatter + empty section skeleton; D-04 ID scheme)
    - .planning/REQUIREMENTS.md (SPEC-01..08 handles for the Requirement back-references)
    - .planning/ROADMAP.md (Phases 69–76 detail blocks — the Depends-on lines and each feature's tier-1 test file)
  </read_first>
  <action>
Create eight stub files, one per feature, at `.planning/spec/01-positive-framing/SPEC.md`, `.planning/spec/02-sha-versioning/SPEC.md`, `.planning/spec/03-hooks-build/SPEC.md`, `.planning/spec/04-eta-materialization/SPEC.md`, `.planning/spec/05-step-numbering/SPEC.md`, `.planning/spec/06-thinking-effort/SPEC.md`, `.planning/spec/07-citation-guard/SPEC.md`, `.planning/spec/08-test-infrastructure/SPEC.md` (create each parent subdirectory under `.planning/spec/` using these fixed literal paths only — no user-supplied path interpolation). Each stub uses block-header frontmatter (H1 then bold-key block then `---`), NOT YAML.

Each stub's H1 is `# SPEC-NN: <Feature Name>`. The frontmatter block carries these keys in this order, populated NOW: `**ID:** NN` (two-digit dir number), `**Requirement:** SPEC-NN`, `**Status:** Draft`, `**Confidence:** <!-- set when body is written -->`, `**Specced:** <!-- set when body is written -->`, `**Reimplementation target:** v2.1.0-h fork features on refactored upstream`, `**Depends on:** <value>`, `**Reimplementation evidence (tier-1 test):** <test path>`. Use the em-dash `—` for "no dependency".

Per-stub values (feature name, Depends on, tier-1 test): 01 positive-framing — Positive Framing Standard, Depends on `—`, tier-1 `tests/negative-framing-scan.test.cjs`. 02 sha-versioning — SHA Versioning System, Depends on `—`, tier-1 `tests/version-detection.test.cjs` (also `tests/semver-compare.test.cjs`). 03 hooks-build — On-Demand Hooks Build, Depends on `SPEC-02`, tier-1 `tests/bug-1924-ensure-hooks-dist-on-demand.test.cjs`. 04 eta-materialization — Eta v4 Install-Time Materialization, Depends on `—`, tier-1 `tests/install-eta-regression.test.cjs`. 05 step-numbering — Step Numbering System, Depends on `SPEC-08`, tier-1 `tests/step-numbering-scan.test.cjs` (also `tests/cross-file-step-refs.test.cjs`). 06 thinking-effort — Per-Agent Thinking Effort, Depends on `SPEC-08`, tier-1 `tests/feat-58-regression.test.cjs`. 07 citation-guard — Citation Cleanup Guard, Depends on `SPEC-08`, tier-1 `tests/no-issue-citations.test.cjs`. 08 test-infrastructure — Fork Test Infrastructure, Depends on `—`, tier-1 the fork test-suite layout / `SERIAL_FILES` convention (record this as the policy source since spec-08 is a standing-policy spec with no single tier-1 test).

After the frontmatter `---` rule, write the empty 7-section skeleton in the locked D-01 order: `## Purpose`, `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`. Under each header place exactly one HTML comment placeholder describing what the feature-spec phase (69–76) will fill — for Purpose a one-paragraph "why" note; Scope an in-scope/out-of-scope note; Invariants a "numbered EARS statements, RFC 2119, minimum 2, IDs NN-INV-M" note; Acceptance Tests an "invariant→test traceability table keyed on NN-INV-M" note; Key Decisions a "settled decisions, do not reopen, with consequence" note; Code Context an `<!-- advisory -->` marker plus a "current paths/symbols; will not survive upstream refactor" note. Do NOT pre-write any invariants, scope bullets, or decisions — that content belongs to Phases 69–76 (D-02). Bodies stay empty except the placeholder comments.
  </action>
  <verify>
    <automated>fail=0; for d in 01-positive-framing 02-sha-versioning 03-hooks-build 04-eta-materialization 05-step-numbering 06-thinking-effort 07-citation-guard 08-test-infrastructure; do f=".planning/spec/$d/SPEC.md"; [ -f "$f" ] || { echo "missing $f"; fail=1; continue; }; command grep -q "Status:.*Draft" "$f" || { echo "no Status:Draft $f"; fail=1; }; command grep -qE "Requirement:.*SPEC-0[1-8]" "$f" || { echo "no Requirement $f"; fail=1; }; for s in Purpose Scope Invariants "Acceptance Tests" "Key Decisions" "Code Context"; do command grep -q "## $s" "$f" || { echo "no ## $s in $f"; fail=1; }; done; done; command grep -q "Depends on:.*SPEC-02" .planning/spec/03-hooks-build/SPEC.md || { echo "03 dep wrong"; fail=1; }; command grep -q "Depends on:.*SPEC-08" .planning/spec/05-step-numbering/SPEC.md || { echo "05 dep wrong"; fail=1; }; exit $fail</automated>
  </verify>
  <done>
All eight `.planning/spec/NN-*/SPEC.md` stubs exist. Each has block-header frontmatter (not YAML) carrying `ID`, `Requirement: SPEC-NN`, `Status: Draft`, `Reimplementation target`, `Depends on`, and `Reimplementation evidence (tier-1 test)`. Each contains the empty 7-section skeleton (`## Purpose` … `## Code Context`) in the locked D-01 order with comment placeholders and no pre-written feature content. The `Depends on` values match ROADMAP Phases 69–76: 03→SPEC-02; 05/06/07→SPEC-08; 01/02/04/08→— (none).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes only Markdown files under `.planning/spec/`, a developer-local planning directory. No untrusted input crosses any boundary; no executable code, network surface, or runtime is introduced. All file paths are fixed literals chosen by the author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | Directory creation for the eight `NN-*/` subdirectories | mitigate | Use fixed literal paths under `.planning/spec/` only; no user-supplied or variable path interpolation, so path traversal is impossible. |
| T-68-02 | Information Disclosure | Stub frontmatter content | accept | Stubs reference only public test filenames and milestone IDs already in the repo; no secrets, credentials, or PII. Developer-local planning docs. |

No package-manager installs occur in this plan, so no Package Legitimacy Gate applies. No high-severity threats exist for this docs-only plan.
</threat_model>

<verification>
- `.planning/spec/00-CONVENTIONS.md` exists; first line is an H1 (block-header frontmatter, not YAML); lists 7 canonical sections in D-01 order; defines `NN-INV-M` scheme, status vocabulary, and tier-1..tier-4 hierarchy.
- All eight `.planning/spec/NN-*/SPEC.md` stubs exist with populated block-header frontmatter (`Status: Draft`, `Requirement: SPEC-NN`) and the empty 7-section skeleton matching the locked template.
- `Depends on` values in stubs agree with ROADMAP Phases 69–76 (03→SPEC-02, 05/06/07→SPEC-08).
- No file in `.planning/spec/` uses YAML frontmatter (no leading `---` fence).
</verification>

<success_criteria>
- SCAF-02 satisfied: 00-CONVENTIONS.md defines the per-feature spec template, the REQ-ID/SPEC-ID/invariant-ID scheme, the status vocabulary, and the source-of-truth hierarchy.
- Phase 68 success criterion 2 (CONVENTIONS) and success criterion 4 (eight stubs with populated frontmatter matching manifest order) are met.
- The locked D-01 template order and D-04 ID scheme are reproduced without drift or scope reduction.
</success_criteria>

<output>
Create `.planning/phases/68-spec-scaffold/68-01-SUMMARY.md` when done.
</output>
