---
phase: 61-worktree-safety-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bug-3097-3099-executor-worktree-path-safety.test.cjs
autonomous: true
requirements: [WSC-01]

must_haves:
  truths:
    - "D-01: A new `describe('phase-61: submodule exclusion guard', () => { ... })` block is appended to tests/bug-3097-3099-executor-worktree-path-safety.test.cjs after the two existing describe blocks (not inserted between them, not renamed onto a bug number)"
    - "D-04: The new test slices the `<task_commit_protocol>` XML block from `executorSrc` using the indexOf/indexOf/.slice() pattern already established in the file, and all token assertions run against the slice variable (not against `executorSrc` directly)"
    - "D-02/D-03: The sliced protocol block contains three tokens in three separate `assert.ok` calls — `.git/worktrees/` (worktree-positive condition, SC-1), `GIT_CONTENT=` (submodule skip-branch reset mechanism, SC-2), and `skip worktree guards` (intent-documenting comment text, SC-2)"
    - "Running `npm test 2>&1 | tee /tmp/gsd-test-output.txt` after the edit reports 0 new failures, the new test is live (not skipped), and existing tests in the file still pass"
  artifacts:
    - path: "tests/bug-3097-3099-executor-worktree-path-safety.test.cjs"
      provides: "Phase 61 submodule exclusion guard regression test"
      contains: "describe('phase-61: submodule exclusion guard'"
  key_links:
    - from: "tests/bug-3097-3099-executor-worktree-path-safety.test.cjs (new describe block)"
      to: "agents/gsd-executor.md <task_commit_protocol> block"
      via: "executorSrc.indexOf('<task_commit_protocol>') / indexOf('</task_commit_protocol>') / .slice() — reuses the module-scope executorSrc constant"
      pattern: "protocol\\.includes\\('(\\.git/worktrees/|GIT_CONTENT=|skip worktree guards)'\\)"
---

<objective>
Add one new `describe` block containing one `test()` to `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` that asserts the `<task_commit_protocol>` block in `agents/gsd-executor.md` distinguishes worktree `.git` files from submodule `.git` files. Three standalone `assert.ok` calls verify the worktree-positive condition (`.git/worktrees/`), the behavioral reset mechanism (`GIT_CONTENT=`), and the intent-documenting comment text (`skip worktree guards`) — all scoped to the protocol block slice, not the full file.

Purpose: Closes WSC-01. Guards the submodule-exclusion logic added at lines ~455–465 of `gsd-executor.md` against silent regression in future upstream merges. Without this test, an upstream merge could revert the submodule guard and the existing bug #3097/#3099 tests would still pass (they only check for `worktrees/` as a disjunction, not the submodule branch).

Output: One modified test file with one new describe block; no source agent/workflow files changed; full suite still green.
</objective>

<context>
!`cat .planning/PROJECT.md`
!`cat .planning/STATE.md`
@.planning/phases/61-worktree-safety-coverage/61-CONTEXT.md
@.planning/phases/61-worktree-safety-coverage/61-PATTERNS.md
@tests/bug-3097-3099-executor-worktree-path-safety.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append phase-61 submodule exclusion guard describe block</name>
  <files>tests/bug-3097-3099-executor-worktree-path-safety.test.cjs</files>
  <read_first>
    - tests/bug-3097-3099-executor-worktree-path-safety.test.cjs — read the full file to see the two existing describe blocks (`'bug #3097: cwd-drift sentinel in gsd-executor.md'` and `'bug #3099: absolute-path safety guidance in gsd-executor.md'`), the module-scope `executorSrc` constant (lines 22–25), the block-slice pattern (lines 32–35, 43–45, 65–68), and the `assert.ok(protocol.includes(...), 'message')` style. Append after the bug #3099 describe block — do not insert between blocks.
    - agents/gsd-executor.md (lines 450–485) — confirm the three exact source tokens are present in the `<task_commit_protocol>` block before asserting them: `.git/worktrees/` (lines 457, 465), `GIT_CONTENT=` (line 462), and `skip worktree guards` (line 461). If any token text has drifted in this file at execution time, halt and report — do not invent assertions against tokens that do not exist.
    - .planning/phases/61-worktree-safety-coverage/61-CONTEXT.md — confirm D-01 (block name `'phase-61: submodule exclusion guard'`, appended after bug #3099 block), D-02 (two assertions for SC-2: `GIT_CONTENT=` and `skip worktree guards`, separate `assert.ok` calls), D-03 (also assert `.git/worktrees/` for SC-1 standalone, not the existing disjunction), D-04 (scope all assertions to the sliced `protocol` variable).
    - .planning/phases/61-worktree-safety-coverage/61-PATTERNS.md — confirm the canonical block-slice snippet and the assert.ok style; mirror them exactly.
  </read_first>
  <action>
    Append a new `describe('phase-61: submodule exclusion guard', () => { ... })` block to the end of `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs`, after the existing `describe('bug #3099: ...')` block closes (per D-01).

    Inside the new describe block, add exactly one `test('task_commit_protocol distinguishes worktree .git from submodule .git', () => { ... })`.

    Inside that test body (per D-04):
    1. Slice the `<task_commit_protocol>` block from the module-scope `executorSrc` constant using the canonical pattern already used three times in this file: `const protocolIdx = executorSrc.indexOf('<task_commit_protocol>')`, `const protocolEnd = executorSrc.indexOf('</task_commit_protocol>')`, `assert.ok(protocolIdx !== -1 && protocolEnd !== -1, 'task_commit_protocol block not found')`, `const protocol = executorSrc.slice(protocolIdx, protocolEnd)`.
    2. Add three separate `assert.ok` calls against the `protocol` slice variable (NEVER against `executorSrc` directly):
       - `assert.ok(protocol.includes('.git/worktrees/'), 'gsd-executor.md task_commit_protocol missing standalone .git/worktrees/ token — worktree-positive condition (WSC-01 SC-1) not provable')` — per D-03 / SC-1. Use the full token `.git/worktrees/` (with leading `.git/`), not the partial `worktrees/`. This is a standalone assertion, NOT a disjunction with `rev-parse --git-dir` (the existing bug #3097 test already covers the disjunction; this new assertion is stronger and stand-alone per the CONTEXT `<specifics>` block).
       - `assert.ok(protocol.includes('GIT_CONTENT='), 'gsd-executor.md task_commit_protocol missing GIT_CONTENT= reset — submodule skip-branch mechanism (WSC-01 SC-2) not present')` — per D-02.1. The `GIT_CONTENT=` empty-string assignment in the else-branch is what makes the second `if [ -f .git ] && echo "${GIT_CONTENT:-}" | grep ...` block evaluate false for submodule paths, thereby skipping the worktree guard.
       - `assert.ok(protocol.includes('skip worktree guards'), 'gsd-executor.md task_commit_protocol missing skip worktree guards comment — intent-documenting text (WSC-01 SC-2, D-02.2) not present')` — per D-02.2. This is the documentation-as-contract assertion: in this fork, comment text inside agent files is part of the deployed prompt contract.

    Reuse `executorSrc` from the existing module scope — do not re-read the file. Do not add new `require()` calls. Do not modify any of the existing three describe blocks or their tests. Do not add a `skip` option. The `// allow-test-rule` header at line 2 already covers source-text assertions on `gsd-executor.md`.
  </action>
  <verify>
    <automated>node --test tests/bug-3097-3099-executor-worktree-path-safety.test.cjs 2>&1 | tee /tmp/gsd-61-01-file.txt | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "describe('phase-61: submodule exclusion guard'" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` returns exactly `1`.
    - Source assertion: `grep -c "task_commit_protocol distinguishes worktree .git from submodule .git" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` returns exactly `1` (the new test name).
    - Source assertion: inside the new describe block (lines after the bug #3099 describe close), all three tokens appear as `protocol.includes('<token>')`: `grep -cE "protocol\.includes\('(\\.git/worktrees/|GIT_CONTENT=|skip worktree guards)'\\)" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` returns at least `3` (existing tests do not use these exact three literals in this exact form).
    - Source assertion: the new test body slices the protocol block — `grep -c "executorSrc.indexOf('<task_commit_protocol>')" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` returns at least `4` (3 from existing tests + 1 from the new one).
    - Source assertion: the new test has no `{ skip:` option — `awk '/describe\('"'"'phase-61/,/^\}\);/' tests/bug-3097-3099-executor-worktree-path-safety.test.cjs | grep -c 'skip:'` returns `0`.
    - Behavior assertion: `node --test tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` exits 0 and the TAP output shows the new test name as a passing line (`ok ... task_commit_protocol distinguishes worktree .git from submodule .git`), not a skipped or pending line.
    - Behavior assertion: the existing test count from this file is preserved — `node --test tests/bug-3097-3099-executor-worktree-path-safety.test.cjs 2>&1 | grep -E '^# (tests|pass|fail|skipped)'` shows tests count increased by exactly 1 from the pre-edit baseline (or, equivalently, the TAP summary line shows pass count = pre-edit pass count + 1).
    - Full-suite gate: `npm test 2>&1 | tee /tmp/gsd-test-output.txt | tail -5` shows the suite passes with no NEW failures versus the pre-edit baseline (existing failure count, if any, is unchanged or lower).
  </acceptance_criteria>
  <done>
    The new `describe('phase-61: submodule exclusion guard', ...)` block exists at the end of `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` with one passing `test()` that makes three standalone `protocol.includes(...)` assertions against the sliced `<task_commit_protocol>` block — covering `.git/worktrees/`, `GIT_CONTENT=`, and `skip worktree guards`. `npm test` is green with one additional passing test versus baseline. WSC-01 closed.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. Confirm the new describe block is positioned at the end of the file (after the bug #3099 block):
   `tail -25 tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` shows the closing `});` of the phase-61 describe as the last non-empty line.

2. Confirm WSC-01 success criteria from ROADMAP.md Phase 61:
   - SC-1: `grep -c "protocol\\.includes('\\.git/worktrees/')" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` >= 1
   - SC-2: `grep -c "protocol\\.includes('GIT_CONTENT=')" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` >= 1 AND `grep -c "protocol\\.includes('skip worktree guards')" tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` >= 1
   - SC-3: the new assertions appear inside a `protocol = executorSrc.slice(...)` scope — confirmed by reading the new describe block; all `protocol.includes(...)` calls reference the local `protocol` variable, never `executorSrc`.
   - SC-4: `npm test 2>&1 | tail -5` shows 0 new failures versus baseline.

3. No source agent/workflow file was modified: `git status --short` shows only `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` as modified (plus planning artifacts).
</verification>

<success_criteria>
- WSC-01 closed: the new test asserts the `<task_commit_protocol>` block contains both the worktree-positive condition (`.git/worktrees/`) and the submodule skip-branch mechanism (`GIT_CONTENT=` + `skip worktree guards`).
- All four ROADMAP Phase 61 success criteria (SC-1 through SC-4) are demonstrably true.
- The new test is live (not skipped) and passing.
- `npm test` shows 0 new failures versus the pre-edit baseline (8,243 pass / 8,255 total per ROADMAP).
- No agent or workflow source file modified — the milestone's "test-only" out-of-scope rule is honored.
</success_criteria>

<output>
Modified: `tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` (one new describe block, one new test, three standalone assertions).
Create `.planning/phases/61-worktree-safety-coverage/61-01-SUMMARY.md` when done.
</output>
