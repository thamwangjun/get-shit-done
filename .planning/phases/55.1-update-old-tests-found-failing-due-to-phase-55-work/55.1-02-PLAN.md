---
phase: 55.1-update-old-tests-found-failing-due-to-phase-55-work
plan: 02
type: execute
wave: 2
depends_on: ["55.1-01"]
files_modified:
  - tests/feat-53-config-sites-and-golden.test.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-103.2/D-101: the D-08 'bare config → effort null' premise is root-caused against the SOURCE before editing"
    - "Verdict recorded: hand-assigned ;effort catalog slots (D-102) legitimately supply effort for bare configs — the ASSUMPTION 'no ;effort anywhere' is now false because the catalog itself carries ;effort"
    - "Updated expectations derive each agent's effort from its live hand-assigned slot, never a blanket null"
    - "The same-slot invariant and the no-;-leak-in-model assertion are PRESERVED (they encode real behavior and exercise the Plan 01 source fix)"
    - "D-104: the golden snapshot is regenerated from confirmed-correct resolver output, not blindly accepted"
    - "tests/feat-53-config-sites-and-golden.test.cjs passes with 0 failures (node --test exits 0)"
  artifacts:
    - path: tests/feat-53-config-sites-and-golden.test.cjs
      provides: "D-08 back-compat expectations realigned to hand-assigned slot effort; golden regenerated"
  key_links:
    - from: "D-08 bare-config test"
      to: "sdk/shared/model-catalog.json hand-assigned slots"
      via: "expected effort derived per-agent from parseModelEffort(slot).effort"
      pattern: "parseModelEffort"
---

<objective>
Root-cause and reconcile SUSPECT CLUSTER #2 (CONTEXT D-103.2): the D-08
"bare config → resolveReasoningEffortInternal === null" back-compat block
(~150 assertions) plus the cross-resolver golden snapshot in
feat-53-config-sites-and-golden.test.cjs. Determine whether hand-assigned slots
legitimately supply effort for bare configs (→ update expectation + regenerate
golden) or whether effort leaks where it must stay null (→ fix source). Depends on
Plan 01 because the same-slot/no-;-leak assertions exercise Plan 01's codex fix.

Purpose: D-101 per-cluster verdict; D-104 golden regenerated only from confirmed
output. The verdict (see Task 1) is that the catalog now ships ;effort slots, so the
test's "no ;effort anywhere" premise is stale — update expectations, do not delete
the invariant.

Output: feat-53-config-sites-and-golden.test.cjs green.
</objective>

<context>
!`command grep -n "back-compat\|null\|golden\|same-slot\|includes(';')" tests/feat-53-config-sites-and-golden.test.cjs | head -30`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause cluster #2 — record the verdict, then realign the bare-config effort expectations</name>
  <files>tests/feat-53-config-sites-and-golden.test.cjs</files>
  <read_first>
    - tests/feat-53-config-sites-and-golden.test.cjs lines 233–281 (D-08 bare-config loop: asserts effort===null for every agent×profile, plus the same-slot invariant)
    - tests/feat-53-config-sites-and-golden.test.cjs lines 100–230 (CONFIG-03/CONFIG-04 override-site assertions that currently see 'low' from hand-assigned opus;low slots)
    - sdk/shared/model-catalog.json lines 87–122 (per-agent hand-assigned ;effort slots)
    - get-shit-done/bin/lib/core.cjs lines 1598–1637 (resolveReasoningEffortInternal precedence — slot effort wins before codex per-tier fallback)
  </read_first>
  <action>
    Record the per-cluster verdict (per D-101) in the SUMMARY: the D-08 block assumed bare
    claude config yields effort null because "no ;effort exists anywhere". Post-handover
    (D-102) the CATALOG ITSELF carries hand-assigned ;effort slots (e.g. gsd-planner golden
    = opus;low, gsd-executor balanced = sonnet;medium), so on a bare config the resolved
    slot legitimately carries ;effort and the resolver correctly returns it. This is INTENDED,
    not a leak — the premise is stale, not the source. (Confirm there is genuinely NO leak:
    for the `inherit` profile and any unmapped/inherit slot, effort must STILL be null —
    verify the resolver returns null there before updating; if it does not, STOP and escalate
    as a real source regression per D-103.)

    Realign expectations: replace the blanket `effort === null` with the per-agent expected
    effort derived from the live slot — i.e. compute `parseModelEffort(_resolveAgentSlot(dir,agent)).effort`
    as the expected value (or read the catalog slot directly). For the `inherit` profile keep
    `null`. PRESERVE both the same-slot invariant assertion and the `!model.includes(';')`
    no-leak assertion (line ~275) verbatim — they encode real behavior and the no-;-leak
    assertion now passes only because of Plan 01's codex/model fix. Update the override-site
    CONFIG-03/CONFIG-04 expectations (~108–228) the same way: derive expected effort from the
    resolved slot, keeping strict equality (no indexOf/substring false-passes, D-105).
    Do NOT introduce skips (D-105).
  </action>
  <verify>
    <automated>node --test tests/feat-53-config-sites-and-golden.test.cjs 2>&1 | command grep -E "ℹ fail [0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/feat-53-config-sites-and-golden.test.cjs` exits 0 (reported `fail 0`)
    - the same-slot invariant block and the `!model.includes(';')` assertion still exist — confirmed by `command grep -c "includes(';')" tests/feat-53-config-sites-and-golden.test.cjs` returning ≥1
    - no `skip`/`todo` added — `command grep -nE "\.(skip|todo)\(" tests/feat-53-config-sites-and-golden.test.cjs` returns nothing new vs the documented intentional skips
    - SUMMARY records the explicit verdict for cluster #2 (stale-premise vs source-leak) and confirms inherit-profile effort stays null
  </acceptance_criteria>
  <done>feat-53-config-sites-and-golden.test.cjs passes with 0 failures; invariants preserved; verdict documented.</done>
</task>

</tasks>

<verification>
node --test tests/feat-53-config-sites-and-golden.test.cjs exits 0 with fail 0.
The golden snapshot (if file-backed) regenerated only from confirmed-correct output (D-104).
</verification>

<success_criteria>
Cluster #2 root-caused with a documented verdict; D-08 bare-config and override-site
expectations realigned to hand-assigned slot effort; same-slot + no-;-leak invariants
preserved; test green; no skips.
</success_criteria>

<output>
Create .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-02-SUMMARY.md when done.
</output>
