---
phase: 55.1-update-old-tests-found-failing-due-to-phase-55-work
plan: 03
type: execute
wave: 2
depends_on: ["55.1-01"]
files_modified:
  - tests/issue-2517-runtime-aware-profiles.test.cjs
  - tests/model-profiles.test.cjs
  - tests/model-catalog-runtime-defaults.test.cjs
  - tests/feat-3023-model-phase-types.test.cjs
  - tests/bug-2794-opencode-model-profile-overrides.test.cjs
  - tests/model-alias-map.test.cjs
  - tests/core.test.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-102: stale pre-handover / pre-bump literals (plain tiers, 4-7 IDs, codex-without-effort) are updated to current catalog output"
    - "D-101: each updated literal is confirmed to be a stale expectation (intended new output), not a masked regression — any anomaly is escalated, not silently realigned"
    - "Each updated expected value is read from live catalog/resolver output, never guessed"
    - "All 7 test files pass with 0 failures (node --test exits 0 for each)"
    - "D-105: no skips introduced; the 4 documented intentional skips are untouched"
  artifacts:
    - path: tests/issue-2517-runtime-aware-profiles.test.cjs
      provides: "runtime tier / model-ID-bump expectations updated to opus 4-8 + codex effort"
    - path: tests/model-profiles.test.cjs
      provides: "profile model-map expectations updated"
    - path: tests/model-alias-map.test.cjs
      provides: "MODEL_ALIAS_MAP opus → claude-opus-4-8 expectation updated"
  key_links:
    - from: "updated test literals"
      to: "sdk/shared/model-catalog.json"
      via: "expected values read from current catalog output"
      pattern: "claude-opus-4-8"
---

<objective>
Bulk-update the 7 remaining failing test files whose assertions hardcoded
PRE-HANDOVER / PRE-BUMP catalog values. Per D-102 these are STALE literals
(opus 4-7 → claude-opus-4-8; codex tiers now carry reasoning_effort; plain-tier
profile maps now carry ;effort) — update expectations to current catalog output.
Runs in Wave 2 (after Plan 01's source fix lands) so any codex/runtime assertion
already reflects the corrected resolver. No source files are touched here; if any
"stale" literal turns out to mask a regression, STOP and escalate (D-101).

Purpose: D-102 stale-literal reconciliation across the mechanical cluster.
Output: 7 test files green.
</objective>

<context>
!`command grep -rln "claude-opus-4-7\|opus-4-7" tests/ | head`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update model-ID-bump + alias-map files (model-profiles, model-alias-map, model-catalog-runtime-defaults, core)</name>
  <files>tests/model-profiles.test.cjs, tests/model-alias-map.test.cjs, tests/model-catalog-runtime-defaults.test.cjs, tests/core.test.cjs</files>
  <read_first>
    - sdk/shared/model-catalog.json (runtimeTierDefaults — claude opus = claude-opus-4-8; codex tiers carry reasoning_effort; opencode/copilot/hermes IDs)
    - get-shit-done/bin/lib/model-catalog.cjs (MODEL_ALIAS_MAP, RUNTIMES_WITH_REASONING_EFFORT)
    - the 4 test files' failing assertions (run `node --test <file>` first to see expected-vs-actual)
  </read_first>
  <action>
    For each file run `node --test tests/<file>` to capture the exact expected-vs-actual diffs.
    Per D-102 update each stale literal to the live value: opus model ID 4-7 → claude-opus-4-8;
    codex runtime-default rows that now include reasoning_effort; any docs-table parity assertion
    (model-catalog-runtime-defaults references CONFIGURATION.md / settings-advanced runtime tables —
    if the assertion compares against a doc table, update the DOC table to current catalog values,
    not the test, per CONTEXT specifics). Read each replacement value from sdk/shared/model-catalog.json
    or the resolver output — do not guess. Keep strict-equality assertions (D-105). Confirm each
    change is a stale expectation, not a masked regression; if an actual value looks wrong
    (e.g. an effort leaking onto a null-tier runtime), STOP and escalate per D-101.
  </action>
  <verify>
    <automated>for f in model-profiles model-alias-map model-catalog-runtime-defaults core; do node --test tests/$f.test.cjs >/dev/null 2>&1 &amp;&amp; echo "$f OK" || echo "$f FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/model-profiles.test.cjs` exits 0
    - `node --test tests/model-alias-map.test.cjs` exits 0
    - `node --test tests/model-catalog-runtime-defaults.test.cjs` exits 0
    - `node --test tests/core.test.cjs` exits 0
    - no new `.skip(`/`.todo(` added in these 4 files
  </acceptance_criteria>
  <done>All 4 files pass with 0 failures; replacements read from live catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Update runtime-aware + phase-type + opencode-override files (issue-2517, feat-3023, bug-2794)</name>
  <files>tests/issue-2517-runtime-aware-profiles.test.cjs, tests/feat-3023-model-phase-types.test.cjs, tests/bug-2794-opencode-model-profile-overrides.test.cjs</files>
  <read_first>
    - sdk/shared/model-catalog.json (runtimeTierDefaults for all runtimes + per-agent ;effort slots)
    - the 3 test files' failing assertions (run `node --test <file>` first)
    - get-shit-done/bin/lib/core.cjs lines 1405–1485 (resolveModelInternal — already fixed by Plan 01 for the codex slot;effort case)
  </read_first>
  <action>
    Run `node --test tests/<file>` for each to capture diffs. Per D-102 update stale runtime
    tier / model-ID / phase-type-override-effort literals to current catalog output (opus 4-8,
    codex tiers with reasoning_effort, hand-assigned ;effort flowing through phase-type overrides).
    issue-2517 (31 failures) is runtime tier resolution + model-ID bumps; feat-3023 (2) is
    phase-type override effort; bug-2794 (2) is opencode overrides + install embed (this file's
    end-to-end install runs ~6s — expect slow, per CONTEXT specifics). Where bug-2794 asserts an
    embedded install snapshot, regenerate it from confirmed-correct output (D-104), not blindly.
    Keep strict equality (D-105). Any value that looks like a leak rather than a bump → STOP and
    escalate per D-101.
  </action>
  <verify>
    <automated>for f in issue-2517-runtime-aware-profiles feat-3023-model-phase-types bug-2794-opencode-model-profile-overrides; do node --test tests/$f.test.cjs >/dev/null 2>&1 &amp;&amp; echo "$f OK" || echo "$f FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/issue-2517-runtime-aware-profiles.test.cjs` exits 0
    - `node --test tests/feat-3023-model-phase-types.test.cjs` exits 0
    - `node --test tests/bug-2794-opencode-model-profile-overrides.test.cjs` exits 0
    - no new `.skip(`/`.todo(` added in these 3 files
  </acceptance_criteria>
  <done>All 3 files pass with 0 failures.</done>
</task>

</tasks>

<verification>
Each of the 7 files: node --test tests/<file> exits 0.
</verification>

<success_criteria>
All 7 stale-literal test files green; expectations read from live catalog; no skips;
the 4 documented intentional skips untouched; any anomaly escalated rather than masked.
</success_criteria>

<output>
Create .planning/phases/55.1-update-old-tests-found-failing-due-to-phase-55-work/55.1-03-SUMMARY.md when done.
</output>
