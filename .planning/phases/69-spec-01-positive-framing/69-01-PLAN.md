---
phase: 69-spec-01-positive-framing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/01-positive-framing/SPEC.md
autonomous: true
requirements: [SPEC-01, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md enumerates all 12+ scanner detection branches, the affirmative-rewrite replacement rule, the paired-pattern exception, and the four scan directories (ROADMAP criterion 1 / SPEC-01)"
    - "SPEC.md states >=2 numbered EARS invariants in NN-INV-M format citing tests/negative-framing-scan.test.cjs as tier-1 source, with a traceability table mapping each MUST invariant to a real subtest (ROADMAP criterion 2 / QUAL-01, QUAL-02, QUAL-04)"
    - "SPEC.md marks current file paths/symbols advisory and records a Key Decisions section of settled framing decisions marked 'do not reopen'; the spec survives a file move (ROADMAP criterion 3 / QUAL-03, QUAL-05)"
    - "SPEC.md frontmatter Status advances Draft -> Ready with Confidence and Specced (2026-06-12) set, gated on QUAL-01..05"
  artifacts:
    - path: ".planning/spec/01-positive-framing/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-01 (Purpose, Scope, 5 invariants, Acceptance Tests, Key Decisions, Code Context) with Status: Ready"
      contains: "01-INV-1"
  key_links:
    - from: ".planning/spec/01-positive-framing/SPEC.md (Acceptance Tests table)"
      to: "tests/negative-framing-scan.test.cjs (subtests)"
      via: "verbatim subtest names keyed on 01-INV-M"
      pattern: "tests/negative-framing-scan.test.cjs"
    - from: ".planning/spec/01-positive-framing/SPEC.md (Invariants)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme)"
      via: "section order + invariant ID format conformance"
      pattern: "01-INV-[1-5]"
---

<objective>
Author the full body of `.planning/spec/01-positive-framing/SPEC.md` — a behavioral-contract
specification of the affirmative-framing standard and the negative-framing corpus scanner — so a
reimplementer can rebuild the scanner on a refactored upstream without reading the current source.
The phase fills an existing stub (frontmatter + empty 7-section skeleton from Phase 68); it does
NOT modify the scanner, the test, or any corpus file — it narrates the tier-1 test
`tests/negative-framing-scan.test.cjs` into the locked 7-section contract and advances Status
`Draft -> Ready`.

Purpose: Capture the affirmative-framing standard as a durable, move-proof contract (shape
normative, counts/paths/regexes advisory), satisfying SPEC-01 and the shared QUAL-01..05 bars so
the spec reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, five invariants (`01-INV-1`..`01-INV-5`), an
Acceptance Tests traceability table citing real subtests, Key Decisions, advisory Code Context, and
frontmatter Status `Ready` (Confidence + Specced 2026-06-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-69 source artifacts (narration map + locked decisions + locked template)
@.planning/phases/69-spec-01-positive-framing/69-CONTEXT.md
@.planning/phases/69-spec-01-positive-framing/69-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/01-positive-framing/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose — no code/test
symbols are created; scanner symbols are PRE-EXISTING and appear only under advisory Code Context):

- **Invariant IDs:** `01-INV-1` (Detection), `01-INV-2` (Two-tier severity), `01-INV-3` (Exception
  suppression), `01-INV-4` (Scan scope — the four SCAN_DIRS), `01-INV-5` (Code-fence exclusion).
  (Per D-01/D-05 the affirmative-rewrite rule is NOT an invariant — it has NO invariant ID and NO
  Acceptance Tests row; it lives in Purpose + Key Decisions.)
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** frontmatter `**Status:**` advances `Draft -> Ready`; `**Confidence:**`
  and `**Specced:** 2026-06-12` set.

PRE-EXISTING (advisory, NOT produced here — list under Code Context only):
`tests/negative-framing-scan.test.cjs`, `scanForNegativeFraming`, `SCAN_DIRS`, the seven exception
predicates (`hasPositiveComplement`, `isReframePattern`, `isFactualNever`, `isAvoidDirective`,
`isForbiddenDirective`, `isFactualDont`, `isConditionalOrFactual`), `collectMarkdownFiles`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, five invariants, Acceptance Tests table</name>
  <files>.planning/spec/01-positive-framing/SPEC.md</files>
  <read_first>
    - .planning/spec/01-positive-framing/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose)
    - .planning/phases/69-spec-01-positive-framing/69-RESEARCH.md (THE narration map — §1 SCAN_DIRS,
      §2 severity buckets, §3 12-branch inventory, §4 the seven exception classes, §5 code-fence
      exclusion, §6 invariant->subtest grouping with verbatim subtest names + line refs)
    - .planning/phases/69-spec-01-positive-framing/69-CONTEXT.md (LOCKED decisions D-01..D-05)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template, NN-INV-M scheme, EARS patterns,
      RFC 2119 strength, the example `01-INV-1`)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (the body must model the standard it specifies — state
    correct behavior, do not lean on prohibition-only phrasing).

    Purpose (one paragraph): state the "why" of the affirmative-framing standard — negative
    directives are rewritten as affirmative instructions that state the correct behavior (the D-05
    replacement rule lives HERE and in Key Decisions, NOT as an invariant). State what breaks if the
    standard is absent: non-conforming negative directives ship unflagged. Cite the tier-1 test
    `tests/negative-framing-scan.test.cjs` as the behavioral authority and CLAUDE.md "Positive
    framing replacement rule" as the tier-4 human-authoring source for the rewrite rule.

    Scope: two bullet lists. In scope: the negative-framing corpus scanner behavior — detection of
    clause-start negative directives across the four scan directories, two-tier severity, the
    exception classes, code-fence exclusion. Out of scope (each with a one-line rationale): the
    rewrite/remediation ACT itself (scanner only flags, does not rewrite); non-`.md` files; runtime
    prompt-content edits; the stale reference guides (tier-4 background).

    Invariants: author exactly five numbered EARS statements with RFC 2119 strength, IDs
    `01-INV-1`..`01-INV-5` (NN-INV-M format per 00-CONVENTIONS.md §2). Each is a single falsifiable
    claim mapping to a real subtest cluster in RESEARCH.md §6. Per D-05 there is NO invariant for the
    affirmative-rewrite rule.
      - 01-INV-1 Detection (Event-driven MUST): shape normative — a non-fenced line whose clause
        start carries a negative-directive token, minus the exception classes, is flagged. Render the
        12+ branch enumeration (per D-02) as a SUPPORTING list/table marked literally
        `current as of 2026-06-12`, NOT as the contract; name every block token (never, do not,
        avoid, don't, `<anti_patterns>` tag, must not, should not, prohibited, forbidden) and the
        three warn-only tokens (cannot, won't, will not).
      - 01-INV-2 Two-tier severity (Unwanted-behavior MUST / SHALL NOT) per D-04: block-set tokens
        land in violations and cause a hard failure; the warn-only set (cannot, won't, will not)
        lands in warnings and never fails the run.
      - 01-INV-3 Exception suppression (Unwanted-behavior MUST NOT) per D-03: a matched token that is
        factual, conditional, or reframe, or paired with a positive complement, MUST NOT be flagged.
        State the behavioral CLASSES as normative (paired-complement, factual-never,
        conditional-or-factual, reframe, factual-dont, avoid-directive, forbidden-directive); the
        regex predicate names are advisory and belong only in Code Context.
      - 01-INV-4 Scan scope (Ubiquitous MUST): the scanner traverses a fixed set of prompt-corpus
        directories and scans every `.md` file recursively (the four SCAN_DIRS — agents,
        get-shit-done/workflows, get-shit-done/references, commands/gsd — named as advisory
        supporting detail, the "fixed corpus directory set" being normative).
      - 01-INV-5 Code-fence exclusion (Ubiquitous MUST / Unwanted-behavior): content inside fenced
        code blocks MUST NOT be scanned.
      End each invariant with a "Consequence of violating: <what breaks>" clause per the
      00-CONVENTIONS.md example shape.

    Acceptance Tests: a traceability table keyed on `01-INV-M`, columns exactly
    `Invariant | Test File | Subtest / Assertion Shape`. Test File is `tests/negative-framing-scan.test.cjs`
    for every row. Lift REAL verbatim subtest names from RESEARCH.md §6 (e.g. 'flags bare NEVER
    directive', 'return shape: result has violations and warnings buckets', the seven helper-unit
    describe blocks, 'does not flag patterns inside fenced code blocks', 'found prompt files to
    scan'). One row per MUST invariant 01-INV-1..01-INV-5. The affirmative-rewrite Key Decision has
    no MUST invariant -> NO row (do NOT add a `[MISSING]` row for it).
  </action>
  <verify>
    <automated>command grep -c '01-INV-' .planning/spec/01-positive-framing/SPEC.md  # >=5 invariant-ID occurrences</automated>
    <automated>command grep -q 'flags bare NEVER directive' .planning/spec/01-positive-framing/SPEC.md && command grep -q 'current as of 2026-06-12' .planning/spec/01-positive-framing/SPEC.md && echo OK</automated>
    <automated>for tok in 'never' 'do not' 'avoid' "don't" 'anti_patterns' 'must not' 'should not' 'prohibited' 'forbidden' 'cannot' "won't" 'will not'; do command grep -qi "$tok" .planning/spec/01-positive-framing/SPEC.md || echo "MISSING TOKEN: $tok"; done</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c '01-INV-' SPEC.md` returns >=5 (five invariant IDs 01-INV-1..01-INV-5 present).
    - The Acceptance Tests table contains the literal subtest name `flags bare NEVER directive` and
      cites `tests/negative-framing-scan.test.cjs` in every data row.
    - The literal marker `current as of 2026-06-12` appears on the branch enumeration.
    - Every block-set token (never, do not, avoid, don't, anti_patterns, must not, should not,
      prohibited, forbidden) and every warn-only token (cannot, won't, will not) is named in the body
      (the per-token grep loop emits no `MISSING TOKEN` line).
    - There is NO invariant ID for the affirmative-rewrite rule and NO `[MISSING — write test first]`
      row in the Acceptance Tests table.
    - The four sections appear under the exact headers `## Purpose`, `## Scope`, `## Invariants`,
      `## Acceptance Tests` in that order, with no placeholder `<!-- to be filled -->` comment
      remaining in them.
  </acceptance_criteria>
  <done>
    Purpose, Scope, Invariants (five `01-INV-M` EARS statements), and the Acceptance Tests
    traceability table are authored; ROADMAP criteria 1 and 2 are satisfied; all 12+ branches, the
    rewrite rule, the paired-pattern exception, and the four scan dirs are present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Key Decisions + advisory Code Context, advance Status Draft -> Ready</name>
  <files>.planning/spec/01-positive-framing/SPEC.md</files>
  <read_first>
    - .planning/spec/01-positive-framing/SPEC.md (now carrying the Task 1 normative core; fill the
      remaining `## Key Decisions` and `## Code Context` sections and update the frontmatter block)
    - .planning/phases/69-spec-01-positive-framing/69-CONTEXT.md (D-02 shape-not-count, D-03 exception
      classes, D-04 two-tier severity + test decisions D-16–D-23, D-05 affirmative-rewrite rule)
    - .planning/phases/69-spec-01-positive-framing/69-RESEARCH.md (§7 advisory line ranges; §3/§4
      regex predicate names and SCAN_DIRS literals to mark advisory)
    - .planning/spec/00-CONVENTIONS.md (§1 Section 6/7 shape; §3 status vocabulary Draft|Ready|
      Implemented|Verified and the "MUST NOT advance to Ready unless QUAL-01..05 satisfied" rule)
  </read_first>
  <action>
    Fill the final two sections and update the frontmatter block; keep authoring affirmative-framed
    prose.

    Key Decisions (`## Key Decisions`): record the settled framing decisions, each as
    statement + one-sentence rationale + "Settled — do not reopen. Consequence of reopening:
    <what breaks>":
      (a) Two-tier severity / warn-only membership (D-04, test decisions D-16–D-23): the warn-only
          set is cannot/won't/will not; consequence of reopening — warn-only intent is lost and the
          scanner either over-blocks or stops surfacing soft signals.
      (b) Affirmative-rewrite replacement rule (D-05): negatives are replaced with affirmative
          instructions stating the correct behavior, not merely deleted; this is a human-authoring
          standard, so it is a Key Decision and NOT a MUST invariant (the scanner does not test
          rewriting); consequence of reopening — rewrites regress to bare deletions that drop the
          correct-behavior instruction.
      (c) Shape-normative-not-count (D-02): the detection SHAPE is the contract, the branch count is
          dated `current as of 2026-06-12`; consequence of reopening — the traceability table rots on
          every upstream merge and the spec stops surviving file/branch changes.

    Code Context (`## Code Context`): mark the section (and/or each item) `<!-- advisory -->`. List
    the PRE-EXISTING current paths/symbols ONLY here, with no normative claim resting on them:
    `tests/negative-framing-scan.test.cjs`, `scanForNegativeFraming`, `SCAN_DIRS` (the four literals),
    `collectMarkdownFiles`, and the seven exception predicate names (hasPositiveComplement,
    isReframePattern, isFactualNever, isAvoidDirective, isForbiddenDirective, isFactualDont,
    isConditionalOrFactual). Note that line numbers from RESEARCH.md §7 are advisory and shift on any
    test edit (move-proofing for the upstream refactor).

    Frontmatter transition (block-header frontmatter, NOT YAML): set `**Status:** Ready` (from
    Draft), set `**Confidence:**` to a stated value (e.g. High — Claude's discretion per CONTEXT.md),
    and set `**Specced:** 2026-06-12`. Advancing to Ready asserts QUAL-01 (numbered falsifiable EARS
    invariants), QUAL-02 (traceability table maps each MUST to a subtest), QUAL-03 (advisory-marked
    paths, move-proof), QUAL-04 (tier-1 test cited), QUAL-05 (Key Decisions with do-not-reopen +
    consequence) are all satisfied by the finished body.
  </action>
  <verify>
    <automated>command grep -q '\*\*Status:\*\* Ready' .planning/spec/01-positive-framing/SPEC.md && command grep -q '\*\*Specced:\*\* 2026-06-12' .planning/spec/01-positive-framing/SPEC.md && echo OK</automated>
    <automated>command grep -c 'Settled — do not reopen' .planning/spec/01-positive-framing/SPEC.md  # >=3 Key Decisions</automated>
    <automated>command grep -q 'advisory' .planning/spec/01-positive-framing/SPEC.md && command grep -q 'scanForNegativeFraming' .planning/spec/01-positive-framing/SPEC.md && echo OK</automated>
    <automated>command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' .planning/spec/01-positive-framing/SPEC.md  # ==6, locked section set, none renamed/added</automated>
    <automated>command grep -c 'to be filled' .planning/spec/01-positive-framing/SPEC.md  # ==0, no placeholder comments remain</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` contains the literal `**Status:** Ready` and `**Specced:** 2026-06-12`; `Confidence`
      is set to a non-placeholder value.
    - `command grep -c 'Settled — do not reopen'` returns >=3 (the three Key Decisions a/b/c).
    - The `## Code Context` section is marked `<!-- advisory -->` and lists `scanForNegativeFraming`,
      `SCAN_DIRS`, and at least one exception predicate name; no normative invariant text appears in
      Code Context.
    - The six body section headers appear exactly once each in the locked order
      (`## Purpose`, `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`,
      `## Code Context`) — the section-header grep returns 6 and no section is renamed/added/removed
      versus 00-CONVENTIONS.md §1.
    - `command grep -c 'to be filled' SPEC.md` returns 0 (every placeholder comment is replaced).
  </acceptance_criteria>
  <done>
    Key Decisions and advisory Code Context are authored, frontmatter Status is `Ready` with
    Confidence + Specced set; ROADMAP criterion 3 is satisfied and the spec conforms exactly to the
    locked 7-section template (no drift for Phase 77).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This phase authors a static Markdown documentation artifact (`SPEC.md`). There is no untrusted input, no code execution, no runtime surface, no network call, and no secret handled. Standard STRIDE injection/auth/DoS categories do not apply to a prose deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information disclosure / Tampering (spec INACCURACY) | SPEC.md normative claims | mitigate | Every invariant and Acceptance Tests row is traced to the tier-1 test `tests/negative-framing-scan.test.cjs` with verbatim subtest names (per QUAL-02/QUAL-04); no normative claim rests on an advisory path/symbol/line-number that the upstream refactor could move (per D-02/QUAL-03 advisory-marking). |
| T-69-02 | Tampering (advisory-claim drift) | SPEC.md Code Context paths/symbols | accept | Current paths, regex predicate names, and line numbers will shift on any test edit; this is accepted because they are confined to `## Code Context` and marked `<!-- advisory -->`, so no contract depends on them. |

No package-manager installs occur in this phase, so the `T-{phase}-SC` supply-chain row and the
package-legitimacy checkpoint do not apply. No high-severity threats are present.
</threat_model>

<verification>
Overall phase checks (run after both tasks):
- `command grep -c '01-INV-' SPEC.md` >= 5 (five invariants present).
- `command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' SPEC.md` == 6 (locked section set, exact order, no drift vs 00-CONVENTIONS.md §1).
- `command grep -q '**Status:** Ready'` and `command grep -q '**Specced:** 2026-06-12'` both succeed.
- Per-token presence loop over all 9 block tokens + 3 warn-only tokens emits no `MISSING TOKEN`.
- The Acceptance Tests table cites `tests/negative-framing-scan.test.cjs` and at least one real
  subtest name from RESEARCH.md §6; no `[MISSING — write test first]` row exists.
- `command grep -c 'to be filled' SPEC.md` == 0 (no placeholder comments remain).
</verification>

<success_criteria>
Measurable completion — maps to the three ROADMAP Phase 69 success criteria:
1. SPEC.md enumerates all 12+ detection branches, the affirmative-rewrite replacement rule, the
   paired-pattern exception, and the four scan directories. [SPEC-01]
2. SPEC.md states >=2 numbered EARS invariants (five authored) citing
   `tests/negative-framing-scan.test.cjs` as tier-1; the Acceptance Tests table maps each MUST
   invariant to a real subtest. [QUAL-01, QUAL-02, QUAL-04]
3. Current paths/symbols are advisory-marked and the spec survives a file move; a Key Decisions
   section records settled framing decisions "do not reopen" with consequence-of-reopening; Status
   is advanced `Draft -> Ready`. [QUAL-03, QUAL-05]
</success_criteria>

<output>
Create `.planning/phases/69-spec-01-positive-framing/69-01-SUMMARY.md` when done.
</output>
