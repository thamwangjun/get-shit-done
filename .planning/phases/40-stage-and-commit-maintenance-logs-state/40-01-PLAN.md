---
phase: "40"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/stage-batch-5.cjs
  - get-shit-done/bin/lib/security.cjs
  - get-shit-done/bin/lib/state.cjs
  - .planning/v1.41.5-MILESTONE-AUDIT.md
  - bin/install.js
  - hooks/gsd-check-update-worker.js
  - hooks/gsd-statusline.js
  - scripts/gen-inventory-manifest.cjs
  - scripts/stage-batch-1.cjs
  - scripts/stage-batch-2.cjs
  - scripts/stage-batch-3.cjs
  - logs/1775648987.md
autonomous: true
requirements:
  - STAGE-05

must_haves:
  truths:
    - ".gitignore excludes .antigravity/, .antigravitycli/, and .claudeignore under a 'Antigravity CLI' comment block"
    - "D-02: security.cjs and state.cjs are committed in a separate fix(lib) commit before Batch 5"
    - "D-05: Batch 5 commit message is exactly 'chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)'"
    - "D-03: stage-batch-5.cjs uses full git log for duplicate commit detection (not just latest commit)"
    - "D-04: stage-batch-5.cjs is self-referential — it commits itself as part of Batch 5"
    - "Working directory is completely clean after Batch 5 commit (git status returns empty)"
  artifacts:
    - path: "scripts/stage-batch-5.cjs"
      provides: "Batch 5 staging and commit automation"
      exports: []
    - path: ".gitignore"
      provides: "Excludes Antigravity runtime config dirs from git tracking"
      contains: ".antigravity/"
  key_links:
    - from: "scripts/stage-batch-5.cjs"
      to: "git log"
      via: "execFileSync('git', ['log', '--pretty=format:%s'])"
      pattern: "chore\\(maintenance\\).*Batch 5"
    - from: ".gitignore"
      to: ".antigravity/ .antigravitycli/ .claudeignore"
      via: "gitignore pattern match"
      pattern: "\\.antigravity/"
---

<objective>
Complete Batch 5 of the v1.41.5 commit history refactor: update .gitignore to exclude Antigravity runtime dirs, produce a fix(lib) commit for the two source lib bug fixes, create the stage-batch-5.cjs automation script, and execute it to produce the final Batch 5 commit — leaving the working directory completely clean for Phase 41 verification.

Purpose: Satisfies STAGE-05 and sets up a clean working tree so Phase 41 can run the zero-diff parity audit.
Output: Two new commits (fix(lib) + Batch 5) on thamw-main; .gitignore updated; scripts/stage-batch-5.cjs created and committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/40-stage-and-commit-maintenance-logs-state/40-CONTEXT.md
@.planning/phases/40-stage-and-commit-maintenance-logs-state/40-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from scripts/stage-batch-4.cjs (165 lines). -->
<!-- Use stage-batch-4.cjs as direct copy base — swap file list, commit message, duplicate check string. -->

From scripts/stage-batch-4.cjs (full pattern to replicate):
- Imports: `const fs = require('fs'); const path = require('path'); const { execFileSync } = require('child_process');`
- `hasChangesSinceV1_41_2(file, repoRoot)` helper: checks `git cat-file -e v1.41.2:<file>` and `git diff --quiet v1.41.2 -- <file>`
- `run()` function contains: branch guard, duplicate commit detection (full log), expectedFiles Set, missing-file abort, git reset, conditional staging, subset verification, commit
- Branch guard: `ALLOW_ANY_BRANCH !== '1'` override, requires `thamw-main`
- Duplicate check form: `execFileSync('git', ['log', '--pretty=format:%s'])` then `.includes(<commit message>)`
- Untracked dirs: `git add -f <dir>/` then enumerate with `fs.readdirSync` and add `<dir>/<filename>` entries to expectedFiles

From .gitignore (lines 8-14, the section to extend after):
```
# Local test installs
.claude/

# Cursor IDE — local agents/skills bundle (never commit)
.cursor/
```
New block to append after `.cursor/` block:
```
# Antigravity CLI — local runtime config and session state (never commit)
.antigravity/
.antigravitycli/
.claudeignore
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and create scripts/stage-batch-5.cjs</name>
  <files>.gitignore, scripts/stage-batch-5.cjs</files>
  <read_first>
    - .gitignore (read current content — insert after the .cursor/ block, lines 11-12)
    - scripts/stage-batch-4.cjs (read full file — copy as base for stage-batch-5.cjs, 165 lines)
    - .planning/phases/40-stage-and-commit-maintenance-logs-state/40-PATTERNS.md (read full — contains exact patterns, expectedFiles set, logs/ handling, duplicate check form)
    - .planning/phases/40-stage-and-commit-maintenance-logs-state/40-CONTEXT.md (read D-01 through D-05 for decision constraints)
  </read_first>
  <action>
    Two files to produce:

    **.gitignore** — Add a new comment block immediately after the `.cursor/` block (after the blank line following `.cursor/`). The new block (per D-01): `# Antigravity CLI — local runtime config and session state (never commit)` then `.antigravity/` then `.antigravitycli/` then `.claudeignore` (no trailing slash on .claudeignore — it is a file, not a directory). Comment style matches the existing `.cursor/` comment block pattern from the PATTERNS.md analog.

    **scripts/stage-batch-5.cjs** — Copy scripts/stage-batch-4.cjs as the base, then apply these changes:

    1. Replace the duplicate commit detection string: change `'test: refactor core tests and SDK validation (Batch 4)'` to `'chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)'` and rename variable `batch4InHistory` to `batch5InHistory` (per D-05).

    2. Replace the `expectedFiles` Set construction: remove the `tests/` dynamic scan. Use a hardcoded Set with these exact entries (per D-03 file list in CONTEXT.md):
       - `.gitignore`
       - `.planning/v1.41.5-MILESTONE-AUDIT.md`
       - `bin/install.js`
       - `hooks/gsd-check-update-worker.js`
       - `hooks/gsd-statusline.js`
       - `scripts/gen-inventory-manifest.cjs`
       - `scripts/stage-batch-1.cjs`
       - `scripts/stage-batch-2.cjs`
       - `scripts/stage-batch-3.cjs`
       - `scripts/stage-batch-5.cjs`

    3. Handle the `logs/` untracked directory (per PATTERNS.md handling guidance): After the hardcoded expectedFiles Set, add a block that checks if `logs/` directory exists, runs `git add -f logs/` via execFileSync, then enumerates `fs.readdirSync(logsDir)` and adds each file as `logs/<filename>` to expectedFiles. This ensures subset verification accounts for actual log file paths, not the directory string.

    4. Replace all log messages referencing "Batch 4" with "Batch 5".

    5. Replace commit message argument in the final `git commit -m` call: use `'chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)'` (per D-05).

    6. Keep `ALLOW_ANY_BRANCH !== '1'` branch guard, `hasChangesSinceV1_41_2` helper, missing-file pre-flight, git reset, conditional staging loop, subset verification, and no-staged-files early exit — all unchanged from the stage-batch-4.cjs pattern.

    NOTE (per D-02): security.cjs and state.cjs are NOT in the expectedFiles set for Batch 5 — they go in a separate fix(lib) commit handled in Task 2.
  </action>
  <verify>
    <automated>
      grep -n '.antigravity' /home/thamw/development/remote-dev/get-shit-done/.gitignore && grep -n '.antigravitycli' /home/thamw/development/remote-dev/get-shit-done/.gitignore && grep -n '.claudeignore' /home/thamw/development/remote-dev/get-shit-done/.gitignore
    </automated>
    <automated>
      node -e "require('/home/thamw/development/remote-dev/get-shit-done/scripts/stage-batch-5.cjs')" 2>&1 | head -5; echo "syntax OK: $?"
    </automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains `.antigravity/`, `.antigravitycli/`, and `.claudeignore` as separate lines under a `# Antigravity CLI` comment block, positioned after the `.cursor/` block
    - .gitignore does NOT have a trailing slash on `.claudeignore`
    - scripts/stage-batch-5.cjs exists on disk and `node --check scripts/stage-batch-5.cjs` exits 0 (no syntax errors)
    - scripts/stage-batch-5.cjs contains `'chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)'` as the commit message string
    - scripts/stage-batch-5.cjs contains `git log --pretty=format:%s` (full log duplicate check, not `-n 1`)
    - scripts/stage-batch-5.cjs does NOT contain `security.cjs` or `state.cjs` in its expectedFiles (those are in the fix(lib) commit)
    - scripts/stage-batch-5.cjs contains `logs/` staging logic using `fs.readdirSync` to enumerate log files into expectedFiles
    - scripts/stage-batch-5.cjs contains `'scripts/stage-batch-5.cjs'` in its expectedFiles Set (self-referential, per D-04)
  </acceptance_criteria>
  <done>
    .gitignore updated with Antigravity exclusions. scripts/stage-batch-5.cjs created with correct file list, commit message, duplicate detection using full git log, logs/ directory handling, and self-referential entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute fix(lib) commit then run stage-batch-5.cjs for Batch 5 commit</name>
  <files>get-shit-done/bin/lib/security.cjs, get-shit-done/bin/lib/state.cjs</files>
  <read_first>
    - scripts/stage-batch-5.cjs (read the script created in Task 1 before executing to confirm it looks correct)
    - .planning/phases/40-stage-and-commit-maintenance-logs-state/40-CONTEXT.md (D-02 fix(lib) commit message and file list)
  </read_first>
  <action>
    Execute in this exact order (per PATTERNS.md execution ordering and D-02):

    Step 1 — Stage and commit the fix(lib) changes:
    Run `git add -f get-shit-done/bin/lib/security.cjs get-shit-done/bin/lib/state.cjs` to stage only the two lib files.
    Verify staged files with `git diff --cached --name-only` — must show exactly these two files and no others.
    Commit with message (per D-02): `fix(lib): regex boundary in security, cross-milestone progress tracking in state`

    Step 2 — Run Batch 5 script:
    Run `node scripts/stage-batch-5.cjs` from the repo root. The script performs: branch guard, duplicate check, git reset, stage all Batch 5 files (including logs/ files and itself), subset verification, then commits with `chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)`.

    Step 3 — Verify clean working tree:
    Run `git status` and confirm the output shows nothing to commit, working tree clean. If any untracked files remain, they must match entries already in .gitignore (i.e., .antigravity/, .antigravitycli/, .claudeignore, and the phase planning artifacts that are untracked by design).

    If `git status` still shows untracked files that are not gitignored and not planning artifacts (e.g., scripts/stage-batch-4.cjs which was already committed in Batch 4, or .planning/phases/ files), investigate whether those files were already committed in a prior batch or should be added to .gitignore. Do not proceed to Phase 41 until the tracked-file working tree is clean.
  </action>
  <verify>
    <automated>
      git -C /home/thamw/development/remote-dev/get-shit-done log --oneline -5
    </automated>
    <automated>
      git -C /home/thamw/development/remote-dev/get-shit-done diff --cached --name-only
    </automated>
    <automated>
      git -C /home/thamw/development/remote-dev/get-shit-done status --short
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -3` shows two new commits: the fix(lib) commit and the Batch 5 commit, in that order (fix(lib) below, Batch 5 on top)
    - `git log --pretty=format:%s` contains `fix(lib): regex boundary in security, cross-milestone progress tracking in state`
    - `git log --pretty=format:%s` contains `chore(maintenance): refactor quick tasks, logs, and state files (Batch 5)`
    - `git diff --cached --name-only` returns empty output (nothing left staged)
    - `git status --short` shows no modified tracked files (M lines absent); untracked lines (??) are only gitignored runtime dirs (.antigravity/, .antigravitycli/, .claudeignore) and any .planning/phases/ artifacts not yet committed by design
    - scripts/stage-batch-5.cjs appears in the Batch 5 commit (self-referential, per D-04): `git show --name-only HEAD | grep stage-batch-5.cjs` returns a match
    - `git show --name-only HEAD~1` includes `get-shit-done/bin/lib/security.cjs` and `get-shit-done/bin/lib/state.cjs` (the fix(lib) commit)
  </acceptance_criteria>
  <done>
    fix(lib) commit created with security.cjs and state.cjs. Batch 5 commit created with all remaining maintenance files. Working tree is clean of tracked file modifications.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → git index | stage-batch-5.cjs writes to the git index via execFileSync; unauthorized files could corrupt the commit if subset verification fails |
| working tree → committed history | fix(lib) commit must stage only two specific files; any over-staging would mix bug fixes with maintenance in the same commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | stage-batch-5.cjs → git index | mitigate | Subset verification aborts and resets index if unauthorized files are staged; execFileSync propagates non-zero exits |
| T-40-02 | Repudiation | fix(lib) commit staging | mitigate | Verify `git diff --cached --name-only` shows exactly two files before committing; abort if any other file appears |
| T-40-03 | Denial of Service | logs/ readdirSync | accept | logs/ is a local artifact directory; no PII, no external input; directory walk is bounded and local |
</threat_model>

<verification>
After both tasks complete:
1. `git log --oneline -5` shows the fix(lib) commit followed by the Batch 5 commit as the two most recent commits
2. `git show --name-only HEAD | grep -E '(stage-batch-5|logs/|install\.js|MILESTONE-AUDIT)'` returns matches confirming Batch 5 content
3. `git show --name-only HEAD~1 | grep -E '(security|state)\.cjs'` confirms fix(lib) content
4. `git status --short | grep -v '^?? .antigravity' | grep -v '^?? .antigravitycli' | grep -v '^?? .claudeignore' | grep -v '^?? .planning/phases/'` returns empty output (all tracked modifications committed)
5. `node --check scripts/stage-batch-5.cjs` exits 0
</verification>

<success_criteria>
- STAGE-05 requirement satisfied: .planning/quick/ (already committed in prior batches if applicable), logs/, and maintenance state files committed in Batch 5
- Two commits created: fix(lib) for security.cjs + state.cjs, and Batch 5 for all remaining maintenance files
- .gitignore excludes .antigravity/, .antigravitycli/, and .claudeignore
- scripts/stage-batch-5.cjs committed as part of Batch 5 (self-referential)
- Working tree clean of all tracked file modifications — ready for Phase 41 zero-diff parity audit
</success_criteria>

<output>
After completion, create `.planning/phases/40-stage-and-commit-maintenance-logs-state/40-01-SUMMARY.md`
</output>
