---
phase: 50-maintenance-script-and-cross-ref-scanner
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - tests/cross-file-step-refs.test.cjs
autonomous: true
requirements: [XREF-01]
must_haves:
  truths:
    - "D-04: Scanner detects both word-order variants of cross-file refs — `filename.md step N` AND `step N in filename.md`"
    - "D-04: Same-file refs are skipped — if the file referencing equals the file being referenced, the ref is internal and not classified as cross-file"
    - "D-05: Step existence check uses prose headings only — content inside ``` code fences is skipped on BOTH source-ref extraction AND target-step extraction (symmetric fence skip per RESEARCH.md Pitfall 3)"
    - "D-06: The RED test injects a synthetic stale reference into a TEMPORARY file via `fs.mkdtempSync(os.tmpdir())` — the corpus is never mutated"
    - "D-08: Plan 3 builds `tests/cross-file-step-refs.test.cjs` only — final plan of Phase 50"
    - "Auto-discovered by `scripts/run-tests.cjs` — no `package.json` or test-runner wiring change needed"
    - "GREEN against post-Phase-49 + post-Plan-1/Plan-2 clean corpus: the 3 existing refs (`execute-plan.md` → `execute-phase.md step 7`, lines 143, 369, 475) all resolve to a real Step 7 heading in `get-shit-done/workflows/execute-phase.md`"
    - "Step-set extraction recognizes both whole-integer headings (`## Step N`, `### Step N`, `**Step N:**`) and Pattern D ordered-list items at columns 0-2 (`N. `)"
  artifacts:
    - path: "tests/cross-file-step-refs.test.cjs"
      provides: "Cross-file step reference integrity scanner — per-source-file subtest asserts every prose cross-file ref points at a real step heading or Pattern D item in the target file"
      contains: "describe('corpus scan"
    - path: "tests/cross-file-step-refs.test.cjs"
      provides: "Synthetic-fixture RED test that injects a stale ref via `fs.mkdtempSync(os.tmpdir())` and confirms detection works"
      contains: "fs.mkdtempSync"
    - path: "tests/cross-file-step-refs.test.cjs"
      provides: "Module-scope ALL_FILES + SCAN_FILES collection identical to step-numbering-scan.test.cjs (scope synchronization with Phase 48 scanner)"
      contains: "PATTERN_C_EXCLUDES"
  key_links:
    - from: "tests/cross-file-step-refs.test.cjs corpus subtest block"
      to: "Per-file findCrossFileRefs + extractStepSet evaluation"
      via: "For each source file in SCAN_FILES: find refs → for each ref → load target file → compare ref.step against target step set → assert membership"
      pattern: "corpus scan — cross-file step refs"
    - from: "tests/cross-file-step-refs.test.cjs RED test"
      to: "Synthetic temp-file fixture under os.tmpdir()"
      via: "fs.mkdtempSync + fs.writeFileSync inject `execute-phase.md step 999` (or similar guaranteed-nonexistent step) and assert findCrossFileRefs flags it"
      pattern: "synthetic stale ref"
    - from: "tests/cross-file-step-refs.test.cjs scope constants"
      to: "tests/step-numbering-scan.test.cjs SCAN_DIRS + PATTERN_C_EXCLUDES"
      via: "Copy literals to keep scanner scope synchronized; future scope changes must update both files in lock-step"
      pattern: "SCAN_DIRS"
---

<objective>
Plan 3 of Phase 50 (per D-08). Build `tests/cross-file-step-refs.test.cjs`, a new test/scanner that:

1. Walks the same `SCAN_DIRS` / `PATTERN_C_EXCLUDES` scope as `tests/step-numbering-scan.test.cjs` to keep coverage synchronized.
2. For every source file, extracts prose cross-file refs using two regexes (per D-04): `filename.md step N` AND `step N in filename.md`.
3. Skips same-file refs by comparing source-file basename to matched target-file basename (per D-04 + RESEARCH.md Pitfall 4).
4. For each remaining cross-file ref, loads the target file, extracts its real step set (whole-integer headings + Pattern D items at columns 0-2, skipping code fences per D-05), and asserts the ref's step number is a member.
5. Reports any stale ref via a per-source-file subtest failure with line numbers and ref context.
6. Includes a synthetic-fixture RED test using `fs.mkdtempSync(os.tmpdir())` to inject a guaranteed-stale ref into a temp file and confirm detection works (per D-06).
7. Goes GREEN against the post-Phase-49 + post-Plan-1/Plan-2 clean corpus on every CI run, locking in the cross-file integrity invariant for future upstream merges (per XREF-01).

Purpose (XREF-01): Future upstream merges that introduce or break cross-file step references must fail CI immediately — no more manual cross-file ref audits each merge cycle.

Output: `tests/cross-file-step-refs.test.cjs` (new file, auto-discovered by `scripts/run-tests.cjs`). Plan 3 completes Phase 50.
</objective>

<execution_context>
Required reading before writing any code:
- `tests/step-numbering-scan.test.cjs` (full file, 318 lines) — primary template: imports/constants (lines 28-47), module-scope ALL_FILES + SCAN_FILES (lines 49-63), `collectMarkdownFiles` (lines 168-186), code-fence toggle (lines 83-94), per-file corpus subtest pattern (lines 275-318)
- `tests/negative-framing-scan.test.cjs` (focused reads at lines 1-30, 374-392, 566-685, 802-901) — secondary template: synthetic-content `describe` pattern (lines 566-578), corpus subtest variant (lines 812-833)
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md` — §Code Examples Example 3 (cross-file scanner skeleton), Example 4 (RED test stale-ref injection), §Common Pitfalls Pitfalls 2, 3, 4
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md` — §`tests/cross-file-step-refs.test.cjs` (NEW — Plan 3) for line-by-line guidance, §Shared Patterns
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-CONTEXT.md` — D-04 (two word-order variants, same-file skip), D-05 (code-fence skip on both sides), D-06 (temp-file RED test), D-08 (Plan 3 scope)
- `scripts/normalize-step-numbers.cjs` (created in Plan 2) — its `XREF_PATTERNS` regex pair is the contract Plan 3 must mirror (single source of truth for ref discovery; future drift in either file invalidates the other)
- `scripts/run-tests.cjs` — confirm test auto-discovery; no need to modify
- Empirical corpus cross-file refs (verified 2026-05-30, post-Phase-49 clean state):
  - `get-shit-done/workflows/execute-plan.md:143` → `execute-phase.md step 7`
  - `get-shit-done/workflows/execute-plan.md:369` → `execute-phase.md step 7`
  - `get-shit-done/workflows/execute-plan.md:475` → `execute-phase.md step 7`
  All three must resolve to a real `Step 7` heading in `get-shit-done/workflows/execute-phase.md` — the scanner GREEN baseline.

Use the Write tool to create the new file in one pass. Mirror the structural template of `tests/step-numbering-scan.test.cjs` exactly — same imports, same constants, same file-collection idiom — only the detection / assertion logic differs.

Verification protocol (per CLAUDE.md): run tests once, pipe to `/tmp`, then read the file. Avoid re-running `npm test` repeatedly.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/cross-file-step-refs.test.cjs — corpus subtests + helper functions</name>
  <files>tests/cross-file-step-refs.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (full file — primary structural template; copy imports lines 28-31, SCAN_DIRS lines 36-40, PATTERN_C_EXCLUDES lines 43-47, module-scope ALL_FILES/SCAN_FILES lines 49-63, collectMarkdownFiles lines 168-186, code-fence toggle pattern lines 83-94, corpus subtest block lines 275-288)
    - tests/negative-framing-scan.test.cjs (lines 1-30 for imports, lines 374-392 for the duplicated collectMarkdownFiles helper, lines 812-833 for an alternate corpus-subtest single-loop variant)
    - scripts/normalize-step-numbers.cjs (the file just created in Plan 2 — confirm the two XREF_PATTERNS regexes are written as `[/([a-z0-9_./-]+\.md)\s+step\s+(\d+(?:\.\d|[a-z])?)/gi, /step\s+(\d+(?:\.\d|[a-z])?)\s+in\s+([a-z0-9_./-]+\.md)/gi]`; Plan 3's regexes for the scanner should match the same shape but may capture only `(\d+)` since the scanner only validates whole-integer membership)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md (§Code Examples Example 3; §Common Pitfalls Pitfalls 2, 3, 4)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md (§`tests/cross-file-step-refs.test.cjs` (NEW — Plan 3))
  </read_first>
  <action>
    Create `tests/cross-file-step-refs.test.cjs` as a new file. Required structure (mirroring `tests/step-numbering-scan.test.cjs` for scope + collection, with new detection logic):

    1. `'use strict';` at the top.
    2. Module docstring as a JSDoc block: name the file, cite XREF-01, D-04, D-05, D-06, D-08, and explain the invariant: "Every prose cross-file reference `<file>.md step N` (or `step N in <file>.md`) in SCAN_DIRS must point at a real Step N heading or Pattern D item in the target file. Same-file refs are skipped. Code-fenced refs and code-fenced target steps are skipped (per D-05). Future upstream merges that break this invariant will fail CI."
    3. Imports:
       - `const { describe, test } = require('node:test');`
       - `const assert = require('node:assert/strict');`
       - `const fs = require('fs');`
       - `const path = require('path');`
       - `const os = require('os');` — required for the RED-test temp-file injection per D-06
    4. Constants:
       - `PROJECT_ROOT = path.join(__dirname, '..')`
       - `SCAN_DIRS = ['agents', 'get-shit-done/workflows', 'commands/gsd']` (literal copy from `tests/step-numbering-scan.test.cjs:36-40`)
       - `PATTERN_C_EXCLUDES = new Set([...])` (literal copy from `tests/step-numbering-scan.test.cjs:43-47`)
       - `XREF_PATTERNS = [ /([a-z0-9_./-]+\.md)\s+step\s+(\d+)/gi, /step\s+(\d+)\s+in\s+([a-z0-9_./-]+\.md)/gi ]` — two word-order variants per D-04. Capture groups differ between the two: variant 1 → m[1]=file, m[2]=step; variant 2 → m[1]=step, m[2]=file. Document this asymmetry inline.
    5. `collectMarkdownFiles(dir)` helper duplicated inline from `tests/step-numbering-scan.test.cjs:168-186` (per Phase 48 D-06 — do NOT add to `tests/helpers.cjs`).
    6. Module-scope file enumeration: `ALL_FILES`, then `SCAN_FILES = ALL_FILES.filter(...)` — identical to `tests/step-numbering-scan.test.cjs:55-63`. Build a second index for fast target lookup: `const FILES_BY_BASENAME = new Map()` keyed by `path.basename(file)` → full absolute path, populated from `ALL_FILES` (not filtered by PATTERN_C_EXCLUDES — target files may include Pattern C if a ref points at them, but the scanner only ASSERTS over SCAN_FILES as the source set).
    7. `extractStepSet(content)` function returning a `Set<number>` of valid step labels in a target file:
       - Walk lines with the standard `inCodeBlock` toggle (per D-05 — skip fenced content).
       - Heading match: `/^(?:#{1,6}\s+|\*?\*?)Step\s+(\d+)(?!\d|\.|[a-z])/i` — matches `## Step N`, `### Step N`, `**Step N:**`, plain `Step N:` (whole integer only — negative lookahead excludes decimals and letter suffixes).
       - Pattern D match: `/^\s{0,2}(\d+)\.\s/` — ordered-list item at column 0-2, captures the integer.
       - Add `parseInt(match[1], 10)` to the Set on either match. Use `assert`-safe assertions inside any internal sanity check (RESEARCH.md §Anti-Patterns — never bare `assert.ok(indexOf(...))`).
    8. `findCrossFileRefs(sourceFile, content)` function returning `Array<{ lineNumber, targetBasename, step, context }>`:
       - Walk lines with the standard `inCodeBlock` toggle (per D-05 — symmetric fence skip on source side; per RESEARCH.md Pitfall 3).
       - For each non-fenced line, run BOTH `XREF_PATTERNS` regexes (`re.lastIndex = 0` before each `re.exec` loop — `/g` regexes carry state).
       - For each match, determine capture-group mapping: if `m[1]` ends with `.md`, then `m[1]=file, m[2]=step`; else `m[1]=step, m[2]=file`.
       - Extract `targetBasename = path.basename(matchedFile)` and `stepNum = parseInt(matchedStep, 10)`. If `parseInt` returns NaN (e.g., decimal step), skip — the cross-file scanner validates whole-integer refs only (decimal refs are the normalize script's domain).
       - Skip same-file refs (per D-04 + RESEARCH.md Pitfall 4): if `path.basename(sourceFile) === targetBasename` AND `path.relative(PROJECT_ROOT, sourceFile).split(path.sep).join('/').endsWith('/' + targetBasename)`, continue. Both checks must agree before classifying as same-file.
       - Push `{ lineNumber: i + 1, targetBasename, step: stepNum, context: lines[i].trim() }`.
    9. Corpus subtest block:
       ```
       describe('corpus scan — cross-file step refs point at existing steps', () => {
         for (const file of SCAN_FILES) { ... per-file test ... }
       });
       ```
       Inside each subtest:
       - Read source content.
       - Call `findCrossFileRefs(file, content)` → list of refs.
       - For each ref: look up target absolute path via `FILES_BY_BASENAME.get(ref.targetBasename)`. If the target is missing from the map, push to `unresolvedTargets` (a missing target file is itself a violation — the ref points at a file that does not exist anywhere in the corpus).
       - For resolvable refs: read target content, compute `extractStepSet(targetContent)`, filter refs where `!targetSteps.has(ref.step)` → push to `staleRefs`.
       - Assert `staleRefs` is empty with a descriptive message including ref source, line number, target file, missing step number, and the line context.
       - Assert `unresolvedTargets` is empty with a similar descriptive message.
    10. (No synthetic-content unit tests in Task 1 — those land in Task 2 along with the RED test, to keep Task 1's commit focused on the corpus-integrity contract.)

    Anti-pattern guards (per RESEARCH.md §Anti-Patterns and §Common Pitfalls):
    - Apply the code-fence toggle SYMMETRICALLY: in `extractStepSet` AND in `findCrossFileRefs` (Pitfall 3). Both functions must skip content inside ``` fences.
    - Use `assert.deepStrictEqual(staleRefs, [], msg)` — never `assert.ok(staleRefs.length === 0)` (silent truthy semantics for non-zero counts).
    - Discover cross-file refs dynamically — do not read any prior `.planning/phases/49-*/49-MAP-01.md` manifest.
    - For same-file ref classification (Pitfall 4): require BOTH the basename equality check AND the path-suffix endsWith check to agree before classifying as same-file. Naive `endsWith` alone produces false negatives on basename collisions across different paths.
    - Reset `re.lastIndex = 0` before each `while ((m = re.exec(line)) !== null)` loop — global-flag regexes carry mutable state across calls.
    - Treat NaN step numbers (from a decimal ref like `step 7.5`) as out-of-scope for this scanner — they are the normalize script's domain; cross-file integrity validates whole-integer refs only.

    Empirical baseline check (per RESEARCH.md §Validation Architecture):
    - The clean corpus has exactly 3 cross-file refs (`get-shit-done/workflows/execute-plan.md` lines 143, 369, 475 → `execute-phase.md step 7`).
    - All three must resolve: `Step 7` must appear as a heading or Pattern D item in `get-shit-done/workflows/execute-phase.md`.
    - Subtest result: `pass` for every SCAN_FILE; zero stale refs reported.
  </action>
  <verify>
    <automated>node --test tests/cross-file-step-refs.test.cjs 2>&1 | tee /tmp/gsd-xref-plan-3-task-1.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/cross-file-step-refs.test.cjs` exists as a regular file (verify with `[ -f tests/cross-file-step-refs.test.cjs ] && echo OK`)
    - File contains `'use strict';` on the first non-comment line (verify with `command grep -c \"^'use strict';\" tests/cross-file-step-refs.test.cjs` reports `1`)
    - File contains `const os = require('os');` for the upcoming RED test (verify with `command grep -c \"require('os')\" tests/cross-file-step-refs.test.cjs` reports at least `1`)
    - File contains the SCAN_DIRS literal `'get-shit-done/workflows'` (verify with `command grep -c \"'get-shit-done/workflows'\" tests/cross-file-step-refs.test.cjs` reports at least `1`)
    - File contains `PATTERN_C_EXCLUDES = new Set([` (verify with `command grep -c 'PATTERN_C_EXCLUDES = new Set' tests/cross-file-step-refs.test.cjs` reports `1`)
    - File contains both XREF_PATTERNS regex variants — verify with `command grep -c '/gi' tests/cross-file-step-refs.test.cjs` reports at least `2`
    - File contains `function extractStepSet(content)` (verify with `command grep -c 'function extractStepSet' tests/cross-file-step-refs.test.cjs` reports `1`)
    - File contains `function findCrossFileRefs(sourceFile, content)` (verify with `command grep -c 'function findCrossFileRefs' tests/cross-file-step-refs.test.cjs` reports `1`)
    - File contains `function collectMarkdownFiles(dir)` duplicated inline (verify with `command grep -c 'function collectMarkdownFiles(dir)' tests/cross-file-step-refs.test.cjs` reports `1`)
    - File contains the corpus subtest describe block (verify with `command grep -c \"describe('corpus scan\" tests/cross-file-step-refs.test.cjs` reports at least `1`)
    - File contains the code-fence toggle pattern in BOTH helper functions (verify by reading `extractStepSet` and `findCrossFileRefs` bodies and confirming each includes `if (/^\`\`\`/.test(trimmed)) { inCodeBlock = !inCodeBlock; continue; }`)
    - File does NOT modify `tests/helpers.cjs` (verify with `git status --porcelain tests/helpers.cjs` returns empty — the helper duplication is intentional per Phase 48 D-06)
    - File contains the same-file skip with both checks (verify by reading `findCrossFileRefs` body and confirming both `path.basename(sourceFile) === targetBasename` and `path.relative(...).endsWith('/' + targetBasename)` appear)
    - `node --test tests/cross-file-step-refs.test.cjs` exits 0 (verify exit code via `echo "Exit: $?"` shows `Exit: 0`)
    - The captured output `/tmp/gsd-xref-plan-3-task-1.txt` contains `# fail 0`
    - The captured output shows at least 1 corpus subtest passed (i.e., the scanner ran against `SCAN_FILES` and did not error)
    - The 3 known clean-corpus refs (`get-shit-done/workflows/execute-plan.md` → `execute-phase.md step 7`) all resolve GREEN — confirm by reading the test output and ensuring no subtest for `execute-plan.md` is reported as failed
    - `scripts/run-tests.cjs` auto-discovers the new file (verify with `npm test 2>&1 | tee /tmp/gsd-full-plan-3-task-1.txt; echo "Exit: $?"` showing `tests/cross-file-step-refs.test.cjs` runs as part of the full suite; full suite exits 0)
  </acceptance_criteria>
  <done>
    `tests/cross-file-step-refs.test.cjs` exists with corpus subtests that GREEN against the clean post-Phase-49 + post-Plan-2 corpus. The three known cross-file refs in `execute-plan.md` resolve to real Step 7 in `execute-phase.md`. The synthetic RED test is added in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add synthetic-fixture RED test + helper unit tests for extractStepSet and findCrossFileRefs</name>
  <files>tests/cross-file-step-refs.test.cjs</files>
  <read_first>
    - tests/cross-file-step-refs.test.cjs (the file created in Task 1 — re-read end-to-end to confirm helper signatures before writing tests against them)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md (§Code Examples Example 4 — exact RED-test temp-file pattern; §Common Pitfalls Pitfalls 3, 4)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md (§RED-test stale-ref injection)
    - tests/negative-framing-scan.test.cjs (lines 566-685 — synthetic-content describe pattern for inline unit tests on helper functions)
  </read_first>
  <action>
    Extend `tests/cross-file-step-refs.test.cjs` (no new file — append to the file created in Task 1) with two new describe blocks of synthetic-content tests. Order: helper unit tests first, then the RED test last (per RESEARCH.md three-layer test design recommendation in §Open Questions Q3).

    Required additions:

    1. ADD `describe('extractStepSet() — synthetic content', () => { ... })` block with at least these unit tests:
       - `test('detects whole-integer **Step N:** heading', ...)`: fixture `'**Step 5:** thing\n'` → assert returned Set has `5` and size 1.
       - `test('detects ## Step N heading', ...)`: fixture `'## Step 7\n'` → assert Set has `7`.
       - `test('detects Pattern D ordered-list item at column 0', ...)`: fixture `'5. **Worktree cleanup**\n'` → assert Set has `5`.
       - `test('detects Pattern D ordered-list item at column 2', ...)`: fixture `'  5. **Nested**\n'` → assert Set has `5`.
       - `test('skips Pattern D inside code fence (per D-05)', ...)`: fixture `'\`\`\`\n5. inside fence\n\`\`\`\n'` → assert Set size 0.
       - `test('skips **Step N:** inside code fence (per D-05 — symmetric)', ...)`: fixture `'\`\`\`\n**Step 5:** inside fence\n\`\`\`\n'` → assert Set size 0.
       - `test('skips decimal labels (whole-integer only)', ...)`: fixture `'**Step 5.5:** decimal\n'` → assert Set size 0 (negative lookahead in extractStepSet heading regex).
       - `test('skips letter-suffix labels (whole-integer only)', ...)`: fixture `'**Step 5a:** suffix\n'` → assert Set size 0.

    2. ADD `describe('findCrossFileRefs() — synthetic content', () => { ... })` block with at least these unit tests:
       - `test('detects filename.md step N variant', ...)`: pass a fake `sourceFile = '/tmp/fake/source.md'` and content `'See execute-phase.md step 7 for the flow.\n'` → assert returned array has length 1, element targetBasename === 'execute-phase.md', step === 7.
       - `test('detects step N in filename.md variant', ...)`: same fake source, content `'See step 7 in execute-phase.md for the flow.\n'` → assert length 1, same target + step.
       - `test('skips same-file refs (D-04)', ...)`: `sourceFile = path.join(PROJECT_ROOT, 'get-shit-done/workflows/execute-phase.md')`, content `'See execute-phase.md step 5 here.\n'` → assert length 0 (same-file skip).
       - `test('skips refs inside code fences (per D-05 — symmetric source-side)', ...)`: fake source, content `'\`\`\`\nSee execute-phase.md step 999 inside fence.\n\`\`\`\n'` → assert length 0.
       - `test('skips decimal step refs (whole-integer only)', ...)`: fake source, content `'See execute-phase.md step 5.5 for decimal.\n'` → assert length 0 (NaN guard) or length 1 depending on implementation choice in Task 1; if Task 1 chose to flag decimal refs, the test asserts length 1 and the behavior is documented. Pick one direction and document it in the test message.

    3. ADD `describe('cross-file scanner — RED test (synthetic stale ref)', () => { ... })` block (per D-06 — the canonical RED test, must use `fs.mkdtempSync(os.tmpdir())`):
       Per RESEARCH.md Example 4:
       - Inside the test body: create a temp directory via `const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-xref-red-'));`.
       - Write a synthetic source file: `const synthFile = path.join(tmpDir, 'synthetic-source.md');` with content `'# Synthetic test\n\nSee execute-phase.md step 999 for more.\n'`.
       - Wrap the body in `try { ... } finally { fs.rmSync(tmpDir, { recursive: true, force: true }); }` to guarantee cleanup even on assertion failure.
       - Inside `try`:
         - Read `get-shit-done/workflows/execute-phase.md` content.
         - Call `extractStepSet(targetContent)` → assert `!targetSteps.has(999)` (sanity check — Step 999 must NOT exist in the real corpus, otherwise the RED test cannot trigger).
         - Read `synthFile` content and call `findCrossFileRefs(synthFile, sourceContent)` → assert length 1, step === 999, targetBasename === 'execute-phase.md'.
         - Filter `refs.filter(r => !targetSteps.has(r.step))` → assert length 1 (the stale ref to Step 999 must be flagged as missing in the target step set).

    Anti-pattern guards (per RESEARCH.md §Anti-Patterns and §Common Pitfalls):
    - Always wrap temp-file fixtures in `try/finally` with `fs.rmSync(tmpDir, { recursive: true, force: true })` so the temp directory is cleaned even if assertions fail (CI hygiene).
    - Use `assert.equal(refs.length, 1, msg)` — never `assert.ok(refs.length)` (silent truthy `0`/`-1` semantics).
    - Sanity-check `!targetSteps.has(999)` before asserting the RED outcome — if a future upstream merge introduces an actual Step 999, the sanity assertion fails first with a clear message rather than producing a confusing false-positive on the main assertion.
    - Use a step number that is guaranteed unrealistic in the corpus (999 is safe; the largest real step in any corpus file is well below 100).

    The Task 2 verify command must show every new test passing. After Task 2, the total file subtest count is the Task 1 baseline plus the new helper unit tests (8 in `extractStepSet`, 5 in `findCrossFileRefs`, 1 RED test = 14 additions; corpus subtests unchanged).
  </action>
  <verify>
    <automated>node --test tests/cross-file-step-refs.test.cjs 2>&1 | tee /tmp/gsd-xref-plan-3-task-2.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/cross-file-step-refs.test.cjs` contains the describe block `describe('extractStepSet() — synthetic content'` (verify with `command grep -c \"describe('extractStepSet() — synthetic content'\" tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` contains the describe block `describe('findCrossFileRefs() — synthetic content'` (verify with `command grep -c \"describe('findCrossFileRefs() — synthetic content'\" tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` contains the describe block `describe('cross-file scanner — RED test (synthetic stale ref)'` (verify with `command grep -c 'RED test (synthetic stale ref)' tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` contains `fs.mkdtempSync(path.join(os.tmpdir()` (verify with `command grep -c 'fs.mkdtempSync(path.join(os.tmpdir()' tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` contains a `try { ... } finally { fs.rmSync(` block (verify with `command grep -c 'fs.rmSync(tmpDir' tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` contains the sanity assertion `!targetSteps.has(999)` (verify with `command grep -c '!targetSteps.has(999)' tests/cross-file-step-refs.test.cjs` reports `1`)
    - `tests/cross-file-step-refs.test.cjs` extractStepSet test count: at least 6 `test(` lines inside the `extractStepSet() — synthetic content` describe block (verify by reading the file and counting inline tests)
    - `tests/cross-file-step-refs.test.cjs` findCrossFileRefs test count: at least 4 `test(` lines inside the `findCrossFileRefs() — synthetic content` describe block
    - `tests/cross-file-step-refs.test.cjs` legacy assertion message check: zero occurrences of `assert.ok(.*length)` patterns that could silently pass with truthy non-1 values (verify by reading the file and confirming all assertions on array length use `assert.equal` or `assert.deepStrictEqual`)
    - The new file does NOT mutate any file under `agents/`, `commands/gsd/`, or `get-shit-done/workflows/` during test execution — verify with `git status --porcelain agents/ commands/gsd/ get-shit-done/workflows/` returning empty after the test run
    - The new file does NOT leave temp directories behind — verify with `ls /tmp/gsd-xref-red-* 2>/dev/null` returning no output (or, if it does return entries from earlier failed runs, document them as pre-existing)
    - `node --test tests/cross-file-step-refs.test.cjs` exits 0 with `# fail 0`
    - The captured output `/tmp/gsd-xref-plan-3-task-2.txt` shows pass count increased vs Task 1 baseline by at least 13 (8 extractStepSet + 4-5 findCrossFileRefs + 1 RED test)
    - Full `npm test 2>&1 | tee /tmp/gsd-full-plan-3-task-2.txt; echo "Exit: $?"` exits 0 with no new failures attributable to Plan 3
  </acceptance_criteria>
  <done>
    The cross-file scanner has three-layer test coverage (unit tests on helpers, corpus integrity subtests, RED test on a synthetic stale ref). The RED test uses a `fs.mkdtempSync(os.tmpdir())` temp file per D-06 and never mutates the corpus. XREF-01 is satisfied. Phase 50 is complete — Plans 1, 2, 3 ship together as a maintenance-layer trio.
  </done>
</task>

</tasks>

<verification>
After both tasks: confirm Plan 3 ships one new test file and zero collateral changes:
- `git status --porcelain` shows `tests/cross-file-step-refs.test.cjs` as untracked / new and zero modifications under SCAN_DIRS or `tests/helpers.cjs`
- `node --test tests/cross-file-step-refs.test.cjs` exits 0
- `node --test tests/step-numbering-scan.test.cjs` still exits 0 with Plan 1 baseline pass count (632) — Plan 3 did not regress the Plan 1 scanner
- `node scripts/normalize-step-numbers.cjs --dry-run` still exits 0 with `No changes needed` — Plan 3 did not perturb Plan 2's idempotency
- `npm test 2>&1 | tee /tmp/gsd-full-suite-plan-3.txt; echo "Exit: $?"` exits 0 with no new failures attributable to Plan 3
- The captured full-suite output mentions `tests/cross-file-step-refs.test.cjs` (auto-discovery confirmed)
</verification>

<success_criteria>
- `tests/cross-file-step-refs.test.cjs` exists, auto-discovered by `scripts/run-tests.cjs`
- Both word-order ref variants detected (per D-04)
- Same-file refs skipped via dual-check (per D-04 + Pitfall 4)
- Code-fence skip applied symmetrically in `extractStepSet` and `findCrossFileRefs` (per D-05 + Pitfall 3)
- RED test injects via `fs.mkdtempSync(os.tmpdir())` and never mutates the corpus (per D-06)
- Three-layer test design: helper unit tests + corpus integrity subtests + RED test
- GREEN against post-Phase-49 + post-Plan-1/Plan-2 clean corpus
- XREF-01 satisfied: stale cross-file step refs will fail CI on future upstream merges
- `tests/helpers.cjs` untouched (Phase 48 D-06 preserved)
- Full `npm test` exits 0 with no regressions
</success_criteria>

<output>
Update STATE.md after completion. Create `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-03-SUMMARY.md` documenting the new scanner's design, the three-layer test structure, and confirmation that XREF-01 is satisfied. Update ROADMAP.md to mark Phase 50 plans complete (3/3).
</output>
