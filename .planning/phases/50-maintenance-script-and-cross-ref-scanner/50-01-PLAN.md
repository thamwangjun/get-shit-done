---
phase: 50-maintenance-script-and-cross-ref-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/step-numbering-scan.test.cjs
autonomous: true
requirements: [NORM-02]
must_haves:
  truths:
    - "D-07: scanForOutOfOrder anchor hardening lands as a standalone first plan — before normalize script or cross-file scanner build on top of it"
    - "scanForOutOfOrder detects step labels preceded by list markers (`- **Step N:**`, `* **Step N:**`, `+ **Step N:**`, `1. **Step N:**`) and blockquotes (`> **Step N:**`)"
    - "The G-01 limitation unit test (lines 266-270) is FLIPPED — it now asserts list-marker-prefixed reversed steps ARE detected, not that they are missed"
    - "All 7 corpus subtest blocks remain GREEN against the post-Phase-49 clean corpus (629 subtests baseline)"
    - "`Step 0` remains a valid starting label after the anchor change (Phase 48 D-04 preserved)"
    - "Code-fence skip toggle still suppresses Step labels inside ``` fences (no regression)"
    - "Section-reset behavior on `##`/`###` headings still resets `expectedNext` to null"
  artifacts:
    - path: "tests/step-numbering-scan.test.cjs"
      provides: "Hardened scanForOutOfOrder with list-marker stripping + permissive anchor; flipped G-01 limitation test renamed and asserting detection works; companion blockquote, asterisk-list, and numbered-list unit tests"
      contains: "line.replace(/^(\\s*(?:[-*+]|\\d+\\.|>)\\s*)+/"
  key_links:
    - from: "tests/step-numbering-scan.test.cjs scanForOutOfOrder"
      to: "scanForOutOfOrder corpus subtest block (lines 305-318)"
      via: "shared function — corpus regression gate proves anchor change did not break clean corpus"
      pattern: "corpus scan — out-of-order step numbering"
    - from: "tests/step-numbering-scan.test.cjs (flipped G-01 unit test)"
      to: "scanForOutOfOrder line-iteration loop"
      via: "synthetic list-marker fixture asserts new strip + anchor combination detects the violations"
      pattern: "list-marker-prefixed reversed steps are now detected"
---

<objective>
Plan 1 of Phase 50 (per D-07). Harden `scanForOutOfOrder()` in `tests/step-numbering-scan.test.cjs` so that step labels preceded by list markers (`- `, `* `, `+ `, `1. `) and blockquote markers (`> `) are detected when out of order. Flip the existing G-01 limitation unit test (lines 266-270) from asserting the limitation EXISTS to asserting detection WORKS. Add companion synthetic tests for blockquote, asterisk-list, and numbered-list prefixes. Confirm all 629 existing subtests remain GREEN on the post-Phase-49 clean corpus.

Purpose (per D-07): The scanner is the foundation that Plans 2 and 3 build on top of. Hardening it first ensures the normalize script and cross-file scanner inherit a complete, correct out-of-order detector. NORM-02's idempotency guarantee depends on the scanner correctly classifying clean vs. dirty corpus — a hidden G-01 limitation would let dirty input pass the scanner and then surprise the normalize script.

Output: Modified `tests/step-numbering-scan.test.cjs` with hardened `scanForOutOfOrder` and an updated G-01 unit test that asserts the new behavior, plus three new companion unit tests.
</objective>

<execution_context>
Required reading before editing:
- `tests/step-numbering-scan.test.cjs` (full file) — read scanForOutOfOrder (lines 118-166), the KNOWN LIMITATION comment (lines 144-148), the existing G-01 unit test (lines 266-270), the corpus subtest blocks (lines 275-318)
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md` §Code Examples Example 1 — exact anchor replacement
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md` §`tests/step-numbering-scan.test.cjs` (MODIFIED — Plan 1) — line-by-line guidance for the edit
- `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-CONTEXT.md` — D-07 (Plan 1 is standalone and lands first)

Use the Edit tool. Do not rewrite the file end-to-end. Two edits are required: (a) the anchor inside `scanForOutOfOrder` (after the code-fence and section-reset blocks, before the `if (stepMatch)` block), (b) the unit test block at lines 266-270 — rename, flip assertion, add three companion tests for blockquote, asterisk-list, and numbered-list prefixes.

Verification protocol (per CLAUDE.md): run tests once, pipe to `/tmp`, then read the file. Avoid re-running `npm test` repeatedly when reading the captured output suffices.
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Harden scanForOutOfOrder anchor with list-marker/blockquote stripping</name>
  <files>tests/step-numbering-scan.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (full file — read scanForOutOfOrder lines 118-166, KNOWN LIMITATION comment lines 144-148, code-fence toggle lines 128-133, section-reset lines 135-139, and the corpus subtest block at lines 305-318 to confirm regression surface)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md (Example 1, §Common Pitfalls Pitfall 1 — anchor permissive-widening risk)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md (§`tests/step-numbering-scan.test.cjs` (MODIFIED — Plan 1))
  </read_first>
  <action>
    Inside `scanForOutOfOrder()` (currently `tests/step-numbering-scan.test.cjs:118-166`), per D-07 and RESEARCH.md Example 1, replace the single-line `stepMatch` assignment at line 149 with a two-step strip-then-match approach:

    1. First strip leading list markers and blockquote markers from the line. Use this exact stripping regex literal: `/^(\s*(?:[-*+]|\d+\.|>)\s*)+/` — applied with `String.prototype.replace` against the current `line` variable (the raw line, not `trimmed`). The replacement string is empty (`''`). This handles `- `, `* `, `+ `, `1. `, `2. `, `> ` and any combination preceded by whitespace.
    2. Then match the stripped result against the permissive anchor literal `/^[\s*]*Step\s+(\d+)(?![\.\da-z])/i` (the only character-class change vs current: `\s*\*?\*?` becomes `[\s*]*`).
    3. Keep the rest of the `if (stepMatch)` block (parseInt + expectedNext tracking + violation push) byte-identical.

    Also UPDATE the KNOWN LIMITATION comment block (currently lines 144-148): keep the historical context wording (`upstream merges could introduce them`), but rewrite the tense from "should replace ... should add" to a description of what WAS done — note the new anchor `^[\s*]*` and the list-marker/blockquote stripping. Reference Phase 50 Plan 1 as the source of the change. Do not delete the comment block; the historical context is valuable for future maintainers.

    Anti-pattern guard (per RESEARCH.md Pitfall 1 — anchor permissive-widening risk): the new anchor `[\s*]*` is permissive — after the edit, the corpus regression gate in the `<verify>` block must show all 7 corpus subtest blocks still GREEN (629 subtests baseline). If any previously-passing file flips to RED, the anchor matched something it should not — narrow the strip regex before re-running.

    Per D-07: this is a standalone first plan; no new artifact creation, only an in-place edit to one function inside one existing file.

    Skip modifications to `scanContent()`, `collectMarkdownFiles()`, the module-scope file collection at lines 49-63, and any of the existing `describe(...)` blocks above line 266. Task 2 handles the unit test flip.
  </action>
  <verify>
    <automated>node --test tests/step-numbering-scan.test.cjs 2>&1 | tee /tmp/gsd-step-scan-plan-1-task-1.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/step-numbering-scan.test.cjs` `scanForOutOfOrder` function contains the literal string `line.replace(/^(\s*(?:[-*+]|\d+\.|>)\s*)+/` (verify with `command grep -c 'line\.replace(/\^(' tests/step-numbering-scan.test.cjs` returns at least 1)
    - `tests/step-numbering-scan.test.cjs` `scanForOutOfOrder` function contains the new permissive anchor `/^[\s*]*Step\s+(\d+)(?![\.\da-z])/i` (verify by reading lines 140-160 and confirming the literal appears)
    - The legacy literal anchor `^\s*\*?\*?Step` no longer appears inside `scanForOutOfOrder` body (verify with `command grep -c '\\\\s\\*\\\\\\*?\\\\\\*?Step' tests/step-numbering-scan.test.cjs` reports `0`; if the comment block paraphrases the old anchor as text, that is allowed — the regex literal must be gone)
    - `node --test tests/step-numbering-scan.test.cjs` exits 0 with `# pass 629` (or `# pass 629` plus zero failures; in isolation for Task 1, the legacy G-01 test still passes at `length === 0`)
    - `scanContent()` is unchanged: `command grep -c 'function scanContent' tests/step-numbering-scan.test.cjs` reports `1` and reading lines 79-108 shows no diff vs current
    - The KNOWN LIMITATION comment block above the matcher still exists and now references Plan 1 / Phase 50 — verify by reading the file and confirming the comment retains the historical context wording (`upstream merges could introduce them`) and adds a Phase 50 attribution sentence
    - The captured test output `/tmp/gsd-step-scan-plan-1-task-1.txt` contains `# fail 0`
  </acceptance_criteria>
  <done>
    `scanForOutOfOrder` now strips list markers and blockquotes before applying a permissive anchor; all 629 existing subtests remain GREEN; the G-01 unit test still asserts `length === 0` at this point — Task 2 will flip it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip the G-01 limitation unit test and add companion blockquote/asterisk-list/numbered-list cases</name>
  <files>tests/step-numbering-scan.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (after Task 1's edits — re-read scanForOutOfOrder lines 118-170 and the existing G-01 unit test that was at lines 266-270 in the original numbering)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-RESEARCH.md (§Open Questions Q2 — explicit recommendation to flip the assertion)
    - .planning/phases/50-maintenance-script-and-cross-ref-scanner/50-PATTERNS.md (§Post-hardening direction — rename + flip + companion cases)
  </read_first>
  <action>
    Update the existing G-01 limitation unit test (currently `test('does not detect out-of-order steps preceded by list markers (known G-01 limitation)', ...)` at lines 266-270 of the pre-Task-1 file; line numbers may have shifted slightly after Task 1's edit). After Task 1's anchor change, the limitation no longer exists — the test must now assert that detection works.

    Required changes inside the `describe('scanForOutOfOrder() — synthetic content', ...)` block:

    1. RENAME the existing list-marker test from `'does not detect out-of-order steps preceded by list markers (known G-01 limitation)'` to `'detects out-of-order steps preceded by dash list markers'`. Keep the same fixture content (`['## Section', '- **Step 3:** reversed', '- **Step 1:** also reversed']`). Replace the body assertion `assert.equal(violations.length, 0, ...)` with `assert.equal(violations.length, 1, 'reversed list-marker steps are now detected — Step 1 after baseline Step 3 must produce one violation')`. Rationale for `length === 1`: when `expectedNext` is null the first step seen sets the baseline (Step 3 → expectedNext becomes 4) and pushes no violation; the second step (Step 1) mismatches `expectedNext=4` and pushes one violation. Confirm this count by re-reading `scanForOutOfOrder` lines 150-162 before writing the assertion — if the body logic was inadvertently changed in Task 1, re-derive.

    2. ADD a new companion test immediately after, named `'detects out-of-order steps preceded by numbered-list markers'`. Use fixture `['## Section', '1. **Step 3:** reversed', '2. **Step 1:** also reversed'].join('\n')`. Assert `violations.length === 1` with the same baseline-vs-mismatch rationale in the message.

    3. ADD a third companion test named `'detects out-of-order steps preceded by blockquote markers'`. Use fixture `['## Section', '> **Step 3:** reversed', '> **Step 1:** also reversed'].join('\n')`. Assert `violations.length === 1`.

    4. ADD a fourth companion test named `'detects out-of-order steps preceded by asterisk list markers'`. Use fixture `['## Section', '* **Step 3:** reversed', '* **Step 1:** also reversed'].join('\n')`. Assert `violations.length === 1`.

    All four tests live inside the existing `describe('scanForOutOfOrder() — synthetic content', () => { ... })` block. Place them at the end of that block, replacing the old G-01 test in place (the original test becomes test 1 of the four with its new name); the three new companion tests follow immediately after.

    Anti-pattern guard (per RESEARCH.md §Anti-Patterns to Avoid — silent truthy `-1`): do NOT write `assert.ok(violations.length)` — any positive count would pass silently if the renumbering logic regresses. Always use `assert.equal(violations.length, EXPECTED, msg)` with a numeric expected value and a descriptive message.

    Per D-07: this completes Plan 1. The corpus regression is gated by the `<automated>` verify command below; the full-suite check is implicit at the plan level.
  </action>
  <verify>
    <automated>node --test tests/step-numbering-scan.test.cjs 2>&1 | tee /tmp/gsd-step-scan-plan-1-task-2.txt; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - The string `detects out-of-order steps preceded by dash list markers` appears exactly once in `tests/step-numbering-scan.test.cjs` (verify with `command grep -c 'detects out-of-order steps preceded by dash list markers' tests/step-numbering-scan.test.cjs` reports `1`)
    - The string `detects out-of-order steps preceded by numbered-list markers` appears exactly once
    - The string `detects out-of-order steps preceded by blockquote markers` appears exactly once
    - The string `detects out-of-order steps preceded by asterisk list markers` appears exactly once
    - The legacy assertion message `known G-01 limitation` is removed (verify with `command grep -c 'G-01 limitation' tests/step-numbering-scan.test.cjs` reports `0`)
    - `node --test tests/step-numbering-scan.test.cjs` exits 0; pass count is `632` (629 baseline + 3 net new tests — original G-01 test renamed in place + 3 new companions)
    - The captured test output `/tmp/gsd-step-scan-plan-1-task-2.txt` contains `# fail 0`
    - Zero corpus subtests in the 3 describe blocks at lines 275-318 flip from GREEN to RED — confirm by reading the test runner output and ensuring no `corpus scan` test name appears under a failure summary
  </acceptance_criteria>
  <done>
    The G-01 limitation is no longer documented as a limitation; the unit-test suite now actively asserts the hardened anchor detects list-marker, blockquote, asterisk-list, and numbered-list prefixed reversed steps. Plan 1 is complete — the scanner is ready for Plan 2 (normalize script) and Plan 3 (cross-file scanner) to build on top of.
  </done>
</task>

</tasks>

<verification>
After both tasks: run `npm test 2>&1 | tee /tmp/gsd-full-suite-plan-1.txt; echo "Exit: $?"` and confirm:
- `# fail 0` in the step-numbering-scan section of the captured output
- Total subtest count for `tests/step-numbering-scan.test.cjs` is `632` (was `629`)
- No other test file flips RED due to the edit (the edit is localized to `scanForOutOfOrder`; no public API change)
</verification>

<success_criteria>
- `tests/step-numbering-scan.test.cjs` `scanForOutOfOrder` now uses `^[\s*]*Step\s+(\d+)(?![\.\da-z])/i` after stripping `^(\s*(?:[-*+]|\d+\.|>)\s*)+`
- The G-01 limitation test is flipped: list-marker-prefixed reversed step labels are now detected
- Three companion tests added for numbered-list, blockquote, and asterisk-list markers
- Full `npm test` exits 0 with no new failures
- Plan 2 and Plan 3 (later waves) can rely on the hardened scanner as a foundation
</success_criteria>

<output>
Update STATE.md after completion. Create `.planning/phases/50-maintenance-script-and-cross-ref-scanner/50-01-SUMMARY.md` documenting the anchor change, the test flip, and the new subtest count.
</output>
