---
phase: 48-tdd-red-gate
plan: 02
type: execute
wave: 1
depends_on: ["48-01"]
files_modified:
  - tests/step-numbering-scan.test.cjs
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02

must_haves:
  truths:
    - "scanContent() flags letter-suffix steps (e.g., Step 7a, Step 7b, Step 7c) as Pattern A/B violations (per gap closure: user decision to flag and renumber, not preserve)"
    - "The unit test 'does not flag letter-suffix step (Step 7a)' is replaced by 'flags letter-suffix step (Step 7a)' asserting patternAB.length === 1"
    - "agents/gsd-verifier.md appears in corpus Pattern A/B failure output (Step 2a, Step 2b, Step 2c, Step 3b, Step 4b, Step 7b, Step 7c, Step 9b trigger the expanded regex)"
    - "All previously-passing unit tests remain passing (7 scanContent tests green, 7 scanForOutOfOrder tests green)"
    - "The expanded regex still does not flag whole-integer steps (Step 7) and still flags decimal steps (Step 2.5, Step 7.0)"
    - "The STEP_DECIMAL_RE constant comment is updated to reflect that letter-suffix steps are now included as violations"
  artifacts:
    - path: "tests/step-numbering-scan.test.cjs"
      provides: "Step numbering scanner with letter-suffix detection"
      min_lines: 200
  key_links:
    - from: "tests/step-numbering-scan.test.cjs"
      to: "agents/gsd-verifier.md"
      via: "corpus Pattern A/B scan — letter-suffix steps now trigger STEP_DECIMAL_RE"
      pattern: "STEP_DECIMAL_RE"
---

<objective>
Close UAT gap: expand `scanContent()` in `tests/step-numbering-scan.test.cjs` to flag
letter-suffix step labels (e.g., Step 7a, Step 7b) as Pattern A/B violations.

Purpose: The original scanner excluded letter-suffix steps via `\d+\.\d` (requiring a digit
after the dot). The user decision (UAT test 7 issue) is that letter-suffix steps are violations
requiring renumbering to whole integers — the same enforcement applied to decimal steps.
Phase 49 will renumber them. Phase 48 must detect them.

Output: Modified `tests/step-numbering-scan.test.cjs` where:
1. `STEP_DECIMAL_RE` is expanded to match both `Step N.M` (decimal) and `Step Na` (letter-suffix)
2. The "does not flag Step 7a" unit test becomes "flags Step 7a" (asserts length === 1)
3. The file module-scope JSDoc lists `agents/gsd-verifier.md` as a new corpus failure
4. Running the test confirms `agents/gsd-verifier.md` now appears in corpus RED failures
</objective>

<context>
@.planning/phases/48-tdd-red-gate/48-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand STEP_DECIMAL_RE and update letter-suffix unit test</name>
  <files>tests/step-numbering-scan.test.cjs</files>

  <read_first>
    - tests/step-numbering-scan.test.cjs — read the full file before making any edits.
      Key locations:
      Line 67–69: STEP_DECIMAL_RE constant and comment block
      Line 204–207: the unit test "does not flag letter-suffix step (Step 7a)"
      Line 3–25: the module-level JSDoc (update RED expectation list)
  </read_first>

  <action>
Make three targeted edits to `tests/step-numbering-scan.test.cjs`:

**Edit 1 — Expand STEP_DECIMAL_RE (lines 66–69):**

Replace the comment and constant:

```
// Pattern A/B: bold or plain "Step N.M" labels with decimal point
// Guard: require \.[0-9] (excludes letter-suffix branches like "Step 7a")
// D-05: no indentation guard — all leading whitespace allowed
const STEP_DECIMAL_RE = /(?:^|\s|\*\*)Step\s+\d+\.\d/i;
```

With:

```
// Pattern A/B: bold or plain "Step N.M" decimal labels and "Step Na" letter-suffix labels.
// Both are violations requiring renumbering to whole integers (user decision, UAT gap closure).
// Matches either: digit-dot-digit (Step 7.0, Step 2.5) or digit-letter (Step 7a, Step 7b).
// D-05: no indentation guard — all leading whitespace allowed.
const STEP_DECIMAL_RE = /(?:^|\s|\*\*)Step\s+\d+(?:\.\d|[a-z])/i;
```

The change: `\.\d` becomes `(?:\.\d|[a-z])` — an alternation that matches either a dot+digit suffix OR a letter suffix. The `(?:^|\s|\*\*)` leading boundary is preserved unchanged (prevents mid-sentence cross-references like "see Step 7a" from matching only when not preceded by the right boundary — note this boundary is intentionally loose to catch most forms; the corpus cross-reference pattern differs from heading pattern).

**Edit 2 — Update the "does not flag Step 7a" unit test (inside describe('scanContent() — decimal detection', ...)):**

Replace:

```javascript
  test('does not flag letter-suffix step (Step 7a)', () => {
    const { patternAB } = scanContent('**Step 7a:** branch label\n');
    assert.equal(patternAB.length, 0, 'letter-suffix steps must not be flagged');
  });
```

With:

```javascript
  test('flags letter-suffix step (Step 7a) as violation', () => {
    const { patternAB } = scanContent('**Step 7a:** branch label\n');
    assert.equal(patternAB.length, 1, 'letter-suffix steps are violations requiring renumbering to whole integers');
  });
```

The assertion flips from `=== 0` to `=== 1` and the test name/message update to reflect the new design decision.

**Edit 3 — Update module-level JSDoc to add agents/gsd-verifier.md to RED expectation list (lines 18–25):**

Replace the Phase 48 RED expectation comment:

```
 * Phase 48 RED expectation: 6 files fail (5 primary + 1 cross-ref):
 *   - agents/gsd-intel-updater.md (Step 6.5)
 *   - agents/gsd-phase-researcher.md (Step 1.3, 1.5, 2.5, 2.6)
 *   - get-shit-done/workflows/progress.md (Step 1.5, 1.6)
 *   - get-shit-done/workflows/quick.md (Step 2.5, 4.5, 4.75, 5.5, 5.6, 6.25, 6.5)
 *   - get-shit-done/workflows/execute-phase.md (Pattern A/B 7.0-7.3, Pattern D 2.5/5.5-5.8)
 *   - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md (inline "step 5.8" ref)
```

With:

```
 * Phase 48 RED expectation: 7 files fail (letter-suffix detection added via UAT gap closure):
 *   - agents/gsd-verifier.md (Step 2a, Step 2b, Step 2c, Step 3b, Step 4b, Step 7b, Step 7c, Step 9b)
 *   - agents/gsd-intel-updater.md (Step 6.5)
 *   - agents/gsd-phase-researcher.md (Step 1.3, 1.5, 2.5, 2.6)
 *   - get-shit-done/workflows/progress.md (Step 1.5, 1.6)
 *   - get-shit-done/workflows/quick.md (Step 2.5, 4.5, 4.75, 5.5, 5.6, 6.25, 6.5)
 *   - get-shit-done/workflows/execute-phase.md (Pattern A/B 7.0-7.3, Pattern D 2.5/5.5-5.8)
 *   - get-shit-done/workflows/execute-phase/steps/post-merge-gate.md (inline "step 5.8" ref)
```

After all three edits, run the verify command below.
  </action>

  <verify>
    <automated>
cd /home/thamw/development/remote-dev/get-shit-done && node --test tests/step-numbering-scan.test.cjs 2>&1 | grep -E "(FAIL|PASS|gsd-verifier|Step 7a)" | head -40
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "(?:\\.\\\\d|[a-z])" tests/step-numbering-scan.test.cjs` returns at least 1 (alternation present in regex)
    - Running `node --test tests/step-numbering-scan.test.cjs` produces output containing a FAIL line for `agents/gsd-verifier.md`
    - Running `node --test tests/step-numbering-scan.test.cjs` does NOT produce a FAIL for `gsd-verifier.md` in the Pattern D or out-of-order blocks (letter-suffix only affects Pattern A/B)
    - The unit test "flags letter-suffix step (Step 7a) as violation" passes (PASS in output)
    - All other unit tests in `scanContent() — decimal detection` still pass
    - All 7 unit tests in `scanForOutOfOrder() — synthetic content` still pass
    - Previously-failing corpus files (gsd-intel-updater.md, gsd-phase-researcher.md, progress.md, quick.md, execute-phase.md) still appear as FAIL
    - `grep -c "does not flag letter-suffix" tests/step-numbering-scan.test.cjs` returns 0 (old test name removed)
  </acceptance_criteria>

  <done>
`tests/step-numbering-scan.test.cjs` detects letter-suffix step labels as violations.
`node --test tests/step-numbering-scan.test.cjs` output shows `agents/gsd-verifier.md`
failing the Pattern A/B corpus subtest. All unit tests (both describe blocks) pass GREEN.
All previously-failing corpus files continue to fail RED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → fs | Read-only access to committed .md files; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Tampering | Expanded regex catastrophic backtracking | accept | `(?:\.\d\|[a-z])` alternation is linear — no nested quantifiers, no ReDoS risk |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After the plan executes, confirm:

1. `node --test tests/step-numbering-scan.test.cjs 2>&1 | grep "gsd-verifier"` shows a FAIL line
2. `node --test tests/step-numbering-scan.test.cjs 2>&1 | grep "flags letter-suffix"` shows PASS
3. `node --test tests/step-numbering-scan.test.cjs 2>&1 | grep "does not flag letter-suffix"` returns nothing (old test name gone)
4. `node --test tests/step-numbering-scan.test.cjs 2>&1 | grep "gsd-intel-updater"` still shows FAIL (regression check)
5. `npm test 2>&1 | grep -E "^(not ok|ok) [0-9]+ .*step-numbering" | wc -l` is greater than 0 (test is in the suite)
</verification>

<success_criteria>
1. `STEP_DECIMAL_RE` uses `(?:\.\d|[a-z])` alternation — letter-suffix steps flagged as violations
2. Unit test "flags letter-suffix step (Step 7a) as violation" passes GREEN
3. `agents/gsd-verifier.md` fails the corpus Pattern A/B subtest (Step 2a, 2b, 2c, etc. detected)
4. All pre-existing corpus failures (6 files from Plan 01) continue to fail RED
5. No regressions in any other test in the describe blocks
</success_criteria>

<output>
Create `.planning/phases/48-tdd-red-gate/48-02-SUMMARY.md` when done.
</output>
