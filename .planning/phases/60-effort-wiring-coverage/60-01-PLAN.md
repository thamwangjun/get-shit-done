---
phase: 60-effort-wiring-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-56-effort-wiring.test.cjs
autonomous: true
requirements: [EWC-01, EWC-02, EWC-03, EWC-04, EWC-05, EWC-06, EWC-07, EWC-08]

must_haves:
  truths:
    - "A passing test asserts audit-fix.md contains resolve-model-effort gsd-executor and executor_model_effort_arg (EWC-01)"
    - "A passing test asserts diagnose-issues.md contains resolve-model-effort gsd-debugger and debugger_model_effort_arg (EWC-02)"
    - "A passing test asserts code-review.md contains resolve-model-effort gsd-code-reviewer and code_reviewer_model_effort_arg (EWC-03)"
    - "A single passing test asserts code-review-fix.md contains all four tokens for gsd-code-reviewer and gsd-code-fixer (EWC-04, D-12)"
    - "A passing test asserts explore.md contains resolve-model-effort gsd-phase-researcher and phase_researcher_model_effort_arg (EWC-05)"
    - "A passing test asserts import.md contains resolve-model-effort gsd-plan-checker and plan_checker_model_effort_arg (EWC-06)"
    - "A single passing test asserts ingest-docs.md contains all four tokens for gsd-doc-synthesizer and gsd-roadmapper (EWC-07, D-12)"
    - "A passing test asserts discuss-phase-assumptions.md contains resolve-model-effort gsd-assumptions-analyzer and assumptions_analyzer_model_effort_arg (EWC-08)"
    - "D-13: The 8 new tests are live (not .skip) and pass GREEN; npm test shows 0 new failures"
  artifacts:
    - path: "tests/phase-56-effort-wiring.test.cjs"
      provides: "New phase-60 Group B describe block with 8 live tests"
      contains: "phase-60 Group B effort wiring"
  key_links:
    - from: "tests/phase-56-effort-wiring.test.cjs"
      to: "get-shit-done/workflows/audit-fix.md and 7 sibling workflow files"
      via: "read(rel) helper + assert.ok(content.includes(token))"
      pattern: "resolve-model-effort gsd-"
---

<objective>
Add 8 Group B effort-wiring regression tests to the existing file
`tests/phase-56-effort-wiring.test.cjs`, guarding the spawn-template wiring
added in v2.1.0-e Phase 56 against silent regression. Each test asserts a
workflow `.md` file under `get-shit-done/workflows/` contains its
`resolve-model-effort gsd-<agent>` capture line(s) and its
`<agent>_model_effort_arg` token(s).

Purpose: Without these guards, a future upstream merge or refactor could strip
the effort-wiring tokens from these 8 workflows with no test catching it. All 20
target tokens were grep-verified present on 2026-06-07 — these are GREEN guards,
not red TDD specs.

Output: One new `describe('phase-60 Group B effort wiring: ...', ...)` block
appended to the end of the existing test file (after the GAP B block), containing
8 live tests covering EWC-01 through EWC-08.
</objective>

<context>
@.planning/phases/60-effort-wiring-coverage/60-CONTEXT.md
@tests/phase-56-effort-wiring.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append phase-60 Group B describe block with 8 effort-wiring guard tests</name>
  <files>tests/phase-56-effort-wiring.test.cjs</files>
  <read_first>
    - tests/phase-56-effort-wiring.test.cjs (MANDATORY — mirror the existing GAP B describe block exactly: the `read(rel)` helper at line 29, the `ROOT` constant at line 27, the `// allow-test-rule: source-text-is-the-product` header at line 1, and the `assert.ok(content.includes(token), '<msg>')` assertion style. Per D-02, append the new block AFTER the existing GAP B block — do not fold into the Phase 56 GAP A/B describe blocks.)
    - get-shit-done/workflows/audit-fix.md (source of truth for EWC-01 tokens)
    - get-shit-done/workflows/diagnose-issues.md (source of truth for EWC-02 tokens)
    - get-shit-done/workflows/code-review.md (source of truth for EWC-03 tokens)
    - get-shit-done/workflows/code-review-fix.md (source of truth for EWC-04 tokens — two spawn sites)
    - get-shit-done/workflows/explore.md (source of truth for EWC-05 tokens)
    - get-shit-done/workflows/import.md (source of truth for EWC-06 tokens)
    - get-shit-done/workflows/ingest-docs.md (source of truth for EWC-07 tokens — two spawn sites)
    - get-shit-done/workflows/discuss-phase-assumptions.md (source of truth for EWC-08 tokens)
  </read_first>
  <action>
    Append a new `describe('phase-60 Group B effort wiring: newly-covered workflows', () => { ... })` block to the END of tests/phase-56-effort-wiring.test.cjs, after the existing GAP B block (which closes at line 228). Per D-03 use the file's existing `read(rel)` helper plus plain `assert.ok(content.includes(token), msg)` substring checks — no regex, no adjacency/wiring-position assertions. Per the STATE.md plan-time flag, keep the `read()` call inside each individual `test()` block to prevent copy-paste errors that assert the wrong agent against the wrong file. Title/message wording follows the existing file's phrasing style (Claude's discretion per D-03 final note). Paths passed to `read()` are relative to ROOT and prefixed `get-shit-done/workflows/`.

    Write exactly 8 live `test()` blocks (no `.skip`, no `{ skip: ... }`), one per EWC requirement, asserting these exact tokens (per D-04 through D-11):

    - EWC-01 test reads `get-shit-done/workflows/audit-fix.md` and asserts it includes `resolve-model-effort gsd-executor` and `executor_model_effort_arg`.
    - EWC-02 test reads `get-shit-done/workflows/diagnose-issues.md` and asserts it includes `resolve-model-effort gsd-debugger` and `debugger_model_effort_arg`.
    - EWC-03 test reads `get-shit-done/workflows/code-review.md` and asserts it includes `resolve-model-effort gsd-code-reviewer` and `code_reviewer_model_effort_arg`.
    - EWC-04 test (single test, four asserts per D-12) reads `get-shit-done/workflows/code-review-fix.md` and asserts it includes `resolve-model-effort gsd-code-reviewer`, `resolve-model-effort gsd-code-fixer`, `code_reviewer_model_effort_arg`, and `code_fixer_model_effort_arg`.
    - EWC-05 test reads `get-shit-done/workflows/explore.md` and asserts it includes `resolve-model-effort gsd-phase-researcher` and `phase_researcher_model_effort_arg`.
    - EWC-06 test reads `get-shit-done/workflows/import.md` and asserts it includes `resolve-model-effort gsd-plan-checker` and `plan_checker_model_effort_arg`.
    - EWC-07 test (single test, four asserts per D-12) reads `get-shit-done/workflows/ingest-docs.md` and asserts it includes `resolve-model-effort gsd-doc-synthesizer`, `resolve-model-effort gsd-roadmapper`, `doc_synthesizer_model_effort_arg`, and `roadmapper_model_effort_arg`.
    - EWC-08 test reads `get-shit-done/workflows/discuss-phase-assumptions.md` and asserts it includes `resolve-model-effort gsd-assumptions-analyzer` and `assumptions_analyzer_model_effort_arg`.

    Per D-01, modify only this existing file — do not create a new test file and do not touch any workflow `.md` file. The `// allow-test-rule: source-text-is-the-product` header already present at line 1 covers the new source-text assertions; do not duplicate it.
  </action>
  <verify>
    <automated>node --test tests/phase-56-effort-wiring.test.cjs 2>&1 | tee /tmp/gsd-60-test-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c "phase-60 Group B effort wiring" tests/phase-56-effort-wiring.test.cjs` returns 1 (the new describe block exists).
    - `command grep -c "resolve-model-effort gsd-executor" tests/phase-56-effort-wiring.test.cjs` returns at least 1; same for gsd-debugger, gsd-code-reviewer, gsd-code-fixer, gsd-phase-researcher, gsd-plan-checker, gsd-doc-synthesizer, gsd-roadmapper, gsd-assumptions-analyzer.
    - `command grep -c "executor_model_effort_arg\|debugger_model_effort_arg\|code_reviewer_model_effort_arg\|code_fixer_model_effort_arg\|phase_researcher_model_effort_arg\|plan_checker_model_effort_arg\|doc_synthesizer_model_effort_arg\|roadmapper_model_effort_arg\|assumptions_analyzer_model_effort_arg" tests/phase-56-effort-wiring.test.cjs` returns at least 9 (one per distinct arg token; some appear in the new block plus prior GAP A blocks).
    - No occurrence of `.skip` or `{ skip:` inside the new phase-60 describe block (tests are live per D-13).
    - `node --test tests/phase-56-effort-wiring.test.cjs` reports 0 failures and the test count increased by exactly 8 versus the pre-edit run.
  </acceptance_criteria>
  <done>
    The new phase-60 Group B describe block exists at the end of the file with 8 live (non-skipped) tests covering EWC-01 through EWC-08; all assert the exact tokens from D-04 through D-11; `node --test tests/phase-56-effort-wiring.test.cjs` passes with 0 failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite gate — confirm 0 new failures with the 8 new tests live</name>
  <files>tests/phase-56-effort-wiring.test.cjs</files>
  <read_first>
    - tests/phase-56-effort-wiring.test.cjs (confirm the 8 new tests from Task 1 are present and live before running the full suite)
  </read_first>
  <action>
    Run the full test suite once and pipe to a temp file per CLAUDE.md convention (per D-13, success criterion 5): `npm test 2>&1 | tee /tmp/gsd-test-output.txt`. Read the piped output file and confirm 0 new failures versus the milestone baseline (`npm test` 8,243 pass / 8,255 total per ROADMAP). Confirm the 8 new phase-60 tests appear as passing and not skipped. Do NOT modify any workflow `.md` file or any source file to make tests pass — these are GREEN guards against already-present tokens; if any new test fails, the cause is a wrong token literal or wrong file path in Task 1, which must be corrected in the test, not by editing a workflow.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt; command grep -E "tests [0-9]+|pass [0-9]+|fail [0-9]+" /tmp/gsd-test-output.txt | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `fail N` summary line in /tmp/gsd-test-output.txt shows 0 new failures versus the baseline (8,243 pass / 8,255 total).
    - The phase-56-effort-wiring suite contributes 8 additional passing tests in the run versus the pre-Task-1 count.
    - `command grep -c "skipped" /tmp/gsd-test-output.txt` shows no new skips introduced by this phase (the 8 new tests are live).
    - No workflow `.md` file under get-shit-done/workflows/ was modified (`git status --porcelain get-shit-done/workflows/` returns empty).
  </acceptance_criteria>
  <done>
    `npm test` passes with 0 new failures, the 8 new phase-60 tests run live and green, and no source or workflow file was modified — only tests/phase-56-effort-wiring.test.cjs changed.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/phase-56-effort-wiring.test.cjs` exits with 0 failures.
- The new `phase-60 Group B effort wiring` describe block contains exactly 8 live tests (no `.skip`).
- All 20 target tokens (D-04 through D-11) are asserted across the 8 tests, with EWC-04 and EWC-07 each as a single test carrying four asserts (D-12).
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` shows 0 new failures versus the 8,243-pass baseline (D-13).
- Only `tests/phase-56-effort-wiring.test.cjs` is modified; no workflow `.md` or source file changed.
</verification>

<success_criteria>
- 8 new live tests added to tests/phase-56-effort-wiring.test.cjs covering EWC-01 through EWC-08.
- EWC-04 (code-review-fix.md) and EWC-07 (ingest-docs.md) are single tests asserting all four tokens each.
- Tests mirror the existing GAP B pattern (read helper + content.includes substring asserts).
- Full suite green with 0 new failures; new tests live, not skipped.
</success_criteria>

<output>
Create `.planning/phases/60-effort-wiring-coverage/60-01-SUMMARY.md` when done.
</output>
