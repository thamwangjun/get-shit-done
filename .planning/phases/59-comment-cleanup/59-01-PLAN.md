---
phase: 59-comment-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/step-numbering-scan.test.cjs]
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "The string 'Phase 48 RED expectation' is absent from tests/step-numbering-scan.test.cjs"
    - "The SCAN_DIRS/EXCLUDED documentation in the leading JSDoc block remains intact"
    - "node --test tests/step-numbering-scan.test.cjs reports the same test count as before the edit (no tests lost, no parse errors)"
    - "npm test passes with 0 new failures after the deletion"
  artifacts:
    - path: "tests/step-numbering-scan.test.cjs"
      provides: "Step numbering scanner test with stale Phase 48 RED comment removed"
      contains: "SCAN_DIRS"
  key_links: []
---

<objective>
Remove the stale "Phase 48 RED expectation:" JSDoc paragraph (and its bulleted file
list) from `tests/step-numbering-scan.test.cjs`. The v2.1.0-d milestone shipped
2026-05-31, so the Phase-48-specific RED expectation is obsolete; the surrounding
JSDoc block documenting the scanner's purpose, SCAN_DIRS, and EXCLUDED files stays.

Purpose: Establish a clean baseline before any substantive test additions in later
v2.1.0-f phases (DOC-01).
Output: `tests/step-numbering-scan.test.cjs` with the stale paragraph deleted and the
test suite still green.
</objective>

<context>
This is a single mechanical deletion. No test logic, assertions, or behavior change.
Per CONTEXT.md D-01, no implementation gray areas exist.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the stale Phase 48 RED expectation JSDoc paragraph</name>
  <files>tests/step-numbering-scan.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (read the leading JSDoc block to confirm the
      exact current line boundaries of the "Phase 48 RED expectation:" paragraph and
      its bulleted file list — line numbers may have drifted from the CONTEXT.md
      estimate of 18–26)
  </read_first>
  <action>
    Within the leading `/** ... */` JSDoc block, delete the "Phase 48 RED expectation: 7
    files fail (letter-suffix detection added via UAT gap closure):" line together with
    every bulleted file-list line directly beneath it (the `- agents/gsd-verifier.md ...`
    through `- get-shit-done/workflows/execute-phase/steps/post-merge-gate.md ...`
    entries). Also remove the now-orphaned blank ` *` line immediately preceding the
    deleted paragraph so no dangling blank comment line is left before the closing `*/`.
    Preserve everything else in the JSDoc block verbatim: the "Step Numbering Scan"
    title, the milestone/violation-class description, the `SCAN_DIRS:` line, the
    `EXCLUDED:` lines, and the closing `*/`. Make NO changes outside this comment block —
    no test logic, no new assertions, no behavioral changes (per CONTEXT.md D-01).
  </action>
  <verify>
    <automated>command grep -c 'Phase 48 RED expectation' tests/step-numbering-scan.test.cjs; command grep -c 'SCAN_DIRS' tests/step-numbering-scan.test.cjs; node --test tests/step-numbering-scan.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c 'Phase 48 RED expectation' tests/step-numbering-scan.test.cjs` returns 0
    - `command grep -c 'SCAN_DIRS' tests/step-numbering-scan.test.cjs` returns ≥ 1 (SCAN_DIRS/EXCLUDED documentation preserved)
    - `node --test tests/step-numbering-scan.test.cjs` exits 0 with the same test count as before the edit (no parse errors, no tests lost)
  </acceptance_criteria>
  <done>The stale Phase 48 RED expectation paragraph and its bulleted list are gone; the SCAN_DIRS/EXCLUDED documentation and all test logic remain unchanged; the file parses and runs with an unchanged test count.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite gate</name>
  <files>tests/step-numbering-scan.test.cjs</files>
  <read_first>
    - tests/step-numbering-scan.test.cjs (confirm Task 1's deletion is in place before gating)
  </read_first>
  <action>
    Run the full test suite, piping output to a file, and confirm 0 new failures versus
    the milestone baseline (8,243 pass / 8,255 total per ROADMAP v2.1.0-f). This is a
    verification-only gate (per CONTEXT.md D-03) — make no further source edits unless the
    gate surfaces a regression directly caused by the Task 1 deletion.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt; command grep -E 'pass|fail' /tmp/gsd-test-output.txt | command tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` completes and `/tmp/gsd-test-output.txt` shows 0 new failures relative to the v2.1.0-f baseline (8,243 pass / 8,255 total)
    - The step-numbering-scan.test.cjs subtests all pass within the full run
  </acceptance_criteria>
  <done>Full suite passes with 0 new failures; the deletion introduced no regression.</done>
</task>

</tasks>

<verification>
- `command grep 'Phase 48 RED expectation' tests/step-numbering-scan.test.cjs` returns no matches
- `command grep 'SCAN_DIRS' tests/step-numbering-scan.test.cjs` still matches (documentation preserved)
- `node --test tests/step-numbering-scan.test.cjs` exits 0 with an unchanged test count
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` passes with 0 new failures
</verification>

<success_criteria>
- Stale Phase 48 RED expectation paragraph and bulleted file list removed (DOC-01)
- Surrounding JSDoc block (title, description, SCAN_DIRS, EXCLUDED) intact
- No test logic or behavioral changes
- Test suite green with 0 new failures
</success_criteria>

<output>
Create `.planning/phases/59-comment-cleanup/59-01-SUMMARY.md` when done
</output>
