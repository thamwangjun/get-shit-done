---
phase: 56-spawn-template-wiring
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/new-project.md
  - get-shit-done/workflows/new-milestone.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/map-codebase.md
  - get-shit-done/workflows/quick.md
autonomous: true
requirements: [SPAWN-01, SPAWN-02, SPAWN-03]
must_haves:
  truths:
    - "Every Group A workflow's 'Parse JSON for:' instruction lists the *_effort sibling for each *_model var it consumes"
    - "Every Agent() block in Group A carries an effort= arg line adjacent to its model= line, fed by a pre-built {<agent>_model_effort_arg} token"
    - "The effort token is empty (renders nothing) for inherit/non-effort runtimes via the pre-built fragment — no conditional render instruction (D-04)"
    - "Fork gates unchanged after edits: agent-frontmatter 155/155, negative-framing 99/99, step-numbering 632/632, cross-file-refs 219/219, eta-include pass"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "executor/verifier effort wired into parse instruction + both Agent() blocks"
      contains: "executor_model_effort_arg"
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "researcher/planner/checker effort wired"
      contains: "_effort_arg"
    - path: "get-shit-done/workflows/quick.md"
      provides: "planner/executor/checker effort wired"
      contains: "_effort_arg"
  key_links:
    - from: "Group A workflow Agent() blocks"
      to: "init JSON *_effort fields (already emitted by init.cjs, Phase 54)"
      via: "parse instruction + pre-built {<agent>_model_effort_arg} token"
      pattern: "_model_effort_arg"
---

<objective>
Wire resolved effort into the 8 init-fed (Group A) workflows. init.cjs already emits `*_effort` siblings (Phase 54) — these workflows simply do not consume them yet. For each: extend the "Parse JSON for:" instruction to include the `*_effort` var(s), build the pre-built `{<agent>_model_effort_arg}` token from each effort value, and add the `effort=` arg line adjacent to each `model=` line in every Agent() block.

Purpose: SPAWN-01/02 — resolved effort reaches every Group A spawned agent via the carrier confirmed in Plan 01 (D-02). SPAWN-03 — done by extending existing model= blocks, never renumbering steps.
Output: 8 workflow files passing the per-agent effort token to their Agent() calls; full fork gate suite green.
</objective>

<context>
!`cat .planning/phases/56-spawn-template-wiring/56-PATTERNS.md`
@.planning/phases/56-spawn-template-wiring/56-RESEARCH.md
@.planning/phases/56-spawn-template-wiring/56-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries
Internal markdown-template editing; no external attack surface. Interpolated token is the enum-constrained `effort="<enum>"` / `""` string from Plan 01's query (parseModelEffort allowlist, V5).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-A1 | Tampering | effort token in Agent() pseudocode | accept | Enum-constrained, pre-built by Plan 01 query; no free-text path. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire execute-phase.md + execute-plan.md + plan-phase.md + quick.md</name>
  <files>get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/quick.md</files>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (lines 83–95 parse instruction, 538–548 executor Agent() block, 1388–1398 verifier Agent() block)
    - get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/quick.md (their parse-JSON instruction + Agent() blocks)
    - .planning/phases/56-spawn-template-wiring/56-PATTERNS.md (§"Group A workflows — init-fed pattern" and §"Shared Patterns")
    - .planning/phases/56-spawn-template-wiring/56-RESEARCH.md (§"D-07" Group A table — exact effort fields already in each init JSON)
  </read_first>
  <action>For each file, per the Group A pattern: (1) extend the "Parse JSON for:" instruction to list each `*_effort` sibling adjacent to its `*_model` var (execute-phase: executor_effort, verifier_effort; execute-plan: executor_effort; plan-phase: researcher_effort, planner_effort, checker_effort; quick: planner_effort, executor_effort, checker_effort). (2) After the parse block, before the first Agent() call, add one shell assignment per agent building the pre-built token, e.g. `executor_model_effort_arg=$([ -n "$executor_effort" ] && [ "$executor_effort" != "null" ] && echo "effort=\"$executor_effort\"" || echo "")` — empty for inherit/non-effort runtimes (D-04, no conditional render instruction). (3) In every Agent() block add the `{<agent>_model_effort_arg}` line directly adjacent to the existing `model="{...}"` line. Per D-04 — the empty token renders nothing, so no per-spawn judgment is required; pass the token unconditionally. Per SPAWN-03 — extend existing model= blocks only; do NOT add or renumber any `Step N.M` heading, alter frontmatter, or add @-refs. Use the naming convention `{existing_model_var}_effort_arg` (PATTERNS.md table) verbatim. Where execute-phase.md line 87 has the existing "Model resolution: if inherit, omit model=" instruction, add an adjacent sentence stating the effort token is empty when absent so interpolation renders nothing (mirrors PATTERNS.md §"Model resolution omit-when-inherit instruction").</action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_model_effort_arg" get-shit-done/workflows/execute-phase.md` ≥ 3 (2 Agent() args + ≥1 shell assignment)
    - Each of the four files: every `model="{X_model}"` Agent() line has an adjacent `{X_model_effort_arg}` line
    - Each file's "Parse JSON for:" lists the `*_effort` sibling for every `*_model` it consumes
    - `npm test` green; step-numbering 632/632, agent-frontmatter 155/155, negative-framing 99/99, cross-file-refs 219/219 unchanged
  </acceptance_criteria>
  <done>execute-phase, execute-plan, plan-phase, quick all carry the per-agent effort token; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new-project.md + new-milestone.md + verify-work.md + map-codebase.md</name>
  <files>get-shit-done/workflows/new-project.md, get-shit-done/workflows/new-milestone.md, get-shit-done/workflows/verify-work.md, get-shit-done/workflows/map-codebase.md</files>
  <read_first>
    - get-shit-done/workflows/new-project.md, new-milestone.md, verify-work.md, map-codebase.md (each file's parse-JSON instruction + Agent() blocks)
    - .planning/phases/56-spawn-template-wiring/56-PATTERNS.md (§"Group A workflows — init-fed pattern")
    - .planning/phases/56-spawn-template-wiring/56-RESEARCH.md (§"D-07" Group A table — new-project: researcher/synthesizer/roadmapper_effort; new-milestone: planner/executor/checker/verifier_effort; verify-work: verifier/planner/checker_effort; map-codebase: mapper_effort)
  </read_first>
  <action>Apply the identical Group A three-step pattern from Task 1 to these four files using each file's effort fields (RESEARCH.md D-07 table). Add the `*_effort` siblings to the "Parse JSON for:" instruction, add one pre-built-token shell assignment per agent after the parse block, and add the `{<agent>_model_effort_arg}` line adjacent to each `model="{...}"` line in every Agent() block. Per D-04 — pass the token unconditionally; empty token renders nothing for inherit/non-effort runtimes. Per SPAWN-03 — extend existing model= blocks only; no step renumbering, no frontmatter or @-ref changes. Follow the `{existing_model_var}_effort_arg` naming convention verbatim.</action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/gsd-test-output.txt | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four files: every `model="{X_model}"` Agent() line has an adjacent `{X_model_effort_arg}` line and the parse instruction lists the matching `*_effort` sibling
    - `grep -c "_model_effort_arg" get-shit-done/workflows/new-project.md` ≥ 3 (researcher/synthesizer/roadmapper)
    - `npm test` green; all four fork gate counts unchanged (155/99/632/219)
  </acceptance_criteria>
  <done>new-project, new-milestone, verify-work, map-codebase carry the per-agent effort token; gates green.</done>
</task>

</tasks>

<verification>
- `grep -rn "_model_effort_arg" get-shit-done/workflows/{execute-phase,execute-plan,plan-phase,new-project,new-milestone,verify-work,map-codebase,quick}.md` shows a token adjacent to every Agent() model= line
- `npm test 2>&1 | tee /tmp/gsd-test-output.txt` — full suite green; fork gates hold (agent-frontmatter 155/155, negative-framing 99/99, step-numbering 632/632, cross-file-refs 219/219, eta-include)
</verification>

<success_criteria>
- All 8 Group A workflows parse + interpolate the per-agent effort token into every Agent() block
- No step renumbered, no frontmatter or @-ref altered
- Full npm test green with no new regressions
</success_criteria>

<output>
Create `.planning/phases/56-spawn-template-wiring/56-02-SUMMARY.md` when done.
</output>
