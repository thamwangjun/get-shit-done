---
phase: 70-spec-02-sha-versioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/02-sha-versioning/SPEC.md
autonomous: true
requirements: [SPEC-02, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "SPEC.md specifies install.js git-SHA emit, no-network sentinel semantics (signals an invalid install, not an empty string), the update worker via the GitHub Commits API with isNewer SHA-equality, statusline display, the check-latest-version.cjs injectable seam, and the {{GSD_REPO}}/{{GSD_BRANCH}} template-placeholder boundary (ROADMAP criterion 1 / SPEC-02, per D-01/D-04/D-05/D-06)"
    - "SPEC.md records 'GitHub Commits API, not npmjs.com' and 'SHA equality, not semver ordering' as settled Key Decisions, each with consequence of reopening stated (ROADMAP criterion 2 / QUAL-05, per D-07)"
    - "SPEC.md states ~6 numbered EARS invariants in 02-INV-M format citing the five tier-1 SHA test files, with an Acceptance Tests traceability table mapping each MUST invariant to a real subtest and no [MISSING] rows (ROADMAP criterion 3 / QUAL-01, QUAL-02, QUAL-04, per D-02); current paths/symbols advisory-marked (QUAL-03)"
    - "SPEC.md frontmatter Status advances Draft -> Ready with Confidence and Specced (2026-06-12) set, gated on QUAL-01..05"
  artifacts:
    - path: ".planning/spec/02-sha-versioning/SPEC.md"
      provides: "Behavioral-contract spec body for SPEC-02 (Purpose, Scope, ~6 invariants, Acceptance Tests, Key Decisions, advisory Code Context) with Status: Ready"
      contains: "02-INV-1"
  key_links:
    - from: ".planning/spec/02-sha-versioning/SPEC.md (Acceptance Tests table)"
      to: "the five tier-1 SHA test files (subtests)"
      via: "verbatim subtest names keyed on 02-INV-M"
      pattern: "tests/(version-detection|semver-compare|bug-2992-check-latest-version|statusline-sha|update-sha-migration)\\.test\\.cjs"
    - from: ".planning/spec/02-sha-versioning/SPEC.md (Invariants)"
      to: ".planning/spec/00-CONVENTIONS.md (7-section template + NN-INV-M scheme)"
      via: "section order + invariant ID format conformance"
      pattern: "02-INV-[1-7]"
---

<objective>
Author the full body of `.planning/spec/02-sha-versioning/SPEC.md` — a behavioral-contract
specification of the SHA-based versioning system and its coordination topology across the five
files that implement it — so a reimplementer can rebuild the system on a refactored upstream
without reading the current source. The phase fills an existing stub (frontmatter + empty
7-section skeleton from Phase 68); it does NOT modify `install.js`, the update worker, the
statusline, `check-latest-version.cjs`, or `update.md` — it narrates the five tier-1 SHA test
files into the locked 7-section contract and advances Status `Draft -> Ready`.

Purpose: Capture the SHA-based versioning contract (install-time git-SHA emit, the `no-network`
sentinel, `isNewer` SHA-equality, the GitHub Commits API source, the `check-latest-version.cjs`
injectable seam, statusline/update display, and the `{{...}}` placeholder boundary) as a durable,
move-proof contract (shape normative; counts/paths/regexes advisory), satisfying SPEC-02 and the
shared QUAL-01..05 bars so the spec reaches `Ready` and survives the Phase 77 cross-spec review.

Output: A completed `SPEC.md` with Purpose, Scope, ~6 invariants (`02-INV-1`..`02-INV-6`; split
to `02-INV-7` is permitted per D-01 only if the emit invariant becomes overloaded), an Acceptance
Tests traceability table citing real subtests across all five tier-1 files, four settled Key
Decisions, advisory Code Context, and frontmatter Status `Ready` (Confidence + Specced 2026-06-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase-70 source artifacts (locked decisions + narration map + locked template + the stub)
@.planning/phases/70-spec-02-sha-versioning/70-CONTEXT.md
@.planning/phases/70-spec-02-sha-versioning/70-RESEARCH.md
@.planning/spec/00-CONVENTIONS.md
@.planning/spec/02-sha-versioning/SPEC.md

# Sibling Wave-1 worked reference (section shape, EARS phrasing, advisory marking, QUAL pattern)
@.planning/spec/01-positive-framing/SPEC.md
</context>

<artifacts_this_phase_produces>
NEW identifiers/contracts this phase creates (all live inside `SPEC.md` prose — no code/test
symbols are created; all system symbols are PRE-EXISTING and appear only under advisory Code
Context):

- **Invariant IDs:** `02-INV-1` (SHA emit at install + `{{...}}` placeholder substitution boundary),
  `02-INV-2` (no-network sentinel), `02-INV-3` (SHA-equality comparison via `isNewer`),
  `02-INV-4` (update source = GitHub Commits API), `02-INV-5` (`check-latest-version.cjs` seam
  contract), `02-INV-6` (Display — SHA labels, no `parseV()` semver block, no `isDevInstall`
  branch). A 7th invariant `02-INV-7` is created ONLY if the executor judges `02-INV-1` overloaded
  and splits placeholder substitution out (Claude's Discretion per D-01 / RESEARCH Open Question 1).
- **Section headers authored (locked order, verbatim from 00-CONVENTIONS.md §1):** `## Purpose`,
  `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`.
- **Status transition:** frontmatter `**Status:**` advances `Draft -> Ready`; `**Confidence:**`
  and `**Specced:** 2026-06-12` set; the `**Reimplementation evidence (tier-1 test):**` line is
  expanded to the five tier-1 files (RESEARCH §8 recommendation; within Claude's Discretion).

PRE-EXISTING (advisory, NOT produced here — list under Code Context only, per RESEARCH §5):
the five tier-1 test files; `isNewer`, `writeResult`, the `https.get` call; `GITHUB_API_URL`,
`CHECK_REASON`, `checkLatestVersion`; `gsdVersion`, the `'no-network'` sentinel literal, the
`git rev-parse --short=7` call; the `{{GSD_REPO}}` / `{{GSD_BRANCH}}` / `{{GSD_VERSION}}`
placeholders; `parseV`, `isDevInstall`, `cache.stale_hooks`; the `compare_versions` step and
its `grep -Eq '^[0-9a-f]{7}|no-network'` pattern.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author normative core — Purpose, Scope, ~6 02-INV-M invariants, Acceptance Tests table</name>
  <files>.planning/spec/02-sha-versioning/SPEC.md</files>
  <read_first>
    - .planning/spec/02-sha-versioning/SPEC.md (the stub being filled — replace the
      `<!-- to be filled -->` comments in Purpose, Scope, Invariants, Acceptance Tests with prose)
    - .planning/phases/70-spec-02-sha-versioning/70-RESEARCH.md (THE narration map — §3 the six
      invariant->subtest mappings with VERBATIM subtest names + EARS phrasing per invariant; §6 the
      discrepancy audit (D-10 count is shape-normative, HOOK-04 checks `{{GSD_REPO}}` template form,
      HOOK-03 folds into invariant 3); §7 the QUAL-01/02/04 satisfaction pattern)
    - .planning/phases/70-spec-02-sha-versioning/70-CONTEXT.md (LOCKED decisions D-01..D-07)
    - .planning/spec/00-CONVENTIONS.md (LOCKED 7-section template, NN-INV-M scheme §2, EARS patterns
      + RFC 2119 strength §4, the example `01-INV-1` invariant shape)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference for invariant prose +
      consequence-clause shape + Acceptance Tests table layout)
  </read_first>
  <action>
    Replace the placeholder comments in the first four sections of SPEC.md, in the locked order,
    authoring affirmative-framed prose (state the correct behavior directly; the spec models the
    fork's affirmative-framing standard it sits beside).

    Purpose (one paragraph): state the "why" of the SHA-based versioning system — the fork is
    distinguished from the upstream npm package, so the fork's HEAD commit SHA (not a semver string)
    is the authoritative version signal, derived at install time and compared by exact equality
    against the latest SHA fetched from the fork's GitHub Commits API. State what breaks if the
    contract is absent or wrong: the version display and update detection regress to the upstream
    npm package version, which never reflects fork commits, or semver ordering is applied to hex
    strings (undefined behavior). Cite the five tier-1 SHA test files as the behavioral authority
    (source-of-truth tier 1 per 00-CONVENTIONS.md §4).

    Scope: two bullet lists. In scope (each behavioral role): install-time git-SHA emit; the
    `no-network` sentinel semantics; `isNewer` 7-char SHA-equality with null/undefined/empty safety;
    the GitHub Commits API fetch via `https.get`; the `check-latest-version.cjs` injectable seam I/O
    contract; statusline + `update.md` SHA-label display; and the literal `{{GSD_REPO}}` /
    `{{GSD_BRANCH}}` / `{{GSD_VERSION}}` placeholder substitution that wires SHA + repo identity into
    installed hooks. Out of scope (each with a one-line rationale): (1) Eta `<%~ include() %>` /
    `@~/` content materialization — that is SPEC-04; the literal `{{...}}` repo/branch/version
    regex substitution that wires the SHA in IS SPEC-02 (the explicit D-06 boundary note — no INDEX
    dependency edge to SPEC-04, both remain root nodes); (2) the win32 `shell: true` spawn gating
    asserted by `gsd-check-update-worker-platform-gate.test.cjs` — that is a platform-security
    concern of the spawn primitive, not SHA-versioning behavior (the explicit D-03 exclusion).

    Invariants: author ~6 numbered EARS statements with RFC 2119 strength, IDs `02-INV-1`..`02-INV-6`
    (NN-INV-M format per 00-CONVENTIONS.md §2). Each is a single falsifiable claim mapping to a real
    subtest cluster in RESEARCH §3. Use the per-invariant EARS pattern recommended in RESEARCH §3.
      - 02-INV-1 SHA emit at install (Ubiquitous MUST + SHALL NOT sub-clause): install derives the
        version from `git rev-parse --short=7 HEAD` and substitutes the resolved SHA into hooks via
        `{{GSD_VERSION}}` regex replacement (using the derived `gsdVersion`, not `pkg.version`), and
        SHALL NOT call the GitHub Commits API at install time. Placeholder substitution
        (`{{GSD_REPO}}`/`{{GSD_BRANCH}}`/`{{GSD_VERSION}}`) folds in HERE as the emit boundary per
        D-01; split it to a separate `02-INV-7` ONLY if this invariant becomes overloaded (RESEARCH
        Open Question 1 — Claude's Discretion).
      - 02-INV-2 no-network sentinel (Unwanted-behavior MUST per D-04): if `git rev-parse` fails at
        install time the system initializes `gsdVersion` to the sentinel string `'no-network'` and
        SHALL NOT fall back to `pkg.version` or any semver string; the sentinel is never an
        equality-branch target. Cite the D-10 "appears exactly 3 times" assertion as the CURRENT
        shape `current as of 2026-06-12`, narrating the behavioral contract as "grep-Eq-only
        placement, never an equality branch," NOT as a durable magic count (RESEARCH §6 note 1).
      - 02-INV-3 SHA-equality comparison (Ubiquitous MUST + Unwanted-behavior): `isNewer` truncates
        `latest` to 7 chars and compares for exact equality; `null`/`undefined`/empty `latest` yields
        `false` (no false positive); it SHALL NOT perform semver ordering. Fold the three HOOK-03
        "isNewer defined before use" subtests in here as a structural property of `isNewer` (RESEARCH
        §6 note 3 / Open Question 2).
      - 02-INV-4 Update source = GitHub Commits API (Ubiquitous + Unwanted-behavior): the worker
        fetches the latest SHA from the GitHub Commits API path via `https.get`; it SHALL NOT contact
        `npmjs.com` or reference the `get-shit-done-cc` npm package name. Note that the worker source
        carries the `{{GSD_REPO}}`/`{{GSD_BRANCH}}` TEMPLATE form (HOOK-04 asserts the template, not
        a resolved URL — RESEARCH §6 note 2); the resolved URL is pinned by `GITHUB_API_URL` in the
        seam (02-INV-5).
      - 02-INV-5 check-latest-version.cjs seam contract (Ubiquitous + Event-driven error paths): the
        seam exposes a constant `GITHUB_API_URL` and a `CHECK_REASON` enum with exactly three keys
        (`OK`, `FAIL_FETCH_FAILED`, `FAIL_INVALID_SHA`), and returns `{ ok: true, sha }` (7-char
        truncated) on success, `FAIL_FETCH_FAILED` on network/non-200, and `FAIL_INVALID_SHA` on a
        missing/malformed/too-short sha field. May render as one invariant with a 3-condition detail
        table or split success/error (either satisfies QUAL-01 per RESEARCH §3 invariant 5).
      - 02-INV-6 Display (Unwanted-behavior MUST NOT + Ubiquitous MUST): the statusline SHALL NOT
        define `parseV` or reference `isDevInstall`, and the stale-hooks block carries exactly one
        `gsdUpdate +=` assignment with no IIFE; `update.md` SHALL display `Installed SHA:` and
        `Latest SHA:` labels and SHALL NOT contain `isDevInstall`, `installed > latest`, or semver
        grep/ordering logic. May be one compound invariant or split positive/negative (RESEARCH §3
        invariant 6).
      End each invariant with a "Consequence of violating: <what breaks>" clause per the
      00-CONVENTIONS.md §4 example shape.

    Acceptance Tests: a traceability table keyed on `02-INV-M`, columns exactly
    `Invariant | Test File | Subtest / Assertion Shape`. Lift REAL verbatim subtest names from
    RESEARCH §3 for every MUST invariant — multiple rows per invariant where a cluster exists.
    Every MUST invariant traces to a real subtest (D-02): NO `[MISSING — write test first]` rows.
    Test File column carries the correct one of the five tier-1 files per row
    (`version-detection`, `semver-compare`, `bug-2992-check-latest-version`, `statusline-sha`,
    `update-sha-migration` `.test.cjs`). Include at least the literal subtest names
    `install.js source contains git rev-parse --short` (02-INV-1), `install.js has no-network
    sentinel as initial gsdVersion value` (02-INV-2), `null latest — no false positive` (02-INV-3),
    `worker source does not contact npmjs.com` (02-INV-4), `CHECK_REASON enum exposes the documented
    codes` (02-INV-5), and `update.md contains "Installed SHA:" label` (02-INV-6).
  </action>
  <verify>
    <automated>command grep -c '02-INV-' .planning/spec/02-sha-versioning/SPEC.md  # >=6 invariant-ID occurrences (>=7 if placeholder split)</automated>
    <automated>command grep -q 'install.js source contains git rev-parse --short' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'CHECK_REASON enum exposes the documented codes' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'worker source does not contact npmjs.com' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'current as of 2026-06-12' .planning/spec/02-sha-versioning/SPEC.md && echo OK</automated>
    <automated>for f in version-detection semver-compare bug-2992-check-latest-version statusline-sha update-sha-migration; do command grep -q "tests/$f.test.cjs" .planning/spec/02-sha-versioning/SPEC.md || echo "MISSING TIER-1 FILE: $f"; done</automated>
    <automated>command grep -c 'MISSING — write test first' .planning/spec/02-sha-versioning/SPEC.md  # ==0, every MUST invariant has a real subtest (D-02)</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c '02-INV-' SPEC.md` returns >=6 (six invariant IDs `02-INV-1`..`02-INV-6`
      present; >=7 if the executor split placeholder substitution to `02-INV-7`).
    - All five tier-1 test files appear in the Acceptance Tests table (the per-file grep loop emits
      no `MISSING TIER-1 FILE` line): `tests/version-detection.test.cjs`,
      `tests/semver-compare.test.cjs`, `tests/bug-2992-check-latest-version.test.cjs`,
      `tests/statusline-sha.test.cjs`, `tests/update-sha-migration.test.cjs`.
    - The table contains the six anchor verbatim subtest names listed in the action (one per
      invariant), lifted exactly from RESEARCH §3.
    - The literal marker `current as of 2026-06-12` appears on the D-10 sentinel-count narration.
    - `command grep -c 'MISSING — write test first' SPEC.md` returns 0 (no MISSING rows — D-02).
    - The four sections appear under the exact headers `## Purpose`, `## Scope`, `## Invariants`,
      `## Acceptance Tests` in that order, with no placeholder `<!-- to be filled -->` comment
      remaining in them.
  </acceptance_criteria>
  <done>
    Purpose, Scope, Invariants (~6 `02-INV-M` EARS statements), and the Acceptance Tests
    traceability table are authored; ROADMAP criteria 1 and 3 are satisfied; install.js git-SHA
    emit, no-network sentinel, isNewer SHA-equality, GitHub Commits API source, the
    check-latest-version.cjs seam, statusline/update display, and the `{{...}}` placeholder boundary
    are all specified and every MUST invariant traces to a real subtest across the five tier-1 files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author four settled Key Decisions + advisory Code Context, advance Status Draft -> Ready</name>
  <files>.planning/spec/02-sha-versioning/SPEC.md</files>
  <read_first>
    - .planning/spec/02-sha-versioning/SPEC.md (now carrying the Task 1 normative core; fill the
      remaining `## Key Decisions` and `## Code Context` sections and update the frontmatter block)
    - .planning/phases/70-spec-02-sha-versioning/70-RESEARCH.md (§4 the four ready-to-insert Key
      Decisions KD-A..KD-D with rationale + consequence-of-reopening + tier-1 evidence; §5 the full
      advisory symbol inventory across the five implementation files; §8 the frontmatter
      `Reimplementation evidence` expansion recommendation)
    - .planning/phases/70-spec-02-sha-versioning/70-CONTEXT.md (D-04 sentinel-is-also-a-KD, D-05
      seam-is-also-a-KD, D-07 the two ROADMAP-mandated KDs verbatim)
    - .planning/spec/00-CONVENTIONS.md (§1 Section 6/7 shape; §3 status vocabulary Draft|Ready|
      Implemented|Verified and the "MUST NOT advance to Ready unless QUAL-01..05 satisfied" rule)
    - .planning/spec/01-positive-framing/SPEC.md (sibling worked reference for the settled/consequence
      footer format and the advisory Code Context layout)
  </read_first>
  <action>
    Fill the final two sections and update the frontmatter block; keep authoring affirmative-framed
    prose.

    Key Decisions (`## Key Decisions`): record the four settled decisions from RESEARCH §4, each as
    statement + one-sentence rationale + "Settled — do not reopen. Consequence of reopening:
    <what breaks>" (use the consequences supplied in RESEARCH §4 verbatim or near-verbatim, and cite
    the tier-1 evidence given there):
      (a) KD-A "GitHub Commits API, not npmjs.com" (D-07, ROADMAP-mandated verbatim): the worker
          fetches from the GitHub Commits API, not the npm registry; consequence of reopening — the
          npm registry returns the upstream package version, which never reflects fork commits,
          breaking fork version display and update detection entirely.
      (b) KD-B "SHA equality, not semver ordering" (D-07, ROADMAP-mandated verbatim): comparison is
          exact 7-char SHA equality with no ordering relationship; consequence of reopening —
          applying semver ordering to hex strings produces undefined behavior, breaking update
          detection.
      (c) KD-C no-network sentinel signals invalid install, not empty string (D-04): the sentinel is
          used when `git rev-parse` fails and is never an equality-branch target; consequence of
          reopening — an empty string or `pkg.version` fallback introduces semver strings into the
          SHA pipeline, causing false-positive update signals.
      (d) KD-D check-latest-version.cjs extracted as an injectable seam (D-05): the SHA-check logic
          is extracted into a module with an injectable `request` function so it is testable without
          network access; consequence of reopening — inlining the fetch forces live/flaky network
          tests or leaves the GitHub API interaction untested.
    KD-A and KD-B are the two ROADMAP-mandated decisions (ROADMAP criterion 2 / QUAL-05); KD-C and
    KD-D are the locked sentinel/seam design intents (D-04/D-05) that pair with invariants 02-INV-2
    and 02-INV-5 respectively.

    Code Context (`## Code Context`): mark the section `<!-- advisory -->` (open with a dated line in
    the Phase 69 style, e.g. "The items below are current as of 2026-06-12. All file paths, function
    names, regex bodies, and line numbers are advisory and will shift on any test edit or upstream
    refactor."). List the PRE-EXISTING current paths/symbols ONLY here, with no normative claim
    resting on them, grouped by the five implementation files (RESEARCH §5): `bin/install.js`
    (`gsdVersion`, the `'no-network'` literal, `git rev-parse --short=7`, the
    `{{GSD_REPO}}`/`{{GSD_BRANCH}}`/`{{GSD_VERSION}}` replacements, `fs.writeFileSync(versionDest)`);
    `hooks/gsd-check-update-worker.js` (`isNewer`, `writeResult`, the `https.get` call,
    `MANAGED_HOOKS`); `get-shit-done/bin/check-latest-version.cjs` (`GITHUB_API_URL`, `CHECK_REASON`,
    `checkLatestVersion`, the `/^[0-9a-f]{7}/i` validation regex, the 15s timeout);
    `hooks/gsd-statusline.js` (`cache.stale_hooks`, the single `gsdUpdate +=`, absence of `parseV` /
    `isDevInstall`); `get-shit-done/workflows/update.md` (the `compare_versions` step, the `==` SHA
    equality, the `Installed SHA:`/`Latest SHA:` labels, the `grep -Eq '^[0-9a-f]{7}|no-network'`
    pattern). Note that line numbers from RESEARCH §5 are advisory and shift on any source edit
    (move-proofing for the upstream refactor).

    Frontmatter transition (block-header frontmatter, NOT YAML): set `**Status:** Ready` (from
    Draft), set `**Confidence:**` to a stated value (e.g. High — Claude's discretion per CONTEXT.md),
    and set `**Specced:** 2026-06-12`. Expand `**Reimplementation evidence (tier-1 test):**` to list
    all five tier-1 files (RESEARCH §8 recommendation; Claude's Discretion). Advancing to Ready
    asserts QUAL-01 (numbered falsifiable EARS invariants), QUAL-02 (traceability table maps each
    MUST to a subtest, no MISSING rows), QUAL-03 (advisory-marked paths, move-proof), QUAL-04 (five
    tier-1 tests cited), QUAL-05 (four Key Decisions with do-not-reopen + consequence) are all
    satisfied by the finished body.
  </action>
  <verify>
    <automated>command grep -q '\*\*Status:\*\* Ready' .planning/spec/02-sha-versioning/SPEC.md && command grep -q '\*\*Specced:\*\* 2026-06-12' .planning/spec/02-sha-versioning/SPEC.md && echo OK</automated>
    <automated>command grep -c 'Settled — do not reopen' .planning/spec/02-sha-versioning/SPEC.md  # >=4 Key Decisions (KD-A..KD-D)</automated>
    <automated>command grep -q 'npmjs.com' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'semver ordering' .planning/spec/02-sha-versioning/SPEC.md && echo OK  # the two ROADMAP-mandated KDs present</automated>
    <automated>command grep -q 'advisory' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'GITHUB_API_URL' .planning/spec/02-sha-versioning/SPEC.md && command grep -q 'checkLatestVersion' .planning/spec/02-sha-versioning/SPEC.md && echo OK</automated>
    <automated>command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' .planning/spec/02-sha-versioning/SPEC.md  # ==6, locked section set, none renamed/added/reordered</automated>
    <automated>command grep -c 'to be filled' .planning/spec/02-sha-versioning/SPEC.md  # ==0, no placeholder comments remain</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` contains the literal `**Status:** Ready` and `**Specced:** 2026-06-12`; `Confidence`
      is set to a non-placeholder value.
    - `command grep -c 'Settled — do not reopen'` returns >=4 (the four Key Decisions KD-A..KD-D).
    - Both ROADMAP-mandated decisions are present: `npmjs.com` (KD-A) and `semver ordering` (KD-B)
      each appear with a consequence-of-reopening sentence.
    - The `## Code Context` section is marked `<!-- advisory -->` and lists `GITHUB_API_URL`,
      `CHECK_REASON`, `checkLatestVersion`, `isNewer`, and the `'no-network'` sentinel; no normative
      invariant text appears in Code Context.
    - The six body section headers appear exactly once each in the locked order (`## Purpose`,
      `## Scope`, `## Invariants`, `## Acceptance Tests`, `## Key Decisions`, `## Code Context`) —
      the section-header grep returns 6 and no section is renamed/added/removed versus
      00-CONVENTIONS.md §1.
    - `command grep -c 'to be filled' SPEC.md` returns 0 (every placeholder comment is replaced).
  </acceptance_criteria>
  <done>
    Four settled Key Decisions (KD-A..KD-D, including the two ROADMAP-mandated ones) and advisory
    Code Context are authored; frontmatter Status is `Ready` with Confidence + Specced set and the
    five-file evidence line expanded; ROADMAP criterion 2 is satisfied and the spec conforms exactly
    to the locked 7-section template (no drift for Phase 77).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This phase authors a static Markdown documentation artifact (`SPEC.md`). There is no untrusted input, no code execution, no runtime surface, no network call, and no secret handled. The SHA-versioning system the spec describes has a network surface (the GitHub Commits API fetch), but specifying it in prose introduces none of that surface — standard STRIDE injection/auth/DoS categories do not apply to a prose deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information disclosure / Tampering (spec INACCURACY) | SPEC.md normative claims (Invariants + Acceptance Tests) | mitigate | Every invariant and Acceptance Tests row is traced to a real subtest in one of the five tier-1 SHA test files with verbatim subtest names (per QUAL-02/QUAL-04 / D-02 — no MISSING rows); no normative claim rests on an advisory path/symbol/line-number that the upstream refactor could move (per QUAL-03 advisory-marking); the D-10 sentinel count is narrated as `current as of 2026-06-12` shape, not a durable magic number. |
| T-70-02 | Tampering (advisory-claim drift) | SPEC.md Code Context paths/symbols | accept | Current paths, symbol names (`isNewer`, `GITHUB_API_URL`, `CHECK_REASON`, the `'no-network'` literal, the `{{...}}` placeholders, `check-latest-version.cjs`), regex bodies, and line numbers will shift on any source edit or upstream refactor; this is accepted because they are confined to `## Code Context` and marked `<!-- advisory -->`, so no contract depends on them. |

No package-manager installs occur in this phase, so the `T-{phase}-SC` supply-chain row and the
package-legitimacy checkpoint do not apply. No high-severity threats are present.
</threat_model>

<verification>
Overall phase checks (run after both tasks):
- `command grep -c '02-INV-' SPEC.md` >= 6 (six invariants present; >=7 if placeholder split).
- `command grep -cE '^## (Purpose|Scope|Invariants|Acceptance Tests|Key Decisions|Code Context)$' SPEC.md` == 6 (locked section set, exact order, no drift vs 00-CONVENTIONS.md §1).
- All five tier-1 files cited: the loop over `version-detection`, `semver-compare`,
  `bug-2992-check-latest-version`, `statusline-sha`, `update-sha-migration` emits no
  `MISSING TIER-1 FILE` line.
- `command grep -c 'MISSING — write test first' SPEC.md` == 0 (every MUST invariant has a real
  subtest — D-02).
- `command grep -c 'Settled — do not reopen' SPEC.md` >= 4 (KD-A..KD-D); both `npmjs.com` and
  `semver ordering` appear (the two ROADMAP-mandated decisions — QUAL-05).
- `command grep -q '**Status:** Ready'` and `command grep -q '**Specced:** 2026-06-12'` both succeed.
- `command grep -c 'to be filled' SPEC.md` == 0 (no placeholder comments remain).
</verification>

<success_criteria>
Measurable completion — maps to the three ROADMAP Phase 70 success criteria:
1. SPEC.md specifies install.js git-SHA emit, `no-network` sentinel semantics (signals an invalid
   install, not an empty string), the update worker via the GitHub Commits API with `isNewer`
   SHA-equality, statusline display, the `check-latest-version.cjs` injectable seam, and the
   `{{GSD_REPO}}`/`{{GSD_BRANCH}}` template-placeholder boundary. [SPEC-02]
2. The "GitHub Commits API, not npmjs.com" and "SHA equality, not semver ordering" decisions are
   recorded as settled in Key Decisions with the consequence of reopening stated. [QUAL-05]
3. Invariants are ~6 numbered EARS statements citing the five version/semver/SHA test files as
   tier-1 sources; the Acceptance Tests traceability table is complete (no MISSING rows); current
   paths/symbols are advisory-marked; Status is advanced `Draft -> Ready`.
   [QUAL-01, QUAL-02, QUAL-03, QUAL-04]
</success_criteria>

<output>
Create `.planning/phases/70-spec-02-sha-versioning/70-01-SUMMARY.md` when done.
</output>
