---
phase: 52-parser-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
autonomous: true
requirements: [PARSE-01, PARSE-02]
must_haves:
  truths:
    - "parseModelEffort('opus;high') deep-equals {model:'opus', effort:'high'}"
    - "parseModelEffort('openrouter:anthropic/claude-opus') deep-equals {model:'openrouter:anthropic/claude-opus', effort:null} (colon never a delimiter)"
    - "parseModelEffort('opus') deep-equals {model:'opus', effort:null} (bare model, backward-compatible omit)"
    - "parseModelEffort('opus;hihg') deep-equals {model:'opus', effort:null} AND emits exactly one stderr warning per distinct label"
    - "parseModelEffort accepts every token in {low, medium, high, xhigh, max} as a valid effort suffix"
    - "D1: split on lastIndexOf(';'); the suffix is stripped only when it is an exact member of the effort allowlist"
    - "D2: the one-time warn lives inside parseModelEffort, Set-tracked, with a test-reset helper; a label with no ';' never warns"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Exported parseModelEffort(label) parser + module-level effort warn cache + test-reset helper"
      contains: "function parseModelEffort"
  key_links:
    - from: get-shit-done/bin/lib/core.cjs module.exports
      to: parseModelEffort
      via: "exports block addition"
      pattern: "parseModelEffort"
---

<objective>
Add the pure `parseModelEffort(label)` parser to `core.cjs` with a one-time malformed-suffix warning, and export it. This is the foundational parsing primitive both the model resolver (Plan 02) and the SDK mirror (Plan 03) consume.

Purpose: Establish the single correct `model;effort` splitting algorithm (per D1) so colons in provider IDs are never treated as delimiters and typos degrade gracefully with a warning (per D2).
Output: Exported `parseModelEffort` + `_warnedEffortLabels` Set + a test-reset helper in `get-shit-done/bin/lib/core.cjs`.
</objective>

<context>
@.planning/phases/52-parser-foundation/52-CONTEXT.md
@.planning/phases/52-parser-foundation/52-RESEARCH.md
@.planning/phases/52-parser-foundation/52-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and export parseModelEffort with one-time typo warning</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs lines 1151-1212 (the `// ─── Model alias resolution ───` section: `_warnedConfigKeys` Set at 1154, the warn-body try/catch at 1162-1170, and `_resetRuntimeWarningCacheForTests` at 1208-1212 — the EXACT warn-once pattern to mirror)
    - get-shit-done/bin/lib/core.cjs lines 1861-1909 (the `module.exports` block; add `parseModelEffort` and the effort warn-reset helper next to `resolveModelInternal`, `resolveReasoningEffortInternal`, `resolveTierEntry`, `_resetRuntimeWarningCacheForTests`)
    - .planning/phases/52-parser-foundation/52-RESEARCH.md lines 213-236 (canonical CJS algorithm)
    - .planning/phases/52-parser-foundation/52-PATTERNS.md lines 21-52 (placement + warn-once + reset-helper analog)
  </read_first>
  <behavior>
    - parseModelEffort('opus') → {model:'opus', effort:null} (no ';', no warn)
    - parseModelEffort('openrouter:anthropic/claude-opus') → {model:'openrouter:anthropic/claude-opus', effort:null} (colon untouched, no warn)
    - parseModelEffort('opus;high') → {model:'opus', effort:'high'}
    - parseModelEffort('opus;low'/'opus;medium'/'opus;xhigh'/'opus;max') → effort equals that token
    - parseModelEffort('opus;hihg') → {model:'opus', effort:null} AND writes exactly one stderr warning for that label across repeated calls
    - parseModelEffort(non-string, e.g. null) → {model: <input>, effort:null} (no throw)
    - Calling the effort warn-reset helper allows the same label to warn again
  </behavior>
  <action>
    Add a pure module-level `parseModelEffort(label)` function in the `// ─── Model alias resolution ───` section near `resolveTierEntry` (around line 1236), placed after the `_resetRuntimeWarningCacheForTests` helper. Define a module-level allowlist `EFFORT_TOKENS = new Set(['low','medium','high','xhigh','max'])` and a module-level `_warnedEffortLabels = new Set()`. Algorithm (per D1): if `label` is not a string, return `{model: label, effort: null}`; compute `idx = label.lastIndexOf(';')`; if `idx === -1` return `{model: label, effort: null}`; split into `base = label.slice(0, idx)` and `suffix = label.slice(idx + 1)`; if `EFFORT_TOKENS.has(suffix)` return `{model: base, effort: suffix}`; otherwise (per D2) this is an unambiguous typo — gate on `_warnedEffortLabels.has(label)`, add the label, and write a `gsd: warning — ` prefixed message naming the unknown suffix, the original label, and the allowed tokens to `process.stderr.write` wrapped in `try { } catch { /* stderr might be closed in some test harnesses */ }`, then return `{model: base, effort: null}`. Use `lastIndexOf(';')` — never greedy `split(';')`. Warnings are a side channel and never appear in the returned shape. Add a `_resetEffortWarningCacheForTests()` that clears `_warnedEffortLabels` (or, per RESEARCH A3, extend the existing `_resetRuntimeWarningCacheForTests` to also clear it — Claude's discretion; pick one and make it the exported reset path tests use). Add `parseModelEffort` and the effort warn-reset export name to the `module.exports` block at ~1880.
  </action>
  <verify>
    <automated>node -e "const c=require('./get-shit-done/bin/lib/core.cjs'); const a=require('assert'); a.deepStrictEqual(c.parseModelEffort('opus;high'),{model:'opus',effort:'high'}); a.deepStrictEqual(c.parseModelEffort('openrouter:anthropic/claude-opus'),{model:'openrouter:anthropic/claude-opus',effort:null}); a.deepStrictEqual(c.parseModelEffort('opus'),{model:'opus',effort:null}); a.deepStrictEqual(c.parseModelEffort('opus;hihg'),{model:'opus',effort:null}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `core.cjs` exports `parseModelEffort` (assert: `require('./get-shit-done/bin/lib/core.cjs').parseModelEffort` is a function)
    - `parseModelEffort('opus;high')` deep-equals `{model:'opus', effort:'high'}`
    - `parseModelEffort('openrouter:anthropic/claude-opus')` deep-equals `{model:'openrouter:anthropic/claude-opus', effort:null}`
    - `parseModelEffort('opus')` deep-equals `{model:'opus', effort:null}`
    - each of `low,medium,high,xhigh,max` as `opus;<token>` returns `effort:<token>`
    - `parseModelEffort('opus;hihg')` deep-equals `{model:'opus', effort:null}`
    - the inline node -e verify command above exits 0 (prints OK)
    - `command grep -n "function parseModelEffort" get-shit-done/bin/lib/core.cjs` returns exactly one match
    - `npm test 2>&1 | tail -5` shows no new failures vs baseline (existing core/model-profiles suites still green)
  </acceptance_criteria>
  <done>parseModelEffort is implemented per D1/D2, exported, and the four canonical cases plus all five effort tokens return the exact {model,effort} shapes; an effort warn-reset helper is exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/catalog JSON → parser | A `model;effort` slot string crosses from on-disk JSON into `parseModelEffort` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | effort suffix value | mitigate | Allowlist validation in `parseModelEffort` (V5): non-token suffixes are stripped + warned, never passed through (per D2) |
| T-52-SC | Tampering/Elevation | `;` in a model token reaching an unquoted shell | accept (structurally mitigated) | RESEARCH Shell-Safety Audit verdict SAFE: the combined token lives only in JSON; `resolveModelInternal` returns `.model` only, so `;` is parsed out before any `resolve-model | jq -r '.model'` serialization. The asserting regression (`.model` contains no `;`) lands in Plan 02. |
</threat_model>

<verification>
- `node -e` inline assertions (above) pass.
- `npm test` shows no new failures vs the pre-change baseline.
</verification>

<success_criteria>
- `parseModelEffort` exported and correct for all canonical + token cases.
- One-time typo warning fires once per label and is resettable for tests.
- No existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/52-parser-foundation/52-01-SUMMARY.md` when done.
</output>
