---
phase: 52-parser-foundation
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - tests/parse-model-effort.test.cjs
autonomous: true
requirements: [PARSE-03]
must_haves:
  truths:
    - "_resolveAgentSlot(cwd, agentType) returns the single raw tier/slot string both the model resolver and the (Phase 53) effort resolver read from (kills the #3023 divergence class)"
    - "resolveModelInternal returns byte-identical model strings for every existing bare config — proven by a pre-change golden snapshot across all MODEL_PROFILES agents and profiles quality/balanced/budget/inherit"
    - "the model_overrides.<agent> path runs parseModelEffort and returns .model, so a bare full ID with no ';' comes back verbatim"
    - "resolve-model JSON .model never contains a ';' (shell-safety regression — D1)"
    - "D3: resolveModelInternal calls _resolveAgentSlot then parseModelEffort(slot).model; the string return contract is preserved"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Exported _resolveAgentSlot helper + refactored resolveModelInternal consuming parseModelEffort"
      contains: "function _resolveAgentSlot"
    - path: tests/parse-model-effort.test.cjs
      provides: "Golden-snapshot behavior-preservation test + _resolveAgentSlot single-slot test + shell-safety regression"
      contains: "_resolveAgentSlot"
  key_links:
    - from: get-shit-done/bin/lib/core.cjs resolveModelInternal
      to: _resolveAgentSlot
      via: "tier-resolution extracted into shared helper"
      pattern: "_resolveAgentSlot\\("
    - from: get-shit-done/bin/lib/core.cjs resolveModelInternal override path
      to: parseModelEffort
      via: "parseModelEffort(override).model"
      pattern: "parseModelEffort\\("
---

<objective>
Extract the duplicated phase-type-tier lookup out of `resolveModelInternal` into a shared `_resolveAgentSlot(cwd, agentType)` helper that returns the raw slot string, and refactor `resolveModelInternal` to consume it plus `parseModelEffort` while preserving its exact string return contract. Lock the refactor behind a pre-change golden snapshot.

Purpose: Give the model resolver and the Phase 53 effort resolver a single tier-resolution path (per D3), structurally eliminating the #3023 model/effort divergence class (PARSE-03).
Output: Exported `_resolveAgentSlot` + refactored `resolveModelInternal` in `core.cjs`; a golden-snapshot + slot + shell-safety test suite in `tests/parse-model-effort.test.cjs`.
</objective>

<context>
@.planning/phases/52-parser-foundation/52-CONTEXT.md
@.planning/phases/52-parser-foundation/52-RESEARCH.md
@.planning/phases/52-parser-foundation/52-PATTERNS.md
@.planning/phases/52-parser-foundation/52-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the pre-change golden snapshot test BEFORE refactoring</name>
  <files>tests/parse-model-effort.test.cjs</files>
  <read_first>
    - tests/feat-3023-model-phase-types.test.cjs (resolver-test conventions: header `'use strict'` + `process.env.GSD_TEST_MODE='1'`, imports of `resolveModelInternal` from core.cjs and `MODEL_PROFILES`/`AGENT_TO_PHASE_TYPE` from model-profiles.cjs, `createTempDir` from helpers.cjs, writeConfig seeding `.planning/config.json`)
    - tests/helpers.cjs (the `createTempDir` utility signature)
    - get-shit-done/bin/lib/core.cjs lines 1267-1354 (current resolveModelInternal — every precedence branch that must stay byte-identical)
    - .planning/phases/52-parser-foundation/52-PATTERNS.md lines 130-157 (test analog + golden-snapshot guidance)
  </read_first>
  <action>
    Create `tests/parse-model-effort.test.cjs` (if Plan 03 has not already created it; if it exists, add a describe block — do not clobber). Import `resolveModelInternal` (and later `_resolveAgentSlot`, `parseModelEffort`) from `../get-shit-done/bin/lib/core.cjs`, and `MODEL_PROFILES`/`AGENT_TO_PHASE_TYPE` from `../get-shit-done/bin/lib/model-profiles.cjs`. Build a golden-snapshot describe block: for every agentType key in `MODEL_PROFILES`, and for each profile in `['quality','balanced','budget','inherit']`, write a temp `.planning/config.json` via `createTempDir`+writeConfig with `{model_profile: <profile>}`, capture `resolveModelInternal(cwd, agent)`. Also capture representative configs: `{resolve_model_ids:'omit'}`, `{runtime:'codex'}` (non-claude), and `{model_profile:'inherit', models:{execution:'opus'}}` (the #3030 case). Record all results into one object and assert each against a captured baseline using `assert.deepStrictEqual` on the whole map. Capture the baseline by running the test ONCE against the current unmodified core.cjs and freezing those expected values inline in the test (this is the pre-change snapshot — it must be frozen before Task 2 edits core.cjs). Use `assert.strictEqual`/`assert.deepStrictEqual` only — never `indexOf` truthiness or `includes` (per Pitfall 4).
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/parse-model-effort.test.cjs` exists and the golden-snapshot block passes GREEN against the CURRENT unmodified core.cjs (`node --test tests/parse-model-effort.test.cjs` exits 0)
    - the snapshot iterates every key of `MODEL_PROFILES` across profiles `quality,balanced,budget,inherit` plus the three representative configs (`resolve_model_ids:'omit'`, non-claude `runtime`, `{model_profile:'inherit',models:{execution:'opus'}}`)
    - assertions use `assert.deepStrictEqual`/`assert.strictEqual` — `command grep -nE "indexOf|\.includes\(" tests/parse-model-effort.test.cjs` returns no assertion-as-boolean usage
  </acceptance_criteria>
  <done>The frozen pre-change golden snapshot of resolveModelInternal output passes against the unmodified core.cjs, ready to detect any drift introduced by the Task 2 refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Extract _resolveAgentSlot and refactor resolveModelInternal to consume it + parseModelEffort</name>
  <files>get-shit-done/bin/lib/core.cjs, tests/parse-model-effort.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs lines 1267-1354 (resolveModelInternal — the block to extract is 1285-1307; steps 3-5 at 1309-1353 stay unchanged)
    - get-shit-done/bin/lib/core.cjs lines 1454-1504 (resolveReasoningEffortInternal — the divergent duplicate; note its VALID_TIERS is {opus,sonnet,haiku} and it short-circuits null on phaseTypeTier==='inherit'. Do NOT bake that opt-out into the shared helper — Phase 53 layers it on top)
    - get-shit-done/bin/lib/core.cjs line 9 (MODEL_PROFILES and AGENT_TO_PHASE_TYPE are imported from ./model-profiles.cjs — the extracted helper uses the already-imported symbols)
    - get-shit-done/bin/lib/core.cjs lines 1875-1899 (module.exports block — add _resolveAgentSlot)
    - .planning/phases/52-parser-foundation/52-PATTERNS.md lines 56-98 (extraction block + critical reconciliation subtlety + branches that must not drift)
  </read_first>
  <action>
    Add a module-level `_resolveAgentSlot(cwd, agentType)` near `resolveModelInternal`. It loads config, then computes the tier exactly as the current lines 1285-1307: `profile = String(config.model_profile||'balanced').toLowerCase()`, `agentModels = MODEL_PROFILES[agentType]`, `phaseType = AGENT_TO_PHASE_TYPE[agentType]`, `phaseTypeTier` from `config.models[phaseType]`, `VALID_TIERS = {opus,sonnet,haiku,inherit}` (KEEP `inherit` — this is the model-resolver's set, NOT the effort resolver's), and the `tier` ternary including the `profile==='inherit' ? 'inherit'` synthesis at 1305. Return that raw `tier`/slot value BEFORE alias-map expansion (per RESEARCH A1 — so a `;effort` suffix survives for Phase 53). Then refactor `resolveModelInternal`: (a) the override path becomes `if (override) return parseModelEffort(override).model;` (per D3 — bare full IDs with no ';' return verbatim); (b) replace lines 1285-1307 with `const tier = _resolveAgentSlot(cwd, agentType);` (or pass derived values — internal structure is Claude's discretion per RESEARCH line 24), re-deriving `profile`/`agentModels` locally as steps 3-5 still need them. Leave steps 3 (`_resolveRuntimeTier` #2517), 4 (`resolve_model_ids:'omit'`), and 5 (profile lookup + `MODEL_ALIAS_MAP` expansion) byte-for-byte unchanged. Export `_resolveAgentSlot` in the module.exports block at ~1880. Then add two test blocks to `tests/parse-model-effort.test.cjs`: (1) `_resolveAgentSlot` returns a single slot string for a representative agent under a known config; (2) a shell-safety regression asserting `resolveModelInternal(cwd, agent)` output contains no `;` for a config whose slot carries an effort suffix (seed `models.<phaseType>` is not used here since tiers are aliases; instead assert the override path: `model_overrides:{<agent>:'opus;high'}` resolves `.model` with no `;`). Re-run the golden snapshot — it MUST still pass byte-identical.
  </action>
  <verify>
    <automated>node --test tests/parse-model-effort.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -n "function _resolveAgentSlot" get-shit-done/bin/lib/core.cjs` returns exactly one match
    - `core.cjs` exports `_resolveAgentSlot` (assert: `require('./get-shit-done/bin/lib/core.cjs')._resolveAgentSlot` is a function)
    - the pre-change golden snapshot block from Task 1 still passes byte-identical after the refactor (`node --test tests/parse-model-effort.test.cjs` exits 0)
    - `node -e "const c=require('./get-shit-done/bin/lib/core.cjs'); const a=require('assert'); const {mkdtempSync,writeFileSync,mkdirSync}=require('fs'); const os=require('os'); const p=require('path'); const d=mkdtempSync(p.join(os.tmpdir(),'g')); mkdirSync(p.join(d,'.planning')); writeFileSync(p.join(d,'.planning','config.json'), JSON.stringify({model_overrides:{'gsd-executor':'opus;high'}})); const m=c.resolveModelInternal(d,'gsd-executor'); a.ok(!m.includes(';'),'model has ;'); console.log('OK')"` exits 0 (override .model carries no ';')
    - existing resolver suite stays green: `node --test tests/feat-3023-model-phase-types.test.cjs 2>&1 | tail -3` shows no failures
    - `npm test 2>&1 | tail -5` shows no new failures vs baseline
  </acceptance_criteria>
  <done>_resolveAgentSlot is extracted and exported, resolveModelInternal consumes it + parseModelEffort while returning byte-identical model strings (golden snapshot green), and the override .model carries no ';' (shell-safety regression green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/catalog JSON → resolver | A `model;effort` override/slot string crosses from JSON into `resolveModelInternal` |
| resolver `.model` → shell (`resolve-model \| jq -r '.model'`) | Resolved model string is interpolated into a shell variable in workflows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-SC | Tampering / Elevation | `;` in a model token reaching an unquoted shell | mitigate (structurally) | Per D1 + RESEARCH Shell-Safety Audit (verdict SAFE): `resolveModelInternal` returns `parseModelEffort(...).model` only, so the `;` is stripped before any `resolve-model \| jq -r '.model'` serialization. This plan adds the asserting regression: resolved `.model` contains no `;`. |
| T-52-DRIFT | Tampering | resolveModelInternal behavior drift during extraction | mitigate | Pre-change golden snapshot (Task 1) asserts byte-identical output across all agents/profiles/representative configs before the refactor lands |
</threat_model>

<verification>
- Pre-change golden snapshot passes both before and after the refactor (byte-identical).
- `node --test tests/feat-3023-model-phase-types.test.cjs` stays green.
- `npm test` shows no new failures vs the pre-change baseline.
- Shell-safety regression: resolved `.model` contains no `;`.
</verification>

<success_criteria>
- `_resolveAgentSlot` exported, returns the raw slot string, used by `resolveModelInternal`.
- `resolveModelInternal` preserves its exact string return contract (golden snapshot identical).
- The #3023 copy-paste divergence is removed at the model-resolver site.
</success_criteria>

<output>
Create `.planning/phases/52-parser-foundation/52-02-SUMMARY.md` when done.
</output>
